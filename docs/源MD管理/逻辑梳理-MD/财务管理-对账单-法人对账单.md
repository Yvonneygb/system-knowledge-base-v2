# 财务管理-对账单-法人对账单

## 业务流程

### 业务流程图

```text
法人对账单页面(hlod低代码页面 + 自定义React详情页)
        │
        ├── 查询列表 ──→ GET /cuxCustomerStateHeader/queryCuxCustomerStateHeaderPage
        │                  （按交易公司/法人/期间/对账单类型查询）
        │
        ├── 查看明细 ──→ GET /cuxCustomerStateHeader/queryCuxCustomerStateHeaderDetail
        │                  ├── 四类对账明细：货款(stateType=1)、未结算订单(stateType=2)、
        │                  │   费款(stateType=3)、保证金(stateType=4)
        │                  └── 数据来源：通过EBS SDK接口从EBS系统获取
        │
        ├── 状态更新 ──→ POST /cuxCustomerStateHeader/cuxCustomerStateHeaderStatusUpd
        │
        └── 导出 ──→ POST /cuxCustomerStateHeader/cuxCustomerStateHeaderDetailExport
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| EBS系统 | 数据依赖 | 对账数据实时从EBS系统获取 | EBS系统可用 |
| 客户主数据 | 数据依赖 | 提供经销商/法人基础信息 | 客户主数据已同步 |
| 交易公司主数据 | 数据依赖 | 提供交易公司/OU信息 | 交易公司已配置 |

### 下游影响

- 影响1：财务对账
  - 法人对账单为财务对账提供法人维度的往来数据核对依据

- 影响2：对账单打印
  - 对账单明细可导出打印，作为对账凭证

---

## 重点逻辑

### 重点逻辑1：EBS实时数据获取 {数据来源}

- **业务意义**：法人对账数据存储在EBS系统中，通过实时接口获取确保数据准确性

- **具体逻辑描述**

  - 第1点：对账单列表和明细数据通过EBS SDK接口从EBS系统实时获取

  - 第2点：不存储在DMS本地数据库，每次查询实时调用EBS接口

  - 第3点：数据包含期初余额、本期增加（借方）、本期减少（贷方）、期末余额

### 重点逻辑2：四类对账明细 {分类展示}

- **业务意义**：按业务类型分类展示对账明细，清晰呈现不同类型往来数据

- **具体逻辑描述**

  - 第1点：货款（stateType=1）：展示货款类往来明细

  - 第2点：未结算订单/发出商品（stateType=2）：展示未结算订单明细

  - 第3点：货款（stateType=3）：展示货款类调整明细

  - 第4点：保证金（stateType=4）：展示保证金类往来明细

### 重点逻辑3：多维度查询 {查询检索}

- **业务意义**：支持按交易公司、法人、期间、对账单类型等多维度查询

- **具体逻辑描述**

  - 第1点：支持按期间（periodName）、对账单类型（stateType）、OU名称（orgName）查询

  - 第2点：支持按客户编号（partyNumber）、事业部（businessDept）筛选

  - 第3点：支持按币种（currencyCode）区分查询

---

## 详细逻辑

### 界面模块1：法人对账单列表页（hlod低代码页面）

> 本页面为hlod低代码页面，无独立前端源码，基于后端Entity和API梳理。数据通过EBS SDK接口从EBS系统获取。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 期间 | CuxCustomerStateHeader.periodName | 文本框 | 会计期间 | 常显 | 手动选择 |
| 对账单类型 | CuxCustomerStateHeader.stateType | 下拉选择框 | 对账单类型 | 常显 | 来源快码 |
| OU名称 | CuxCustomerStateHeader.orgName | 文本框 | OU组织名称 | 常显 | 手动选择或带出 |
| 客户编号 | CuxCustomerStateHeader.partyNumber | 文本框 | 客户编号 | 常显 | 手动输入 |
| 事业部 | CuxCustomerStateHeader.businessDept | 文本框 | 事业部 | 常显 | 手动选择 |
| 币种 | CuxCustomerStateHeader.currencyCode | 文本框 | 币种 | 常显 | 手动选择 |
| 期初余额 | CuxCustomerStateHeader.beginBalance | 数字显示框 | 期初余额 | 常显 | EBS接口返回 |
| 本期增加(借方) | CuxCustomerStateHeader.debitAmount | 数字显示框 | 本期增加金额 | 常显 | EBS接口返回 |
| 本期减少(贷方) | CuxCustomerStateHeader.creditAmount | 数字显示框 | 本期减少金额 | 常显 | EBS接口返回 |
| 期末余额 | CuxCustomerStateHeader.endBalance | 数字显示框 | 期末余额 | 常显 | EBS接口返回 |
| 发送状态 | CuxCustomerStateHeader.sendFlag | 文本框 | 对账单发送状态 | 常显 | 系统更新 |

### 界面模块2：法人对账单明细页（自定义React页面）

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 对账主体 | - | 文本框 | 对账主体信息 | 常显 | 从列表带出 |
| 客户名称 | - | 文本框 | 客户名称 | 常显 | 从列表带出 |
| 会计期 | - | 文本框 | 会计期间 | 常显 | 从列表带出 |
| 币种信息 | - | 文本框 | 币种 | 常显 | 从列表带出 |
| 货款明细(stateType=1) | - | 表格 | 货款类往来明细 | 常显 | EBS接口返回 |
| 未结算订单明细(stateType=2) | - | 表格 | 未结算订单/发出商品明细 | 常显 | EBS接口返回 |
| 货款明细(stateType=3) | - | 表格 | 货款类调整明细 | 常显 | EBS接口返回 |
| 保证金明细(stateType=4) | - | 表格 | 保证金类往来明细 | 常显 | EBS接口返回 |

### 选择弹窗

> 本页面查询条件主要使用文本输入和下拉选择，无独立弹窗。

### 导入

> 本页面为查询页面，无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询法人对账单 | 列表页 | 始终可用 | 调用EBS接口获取数据 |
| 查看明细 | 查看对账单四类明细 | 列表页 | 选中一条记录 | 打开明细页展示四类对账明细 |
| 状态更新 | 更新对账单发送状态 | 列表页 | 选中记录 | 调用状态更新接口 |
| 导出 | 导出对账单明细 | 列表页/明细页 | 有数据时 | 导出Excel |

#### 按钮1：查询（列表页）

- **触发条件**：始终可用
- **执行逻辑**：
  - 第1点：按期间、对账单类型、OU、客户编号、事业部等条件查询
  - 第2点：通过EBS SDK接口实时获取对账数据
- **接口调用**：GET `/v1/{organizationId}/cuxCustomerStateHeader/queryCuxCustomerStateHeaderPage`
- **排查SQL**：

```sql
-- 数据从EBS系统获取，非本地SQL
-- EBS接口查询参数: periodName, stateType, orgName, partyNumber, businessDept
```

#### 按钮2：查看明细（列表页）

- **触发条件**：选中一条记录
- **执行逻辑**：
  - 第1点：查询选中对账单的四类明细数据
  - 第2点：分别展示货款、未结算订单、货款调整、保证金明细
- **接口调用**：GET `/v1/{organizationId}/cuxCustomerStateHeader/queryCuxCustomerStateHeaderDetail`
- **排查SQL**：

```sql
-- 明细数据从EBS系统获取，按stateType分类查询
-- stateType=1: 货款, stateType=2: 未结算订单, stateType=3: 货款调整, stateType=4: 保证金
```

#### 按钮3：状态更新（列表页）

- **触发条件**：选中记录
- **执行逻辑**：
  - 第1点：更新对账单发送状态
- **接口调用**：POST `/v1/{organizationId}/cuxCustomerStateHeader/cuxCustomerStateHeaderStatusUpd`
- **排查SQL**：

```sql
-- 更新EBS系统中的对账单发送状态
```

#### 按钮4：导出（列表页/明细页）

- **触发条件**：有数据时可用
- **执行逻辑**：
  - 第1点：将当前查询结果导出为Excel文件
- **接口调用**：POST `/v1/{organizationId}/cuxCustomerStateHeader/cuxCustomerStateHeaderDetailExport`
- **排查SQL**：同查询SQL

### 保存校验

> 本页面为查询页面，无保存操作。

### 提交校验

> 本页面为查询页面，无提交操作。

### 状态机

> 本页面为查询页面，无状态流转。对账单发送状态由状态更新按钮控制。

---

## 数据库表详解

> 法人对账单数据不存储在DMS本地数据库，通过EBS SDK接口从EBS系统实时获取。以下为后端Entity字段定义（无@Table注解，继承CrmBasicModel）。

### 表1：CuxCustomerStateHeader（法人对账单头 - EBS数据）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| STATE_ID | VARCHAR | 对账单ID | - | EBS系统生成 |
| PERIOD_NAME | VARCHAR | 会计期间 | 期间 | 手动选择 |
| STATE_TYPE | VARCHAR | 对账单类型 | 对账单类型 | 来源快码 |
| ORG_NAME | VARCHAR | OU名称 | OU名称 | 手动选择 |
| PARTY_NUMBER | VARCHAR | 客户编号 | 客户编号 | 手动输入 |
| BUSINESS_DEPT | VARCHAR | 事业部 | 事业部 | 手动选择 |
| CURRENCY_CODE | VARCHAR | 币种 | 币种 | 手动选择 |
| BEGIN_BALANCE | VARCHAR | 期初余额 | 期初余额 | EBS接口返回 |
| DEBIT_AMOUNT | VARCHAR | 本期增加(借方) | 本期增加 | EBS接口返回 |
| CREDIT_AMOUNT | VARCHAR | 本期减少(贷方) | 本期减少 | EBS接口返回 |
| END_BALANCE | VARCHAR | 期末余额 | 期末余额 | EBS接口返回 |
| SEND_FLAG | VARCHAR | 发送状态 | 发送状态 | 系统更新 |
| ADD_FEE_TRANSFER | VARCHAR | 本期增加-费用转货款 | - | EBS接口返回 |
| ADD_RECEIPT | VARCHAR | 本期增加-收款 | - | EBS接口返回 |
| REDUCEAMT_REFUND | VARCHAR | 本期减少-退款 | - | EBS接口返回 |
| REDUCEAMT_OTHERS | VARCHAR | 本期减少-其他扣项 | - | EBS接口返回 |
| REDUCEAMT_DELIVERY | VARCHAR | 本期减少-出货 | - | EBS接口返回 |
| ADD_DEPOSIT_TRANSFER | VARCHAR | 本期增加-保证金转货款 | - | EBS接口返回 |
| ADD_ADJUST_TRANSFER | VARCHAR | 本期增加-调整收款 | - | EBS接口返回 |
| STATE_ACCOUNT | VARCHAR | 会计科目 | - | EBS接口返回 |
| ORG_CODE | VARCHAR | OU编码 | - | EBS接口返回 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| EBS接口调用失败 | 查询时 | EBS系统不可用或网络异常，检查EBS系统状态 | 阻断性报错 | [查看] |
| 查询无数据 | 查询时 | 查询条件不匹配或EBS中无对应期间数据，放宽条件 | toast提醒 | [查看] |
| 请传入对账单类型 | 导出时 | 导出未传入stateType参数，前端补充对账单类型后重试 | toast提醒 | [查看] |
| 状态更新失败 | 状态更新时 | EBS接口返回失败或对账单状态不允许更新，确认状态后重试 | 阻断性报错 | [查看] |
| 网络请求失败 | 调用接口时 | 后端服务不可用或网络中断，稍后重试或联系运维 | toast提醒 | [查看] |
| 权限不足 | 查询/导出/状态更新时 | 当前用户无该组织或菜单访问权限，联系管理员分配权限 | toast提醒 | [查看] |

#### 报错1：EBS接口调用失败
- **触发条件**：用户点击查询或查看明细，后端通过EBS SDK接口实时获取数据时抛出连接异常或超时
- **逻辑分析**：法人对账单数据不存储在DMS本地数据库，每次查询实时调用EBS SDK接口（queryCuxCustomerStateHeaderPage/queryCuxCustomerStateHeaderDetail）。失败根因有三类：(1)EBS系统宕机或服务不可用；(2)DMS与EBS网络中断或SDK配置错误（地址/账号/密钥）；(3)EBS接口入参异常（periodName/stateType/orgName格式不符EBS要求但DMS未做转换）。此为阻断性报错，需联系EBS运维确认系统状态及SDK连接配置
- **排查SQL**：
  ```sql
  -- EBS数据非本地存储，以下为DMS侧EBS接口调用配置核查（具体表名以EBS适配层为准）
  -- 核查EBS接口配置是否存在及启用状态
  SELECT INTERFACE_CODE, INTERFACE_NAME, TARGET_URL, ENABLED, LAST_SYNC_TIME
  FROM EBS_INTERFACE_CONFIG
  WHERE INTERFACE_CODE IN ('CUX_CUSTOMER_STATE_HEADER', 'CUX_CUSTOMER_STATE_DETAIL');
  ```

#### 报错2：查询无数据
- **触发条件**：EBS接口调用成功但返回空结果集
- **逻辑分析**：EBS接口按periodName（会计期间）、stateType（对账单类型1-4）、orgName（OU名称）、partyNumber（客户编号）、businessDept（事业部）组合过滤。无数据根因有二：(1)查询条件过窄，如选了特定期间+特定客户但EBS中该客户在该期间无往来；(2)EBS中该期间尚未结账或数据未录入。需放宽条件（如去掉客户编号只按期间+OU查询）确认是数据缺失还是条件过滤问题
- **排查SQL**：
  ```sql
  -- 通过EBS接口核查指定期间+OU下是否有任意往来数据（stateType=1货款为例）
  -- 以下为EBS侧核查SQL（在EBS库执行）
  SELECT PERIOD_NAME, ORG_NAME, PARTY_NUMBER, BEGIN_BALANCE, DEBIT_AMOUNT, CREDIT_AMOUNT, END_BALANCE
  FROM CUX_CUSTOMER_STATE_HEADER_V
  WHERE PERIOD_NAME = #{periodName}
    AND (ORG_NAME = #{orgName} OR #{orgName} IS NULL)
    AND (STATE_TYPE = #{stateType} OR #{stateType} IS NULL)
  ORDER BY PARTY_NUMBER;
  ```

#### 报错3：请传入对账单类型
- **触发条件**：用户点击导出按钮但未传入对账单类型（stateType）参数
- **逻辑分析**：导出接口cuxCustomerStateHeaderDetailExport使用@ExcelExport动态导出，CuxCustomerStateHeader实体实现DynamicExportEntity接口的columns方法（CuxCustomerStateHeader.java:140）校验customData非空，若StringUtils.isBlank(customData)则抛出CommonException("请传入对账单类型")。customData中包含stateType字段用于区分四类导出列（1=货款、2=未结算订单、3=货款调整、4=保证金）。根因有二：(1)前端导出时未将stateType拼装到customData参数中；(2)customData JSON格式错误无法解析。需前端在导出请求中补充stateType参数
- **排查SQL**：
  ```sql
  -- 核查EBS侧四类对账单类型是否有数据（在EBS库执行）
  SELECT STATE_TYPE, COUNT(1) AS 记录数
  FROM CUX_CUSTOMER_STATE_HEADER_V
  WHERE PERIOD_NAME = #{periodName}
  GROUP BY STATE_TYPE
  ORDER BY STATE_TYPE;
  ```

#### 报错4：状态更新失败
- **触发条件**：用户点击"状态更新"按钮，EBS接口cuxCustomerStateHeaderStatusUpd返回失败
- **逻辑分析**：状态更新调用ArrowEbsSdkServiceImpl.cuxCustomerStateHeaderStatusUpd（ArrowEbsSdkServiceImpl.java:94），通过interfaceInvokeSdk.invoke调用EBS的CUX_CUSTOMER_UPDATE_STATE接口更新对账单发送状态（SEND_FLAG）。失败根因有三：(1)EBS接口返回returnStatus非S（业务校验失败，如对账单状态不允许更新）；(2)EBS接口入参List<CuxCustomerStateHeaderDto>格式不符EBS要求；(3)EBS系统内部异常（如数据库锁、约束冲突）。此为阻断性报错，需联系EBS运维确认接口返回的msgData错误信息
- **排查SQL**：
  ```sql
  -- 核查DMS侧EBS状态更新接口配置
  SELECT INTERFACE_CODE, INTERFACE_NAME, TARGET_URL, ENABLED, LAST_SYNC_TIME
  FROM EBS_INTERFACE_CONFIG
  WHERE INTERFACE_CODE = 'CUX_CUSTOMER_UPDATE_STATE';
  ```

#### 报错5：网络请求失败
- **触发条件**：用户点击查询、查看明细、状态更新或导出，前端axios请求抛出网络异常或超时
- **逻辑分析**：前端调用/v1/{organizationId}/cuxCustomerStateHeader/*系列接口时，因后端crm-business服务不可用、网关路由异常、网络中断或请求超时导致连接失败。根因有四：(1)crm-business微服务未注册到Nacos或已宕机；(2)EBS SDK调用超时（EBS系统响应慢或网络延迟）导致整体请求超时；(3)网络中断或防火墙拦截DMS与EBS的连接；(4)导出数据量大导致Excel生成超时。需联系运维确认crm-business和EBS系统状态
- **排查SQL**：
  ```sql
  -- 核查DMS侧EBS接口配置及启用状态
  SELECT INTERFACE_CODE, INTERFACE_NAME, TARGET_URL, ENABLED, LAST_SYNC_TIME
  FROM EBS_INTERFACE_CONFIG
  WHERE INTERFACE_CODE IN ('CUX_CUSTOMER_STATE_LIST_PAGE', 'CUX_CUSTOMER_STATE_DETAIL', 'CUX_CUSTOMER_UPDATE_STATE');
  ```

#### 报错6：权限不足
- **触发条件**：用户访问法人对账单页面或调用接口时，返回403或"无权限访问"提示
- **逻辑分析**：后端Controller使用@Permission(level = ResourceLevel.ORGANIZATION, permissionLogin = true)控制访问权限，要求用户登录且拥有当前组织（organizationId）的访问权限。根因有三：(1)用户未分配该菜单（hlod页面+自定义React详情页）的访问角色；(2)用户当前切换的组织不在其授权组织范围内；(3)用户数据权限未覆盖查询的交易公司/法人/事业部。需联系管理员分配菜单角色和数据权限
- **排查SQL**：
  ```sql
  -- 核查用户在当前组织下的角色分配（表名以HZERO IAM实际表为准）
  SELECT USER_ID, ROLE_ID, ORGANIZATION_ID
  FROM IAM_USER_ROLE
  WHERE USER_ID = #{userId} AND ORGANIZATION_ID = #{organizationId};
  ```

### 常见问题

- 问题1：对账数据与EBS不一致
  - 原因：EBS接口返回数据有延迟或缓存问题
  - 解决思路：确认EBS系统数据已更新，重新查询刷新

- 问题2：四类明细中某类无数据
  - 原因：该法人在该期间内无对应类型往来数据
  - 解决思路：检查EBS中该期间该法人的往来数据，确认stateType对应类型是否有数据

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充界面模块、数据库表详解 |
| 2025-10-28 | - | tzx | 初始创建CuxCustomerStateHeader实体 |
