# 产品中心-价目表-跨事业部产品销售清单

---

## 业务流程

### 业务流程图

```text
上游：跨事业部产品销售申请（审批通过） ──同步写入──→ LNK_CROSS_BU_SALES_LIST
                                                        │
定时任务（每天凌晨） ──检查到期记录──→ 失效过期记录 + 联动失效价目表
                                                        │
开始 → ★跨事业部产品销售清单查询★ → 设置查询条件 → 查询/导出 → 结束（仅查询，无增删改）
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 跨事业部产品销售申请 | 数据依赖 | 申请单审批通过后同步写入销售清单记录 | 申请单状态为APPROVED |
| 产品主档（LNK_PROD） | 数据依赖 | 查询时关联展示产品名称、型号、生命周期状态、品牌事业部 | 产品数据存在 |
| 事业部主档（LNK_ORG_EXT） | 数据依赖 | 展示品牌事业部名称 | 事业部数据存在 |
| 用户表（HZERO.IAM_USER） | 数据依赖 | 展示创建人、最后修改人姓名 | 用户数据存在 |
| 值集AE.ITEM_ORGANIZATION | 配置依赖 | 所需销售事业部、品牌事业部下拉选择 | 值集已维护 |
| 值集CRM.COMMON_STATUS | 配置依赖 | 状态（有效/失效）显示 | 值集已维护 |
| 值集CRM.PRODUCT_Z_STATE | 配置依赖 | 产品生命周期状态显示 | 值集已维护 |
| 定时任务调度平台 | 配置依赖 | 定时任务crossBuSalesListExpireJob每天执行到期失效 | 调度平台已配置任务 |

### 下游影响

- 影响1：价目表明细（LNK_PRICE_LIST_ITEM）
  - 定时任务失效过期销售清单记录时，联动更新对应事业部价目表中该产品有效价格明细行的有效结束时间

- 影响2：箭牌大客户事业部价目表
  - 定时任务失效箭牌卫浴事业部记录时，同步失效箭牌大客户事业部价目表对应产品

- 影响3：经营分析与对账
  - 导出销售清单数据供经营分析、跨事业部对账结算使用

---

## 重点逻辑

### 重点逻辑1：数据只读 `纯查询页面`

- **业务意义**：销售清单为审批通过后自动生成的正式记录，不允许人工修改，保证数据一致性

- **具体逻辑描述**

  - 第1点：页面仅提供查询和导出功能，无新增、编辑、删除按钮

  - 第2点：后端仅提供list和export两个接口，无增删改接口

### 重点逻辑2：数据来源 `审批同步写入`

- **业务意义**：销售清单数据由跨事业部产品销售申请审批通过后自动写入，非人工创建

- **具体逻辑描述**

  - 第1点：申请单OA审批通过或流程结束时，遍历申请单明细行写入销售清单

  - 第2点：同一产品同一事业部已存在有效记录时，先失效旧记录再写入新记录

  - 第3点：明细类型新增→清单状态valid(有效)，明细类型失效→清单状态invalid(失效)

### 重点逻辑3：定时任务到期失效 `自动过期`

- **业务意义**：销售清单有效结束日期到期后自动失效，同步终止价目表有效价格

- **具体逻辑描述**

  - 第1点：定时任务crossBuSalesListExpireJob每天凌晨执行

  - 第2点：查询状态=有效且有效结束日期≤今天的所有记录

  - 第3点：对每条记录联动更新对应事业部价目表中该产品有效价格明细行的有效结束时间为清单有效结束日期

  - 第4点：特殊规则——若失效事业部为箭牌卫浴事业部，同步失效箭牌大客户事业部价目表

  - 第5点：将销售清单记录状态置为失效

---

## 详细逻辑

### 界面模块1：跨事业部产品销售清单列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 所需销售事业部 | LNK_CROSS_BU_SALES_LIST.SALES_DEPT_CODE | 下拉选择框 | 产品跨事业部销售的目标事业部 | 常显 | 1. 默认值：无 2. 来源：值集AE.ITEM_ORGANIZATION 3. 可编辑：否 4. 查询支持精确匹配 |
| 产品编码 | LNK_CROSS_BU_SALES_LIST.PROD_CODE | 文本框 | 销售产品编码 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 查询支持模糊匹配 |
| 产品名称 | LNK_PROD.PROD_NAME | 文本框 | 产品名称 | 常显 | 1. 默认值：无 2. 来源：关联LNK_PROD查询 3. 可编辑：否 4. 查询支持模糊匹配 |
| 型号 | LNK_PROD.LH_PROD_MODEL | 文本框 | 产品型号 | 常显 | 1. 默认值：无 2. 来源：关联LNK_PROD查询 3. 可编辑：否 4. 查询支持模糊匹配 |
| 生命周期状态 | LNK_PROD.SM_STATE | 下拉选择框 | 产品生命周期状态 | 常显 | 1. 默认值：无 2. 来源：值集CRM.PRODUCT_Z_STATE 3. 可编辑：否 4. 查询支持精确匹配 |
| 所属品牌事业部 | LNK_PROD.DEPT_ID | 下拉选择框 | 产品所属品牌事业部 | 常显 | 1. 默认值：无 2. 来源：值集CRM.ORG 3. 可编辑：否 4. 查询支持精确匹配 |
| 状态 | LNK_CROSS_BU_SALES_LIST.STATUS | 下拉选择框 | 清单记录状态 | 常显 | 1. 默认值：无 2. 来源：值集CRM.COMMON_STATUS（valid=有效/invalid=失效） 3. 可编辑：否 4. 查询支持精确匹配 |
| 有效开始时间 | LNK_CROSS_BU_SALES_LIST.EFF_START_DATE | 日期选择框 | 跨事业部销售有效起始日期 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 格式YYYY-MM-DD |
| 有效结束时间 | LNK_CROSS_BU_SALES_LIST.EFF_END_DATE | 日期选择框 | 跨事业部销售有效截止日期 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 格式YYYY-MM-DD 4. 查询为范围查询 |
| 关联申请单号 | LNK_CROSS_BU_SALES_LIST.FORM_CODE | 文本框 | 生成该清单记录的申请单号 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 查询支持模糊匹配 |
| 创建人 | HZERO.IAM_USER.REAL_NAME | 文本框 | 清单记录创建人姓名 | 常显 | 1. 默认值：无 2. 来源：关联IAM_USER查询（取申请单创建人） 3. 可编辑：否 4. 查询支持模糊匹配 |
| 创建时间 | LNK_CROSS_BU_SALES_LIST.CREATION_DATE | 日期选择框 | 清单记录创建时间 | 常显 | 1. 默认值：查询默认近一个月 2. 可编辑：否 3. 格式YYYY-MM-DD 4. 查询为范围查询 |
| 最后修改时间 | LNK_CROSS_BU_SALES_LIST.LAST_UPDATE_DATE | 日期选择框 | 最后更新时间 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 格式YYYY-MM-DD |

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 导出 | 导出销售清单列表 | 列表页 | 常显（需权限hzero.c.crm.cross-bu-sales-list.button.export） | 调用导出接口，异步导出Excel |

#### 按钮1：导出（列表页）

- **触发条件**：常显
- **执行逻辑**：
  - 第1点：使用当前查询条件作为导出参数
  - 第2点：异步导出，导出数据与列表查询数据范围一致
  - 第3点：导出字段包含申请单号、事业部、产品编码、产品名称、型号、品牌事业部、类型、有效时间、备注、创建人、创建时间
- **接口调用**：GET /v1/{orgId}/crossBuSalesList/export

---

## 数据库表详解

### 表1：LNK_CROSS_BU_SALES_LIST（跨事业部产品销售清单表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ID | NUMBER | 主键 | 无（隐藏） | 序列lnk_cross_bu_sales_list_s自增生成 |
| SALES_DEPT_CODE | VARCHAR | 所需销售事业部编码 | 所需销售事业部 | 取申请单头DEPT_CODE |
| PROD_CODE | VARCHAR | 产品编码 | 产品编码 | 取申请单明细PROD_CODE |
| STATUS | VARCHAR | 状态 | 状态 | 值集CRM.COMMON_STATUS：valid(有效)/invalid(失效)。新增类型→valid，失效类型→invalid。定时任务到期自动置为invalid |
| EFF_START_DATE | DATE | 有效开始时间 | 有效开始时间 | 取申请单明细EFF_START_DATE |
| EFF_END_DATE | DATE | 有效结束时间 | 有效结束时间 | 取申请单明细EFF_END_DATE。到期后定时任务联动失效价目表 |
| FORM_CODE | VARCHAR | 关联申请单号 | 关联申请单号 | 取申请单头FORM_CODE |
| OBJECT_VERSION_NUMBER | NUMBER | 乐观锁版本号 | 无 | 框架自动维护，初始值1 |
| CREATED_BY | NUMBER | 创建人ID | 无 | 取申请单审计字段（非当前操作人） |
| CREATION_DATE | TIMESTAMP | 创建时间 | 创建时间 | 取申请单审计字段 |
| LAST_UPDATED_BY | NUMBER | 最后更新人ID | 无 | 取申请单审计字段；定时任务失效时由框架更新 |
| LAST_UPDATE_DATE | TIMESTAMP | 最后更新时间 | 最后修改时间 | 取申请单审计字段；定时任务失效时更新为sysdate |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询失败，请稍后重试 | 列表查询 | 后端接口异常或数据库连接超时。检查后端服务状态及网络连接 | toast提醒 | [查看] |
| 权限不足，无法访问 | 页面加载 | 当前用户未分配hzero.c.crm.cross-bu-sales-list相关权限。联系管理员分配权限 | 阻断性报错 | [查看] |
| 登录已过期，请重新登录 | 页面操作 | 用户会话过期或Token失效。重新登录系统 | 阻断性报错 | [查看] |
| 暂无数据 | 列表展示 | 查询条件无匹配数据或销售清单未同步写入。调整查询条件或检查上游申请单审批状态 | toast提醒 | [查看] |
| 导出失败，请稍后重试 | 导出按钮 | 导出接口异常或导出数据量超限。缩小查询范围后重试 | toast提醒 | [查看] |

#### 报错1：查询失败，请稍后重试
- **触发条件**：列表页点击"查询"按钮时，后端接口`GET /v1/{orgId}/crossBuSalesList`返回非200状态码或响应超时
- **逻辑分析**：前端DataSet transport.read发起GET请求至CRM服务`crossBuSalesList`接口，后端`LnkCrossBuSalesListServiceImpl.selectList`通过`PageHelper.doPageAndSort`分页查询`LnkCrossBuSalesListMapper.selectList`。当数据库连接异常、SQL执行超时或服务不可用时，HZERO框架捕获异常并返回错误响应，前端axios拦截器统一提示"查询失败"。
- **排查SQL**：
  ```sql
  -- 检查销售清单表是否可正常访问及数据量
  SELECT COUNT(1) AS 销售清单总记录数
  FROM LNK_CROSS_BU_SALES_LIST;
  ```

#### 报错2：权限不足，无法访问
- **触发条件**：用户访问跨事业部产品销售清单菜单或点击导出按钮时，当前角色未配置对应权限编码
- **逻辑分析**：前端页面通过`PERMISSION_PREFIX=hzero.c.crm.cross-bu-sales-list`配置权限，导出按钮权限编码为`hzero.c.crm.cross-bu-sales-list.button.export`。HZERO权限拦截器校验用户角色权限，未配置时返回403，前端路由守卫或按钮权限指令拦截访问。
- **排查SQL**：
  ```sql
  -- 查询当前用户角色是否分配了销售清单菜单权限
  SELECT U.REAL_NAME AS 用户名, R.NAME AS 角色名, P.CODE AS 权限编码, P.TYPE AS 权限类型
  FROM HZERO.IAM_USER U
  JOIN HZERO.IAM_MEMBER M ON M.MEMBER_ID = U.ID
  JOIN HZERO.IAM_ROLE R ON R.ID = M.ROLE_ID
  JOIN HZERO.IAM_ROLE_PERMISSION RP ON RP.ROLE_ID = R.ID
  JOIN HZERO.IAM_PERMISSION P ON P.ID = RP.PERMISSION_ID
  WHERE U.REAL_NAME = :用户名
    AND P.CODE LIKE 'hzero.c.crm.cross-bu-sales-list%';
  ```

#### 报错3：登录已过期，请重新登录
- **触发条件**：用户在页面操作时，HZERO Token过期或被踢出登录
- **逻辑分析**：前端axios请求携带Authorization Token访问后端接口，后端网关校验Token有效性。Token过期时返回401状态码，前端axios响应拦截器捕获401并跳转登录页。常见于长时间未操作或单点登录会话超时。
- **排查SQL**：
  ```sql
  -- 查询用户最近登录会话状态（HZERO OAuth Token表）
  SELECT U.REAL_NAME AS 用户名, T.TOKEN_VALUE AS 令牌, T.EXPIRES_IN AS 有效期秒,
         T.CREATE_TIME AS 创建时间
  FROM HZERO.OAUTH_ACCESS_TOKEN T
  JOIN HZERO.IAM_USER U ON U.ID = T.USER_ID
  WHERE U.REAL_NAME = :用户名
  ORDER BY T.CREATE_TIME DESC;
  ```

#### 报错4：暂无数据
- **触发条件**：列表页查询返回空列表，前端DataSet无数据渲染
- **逻辑分析**：前端`crossBuSalesList`接口返回`content`为空数组，`totalElements`为0。根因可能为：1）查询条件过严无匹配数据；2）上游跨事业部产品销售申请单未审批通过，`syncFromApprovedForm`未写入销售清单；3）定时任务已将所有记录置为失效。前端FilterTableCom展示"暂无数据"占位。
- **排查SQL**：
  ```sql
  -- 检查是否有审批通过的申请单及其明细行数、销售清单同步数
  SELECT F.FORM_CODE AS 申请单号, F.STATUS AS 申请单状态,
         (SELECT COUNT(1) FROM LNK_CROSS_BU_APP_FORM_ITEM I WHERE I.HEAD_ID = F.ID) AS 明细行数,
         (SELECT COUNT(1) FROM LNK_CROSS_BU_SALES_LIST S WHERE S.FORM_CODE = F.FORM_CODE) AS 清单同步数
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.STATUS = 'APPROVED'
  ORDER BY F.CREATION_DATE DESC;
  ```

#### 报错5：导出失败，请稍后重试
- **触发条件**：点击"导出"按钮时，导出接口`GET /v1/{orgId}/crossBuSalesList/export`返回异常或导出文件生成失败
- **逻辑分析**：前端`ExcelExportPro`组件调用导出接口，后端`LnkCrossBuSalesListController.export`通过`@ExcelExport`注解导出`LnkCrossBuSalesListExport`类型数据。`exportList`方法先分页查询再通过`PageUtil.convert`转换为导出VO。失败原因可能为：1）查询数据量过大导致内存溢出；2）`@ProcessLovValue`值集翻译异常；3）Excel生成组件异常。前端异步导出，失败时notification.error提示。
- **排查SQL**：
  ```sql
  -- 检查当前查询条件下数据量是否过大
  SELECT COUNT(1) AS 待导出记录数,
         MIN(S.CREATION_DATE) AS 最早记录时间,
         MAX(S.CREATION_DATE) AS 最晚记录时间
  FROM LNK_CROSS_BU_SALES_LIST S
  WHERE S.STATUS = 'valid'
    AND TRUNC(S.CREATION_DATE) BETWEEN TRUNC(SYSDATE) - 30 AND TRUNC(SYSDATE);
  ```

### 常见问题

- 问题1：销售清单中查不到审批通过的申请单数据
  - 原因：申请单审批回调未成功执行或明细行为空。排查SQL：

    ```sql
    SELECT f.FORM_CODE, f.STATUS, 
           (SELECT COUNT(1) FROM LNK_CROSS_BU_APP_FORM_ITEM i WHERE i.HEAD_ID = f.ID) AS item_count,
           (SELECT COUNT(1) FROM LNK_CROSS_BU_SALES_LIST s WHERE s.FORM_CODE = f.FORM_CODE) AS sales_count
    FROM LNK_CROSS_BU_APP_FORM f
    WHERE f.STATUS = 'APPROVED';
    ```

  - 解决思路：确认申请单状态为APPROVED且明细行不为空，检查OA审批回调日志

- 问题2：销售清单状态为有效但产品在价目表中查不到有效价格
  - 原因：定时任务已将价目表有效价格明细失效但销售清单记录尚未更新，或价目表本身无有效价格。排查SQL：

    ```sql
    SELECT s.PROD_CODE, s.SALES_DEPT_CODE, s.STATUS, s.EFF_END_DATE,
           (SELECT COUNT(1) FROM LNK_PRICE_LIST_ITEM pl 
            WHERE pl.PROD_CODE = s.PROD_CODE 
              AND pl.EFF_END_DATE >= TRUNC(SYSDATE)) AS valid_price_count
    FROM LNK_CROSS_BU_SALES_LIST s
    WHERE s.STATUS = 'valid';
    ```

  - 解决思路：检查定时任务执行日志，确认价目表是否有对应产品的有效价格

- 问题3：定时任务未自动失效过期记录
  - 原因：定时任务crossBuSalesListExpireJob未在调度平台配置或执行异常。排查SQL：

    ```sql
    SELECT ID, PROD_CODE, SALES_DEPT_CODE, STATUS, EFF_END_DATE
    FROM LNK_CROSS_BU_SALES_LIST
    WHERE STATUS = 'valid'
      AND TRUNC(EFF_END_DATE) <= TRUNC(SYSDATE);
    ```

  - 解决思路：检查HZERO调度平台任务配置，确认任务crossBuSalesListExpireJob已启用且每天执行

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-29 | - | - | 按skill规范完整重写，基于前后端代码梳理 |
| 2026-07-18 | - | - | 新增定时任务crossBuSalesListExpireJob：到期自动失效 |
| 2026-06-06 | - | - | 初始创建：跨事业部产品销售清单功能开发 |
