# 门店管理-门店装修-额度内兑现批量复核

## 业务流程

```
额度内兑现批量复核列表页(hlod低代码页面)
        │
        ├── 新增 ──→ 选择多张额度内兑现单 ──→ 创建批量复核单
        │                                    │
        │                                    ▼
        │                              生成批量复核单号 ──→ HZ_APPROVE_STATUS=NEW
        │                                    │
        │                                    ▼
        │                              提交审批 ──→ 启动OA工作流(STORE_FIN_FEE_IN_CASH_HEAD)
        │                                    │
        │                                    ▼
        │                              OA审批流程中(多节点复核)
        │                                    │
        │                         ├──审批通过──→ HZ_APPROVE_STATUS=APPROVED
        │                         │              ├─ 批量回写兑现单审批状态
        │                         │              ├─ 批量同步资金池(EBS)
        │                         │              └─ 批量推送MBO
        │                         │
        │                         └──审批驳回──→ HZ_APPROVE_STATUS=REJECTED
        │
        └── 删除 ──→ 仅NEW状态可删除
```

## 重点逻辑

### 重点逻辑1：批量关联兑现单

- **业务意义**：将多张额度内兑现单通过batchCashId统一关联，批量提交审批
- **具体逻辑描述**：
  - 创建批量复核单FIN_FEE_IN_CASH_HEAD
  - 多张FIN_FEE_TERMINAL_CASHOUT通过batchCashId关联到批量复核单
  - 批量提交后统一走审批流程

### 重点逻辑2：批量审批通过处理

- **业务意义**：审批通过后批量处理所有关联的兑现单
- **具体逻辑描述**：
  - 批量回写兑现单审批状态为APPROVED
  - 批量同步资金池(EBS)
  - 批量推送MBO系统
  - 批量回写验收报销单兑现信息

## 详细逻辑

### 界面模块

本页面为hlod低代码页面，前端尚无独立React组件。后端Controller(FinFeeInCashHeadController)提供4个API。

#### 头部信息区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 批量复核单号 | CASH_CODE | TextField | 批量复核单号 | 始终 | 编码规则生成 |
| 申请人 | CREATOR_NAME | TextField | 申请人 | 始终 | 默认当前用户 |
| 申请日期 | CREATE_TIME | DatePicker | 申请日期 | 始终 | 默认当前时间 |
| 审核状态 | HZ_APPROVE_STATUS | Select(HWKF.APPROVE_STATUS) | 审核状态 | 始终 | 默认NEW |
| 审核人 | CHECKER | TextField | 审核人 | 始终 | 审批通过时赋值 |
| 审核时间 | CHECK_TIME | DatePicker | 审核时间 | 始终 | 审批通过时赋值 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表查询 | GET | `/v1/{organizationId}/fin-fee-in-cash-heads` | 查询批量复核列表 |
| 详情查询 | GET | `/v1/{organizationId}/fin-fee-in-cash-heads/{cashId}/detail` | 查询批量复核详情(含关联兑现单) |
| 保存 | POST | `/v1/{organizationId}/fin-fee-in-cash-heads` | 创建或更新批量复核 |
| 删除 | DELETE | `/v1/{organizationId}/fin-fee-in-cash-heads` | 删除批量复核(仅NEW状态) |

### 选择弹窗

| 弹窗名称 | LOV编码 | 参数 | 说明 |
|----------|---------|------|------|
| 额度内兑现单 | - | - | 选择多张NEW状态的额度内兑现单(FIN_FEE_TERMINAL_CASHOUT) |

### 导入

本页面无导入功能。

### 其他按钮

| 按钮名称 | 触发条件 | 执行逻辑 | 接口调用 |
|----------|---------|---------|---------|
| 删除 | HZ_APPROVE_STATUS为NEW | 删除批量复核单，解除兑现单关联 | DELETE |

### 保存校验

- 校验1：至少选择一张兑现单 —— 确保批量复核有关联数据
  - **详细逻辑**：前端校验已选择兑现单数量>0
  - **系统体现**：前端校验
  - **排查SQL**：`SELECT COUNT(*) FROM FIN_FEE_TERMINAL_CASHOUT WHERE BATCH_CASH_ID={cashId}`

### 提交校验

- 校验1：关联兑现单状态校验 —— 确保所有关联兑现单为NEW状态
  - **详细逻辑**：提交时校验所有关联兑现单hzApproveStatus=NEW
  - **系统体现**：后端校验
  - **排查SQL**：`SELECT * FROM FIN_FEE_TERMINAL_CASHOUT WHERE BATCH_CASH_ID={cashId} AND HZ_APPROVE_STATUS!='NEW'`

### 状态机

```text
NEW(新建) ──提交审批──→ RUN(审批中) ──┬──审批通过──→ APPROVED(已审批)
                                       │              ├─ 批量回写兑现单APPROVED
                                       │              ├─ 批量同步资金池
                                       │              └─ 批量推送MBO
                                       │
                                       └──审批驳回──→ REJECTED(已驳回)

NEW ──删除──→ (删除)
```

### 工作流

- **工作流编码**：`STORE_FIN_FEE_IN_CASH_HEAD`（MCS_AW额度内批量复核）
- **编码规则**：批量复核单号由系统生成

## 数据库表详解

### FIN_FEE_IN_CASH_HEAD（额度内兑现批量复核表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| CASH_ID | Long | 主键ID | - | 自增 |
| CASH_CODE | String | 批量复核单号 | 批量复核单号 | 编码规则生成 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |
| CREATOR | String | 申请人 | - | 系统赋值 |
| CREATE_TIME | Date | 申请日期 | 申请日期 | 系统赋值 |
| CREATOR_NAME | String | 申请人名称 | 申请人 | 系统赋值 |
| UPDATOR | String | 更新人 | - | 系统赋值 |
| UPDATE_TIME | Date | 更新日期 | - | 系统赋值 |
| UPDATOR_NAME | String | 更新人名称 | - | 系统赋值 |
| STAT | Long | 单据状态 | - | 系统维护 |
| WFID | Long | 流程ID | - | 系统维护 |
| WFFLAG | Long | 流程状态 | - | 系统维护 |
| CHECKER | String | 审核人 | 审核人 | 审批通过时赋值 |
| CHECK_TIME | Date | 审核时间 | 审核时间 | 审批通过时赋值 |
| AUDIT_STAT | String | 审核状态 | - | 系统维护 |
| HZ_INSTANCE_ID | Long | 流程实例ID | - | 工作流启动后赋值 |
| HZ_APPROVE_STATUS | String | 流程审批状态 | 审核状态 | NEW/RUN/APPROVED/REJECTED |

### FIN_FEE_TERMINAL_CASHOUT（额度内门店装修兑现表，关联表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| TERMINAL_CASHOUT_ID | Long | 兑现ID | - | 自增 |
| CASH_ID | Long | 批量复核ID | - | 关联FIN_FEE_IN_CASH_HEAD |
| TERMINAL_CASHOUT_CODE | String | 兑现单号 | - | 编码规则生成 |
| CHECK_BX_ID | Long | 验收报销单ID | - | 关联验收报销单 |
| IN_THIS_CASHOUT_AMT | BigDecimal | 额度内本次兑现金额 | - | 用户输入 |
| HZ_APPROVE_STATUS | String | 流程审批状态 | - | 随批量复核单状态联动 |

## 常见问题FAQ

### Q1：提交时报关联兑现单状态异常

**根因**：关联的兑现单中存在非NEW状态的记录
**解决方案**：确保所有关联兑现单均为NEW状态后再提交

### Q2：删除时报状态不允许

**根因**：批量复核单状态非NEW
**解决方案**：仅NEW状态可删除

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 额度内门店装修兑现(FIN_FEE_TERMINAL_CASHOUT) | 数据依赖 | 选择多张NEW状态的兑现单关联到批量复核 | 存在NEW状态的兑现单 |

## 下游影响

- **额度内门店装修兑现(FIN_FEE_TERMINAL_CASHOUT)** — 审批通过后批量回写兑现单状态为APPROVED
- **资金池(EBS)** — 审批通过后批量同步资金池
- **MBO系统** — 审批通过后批量推送MBO数据
- **门店验收与报销单(FIN_FEE_CHECK_BX_HEADER)** — 审批通过后批量回写验收报销单兑现信息

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 不能删除非制单状态的单据 | doDelete | hzApproveStatus≠NEW | 中 | [查看](#报错1不能删除非制单状态的单据) |
| 关联兑现单状态异常 | 提交时 | 关联兑现单中存在非NEW状态 | 中 | [查看](#报错2关联兑现单状态异常) |
| 批量复核单不存在 | doSelect/doDelete | cashId对应记录不存在 | 高 | [查看](#报错3批量复核单不存在) |
| 单据信息不存在 | doSelect/doDelete/onWfComplete/onUserSubmit | cashId对应FIN_FEE_IN_CASH_HEAD记录不存在 | 高 | [查看](#报错4单据信息不存在) |
| 验收报销单号和门头兑现单号为空 | doSendShare | 关联兑现单checkBxCode与dhCashoutNo均为空 | 中 | [查看](#报错5验收报销单号和门头兑现单号为空) |
| 验收单号：{bxCodes}，总账日期不能为空，请检查 | onUserSubmit | 关联兑现单存在ledgerDate为空的记录 | 中 | [查看](#报错6验收单号bxcodes总账日期不能为空请检查) |

#### 报错1：不能删除非制单状态的单据
- **触发条件**：执行doDelete(删除)接口时，校验批量复核单HZ_APPROVE_STATUS不等于WorkflowInstanceStatus.NEW.getCode()（即非"新建"状态）
- **逻辑分析**：删除操作仅允许在NEW(新建)状态执行，避免删除已进入审批流程(RUN)或已审批生效(APPROVED/REJECTED)的批量复核单，防止关联的兑现单状态不一致及资金池/MBO推送数据错乱。后端通过WorkflowInstanceStatus.NEW.getCode().equals(hzApproveStatus)校验，若状态非NEW则抛出阻断性异常，删除被拒绝。
- **排查SQL**：
  ```sql
  SELECT cash_id            AS 批量复核单ID,
         cash_code          AS 批量复核单号,
         hz_approve_status  AS 审批状态,
         create_time        AS 创建时间
  FROM   fin_fee_in_cash_head
  WHERE  hz_approve_status <> 'NEW'
  ORDER  BY create_time DESC;
  ```

#### 报错2：关联兑现单状态异常
- **触发条件**：执行submit(提交)接口时，按batchCashId批量查询FIN_FEE_TERMINAL_CASHOUT，发现存在HZ_APPROVE_STATUS不等于NEW的关联兑现单
- **逻辑分析**：批量复核单提交审批前，需确保所有关联的额度内兑现单均为NEW(新建)状态，因为审批通过后会批量回写兑现单为APPROVED并同步资金池/MBO。若某张兑现单已被其他批量复核单关联并提交(RUN)、已审批(APPROVED)或已驳回(REJECTED)，再次纳入批量提交会造成同一兑现单被重复审批、重复推送资金池，导致资金双倍兑现。后端批量校验所有关联兑现单状态，存在非NEW即抛异常。
- **排查SQL**：
  ```sql
  SELECT t.terminal_cashout_id   AS 兑现单ID,
         t.terminal_cashout_code AS 兑现单号,
         t.cash_id               AS 批量复核单ID,
         t.hz_approve_status     AS 兑现单审批状态,
         h.cash_code             AS 批量复核单号,
         h.hz_approve_status     AS 批量复核单状态
  FROM   fin_fee_terminal_cashout t
  INNER JOIN fin_fee_in_cash_head h
  ON     t.cash_id = h.cash_id
  WHERE  h.hz_approve_status = 'NEW'
  AND    t.hz_approve_status <> 'NEW'
  ORDER  BY h.create_time DESC;
  ```

#### 报错3：批量复核单不存在
- **触发条件**：执行doSelect(详情查询)/doDelete(删除)接口时，按cashId调用selectByPrimaryKey查询FIN_FEE_IN_CASH_HEAD返回null
- **逻辑分析**：详情查询和删除均需先按主键CASH_ID定位批量复核单。若单据在操作前被其他用户物理删除，或前端传入的cashId为错误值/过期值（如列表缓存后他人已删除），selectByPrimaryKey返回null，后端抛出"批量复核单不存在"阻断性异常，后续读取HZ_APPROVE_STATUS、关联兑现单等逻辑均无法进行。
- **排查SQL**：
  ```sql
  SELECT cash_id            AS 批量复核单ID,
         cash_code          AS 批量复核单号,
         hz_approve_status  AS 审批状态,
         create_time        AS 创建时间
  FROM   fin_fee_in_cash_head
  WHERE  cash_id = #{传入的cashId};
  ```

#### 报错4：单据信息不存在
- **触发条件**：执行doSelect(详情查询)/doDelete(删除)/onWfComplete(审批通过回调)/onUserSubmit(提交)接口时，finFeeInCashHeadRepository.selectByPrimaryKey按cashId查询FIN_FEE_IN_CASH_HEAD返回null
- **逻辑分析**：详情查询、删除、审批通过回调和提交均需先按主键CASH_ID定位批量复核单。若单据在操作前被其他用户物理删除，或前端传入的cashId为错误值/过期值（如列表缓存后他人已删除），selectByPrimaryKey返回null，后端抛出"单据信息不存在"阻断性异常，后续读取HZ_APPROVE_STATUS、关联兑现单等逻辑均无法进行。
- **排查SQL**：
  ```sql
  SELECT cash_id            AS 批量复核单ID,
         cash_code          AS 批量复核单号,
         hz_approve_status  AS 审批状态,
         create_time        AS 创建时间
  FROM   fin_fee_in_cash_head
  WHERE  cash_id = #{传入的cashId};
  ```

#### 报错5：验收报销单号和门头兑现单号为空
- **触发条件**：执行doSendShare(推送共享)接口时，遍历关联兑现单FIN_FEE_TERMINAL_CASHOUT，某条记录的checkBxCode(验收报销单号)与dhCashoutNo(门头兑现单号)均为空字符串
- **逻辑分析**：推送共享需根据验收报销单号或门头兑现单号查询对应的支付方式(payType)，决定推送资金池还是共享系统。若关联兑现单未维护checkBxCode和dhCashoutNo（如兑现单未关联验收报销单或门头兑现单即被纳入批量复核），无法定位支付方式，推送逻辑无法继续，抛出阻断性异常。需核查兑现单关联数据完整性。
- **排查SQL**：
  ```sql
  SELECT t.terminal_cashout_id   AS 兑现单ID,
         t.terminal_cashout_code AS 兑现单号,
         t.cash_id               AS 批量复核单ID,
         t.check_bx_code         AS 验收报销单号,
         t.dh_cashout_no         AS 门头兑现单号,
         h.cash_code             AS 批量复核单号
  FROM   fin_fee_terminal_cashout t
  INNER JOIN fin_fee_in_cash_head h
  ON     t.cash_id = h.cash_id
  WHERE  h.hz_approve_status IN ('RUN','APPROVED')
  AND    NVL(t.check_bx_code, '') = ''
  AND    NVL(t.dh_cashout_no, '') = ''
  ORDER  BY h.create_time DESC;
  ```

#### 报错6：验收单号：{bxCodes}，总账日期不能为空，请检查
- **触发条件**：执行onUserSubmit(提交)接口时，查询关联兑现单FIN_FEE_TERMINAL_CASHOUT，过滤出ledgerDate(总账日期)为空的记录，收集其checkBxCode拼接为bxCodes
- **逻辑分析**：批量复核单提交审批前需确保所有关联兑现单已维护总账日期(ledgerDate)，因为审批通过后会推送共享/资金池，推送需以总账日期作为记账日期。若某张兑现单未维护ledgerDate（如验收报销单未完成财务核算即被纳入批量复核），推送时记账日期缺失，校验不通过抛异常。需先在验收报销单中维护总账日期再提交批量复核。
- **排查SQL**：
  ```sql
  SELECT t.terminal_cashout_id   AS 兑现单ID,
         t.terminal_cashout_code AS 兑现单号,
         t.cash_id               AS 批量复核单ID,
         t.check_bx_code         AS 验收报销单号,
         t.ledger_date           AS 总账日期,
         h.cash_code             AS 批量复核单号,
         h.hz_approve_status     AS 批量复核单状态
  FROM   fin_fee_terminal_cashout t
  INNER JOIN fin_fee_in_cash_head h
  ON     t.cash_id = h.cash_id
  WHERE  h.hz_approve_status = 'NEW'
  AND    t.ledger_date IS NULL
  ORDER  BY h.create_time DESC;
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-12-01 | - | hfy | 初始创建FinFeeInCashHeadServiceImpl应用服务 |
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
