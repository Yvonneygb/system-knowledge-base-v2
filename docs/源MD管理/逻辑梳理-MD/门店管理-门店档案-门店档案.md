# 门店管理-门店档案-门店档案

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

门店档案是门店的基础主数据表，供各下游模块统一引用。门店档案由"新建门店申请"审批通过后自动生成（syncMktTerminal），也可通过"门店变更申请"审批通过后更新。本页面支持门店档案列表查询、补充信息保存（其他情况说明、面积变动说明、附件），以及为门店装修申请和门头展板报销提供门店LOV选择。

**核心流程：**
1. 用户进入门店档案页面，系统自动按当前用户组织ID和经销商编码过滤
2. 查看门店档案列表，支持分页查询
3. 可补充编辑"其他情况说明"和"面积变动说明"，保存附件
4. 门店装修申请时通过finFeeApplyLov接口选择门店（注入装修提前天数）
5. 门头展板报销时通过custDhReimburseHead接口选择门店

```
开始 → 查询门店档案(按组织+经销商过滤) → 查看列表 → 补充信息保存(其他情况+面积变动+附件) → 结束
```

- 上游支撑：新建门店申请（MKT_TERMINAL_APPLY）、门店变更申请（MKT_TERMINAL_MODIFY）、经销商主数据、事业部基础设置、区域表
- 下游影响：门店装修申请、门店验收与报销、门头展板报销、门店报表

## 重点逻辑

### 重点逻辑1：主数据表无审批流程

- **业务意义**：门店档案是基础主数据，不涉及审批流程，由上游申请单审批通过后自动生成/更新
- **具体逻辑描述**：
  - 门店档案由"新建门店申请"审批通过后通过syncMktTerminal方法自动生成
  - 门店档案由"门店变更申请"审批通过后通过onWfComplete方法自动更新
  - 本页面仅支持查询和补充信息编辑，不支持新增/删除

### 重点逻辑2：数据权限按组织+经销商隔离

- **业务意义**：查询时自动按当前用户组织ID和经销商编码过滤，确保数据隔离
- **具体逻辑描述**：
  - selectList方法：entid为空时自动填充当前组织ID，custCode为空时自动填充当前经销商编码
  - finFeeApplyLov方法：同样自动填充组织ID、经销商编码，并注入装修提前天数
  - 默认按terminalId降序排列

### 重点逻辑3：保存仅支持局部更新

- **业务意义**：保存接口仅更新补充信息字段，不支持全字段编辑
- **具体逻辑描述**：
  - save方法先按主键查询，不存在抛CommonException("数据不存在")
  - 仅更新两个字段：otherCondition（其他情况说明）、terminalAreaChange（门店面积变动说明）
  - 使用updateByPrimaryKeySelective选择性更新
  - 批量插入附件关系（ObjAttachRel）

### 重点逻辑4：LOV接口支持装修申请和门头报销

- **业务意义**：为下游模块提供门店选择LOV接口
- **具体逻辑描述**：
  - finFeeApplyLov：门店装修申请与进度更新时选择门店，注入advancePermissibleTime（装修提前天数）
  - custDhReimburseHead：门头展板报销申请时选择门店，直接透传查询

## 详细逻辑

### 界面模块

#### 门店档案列表

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 门店ID | TERMINAL_ID | 隐藏 | 主键 | 常显 | 系统自动生成 |
| 门店编码 | TERMINAL_CODE | 文本 | 门店编码 | 常显 | 新建门店申请审批通过时生成 |
| 门店名称 | TERMINAL_NAME | 文本 | 门店名称 | 常显 | 从申请单同步 |
| 经销商编码 | CUST_CODE | 文本 | 经销商编码 | 常显 | 从申请单同步 |
| 经销商名称 | CUST_NAME | 文本 | 经销商名称 | 常显 | 从申请单同步 |
| 门店类型 | TERMINAL_TYPE | 文本 | 门店类型 | 常显 | 1=专卖/2=商超/3=家装/4=社区/5=乡镇 |
| 门店面积 | TERMINAL_AREA | 数值 | 门店面积 | 常显 | 从申请单同步 |
| 门店状态 | TERMINAL_STAT | 文本 | 门店状态 | 常显 | 1=运营中, 2=撤店 |
| 经营属性 | CUSTOMER_CLASS | 文本 | 经营属性 | 常显 | 1=直营专营/2=经销专营/3=分销 |
| 装修风格 | DECORATION_STYLE | 文本 | 店面装修风格 | 常显 | LOV翻译 |
| 门店装修等级 | FIXUP_GRADE | 文本 | 门店装修等级 | 常显 | LOV翻译(AE.FIXUP_GRADE) |
| 有效状态 | USABLE | 文本 | 有效状态 | 常显 | 2=有效 |
| 省份名称 | PROVINCE_AREANAME | 文本 | 省份 | 常显 | 从申请单同步 |
| 城市名称 | CITY_AREANAME | 文本 | 城市 | 常显 | 从申请单同步 |
| 区县名称 | COUNTY_AREANAME | 文本 | 区县 | 常显 | 从申请单同步 |
| 门店详细地址 | ADDR | 文本 | 门店详细地址 | 常显 | 从申请单同步 |
| 开店日期 | IN_SHOP_DATE | 日期 | 开店日期 | 常显 | 从申请单同步 |
| 撤店日期 | SHUT_DATE | 日期 | 撤店日期 | 常显 | 门店变更申请审批通过时更新 |
| 最新装修日期 | LATEST_DECORATION_DATE | 日期 | 最新装修日期 | 常显 | 从申请单同步 |
| 店长姓名 | SORE_MANAGERS_NAME | 文本 | 店长姓名 | 常显 | 从申请单同步 |
| 店长联系电话 | SORE_MANAGERS_TEL | 文本 | 店长联系电话 | 常显 | 从申请单同步 |
| 其他情况说明 | OTHER_CONDITION | 文本域 | 其他情况说明 | 常显 | 用户输入（补充信息） |
| 门店面积变动说明 | TERMINAL_AREA_CHANGE | 文本域 | 面积变动说明 | 常显 | 用户输入（补充信息） |
| 备注 | NOTE | 文本域 | 备注 | 常显 | 从申请单同步 |
| 创建人 | CREATOR | 文本 | 创建人 | 常显 | 系统自动赋值 |
| 创建时间 | CREATE_TIME | 日期 | 创建时间 | 常显 | 系统自动赋值 |

#### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 查询门店档案列表 | 列表页 | 常显 | 调用GET /mkt-terminals，按组织+经销商过滤 |
| 保存 | 保存补充信息 | 详情页 | 常显 | 调用POST /mkt-terminals，更新otherCondition和terminalAreaChange+附件 |

#### 按钮1：查询（列表页）

- **业务意义**：查询门店档案列表
- **具体逻辑描述**：
  - 调用GET `/v1/{organizationId}/mkt-terminals` 接口
  - 自动填充entid（当前组织ID）和custCode（当前经销商编码）
  - 使用@ProcessLovValue翻译LOV字段
  - 默认按terminalId降序排列

#### 按钮2：保存（详情页）

- **业务意义**：保存门店补充信息
- **具体逻辑描述**：
  - 调用POST `/v1/{organizationId}/mkt-terminals` 接口
  - 先按主键查询，不存在抛CommonException("数据不存在")
  - 仅更新otherCondition和terminalAreaChange两个字段
  - 批量插入附件关系

### 后端接口

| 接口名称 | 请求方式 | 接口路径 | 权限 | 说明 |
|----------|----------|----------|------|------|
| 网点资料列表 | GET | `/v1/{organizationId}/mkt-terminals` | permissionLogin=true | 分页查询门店档案，自动按组织+经销商过滤 |
| 网点资料保存 | POST | `/v1/{organizationId}/mkt-terminals` | permissionLogin=true | 保存补充信息（otherCondition+terminalAreaChange+附件） |
| 门店装修申请LOV | GET | `/v1/{organizationId}/mkt-terminals/fin-fee-apply-lov` | permissionLogin=true | 装修申请时选择门店，注入装修提前天数 |
| 门头报销门店LOV | GET | `/v1/{organizationId}/mkt-terminals/cust-dh-reimburse` | permissionLogin=true | 门头展板报销时选择门店 |

### 状态机

> 门店档案为主数据表，无状态流转。门店状态（TERMINAL_STAT）由门店变更申请审批通过后更新：1=运营中 → 2=撤店。

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 新建门店申请 | 数据来源 | 审批通过后通过syncMktTerminal生成门店档案 | 新建门店申请已审批通过 |
| 门店变更申请 | 数据来源 | 审批通过后更新门店档案 | 门店变更申请已审批通过 |
| 经销商主数据 | 数据来源 | 提供经销商信息 | 经销商已创建 |
| 事业部基础设置 | 数据来源 | 提供事业部信息 | 事业部已配置 |

### 下游影响

- 门店装修申请：通过finFeeApplyLov接口选择门店
- 门店验收与报销：基于门店档案进行验收报销
- 门头展板报销：通过custDhReimburseHead接口选择门店
- 门店报表：所有门店报表的数据基础

## 数据库表详解

### MKT_TERMINAL（门店档案表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|------------|------|
| TERMINAL_ID | NUMBER | 门店ID(主键) | 门店ID | 自增 |
| TERMINAL_CODE | VARCHAR2 | 门店编码 | 门店编码 | barCode+divisionCode+5位Redis流水号 |
| TERMINAL_NAME | VARCHAR2 | 门店名称 | 门店名称 | 从申请单同步 |
| CUST_ID | NUMBER | 经销商ID | - | 从申请单同步 |
| CUST_CODE | VARCHAR2 | 经销商编码 | 经销商编码 | 从申请单同步 |
| CUST_NAME | VARCHAR2 | 经销商名称 | 经销商名称 | 从申请单同步 |
| TERMINAL_TYPE | NUMBER | 门店类型 | 门店类型 | 1=专卖/2=商超/3=家装/4=社区/5=乡镇 |
| TERMINAL_AREA | NUMBER | 门店面积 | 门店面积 | 从申请单同步 |
| TERMINAL_STAT | NUMBER | 门店状态 | 门店状态 | 1=运营中, 2=撤店 |
| CUSTOMER_CLASS | NUMBER | 经营属性 | 经营属性 | 1=直营专营/2=经销专营/3=分销 |
| DECORATION_STYLE | NUMBER | 装修风格 | 装修风格 | LOV翻译 |
| FIXUP_GRADE | NUMBER | 门店装修等级 | 门店装修等级 | LOV翻译(AE.FIXUP_GRADE) |
| USABLE | NUMBER | 有效状态 | 有效状态 | 2=有效 |
| PROVINCE_AREANAME | VARCHAR2 | 省份名称 | 省份名称 | 从申请单同步 |
| CITY_AREANAME | VARCHAR2 | 城市名称 | 城市名称 | 从申请单同步 |
| COUNTY_AREANAME | VARCHAR2 | 区县名称 | 区县名称 | 从申请单同步 |
| ADDR | VARCHAR2 | 门店详细地址 | 门店详细地址 | 从申请单同步 |
| IN_SHOP_DATE | DATE | 开店日期 | 开店日期 | 从申请单同步 |
| SHUT_DATE | DATE | 撤店日期 | 撤店日期 | 变更申请审批通过时更新 |
| OTHER_CONDITION | VARCHAR2 | 其他情况说明 | 其他情况说明 | 用户输入（补充信息） |
| TERMINAL_AREA_CHANGE | VARCHAR2 | 面积变动说明 | 面积变动说明 | 用户输入（补充信息） |
| ORGANIZATION_ID | NUMBER | 组织ID | - | 租户组织标识 |
| ENTID | NUMBER | 企业ID | - | LOV翻译(AE.ITEM_ORGANIZATION) |
| DIVISION_ID | NUMBER | 事业部ID | - | 从申请单同步 |

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|---------------|------|---------|
| 数据不存在 | 保存时 | 门店档案不存在或已被删除，请检查门店ID | 高 | [查看](#报错1数据不存在) |
| 网络请求失败 | 全局 | 后端服务不可达或超时，请检查网络与服务状态 | 中 | [查看](#报错2网络请求失败) |
| 权限不足 | 全局 | 当前用户无门店档案操作权限或数据权限隔离 | 中 | [查看](#报错3权限不足) |
| 查询无数据 | 列表查询时 | 当前组织/经销商下无门店档案数据 | 低 | [查看](#报错4查询无数据) |

#### 报错1：数据不存在
- **触发条件**：点击"保存"按钮补充其他情况说明/面积变动说明时，按terminalId调用selectByPrimaryKey查询MKT_TERMINAL返回null
- **逻辑分析**：保存接口仅支持局部更新otherCondition和terminalAreaChange两个字段，前置需先校验门店档案存在。若门店档案在编辑期间被上游"门店变更申请"流程撤店删除（实际为状态置为撤店而非物理删除）、terminalId传值错误（如前端缓存了已失效ID）、或并发场景下被清理，查询返回空，抛CommonException中断保存。需核查门店档案是否仍处于有效运营状态。
- **排查SQL**：
  ```sql
  SELECT t.terminal_id         AS 门店ID,
         t.terminal_code       AS 门店编码,
         t.terminal_name       AS 门店名称,
         t.terminal_stat       AS 门店状态,
         t.usable              AS 有效状态,
         t.update_time         AS 最后更新时间
  FROM   mkt_terminal t
   WHERE  t.terminal_id = #{传入的terminalId}
   AND    (t.usable IS NULL OR t.terminal_stat = 2)
   ORDER  BY t.update_time DESC;
   ```

#### 报错2：网络请求失败
- **触发条件**：前端调用门店档案接口（查询列表、保存补充信息、LOV选择）时，请求超时或后端服务不可达
- **逻辑分析**：低代码页面通过axios请求后端API（/v1/{organizationId}/mkt-terminals、fin-fee-apply-lov、cust-dh-reimburse等）。若后端ae-business服务未启动、网络中断、网关超时、或数据库连接池耗尽导致请求堆积，axios捕获网络异常，前端展示通用错误提示，列表数据无法加载或保存操作失败。需核查后端服务健康状态、网络连通性、网关配置及数据库连接池。
- **排查SQL**：
  ```sql
  SELECT '服务连通性检查' AS 检查项,
         COUNT(*)          AS 门店档案总记录数,
         MAX(t.update_time) AS 最后更新时间
  FROM   mkt_terminal t
  WHERE  t.usable = 2;
  ```

#### 报错3：权限不足
- **触发条件**：当前用户无门店档案相关操作权限（查询/保存/LOV选择）
- **逻辑分析**：低代码页面通过permissionLogin=true进行登录校验，selectList方法自动按当前用户组织ID（entid）和经销商编码（custCode）过滤数据实现数据权限隔离。若用户角色未分配门店档案菜单权限、权限码配置缺失、或组织ID/经销商编码不匹配导致数据权限隔离后无可见数据，接口返回403/401或空列表。需核查用户角色权限配置及组织数据权限、经销商主数据关联。
- **排查SQL**：
  ```sql
  SELECT t.terminal_id         AS 门店ID,
         t.terminal_code       AS 门店编码,
         t.terminal_name       AS 门店名称,
         t.cust_code           AS 经销商编码,
         t.entid               AS 企业ID,
         t.organization_id     AS 组织ID
  FROM   mkt_terminal t
  WHERE  t.organization_id = #{当前用户组织ID}
  AND    t.cust_code = #{当前经销商编码}
  ORDER  BY t.terminal_id DESC;
  ```

#### 报错4：查询无数据
- **触发条件**：进入门店档案列表页或调用LOV接口查询时，按当前组织ID和经销商编码过滤后返回空列表
- **逻辑分析**：selectList方法在entid为空时自动填充当前组织ID、custCode为空时自动填充当前经销商编码，按terminalId降序排列。若当前用户所属组织/经销商下确实无门店档案（如新建门店申请未审批通过、门店档案均被撤店terminal_stat=2、或经销商主数据未关联门店），查询返回空列表，前端展示空表格。此为正常业务情况而非错误，但用户可能误判为故障。需核查该组织/经销商下门店档案是否已建立及有效状态。
- **排查SQL**：
  ```sql
  SELECT t.terminal_id         AS 门店ID,
         t.terminal_code       AS 门店编码,
         t.terminal_name       AS 门店名称,
         t.cust_code           AS 经销商编码,
         t.terminal_stat       AS 门店状态,
         t.usable              AS 有效状态,
         t.entid               AS 企业ID,
         t.organization_id     AS 组织ID
  FROM   mkt_terminal t
  WHERE  t.organization_id = #{当前用户组织ID}
  AND    t.cust_code = #{当前经销商编码}
  ORDER  BY t.terminal_id DESC;
  ```

## 常见问题FAQ

**Q1：门店档案如何创建？**
A：由"新建门店申请"审批通过后自动生成（syncMktTerminal方法），不支持在本页面直接新增。

**Q2：门店档案如何更新？**
A：由"门店变更申请"审批通过后自动更新（onWfComplete方法）。本页面仅支持补充"其他情况说明"和"面积变动说明"。

**Q3：查询时如何过滤数据？**
A：自动按当前用户组织ID（entid）和经销商编码（custCode）过滤，确保数据隔离。

**Q4：LOV接口有什么用？**
A：finFeeApplyLov为门店装修申请提供门店选择（注入装修提前天数），custDhReimburseHead为门头展板报销提供门店选择。

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-11-13 | - | YD | 初始创建门店档案管理功能 |
