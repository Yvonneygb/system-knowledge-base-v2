# 工程管理-服务费-工程服务费报销

> **说明**：本菜单为低代码页面（hlod），前端无独立页面源码，通过低代码平台配置绑定后端接口渲染。后端Controller为FinSvcExpAccHeadController，API路径/v1/{orgId}/fin-svc-exp-acc-heads。

---

## 业务流程

### 业务流程图

```text
开始 → ★新建服务费报销★ → 选择项目/合同 → 填写报销信息 → 保存 → 提交审批 → ⚖审批通过？
  ├─（通过）报销完成 → 财务付款 → 结束
  └─（拒绝）可修改后重新提交 → 结束
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 项目主档 | 数据依赖 | 选择工程项目 | 项目已创建 |
| 合同主档 | 数据依赖 | 关联合同 | 合同已签订 |
| OA工作流引擎 | 服务依赖 | 提交审批触发工作流 | 工作流已配置 |
| 低代码平台 | 配置依赖 | 页面通过低代码配置渲染 | 配置已完成 |

### 下游影响

- 影响1：财务付款
  - 报销审批通过后进入财务付款流程

---

## 详细逻辑

### 界面模块1：工程服务费报销（低代码页面）

> 低代码页面，字段配置由低代码平台维护，后端API: /v1/{orgId}/fin-svc-exp-acc-heads

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 报销单号 | FIN_SVC_EXP_ACC_HEAD.ACC_NO | 文本框 | 报销单唯一标识 | 常显 | 1. 保存时自动生成 |
| 项目编码 | FIN_SVC_EXP_ACC_HEAD.PROJECT_CODE | 文本框 | 项目编码 | 常显 | 1. 选择项目带出 |
| 审核状态 | FIN_SVC_EXP_ACC_HEAD.HZ_APPROVE_STATUS | 下拉选择框 | 审批状态 | 常显 | 1. 来源：值集HWKF.APPROVE_STATUS |

---

## 数据库表详解

### 表1：FIN_SVC_EXP_ACC_HEAD（服务费报销头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ACC_ID | NUMBER | 主键 | 无 | 自增生成 |
| ACC_NO | VARCHAR | 报销单号 | 报销单号 | 自动生成 |
| PROJECT_CODE | VARCHAR | 项目编码 | 项目编码 | 关联项目主档 |
| HZ_APPROVE_STATUS | VARCHAR | 审批状态 | 审核状态 | 默认NEW |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 报销单不存在 | 查询详情时 | 按ACC_NO查询FIN_SVC_EXP_ACC_HEAD为空。检查报销单号有效性 | 高 | [查看] |
| 报销金额超过可报销金额 | 提交时 | 报销金额超过剩余可报销金额。调整报销金额 | 高 | [查看] |
| 仅新建状态单据允许删除 | 删除时 | 单据HZ_APPROVE_STATUS非NEW。仅新建状态可删除 | 高 | [查看] |
| 未查询到相关工程费报销单 | 查询详情时 | 按条件查询FIN_SVC_EXP_ACC_HEAD为空。检查查询条件 | 高 | [查看] |
| 该报销单税率为空 | 保存校验 | 报销单TAX_RATE字段为空。需维护税率 | 高 | [查看] |
| 未找到对应的费用报销单 | 查询关联单据时 | 关联费用报销单查询为空。检查关联单据有效性 | 高 | [查看] |
| 发票附件名称异常，请检查文件后缀名后再上传！ | 上传发票附件 | 发票附件文件名格式异常。检查文件后缀名 | 高 | [查看] |
| 发票附件类型异常，请上传【jpg、jpeg、png、pdf】类型的附件！ | 上传发票附件 | 发票附件类型不在允许范围内。使用jpg/jpeg/png/pdf格式 | 高 | [查看] |
| 上传的发票类型与报销类型不一致。正数报销时，需上传蓝字发票。负数报销时，需上传红字发票。 | 上传发票 | 发票类型与报销类型不匹配。按报销类型上传对应发票 | 高 | [查看] |
| 税率不能为空 | 保存校验 | 税率字段为空。需维护税率 | 高 | [查看] |
| 发票税率不存在 | 保存校验 | 发票税率在税率表不存在。检查税率配置 | 高 | [查看] |
| 流程完结异常：缺失单据id | 流程完结 | 未传入单据ID。检查前端传参完整性 | 高 | [查看] |
| 流程启动异常：缺失单据id | 流程启动 | 未传入单据ID。检查前端传参完整性 | 高 | [查看] |
| 流程操作异常：实际报销金额必须大于0 | 流程操作 | 实际报销金额≤0。调整报销金额 | 高 | [查看] |
| 流程操作异常：发票必须上传 | 流程操作 | 未上传发票。需上传发票附件 | 高 | [查看] |
| 流程操作异常，以下发票明细异常：xxx | 流程操作 | 发票明细数据校验未通过。检查发票明细数据 | 高 | [查看] |
| 推送数据到共享异常：查找申请人OA信息未空 | 推送共享 | 申请人OA信息未配置。联系管理员维护申请人OA账号 | 高 | [查看] |
| 推送数据到共享异常：单据查询数据为空 | 推送共享 | 按单据ID查询报销单为空。检查单据ID有效性 | 高 | [查看] |
| 附件ocr接口识别失败 | 发票OCR识别 | OCR接口调用失败。检查OCR服务状态 | 高 | [查看] |

#### 报错1：报销单不存在
- **触发条件**：查询工程服务费报销单详情时，按ACC_NO查询FIN_SVC_EXP_ACC_HEAD返回null
- **逻辑分析**：详情方法中按ACC_NO查询报销单，若返回null则抛出阻断性报错。需检查报销单号有效性
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.PROJECT_CODE, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_NO = :accNo
  -- 若返回空，说明报销单不存在
  ```

#### 报错2：报销金额超过可报销金额
- **触发条件**：提交工程服务费报销申请时，报销金额超过剩余可报销金额
- **逻辑分析**：提交校验中按PROJECT_CODE查询剩余可报销金额(已兑现服务费-已报销金额)，若本次报销金额>剩余则抛出阻断性报错。需调整报销金额
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.ACC_AMOUNT, fseah.PROJECT_CODE,
         fseah.TOTAL_CASH_AMOUNT, fseah.EXPENSED_AMOUNT,
         fseah.TOTAL_CASH_AMOUNT - fseah.EXPENSED_AMOUNT AS 剩余可报销金额
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_NO = :accNo
  -- 对比报销金额与剩余可报销金额
  ```

#### 报错3：仅新建状态单据允许删除
- **触发条件**：删除工程服务费报销单时，单据HZ_APPROVE_STATUS非NEW
- **逻辑分析**：删除方法中校验单据状态为NEW，其他状态不允许删除。该报错为阻断性报错
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.HZ_APPROVE_STATUS, fseah.VALID
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 期望 HZ_APPROVE_STATUS = 'NEW'
  ```

#### 报错4：未查询到相关工程费报销单
- **触发条件**：查询工程服务费报销单时，按条件查询FIN_SVC_EXP_ACC_HEAD返回空
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl查询方法中(line 179)，按条件查询报销单，若返回空则抛出CommonException("未查询到相关工程费报销单")。需检查查询条件
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.PROJECT_CODE, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_NO = :accNo
  -- 若返回空，说明未查询到报销单
  ```

#### 报错5：该报销单税率为空
- **触发条件**：保存工程服务费报销单时，TAX_RATE字段为空
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl保存校验方法中(line 214)，校验报销单税率字段，若为空则抛出CommonException("该报销单税率为空")。需维护税率
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.TAX_RATE
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
    AND (fseah.TAX_RATE IS NULL OR fseah.TAX_RATE = '')
  ```

#### 报错6：未找到对应的费用报销单
- **触发条件**：查询关联费用报销单时，关联单据查询返回空
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl方法中(line 548)，查询关联费用报销单，若返回空则抛出CommonException("未找到对应的费用报销单")。需检查关联单据有效性
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.PROJECT_CODE, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 若返回空，说明关联报销单不存在
  ```

#### 报错7：发票附件名称异常，请检查文件后缀名后再上传！
- **触发条件**：上传发票附件时，文件名格式异常(无后缀名或后缀名格式错误)
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl发票附件校验方法getDeductParam中(line 634)，检查发票附件文件名后缀，若格式异常则抛出CommonException("发票附件名称异常，请检查文件后缀名后再上传！")。需检查文件后缀名
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查发票附件文件名格式
  ```

#### 报错8：发票附件类型异常，请上传【jpg、jpeg、png、pdf】类型的附件！
- **触发条件**：上传发票附件时，文件类型不在允许范围内(jpg/jpeg/png/pdf)
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl发票附件校验方法getDeductParam中(line 642)，检查发票附件类型，若不在jpg/jpeg/png/pdf范围内则抛出CommonException。需使用正确格式的附件
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查发票附件类型是否为jpg/jpeg/png/pdf
  ```

#### 报错9：上传的发票类型与报销类型不一致。正数报销时，需上传蓝字发票。负数报销时，需上传红字发票。
- **触发条件**：上传发票时，发票类型与报销类型不匹配
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl发票校验方法中(line 734)，校验发票类型与报销类型是否一致，正数报销需蓝字发票、负数报销需红字发票，若不匹配则抛出CommonException。需按报销类型上传对应发票
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.ACC_AMOUNT AS 报销金额,
         CASE WHEN fseah.ACC_AMOUNT > 0 THEN '正数报销(蓝字)' ELSE '负数报销(红字)' END AS 报销类型
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查发票类型与报销类型是否匹配
  ```

#### 报错10：税率不能为空
- **触发条件**：保存工程服务费报销单时，税率字段为空
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl保存校验方法中(line 878)，校验税率字段，若为空则抛出CommonException("税率不能为空")。需维护税率
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.TAX_RATE
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
    AND (fseah.TAX_RATE IS NULL OR fseah.TAX_RATE = '')
  ```

#### 报错11：发票税率不存在
- **触发条件**：保存工程服务费报销单时，发票税率在税率表不存在
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl保存校验方法中(line 913)，查询税率表校验发票税率，若不存在则抛出CommonException("发票税率不存在")。需检查税率配置
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.TAX_RATE
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查TAX_RATE是否在税率主档表中存在
  ```

#### 报错12：流程完结异常：缺失单据id
- **触发条件**：流程完结时，未传入单据ID
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl流程完结方法中(line 949)，校验单据ID参数，若为空则抛出CommonException("流程完结异常：缺失单据id")。需检查前端传参完整性
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  ```

#### 报错13：流程启动异常：缺失单据id
- **触发条件**：流程启动时，未传入单据ID
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl流程启动方法中(line 1001)，校验单据ID参数，若为空则抛出CommonException("流程启动异常：缺失单据id")。需检查前端传参完整性
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  ```

#### 报错14：流程操作异常：实际报销金额必须大于0
- **触发条件**：流程操作时，实际报销金额≤0
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl流程操作方法中(line 1064)，校验实际报销金额，若≤0则抛出CommonException("流程操作异常：实际报销金额必须大于0")。需调整报销金额
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.ACC_AMOUNT AS 实际报销金额
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
    AND (fseah.ACC_AMOUNT IS NULL OR fseah.ACC_AMOUNT <= 0)
  ```

#### 报错15：流程操作异常：发票必须上传
- **触发条件**：流程操作时，未上传发票附件
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl流程操作方法中(line 1077)，校验发票是否已上传，若未上传则抛出CommonException("流程操作异常：发票必须上传")。需上传发票附件
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO,
         (SELECT COUNT(1) FROM FIN_SVC_EXP_ACC_INVOICE fsai
          WHERE fsai.ACC_HEAD_ID = fseah.ACC_ID) AS 发票数量
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 期望 发票数量 > 0
  ```

#### 报错16：流程操作异常，以下发票明细异常：xxx
- **触发条件**：流程操作时，发票明细数据校验未通过
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl流程操作方法中(line 1094)，遍历发票明细校验数据，若存在异常则拼接错误信息抛出CommonException("流程操作异常，以下发票明细异常：\n" + errMsg)。需检查发票明细数据
- **排查SQL**：
  ```sql
  SELECT fsai.INVOICE_ID, fsai.ACC_HEAD_ID, fsai.INVOICE_NO, fsai.INVOICE_AMOUNT,
         fsai.TAX_RATE, fsai.INVOICE_TYPE
  FROM FIN_SVC_EXP_ACC_INVOICE fsai
  WHERE fsai.ACC_HEAD_ID = :accId
  -- 检查发票明细数据完整性
  ```

#### 报错17：推送数据到共享异常：查找申请人OA信息未空
- **触发条件**：报销单推送共享中心时，申请人OA信息未配置
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl推送共享方法中(line 1167)，查找申请人OA账号信息，若为空则抛出CommonException("推送数据到共享异常：查找申请人OA信息未空")。需联系管理员维护申请人OA账号
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.CREATED_BY
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查申请人OA账号是否已配置
  ```

#### 报错18：推送数据到共享异常：单据查询数据为空
- **触发条件**：报销单推送共享中心时，按单据ID查询报销单返回空
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl推送共享方法中(line 1173)，按单据ID查询报销单，若返回空则抛出CommonException("推送数据到共享异常：单据查询数据为空")。需检查单据ID有效性
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO, fseah.HZ_APPROVE_STATUS
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 若返回空，说明单据不存在
  ```

#### 报错19：附件ocr接口识别失败
- **触发条件**：发票附件OCR识别时，OCR接口调用失败
- **逻辑分析**：在FinSvcExpAccHeadServiceImpl附件OCR识别方法中(line 1314/1320)，调用OCR接口识别发票附件，若接口返回失败则抛出CommonException("附件ocr接口识别失败")或带失败原因。需检查OCR服务状态
- **排查SQL**：
  ```sql
  SELECT fseah.ACC_ID, fseah.ACC_NO
  FROM FIN_SVC_EXP_ACC_HEAD fseah
  WHERE fseah.ACC_ID = :accId
  -- 检查OCR服务连通性
  ```

### 常见问题

- 问题1：低代码页面配置问题
  - 原因：低代码平台配置未完成
  - 解决思路：检查低代码平台配置

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-29 | - | - | 按skill规范完整重写，基于后端代码梳理（低代码页面） |
