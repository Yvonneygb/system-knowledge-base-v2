# 经销合同管理-合同管理-合同任务完成率

## 业务流程

### 业务流程图

```text
合同任务完成率报表页面(ae-report报表)
        │
        ├── 查询年月汇总 ──→ POST /contractReport/epm-project-contract/searchYearContractRate
        │
        ├── 查询合同汇总 ──→ POST /contractReport/searchContractRate
        │
        ├── 查询项目汇总 ──→ POST /contractReport/searchProjectRate
        │
        └── 导出 ──→ POST /contractReport/exportContractRate 或 /exportProjectRate
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 年度经销合同 | 数据依赖 | 完成率基于合同任务数据计算 | 合同已签订 |
| 出库单 | 数据依赖 | 完成率基于出库完成情况 | 出库单已生成 |

### 下游影响

- 影响1：合同管理决策
  - 完成率报表为合同管理提供任务执行情况分析依据

---

## 重点逻辑

### 重点逻辑1：合同完成率计算 {完成率计算}

- **业务意义**：按年月维度汇总合同B合同任务完成率，掌握合同执行进度

- **具体逻辑描述**

  - 第1点：通过searchYearContractRate接口按年月汇总合同完成率

  - 第2点：完成率=已出库金额/合同任务金额×100%

  - 第3点：支持按合同汇总和项目汇总两个维度查询

---

## 详细逻辑

### 界面模块1：合同任务完成率报表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 年月 | EPM_PROJECT_CONTRACT.YEARMONTH | 文本框 | 统计年月 | 常显 | 手动选择 |
| 合同编码 | EPM_PROJECT_CONTRACT.CONTRACT_CODE | 文本框 | 合同编码 | 常显 | 系统带出 |
| 合同名称 | EPM_PROJECT_CONTRACT.CONTRACT_NAME | 文本框 | 合同名称 | 常显 | 系统带出 |
| 经销商 | EPM_PROJECT_CONTRACT.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 系统带出 |
| 任务金额 | EPM_PROJECT_CONTRACT.TASK_AMT | 数字显示框 | 合同任务金额 | 常显 | 系统带出 |
| 已出库金额 | EPM_PROJECT_CONTRACT.DELIVERY_AMT | 数字G显示框 | 已出库金额 | 常显 | 系统计算 |
| 完成率 | - | 数字显示框 | 任务完成率 | 常显 | 自动计算=已出库金额/任务金额×100% |

### 选择弹窗

> 本页面为报表查询页面，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询完成率 | 列表页 | 始终可用 | 调用searchYearContractRate接口 |
| 导出 | 导出报表 | 列表页 | 有数据时 | 导出Excel |

### 保存校验

> 本页面为报表页面，无保存操作。

### 提交校验

> 本页面为报表页面，无提交操作。

### 状态C状态机

> 本页面为报表页面，无状态流转。

---

## 数据库表详解

### 表1：EPM_PROJECT_CONTRACT（项目合同表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| PROJECT_CONTRACT_ID | BIGINT | 主键ID | - | 自增主键 |
| YEARMONTH | VARCHAR | 年月 | 年月 | 系统带出 |
| CONTRACT_CODE | VARCHAR | 合同编码 | 合同编码 | 系统带出 |
| CONTRACT_NAME | VARCHAR | 合同名称 | 合同名称 | 系统带出 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商 | 系统带出 |
| TASK_AMT | DECIMAL | 任务金额 | 任务金额 | 系统带出 |
| DELIVERY_AMT | DECIMAL | 已出库金额 | 已出库金额 | 系统计算 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 年月选择错误或无合同数据，放宽条件 | toast提醒 | [查看] |
| 年月参数为空 | 查询时 | 未选择年月，需先选择统计年月 | toast提醒 | [查看] |
| 导出无数据 | 导出时 | 当前查询无数据，无法导出，先查询有效数据 | toast提醒 | [查看] |
| 网络请求失败 | 查询/导出时 | 网络中断或报表服务不可用，检查网络和AE_REPORT服务 | toast提醒 | [查看] |
| 权限不足 | 查询/导出时 | 当前用户无报表查询或导出权限，联系管理员授权 | toast提醒 | [查看] |
| 任务金额为0 | 查询时 | 合同任务金额为0，完成率无法计算，检查合同任务配置 | toast提醒 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按年月/合同/经销商查询合同任务完成率，searchYearContractRate接口返回空结果集
- **逻辑分析**：完成率报表基于EPM_PROJECT_CONTRACT表，按YEARMONTH（年月）维度汇总合同B合同任务完成率，完成率=已出库金额（DELIVERY_AMT）/合同任务金额（TASK_AMT）×100%。无数据根因有三类：(1)年月（YEARMONTH）选择错误，该年月无合同任务数据；(2)该年月内合同已签订但未生成项目合同记录（EPM_PROJECT_CONTRACT由年度经销合同派生）；(3)合同/经销商筛选条件与记录不匹配。需先放宽条件（去掉合同和经销商只按年月查）确认是否有任何合同数据
- **排查SQL**：
  ```sql
  SELECT YEARMONTH, CONTRACT_CODE, CONTRACT_NAME, CUSTOMER_NAME,
         TASK_AMT, DELIVERY_AMT,
         CASE WHEN TASK_AMT > 0 THEN ROUND(DELIVERY_AMT / TASK_AMT * 100, 2) ELSE 0 END AS 完成率
  FROM EPM_PROJECT_CONTRACT
  WHERE (YEARMONTH = #{yearmonth} OR #{yearmonth} IS NULL)
    AND (CONTRACT_CODE = #{contractCode} OR #{contractCode} IS NULL)
  ORDER BY YEARMONTH DESC, CONTRACT_CODE;
  ```

#### 报错2：年月参数为空
- **触发条件**：用户未选择年月即点击查询，searchYearContractRate接口接收到yearmonth为空
- **逻辑分析**：合同任务完成率报表按YEARMONTH（年月）维度汇总合同B合同任务完成率，年月为查询的核心过滤条件。YearMonthContractRateDTO中年月参数为空时，接口可能返回全量数据或空结果集，导致页面性能问题或无数据展示。前端应强制要求年月为必填项，后端应校验yearmonth非空
- **排查SQL**：
  ```sql
  SELECT YEARMONTH, COUNT(*) AS 合同数量
  FROM EPM_PROJECT_CONTRACT
  GROUP BY YEARMONTH
  ORDER BY YEARMONTH DESC;
  ```

#### 报错3：导出无数据
- **触发条件**：用户点击"导出"按钮，但当前查询结果为空
- **逻辑分析**：导出接口exportContractRate/exportProjectRate在EpmProjectContractServiceImpl.java:176和168处通过PageHelper.doPageAndSort查询数据，当查询结果为空时导出空Excel或提示无数据。根因有二：(1)当前查询条件无匹配数据；(2)未先执行查询直接导出。需先查询有效数据再导出
- **排查SQL**：
  ```sql
  SELECT YEARMONTH, CONTRACT_CODE, CONTRACT_NAME, CUSTOMER_NAME, TASK_AMT, DELIVERY_AMT
  FROM EPM_PROJECT_CONTRACT
  WHERE (YEARMONTH = #{yearmonth} OR #{yearmonth} IS NULL)
    AND (CONTRACT_CODE = #{contractCode} OR #{contractCode} IS NULL);
  ```

#### 报错4：网络请求失败
- **触发条件**：前端调用searchYearContractRate或导出接口时网络中断或AE_REPORT服务不可用
- **逻辑分析**：前端contractInvRateApi.ts中定义了searchYearContractRate、exportContractRateUrl、exportProjectRateUrl等接口，均指向AE_REPORT服务（envConfig.AE_REPORT）。fetchChartData方法在index.tsx:137处调用searchYearContractRate，catch块中console.error('获取图表数据失败:', error)处理异常。网络请求失败根因有三类：(1)AE_REPORT报表服务不可用；(2)网络连接中断；(3)请求超时。需检查网络连通性和AE_REPORT服务状态
- **排查SQL**：
  ```sql
  SELECT YEARMONTH, CONTRACT_CODE, CONTRACT_NAME, TASK_AMT, DELIVERY_AMT
  FROM EPM_PROJECT_CONTRACT
  WHERE YEARMONTH = #{yearmonth};
  ```

#### 报错5：权限不足
- **触发条件**：当前用户无合同任务完成率报表的查询或导出权限
- **逻辑分析**：前端页面通过路由配置authorized: true进行权限校验，导出按钮通过ExcelExportPro组件配置permissionList进行权限控制。无权限时路由不可访问或导出按钮不显示。需联系管理员为当前用户分配报表查询和导出权限
- **排查SQL**：
  ```sql
  -- 权限校验（伪SQL，具体表名依权限框架而定）
  SELECT ROLE_CODE, PERMISSION_CODE
  FROM USER_ROLE_PERMISSION
  WHERE USER_ID = #{userId} AND PERMISSION_CODE LIKE '%contractReport%';
  ```

#### 报错6：任务金额为0
- **触发条件**：查询结果中某合同的TASK_AMT（任务金额）为0或null，完成率无法计算
- **逻辑分析**：完成率=已出库金额（DELIVERY_AMT）/合同任务金额（TASK_AMT）×100%，当TASK_AMT为0时除以0导致计算异常，前端通过CASE WHEN TASK_AMT > 0 THEN ... ELSE 0 END处理为0。根因有二：(1)年度经销合同未配置任务金额；(2)合同任务金额字段未正确写入EPM_PROJECT_CONTRACT表。需检查合同任务配置
- **排查SQL**：
  ```sql
  SELECT YEARMONTH, CONTRACT_CODE, CONTRACT_NAME, CUSTOMER_NAME, TASK_AMT, DELIVERY_AMT
  FROM EPM_PROJECT_CONTRACT
  WHERE YEARMONTH = #{yearmonth} AND (TASK_AMT IS NULL OR TASK_AMT = 0);
  ```

### 常见问题

- 问题1：完成率为0
  - 原因：该合同无出库数据或任务金额为0
  - 解决思路：检查SQL `SELECT TASK_AMT, DELIVERY_AMT FROM EPM_PROJECT_CONTRACT WHERE CONTRACT_CODE = #{code}`

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
