# 产品中心-价目表-跨事业部产品产品销售申请

---

## 业务流程

### 业务流程图

```text
开始 → ★新建跨事业部产品销售申请★ → 填写申请说明 → 保存头信息 → 维护明细行（产品/类型/有效时间） → 提交审批 → ⚖OA审批通过？ 
  ├─（通过）同步写入跨事业部产品销售清单 → 联动失效价目表有效明细 → 结束
  ├─（拒绝）状态变为审核拒绝 → 可修改后重新提交 → 结束
  └─（撤回）状态恢复为新建 → 可继续编辑 → 结束
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 产品主档（LNK_PROD） | 数据依赖 | 明细行选择产品时查询产品编码、名称、型号、渠道、品牌事业部 | 产品状态为有效 |
| 事业部主档（LNK_ORG_EXT） | 数据依赖 | 明细行展示品牌事业部名称 | 品牌事业部数据存在 |
| 产品分类（LNK_MTL_CATEGORIES） | 数据依赖 | 明细行展示管理分类 | STRUCTURE_ID=101 |
| 责任制内结价定价参数 | 配置依赖 | 审批通过后联动更新价目表有效结束时间 | 价目表存在有效价格明细 |
| OA工作流引擎 | 服务依赖 | 提交时推送OA审批，审批结果回调更新状态 | OA流程配置完成 |
| 编码规则（CROSS_BU_APP_FORM_CODE） | 配置依赖 | 保存时自动生成申请单号 | 编码规则已配置 |
| 值集AE.ITEM_ORGANIZATION | 配置依赖 | 事业部下拉选择 | 值集已维护 |
| 值集HWKF.APPROVE_STATUS | 配置依赖 | 状态显示 | 值集已维护 |
| 值集CRM.CROSS_BU_ITEM_TYPE | 配置依赖 | 明细行类型（新增/失效） | 值集已维护 |

### 下游影响

- 影响1：跨事业部产品销售清单（LNK_CROSS_BU_SALES_LIST）
  - 审批通过后同步写入销售清单记录，如产品已存在则失效旧记录再写入新记录

- 影响2：价目表明细（LNK_PRICE_LIST_ITEM）
  - 明细类型=失效时，联动更新所需销售事业部价目表中该产品当前有效价格明细行的有效结束时间为审批通过当天

- 影响3：箭牌大客户事业部价目表
  - 当失效事业部为箭牌卫浴事业部时，同步失效箭牌大客户事业部价目表对应产品

- 影响4：定时任务自动失效
  - 销售清单中有效结束日期到期时，定时任务自动置为失效并联动失效价目表

---

## 重点逻辑

### 重点逻辑1：申请单号自动生成 `编码规则`

- **业务意义**：保证申请单号唯一且可追溯，无需用户手动填写

- **具体逻辑描述**

  - 第1点：新建保存时，后端通过编码规则CROSS_BU_APP_FORM_CODE自动生成申请单号

  - 第2点：申请单号生成后不可编辑，前端展示为禁用状态

### 重点逻辑2：事业部自动填充 `当前用户事业部`

- **业务意义**：申请单归属当前登录用户所在事业部，无需手动选择

- **具体逻辑描述**

  - 第1点：新建保存时，若事业部编码为空，后端自动填充当前登录用户的事业部编码

  - 第2点：事业部前端展示为禁用状态，不可编辑

### 重点逻辑3：明细行类型联动 `新增/失效`

- **业务意义**：区分产品是新增跨事业部销售还是失效已有跨事业部销售

- **具体逻辑描述**

  - 第1点：类型为"新增"时，有效开始时间和有效结束时间必填，且开始时间须大于等于今天，结束时间须大于等于开始时间

  - 第2点：类型为"失效"时，有效开始时间和有效结束时间自动清空且禁用编辑

  - 第3点：类型切换时前端实时联动时间字段的必填和禁用状态

### 重点逻辑4：审批通过同步写入销售清单 `下游同步`

- **业务意义**：审批通过后申请明细落地为正式销售清单，驱动后续跨事业部销售

- **具体逻辑描述**

  - 第1点：OA审批通过或流程结束时，遍历申请单所有明细行

  - 第2点：对每个产品编码，若销售清单中已存在该事业部的有效记录，则先失效旧记录

  - 第3点：写入新的销售清单记录，状态由明细类型转换：新增→有效，失效→失效

  - 第4点：审计字段（创建人、更新人、创建时间、更新时间）取申请单的审计信息

### 重点逻辑5：失效联动价目表 `价格联动`

- **业务意义**：产品失效跨事业部销售时，同步终止该产品在目标事业部价目表中的有效价格

- **具体逻辑描述**

  - 第1点：明细类型=失效时，更新目标事业部价目表中该产品当前有效价格明细行的有效结束时间为审批通过当天

  - 第2点：特殊规则——若失效事业部为箭牌卫浴事业部，则同步失效箭牌大客户事业部价目表对应产品

---

## 详细逻辑

### 界面模块1：跨事业部产品销售申请列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 申请单号 | LNK_CROSS_BU_APP_FORM.FORM_CODE | 文本框 | 申请单唯一标识 | 常显 | 1. 默认值：无 2. 来源：后端自动生成 3. 可编辑：否 4. 查询支持模糊匹配 |
| 事业部 | LNK_CROSS_BU_APP_FORM.DEPT_CODE | 下拉选择框 | 申请单归属事业部 | 常显 | 1. 默认值：当前用户事业部 2. 来源：值集AE.ITEM_ORGANIZATION 3. 可编辑：否 4. 查询支持精确匹配 |
| 状态 | LNK_CROSS_BU_APP_FORM.STATUS | 下拉选择框 | 审批状态 | 常显 | 1. 默认值：无 2. 来源：值集HWKF.APPROVE_STATUS 3. 可编辑：否 4. 查询支持精确匹配 |
| 申请说明 | LNK_CROSS_BU_APP_FORM.REMARK | 文本框 | 申请事由描述 | 常显 | 1. 默认值：无 2. 可编辑：是（编辑态） 3. 最大长度500 |
| 创建人 | LNK_CROSS_BU_APP_FORM.CREATED_BY_NAME | 文本框 | 申请单创建人姓名 | 常显 | 1. 默认值：无 2. 来源：关联HZERO.IAM_USER.REAL_NAME 3. 可编辑：否 4. 查询支持模糊匹配 |
| 创建时间 | LNK_CROSS_BU_APP_FORM.CREATION_DATE | 日期选择框 | 申请单创建时间 | 常显 | 1. 默认值：查询默认近一个月 2. 可编辑：否 3. 格式YYYY-MM-DD 4. 查询为范围查询 |
| 最后更新时间 | LNK_CROSS_BU_APP_FORM.LAST_UPDATE_DATE | 日期选择框 | 最后更新时间 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 格式YYYY-MM-DD |
| 产品编码 | LNK_CROSS_BU_APP_FORM_ITEM.PROD_CODE | 文本框 | 查询条件：按产品编码筛选含该产品的申请单 | 隐藏（仅查询条件） | 1. 查询支持模糊匹配 2. 通过子查询匹配明细行产品编码 |
| 产品名称 | LNK_PROD.PROD_NAME | 文本框 | 查询条件：按产品名称筛选 | 隐藏（仅查询条件） | 1. 查询支持模糊匹配 2. 关联LNK_PROD表查询 |
| 产品型号 | LNK_PROD.LH_PROD_MODEL | 文本框 | 查询条件：按产品型号筛选 | 隐藏（仅查询条件） | 1. 查询支持模糊匹配 2. 关联LNK_PROD表查询 |

### 界面模块2：跨事业部产品销售申请详情页-头信息

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 申请单号 | LNK_CROSS_BU_APP_FORM.FORM_CODE | 文本框/输出框 | 申请单唯一标识 | 常显 | 1. 默认值：无 2. 来源：保存时后端自动生成 3. 可编辑：否 4. 编辑态为TextField禁用，查看态为Output |
| 事业部 | LNK_CROSS_BU_APP_FORM.DEPT_CODE | 下拉选择框 | 申请单归属事业部 | 常显 | 1. 默认值：当前用户事业部 2. 来源：值集AE.ITEM_ORGANIZATION 3. 可编辑：否 |
| 状态 | LNK_CROSS_BU_APP_FORM.STATUS | 下拉选择框 | 审批状态 | 常显 | 1. 默认值：NEW（新建） 2. 来源：值集HWKF.APPROVE_STATUS 3. 可编辑：否 |
| 创建人 | LNK_CROSS_BU_APP_FORM.CREATED_BY_NAME | 输出框 | 创建人姓名 | 常显 | 1. 默认值：无 2. 来源：后端返回 3. 可编辑：否 |
| 申请说明 | LNK_CROSS_BU_APP_FORM.REMARK | 文本框 | 申请事由描述 | 常显 | 1. 默认值：无 2. 可编辑：是（编辑态） 3. 必填 4. 最大长度500，超过时前端提示但不阻断输入 |
| 创建时间 | LNK_CROSS_BU_APP_FORM.CREATION_DATE | 日期选择框 | 创建时间 | 常显 | 1. 默认值：无 2. 可编辑：否 3. 格式YYYY-MM-DD |

### 界面模块3：跨事业部产品销售申请详情页-明细行

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 产品编码 | LNK_CROSS_BU_APP_FORM_ITEM.PROD_CODE | 文本框（弹窗选择） | 明细行产品编码 | 常显 | 1. 默认值：无 2. 来源：产品选择弹窗 3. 必填 4. 新增时点击弹出产品选择弹窗，编辑时禁用 |
| 产品名称 | LNK_PROD.PROD_NAME | 文本框 | 产品名称 | 常显 | 1. 默认值：无 2. 来源：选择产品后自动带出 3. 可编辑：否 |
| 型号 | LNK_PROD.LH_PROD_MODEL | 文本框 | 产品型号 | 常显 | 1. 默认值：无 2. 来源：选择产品后自动带出 3. 可编辑：否 |
| 渠道 | LNK_PROD.LH_PROD_CHANNEL | 下拉选择框 | 产品渠道 | 常显 | 1. 默认值：无 2. 来源：值集CRM.LH_PROD_CHANNELS 3. 可编辑：否 |
| 品牌事业部 | LNK_ORG_EXT.ORG_NAME | 文本框 | 产品所属品牌事业部 | 常显 | 1. 默认值：无 2. 来源：选择产品后自动带出 3. 可编辑：否 |
| 管理分类 | LNK_MTL_CATEGORIES.ATTRIBUTE10 | 文本框 | 产品管理分类 | 常显 | 1. 默认值：无 2. 来源：关联LNK_MTL_CATEGORIES查询(STRUCTURE_ID=101) 3. 可编辑：否 |
| 类型 | LNK_CROSS_BU_APP_FORM_ITEM.ITEM_TYPE | 下拉选择框 | 明细操作类型 | 常显 | 1. 默认值：add（新增） 2. 来源：值集CRM.CROSS_BU_ITEM_TYPE 3. 必填 4. 切换为失效时清空时间字段 |
| 有效开始时间 | LNK_CROSS_BU_APP_FORM_ITEM.EFF_START_DATE | 日期选择框 | 产品跨事业部销售有效起始 | 常显 | 1. 默认值：无 2. 类型=新增时必填 3. 类型=失效时禁用且清空 4. 须大于等于今天 5. 格式YYYY-MM-DD |
| 有效结束时间 | LNK_CROSS_BU_APP_FORM_ITEM.EFF_END_DATE | 日期选择框 | 产品跨事业部销售有效截止 | 常显 | 1. 默认值：无 2. 类型=新增时必填 3. 类型=失效时禁用且清空 4. 须大于等于有效开始时间 5. 格式YYYY-MM-DD |
| 备注 | LNK_CROSS_BU_APP_FORM_ITEM.REMARK | 文本域 | 明细行备注 | 常显 | 1. 默认值：无 2. 可编辑：是 3. 最大长度250 |

### 选择弹窗

#### 弹窗1：产品选择弹窗（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| prodCode | 产品编码 | 模糊查询产品编码 | ARROW001 | 产品需满足：品牌事业部不为空（deptIdEmpty=N）且品牌事业部≠当前申请单事业部（excludeDeptCode） |
| prodName | 产品名称 | 模糊查询产品名称 | 马桶 | 同上 |
| lhProdModel | 型号 | 模糊查询产品型号 | A100 | 同上 |
| lhProdChannel | 渠道 | 精确查询渠道 | RETAIL | 同上 |
| prodStatus | 上/下架 | 精确查询上下架状态 | ACTIVE | 同上 |
| smState | 生命状态 | 精确查询产品生命周期状态 | ON_SALE | 同上 |
| prodPositioning | 产品定位 | 模糊查询产品定位 | HIGH | 同上 |

> 查询前校验：产品编码、产品名称、型号至少输入一个条件才能查询

> 查询SQL（后端接口：/v1/{orgId}/prod/cross-bu-prod）：

```sql
SELECT p.PROD_CODE, p.PROD_NAME, p.LH_PROD_MODEL, p.LH_PROD_CHANNEL, 
       p.PROD_STATUS, p.SM_STATE, p.PROD_POSITIONING, oe.ORG_NAME AS deptName, p.DEPT_ID
FROM LNK_PROD p
LEFT JOIN LNK_ORG_EXT oe ON p.DEPT_ID = oe.ROW_ID
WHERE p.DEPT_ID IS NOT NULL          -- deptIdEmpty = 'N'
  AND p.DEPT_ID != #{excludeDeptCode} -- 排除当前事业部
  -- AND p.LH_PROD_SIGN = '成品'     -- 注释中预留，当前未启用
  <if test="prodCode != null">AND p.PROD_CODE LIKE '%' || #{prodCode} || '%'</if>
  <if test="prodName != null">AND p.PROD_NAME LIKE '%' || #{prodName} || '%'</if>
  <if test="lhProdModel != null">AND p.LH_PROD_MODEL LIKE '%' || #{lhProdModel} || '%'</if>
```

### 导入

#### 前置约定

- 导入模板编码：CRM.CROSS_BU_APP_LINE
- 导入参数：headId（当前申请单ID）
- 文件格式：Excel，支持批量导入明细行

#### 字段映射

| 字段含义 | 是否必输 | 字段格式 | 重复判定字段 |
|---------|---------|---------|------------|
| 申请单号（formCode） | 是 | 文本 | 否 |
| 产品编码（prodCode） | 是 | 文本 | 是（文件内不可重复） |
| 类型（itemType） | 是 | 新增/失效 | 否 |
| 有效开始时间（effStartDate） | 类型=新增时必填 | yyyy-MM-dd | 否 |
| 有效结束时间（effEndDate） | 类型=新增时必填 | yyyy-MM-dd | 否 |

#### 处理逻辑

- **校验逻辑**：
  - 申请单号不能为空且必须存在于系统中
  - 产品编码不能为空且在文件中不可重复
  - 产品编码必须满足跨事业部产品选择条件（品牌事业部不为空且≠当前用户事业部）
  - 类型=新增时有效开始/结束时间必填，且结束时间≥开始时间
  - 日期格式必须为yyyy-MM-dd
- **导入逻辑**：批量预取已存在申请单号和有效产品编码（按999分批规避Oracle IN上限），逐行做内存校验
- **重复处理策略**：文件内产品编码重复报错
- **性能方案**：批量预查询+内存校验，避免逐行查库

#### 异常与结果约定

- 部分成功/失败时：校验失败的行返回错误明细，可导出失败明细
- 结果反馈机制：成功计数+失败明细导出

#### 运维保障

- 日志记录：HZERO导入框架标准日志
- 断点续传/重试机制：支持失败行单独重导

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新增 | 新建申请单 | 列表页 | 常显（需权限hzero.c.crm.cross-bu-app.button.create） | 跳转详情页新建态 |
| 导出 | 导出申请单列表 | 列表页 | 常显（需权限hzero.c.crm.cross-bu-app.button.export） | 调用导出接口，异步导出Excel |
| 保存 | 保存申请单头信息 | 详情页 | 编辑态显示（需权限hzero.c.crm.cross-bu-app.button.save） | 新建时生成申请单号并跳转详情页，编辑时更新头信息 |
| 取消 | 取消编辑 | 详情页 | 编辑态且已保存时显示 | 重新加载详情数据，退出编辑态 |
| 编辑 | 进入编辑态 | 详情页 | 非编辑态且状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.button.edit） | 切换为编辑态 |
| 提交 | 提交OA审批 | 详情页 | 非编辑态且状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.button.submit） | 推送OA审批，状态变为审批中 |
| 新增行 | 新增明细行 | 详情页明细行 | 头已保存且状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.detail.button.line.create） | 弹出明细行编辑弹窗 |
| 明细导入 | 批量导入明细行 | 详情页明细行 | 头已保存且状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.button.import） | 弹出导入组件 |
| 删除 | 批量删除明细行 | 详情页明细行 | 头已保存且状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.detail.button.line.delete） | 删除选中明细行 |
| 编辑（行） | 编辑单行明细 | 详情页明细行 | 状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.detail.button.line.edit） | 弹出明细行编辑弹窗 |
| 移除（行） | 删除单行明细 | 详情页明细行 | 状态可编辑时显示（需权限hzero.c.crm.cross-bu-app.detail.button.line.remove） | 删除当前明细行 |

#### 按钮1：提交（详情页）

- **触发条件**：非编辑态且状态为NEW或REJECTED时显示
- **执行逻辑**：
  - 第1点：前端校验至少存在一条明细行（通过totalCount判断全量）
  - 第2点：调用后端submit接口，后端校验状态、明细非空、明细有效时间
  - 第3点：推送OA审批流程，OA登录人取当前用户IAM_USER.attribute2
  - 第4点：更新申请单状态为RUN（审批中）
- **接口调用**：POST /v1/{orgId}/crossBuAppForm/submit

#### 按钮2：保存（详情页）

- **触发条件**：编辑态时显示
- **执行逻辑**：
  - 第1点：前端校验申请说明必填且长度不超过500字
  - 第2点：新建时后端自动生成申请单号、填充当前用户事业部
  - 第3点：新建成功后replaceTab跳转到带ID的详情页
- **接口调用**：POST /v1/{orgId}/crossBuAppForm

### 保存校验

- 校验1：申请说明必填 —— 确保申请有明确的事由说明

  - 详细逻辑

    - 第1点：前端提交保存前校验申请说明非空（trim后）

    - 第2点：后端实体注解@NotBlank校验

  - 系统体现：前端notification.error提示"请填写申请说明"

- 校验2：申请说明长度不超过500字 —— 防止超长文本

  - 详细逻辑

    - 第1点：前端校验remark.length > 500时提示

    - 第2点：后端实体注解@Size(max=500)校验

  - 系统体现：前端notification.warning提示"申请说明已超过500字"

  - 排查SQL：

    ```sql
    SELECT FORM_CODE, REMARK, LENGTH(REMARK) AS remark_len
    FROM LNK_CROSS_BU_APP_FORM
    WHERE LENGTH(REMARK) > 500;
    ```

### 提交校验

- 校验1：申请单状态必须为新建或审核拒绝 —— 仅特定状态可提交

  - 详细逻辑

    - 第1点：后端查询申请单当前状态

    - 第2点：状态不为NEW且不为REJECTED时抛出异常

  - 系统体现：后端抛出CommonException"申请单状态不为新建或审核拒绝，不可提交！"

  - 排查SQL：

    ```sql
    SELECT FORM_CODE, STATUS
    FROM LNK_CROSS_BU_APP_FORM
    WHERE ID = #{id};
    ```

- 校验2：明细行不为空 —— 确保申请有具体内容

  - 详细逻辑

    - 第1点：后端查询申请单所有明细行

    - 第2点：明细行列表为空时抛出异常

  - 系统体现：后端抛出CommonException"申请单明细为空，不允许提交！"

  - 排查SQL：

    ```sql
    SELECT COUNT(1) AS item_count
    FROM LNK_CROSS_BU_APP_FORM_ITEM
    WHERE HEAD_ID = #{id};
    ```

- 校验3：类型=新增时有效时间必填且合法 —— 确保新增产品的有效期完整有效

  - 详细逻辑

    - 第1点：遍历所有明细行，类型=add时校验有效开始时间非空

    - 第2点：类型=add时校验有效结束时间非空

    - 第3点：有效开始时间须大于等于今天

    - 第4点：有效结束时间须大于等于有效开始时间

  - 系统体现：后端抛出CommonException，提示具体产品编码及缺失字段

  - 排查SQL：

    ```sql
    SELECT PROD_CODE, ITEM_TYPE, EFF_START_DATE, EFF_END_DATE
    FROM LNK_CROSS_BU_APP_FORM_ITEM
    WHERE HEAD_ID = #{id}
      AND ITEM_TYPE = 'add'
      AND (EFF_START_DATE IS NULL OR EFF_END_DATE IS NULL 
           OR EFF_START_DATE > EFF_END_DATE
           OR EFF_START_DATE < TRUNC(SYSDATE));
    ```

### 状态机

#### 状态机流转图

```text
NEW（新建） ──提交──→ RUN（审批中） ──审批通过/结束──→ APPROVED（已审核）
                         │
                         ├──审核拒绝──→ REJECTED（审核拒绝） ──提交──→ RUN（审批中）
                         │
                         └──撤回──→ NEW（新建）
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| NEW | 新建 | 编辑、保存、提交、删除、新增行、删除行、明细导入 |
| RUN | 审批中 | 查看（不可编辑） |
| APPROVED | 已审核 | 查看（不可编辑） |
| REJECTED | 审核拒绝 | 编辑、保存、提交、新增行、删除行、明细导入 |
| WITHDRAW | 已撤回 | 恢复为NEW后可编辑、提交 |

---

## 数据库表详解

### 表1：LNK_CROSS_BU_APP_FORM（跨事业部产品销售申请单头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ID | NUMBER | 主键 | 无（隐藏） | 自增生成 |
| FORM_CODE | VARCHAR | 申请单号 | 申请单号 | 保存时通过编码规则CROSS_BU_APP_FORM_CODE自动生成 |
| STATUS | VARCHAR | 审批状态 | 状态 | 默认NEW，提交后RUN，审批通过APPROVED，拒绝REJECTED，撤回NEW |
| DEPT_CODE | VARCHAR | 事业部编码 | 事业部 | 保存时自动填充当前用户事业部编码 |
| REMARK | VARCHAR(500) | 申请说明 | 申请说明 | 用户填写，必填，最大500字 |
| OBJECT_VERSION_NUMBER | NUMBER | 乐观锁版本号 | 无 | 框架自动维护 |
| CREATED_BY | NUMBER | 创建人ID | 无 | 框架自动填充 |
| CREATION_DATE | TIMESTAMP | 创建时间 | 创建时间 | 框架自动填充 |
| LAST_UPDATED_BY | NUMBER | 最后更新人ID | 无 | 框架自动填充 |
| LAST_UPDATE_DATE | TIMESTAMP | 最后更新时间 | 最后更新时间 | 框架自动填充 |

### 表2：LNK_CROSS_BU_APP_FORM_ITEM（跨事业部产品销售申请单明细行表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ID | NUMBER | 主键 | 无（隐藏） | 自增生成 |
| HEAD_ID | NUMBER | 主表ID | 无（隐藏） | 关联LNK_CROSS_BU_APP_FORM.ID |
| PROD_CODE | VARCHAR | 产品编码 | 产品编码 | 必填，来源于产品选择弹窗 |
| BRAND_DEPT_ID | VARCHAR | 品牌事业部ID | 品牌事业部 | 必填，选择产品时自动带出 |
| ITEM_TYPE | VARCHAR | 类型 | 类型 | 必填，值集CRM.CROSS_BU_ITEM_TYPE：add(新增)/invalid(失效) |
| EFF_START_DATE | DATE | 有效开始时间 | 有效开始时间 | 类型=新增时必填，须≥今天 |
| EFF_END_DATE | DATE | 有效结束时间 | 有效结束时间 | 类型=新增时必填，须≥有效开始时间 |
| REMARK | VARCHAR(250) | 备注 | 备注 | 选填，最大250字 |
| OBJECT_VERSION_NUMBER | NUMBER | 乐观锁版本号 | 无 | 框架自动维护 |
| CREATED_BY | NUMBER | 创建人ID | 无 | 框架自动填充 |
| CREATION_DATE | TIMESTAMP | 创建时间 | 无 | 框架自动填充 |
| LAST_UPDATED_BY | NUMBER | 最后更新人ID | 无 | 框架自动填充 |
| LAST_UPDATE_DATE | TIMESTAMP | 最后更新时间 | 无 | 框架自动填充 |

### 表3：LNK_CROSS_BU_SALES_LIST（跨事业部产品销售清单表，审批通过后写入）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ID | NUMBER | 主键 | 无 | 自增生成 |
| SALES_DEPT_CODE | VARCHAR | 所需销售事业部编码 | 无 | 取申请单头DEPT_CODE |
| PROD_CODE | VARCHAR | 产品编码 | 无 | 取申请单明细PROD_CODE |
| STATUS | VARCHAR | 状态 | 无 | 值集CRM.COMMON_STATUS：明细类型add→valid，invalid→invalid |
| EFF_START_DATE | DATE | 有效开始时间 | 无 | 取申请单明细EFF_START_DATE |
| EFF_END_DATE | DATE | 有效结束时间 | 无 | 取申请单明细EFF_END_DATE |
| FORM_CODE | VARCHAR | 关联申请单号 | 无 | 取申请单头FORM_CODE |
| OBJECT_VERSION_NUMBER | NUMBER | 乐观锁版本号 | 无 | 框架自动维护 |
| CREATED_BY | NUMBER | 创建人ID | 无 | 取申请单审计字段（非当前操作人） |
| CREATION_DATE | TIMESTAMP | 创建时间 | 无 | 取申请单审计字段 |
| LAST_UPDATED_BY | NUMBER | 最后更新人ID | 无 | 取申请单审计字段 |
| LAST_UPDATE_DATE | TIMESTAMP | 最后更新时间 | 无 | 取申请单审计字段 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 申请单不存在！ | 提交 | 申请单ID在数据库中不存在，可能已被删除。重新确认申请单是否存在 | 阻断性报错 | [查看] |
| 申请单状态不为新建或审核拒绝，不可提交！ | 提交 | 申请单当前状态非NEW或REJECTED。需等待审批完成或撤回后再提交 | 阻断性报错 | [查看] |
| 申请单明细为空，不允许提交！ | 提交 | 申请单未维护明细行。先新增明细行后再提交 | 阻断性报错 | [查看] |
| 产品{prodCode}类型为新增时，有效开始时间必填！ | 提交 | 明细行类型为新增但有效开始时间为空。补充有效开始时间 | 阻断性报错 | [查看] |
| 产品{prodCode}类型为新增时，有效结束时间必填！ | 提交 | 明细行类型为新增但有效结束时间为空。补充有效结束时间 | 阻断性报错 | [查看] |
| 产品{prodCode}的有效结束时间必须大于等于有效开始时间！ | 提交 | 有效结束时间早于有效开始时间。修正时间范围 | 阻断性报错 | [查看] |
| 产品{prodCode}类型为新增时，有效开始时间必须大于等于今天！ | 提交 | 有效开始时间早于当前日期。修改为今天或未来日期 | 阻断性报错 | [查看] |
| 跨事业部产品销售申请单{formCode}不存在！ | OA审批回调 | OA回调时申请单已被删除。检查数据一致性 | 阻断性报错 | [查看] |
| 申请单事业部为空，无法查询产品！ | 新增明细行 | 申请单头事业部为空。检查用户是否已关联事业部 | 阻断性报错 | [查看] |
| 请至少输入"产品编码、产品名称、型号"中的一个条件再进行查询！ | 产品选择弹窗 | 未输入任何查询条件就点击查询。输入至少一个条件 | toast提醒 | [查看] |
| 请填写申请说明！ | 保存按钮 | 申请说明为空或纯空格。填写申请说明 | 阻断性报错 | [查看] |
| 申请说明已超过500字 | 保存按钮 | 申请说明长度超过500。精简内容 | toast提醒 | [查看] |
| 请检查表单必填项！ | 保存按钮 | 表单校验未通过（必填字段缺失） | 阻断性报错 | [查看] |
| 请先保存申请单头信息！ | 提交按钮 | 新建态下未保存头信息就点击提交 | 阻断性报错 | [查看] |
| 请至少添加一条明细行！ | 提交按钮 | 明细行totalCount为0时提交 | toast提醒 | [查看] |
| 产品{prodCode}的类型不合法，仅支持新增/失效！ | 明细行保存 | itemType既非add也非invalid | 阻断性报错 | [查看] |
| 产品编码不能为空！ | 明细行保存 | 明细行prodCode为空 | 阻断性报错 | [查看] |
| 产品{prodCode}的申请单头ID不能为空！ | 明细行保存 | 明细行headId为空 | 阻断性报错 | [查看] |
| 产品编码【{prodCode}】在申请单明细中重复！ | 明细行保存 | 同一申请单内新增行产品编码重复 | 阻断性报错 | [查看] |
| 产品编码【{prodCode}】在当前申请单明细中已存在，不允许重复导入！ | 明细行保存 | 数据库中同一headId+prodCode已存在 | 阻断性报错 | [查看] |
| 产品编码"{prodCode}"不存在！ | 明细行保存 | 产品主档LNK_PROD中无此产品编码 | 阻断性报错 | [查看] |
| 产品{prodCode}品牌事业部为空，不允许导入！ | 明细行保存 | 产品的DEPT_ID为空 | 阻断性报错 | [查看] |
| 申请单头信息不存在！ | 明细行保存 | 按headId查不到申请单头 | 阻断性报错 | [查看] |
| 未找到事业部编码[{deptCode}]对应的CRM事业部！ | 明细行保存 | REL_CRM_AE_ORG中无对应映射 | 阻断性报错 | [查看] |
| 产品{prodCode}所属品牌事业部与申请单事业部相同，不允许导入！ | 明细行保存 | 产品事业部=申请单事业部 | 阻断性报错 | [查看] |
| 产品{prodCode}品牌事业部不匹配！ | 明细行保存 | 传入brandDeptId与产品实际DEPT_ID不一致 | 阻断性报错 | [查看] |
| 有效开始时间必须大于等于今天 | 明细行编辑弹窗 | 类型=新增时开始时间早于今天 | 阻断性报错 | [查看] |
| 有效结束时间必须大于等于有效开始时间 | 明细行编辑弹窗 | 结束时间早于开始时间 | 阻断性报错 | [查看] |
| 保存失败 | 保存按钮 | 后端返回failed或业务校验异常 | toast提醒 | [查看] |
| 查询失败 | 详情加载 | 详情接口请求异常 | toast提醒 | [查看] |
| 提交失败 | 提交按钮 | 后端submit接口返回failed | toast提醒 | [查看] |
| 新增/编辑失败 | 明细行弹窗确认 | 明细行保存接口返回failed | toast提醒 | [查看] |
| 请选择要删除的明细 | 删除按钮 | 未选中任何明细行就点击删除 | toast提醒 | [查看] |
| 删除失败 | 删除按钮 | 明细行删除接口返回failed | toast提醒 | [查看] |
| 是否移除当前明细？/是否删除选中明细？ | 删除/移除按钮 | 删除前确认弹窗 | 确认弹窗 | [查看] |
| 权限不足 | 页面访问/按钮操作 | 当前用户无对应权限编码 | 阻断性报错 | [查看] |
| 暂无数据 | 列表查询 | 查询结果为空集 | toast提醒 | [查看] |
| 会话过期 | 任意操作 | 登录态失效或Token过期 | 阻断性报错 | [查看] |

#### 报错1：申请单不存在！
- **触发条件**：提交时，申请单ID在LNK_CROSS_BU_APP_FORM表中不存在
- **逻辑分析**：后端submit接口按ID查询申请单头表，若记录已被删除则抛出异常。常见于并发场景：提交时申请单已被他人删除。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.STATUS AS 状态,
         F.LAST_UPDATED_BY, F.LAST_UPDATE_DATE
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错2：申请单状态不为新建或审核拒绝，不可提交！
- **触发条件**：提交时，申请单当前状态非NEW且非REJECTED
- **逻辑分析**：后端校验申请单状态，仅NEW（新建）和REJECTED（审核拒绝）可提交。RUN（审批中）、APPROVED（已审核）状态提交会抛出CommonException。常见于并发提交或前端状态未刷新。
- **排查SQL**：
  ```sql
  SELECT F.FORM_CODE AS 申请单号, F.STATUS AS 状态,
         F.LAST_UPDATE_DATE AS 最后更新时间
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错3：申请单明细为空，不允许提交！
- **触发条件**：提交时，申请单未维护任何明细行
- **逻辑分析**：后端按HEAD_ID查询LNK_CROSS_BU_APP_FORM_ITEM表，若COUNT为0则抛出异常。常见于用户新建申请单后未添加产品明细就点击提交。
- **排查SQL**：
  ```sql
  SELECT F.FORM_CODE AS 申请单号, F.STATUS AS 状态,
         (SELECT COUNT(1) FROM LNK_CROSS_BU_APP_FORM_ITEM I WHERE I.HEAD_ID = F.ID) AS 明细行数
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错4：产品{prodCode}类型为新增时，有效开始时间必填！
- **触发条件**：提交时，明细行ITEM_TYPE='add'但EFF_START_DATE为空
- **逻辑分析**：后端遍历所有明细行，类型为新增（add）时校验有效开始时间非空。新增类型产品需明确跨事业部销售的生效起始日期。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型,
         I.EFF_START_DATE AS 有效开始时间, I.EFF_END_DATE AS 有效结束时间
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :formId
    AND I.ITEM_TYPE = 'add'
    AND I.EFF_START_DATE IS NULL;
  ```

#### 报错5：产品{prodCode}类型为新增时，有效结束时间必填！
- **触发条件**：提交时，明细行ITEM_TYPE='add'但EFF_END_DATE为空
- **逻辑分析**：后端遍历所有明细行，类型为新增（add）时校验有效结束时间非空。新增类型产品需明确跨事业部销售的生效截止日期。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型,
         I.EFF_START_DATE AS 有效开始时间, I.EFF_END_DATE AS 有效结束时间
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :formId
    AND I.ITEM_TYPE = 'add'
    AND I.EFF_END_DATE IS NULL;
  ```

#### 报错6：产品{prodCode}的有效结束时间必须大于等于有效开始时间！
- **触发条件**：提交时，明细行EFF_END_DATE早于EFF_START_DATE
- **逻辑分析**：后端校验有效时间范围合法性，有效结束时间须大于等于有效开始时间。常见于用户手动输入日期顺序错误。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型,
         I.EFF_START_DATE AS 有效开始时间, I.EFF_END_DATE AS 有效结束时间
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :formId
    AND I.EFF_END_DATE < I.EFF_START_DATE;
  ```

#### 报错7：产品{prodCode}类型为新增时，有效开始时间必须大于等于今天！
- **触发条件**：提交时，明细行ITEM_TYPE='add'但EFF_START_DATE早于当前日期
- **逻辑分析**：后端校验新增类型产品的有效开始时间须大于等于TRUNC(SYSDATE)，不允许配置过去日期生效。常见于用户误选历史日期。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.EFF_START_DATE AS 有效开始时间,
         TRUNC(SYSDATE) AS 当前日期
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :formId
    AND I.ITEM_TYPE = 'add'
    AND TRUNC(I.EFF_START_DATE) < TRUNC(SYSDATE);
  ```

#### 报错8：跨事业部产品销售申请单{formCode}不存在！
- **触发条件**：OA审批回调时，根据formCode查询申请单不存在
- **逻辑分析**：OA审批完成后回调DMS，按FORM_CODE查询LNK_CROSS_BU_APP_FORM更新状态。若记录已被删除则回调失败，抛出异常。常见于OA审批过程中申请单被删除。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.STATUS AS 状态,
         F.HZ_APPROVE_STATUS AS 审批状态
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.FORM_CODE = :formCode;
  ```

#### 报错9：申请单事业部为空，无法查询产品！
- **触发条件**：新增明细行打开产品选择弹窗时，申请单头事业部DEPT_CODE为空
- **逻辑分析**：产品选择弹窗需排除当前申请单事业部（excludeDeptCode），若头表DEPT_CODE为空则无法过滤，抛出异常。常见于新建保存时未正确填充当前用户事业部。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.DEPT_CODE AS 事业部编码,
         U.REAL_NAME AS 创建人
  FROM LNK_CROSS_BU_APP_FORM F
    LEFT JOIN HZERO.IAM_USER U ON U.ID = F.CREATED_BY
  WHERE F.ID = :formId;
  ```

#### 报错10：请至少输入"产品编码、产品名称、型号"中的一个条件再进行查询！
- **触发条件**：产品选择弹窗未输入任何查询条件就点击查询
- **逻辑分析**：产品选择接口/v1/{orgId}/prod/cross-bu-prod要求至少传入prodCode、prodName、lhProdModel中的一个，防止全表扫描。前端校验未通过则提示。
- **排查SQL**：
  ```sql
  SELECT '产品选择弹窗需至少输入产品编码/产品名称/型号其中一个' AS 提示 FROM DUAL;
  ```

#### 报错11：请填写申请说明！
- **触发条件**：保存按钮点击时，申请说明（remark）为空或纯空格
- **逻辑分析**：前端DetailPage/index.tsx中validHead方法，校验remark.trim()为空时notification.error提示"请填写申请说明！"。申请说明是申请单的必填字段，用于描述申请事由。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.REMARK AS 申请说明
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId AND (F.REMARK IS NULL OR TRIM(F.REMARK) = '');
  ```

#### 报错12：申请说明已超过500字
- **触发条件**：保存按钮点击时，申请说明长度超过500字
- **逻辑分析**：前端validHead方法校验remark.length > 500时notification.warning提示"申请说明已超过500字（当前${length}字），请精简后再保存。"。后端实体注解@Size(max=500)同步校验。
- **排查SQL**：
  ```sql
  SELECT F.FORM_CODE AS 申请单号, F.REMARK AS 申请说明, LENGTH(F.REMARK) AS 备注
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE LENGTH(F.REMARK) > 500;
  ```

#### 报错13：请检查表单必填项！
- **触发条件**：保存按钮点击时，headDs.validate()返回false
- **逻辑分析**：前端validHead方法在remark校验通过后调用headDs.validate()校验表单其他必填字段，未通过则notification.error提示"请检查表单必填项！"。常见于事业部、状态等字段缺失。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.DEPT_CODE AS 事业部, F.STATUS AS 状态
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错14：请先保存申请单头信息！
- **触发条件**：提交按钮点击时，当前页面无id（新建态未保存）
- **逻辑分析**：前端handleSubmit方法，当id为空时notification.error提示"请先保存申请单头信息！"。新建申请单必须先保存生成申请单号后才能提交。
- **排查SQL**：
  ```sql
  SELECT '前端校验：新建态下id为空，需先调用保存接口' AS 提示 FROM DUAL;
  ```

#### 报错15：请至少添加一条明细行！
- **触发条件**：提交按钮点击时，lineDs.totalCount为0
- **逻辑分析**：前端handleSubmit方法，通过lineDs.totalCount判断全量明细行数（避免分页后前端仅持有当前页），为0时notification.warning提示"请至少添加一条明细行！"。后端submit接口也会做二次校验。
- **排查SQL**：
  ```sql
  SELECT F.FORM_CODE AS 申请单号,
         (SELECT COUNT(1) FROM LNK_CROSS_BU_APP_FORM_ITEM I WHERE I.HEAD_ID = F.ID) AS 明细行数
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错16：产品{prodCode}的类型不合法，仅支持新增/失效！
- **触发条件**：明细行保存时，itemType既非add也非invalid
- **逻辑分析**：后端LnkCrossBuAppFormItemServiceImpl.saveData方法校验itemType是否为CrossBuItemTypeEnum.ADD或EXPIRE，不合法则抛出CommonException。常见于前端传入错误枚举值或数据被篡改。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId
    AND I.ITEM_TYPE NOT IN ('add', 'invalid');
  ```

#### 报错17：产品编码不能为空！
- **触发条件**：明细行保存时，prodCode为空
- **逻辑分析**：后端LnkCrossBuAppFormItemServiceImpl.saveData方法校验prodCode非空，为空则抛出CommonException。产品编码是明细行的核心标识，必填。
- **排查SQL**：
  ```sql
  SELECT I.ID, I.HEAD_ID, I.PROD_CODE AS 产品编码
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId AND I.PROD_CODE IS NULL;
  ```

#### 报错18：产品{prodCode}的申请单头ID不能为空！
- **触发条件**：明细行保存时，headId为空
- **逻辑分析**：后端LnkCrossBuAppFormItemServiceImpl.saveData方法校验headId非空，为空则抛出CommonException。常见于前端未正确关联头表ID。
- **排查SQL**：
  ```sql
  SELECT I.ID, I.HEAD_ID, I.PROD_CODE AS 产品编码
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.PROD_CODE = :prodCode AND I.HEAD_ID IS NULL;
  ```

#### 报错19：产品编码【{prodCode}】在申请单明细中重复！
- **触发条件**：明细行批量保存时，同一批次内存在相同headId+prodCode的记录
- **逻辑分析**：后端LnkCrossBuAppFormItemServiceImpl.saveData方法中，使用HashSet校验同一批次新增行的headId+prodCode唯一性，重复则抛出CommonException。防止前端重复提交。
- **排查SQL**：
  ```sql
  SELECT I.HEAD_ID, I.PROD_CODE AS 产品编码, COUNT(*) AS 重复数
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId
  GROUP BY I.HEAD_ID, I.PROD_CODE
  HAVING COUNT(*) > 1;
  ```

#### 报错20：产品编码【{prodCode}】在当前申请单明细中已存在，不允许重复导入！
- **触发条件**：明细行保存时，数据库中同一headId+prodCode已存在记录
- **逻辑分析**：后端validateProdNotExists方法查询LNK_CROSS_BU_APP_FORM_ITEM表，若同一headId+prodCode已存在则抛出CommonException。与报错19的区别在于此校验针对数据库已存在记录，报错19针对同一批次内重复。
- **排查SQL**：
  ```sql
  SELECT I.HEAD_ID, I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型, I.STATUS AS 状态
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId AND I.PROD_CODE = :prodCode;
  ```

#### 报错21：产品编码"{prodCode}"不存在！
- **触发条件**：明细行保存时，产品主档LNK_PROD中无此产品编码
- **逻辑分析**：后端fillBrandDeptId方法按prodCode查询LNK_PROD表，若记录不存在则抛出CommonException。常见于产品编码输入错误或产品尚未在系统中维护。
- **排查SQL**：
  ```sql
  SELECT P.PROD_CODE AS 产品编码, P.PROD_NAME AS 产品名称, P.DEPT_ID AS 品牌事业部ID
  FROM LNK_PROD P
  WHERE P.PROD_CODE = :prodCode;
  ```

#### 报错22：产品{prodCode}品牌事业部为空，不允许导入！
- **触发条件**：明细行保存时，产品的DEPT_ID为空
- **逻辑分析**：后端fillBrandDeptId方法校验prod.getDeptId()非空，为空则抛出CommonException。跨事业部销售要求产品必须已维护品牌事业部。
- **排查SQL**：
  ```sql
  SELECT P.PROD_CODE AS 产品编码, P.PROD_NAME AS 产品名称, P.DEPT_ID AS 品牌事业部ID
  FROM LNK_PROD P
  WHERE P.PROD_CODE = :prodCode AND P.DEPT_ID IS NULL;
  ```

#### 报错23：申请单头信息不存在！
- **触发条件**：明细行保存时，按headId查不到申请单头记录
- **逻辑分析**：后端validateBrandDept方法按item.getHeadId()查询LNK_CROSS_BU_APP_FORM，若记录不存在则抛出CommonException。常见于申请单头被删除后仍操作明细行。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.DEPT_CODE AS 事业部, F.STATUS AS 状态
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :headId;
  ```

#### 报错24：未找到事业部编码[{deptCode}]对应的CRM事业部！
- **触发条件**：明细行保存时，REL_CRM_AE_ORG中无对应事业部映射
- **逻辑分析**：后端validateBrandDept方法按form.getDeptCode()查询REL_CRM_AE_ORG，若记录不存在或crmOrgId为空则抛出CommonException。确保事业部编码在CRM中有对应映射。
- **排查SQL**：
  ```sql
  SELECT R.AE_ORG_CODE AS AE事业部编码, R.CRM_ORG_ID AS CRM事业部ID
  FROM REL_CRM_AE_ORG R
  WHERE R.AE_ORG_CODE = :deptCode;
  ```

#### 报错25：产品{prodCode}所属品牌事业部与申请单事业部相同，不允许导入！
- **触发条件**：明细行保存时，产品的品牌事业部等于申请单事业部
- **逻辑分析**：后端validateBrandDept方法校验prodDeptId不等于rel.getCrmOrgId()（申请单事业部对应的CRM事业部），相同则抛出CommonException。跨事业部销售申请要求产品事业部与申请单事业部不同。
- **排查SQL**：
  ```sql
  SELECT P.PROD_CODE AS 产品编码, P.DEPT_ID AS 产品事业部,
         R.CRM_ORG_ID AS 申请单CRM事业部
  FROM LNK_PROD P, REL_CRM_AE_ORG R
  WHERE P.PROD_CODE = :prodCode
    AND R.AE_ORG_CODE = :deptCode
    AND P.DEPT_ID = R.CRM_ORG_ID;
  ```

#### 报错26：产品{prodCode}品牌事业部不匹配！
- **触发条件**：明细行保存时，传入的brandDeptId与产品实际DEPT_ID不一致
- **逻辑分析**：后端validateBrandDept方法校验item.getBrandDeptId()与prod.getDeptId()一致，不一致则抛出CommonException。防止前端传入错误的品牌事业部ID。
- **排查SQL**：
  ```sql
  SELECT P.PROD_CODE AS 产品编码, P.DEPT_ID AS 产品实际事业部
  FROM LNK_PROD P
  WHERE P.PROD_CODE = :prodCode;
  ```

#### 报错27：有效开始时间必须大于等于今天
- **触发条件**：明细行编辑弹窗中，类型=新增时有效开始时间早于今天
- **逻辑分析**：前端DetailPage/index.tsx中effStartDate字段的validator校验，当itemType为ADD且moment(value).isBefore(moment().startOf('day'), 'day')时返回错误信息。后端submit接口和saveData方法同步校验。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.EFF_START_DATE AS 有效开始时间, TRUNC(SYSDATE) AS 当前日期
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId
    AND I.ITEM_TYPE = 'add'
    AND TRUNC(I.EFF_START_DATE) < TRUNC(SYSDATE);
  ```

#### 报错28：有效结束时间必须大于等于有效开始时间
- **触发条件**：明细行编辑弹窗中，有效结束时间早于有效开始时间
- **逻辑分析**：前端DetailPage/index.tsx中effEndDate字段的validator校验，当itemType为ADD且moment(value).isBefore(moment(start), 'day')时返回错误信息。后端submit接口和saveData方法同步校验。
- **排查SQL**：
  ```sql
  SELECT I.PROD_CODE AS 产品编码, I.EFF_START_DATE AS 有效开始时间, I.EFF_END_DATE AS 有效结束时间
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId
    AND I.EFF_END_DATE < I.EFF_START_DATE;
  ```

#### 报错29：保存失败
- **触发条件**：保存按钮点击时，后端接口返回res.failed=true
- **逻辑分析**：前端handleSave方法调用crossBuAppFormApi.save后，若res.failed为true则通过commonFn_showErrMsg展示后端错误信息。常见于后端校验异常被前端捕获。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.STATUS AS 状态, F.REMARK AS 申请说明
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错30：查询失败
- **触发条件**：详情页加载时，detail接口请求异常
- **逻辑分析**：前端fetchDetail方法调用crossBuAppFormApi.detail，若res.failed为true则通过commonFn_showErrMsg展示"查询失败"。常见于申请单ID不存在或数据库异常。
- **排查SQL**：
  ```sql
  SELECT COUNT(1) AS 申请单数 FROM LNK_CROSS_BU_APP_FORM WHERE ID = :formId;
  ```

#### 报错31：提交失败
- **触发条件**：提交按钮点击时，后端submit接口返回res.failed=true
- **逻辑分析**：前端handleSubmit方法调用crossBuAppFormApi.submit后，若res.failed为true则通过commonFn_showErrMsg展示"提交失败"。常见于后端校验异常（状态不合法、明细为空、时间校验失败）或OA推送失败。
- **排查SQL**：
  ```sql
  SELECT F.ID, F.FORM_CODE AS 申请单号, F.STATUS AS 状态,
         (SELECT COUNT(1) FROM LNK_CROSS_BU_APP_FORM_ITEM I WHERE I.HEAD_ID = F.ID) AS 明细行数
  FROM LNK_CROSS_BU_APP_FORM F
  WHERE F.ID = :formId;
  ```

#### 报错32：新增/编辑失败
- **触发条件**：明细行弹窗点击确认时，明细行保存接口返回res.failed=true
- **逻辑分析**：前端handleAddLine中弹窗确认按钮调用crossBuAppFormItemApi.save后，若res.failed为true则通过commonFn_showErrMsg展示"新增失败"或"编辑失败"。常见于产品校验失败、重复校验失败。
- **排查SQL**：
  ```sql
  SELECT I.HEAD_ID, I.PROD_CODE AS 产品编码, I.ITEM_TYPE AS 类型,
         I.EFF_START_DATE AS 有效开始时间, I.EFF_END_DATE AS 有效结束时间
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId;
  ```

#### 报错33：请选择要删除的明细
- **触发条件**：删除按钮点击时，未选中任何明细行
- **逻辑分析**：前端handleLineDelete方法，当lineDs.selected.length为0时notification.warning提示"请选择要删除的明细"。属于前端预校验，无需请求后端。
- **排查SQL**：
  ```sql
  SELECT '前端校验：未选中明细行' AS 提示 FROM DUAL;
  ```

#### 报错34：删除失败
- **触发条件**：删除明细行时，后端删除接口返回res.failed=true
- **逻辑分析**：前端handleLineDelete方法调用crossBuAppFormItemApi.remove后，若res.failed为true则通过commonFn_showErrMsg展示"删除失败"。常见于明细行已被删除或数据库异常。
- **排查SQL**：
  ```sql
  SELECT I.ID, I.HEAD_ID, I.PROD_CODE AS 产品编码
  FROM LNK_CROSS_BU_APP_FORM_ITEM I
  WHERE I.HEAD_ID = :headId;
  ```

#### 报错35：是否移除当前明细？/是否删除选中明细？
- **触发条件**：点击行移除按钮或批量删除按钮时
- **逻辑分析**：前端handleLineDelete方法中Modal.confirm弹出确认框，单行删除提示"是否移除当前明细？"，批量删除提示"是否删除选中明细？"。用户确认后调用删除接口。
- **排查SQL**：
  ```sql
  SELECT '前端确认弹窗，无需SQL排查' AS 提示 FROM DUAL;
  ```

#### 报错36：权限不足
- **触发条件**：用户访问页面或点击按钮时，未拥有对应权限编码
- **逻辑分析**：前端Button组件通过permissionList配置权限编码（如hzero.c.crm.cross-bu-app.button.create/save/edit/submit/export/import等），HZERO平台校验当前用户角色是否包含该权限编码。
- **排查SQL**：
  ```sql
  SELECT U.REAL_NAME AS 用户名, R.NAME AS 角色名, P.CODE AS 权限编码
  FROM HZERO.IAM_USER U
    JOIN HZERO.IAM_MEMBER M ON U.ID = M.MEMBER_ID
    JOIN HZERO.IAM_ROLE R ON M.ROLE_ID = R.ID
    JOIN HZERO.IAM_ROLE_PERMISSION RP ON R.ID = RP.ROLE_ID
    JOIN HZERO.IAM_PERMISSION P ON RP.PERMISSION_ID = P.ID
  WHERE P.CODE LIKE 'hzero.c.crm.cross-bu-app%';
  ```

#### 报错37：暂无数据
- **触发条件**：列表页查询结果为空集
- **逻辑分析**：前端Table组件查询后端返回content为空数组时，自动展示"暂无数据"占位。属于正常业务场景，非异常。
- **排查SQL**：
  ```sql
  SELECT COUNT(1) AS 申请单数 FROM LNK_CROSS_BU_APP_FORM WHERE STATUS = 'NEW';
  ```

#### 报错38：会话过期
- **触发条件**：任意操作时，登录态失效或Token过期
- **逻辑分析**：HZERO平台网关层校验请求头中的Authorization Token，若Token过期或无效，返回401状态码，前端拦截器跳转登录页。常见于长时间未操作或单点登录会话超时。
- **排查SQL**：
  ```sql
  SELECT '检查HZERO.IAM_USER_TOKEN表或SSO会话状态' AS 排查方向 FROM DUAL;
  ```

### 常见问题

- 问题1：提交后OA审批流程未发起
  - 原因：OA推送服务异常或当前用户IAM_USER.attribute2未配置OA账号。排查SQL：

    ```sql
    SELECT u.ID, u.REAL_NAME, u.ATTRIBUTE2
    FROM HZERO.IAM_USER u
    WHERE u.ID = #{当前用户ID};
    ```

  - 解决思路：确认用户attribute2已配置OA登录账号，检查OA服务连通性

- 问题2：审批通过后销售清单未生成
  - 原因：申请单明细行为空或OA回调时申请单formCode不匹配。排查SQL：

    ```sql
    SELECT f.FORM_CODE, f.STATUS, COUNT(i.ID) AS item_count
    FROM LNK_CROSS_BU_APP_FORM f
    LEFT JOIN LNK_CROSS_BU_APP_FORM_ITEM i ON i.HEAD_ID = f.ID
    WHERE f.FORM_CODE = #{formCode}
    GROUP BY f.FORM_CODE, f.STATUS;
    ```

  - 解决思路：确认申请单状态为APPROVED且明细行存在，检查OA回调日志

- 问题3：产品选择弹窗中查不到产品
  - 原因：产品的品牌事业部为空或品牌事业部等于当前申请单事业部。排查SQL：

    ```sql
    SELECT p.PROD_CODE, p.PROD_NAME, p.DEPT_ID
    FROM LNK_PROD p
    WHERE p.PROD_CODE = #{prodCode}
      AND (p.DEPT_ID IS NULL OR p.DEPT_ID = #{当前事业部});
    ```

  - 解决思路：确认产品已维护品牌事业部且品牌事业部≠当前申请单事业部

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-29 | - | - | 按skill规范完整重写，基于前后端代码梳理 |
| 2026-06-06 | - | - | 初始创建：跨事业部产品销售申请功能开发 |
