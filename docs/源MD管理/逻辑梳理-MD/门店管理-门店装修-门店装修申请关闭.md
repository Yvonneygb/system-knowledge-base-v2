# 门店管理-门店装修-门店装修申请关闭

## 业务流程

```
门店装修申请关闭列表页(hlod低代码页面)
        │
        ├── 新增 ──→ 选择装修申请单(LOV,仅APPROVED状态) ──→ 联动带出门店/经销商/装修信息
        │              ──→ 填写关闭申请理由 ──→ 计算扣除金额(do-cal-deduction-amt)
        │                                    │
        │                                    ▼
        │                              保存 ──→ 生成关闭单号 ──→ HZ_APPROVE_STATUS=NEW
        │                                    │
        │                                    ▼
        │                              提交审批 ──→ 校验验收报销单状态 ──→ 启动OA工作流(FIN_FEE_APPLY_CLOSE)
        │                                    │
        │                                    ▼
        │                              OA审批流程中
        │                                    │
        │                         ├──审批通过──→ HZ_APPROVE_STATUS=APPROVED
        │                         │              ├─ 原申请单标记IS_CLOSE=2, AUDIT_STAT=已关闭
        │                         │              ├─ 原申请单HZ_APPROVE_STATUS=INTERRUPT
        │                         │              └─ 扣减广告费/资金池额度
        │                         │
        │                         └──审批驳回──→ HZ_APPROVE_STATUS=REJECTED
        │
        └── 删除 ──→ 仅NEW状态可删除
```

## 重点逻辑

### 重点逻辑1：扣除金额计算

- **业务意义**：关闭装修申请时按装修面积和公司参数计算需扣减的总额，并分配到广告费和资金池
- **具体逻辑描述**：
  - 扣减总额 = (本次装修面积 + 本次门头面积) × 公司参数Close_Amount
  - 查询广告费可用余额(queryResourceAmt)，余额≤0时取0
  - 广告费扣减额度 = min(扣减总额, 广告费可用余额)
  - 资金池扣减额度 = 扣减总额 - 广告费扣减额度

### 重点逻辑2：审批通过关闭原申请单

- **业务意义**：审批通过后标记原装修申请单为已关闭，阻止后续业务引用
- **具体逻辑描述**：
  - 更新原申请单FinFeeApplyFinishedHeader: isClose=2, auditStat=已关闭, hzApproveStatus=INTERRUPT
  - 记录关闭原因到原申请单applyCause
  - 调用deductFinByAdvOrCapital扣减广告费和资金池额度

### 重点逻辑3：提交前验收单校验

- **业务意义**：已发起验收报销的申请单不允许关闭，防止业务冲突
- **具体逻辑描述**：
  - 提交时查询关联的FinFeeCheckBxHeader(验收报销单)
  - 若存在非INTERRUPT状态的验收报销单，抛异常阻止提交

## 详细逻辑

### 界面模块

本页面为前端React页面，位于`arrow-ae/src/pages/storeManage/finFeeApplyChangeClose/`，含详情页和列表页(hlod低代码页面)。

#### 头部信息区(headDS)

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 关闭单号 | APPLY_CLOSE_NO | TextField | 关闭单号 | 始终 | 编码规则AE.FIN_FEE_APPLY_CLOSE生成 |
| 申请日期 | CREATE_TIME | TextField | 申请日期 | 始终 | 默认当前时间，disabled |
| 申请人 | CREATOR | TextField | 申请人 | 始终 | 默认当前用户，disabled |
| 审核状态 | HZ_APPROVE_STATUS | Select(HWKF.APPROVE_STATUS) | 审核状态 | 始终 | 默认NEW，disabled |
| 装修申请单号 | TERMINAL_APPLY_NO | Lov(AE.STORE_FINFEEAPPLYCLOSE_DATA_CHANGE_VIEW) | 选择装修申请单 | 始终 | 选择后联动带出门店/经销商/装修信息，必填 |
| 门店编码 | TERMINAL_CODE | Lov(AE.STORE_FINFEEAPPLYCLOSE_DATA_VIEW) | 门店编码 | 始终 | 从申请单LOV带入，必填 |
| 门店名称 | TERMINAL_NAME | TextField | 门店名称 | 始终 | 从申请单LOV带入，必填 |
| 关闭申请理由 | APPLY_CAUSE | TextField | 关闭申请理由 | 始终 | 用户输入，必填 |
| 经销商编码 | CUST_CODE | TextField | 经销商编码 | 始终 | 从申请单LOV带入 |
| 经销商名称 | CUST_NAME | TextField | 经销商名称 | 始终 | 从申请单LOV带入，必填 |
| 经销商简称 | SHORT_NAME | TextField | 经销商简称 | 始终 | 从申请单LOV带入，必填 |
| 门店详细地址 | ADDR | TextField | 门店详细地址 | 始终 | 从申请单LOV带入，必填 |
| 产权归属 | PROPERTY_TYPE | Select(AE.PROPERTY_TYPE) | 产权归属 | 始终 | 从申请单LOV带入 |
| 装修等级 | FIXUP_GRADE | Select(AE.FIXUP_GRADE) | 装修等级 | 始终 | 从申请单LOV带入 |
| 装修性质 | DECORATION_TYPE | Select(AE.DECORATION_TYPE) | 装修性质 | 始终 | 从申请单LOV带入 |
| 门店类型 | TERMINAL_TYPE | Select(AE.TERMINAL_TYPE) | 门店类型 | 始终 | 从申请单LOV带入，必填 |
| 经营属性 | CUSTOMER_CLASS | Select(AE.CUSTOMER_CLASS) | 经营属性 | 始终 | 从申请单LOV带入，必填 |
| 门店面积 | TERMINAL_AREA | NumberField | 门店面积 | 始终 | 从申请单LOV带入 |
| 本次装修风格 | THIS_DECORATION_STYLE | Select(AE.DECORATION_STYLE) | 本次装修风格 | 始终 | 从申请单LOV带入，必填 |
| 门店所在地 | AREANAME | Cascader | 省/市/区 | 始终 | 从申请单LOV带入，必填 |
| 扣减总金额 | DEDUCTION_SUM_AMOUNT | NumberField | 扣减总金额 | 始终 | do-cal-deduction-amt接口计算 |
| 广告费扣减额度 | DEDUCTION_ADV_AMOUNT | NumberField | 广告费扣减额度 | 始终 | 系统计算 |
| 资金池扣减额度 | DEDUCTION_CAPITAL_AMOUNT | NumberField | 资金池扣减额度 | 始终 | 系统计算 |
| 备注 | NOTE | TextField | 备注 | 始终 | 用户输入 |
| 店面实际装修面积 | THIS_TERMINAL_AREA | NumberField | 店面实际装修面积 | 始终 | 从申请单LOV带入，必填 |
| 面积分配标准比 | AREA_STANDARD_RATE | NumberField | 面积分配标准比 | 始终 | 必填 |
| 装修周期 | DECORATION_DAYS | NumberField | 装修天数 | 始终 | 从申请单LOV带入，必填 |
| 预算年度 | BUD_YEAR | TextField | 预算年度 | 始终 | 从申请单LOV带入，必填，只读 |
| 负责人 | SHOPMANAGER_NAME | TextField | 门店负责人 | 始终 | 从申请单LOV带入 |
| 负责人电话 | SHOPMANAGER_MOB | TextField | 负责人电话 | 始终 | 从申请单LOV带入 |
| 所属分销商 | D_CUST_NAME | TextField | 所属分销商名称 | 始终 | 从申请单LOV带入，只读 |
| 组织名称 | ENT_NAME | TextField | 组织名称 | 始终 | 从申请单LOV带入 |
| 位置类型 | STORE_LOCATION_TYPE | Select(AE.STORE_LOCATION_TYPE) | 位置类型 | 始终 | 从申请单LOV带入 |
| 最新装修日期 | LATEST_DECORATION_DATE | DatePicker | 最新装修日期 | 始终 | 从申请单LOV带入 |
| 店面租赁到期日 | LEASE_EXPIRATION_DATE | DatePicker | 店面租赁到期日 | 始终 | 从申请单LOV带入 |
| 装修风格 | DECORATION_STYLE | Select(AE.DECORATION_STYLE) | 装修风格 | 始终 | 从申请单LOV带入 |
| 所属销售区域 | SALEZONE_ORG_NAME | TextField | 所属销售区域 | 始终 | 从申请单LOV带入，必填 |
| 所属运营中心 | OPERAT_CENTER_ORG_NAME | TextField | 所属运营中心 | 始终 | 从申请单LOV带入 |
| 导购员数量 | GUIDE_COUNT | NumberField | 导购员数量 | 始终 | 从申请单LOV带入 |
| 服务工程师数量 | SERVICE_ENGINEER_COUNT | NumberField | 服务工程师数量 | 始终 | 从申请单LOV带入 |
| 连锁商场编码 | SYS_CODE | TextField | 连锁商场编码 | 始终 | 从申请单LOV带入 |
| 是否连锁 | IS_LS | NumberField | 是否连锁 | 始终 | 从申请单LOV带入 |
| 品牌 | BRAND | TextField | 品牌 | 始终 | 从申请单LOV带入 |
| 开始经营我司产品日期 | START_SALEME_DATE | DatePicker | 开始经营我司产品日期 | 始终 | 从申请单LOV带入 |
| 店长姓名 | SORE_MANAGERS_NAME | TextField | 店长姓名 | 始终 | 从申请单LOV带入 |
| 店长联系电话 | SORE_MANAGERS_TEL | TextField | 店长联系电话 | 始终 | 从申请单LOV带入 |
| 设计师数量 | DESIGNER_COUNT | NumberField | 设计师数量 | 始终 | 从申请单LOV带入 |
| 所属经销商拼接名称 | CUST_FULL_NAME | TextField | 所属经销商拼接后的名称 | 始终 | 从申请单LOV带入 |
| 门店状态 | TERMINAL_STAT | TextField | 门店状态 | 始终 | 从申请单LOV带入 |
| 门店区域等级 | STORE_AREA_LEVEL | TextField | 门店区域等级 | 始终 | 从申请单LOV带入 |
| 所属分销商拼接名称 | D_CUST_FULL_NAME | TextField | 所属分销商拼接名称 | 始终 | 从申请单LOV带入 |
| 审核人 | CHECKOR | TextField | 审核人 | 始终 | 审批通过时赋值 |
| 审核时间 | CHECK_TIME | DatePicker | 审核时间 | 始终 | 审批通过时赋值 |
| 交易公司名称 | TRADING_COMPANY_NAME | TextField | 交易公司 | 始终 | 从申请单LOV带入 |
| 交易公司编码 | TRADING_COMPANY_CODE | TextField | 交易公司编码 | 始终 | 从申请单LOV带入 |
| 法人客户编码 | BILLING_UNIT_CODE | TextField | 法人客户编码 | 始终 | 从申请单LOV带入，必填 |
| 法人客户名称 | BILLING_UNIT_NAME | TextField | 法人客户名称 | 始终 | 从申请单LOV带入，必填 |
| 当前门店面积 | THISTIME_TERMINAL_AREA | NumberField | 本次店面装修面积 | 始终 | 从申请单LOV带入 |
| 当前门店门头面积 | THISTIME_FRONTDOOR_AREA | NumberField | 本次门头装修面积 | 始终 | 从申请单LOV带入 |
| 关闭原因 | CLOSING_REASONS | TextField | 关闭原因 | 始终 | 用户输入 |
| 成本中心编码 | COST_CENTER_CODE | TextField | 成本中心编码 | 始终 | 从申请单LOV带入 |
| 成本中心名称 | COST_CENTER_NAME | TextField | 成本中心名称 | 始终 | 从申请单LOV带入 |
| 扣减比例 | DEDUCT_PROPORTION | NumberField | 扣减比例 | 始终 | 系统计算 |
| 是否二次变更 | IS_SECOND_CHANGE | NumberField | 是否二次变更 | 始终 | 系统维护 |
| 软装补贴金额 | SOFT_PURCHASE_AMT | NumberField | 软装补贴金额 | 始终 | 系统计算 |
| 灯具补贴金额 | LANTERN_AMT | NumberField | 灯具补贴金额 | 始终 | 系统计算 |
| 补贴模式 | SUBSIDY_MODE | TextField | 补贴模式 | 始终 | 从申请单LOV带入 |
| 装修项目 | DECORATE_PROJECT | TextField | 装修项目 | 始终 | 从申请单LOV带入 |
| 计划开业时间 | PLAN_OPEN_DATE | DatePicker | 计划开业时间 | 始终 | 从申请单LOV带入 |
| 门头实际装修面积 | FRONTDOOR_AREA | TextField | 门头实际装修面积 | 始终 | 从申请单LOV带入 |
| 本次门头装修等级 | FRONTDOOR_FIXUP_GRADE | TextField | 本次门头装修等级 | 始终 | 从申请单LOV带入 |
| 预计装修完成时间 | PRE_DECORATION_FINISHED_TIME | DatePicker | 预计装修完成时间 | 始终 | 从申请单LOV带入 |
| 旧店上次装修时间 | LAST_DECORATION_DATE | DatePicker | 旧店上次装修时间 | 始终 | 从申请单LOV带入 |
| 展示设计师 | DESIGNER | TextField | 展示设计师 | 始终 | 从申请单LOV带入 |
| 设计师手机号码 | DESIGNER_MOB | TextField | 设计师手机号码 | 始终 | 从申请单LOV带入 |
| 软装设计师 | SOFT_DESIGNER | TextField | 软装设计师 | 始终 | 从申请单LOV带入 |
| 软装设计师手机 | SOFT_DESIGNER_MOB | TextField | 软装设计师手机 | 始终 | 从申请单LOV带入 |
| 要求完成设计时间 | PLAN_DESIGN_DATE | DatePicker | 要求完成设计时间 | 始终 | 从申请单LOV带入 |
| 交付设计时间 | REPLY_DESIGN_DATE | DatePicker | 交付设计时间 | 始终 | 从申请单LOV带入 |
| 装修完成时间 | DECORATION_FINISHED_TIME | DatePicker | 装修完成时间 | 始终 | 从申请单LOV带入 |
| 装修完成超期天数 | OVER_DATE | TextField | 装修完成超期天数 | 始终 | 从申请单LOV带入 |
| 装修间隔期至 | DECORATION_INTERVAL_DATE | DatePicker | 装修间隔期至 | 始终 | 从申请单LOV带入 |
| 软装补贴标准 | SOFT_PURCHASE_STANDARD | TextField | 软装补贴标准 | 始终 | 从申请单LOV带入 |
| 灯具补贴标准 | LANTERN_STANDARD | TextField | 灯具补贴标准 | 始终 | 从申请单LOV带入 |
| 额度外申请补贴总额 | SUM_OUT_APPLY_STANDARD_AMT | TextField | 额度外申请补贴总额 | 始终 | 从申请单LOV带入 |
| 是否跨年 | IS_OVER_YEAR | NumberField | 是否跨年 | 始终 | 系统计算 |
| 签名状态 | SIGNATURE_STATE | NumberField | 签名状态 | 始终 | 系统维护 |
| 是否修改 | IS_MODIFY | NumberField | 是否修改 | 始终 | 系统维护 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表查询 | GET | `/v1/{organizationId}/fin-fee-apply-closes` | 查询关闭申请列表 |
| 详情查询 | GET | `/v1/{organizationId}/fin-fee-apply-closes/detail/{applyCloseId}` | 查询关闭申请详情 |
| 保存 | POST | `/v1/{organizationId}/fin-fee-apply-closes` | 新增或更新关闭申请 |
| 删除 | DELETE | `/v1/{organizationId}/fin-fee-apply-closes` | 删除关闭申请(仅NEW状态) |
| 计算扣除金额 | GET | `/v1/{organizationId}/fin-fee-apply-closes/do-cal-deduction-amt` | 计算扣减总额/广告费扣减/资金池扣减 |

### 选择弹窗

| 弹窗名称 | LOV编码 | 参数 | 说明 |
|----------|---------|------|------|
| 装修申请单号 | `AE.STORE_FINFEEAPPLYCLOSE_DATA_CHANGE_VIEW` | organizationId, searchFlag=4 | 选择已审批的装修申请单，联动带出门店/经销商/装修等级等全部信息 |
| 门店编码 | `AE.STORE_FINFEEAPPLYCLOSE_DATA_VIEW` | - | 从申请单LOV带入门店编码 |

### 导入

本页面无导入功能。

### 其他按钮

| 按钮名称 | 触发条件 | 执行逻辑 | 接口调用 |
|----------|---------|---------|---------|
| 删除 | HZ_APPROVE_STATUS为NEW | 弹窗确认→删除关闭申请 | DELETE /fin-fee-apply-closes |
| 计算扣除金额 | 选择申请单后 | 按面积和公司参数计算扣减金额 | GET /do-cal-deduction-amt |

#### 按钮1：删除（列表页行操作）

- **触发条件**：HZ_APPROVE_STATUS为NEW
- **执行逻辑**：
  1. 弹窗确认
  2. 调用do-delete接口，校验状态为NEW后删除

#### 按钮2：计算扣除金额（详情页自动触发）

- **触发条件**：选择装修申请单后自动触发
- **执行逻辑**：
  1. 调用do-cal-deduction-amt接口
  2. 查询广告费可用余额(queryResourceAmt)
  3. 读取公司参数Close_Amount
  4. 计算扣减总额 = (本次装修面积 + 本次门头面积) × Close_Amount
  5. 广告费扣减 = min(扣减总额, max(广告费余额, 0))
  6. 资金池扣减 = 扣减总额 - 广告费扣减

### 保存校验

- 校验1：装修申请单号必填 —— 确保关联有效的装修申请
  - **详细逻辑**：前端DataSet字段terminalApplyNoLov required=true
  - **系统体现**：C7N内置必填校验
  - **排查SQL**：`SELECT APPLY_CLOSE_ID FROM FIN_FEE_APPLY_CLOSE WHERE TERMINAL_APPLY_ID IS NULL`

- 校验2：关闭申请理由必填 —— 记录关闭原因留痕
  - **详细逻辑**：前端DataSet字段applyCause required=true
  - **系统体现**：C7N内置必填校验
  - **排查SQL**：`SELECT APPLY_CLOSE_ID FROM FIN_FEE_APPLY_CLOSE WHERE APPLY_CAUSE IS NULL OR APPLY_CAUSE = ''`

- 校验3：交易公司/开票单位必填 —— 确保财务关联完整
  - **详细逻辑**：后端Entity字段@NotNull注解
  - **系统体现**：后端Bean Validation校验
  - **排查SQL**：`SELECT APPLY_CLOSE_ID FROM FIN_FEE_APPLY_CLOSE WHERE TRADING_COMPANY_ID IS NULL OR BILLING_UNIT_ID IS NULL`

### 提交校验

- 校验1：验收报销单状态校验 —— 防止已验收的申请单被关闭
  - **详细逻辑**：onUserSubmit时查询FinFeeCheckBxHeader，若存在非INTERRUPT状态的记录则抛异常
  - **系统体现**：后端抛异常"该门店申请单：【{no}】已发起门店验收流程，不允许发起门店申请关闭"
  - **排查SQL**：`SELECT * FROM FIN_FEE_CHECK_BX_HEADER WHERE TERMINAL_APPLY_ID={id} AND HZ_APPROVE_STATUS != 'INTERRUPT'`

- 校验2：公司参数Close_Amount配置 —— 确保扣减单价已配置
  - **详细逻辑**：doCalDeductionAmt时validSystemParam("Close_Amount")，空则抛异常
  - **系统体现**：后端抛异常"公司参数：申请关闭扣减【Close_Amount】未找到，请联系管理员！"
  - **排查SQL**：`SELECT PARAM_VALUE FROM SYS_PARAM WHERE PARAM_CODE='Close_Amount'`

### 状态机

```text
NEW(新建) ──提交审批──→ RUN(审批中) ──┬──审批通过──→ APPROVED(已审批)
                                       │              ├─ 原申请单IS_CLOSE=2
                                       │              ├─ 原申请单HZ_APPROVE_STATUS=INTERRUPT
                                       │              └─ 扣减广告费/资金池额度
                                       │
                                       └──审批驳回──→ REJECTED(已驳回)

NEW ──删除──→ (删除)
```

### 工作流

- **工作流编码**：`FIN_FEE_APPLY_CLOSE`（门店装修申请关闭）
- **编码规则**：`AE.FIN_FEE_APPLY_CLOSE`（关闭单号，参数divisionCode）
- **审批流程**：提交后启动OA工作流，审批通过后执行onWfComplete关闭原申请单并扣减额度

## 数据库表详解

### FIN_FEE_APPLY_CLOSE（门店装修申请关闭表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| APPLY_CLOSE_ID | Long | 主键ID | - | 自增 |
| APPLY_CLOSE_NO | String | 关闭单号 | 关闭单号 | 编码规则AE.FIN_FEE_APPLY_CLOSE生成 |
| TERMINAL_APPLY_ID | Long | 关联装修申请ID | - | 用户选择(LOV) |
| TERMINAL_APPLY_NO | String | 关联装修申请单号 | 装修申请单号 | 选择时带入 |
| TERMINAL_ID | Long | 门店ID | - | 从原申请单带入 |
| TERMINAL_CODE | String | 门店编码 | 门店编码 | 从原申请单带入 |
| TERMINAL_NAME | String | 门店名称 | 门店名称 | 从原申请单带入 |
| CUST_ID | Long | 经销商ID | - | 从原申请单带入 |
| CUST_CODE | String | 经销商编码 | 经销商编码 | 从原申请单带入 |
| CUST_NAME | String | 经销商名称 | 经销商名称 | 从原申请单带入 |
| SHORT_NAME | String | 经销商简称 | 经销商简称 | 从原申请单带入 |
| CUST_FULL_NAME | String | 经销商拼接名称 | - | 从原申请单带入 |
| CUSTOMER_CLASS | Long | 经营属性 | 经营属性 | 从原申请单带入 |
| D_CUST_ID | Long | 分销商ID | - | 从原申请单带入 |
| D_CUST_CODE | String | 分销商编码 | - | 从原申请单带入 |
| D_CUST_NAME | String | 分销商名称 | - | 从原申请单带入 |
| D_CUST_FULL_NAME | String | 分销商拼接名称 | - | 从原申请单带入 |
| PROVINCE_AREAID | Long | 省ID | 门店所在地 | 从原申请单带入 |
| PROVINCE_AREANAME | String | 省名称 | - | 从原申请单带入 |
| CITY_AREAID | Long | 市ID | 门店所在地 | 从原申请单带入 |
| CITY_AREANAME | String | 市名称 | - | 从原申请单带入 |
| COUNTY_AREAID | Long | 县/区ID | 门店所在地 | 从原申请单带入 |
| COUNTY_AREANAME | String | 县/区名称 | - | 从原申请单带入 |
| AREANAME | String | 拼接省市区 | - | 从原申请单带入 |
| ADDR | String | 详细地址 | 门店详细地址 | 从原申请单带入 |
| TERMINAL_TYPE | Long | 门店类型 | 门店类型 | 从原申请单带入 |
| STORE_LOCATION_TYPE | Long | 位置类型 | - | 从原申请单带入 |
| TRADING_COMPANY_ID | Long | 交易公司ID | - | 从原申请单带入，@NotNull |
| TRADING_COMPANY_CODE | String | 交易公司编码 | - | 从原申请单带入 |
| TRADING_COMPANY_NAME | String | 交易公司名称 | - | 从原申请单带入 |
| BILLING_UNIT_ID | Long | 开票单位ID | - | 从原申请单带入，@NotNull |
| BILLING_UNIT_CODE | String | 开票单位编码 | 法人客户编码 | 从原申请单带入 |
| BILLING_UNIT_NAME | String | 开票单位名称 | 法人客户名称 | 从原申请单带入 |
| IS_CLOSE | Long | 是否关闭申请单 | - | 系统维护 |
| CLOSING_REASONS | String | 关闭原因 | - | 用户填写 |
| DECORATION_STYLE | Long | 装修风格 | - | 从原申请单带入 |
| THIS_DECORATION_STYLE | Long | 本次装修风格 | 本次装修风格 | 从原申请单带入 |
| TERMINAL_AREA | BigDecimal | 门店面积 | 门店面积 | 从原申请单带入 |
| THIS_TERMINAL_AREA | BigDecimal | 本次装修面积 | - | 从原申请单带入 |
| THISTIME_TERMINAL_AREA | BigDecimal | 本次装修面积(计算用) | - | 从原申请单带入 |
| THISTIME_FRONTDOOR_AREA | BigDecimal | 本次门头面积(计算用) | - | 从原申请单带入 |
| AREA_STANDARD_RATE | BigDecimal | 面积分配标准比例 | - | 从原申请单带入 |
| PLAN_OPEN_DATE | LocalDate | 计划开业日期 | - | 从原申请单带入 |
| FIXUP_GRADE | Long | 装修等级 | 装修等级 | 从原申请单带入 |
| DECORATION_DAYS | Long | 装修周期 | - | 从原申请单带入 |
| DECORATION_TYPE | Long | 装修性质 | 装修性质 | 从原申请单带入 |
| LAST_DECORATION_DATE | LocalDate | 旧店上次装修时间 | - | 从原申请单带入 |
| PROPERTY_TYPE | Long | 产权归属 | 产权归属 | 从原申请单带入 |
| LEASE_EXPIRATION_DATE | LocalDate | 租赁到期日 | - | 从原申请单带入 |
| DESIGNER | String | 委派设计师 | - | 从原申请单带入 |
| DESIGNER_MOB | String | 设计师手机号 | - | 从原申请单带入 |
| SHOPMANAGER_NAME | String | 负责人 | - | 从原申请单带入 |
| SHOPMANAGER_MOB | String | 负责人电话 | - | 从原申请单带入 |
| DECORATION_FINISHED_TIME | LocalDate | 装修完成时间 | - | 从原申请单带入 |
| PLAN_DESIGN_DATE | LocalDate | 要求完成设计日期 | - | 从原申请单带入 |
| REPLY_DESIGN_DATE | LocalDate | 交付设计日期 | - | 从原申请单带入 |
| DECORATION_INTERVAL_DATE | LocalDate | 装修间隔期至 | - | 从原申请单带入 |
| IS_OVER_STANDARD | Long | 是否超标准 | - | 从原申请单带入 |
| SALEZONE_ORG_NAME | String | 销售区域名称 | - | 从原申请单带入 |
| OPERAT_CENTER_ORG_NAME | String | 运营中心名称 | - | 从原申请单带入 |
| SALEZONE_ORG_ID | Long | 销售区域ID | - | 从原申请单带入 |
| OPERAT_CENTER_ORG_ID | Long | 运营中心ID | - | 从原申请单带入 |
| STAT | Long | 单据状态 | - | 系统维护 |
| WFFLAG | Long | 流程状态 | - | 系统维护 |
| WFID | Long | 流程ID | - | 系统维护 |
| AUDIT_STAT | String | 审核状态 | - | 系统维护 |
| APPLY_CAUSE | String | 关闭申请理由 | 关闭申请理由 | 用户填写 |
| CREATOR | String | 申请人 | 申请人 | 系统赋值 |
| CREATE_TIME | Date | 申请时间 | 申请日期 | 系统赋值 |
| UPDATOR | String | 更新人 | - | 系统赋值 |
| UPDATE_TIME | Date | 更新时间 | - | 系统赋值 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |
| HZ_INSTANCE_ID | String | 流程实例ID | - | 工作流启动后赋值 |
| HZ_APPROVE_STATUS | String | 流程审批状态 | 审核状态 | NEW/RUN/APPROVED/REJECTED/INTERRUPT |
| DEDUCTION_SUM_AMOUNT | BigDecimal | 扣减总额 | 扣减总金额 | (thistimeTerminalArea+thistimeFrontdoorArea)×Close_Amount |
| DEDUCTION_ADV_AMOUNT | BigDecimal | 广告费扣减额度 | 广告费扣减额度 | min(扣减总额, max(广告费余额, 0)) |
| DEDUCTION_CAPITAL_AMOUNT | BigDecimal | 资金池扣减额度 | 资金池扣减额度 | 扣减总额-广告费扣减额度 |
| NOTE | String | 备注 | 备注 | 用户输入 |

## 常见问题FAQ

### Q1：提交时报"该门店申请单已发起门店验收流程，不允许发起门店申请关闭"

**根因**：关联的装修申请单已存在非作废状态(INTERRUPT)的验收报销单
**解决方案**：先作废相关验收报销单，再发起关闭申请

### Q2：计算扣除金额时报"公司参数Close_Amount未找到"

**根因**：系统参数Close_Amount(申请关闭扣减单价)未配置
**解决方案**：在系统参数管理中配置Close_Amount

### Q3：删除时报"不能删除非制单状态的单据"

**根因**：单据状态非NEW，可能已提交审批
**解决方案**：仅NEW状态可删除，已提交的单据需通过审批驳回后才能删除

### Q4：装修申请单号LOV无数据

**根因**：没有APPROVED状态的装修申请单，或当前门店无符合条件的申请单
**解决方案**：确认FIN_FEE_APPLY_FINISHED_HEADER中存在APPROVED状态的记录

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 门店装修申请(FIN_FEE_APPLY_FINISHED_HEADER) | 数据依赖 | 选择已审批的装修申请单，带入全部装修信息 | 存在APPROVED状态的装修申请单 |
| 公司参数(SYS_PARAM) | 参数依赖 | Close_Amount(申请关闭扣减单价) | 公司参数已配置 |
| 广告费余额(MKT_INLIMIT_BALANCE_HEADER) | 数据依赖 | 查询广告费可用余额用于扣减计算 | 经销商有广告费额度记录 |
| 事业部基础设置(DIVISION_BASE_SET) | 数据依赖 | 获取事业部ID/编码用于生成单号 | 用户已配置事业部 |

## 下游影响

- **门店装修申请单(FIN_FEE_APPLY_FINISHED_HEADER)** — 审批通过后标记原申请单isClose=2, auditStat=已关闭, hzApproveStatus=INTERRUPT
- **广告费额度(MKT_INLIMIT_BALANCE_HEADER)** — 审批通过后扣减广告费可用余额
- **资金池** — 审批通过后扣减资金池额度
- **门店验收报销单(FIN_FEE_CHECK_BX_HEADER)** — 提交前校验是否存在非作废状态的验收报销单

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 不能删除非制单状态的单据 | doDelete | hzApproveStatus≠NEW，仅新建状态可删除 | 中 | [查看](#报错1不能删除非制单状态的单据) |
| 单据信息不存在 | doSelect | applyCloseId对应记录不存在 | 高 | [查看](#报错2单据信息不存在) |
| 申请关闭信息不存在 | onWfComplete/onUserSubmit/onWfBreak | applyCloseId对应记录不存在 | 高 | [查看](#报错3申请关闭信息不存在) |
| 申请单信息不存在 | onWfComplete | terminalApplyId对应的装修申请单不存在 | 高 | [查看](#报错4申请单信息不存在) |
| 该门店申请单：【{no}】已发起门店验收流程，不允许发起门店申请关闭 | onUserSubmit | 存在非INTERRUPT状态的验收报销单 | 中 | [查看](#报错5该门店申请单no已发起门店验收流程不允许发起门店申请关闭) |
| 公司参数：申请关闭扣减【Close_Amount】未找到，请联系管理员 | doCalDeductionAmt | 系统参数Close_Amount未配置或为空 | 高 | [查看](#报错6公司参数申请关闭扣减close_amount未找到请联系管理员) |
| 请先选择门店编码 | 计算扣除金额 | 门店编码terminalCode为空 | 中 | [查看](#报错7请先选择门店编码) |
| 本次店面装修面积不能大于门店面积 | 面积校验 | thistimeTerminalArea>terminalArea | 中 | [查看](#报错8本次店面装修面积不能大于门店面积) |
| 店面实际装修面积不能大于本次店面装修面积 | 面积校验 | thisTerminalArea>thistimeTerminalArea | 中 | [查看](#报错9店面实际装修面积不能大于本次店面装修面积) |
| 数据格式错误 | 打印/导出 | 前端解析响应数据格式异常 | 中 | [查看](#报错10数据格式错误) |
| 参数格式错误 | 打印/导出 | 前端组装请求参数格式异常 | 中 | [查看](#报错11参数格式错误) |

#### 报错1：不能删除非制单状态的单据
- **触发条件**：doDelete删除操作，WorkflowInstanceStatus.NEW.getCode().equals(hzApproveStatus)校验不通过（hzApproveStatus≠NEW）
- **逻辑分析**：仅NEW(制单)状态的关闭申请单可删除，避免删除已进入审批流程的单据造成工作流悬挂。若单据已提交审批(RUN)、已审批(APPROVED)、已驳回(REJECTED)等非NEW状态，校验不通过抛异常。需先通过审批流程处理单据。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.terminal_apply_no AS 装修申请单号,
         c.hz_approve_status AS 审批状态
  FROM   fin_fee_apply_close c
  WHERE  c.apply_close_id = #{传入的applyCloseId}
  AND    c.hz_approve_status <> 'NEW';
  ```

#### 报错2：单据信息不存在
- **触发条件**：doSelect查询详情时，selectByPrimary按applyCloseId查询FIN_FEE_APPLY_CLOSE返回null
- **逻辑分析**：查询详情需先校验关闭申请单存在。若关闭单在查询期间被其他用户删除、applyCloseId传值错误（如前端缓存失效ID）、或并发场景下被清理，查询返回空抛异常。需刷新列表页重新获取有效数据。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.hz_approve_status AS 审批状态,
         c.create_time       AS 创建时间
  FROM   fin_fee_apply_close c
  WHERE  c.apply_close_id = #{传入的applyCloseId};
  ```

#### 报错3：申请关闭信息不存在
- **触发条件**：onWfComplete/onUserSubmit/onWfBreak工作流回调时，selectByPrimary按applyCloseId查询FIN_FEE_APPLY_CLOSE返回null
- **逻辑分析**：工作流回调需查询关闭申请单更新审批状态、扣减额度、标记原申请单等。若回调期间关闭单被删除、applyCloseId传值错误、或OA回调报文与DMS不一致，查询返回空抛异常。需核查关闭单数据与工作流实例一致性。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.hz_approve_status AS 审批状态,
         c.hz_instance_id    AS 工作流实例ID,
         c.update_time       AS 最后更新时间
  FROM   fin_fee_apply_close c
  WHERE  c.hz_approve_status = 'RUN'
  AND    c.update_time < SYSDATE - 1
  ORDER  BY c.update_time DESC;
  ```

#### 报错4：申请单信息不存在
- **触发条件**：onWfComplete审批通过回调时，finFeeApplyFinishedHeaderRepository.selectByPrimaryKey按terminalApplyId查询原装修申请单返回null
- **逻辑分析**：关闭申请审批通过后需标记原装修申请单为已关闭(isClose=2, auditStat=已关闭, hzApproveStatus=INTERRUPT)。若原申请单在关闭审批期间被删除、terminalApplyId传值错误、或并发场景下被清理，查询返回空抛异常。需核查原装修申请单数据是否存在。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id       AS 关闭单ID,
         c.apply_close_no       AS 关闭单号,
         c.terminal_apply_id    AS 装修申请ID,
         c.terminal_apply_no    AS 装修申请单号,
         f.hz_approve_status    AS 原单审批状态
  FROM   fin_fee_apply_close c
  LEFT   JOIN fin_fee_apply_finished_header f ON f.terminal_apply_id = c.terminal_apply_id
  WHERE  c.hz_approve_status = 'RUN'
  AND    f.terminal_apply_id IS NULL
  ORDER  BY c.update_time DESC;
  ```

#### 报错5：该门店申请单：【{no}】已发起门店验收流程，不允许发起门店申请关闭
- **触发条件**：onUserSubmit提交操作，selectByCondition查询FinFeeCheckBxHeader存在该装修申请单关联的非INTERRUPT状态验收报销单
- **逻辑分析**：已发起验收报销的装修申请单不允许关闭，防止业务冲突（验收报销依赖装修申请单据）。若该装修申请单下存在NEW/RUN/APPROVED等状态的验收报销单，校验不通过抛异常。需先作废关联的验收报销单再提交关闭申请。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id       AS 关闭单ID,
         c.apply_close_no       AS 关闭单号,
         c.terminal_apply_id    AS 装修申请ID,
         c.terminal_apply_no    AS 装修申请单号,
         ck.check_bx_id         AS 验收报销ID,
         ck.check_bx_no         AS 验收报销号,
         ck.hz_approve_status   AS 验收报销审批状态
  FROM   fin_fee_apply_close c
  JOIN   fin_fee_check_bx_header ck ON ck.terminal_apply_id = c.terminal_apply_id
  WHERE  c.hz_approve_status = 'NEW'
  AND    ck.hz_approve_status <> 'INTERRUPT'
  ORDER  BY c.create_time DESC;
  ```

#### 报错6：公司参数：申请关闭扣减【Close_Amount】未找到，请联系管理员
- **触发条件**：doCalDeductionAmt计算扣减金额时，validSystemParam("Close_Amount")查询公司参数返回空或为空值
- **逻辑分析**：扣减金额计算依赖公司参数Close_Amount(申请关闭扣减单价)，扣减总额=(本次装修面积+本次门头面积)×Close_Amount。若参数未在SYS_PARAM表配置、配置值为空、或参数编码拼写错误，校验不通过抛异常。需联系管理员在SYS_PARAM表配置有效的Close_Amount参数。
- **排查SQL**：
  ```sql
  SELECT p.param_code   AS 参数编码,
         p.param_value  AS 参数值,
         p.description  AS 参数说明
  FROM   sys_param p
  WHERE  p.param_code = 'Close_Amount';
  ```

#### 报错7：请先选择门店编码
- **触发条件**：详情页计算扣除金额或保存时，前端校验headDS中terminalCode(门店编码)为空
- **逻辑分析**：扣除金额计算依赖门店关联的装修面积信息，需先选择有效的装修申请单联动带出门店编码。若用户未选择装修申请单即点击计算扣除金额、或LOV选择失败未回填terminalCode，前端message.info提示并阻断操作。需先选择装修申请单再操作。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.terminal_apply_id AS 装修申请ID,
         c.terminal_code     AS 门店编码
  FROM   fin_fee_apply_close c
  WHERE  c.terminal_code IS NULL
  OR     c.terminal_code = '';
  ```

#### 报错8：本次店面装修面积不能大于门店面积
- **触发条件**：详情页编辑面积字段时，前端校验thistimeTerminalArea(本次店面装修面积)>terminalArea(门店面积)
- **逻辑分析**：本次店面装修面积不应超过门店总面积，防止面积超限导致扣减金额计算异常。扣减总额=(本次装修面积+本次门头面积)×Close_Amount，若本次装修面积>门店面积，扣减金额将超出实际范围。前端message.info提示并回填为门店面积，需修改本次装修面积至≤门店面积。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id         AS 关闭单ID,
         c.apply_close_no         AS 关闭单号,
         c.terminal_area          AS 门店面积,
         c.thistime_terminal_area AS 本次店面装修面积
  FROM   fin_fee_apply_close c
  WHERE  c.thistime_terminal_area > c.terminal_area;
  ```

#### 报错9：店面实际装修面积不能大于本次店面装修面积
- **触发条件**：详情页编辑面积字段时，前端校验thisTerminalArea(店面实际装修面积)>thistimeTerminalArea(本次店面装修面积)
- **逻辑分析**：店面实际装修面积应≤本次店面装修面积，防止面积数据逻辑矛盾。本次店面装修面积是本次装修的面积上限，实际装修面积是其中实际完成的部分。前端message.info提示并阻断保存，需修改店面实际装修面积至≤本次店面装修面积。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id         AS 关闭单ID,
         c.apply_close_no         AS 关闭单号,
         c.this_terminal_area     AS 店面实际装修面积,
         c.thistime_terminal_area AS 本次店面装修面积
  FROM   fin_fee_apply_close c
  WHERE  c.this_terminal_area > c.thistime_terminal_area;
  ```

#### 报错10：数据格式错误
- **触发条件**：详情页打印或导出操作时，前端解析后端响应数据格式异常（如响应非预期JSON结构）
- **逻辑分析**：打印和导出需解析后端返回的数据组装打印模板或导出文件。若后端响应数据格式异常（如接口返回错误页面、数据字段缺失、或网络中间件篡改响应），前端JSON.parse或字段读取失败，notification.error提示"数据格式错误"。需检查后端接口响应是否正常。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.hz_approve_status AS 审批状态,
         c.update_time       AS 最后更新时间
  FROM   fin_fee_apply_close c
  WHERE  c.apply_close_id = #{传入的applyCloseId};
  ```

#### 报错11：参数格式错误
- **触发条件**：详情页打印或导出操作时，前端组装请求参数格式异常（如日期格式错误、数值字段为NaN）
- **逻辑分析**：打印和导出需将页面数据组装为请求参数发送后端。若页面字段格式异常（如日期字段未格式化为YYYY-MM-DD、数值字段为NaN或null、或必填字段缺失），前端参数组装失败，notification.error提示"参数格式错误"。需检查页面字段填写是否完整正确。
- **排查SQL**：
  ```sql
  SELECT c.apply_close_id    AS 关闭单ID,
         c.apply_close_no    AS 关闭单号,
         c.hz_approve_status AS 审批状态
  FROM   fin_fee_apply_close c
  WHERE  c.apply_close_id = #{传入的applyCloseId};
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-12-22 | - | hfy | 初始创建FinFeeApplyCloseServiceImpl应用服务 |
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
