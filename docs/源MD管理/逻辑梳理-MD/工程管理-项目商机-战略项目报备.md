# 工程管理-项目商机-战略项目报备

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API与Entity梳理。战略项目报备reportType=2，支持两次OA审批流程，审核通过后生成正式项目档案。

## 业务流程

### 报备如何流转

创建战略报备 → 项目查重 → 保存 → 流程发起前校验 → 提交一次OA审批 → 一次通过生成项目档案 → 发起二次报备 → 二次OA审批 → 二次通过更新项目档案。

```
用户进入战略项目报备页面
  → 新增战略报备（reportType=2）
  → 填写项目基本信息、经销商授权、网点信息、附件
  → 项目查重校验（GET /epm-reports，SQL方式查重）
  → 保存报备数据（POST /epm-reports，EPM_REPORT + EPM_REPORT_AUTH + EPM_REPORT_AUTH_BRANCH + EPM_REPORT_PARTYB）
  → 流程发起前校验（GET /work-flow-start-volidate）
  → 提交一次OA审批（工作流编码PROJECT_ZLBB，单据名称"战略项目报备一次"）
  → 一次审批通过：
    → 复制报备数据到项目档案表(EPM_PROJECT)
    → 新增项目授权(EPM_PROJECT_AUTH)和乙方信息
    → 设置项目有效状态和有效期
    → 更新项目进度阶段
    → 推送CRM
  → 发起二次报备（reportTimes=2）
  → 提交二次OA审批（单据名称"战略项目报备二次"）
  → 二次审批通过：更新项目档案相关字段，重新生成项目授权和乙方信息
```

### 全链路流程图

```
开始 → ★新建战略报备★ → 项目查重 → 保存校验 → 流程发起前校验 → 提交一次OA审批
     → ⚖一次审批通过？ → 生成项目档案/项目授权/乙方信息/推送CRM
     → 发起二次报备 → 提交二次OA审批 → ⚖二次审批通过？ → 更新项目档案 → 结束

上游支撑：经销商主数据、交易公司、事业部、区域、值集配置、OA系统、工作流引擎、CRM系统、编码规则引擎
下游影响：项目档案(EPM_PROJECT)、项目授权(EPM_PROJECT_AUTH)、合同、折扣政策、CRM推送
```

### 两次报备机制

- **一次报备**：首次提交走"战略项目报备一次"OA审批，通过后在项目档案中新增项目记录，设置有效起始/结束日期（根据公司参数Proj_Effective_Cycle配置的有效周期天数计算）
- **二次报备**：一次通过后发起，走"战略项目报备二次"OA审批，通过后更新项目档案相关信息（项目进度、交易公司、项目经理、甲乙方信息等），重新生成项目授权和乙方信息

### 报备类型区分

战略项目报备与单体项目报备、家装战略报备、家装单体报备共用同一套后端代码，通过reportType区分：1=单体报备、2=战略报备、3=家装战略、4=家装单体。战略项目报备reportType=2，不执行ES相似度查重，查重通过SQL方式实现。

## 重点逻辑

### 重点逻辑1：两次OA审批流程

- **业务意义**：战略项目报备需经过一次和二次两个OA审批流程，一次建项目档案、二次补充信息，保障项目数据完整性
- **具体逻辑描述**：
  - 工作流编码：PROJECT_ZLBB（战略项目报备）
  - 一次报备：单据名称"战略项目报备一次"，OA审批通过后执行doStrategicProjectResp生成项目档案
  - 二次报备：单据名称"战略项目报备二次"，OA审批通过后更新项目档案相关字段
  - OA表单配置通过OA_BILL_REF表维护，提交时校验对应关系存在

### 重点逻辑2：审核通过写入项目档案

- **业务意义**：一次报备审核通过后将报备数据复制到项目档案表，生成可跟进的正式项目
- **具体逻辑描述**：
  - 将EPM_REPORT数据复制到EPM_PROJECT（忽略部分不需要复制的字段）
  - 新增项目授权记录（EPM_PROJECT_AUTH，将报备经销商授权转为项目授权）
  - 新增乙方信息记录
  - 更新项目进度阶段
  - 设置项目有效状态为有效(2)，计算有效起始/结束日期（基于Proj_Effective_Cycle参数）
  - 推送项目报备至CRM系统

### 重点逻辑3：项目查重拦截

- **业务意义**：避免重复报备同一项目，战略报备通过SQL方式查重（非ES相似度）
- **具体逻辑描述**：
  - 前端调用GET /epm-reports接口执行doCheckReport查重
  - 战略报备不执行ES相似度查重，通过SQL方式匹配同城市相似项目
  - 查重命中时标记DUPLICATE_REPORTING='Y'，需通过申诉重新提交
  - 单体报备执行ES相似度搜索（战略报备跳过此逻辑）

### 重点逻辑4：申诉机制

- **业务意义**：被查重拦截标记为重复申报的报备，可通过申诉重新提交
- **具体逻辑描述**：
  - 申诉前校验DUPLICATE_REPORTING='Y'，否则提示"非重复申报，不能申诉"
  - 校验本月可用申诉次数（SCPSYSCONF配置ReportAppealNum，或EPM_REPORT_APPEAL_CONFIG按经销商/年/月配置）
  - 超过限制提示"本月申诉次数已到达限制:{n}次"
  - 申诉成功后更新拦截记录并重置重复申报标记

### 重点逻辑5：流程发起前校验

- **业务意义**：提交审批前校验业务条件是否满足，避免无效提交
- **具体逻辑描述**：
  - 前端调用GET /work-flow-start-volidate接口
  - 二次报备且管理类型为经销商操作(2)或公司自营(3)时，必须分配具体经销商，否则提示"该战略工程涉及经销商服务，请分配具体经销商"
  - 校验OA表单对应关系存在（OA_BILL_REF中维护"战略项目报备一次"/"战略项目报备二次"）
  - 校验通过返回成功，失败返回错误信息

### 重点逻辑6：项目编码自动生成

- **业务意义**：新建报备时自动生成唯一项目编码，保证项目可追溯
- **具体逻辑描述**：
  - 编码规则通过编码规则引擎生成
  - 工程类规则编码为PROJECT_CODE
  - 家装类规则编码为AE.HOME_SINGLE_PROJECT_CODE

## 详细逻辑

### 界面模块

> 本页面为hlod低代码页面，界面模块由低代码平台配置承载。以下基于后端Entity/DTO字段梳理。

#### 查询区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 项目编码 | EPM_REPORT.PROJECT_CODE | 文本输入 | 模糊查询项目编码 | 始终显示 | 手动输入，模糊匹配 |
| 项目名称 | EPM_REPORT.PROJECT_NAME | 文本输入 | 模糊查询项目名称 | 始终显示 | 手动输入，模糊匹配 |
| 客户编码 | EPM_REPORT.CUSTOMER_CODE | 文本输入 | 模糊查询客户编码 | 始终显示 | 手动输入，模糊匹配 |
| 客户名称 | EPM_REPORT.CUSTOMER_NAME | 文本输入 | 模糊查询客户名称 | 始终显示 | 手动输入，模糊匹配 |
| 甲方名称 | EPM_REPORT.PARTY_A_NAME | 文本输入 | 模糊查询甲方名称 | 始终显示 | 手动输入，模糊匹配 |
| 审核状态 | EPM_REPORT.HZ_APPROVE_STATUS | 下拉框 | 查询审批状态 | 始终显示 | RUN/APPROVED/REJECTED |
| 有效状态 | EPM_PROJECT.PROJECT_VALID | 下拉框 | 查询项目有效状态 | 始终显示 | 2=有效, 3=失效, 4=冻结 |
| 本地/异地 | EPM_REPORT.IS_LOCAL | 下拉框 | 查询本地异地 | 始终显示 | 1=异地, 2=本地, 3=本地特约 |
| 省/市/区 | EPM_REPORT.PROVINCE/CITY/AREA | 级联选择 | 查询区域 | 始终显示 | 级联选择回写ID和名称 |
| 申报日期 | EPM_REPORT.REPORT_TIME | 日期范围 | 查询报备时间范围 | 始终显示 | 手动选择日期范围 |
| 工程类型 | EPM_REPORT.PROJECT_SOURCE | 下拉框 | 查询工程类型 | 始终显示 | 1=常规工程, 2=战略工程 |

#### 报备单头信息

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 报备ID | EPM_REPORT.REPORT_ID | 隐藏 | 报备主键 | 始终显示 | 系统自动生成 |
| 项目编码 | EPM_REPORT.PROJECT_CODE | 文本 | 项目编码 | 始终显示 | 系统自动生成(编码规则PROJECT_CODE) |
| 项目名称 | EPM_REPORT.PROJECT_NAME | 文本输入 | 项目名称 | 始终显示 | 手动输入，必填 |
| 报备次数 | EPM_REPORT.REPORT_TIMES | 文本 | 报备次数 | 始终显示 | 1=一次报备, 2=二次报备 |
| 报备类型 | EPM_REPORT.REPORT_TYPE | 隐藏 | 报备类型 | 始终显示 | 固定值2(战略报备) |
| 工程类型 | EPM_REPORT.PROJECT_SOURCE | 下拉框 | 项目来源 | 始终显示 | 1=常规工程, 2=战略工程 |
| 业主类型 | EPM_REPORT.PROJECT_TYPE | 下拉框 | 项目类型/业主类型 | 始终显示 | 值集epm.project_type |
| 项目进度阶段 | EPM_REPORT.STAGE_NAME | 下拉框 | 项目进度阶段 | 始终显示 | 值集配置，回写stageId/stageName/stageNote |
| 省/市/区 | EPM_REPORT.PROVINCE/CITY/AREA | 级联选择 | 项目地址 | 始终显示 | 级联选择回写ID和名称，必填 |
| 详细地址 | EPM_REPORT.ADDRESS | 文本输入 | 详细地址 | 始终显示 | 手动输入，必填 |
| 客户编码 | EPM_REPORT.CUSTOMER_CODE | 文本 | 客户编码 | 始终显示 | 选择客户弹窗回写，必填 |
| 客户名称 | EPM_REPORT.CUSTOMER_NAME | 文本 | 客户名称 | 始终显示 | 选择客户弹窗回写 |
| 交易公司 | EPM_REPORT.TRADING_COMPANY_NAME | 下拉框 | 交易公司 | 始终显示 | 选择交易公司弹窗回写，必填 |
| 所属事业部 | EPM_REPORT.DIVISION_NAME | 文本 | 所属事业部 | 始终显示 | 系统自动带出 |
| 甲方名称 | EPM_REPORT.PARTY_A_NAME | 文本输入 | 甲方名称 | 始终显示 | 手动输入或选择客户回写，必填 |
| 甲方联系人 | EPM_REPORT.PARTY_A_LINK_PERSON | 文本输入 | 甲方联系人 | 始终显示 | 手动输入，必填 |
| 甲方电话 | EPM_REPORT.PARTY_A_PHONE | 文本输入 | 甲方联系电话 | 始终显示 | 手动输入，必填 |
| 乙方名称 | EPM_REPORT.PARTY_B_NAME | 文本输入 | 乙方名称 | 始终显示 | 手动输入，必填 |
| 乙方联系人 | EPM_REPORT.PARTY_B_LINK_PERSON | 文本输入 | 乙方联系人 | 始终显示 | 手动输入，必填 |
| 乙方电话 | EPM_REPORT.PARTY_B_PHONE | 文本输入 | 乙方联系电话 | 始终显示 | 手动输入，必填 |
| 管理类型 | EPM_REPORT.OPERATING_MODE | 下拉框 | 管理类型 | 始终显示 | 1=经销商操作, 2=公司自营 |
| 产品线 | EPM_REPORT.PDT_LINE | 下拉框 | 产品线 | 始终显示 | 值集配置，必填 |
| 预计签订日期 | EPM_REPORT.PREDICT_SIGN_DATE | 日期选择 | 预计合同签订日期 | 始终显示 | 手动选择 |
| 预计销售收入 | EPM_REPORT.PREDICT_SALES_AMOUNT | 数值输入 | 预计合同金额(元) | 始终显示 | 手动输入，必填 |
| 背景关系 | EPM_REPORT.BACKGROUND | 下拉框 | 背景关系 | 始终显示 | 值集配置，必填 |
| 预估项目数量 | EPM_REPORT.PREDICT_PROJ_QTY | 文本输入 | 预估生成单体项目数量 | 始终显示 | 手动输入 |
| 预估产品用量 | EPM_REPORT.PREDICT_PDT_QTY | 文本输入 | 预估产品用量 | 始终显示 | 手动输入，必填 |
| 工程意向产品 | EPM_REPORT.INTENT_PRODUCT | 文本输入 | 工程意向产品 | 始终显示 | 手动输入，必填 |
| 竞争品牌 | EPM_REPORT.COMPETITIVE_BRAND | 文本输入 | 竞争品牌 | 始终显示 | 手动输入 |
| 竞争对手 | EPM_REPORT.COMPETITOR | 文本输入 | 竞争对手 | 始终显示 | 手动输入，必填 |
| 工程建筑面积 | EPM_REPORT.SITE_AREA | 数值输入 | 工程建筑面积 | 始终显示 | 手动输入 |
| 工程施工阶段 | EPM_REPORT.CONSTRUCTION_STAGE | 文本输入 | 工程施工阶段 | 始终显示 | 手动输入，必填 |
| 完工日期 | EPM_REPORT.COMPLETION_DATE | 日期选择 | 完工日期 | 始终显示 | 手动选择，必填 |
| 产品送样 | EPM_REPORT.NEED_SAMPLE | 下拉框 | 是否产品送样 | 始终显示 | Y/N，必填 |
| 产品报价 | EPM_REPORT.NEED_QUOTE | 下拉框 | 是否产品报价 | 始终显示 | Y/N，必填 |
| 缴纳保证金 | EPM_REPORT.NEED_DEPOSIT | 下拉框 | 是否缴纳保证金 | 始终显示 | 1=同意, 2=不同意，必填 |
| 保证金金额 | EPM_REPORT.DEPOSIT_AMOUNT | 数值输入 | 保证金金额 | needDeposit=1时显示 | 手动输入 |
| 保证金备注 | EPM_REPORT.DEPOSIT_NOTE | 文本输入 | 保证金备注 | needDeposit=1时显示 | 手动输入 |
| 本地/异地 | EPM_REPORT.IS_LOCAL | 下拉框 | 本地/异地 | 始终显示 | 1=异地, 2=本地, 3=本地特约，必填 |
| 海外 | EPM_REPORT.IS_FOREIGN | 下拉框 | 是否海外 | 始终显示 | Y/N |
| 任务划分比例 | EPM_REPORT.TASK_SHARED_RATE | 数值输入 | 任务划分比例 | 始终显示 | 手动输入 |
| 售后服务金划分比例 | EPM_REPORT.SERVICE_FEE_SHARED_RATE | 数值输入 | 售后服务金划分比例 | 始终显示 | 手动输入 |
| 合作区域 | EPM_REPORT.COOPERATION_AREA | 文本输入 | 合作区域 | 始终显示 | 手动输入 |
| 战略合作范围涉及区域 | EPM_REPORT.STRAC_COOP_INV_REGION | 文本输入 | 战略合作范围涉及区域 | 始终显示 | 手动输入 |
| 经办人 | EPM_REPORT.AGENT | 文本 | 经办人 | 始终显示 | 系统自动带出当前用户 |
| 经办人电话 | EPM_REPORT.AGENT_PHONE | 文本 | 经办人电话 | 始终显示 | 系统自动带出 |
| 经销商现场跟进人 | EPM_REPORT.DEALER_FOLLOWER | 文本输入 | 经销商现场跟进人 | 始终显示 | 手动输入 |
| 经销商跟进人电话 | EPM_REPORT.DEALER_FOLLOWER_PHONE | 文本输入 | 经销商跟进人电话 | 始终显示 | 手动输入 |
| 公司现场跟进人 | EPM_REPORT.OWN_FOLLOWER | 文本输入 | 公司现场跟进人 | 始终显示 | 手动输入 |
| 公司跟进人电话 | EPM_REPORT.OWN_FOLLOWER_PHONE | 文本输入 | 公司跟进人电话 | 始终显示 | 手动输入 |
| 项目经理 | EPM_REPORT.MANAGER | 文本输入 | 项目经理姓名 | 始终显示 | 手动输入 |
| 项目经理工号 | EPM_REPORT.EMPID | 文本输入 | 项目经理工号 | 始终显示 | 手动输入 |
| 战略对接阶段 | EPM_REPORT.STRATEGIC_STAGE | 文本输入 | 战略对接阶段 | 始终显示 | 手动输入 |
| 地标建筑 | EPM_REPORT.LANDMARK_FLAG | 下拉框 | 是否地标建筑 | 始终显示 | Y/N，默认N |
| 项目分类 | EPM_REPORT.PROJECT_CATEGORY | 下拉框 | 项目分类 | 始终显示 | normal=标准项目, small=小型项目 |
| 经度 | EPM_REPORT.LONGITUDE | 数值 | 经度 | 始终显示 | 地图选点回写 |
| 纬度 | EPM_REPORT.LATITUDE | 数值 | 纬度 | 始终显示 | 地图选点回写 |
| 审批状态 | EPM_REPORT.HZ_APPROVE_STATUS | 文本 | 工作流审批状态 | 始终显示 | 系统自动回写 |
| 查重拦截标记 | EPM_REPORT.DUPLICATE_REPORTING | 文本 | 查重标记 | 始终显示 | Y=重复申报, N=正常 |

#### 经销商授权明细

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 经销商ID | EPM_REPORT_AUTH.CUSTOMER_ID | 隐藏 | 经销商ID | 始终显示 | 选择经销商弹窗回写 |
| 经销商编码 | EPM_REPORT_AUTH.CUSTOMER_CODE | 文本 | 经销商编码 | 始终显示 | 选择经销商弹窗回写 |
| 经销商名称 | EPM_REPORT_AUTH.CUSTOMER_NAME | 文本 | 经销商名称 | 始终显示 | 选择经销商弹窗回写 |
| 经办人 | EPM_REPORT_AUTH.CONTACT | 文本输入 | 经办人 | 始终显示 | 手动输入 |
| 经办人电话 | EPM_REPORT_AUTH.TELE | 文本输入 | 经办人电话 | 始终显示 | 手动输入 |

#### 网点信息明细

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 网点ID | EPM_REPORT_AUTH_BRANCH.BRANCH_MESSAGE_ID | 隐藏 | 网点ID | 始终显示 | 选择网点弹窗回写 |
| 对应经销商编码 | EPM_REPORT_AUTH_BRANCH.REL_CUSTOMER_CODE | 文本 | 对应经销商编码 | 始终显示 | 关联经销商授权行 |

### 后端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/v1/{organizationId}/epm-reports` | 报备保存/编辑 |
| GET | `/v1/{organizationId}/epm-reports` | 项目查重(doCheckReport) |
| GET | `/v1/{organizationId}/epm-reports/work-flow-start-volidate` | 流程发起前校验 |
| GET | `/v1/{organizationId}/epm-reports/appeal` | 申诉 |
| POST | `/v1/{organizationId}/epm-reports/query-nearby-project` | 查询周边范围项目数据 |
| POST | `/v1/{organizationId}/epm-reports/query-current-project` | 查询当前项目数据 |
| GET | `/v1/{organizationId}/epm-reports/get-small-disc-rate` | 查询小折扣率 |

**Controller**：`EpmReportController` (EpmReportController.java:34)
**Service**：`EpmReportServiceImpl` (EpmReportServiceImpl.java)

### saveData保存逻辑

```
入参：EpmReportAddDTO (reportType=2)
1. validObject校验DTO参数非空
2. 校验项目名称、工程类型、项目进度阶段、业主类型、区域、详细地址、客户编码、交易公司、甲乙方信息等必填
3. 校验交易公司事业部关联关系存在
4. 校验报备所在区域非禁用区域
5. 编辑时校验报备ID对应记录存在
6. 保存EPM_REPORT主表数据
7. 保存EPM_REPORT_AUTH经销商授权
8. 保存EPM_REPORT_AUTH_BRANCH网点信息
9. 保存EPM_REPORT_PARTYB乙方信息
10. 返回EpmReport
```

### doCheckReport查重逻辑

```
入参：EpmReportCheckDTO
1. 根据项目名称、地址、区域等条件查询相似项目
2. 战略报备通过SQL方式查重(不执行ES相似度搜索)
3. 返回List<EpmReportCheckVO>查重结果列表
```

### workFlowStartVolidate流程发起前校验逻辑

```
入参：EpmReportDTO
1. 二次报备且管理类型为经销商操作(2)或公司自营(3)时：
   → 校验EPM_REPORT_AUTH中已分配经销商
   → 未分配返回"该战略工程涉及经销商服务，请分配具体经销商"
2. 校验OA表单对应关系存在(OA_BILL_REF)
3. 校验通过返回null(成功)，失败返回错误信息
```

### doStrategicProjectResp OA审批回调逻辑

```
一次报备审批通过：
1. 将EPM_REPORT数据复制到EPM_PROJECT(忽略部分字段)
2. 新增项目授权EPM_PROJECT_AUTH(从EPM_REPORT_AUTH转换)
3. 新增乙方信息
4. 更新项目进度阶段
5. 设置项目有效状态为有效(2)
6. 计算有效起始/结束日期(基于Proj_Effective_Cycle参数)
7. 推送CRM

二次报备审批通过：
1. 更新EPM_PROJECT相关字段
2. 重新生成项目授权和乙方信息
```

### 选择弹窗

| 选择项 | 触发场景 | 说明 |
|--------|---------|------|
| 经销商选择弹窗 | 添加经销商授权时 | 选择有效经销商，回写customerId/customerCode/customerName |
| 交易公司选择弹窗 | 选择交易公司时 | 选择与当前事业部关联的有效交易公司，回写tradingCompanyId/tradingCompanyName |
| 关联战略项目选择弹窗 | 单体报备关联战略时 | 选择reportType=2且HZ_APPROVE_STATUS='APPROVED'且PROJECT_VALID=2的项目 |
| 项目进度阶段选择弹窗 | 选择项目进度时 | 选择值集配置的项目进度阶段，回写stageId/stageName/stageNote |
| 客户选择弹窗 | 选择甲方/客户时 | 选择客户主数据，回写customerId/customerCode/customerName |
| 网点选择弹窗 | 添加网点信息时 | 选择经销商下的网点，回写branchMessageId |
| 区域级联选择 | 选择省/市/区时 | 级联选择行政区域，回写provinceId/cityId/areaId及名称 |

### 导入

不支持导入功能。战略项目报备仅支持单条新建/编辑，不支持Excel批量导入。

### 其他按钮

| 序号 | 按钮名 | 显示条件 | 说明 |
|------|--------|---------|------|
| 1 | 保存 | 编辑模式 | 保存报备信息及经销商授权、网点信息 |
| 2 | 保存并提交 | 编辑模式且新建状态 | 保存后发起OA审批流程 |
| 3 | 删除 | 新建状态 | 删除报备记录（仅新建状态可删） |
| 4 | 申诉 | 被查重拦截(DUPLICATE_REPORTING=Y) | 申诉并重新提交，校验本月可用申诉次数 |
| 5 | 查重 | 编辑模式 | 手动触发项目查重，查询相似项目 |
| 6 | 查询周边项目 | 详情模式且有经纬度 | 按距离查询周边项目 |
| 7 | 二次报备 | 一次报备通过 | 发起二次报备流程 |

#### 按钮1：保存（详情页）

- **业务意义**：保存报备信息及关联的经销商授权、网点信息、乙方信息
- **具体逻辑描述**：调用POST /epm-reports接口，触发saveData保存逻辑，含必填校验和交易公司事业部关联校验

#### 按钮2：保存并提交（详情页）

- **业务意义**：保存后直接发起OA审批流程
- **具体逻辑描述**：先执行保存逻辑，再调用work-flow-start-volidate校验，通过后触发工作流PROJECT_ZLBB

#### 按钮3：删除（列表页）

- **业务意义**：删除未提交的报备记录
- **具体逻辑描述**：仅新建状态(HZ_APPROVE_STATUS='NEW')可删除，校验"存在不为新建状态的数据,不可删除"

#### 按钮4：申诉（列表页）

- **业务意义**：对被查重拦截的报备进行申诉重新提交
- **具体逻辑描述**：调用GET /appeal接口，校验DUPLICATE_REPORTING='Y'和本月申诉次数限制

#### 按钮5：查重（详情页）

- **业务意义**：手动触发项目查重，查询相似项目
- **具体逻辑描述**：调用GET /epm-reports接口执行doCheckReport查重

#### 按钮6：查询周边项目（详情页）

- **业务意义**：按距离查询当前项目周边的其他项目
- **具体逻辑描述**：调用POST /query-nearby-project接口，需先维护经纬度

#### 按钮7：二次报备（列表页）

- **业务意义**：对一次报备通过的项目发起二次报备
- **具体逻辑描述**：校验一次报备已通过且无在途二次报备，发起二次报备流程(reportTimes=2)

### 保存校验

- 校验1：项目名称非空 —— 报备必须填写项目名称
  - **详细逻辑**：saveData中校验projectName非空，为空抛出"项目名称不能为空"
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT REPORT_ID FROM EPM_REPORT WHERE PROJECT_NAME IS NULL;`

- 校验2：工程类型非空 —— 报备必须选择工程类型
  - **详细逻辑**：saveData中校验projectSource非空
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT REPORT_ID FROM EPM_REPORT WHERE PROJECT_SOURCE IS NULL;`

- 校验3：区域信息非空 —— 省/市/区ID和名称必须完整
  - **详细逻辑**：saveData中校验provinceId/cityId/areaId及对应名称非空
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT REPORT_ID FROM EPM_REPORT WHERE PROVINCE_ID IS NULL OR CITY_ID IS NULL OR AREA_ID IS NULL;`

- 校验4：交易公司事业部关联 —— 交易公司必须维护事业部关联关系
  - **详细逻辑**：saveData中校验DIVISION_TRADING_REL中存在交易公司关联记录
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT COUNT(1) FROM DIVISION_TRADING_REL WHERE TRADING_COMPANY_ID = #{tradingCompanyId};`

- 校验5：禁用区域校验 —— 报备所在区域不能为禁用区域
  - **详细逻辑**：saveData中校验AREA_ID不在DISABLED_AREA_CONFIG中
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT COUNT(1) FROM EPM_REPORT WHERE REPORT_ID = #{reportId} AND AREA_ID IN (SELECT AREA_ID FROM DISABLED_AREA_CONFIG);`

- 校验6：报备信息存在性 —— 编辑时校验报备ID对应记录存在
  - **详细逻辑**：编辑模式下校验EPM_REPORT中存在该REPORT_ID的记录
  - **系统体现**：后端saveData方法校验
  - **排查SQL**：`SELECT COUNT(1) FROM EPM_REPORT WHERE REPORT_ID = #{reportId};`

### 提交校验

- 校验1：查重拦截标记 —— 被标记为重复申报(Y)时必须通过申诉提交
  - **详细逻辑**：提交时校验DUPLICATE_REPORTING，为Y时需先申诉
  - **系统体现**：提交前前端校验
  - **排查SQL**：`SELECT DUPLICATE_REPORTING FROM EPM_REPORT WHERE REPORT_ID = #{reportId};`

- 校验2：可报备数量 —— 检查经销商可报备数量是否超限
  - **详细逻辑**：从SCPSYSCONF读取ReportCustomerApplyNum配置，校验经销商可报备数量
  - **系统体现**：提交时后端校验
  - **排查SQL**：`SELECT CONFVALUE FROM SCPSYSCONF WHERE CONFNAME = 'ReportCustomerApplyNum';`

- 校验3：战略二次报备经销商 —— 二次报备且管理类型为经销商操作/公司自营时必须分配经销商
  - **详细逻辑**：workFlowStartVolidate中校验EPM_REPORT_AUTH中已分配经销商，未分配返回"该战略工程涉及经销商服务，请分配具体经销商"
  - **系统体现**：GET /work-flow-start-volidate接口校验
  - **排查SQL**：`SELECT COUNT(1) FROM EPM_REPORT_AUTH WHERE REPORT_ID = #{reportId};`

- 校验4：EPMS-OA对应关系 —— 需维护OA表单对应关系
  - **详细逻辑**：提交时校验OA_BILL_REF中存在"战略项目报备一次"/"战略项目报备二次"配置
  - **系统体现**：提交时后端校验
  - **排查SQL**：`SELECT COUNT(1) FROM OA_BILL_REF WHERE BILLNAME IN ('战略项目报备一次', '战略项目报备二次');`

- 校验5：项目有效周期参数 —— 需配置公司参数Proj_Effective_Cycle
  - **详细逻辑**：审核通过时校验SYS_PARAM中存在Proj_Effective_Cycle配置
  - **系统体现**：审核通过回调时校验
  - **排查SQL**：`SELECT COUNT(1) FROM SYS_PARAM WHERE PARAM_CODE = 'Proj_Effective_Cycle' AND ORGANIZATION_ID = #{orgId};`

- 校验6：附件校验 —— 检查必传附件类型是否已上传
  - **详细逻辑**：提交时校验OBJ_ATTACH_TYPE中MIN_ATTACH_NEED>0的附件类型已上传
  - **系统体现**：提交时后端校验
  - **排查SQL**：`SELECT * FROM OBJ_ATTACH_TYPE WHERE ATTACH_CONF_ID = 8005 AND MIN_ATTACH_NEED > 0;`

### 状态机

```text
┌──────────┐  提交   ┌──────────┐  一次OA通过  ┌──────────┐
│  新建    │ ──────→ │ 一次报备  │ ──────────→ │ 一次通过  │
│ (NEW)   │         │ 提交(RUN) │            │(APPROVED) │
└──────────┘         └────┬─────┘            └────┬─────┘
      ↑                   │                       │
      │              一次OA拒绝              发起二次报备
      │                   │                       │
      └───────────────────┘                       ▼
                                          ┌──────────┐  二次OA通过  ┌──────────┐
                                          │ 二次报备  │ ──────────→ │ 二次通过  │
                                          │ 提交(RUN) │            │(APPROVED) │
                                          └────┬─────┘            └──────────┘
                                               │
                                         二次OA拒绝
                                               │
                                          ┌────▼─────┐
                                          │ 二次拒绝  │
                                          │(REJECTED) │
                                          └──────────┘
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 经销商主数据(CUSTOMER_ORG) | 数据依赖 | 报备关联经销商，经销商授权分配 | 经销商存在且DISABLED_FLAG=0 |
| 交易公司(EPM_TRADING_COMPANY) | 数据依赖 | 报备选择交易公司 | 交易公司存在且维护了事业部关联 |
| 事业部(DIVISION) | 数据依赖 | 报备所属事业部 | 当前用户归属事业部有效 |
| 行政区域 | 数据依赖 | 报备项目地址省/市/区 | 区域非禁用区域 |
| 值集配置 | 数据依赖 | 业主类型、产品线、背景关系等值集 | 值集已配置 |
| OA系统 | 流程依赖 | 报备提交走OA审批 | OA_BILL_REF中维护表单对应关系 |
| 工作流引擎 | 流程依赖 | 报备提交触发工作流 | 工作流编码PROJECT_ZLBB已配置 |
| 编码规则引擎 | 数据依赖 | 自动生成项目编码 | 编码规则PROJECT_CODE已配置 |
| 公司参数(SYS_PARAM) | 数据依赖 | 项目有效周期计算 | Proj_Effective_Cycle参数已配置 |
| CRM系统 | 接口依赖 | 审核通过推送项目报备 | CRM推送接口可用 |

### 下游影响

- **项目档案(EPM_PROJECT)**：一次报备审核通过后生成项目记录，二次报备通过后更新项目字段
- **项目授权(EPM_PROJECT_AUTH)**：审核通过后从报备经销商授权转为项目授权
- **项目乙方信息**：审核通过后生成项目乙方信息记录
- **合同**：项目档案生成后可关联创建工程合同
- **折扣政策**：项目档案生成后可关联创建折扣政策
- **CRM系统**：审核通过后推送项目报备数据至CRM
- **项目透视**：项目档案可供项目透视查询

## 数据库表详解

### EPM_REPORT（项目报备主表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| REPORT_ID | BIGINT | 报备ID，主键 | 报备ID | 系统自动生成 |
| REPORT_TIMES | BIGINT | 报备次数 | 报备次数 | 1=一次报备, 2=二次报备 |
| ORGANIZATION_ID | BIGINT | 组织ID | - | 系统自动填充 |
| PROJECT_ID | BIGINT | 项目ID | - | 审核通过后关联项目档案 |
| PROJECT_CODE | VARCHAR | 项目编码 | 项目编码 | 编码规则引擎自动生成 |
| PROJECT_NAME | VARCHAR | 项目名称 | 项目名称 | 手动输入，必填 |
| REL_PROJECT_ID | BIGINT | 关联战略项目ID | - | 单体报备关联战略时使用 |
| PROJECT_SOURCE | VARCHAR | 项目来源 | 工程类型 | 1=常规工程, 2=战略工程 |
| REPORT_TIME | DATETIME | 报备时间 | - | 系统自动记录 |
| REPORT_TYPE | BIGINT | 报备类型 | - | 固定值2(战略报备) |
| STAGE_ID | BIGINT | 项目进度阶段ID | 项目进度阶段 | 选择弹窗回写 |
| STAGE_NAME | VARCHAR | 项目进度阶段名称 | 项目进度阶段 | 选择弹窗回写 |
| PROJECT_TYPE | VARCHAR | 业主类型 | 业主类型 | 值集epm.project_type |
| PROVINCE_ID | BIGINT | 省ID | 省 | 级联选择回写，必填 |
| CITY_ID | BIGINT | 市ID | 市 | 级联选择回写，必填 |
| AREA_ID | BIGINT | 区县ID | 区 | 级联选择回写，必填 |
| ADDRESS | VARCHAR | 详细地址 | 详细地址 | 手动输入，必填 |
| DIVISION_ID | BIGINT | 所属事业部ID | 所属事业部 | 系统自动带出 |
| CUSTOMER_ID | BIGINT | 客户/经销商ID | 客户编码 | 选择弹窗回写，必填 |
| CUSTOMER_CODE | VARCHAR | 客户编码 | 客户编码 | 选择弹窗回写 |
| TRADING_COMPANY_ID | BIGINT | 交易公司ID | 交易公司 | 选择弹窗回写，必填 |
| PARTY_A_NAME | VARCHAR | 甲方名称 | 甲方名称 | 手动输入，必填 |
| PARTY_A_LINK_PERSON | VARCHAR | 甲方联系人 | 甲方联系人 | 手动输入，必填 |
| PARTY_A_PHONE | VARCHAR | 甲方电话 | 甲方电话 | 手动输入，必填 |
| PARTY_B_NAME | VARCHAR | 乙方名称 | 乙方名称 | 手动输入，必填 |
| PARTY_B_LINK_PERSON | VARCHAR | 乙方联系人 | 乙方联系人 | 手动输入，必填 |
| PARTY_B_PHONE | VARCHAR | 乙方电话 | 乙方电话 | 手动输入，必填 |
| DEALER_FOLLOWER | VARCHAR | 经销商现场跟进人 | 经销商现场跟进人 | 手动输入 |
| OWN_FOLLOWER | VARCHAR | 公司现场跟进人 | 公司现场跟进人 | 手动输入 |
| IS_LOCAL | BIGINT | 本地/异地 | 本地/异地 | 1=异地, 2=本地, 3=本地特约，必填 |
| OPERATING_MODE | BIGINT | 管理类型 | 管理类型 | 1=经销商操作, 2=公司自营 |
| PDT_LINE | BIGINT | 产品线 | 产品线 | 值集配置，必填 |
| PREDICT_SIGN_DATE | DATETIME | 预计签订日期 | 预计签订日期 | 手动选择 |
| PREDICT_SALES_AMOUNT | DECIMAL | 预计销售收入 | 预计销售收入 | 手动输入，必填 |
| BACKGROUND | BIGINT | 背景关系 | 背景关系 | 值集配置，必填 |
| PREDICT_PROJ_QTY | VARCHAR | 预估项目数量 | 预估项目数量 | 手动输入 |
| PREDICT_PDT_QTY | VARCHAR | 预估产品用量 | 预估产品用量 | 手动输入，必填 |
| INTENT_PRODUCT | VARCHAR | 工程意向产品 | 工程意向产品 | 手动输入，必填 |
| COMPETITIVE_BRAND | VARCHAR | 竞争品牌 | 竞争品牌 | 手动输入 |
| COMPETITOR | VARCHAR | 竞争对手 | 竞争对手 | 手动输入，必填 |
| SITE_AREA | DECIMAL | 工程建筑面积 | 工程建筑面积 | 手动输入 |
| CONSTRUCTION_STAGE | VARCHAR | 工程施工阶段 | 工程施工阶段 | 手动输入，必填 |
| COMPLETION_DATE | DATETIME | 完工日期 | 完工日期 | 手动选择，必填 |
| AGENT | VARCHAR | 经办人 | 经办人 | 系统自动带出 |
| NEED_SAMPLE | VARCHAR | 产品送样 | 产品送样 | Y/N，必填 |
| NEED_QUOTE | VARCHAR | 产品报价 | 产品报价 | Y/N，必填 |
| MANAGER | VARCHAR | 项目经理 | 项目经理 | 手动输入 |
| EMPID | VARCHAR | 项目经理工号 | 项目经理工号 | 手动输入 |
| STRATEGIC_STAGE | VARCHAR | 战略对接阶段 | 战略对接阶段 | 手动输入 |
| NEED_DEPOSIT | BIGINT | 缴纳保证金 | 缴纳保证金 | 1=同意, 2=不同意，必填 |
| DEPOSIT_AMOUNT | DECIMAL | 保证金金额 | 保证金金额 | needDeposit=1时填写 |
| DEPOSIT_NOTE | VARCHAR | 保证金备注 | 保证金备注 | needDeposit=1时填写 |
| IS_FOREIGN | VARCHAR | 是否海外 | 海外 | Y/N |
| TASK_SHARED_RATE | DECIMAL | 任务划分比例 | 任务划分比例 | 手动输入 |
| SERVICE_FEE_SHARED_RATE | DECIMAL | 售后服务金划分比例 | 售后服务金划分比例 | 手动输入 |
| COOPERATION_AREA | VARCHAR | 合作区域 | 合作区域 | 手动输入 |
| STRAC_COOP_INV_REGION | VARCHAR | 战略合作范围涉及区域 | 战略合作范围涉及区域 | 手动输入 |
| DUPLICATE_REPORTING | VARCHAR | 查重拦截标记 | 查重拦截标记 | Y=重复申报, N=正常 |
| LONGITUDE | DECIMAL | 经度 | 经度 | 地图选点回写 |
| LATITUDE | DECIMAL | 纬度 | 纬度 | 地图选点回写 |
| LANDMARK_FLAG | VARCHAR | 地标建筑 | 地标建筑 | Y/N，默认N |
| PROJECT_CATEGORY | VARCHAR | 项目分类 | 项目分类 | normal=标准项目, small=小型项目 |
| HZ_INSTANCE_ID | BIGINT | H0流程实例ID | - | 工作流引擎回写 |
| HZ_APPROVE_STATUS | VARCHAR | H0流程审批状态 | 审批状态 | NEW/RUN/APPROVED/REJECTED |
| AUDIT_STAT | VARCHAR | 审核状态 | - | 新建/一次报备提交/一次报备通过/一次报备拒绝/二次报备提交/二次报备通过/二次报备拒绝 |
| CREATION_DATE | DATETIME | 创建时间 | - | 系统自动记录 |
| CREATED_BY | BIGINT | 创建人 | - | 系统自动记录 |
| LAST_UPDATE_DATE | DATETIME | 最后更新时间 | - | 系统自动记录 |
| LAST_UPDATED_BY | BIGINT | 最后更新人 | - | 系统自动记录 |
| OBJECT_VERSION_NUMBER | BIGINT | 乐观锁版本号 | - | 系统自动维护 |

### EPM_REPORT_AUTH（项目报备经销商授权表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| ID | BIGINT | 主键ID | - | 系统自动生成 |
| REPORT_ID | BIGINT | 报备ID | - | 关联EPM_REPORT |
| CUSTOMER_ID | BIGINT | 经销商ID | 经销商ID | 选择经销商弹窗回写 |
| CUSTOMER_CODE | VARCHAR | 经销商编码 | 经销商编码 | 选择经销商弹窗回写 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商名称 | 选择经销商弹窗回写 |
| CONTACT | VARCHAR | 经办人 | 经办人 | 手动输入 |
| TELE | VARCHAR | 经办人电话 | 经办人电话 | 手动输入 |
| DISABLED | BIGINT | 禁用标记 | - | 2=禁用 |
| IS_INIT | BIGINT | 初始化标记 | - | 2=初始化产生 |

### EPM_REPORT_AUTH_BRANCH（经销商网点信息表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PK_ID | BIGINT | 主键 | - | 系统自动生成 |
| REPORT_ID | BIGINT | 报备ID | - | 关联EPM_REPORT |
| REL_CUSTOMER_CODE | VARCHAR | 对应经销商编码 | 对应经销商编码 | 关联经销商授权行 |
| BRANCH_MESSAGE_ID | BIGINT | 网点ID | 网点ID | 选择网点弹窗回写 |
| DISABLED | BIGINT | 禁用标记 | - | 2=禁用 |

### EPM_REPORT_PARTYB（项目报备乙方信息表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PK_ID | BIGINT | 主键 | - | 系统自动生成 |
| REPORT_ID | BIGINT | 报备ID | - | 关联EPM_REPORT |
| SEQ | BIGINT | 序号 | - | 系统自动生成 |
| PARTYB_NAME | VARCHAR | 乙方名称 | 乙方名称 | 手动输入 |
| PARTYB_LINK_PERSON | VARCHAR | 乙方联系人 | 乙方联系人 | 手动输入 |
| PARTYB_PHONE | VARCHAR | 乙方联系电话 | 乙方电话 | 手动输入 |

### EPM_PROJECT（项目档案表 - 报备审核通过生成）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PROJECT_ID | BIGINT | 项目ID，主键 | - | 审核通过后自动生成 |
| PROJECT_CODE | VARCHAR | 项目编码 | - | 从EPM_REPORT复制 |
| PROJECT_NAME | VARCHAR | 项目名称 | - | 从EPM_REPORT复制 |
| REPORT_ID | BIGINT | 报备ID | - | 关联EPM_REPORT |
| REPORT_TYPE | BIGINT | 报备类型 | - | 2=战略报备 |
| REPORT_TIMES | BIGINT | 报备次数 | - | 从EPM_REPORT复制 |
| HZ_APPROVE_STATUS | VARCHAR | 审批状态 | - | 从EPM_REPORT复制 |
| PROJECT_VALID | BIGINT | 有效状态 | - | 2=有效, 3=失效, 4=冻结 |
| VALID_START_DATE | DATETIME | 有效起始日期 | - | 审核通过时设置 |
| VALID_END_DATE | DATETIME | 有效结束日期 | - | 基于Proj_Effective_Cycle计算 |
| FREEZE_TYPE | BIGINT | 冻结类型 | - | 0=未冻结, 1=未签合同, 2=进度超时, 4=已签合同 |

### EPM_PROJECT_AUTH（项目授权表 - 报备审核通过生成）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PK_ID | BIGINT | 主键 | - | 系统自动生成 |
| PROJECT_ID | BIGINT | 项目ID | - | 关联EPM_PROJECT |
| CUSTOMER_ID | BIGINT | 经销商ID | - | 从EPM_REPORT_AUTH转换 |
| CUSTOMER_CODE | VARCHAR | 经销商编码 | - | 从EPM_REPORT_AUTH转换 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | - | 从EPM_REPORT_AUTH转换 |

### OA_BILL_REF（OA表单对应关系表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| ID | BIGINT | 主键 | - | 系统自动生成 |
| BILLNAME | VARCHAR | 单据名称 | - | "战略项目报备一次"/"战略项目报备二次" |
| OABILLID | VARCHAR | OA表单ID | - | OA系统配置 |

### EPM_REPORT_APPEAL_CONFIG（报备申诉次数配置表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| ID | BIGINT | 主键 | - | 系统自动生成 |
| CUSTOMER_ID | BIGINT | 经销商ID | - | 按经销商配置申诉次数 |
| APPEAL_YEAR | BIGINT | 申诉年份 | - | 按年配置 |
| APPEAL_MONTH | BIGINT | 申诉月份 | - | 按月配置 |
| APPEAL_NUM | BIGINT | 可申诉次数 | - | 基础申诉次数 |
| APPEAL_NUM_TMP | BIGINT | 临时追加次数 | - | 临时追加的申诉次数 |

### EPM_REPORT_INTERCEPT_HEAD（报备查重拦截头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| ID | BIGINT | 主键 | - | 系统自动生成 |
| REPORT_ID | BIGINT | 报备ID | - | 关联EPM_REPORT |
| CUSTOMER_ID | BIGINT | 经销商ID | - | 拦截关联经销商 |
| INTERCEPT_DATE | DATETIME | 拦截日期 | - | 系统自动记录 |
| APPEALER | VARCHAR | 申诉人 | - | 申诉时记录 |
| APPEAL_DATE | DATETIME | 申诉日期 | - | 申诉时记录 |
| APPEAL_PROJECT_NAME | VARCHAR | 申诉项目名称 | - | 申诉时记录 |
| APPEAL_ADDRESS | VARCHAR | 申诉项目地址 | - | 申诉时记录 |

### 查询SQL

```sql
-- 查询战略项目报备列表
SELECT * FROM EPM_REPORT
WHERE REPORT_TYPE = 2
  AND ORGANIZATION_ID = #{organizationId}
ORDER BY REPORT_ID DESC;

-- 查询报备经销商授权
SELECT * FROM EPM_REPORT_AUTH
WHERE REPORT_ID = #{reportId}
  AND DISABLED != 2;

-- 查询报备网点信息
SELECT * FROM EPM_REPORT_AUTH_BRANCH
WHERE REPORT_ID = #{reportId}
  AND DISABLED != 2;

-- 查询在途二次报备
SELECT * FROM EPM_REPORT
WHERE PROJECT_CODE = #{projectCode}
  AND REPORT_TIMES = 2
  AND HZ_APPROVE_STATUS = 'APPROVED';

-- 查询本月申诉次数
SELECT COUNT(1) FROM EPM_REPORT_INTERCEPT_HEAD
WHERE CUSTOMER_ID = #{customerId}
  AND INTERCEPT_DATE BETWEEN #{startDate} AND #{endDate};
```

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 参数不能为空 | 保存时 | 必填参数缺失，补全必填参数后重新保存 | 高 | saveData中validObject校验 |
| 项目名称不能为空 | 保存时 | projectName为空，补填项目名称 | 高 | saveData必填校验 |
| 未找到经销商ID | 保存时 | 根据projectId未查到经销商，先维护经销商信息 | 高 | saveData经销商校验 |
| 工程类型不能为空 | 保存时 | projectSource为空，选择工程类型 | 高 | saveData必填校验 |
| 交易公司id必须大于0 | 保存时 | tradingCompanyId<=0，检查前端传参 | 高 | saveData交易公司校验 |
| {tradingCompany}交易公司未维护事业部关联关系 | 保存时 | 交易公司未关联事业部，先维护DIVISION_TRADING_REL | 高 | saveData事业部关联校验 |
| 当前区域为禁用区域 | 保存时 | 报备区域在DISABLED_AREA_CONFIG中，更换区域 | 高 | saveData禁用区域校验 |
| 报备信息不存在 | 编辑保存时 | 报备ID对应记录不存在，检查REPORT_ID | 高 | saveData编辑校验 |
| 该战略工程涉及经销商服务，请分配具体经销商 | 二次报备提交时 | 管理类型为经销商操作/公司自营但未分配经销商，先分配经销商 | 高 | workFlowStartVolidate校验 |
| 请先维护EPMS单据和OA表单对应关系表 | 提交时 | OA_BILL_REF中缺少表单配置，维护OA表单对应关系 | 高 | 提交时OA对应关系校验 |
| 非重复申报，不能申诉 | 申诉时 | DUPLICATE_REPORTING不为Y，仅重复申报可申诉 | 中 | appeal校验 |
| 本月申诉次数已到达限制:{n}次 | 申诉时 | 本月申诉次数超限，下月再申诉或申请追加次数 | 中 | appeal申诉次数校验 |
| 项目【{code}】已二次报备，请选择其他项目 | 发起二次报备时 | 该项目已有审核通过的二次报备，选择其他项目 | 高 | 二次报备前校验 |
| 请配置{orgId}公司参数'Proj_Effective_Cycle' | 审核通过时 | 未配置项目有效周期参数，配置SYS_PARAM | 高 | 审核回调校验 |
| 经销商可报备数量剩余{n}条 | 提交时 | 经销商可报备数量不足，检查ReportCustomerApplyNum配置 | 中 | 提交时可报备数量校验 |
| 战略项目报备一次OA回调处理异常 | OA回调时 | OA一次审批回调处理异常，检查OA回调参数及报备数据 | 高 | doStrategicProjectResp异常 |
| 战略项目报备二次OA回调处理异常 | OA回调时 | OA二次审批回调异常，检查OA回调参数及报备数据 | 高 | doStrategicProjectResp异常 |
| 存在不为新建状态的数据,不可删除 | 删除时 | 选中的数据中有非新建状态，仅新建状态可删除 | 中 | 删除前状态校验 |
| 请先维护经纬度再查询 | 查询周边项目时 | 经纬度为空，先维护经纬度 | 低 | queryNearbyProject校验 |
| 项目【{code}】已二次报备，但尚未启动流程 | 发起二次报备时 | 该项目已有二次报备记录但流程未启动，等待流程启动或选择其他项目 | 高 | 二次报备前校验HZ_APPROVE_STATUS非APPROVED |
| ID不能为空 | OA回调时 | OA回调时ID参数为空或<=0，检查OA回调配置 | 高 | doProcessOA校验id非空 |
| 报备数据不存在！ | OA回调时 | 根据ID查询EPM_REPORT无数据，检查报备是否被删除 | 高 | doProcessOA中selectByPrimaryKey返回null |
| 没有找到项目报备信息！ | ES查重时 | 根据reportId查询报备无数据，检查报备数据 | 高 | getRepetitionReport中selectByPrimaryKey返回null |
| 未找到报备信息 | 查询详情时 | 根据查询条件无报备数据，检查查询参数 | 高 | getHomeDecorationReportDetail查询为空 |
| 项目报备不允许取消归档 | 取消归档时 | 报备不支持取消归档操作 | 高 | cancelArchive校验 |
| 请通过申诉提交 | 提交时 | 报备被查重拦截(DUPLICATE_REPORTING=Y)，需通过申诉重新提交 | 高 | 提交时校验DUPLICATE_REPORTING |
| 请先配置报备的可申诉次数 | 申诉时 | 未配置申诉次数(SCPSYSCONF或EPM_REPORT_APPEAL_CONFIG)，先维护配置 | 高 | appeal校验申诉次数配置 |
| 申诉失败:{msg} | 申诉时 | 申诉处理异常，检查申诉参数及报备数据 | 高 | appeal异常try-catch |
| OA回传单号不存在，请检查！ | OA回调时 | OA回调时根据ID查不到报备记录，可能数据被删除或ID传递错误 | 高 | doProcessOA中selectByPrimaryKey返回null |
| 查询参数不能为空 | 查询时 | 查询参数为空，补全查询参数 | 高 | 查询方法校验参数非空 |
| 主键不能为空! | 查询详情时 | 详情查询时未传入主键ID | 高 | detail方法校验reportId为空 |
| 操作数据不能为空 | 删除时 | 删除时未选择数据，先选择待删除数据 | 高 | delete方法校验reportIdList非空 |
| {tradingCompanyName}交易公司不存在,请先维护! | 保存时 | 交易公司在EPM_TRADING_COMPANY中不存在，先维护交易公司主数据 | 高 | saveData交易公司存在性校验 |
| 本月总可用申诉次数({totalNum}次)小于等于已用申诉次数({usedNum}次)，无法进行申诉 | 申诉时 | 本月可用申诉次数已用完，下月再申诉或申请追加次数 | 中 | appeal校验可用次数<=已用次数 |
| 请先配置系统参数-经销商可报备数量:ReportCustomerApplyNum | 提交时 | 未配置经销商可报备数量参数，先维护SCPSYSCONF配置 | 高 | 提交时校验ReportCustomerApplyNum配置 |
| 当前事业部数据异常 | 保存时 | 当前用户所属事业部数据异常，检查事业部配置 | 高 | saveData事业部校验 |

#### 报错1：参数不能为空
- **触发条件**：保存战略项目报备时，必填参数缺失
- **逻辑分析**：saveData方法中通过validObject校验DTO必填字段，缺失则抛出阻断性报错。需补全必填参数后重新保存
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.PROJECT_NAME, er.CUSTOMER_CODE,
         er.TRADING_COMPANY_ID, er.PROJECT_SOURCE, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 检查关键字段是否完整
  ```

#### 报错2：项目名称不能为空
- **触发条件**：保存战略项目报备时，projectName为空
- **逻辑分析**：saveData方法中校验PROJECT_NAME非空，因项目名称为报备必填项。需补填项目名称
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_NAME
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND (er.PROJECT_NAME IS NULL OR TRIM(er.PROJECT_NAME) = '')
  ```

#### 报错3：未找到经销商ID
- **触发条件**：保存战略项目报备时，根据projectId未查到经销商
- **逻辑分析**：saveData方法中按PROJECT_ID查询经销商信息，若未查到则抛出阻断性报错。需先维护经销商信息
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.PROJECT_ID, er.CUSTOMER_ID, er.CUSTOMER_CODE, er.CUSTOMER_NAME
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.CUSTOMER_ID IS NULL
  ```

#### 报错4：工程类型不能为空
- **触发条件**：保存战略项目报备时，projectSource(工程类型)为空
- **逻辑分析**：saveData方法中校验PROJECT_SOURCE非空，因工程类型决定报备后续流程。需选择工程类型
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_SOURCE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.PROJECT_SOURCE IS NULL
  ```

#### 报错5：交易公司id必须大于0
- **触发条件**：保存战略项目报备时，tradingCompanyId<=0
- **逻辑分析**：saveData方法中校验TRADING_COMPANY_ID>0，因交易公司为报备必选关联主数据。需检查前端传参
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.TRADING_COMPANY_ID, er.TRADING_COMPANY_NAME
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND (er.TRADING_COMPANY_ID IS NULL OR er.TRADING_COMPANY_ID <= 0)
  ```

#### 报错6：{tradingCompany}交易公司未维护事业部关联关系
- **触发条件**：保存战略项目报备时，交易公司未关联事业部(DIVISION_TRADING_REL)
- **逻辑分析**：saveData方法中按TRADING_COMPANY_ID查询DIVISION_TRADING_REL，若为空则抛出阻断性报错。需先维护DIVISION_TRADING_REL
- **排查SQL**：
  ```sql
  SELECT er.TRADING_COMPANY_ID, er.TRADING_COMPANY_NAME,
         dtr.DIVISION_ID AS 事业部关联存在性
  FROM EPM_REPORT er
  LEFT JOIN DIVISION_TRADING_REL dtr ON er.TRADING_COMPANY_ID = dtr.TRADING_COMPANY_ID
  WHERE er.REPORT_ID = :reportId
    AND dtr.DIVISION_ID IS NULL
  ```

#### 报错7：当前区域为禁用区域
- **触发条件**：保存战略项目报备时，报备区域在DISABLED_AREA_CONFIG中
- **逻辑分析**：saveData方法中按区域编码查询DISABLED_AREA_CONFIG，若存在则抛出阻断性报错。需更换区域
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.AREA_CODE, er.AREA_FULL_NAME,
         dac.DISABLED_AREA_ID, dac.AREA_CODE AS 禁用区域编码
  FROM EPM_REPORT er
  JOIN DISABLED_AREA_CONFIG dac ON er.AREA_CODE = dac.AREA_CODE
  WHERE er.REPORT_ID = :reportId
  ```

#### 报错8：报备信息不存在
- **触发条件**：编辑保存战略项目报备时，报备ID对应记录不存在
- **逻辑分析**：saveData编辑分支中按REPORT_ID查询EPM_REPORT，若返回null则抛出阻断性报错。需检查REPORT_ID有效性
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.HZ_APPROVE_STATUS, er.VALID
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 若返回空，说明报备信息不存在
  ```

#### 报错9：该战略工程涉及经销商服务，请分配具体经销商
- **触发条件**：二次报备提交时，管理类型为经销商操作/公司自营但未分配经销商
- **逻辑分析**：workFlowStartVolidate方法中校验管理类型为经销商操作或公司自营时必须分配具体经销商，若未分配则抛出阻断性报错。需先分配经销商
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.MANAGE_TYPE, er.CUSTOMER_ID, er.CUSTOMER_CODE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.MANAGE_TYPE IN ('DEALER_OPERATE', 'COMPANY_SELF')
    AND er.CUSTOMER_ID IS NULL
  ```

#### 报错10：请先维护EPMS单据和OA表单对应关系表
- **触发条件**：提交战略项目报备时，OA_BILL_REF中缺少表单配置
- **逻辑分析**：提交方法中按BILL_NAME查询EPM_OA_BILL_REF，若为空则抛出阻断性报错。需维护OA表单对应关系
- **排查SQL**：
  ```sql
  SELECT eoaf.OA_BILL_REF_ID, eoaf.BILL_NAME, eoaf.OA_BILL_ID, eoaf.OA_FORM_KEY
  FROM EPM_OA_BILL_REF eoaf
  WHERE eoaf.BILL_NAME IN ('战略项目报备一次', '战略项目报备二次')
  -- 若返回空，则需维护OA单据关系
  ```

#### 报错11：非重复申报，不能申诉
- **触发条件**：申诉时，DUPLICATE_REPORTING不为Y
- **逻辑分析**：appeal方法中校验DUPLICATE_REPORTING=Y，仅重复申报(查重拦截)可申诉。该报错为中等优先级
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.DUPLICATE_REPORTING, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.DUPLICATE_REPORTING <> 'Y'
  ```

#### 报错12：本月申诉次数已到达限制:{n}次
- **触发条件**：申诉时，本月申诉次数超过限制
- **逻辑分析**：appeal方法中按CUSTOMER_ID和年月查询EPM_REPORT_APPEAL_CONFIG，若申诉次数超限则抛出中等优先级报错。需下月再申诉或申请追加次数
- **排查SQL**：
  ```sql
  SELECT erac.CUSTOMER_ID, erac.APPEAL_YEAR, erac.APPEAL_MONTH,
         erac.APPEAL_NUM, erac.APPEAL_NUM_TMP
  FROM EPM_REPORT_APPEAL_CONFIG erac
  WHERE erac.CUSTOMER_ID = :customerId
    AND erac.APPEAL_YEAR = EXTRACT(YEAR FROM SYSDATE)
    AND erac.APPEAL_MONTH = EXTRACT(MONTH FROM SYSDATE)
  ```

#### 报错13：项目【{code}】已二次报备，请选择其他项目
- **触发条件**：发起二次报备时，该项目已有审核通过的二次报备
- **逻辑分析**：二次报备前校验中按PROJECT_CODE查询EPM_REPORT筛选REPORT_TIMES=2且HZ_APPROVE_STATUS='APPROVED'，若存在则抛出阻断性报错。需选择其他项目
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.REPORT_TIMES, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.PROJECT_CODE = :projectCode
    AND er.REPORT_TIMES = 2
    AND er.HZ_APPROVE_STATUS = 'APPROVED'
  ```

#### 报错14：请配置{orgId}公司参数'Proj_Effective_Cycle'
- **触发条件**：审核通过时，未配置项目有效周期参数(PROJ_EFFECTIVE_CYCLE)
- **逻辑分析**：审核回调中按ORGANIZATION_ID和PARAM_CODE='Proj_Effective_Cycle'查询SYS_PARAM，若为空则抛出阻断性报错。需配置SYS_PARAM
- **排查SQL**：
  ```sql
  SELECT sp.PARAM_ID, sp.PARAM_CODE, sp.PARAM_NAME, sp.VALUE, sp.ORGANIZATION_ID
  FROM SYS_PARAM sp
  WHERE sp.PARAM_CODE = 'Proj_Effective_Cycle'
    AND sp.ORGANIZATION_ID = :orgId
  -- 若返回空，则需补充配置
  ```

#### 报错15：经销商可报备数量剩余{n}条
- **触发条件**：提交战略项目报备时，经销商可报备数量不足
- **逻辑分析**：提交校验中按CUSTOMER_ID查询ReportCustomerApplyNum配置的可报备数量，若剩余不足则抛出中等优先级报错。需检查ReportCustomerApplyNum配置
- **排查SQL**：
  ```sql
  SELECT rcapn.CUSTOMER_ID, rcapn.APPLY_NUM, rcapn.USED_NUM,
         rcapn.APPLY_NUM - rcapn.USED_NUM AS 剩余可报备数量
  FROM REPORT_CUSTOMER_APPLY_NUM rcapn
  WHERE rcapn.CUSTOMER_ID = :customerId
  ```

#### 报错16：战略项目报备一次OA回调处理异常
- **触发条件**：OA一次审批回调处理异常
- **逻辑分析**：doStrategicProjectResp方法中处理一次报备OA回调，若异常则抛出阻断性报错。需检查OA回调参数及报备数据
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.REPORT_TIMES, er.HZ_APPROVE_STATUS,
         er.HZ_INSTANCE_ID, er.CALLBACK_SOURCE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.REPORT_TIMES = 1
  ```

#### 报错17：战略项目报备二次OA回调处理异常
- **触发条件**：OA二次审批回调处理异常
- **逻辑分析**：doStrategicProjectResp方法中处理二次报备OA回调，若异常则抛出阻断性报错。需检查OA回调参数及报备数据
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.REPORT_TIMES, er.HZ_APPROVE_STATUS,
         er.HZ_INSTANCE_ID, er.CALLBACK_SOURCE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.REPORT_TIMES = 2
  ```

#### 报错18：存在不为新建状态的数据,不可删除
- **触发条件**：删除战略项目报备时，选中的数据中有非新建状态
- **逻辑分析**：删除前校验中检查所有选中数据状态为NEW，若存在非NEW状态则抛出中等优先级报错。仅新建状态可删除
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS, er.VALID
  FROM EPM_REPORT er
  WHERE er.REPORT_ID IN (:reportIdList)
    AND er.HZ_APPROVE_STATUS <> 'NEW'
  -- 查出非新建状态的数据
  ```

#### 报错19：请先维护经纬度再查询
- **触发条件**：查询周边项目时，经纬度为空
- **逻辑分析**：queryNearbyProject方法中校验LATITUDE和LONGITUDE非空，因周边项目查询需基于经纬度计算距离。需先维护经纬度
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.LATITUDE, er.LONGITUDE, er.ADDRESS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND (er.LATITUDE IS NULL OR er.LONGITUDE IS NULL)
  ```

#### 报错20：项目【{code}】已二次报备，但尚未启动流程
- **触发条件**：发起二次报备时，该项目已有二次报备记录(REPORT_TIMES=2)但审批状态非APPROVED(流程未启动或审批中)
- **逻辑分析**：二次报备前校验中按PROJECT_CODE查询EPM_REPORT筛选REPORT_TIMES=2，若存在且HZ_APPROVE_STATUS非APPROVED则抛出阻断性报错。需等待原二次报备流程完成或选择其他项目
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.REPORT_TIMES,
         er.HZ_APPROVE_STATUS, er.HZ_INSTANCE_ID
  FROM EPM_REPORT er
  WHERE er.PROJECT_CODE = :projectCode
    AND er.REPORT_TIMES = 2
    AND er.HZ_APPROVE_STATUS <> 'APPROVED'
  -- 查出未完成的二次报备记录
  ```

#### 报错21：ID不能为空
- **触发条件**：OA审批回调(doProcessOA)时，传入的ID参数为空或<=0
- **逻辑分析**：doProcessOA方法中校验dto.getId()非空且>0，因OA回调需按ID查询EPM_REPORT报备记录。需检查OA回调配置和参数传递
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS, er.HZ_INSTANCE_ID
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :id
  -- 校验传入的ID是否存在
  ```

#### 报错22：报备数据不存在！
- **触发条件**：OA审批回调(doProcessOA)时，根据ID查询EPM_REPORT返回null
- **逻辑分析**：doProcessOA方法中epmReportRepository.selectByPrimaryKey(id)按REPORT_ID查询报备，若报备被物理删除或ID错误则返回null。需检查报备数据是否完整
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 若返回空，说明报备数据不存在
  ```

#### 报错23：没有找到项目报备信息！
- **触发条件**：ES查重(getRepetitionReport)时，根据reportId查询EPM_REPORT返回null
- **逻辑分析**：getRepetitionReport方法中按REPORT_ID查询报备信息用于ES查重，若返回null则抛出阻断性报错。需检查报备数据是否存在
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.REPORT_TYPE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 若返回空，说明报备信息不存在
  ```

#### 报错24：未找到报备信息
- **触发条件**：查询报备详情(getHomeDecorationReportDetail)时，根据查询条件无匹配报备数据
- **逻辑分析**：getHomeDecorationReportDetail方法中按查询条件查询报备列表，若为空则抛出阻断性报错。需检查查询参数(reportId等)是否正确
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 若返回空，说明未找到报备信息
  ```

#### 报错25：项目报备不允许取消归档
- **触发条件**：对项目报备执行取消归档操作时
- **逻辑分析**：cancelArchive方法中直接抛出阻断性报错，因项目报备不支持取消归档操作。需通过其他方式(如失效申请)处理报备状态
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS, er.VALID
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 查出报备当前状态
  ```

#### 报错26：请通过申诉提交
- **触发条件**：提交战略项目报备时，报备被查重拦截标记为重复申报(DUPLICATE_REPORTING=Y)
- **逻辑分析**：提交方法中校验DUPLICATE_REPORTING=Y时，将报备状态重置为NEW并抛出阻断性报错。被查重拦截的报备必须通过appeal接口申诉重新提交
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.DUPLICATE_REPORTING, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
    AND er.DUPLICATE_REPORTING = 'Y'
  -- 查出被查重拦截的报备
  ```

#### 报错27：请先配置报备的可申诉次数
- **触发条件**：申诉时，未配置报备的可申诉次数(SCPSYSCONF中ReportAppealNum或EPM_REPORT_APPEAL_CONFIG)
- **逻辑分析**：appeal方法中按CUSTOMER_ID和年月查询EPM_REPORT_APPEAL_CONFIG，若为空则查询SCPSYSCONF中ReportAppealNum配置，两者都为空则抛出阻断性报错。需先维护申诉次数配置
- **排查SQL**：
  ```sql
  SELECT s.CONFNAME, s.CONFVALUE
  FROM SCPSYSCONF s
  WHERE s.CONFNAME = 'ReportAppealNum'
  UNION ALL
  SELECT 'EPM_REPORT_APPEAL_CONFIG' AS CONFNAME,
         TO_CHAR(erac.APPEAL_NUM + NVL(erac.APPEAL_NUM_TMP, 0)) AS CONFVALUE
  FROM EPM_REPORT_APPEAL_CONFIG erac
  WHERE erac.CUSTOMER_ID = :customerId
    AND erac.APPEAL_YEAR = EXTRACT(YEAR FROM SYSDATE)
    AND erac.APPEAL_MONTH = EXTRACT(MONTH FROM SYSDATE)
  -- 若两者都为空，则需配置申诉次数
  ```

#### 报错28：申诉失败:{msg}
- **触发条件**：申诉处理过程中抛出异常
- **逻辑分析**：appeal方法中try-catch捕获申诉处理异常，将异常消息拼接后抛出。可能原因：报备数据异常、拦截记录不存在、更新失败。需检查申诉参数及报备数据
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.DUPLICATE_REPORTING, er.HZ_APPROVE_STATUS,
         erih.ID AS 拦截记录ID, erih.INTERCEPT_DATE, erih.APPEAL_DATE
  FROM EPM_REPORT er
  LEFT JOIN EPM_REPORT_INTERCEPT_HEAD erih ON er.REPORT_ID = erih.REPORT_ID
  WHERE er.REPORT_ID = :reportId
  -- 检查报备及拦截记录数据完整性
  ```

#### 报错29：OA回传单号不存在，请检查！
- **触发条件**：OA审批回调(doProcessOA)时，根据传入ID查询EPM_REPORT报备记录返回null
- **逻辑分析**：doProcessOA方法中selectByPrimaryKey按REPORT_ID查询报备，若返回null则抛出阻断性报错。可能原因：报备被删除、OA传递的ID错误、ID类型转换异常
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS, er.CALLBACK_SOURCE
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :oaCallbackId
  -- 若返回空，说明OA回传的ID在系统中不存在
  ```

#### 报错30：查询参数不能为空
- **触发条件**：查询报备列表或详情时，查询参数为空
- **逻辑分析**：查询方法中校验查询参数非空，因查询需基于有效参数过滤。需补全查询参数后重新查询
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.PROJECT_CODE, er.PROJECT_NAME
  FROM EPM_REPORT er
  WHERE er.REPORT_TYPE = 2
  -- 校验查询参数是否完整
  ```

#### 报错31：主键不能为空!
- **触发条件**：查询报备详情(detail方法)时，未传入报备主键ID(reportId为空)
- **逻辑分析**：detail方法中校验reportId非空，因详情查询需按主键关联EPM_REPORT及子表。该报错为阻断性报错
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID = :reportId
  -- 校验传入的主键ID是否存在
  ```

#### 报错32：操作数据不能为空
- **触发条件**：删除战略项目报备时，未选择待删除数据(reportIdList为空)
- **逻辑分析**：delete方法中校验reportIdList非空，因删除操作需指定目标数据。需先选择待删除数据后执行删除
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.REPORT_CODE, er.HZ_APPROVE_STATUS
  FROM EPM_REPORT er
  WHERE er.REPORT_ID IN (:reportIdList)
  -- 校验待删除数据是否存在
  ```

#### 报错33：{tradingCompanyName}交易公司不存在,请先维护!
- **触发条件**：保存战略项目报备时，交易公司在EPM_TRADING_COMPANY中不存在
- **逻辑分析**：saveData方法中按TRADING_COMPANY_ID查询交易公司主数据，若返回null则抛出阻断性报错。需先维护交易公司主数据
- **排查SQL**：
  ```sql
  SELECT er.REPORT_ID, er.TRADING_COMPANY_ID, er.TRADING_COMPANY_NAME,
         etc.TRADING_COMPANY_ID AS 交易公司档案ID
  FROM EPM_REPORT er
  LEFT JOIN EPM_TRADING_COMPANY etc ON er.TRADING_COMPANY_ID = etc.TRADING_COMPANY_ID
  WHERE er.REPORT_ID = :reportId
    AND etc.TRADING_COMPANY_ID IS NULL
  -- 查出交易公司档案不存在的报备
  ```

#### 报错34：本月总可用申诉次数({totalNum}次)小于等于已用申诉次数({usedNum}次)，无法进行申诉
- **触发条件**：申诉时，本月总可用申诉次数<=已用申诉次数
- **逻辑分析**：appeal方法中计算本月总可用申诉次数(基础次数+临时追加次数)和已用申诉次数，若可用<=已用则抛出中等优先级报错。需下月再申诉或申请追加临时次数
- **排查SQL**：
  ```sql
  SELECT erac.CUSTOMER_ID, erac.APPEAL_YEAR, erac.APPEAL_MONTH,
         erac.APPEAL_NUM, NVL(erac.APPEAL_NUM_TMP, 0) AS APPEAL_NUM_TMP,
         erac.APPEAL_NUM + NVL(erac.APPEAL_NUM_TMP, 0) AS 总可用次数,
         (SELECT COUNT(1) FROM EPM_REPORT_INTERCEPT_HEAD erih
          WHERE erih.CUSTOMER_ID = erac.CUSTOMER_ID
            AND erih.APPEAL_DATE IS NOT NULL
            AND EXTRACT(YEAR FROM erih.APPEAL_DATE) = erac.APPEAL_YEAR
            AND EXTRACT(MONTH FROM erih.APPEAL_DATE) = erac.APPEAL_MONTH) AS 已用次数
  FROM EPM_REPORT_APPEAL_CONFIG erac
  WHERE erac.CUSTOMER_ID = :customerId
    AND erac.APPEAL_YEAR = EXTRACT(YEAR FROM SYSDATE)
    AND erac.APPEAL_MONTH = EXTRACT(MONTH FROM SYSDATE)
  -- 查出可用次数<=已用次数的配置
  ```

#### 报错35：请先配置系统参数-经销商可报备数量:ReportCustomerApplyNum
- **触发条件**：提交战略项目报备时，未配置系统参数ReportCustomerApplyNum(经销商可报备数量)
- **逻辑分析**：提交校验中按CONFNAME='ReportCustomerApplyNum'查询SCPSYSCONF，若为空则抛出阻断性报错。需先维护SCPSYSCONF配置
- **排查SQL**：
  ```sql
  SELECT s.CONFNAME, s.CONFVALUE, s.ORGANIZATION_ID
  FROM SCPSYSCONF s
  WHERE s.CONFNAME = 'ReportCustomerApplyNum'
  -- 若返回空，则需补充配置
  ```

#### 报错36：当前事业部数据异常
- **触发条件**：保存战略项目报备时，当前用户所属事业部数据异常(为空或不存在)
- **逻辑分析**：saveData方法中获取当前用户所属事业部，若事业部数据为空或不存在则抛出阻断性报错。需检查用户事业部配置
- **排查SQL**：
  ```sql
  SELECT u.USER_ID, u.REAL_NAME, u.ORGANIZATION_ID,
         o.ORGANIZATION_ID AS 事业部ID, o.ORGANIZATION_NAME
  FROM IAM_USER u
  LEFT JOIN IAM_ORGANIZATION o ON u.ORGANIZATION_ID = o.ORGANIZATION_ID
  WHERE u.USER_ID = :currentUserId
  -- 检查当前用户事业部数据是否完整
  ```

## 常见问题FAQ

### Q1: 战略项目报备需要几次审批？

需要两次OA审批：战略项目报备一次和战略项目报备二次。一次报备通过后生成项目档案，二次报备通过后更新项目档案信息。

**排查SQL:**
```sql
SELECT REPORT_ID, REPORT_TIMES, HZ_APPROVE_STATUS, AUDIT_STAT
FROM EPM_REPORT
WHERE REPORT_ID = #{reportId};
```

### Q2: 查重不通过怎么办？

可通过appeal接口进行申诉，由管理员审核申诉请求。申诉前校验本月可用申诉次数。

**排查SQL:**
```sql
SELECT DUPLICATE_REPORTING FROM EPM_REPORT WHERE REPORT_ID = #{reportId};
SELECT COUNT(1) FROM EPM_REPORT_INTERCEPT_HEAD
WHERE CUSTOMER_ID = #{customerId}
  AND INTERCEPT_DATE BETWEEN #{startDate} AND #{endDate};
```

### Q3: 一次报备通过后如何发起二次报备？

一次报备通过后，在列表页对应记录上点击"二次报备"按钮发起二次报备流程。若管理类型为经销商操作或公司自营，必须先分配具体经销商。

**排查SQL:**
```sql
SELECT REPORT_ID, REPORT_TIMES, HZ_APPROVE_STATUS
FROM EPM_REPORT
WHERE PROJECT_CODE = #{projectCode}
ORDER BY REPORT_TIMES;
```

### Q4: 审核通过后项目有效期如何计算？

根据公司参数Proj_Effective_Cycle配置的有效周期天数，从审核通过日期起计算项目有效起始日期和有效结束日期。

**排查SQL:**
```sql
SELECT PARAM_CODE, PARAM_VALUE FROM SYS_PARAM
WHERE PARAM_CODE = 'Proj_Effective_Cycle'
  AND ORGANIZATION_ID = #{orgId};
```

### Q5: 申诉次数有限制吗？

是的，可申诉次数通过SCPSYSCONF配置（CONFNAME='ReportAppealNum'），也可通过EPM_REPORT_APPEAL_CONFIG表按经销商/年/月配置。超过限制则不允许申诉。

**排查SQL:**
```sql
SELECT CONFVALUE FROM SCPSYSCONF WHERE CONFNAME = 'ReportAppealNum';
SELECT * FROM EPM_REPORT_APPEAL_CONFIG
WHERE CUSTOMER_ID = #{customerId}
  AND APPEAL_YEAR = #{year}
  AND APPEAL_MONTH = #{month};
```

### Q6: 项目编码如何生成？

新建报备时自动生成项目编码，编码规则通过编码规则引擎生成，工程类规则编码为PROJECT_CODE。

### Q7: 战略报备支持批量导入吗？

不支持，战略项目报备仅支持单条新建/编辑。

### Q8: OA审批回调异常如何排查？

检查OA回调参数完整性及报备数据是否存在。搜索日志关键字"战略项目报备一次OA回调处理异常"或"战略项目报备二次OA回调处理异常"。

**排查SQL:**
```sql
SELECT REPORT_ID, REPORT_CODE, REPORT_TIMES, HZ_APPROVE_STATUS, HZ_INSTANCE_ID
FROM EPM_REPORT
WHERE REPORT_ID = #{reportId};
```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充代码梳理：后端Controller/Entity完整分析，界面模块6列表格、数据库表5列表格、报错一览表5列、上游依赖4列、下游影响bullet points、选择弹窗、保存校验、提交校验、状态机等 |
| 2026-08-29 | - | CodeArts | 对比网站内容补充：两次报备机制、界面模块、选择弹窗、保存校验、提交校验、状态机、9张数据库表详解、报错一览表、FAQ |
| 2025-10-29 | - | liuyk | 初始版本，战略项目报备功能 |
