# 工程管理-项目往来-工程服务费预提

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

```text
定时任务/手工触发 → 按年月从expense_withholding_view视图查询出库签收服务费汇总
  → 按交易公司+法人客户+年月+成本中心+费用科目维度汇总
  → 校验是否已存在非作废预提单 → 已存在保存态则更新金额 → 不存在则新建预提单
  → 生成共享单号(AE.SHARE_NO)和预提单号(AE.WITHHOLDING_NO)
  → 状态=保存(0)
  → 用户点击【执行】→ 推送共享中心(FSSC,单据类型GCYT)
    → 共享返回S → 状态→审批中(3)
    → 共享返回非S → 报错展示共享错误信息
  → 用户点击【作废】→ 推送负数金额冲销(共享单号追加_F后缀)
  → 用户点击【重新生成】→ 校验无非作废单 → 按原年月重新生成新预提单
```

### 全链路流程图

```text
开始 → 定时任务/手工生成预提数据 → ★新建服务费预提单(状态=保存)★
  → 用户点击【执行】→ 推送共享(FSSC)
    → ⚖共享返回S？
      ├── 是 → 状态→审批中(3) → 结束
      └── 否 → 报错 → 结束
  → 用户点击【作废】→ 推送负数冲销(单号+F) → 状态→审批中(3) → 结束
  → 用户点击【重新生成】→ 校验无非作废单 → 重新生成 → 结束
```

### 关键规则

- **按月汇总生成**：按交易公司+法人客户+年月+成本中心+费用科目维度汇总出库签收服务费，自动生成预提单
- **防重复生成**：同口径下已存在非作废预提单则跳过；存在保存态预提单则更新金额，不重复建单
- **作废冲销重生成**：作废时推送负数金额冲销预算，共享单号追加_F后缀，作废单可按原年月重新生成新预提单
- **工程/家装区分**：isHome=1为工程、isHome=2为家装，家装使用独立的编码规则(AE.SHARE_NO_HOME/AE.WITHHOLDING_NO_HOME)

---

## 重点逻辑

### 重点逻辑1：定时任务自动生成预提数据

- **业务意义**：按月自动汇总出库签收的服务费生成预提单，避免人工遗漏与跨期差异
- **具体逻辑描述**：
  - 定时任务WithholdingInProjectJob支持两种模式：指定单月(syncFlag=1,仅传startDate)、指定时间段(syncFlag=2,传startDate+endDate)
  - 从expense_withholding_view视图查询预提数据，按交易公司+法人客户+年月+成本中心+费用科目维度汇总
  - 查询事业部配置(DIVISION_BASE_SET)，无配置则跳过
  - 同一交易公司+法人+年月+成本中心维度下，若已存在非作废状态的预提单则跳过；若存在保存状态的预提单则更新金额
  - 新生成的预提单初始状态为保存(SAVE=0)，自动生成共享单号和预提单号
  - 时间参数格式校验：必须为yyyy-MM格式，否则报错

### 重点逻辑2：执行推送共享

- **业务意义**：将预提数据推送至财务共享系统(FSSC)入账，占用共享预算
- **具体逻辑描述**：
  - 推送前校验：DTO非空、共享单号非空、年月非空、交易公司非空
  - 按共享单号查询所有预提头表记录，汇总不含税金额作为推送总金额
  - 构建表头DTO：attribute1=1(正向执行)、审批年月=年-月、单据号=共享单号、金额=汇总不含税金额、审批日期=年月最后一天
  - 构建明细行：跳过金额为0的记录，每行含金额、成本中心、费用科目、法人客户、借贷类型(DR借方)
  - 获取申请人职位：从LOV值集AE.SIE.POSITION_LDAP_CODE查询expense_withholding_head对应的职位编码
  - 共享接口返回状态为S则成功，更新状态为审批中(AUDITING=3)；否则报错展示共享返回的错误信息

### 重点逻辑3：作废冲销推送

- **业务意义**：作废预提单时向共享系统推送负数金额冲销已入账预算
- **具体逻辑描述**：
  - 作废与执行共用同一推送逻辑(operateExpenseWithholding)，通过flag参数区分(flag=1作废,flag=0执行)
  - 作废时attribute1=2(冲销)，共享单号追加_F后缀以区分正向单据
  - 作废时金额取负数(金额前加负号)
  - 推送成功后同样更新状态为审批中(AUDITING=3)

### 重点逻辑4：重新生成

- **业务意义**：对作废的预提单按原年月重新生成新预提单
- **具体逻辑描述**：
  - 重新生成前校验该年月+交易公司下是否已存在非作废状态的预提单，若存在则报错提示无需重复生成
  - 重新生成时syncFlag=1，按指定年月重新从视图查询数据并生成新预提单
  - 生成次数GENERATED_TIMES记录重新生成次数

### 重点逻辑5：工程/家装双编码规则

- **业务意义**：工程和家装合同使用不同的编码规则，便于区分管理
- **具体逻辑描述**：
  - isHome=2(家装)时，共享单号使用AE.SHARE_NO_HOME编码规则，预提单号使用AE.WITHHOLDING_NO_HOME编码规则
  - isHome=1(工程)时，共享单号使用AE.SHARE_NO编码规则，预提单号使用AE.WITHHOLDING_NO编码规则
  - 编码变量：divisionCode=交易公司编码(共享单号)或事业部编码(预提单号)

---

## 详细逻辑

### 界面模块1：查询区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 预提编号 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_NO | 文本输入框 | 预提单编号模糊查询 | 常显 | 手动输入 |
| 预提年度 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_YEAR | 下拉选择框 | 预提年度查询 | 常显 | 手动选择 |
| 预提月份 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_MONTH | 下拉选择框 | 预提月份查询 | 常显 | 手动选择 |
| 交易公司 | EXPENSE_WITHHOLDING_HEAD.TRADING_COMPANY_NAME | 文本输入框 | 交易公司模糊查询 | 常显 | 手动输入 |
| 法人客户 | EXPENSE_WITHHOLDING_HEAD.BILLING_UNIT_NAME | 文本输入框 | 法人客户模糊查询 | 常显 | 手动输入 |
| 单据状态 | EXPENSE_WITHHOLDING_HEAD.BILL_STATUS | 下拉选择框 | 单据状态查询 | 常显 | 手动选择，0-保存/3-审批中/7-作废 |

### 界面模块2：预提列表表格

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 共享单号 | EXPENSE_WITHHOLDING_HEAD.SHARE_NO | 文本框 | 推送共享系统的单据编号 | 常显 | 新增时按编码规则AE.SHARE_NO/AE.SHARE_NO_HOME自动生成，不可编辑 |
| 预提单号 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_NO | 文本框 | 系统内部预提单编号 | 常显 | 新增时按编码规则AE.WITHHOLDING_NO/AE.WITHHOLDING_NO_HOME自动生成，不可编辑 |
| 交易公司编码 | EXPENSE_WITHHOLDING_HEAD.TRADING_COMPANY_CODE | 文本框 | 交易公司编码 | 常显 | 来源于出库签收数据，不可编辑 |
| 交易公司名称 | EXPENSE_WITHHOLDING_HEAD.TRADING_COMPANY_NAME | 文本框 | 交易公司名称 | 常显 | 来源于出库签收数据，不可编辑 |
| 法人客户编码 | EXPENSE_WITHHOLDING_HEAD.BILLING_UNIT_CODE | 文本框 | 法人客户编码 | 常显 | 来源于出库签收数据，不可编辑 |
| 法人客户名称 | EXPENSE_WITHHOLDING_HEAD.BILLING_UNIT_NAME | 文本框 | 法人客户名称 | 常显 | 来源于出库签收数据，不可编辑 |
| 预提年度 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_YEAR | 文本框 | 预提所属年度 | 常显 | 来源于出库签收数据的年月，不可编辑 |
| 预提月份 | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_MONTH | 文本框 | 预提所属月份 | 常显 | 来源于出库签收数据的年月，不可编辑 |
| 预提总额(含税) | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_RAX_AMOUNT | 数值框 | 预提含税总额 | 常显 | 来源于出库签收汇总，不可编辑 |
| 预提总额(不含税) | EXPENSE_WITHHOLDING_HEAD.WITHHOLDING_NORAX_AMOUNT | 数值框 | 预提不含税总额 | 常显 | 来源于出库签收汇总，不可编辑，推送共享时按此金额汇总 |
| 成本中心编码 | EXPENSE_WITHHOLDING_HEAD.COST_CODE | 文本框 | 成本中心编码 | 常显 | 来源于出库签收数据，不可编辑 |
| 成本中心名称 | EXPENSE_WITHHOLDING_HEAD.COST_NAME | 文本框 | 成本中心名称 | 常显 | 来源于出库签收数据，不可编辑 |
| 费用科目编码 | EXPENSE_WITHHOLDING_HEAD.FEECODE | 文本框 | 费用科目编码 | 常显 | 来源于出库签收数据，不可编辑 |
| 单据状态 | EXPENSE_WITHHOLDING_HEAD.BILL_STATUS | 下拉选择框 | 预提单状态 | 常显 | 系统自动维护，0-保存/3-审批中/7-作废 |
| 是否家装 | EXPENSE_WITHHOLDING_HEAD.IS_HOME | 文本框 | 是否家装合同 | 常显 | 来源于出库签收数据，1-工程/2-家装 |
| 生成次数 | EXPENSE_WITHHOLDING_HEAD.GENERATED_TIMES | 数值框 | 重新生成次数 | 常显 | 默认0，每次重新生成+1 |
| 备注 | EXPENSE_WITHHOLDING_HEAD.REMARK | 文本框 | 备注 | 常显 | 默认空，不可编辑 |

### 后端接口

| 接口名称 | Controller | Service | 路径 | 说明 |
|---------|-----------|---------|------|------|
| 重新生成 | ExpenseWithholdingHeadController | ExpenseWithholdingHeadServiceImpl.doRegenerate | POST /v1/{orgId}/expense-withholding-heads/regenerate | 对作废的预提单按原年月重新生成 |
| 作废 | ExpenseWithholdingHeadController | ExpenseWithholdingHeadServiceImpl.doInvalid | POST /v1/{orgId}/expense-withholding-heads/invalid | 推送负数金额冲销共享预算 |
| 执行 | ExpenseWithholdingHeadController | ExpenseWithholdingHeadServiceImpl.doExecute | POST /v1/{orgId}/expense-withholding-heads/execute | 推送预提数据到共享系统 |
| 定时任务生成 | WithholdingInProjectJob | ExpenseWithholdingHeadServiceImpl.generateBill | Job: withholdingInProjectJob | 定时自动生成预提数据 |
| 共享回调更新状态 | FsscApiController | FsscApiServiceImpl | - | 共享系统回调更新预提单状态，单据类型GCYT |

### 选择弹窗

本页面无选择弹窗。

### 导入

本页面无导入功能。预提数据由定时任务从expense_withholding_view视图自动汇总生成，非手工导入。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 执行 | 推送预提数据到共享系统 | 详情页 | 单据状态=保存(0) | 调用POST /execute，推送成功后状态→审批中(3) |
| 作废 | 推送作废数据(负数金额)到共享系统 | 详情页 | 单据状态=保存(0)或审批中(3) | 调用POST /invalid，共享单号追加_F后缀，推送成功后状态→审批中(3) |
| 重新生成 | 对作废的预提单按原年月重新生成 | 详情页 | 单据状态=作废(7) | 调用POST /regenerate，校验无非作废单后生成新预提单 |
| 详情 | 查看预提完整信息 | 列表页 | 选中某行 | 跳转详情页查看预提完整信息 |

#### 按钮1：执行（详情页）

- **触发条件**：单据状态=保存(0)
- **处理逻辑**：
  1. 校验DTO非空、共享单号非空、年月非空、交易公司非空
  2. 按共享单号查询所有预提头表记录
  3. 汇总不含税金额作为推送总金额
  4. 构建表头DTO(attribute1=1正向)和明细行列表(跳过金额为0的记录)
  5. 调用arrowFsscSdk.inLimitBudPush推送共享
  6. 共享返回S则更新状态为审批中(3)，否则报错

#### 按钮2：作废（详情页）

- **触发条件**：单据状态=保存(0)或审批中(3)
- **处理逻辑**：
  1. 校验DTO非空、共享单号非空、年月非空、交易公司非空
  2. 按共享单号查询所有预提头表记录
  3. 汇总不含税金额，金额取负数
  4. 构建表头DTO(attribute1=2冲销，共享单号追加_F后缀)和明细行列表(金额取负)
  5. 调用arrowFsscSdk.inLimitBudPush推送共享
  6. 共享返回S则更新状态为审批中(3)，否则报错

#### 按钮3：重新生成（详情页）

- **触发条件**：单据状态=作废(7)
- **处理逻辑**：
  1. 按年月+交易公司编码查询EXPENSE_WITHHOLDING_HEAD表中bill_status!=7的记录
  2. 若存在则报错提示"已存在重新生成【xxx】单据，无需重复生成"
  3. 构造ViewDTO(tradingCompanyCode, startDate=年月, syncFlag=1)
  4. 调用generateBill从视图重新查询并生成新预提单

### 保存校验

本页面无手工保存校验。预提单由定时任务自动生成，生成时校验防重复(同口径已存在非作废单则跳过)。

### 提交校验

#### 校验1：重新生成时校验是否已存在非作废状态的预提单 —— 防止重复生成

- **详细逻辑**：按年月+交易公司编码查询EXPENSE_WITHHOLDING_HEAD表中bill_status!=7(非作废)的记录，若存在则报错
- **系统体现**：doRegenerate方法中调用selectRegenerate查询，非空则抛CommonException
- **排查SQL**：
```sql
SELECT SHARE_NO, WITHHOLDING_NO, BILL_STATUS, WITHHOLDING_YEAR, WITHHOLDING_MONTH, TRADING_COMPANY_CODE
FROM EXPENSE_WITHHOLDING_HEAD
WHERE TO_CHAR(CHECK_TIME, 'yyyy-MM') = '{年月}'
  AND TRADING_COMPANY_CODE = '{交易公司编码}'
  AND NVL(BILL_STATUS, 0) != 7
```

#### 校验2：执行/作废时校验必填参数 —— 确保推送数据完整

- **详细逻辑**：校验DTO非空、共享单号(SHARE_NO)非空、年月(CHECK_TIME_STR)非空、交易公司编码(TRADING_COMPANY_CODE)非空
- **系统体现**：operateExpenseWithholding方法中依次校验，为空则抛CommonException
- **排查SQL**：
```sql
SELECT SHARE_NO, WITHHOLDING_YEAR, WITHHOLDING_MONTH, TRADING_COMPANY_CODE, CHECK_TIME
FROM EXPENSE_WITHHOLDING_HEAD
WHERE SHARE_NO = '{共享单号}'
```

#### 校验3：推送前校验预提单数据存在 —— 确保共享单号对应数据未丢失

- **详细逻辑**：按共享单号+isHome查询EXPENSE_WITHHOLDING_HEAD表，若为空则报错"未查询到预提单数据"
- **系统体现**：doserviceWithHolding方法中查询headList，空则抛CommonException
- **排查SQL**：
```sql
SELECT * FROM EXPENSE_WITHHOLDING_HEAD
WHERE SHARE_NO = '{共享单号}'
  AND IS_HOME = {isHome}
```

### 状态机

```text
[保存SAVE(0)] ──执行──> [审批中AUDITING(3)]
[保存SAVE(0)] ──作废──> [审批中AUDITING(3)] (作废冲销，推送负数)
[审批中AUDITING(3)] ──作废──> [审批中AUDITING(3)] (作废冲销，推送负数)
[作废INVALID(7)] ──重新生成──> [保存SAVE(0)] (新单据)
```

| 状态值 | 状态名称 | 可执行操作 |
|--------|---------|-----------|
| 0 | 保存 | 执行、作废 |
| 3 | 审批中 | 作废 |
| 7 | 作废 | 重新生成 |

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| expense_withholding_view视图 | 数据视图 | 预提数据来源，汇总出库签收服务费 | 视图中存在对应年月的出库签收数据 |
| DIVISION_BASE_SET(事业部配置) | 数据表 | 提供事业部编码用于生成预提单号 | 存在对应organizationId的事业部配置 |
| 出库签收单 | 上游业务 | 签收审批通过后服务费数据进入视图 | 出库签收单已审批通过 |
| AE.SIE.POSITION_LDAP_CODE值集 | LOV值集 | 获取申请人职位编码 | 值集中存在expense_withholding_head对应配置 |
| FSSC共享系统 | 外部接口 | 推送预提数据入账 | 共享系统连通且返回状态S |

### 下游影响

- **FSSC共享系统**：预提数据推送至共享系统入账，占用共享预算，单据类型GCYT
- **共享预算**：预提金额占用对应成本中心+费用科目的共享预算
- **财务核算**：共享系统根据预提数据生成财务凭证，费用与收入同期匹配

---

## 数据库表详解

### EXPENSE_WITHHOLDING_HEAD（服务费预提头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| WITHHOLDING_HEAD_ID | Long | 预提主键ID | - | 自增主键，@Id @GeneratedValue |
| DIVISION_ID | Long | 事业部ID | - | 来源于出库签收数据，NotNull |
| ORGANIZATION_ID | Long | 组织ID | - | 来源于出库签收数据 |
| WITHHOLDING_NO | String | 预提单号 | 预提单号 | 按编码规则AE.WITHHOLDING_NO(工程)/AE.WITHHOLDING_NO_HOME(家装)生成 |
| SHARE_NO | String | 共享单号 | 共享单号 | 按编码规则AE.SHARE_NO(工程)/AE.SHARE_NO_HOME(家装)生成，作废时追加_F后缀 |
| TRADING_COMPANY_ID | Long | 交易公司ID | - | 来源于出库签收数据 |
| TRADING_COMPANY_CODE | String | 交易公司编码 | 交易公司编码 | 来源于出库签收数据 |
| TRADING_COMPANY_NAME | String | 交易公司名称 | 交易公司名称 | 来源于出库签收数据 |
| BILLING_UNIT_CODE | String | 法人客户编码 | 法人客户编码 | 来源于出库签收数据 |
| BILLING_UNIT_NAME | String | 法人客户名称 | 法人客户名称 | 来源于出库签收数据 |
| WITHHOLDING_YEAR | String | 预提年度 | 预提年度 | 来源于签收数据的年月 |
| WITHHOLDING_MONTH | String | 预提月份 | 预提月份 | 来源于签收数据的年月 |
| CHECK_TIME | LocalDate | 审批通过时间 | - | 签收审批通过时间，取年月用于推送共享 |
| WITHHOLDING_RAX_AMOUNT | BigDecimal | 预提含税总额 | 预提总额(含税) | 服务费×预提比例(含税) |
| WITHHOLDING_NORAX_AMOUNT | BigDecimal | 预提不含税总额 | 预提总额(不含税) | 服务费×预提比例(不含税)，推送共享时按此金额汇总 |
| COST_CODE | String | 成本中心编码 | 成本中心编码 | 来源于出库签收数据 |
| COST_NAME | String | 成本中心名称 | 成本中心名称 | 来源于出库签收数据 |
| FEECODE | String | 费用科目编码 | 费用科目编码 | 来源于出库签收数据 |
| BILL_STATUS | Long | 单据状态 | 单据状态 | 0-保存/3-审批中/7-作废 |
| GENERATED_TIMES | Long | 重新生成次数 | 生成次数 | 默认0，每次重新生成+1 |
| IS_HOME | Long | 是否家装合同 | 是否家装 | 1-工程/2-家装，影响编码规则选择 |
| REMARK | String | 备注 | 备注 | 默认空 |
| ATTRIBUTE1 | String | 弹性字段 | - | 备用扩展字段 |
| ATTRIBUTE2 | String | 弹性字段 | - | 备用扩展字段 |
| ATTRIBUTE3 | String | 弹性字段 | - | 备用扩展字段 |
| ATTRIBUTE4 | String | 弹性字段 | - | 备用扩展字段 |
| ATTRIBUTE5 | String | 弹性字段 | - | 备用扩展字段 |

**查询SQL**：
```sql
SELECT * FROM EXPENSE_WITHHOLDING_HEAD
WHERE ORGANIZATION_ID = #{organizationId}
  AND NVL(WITHHOLDING_NO, '') LIKE '%' || #{withholdingNo} || '%'
  AND NVL(WITHHOLDING_YEAR, '') = #{withholdingYear}
  AND NVL(WITHHOLDING_MONTH, '') = #{withholdingMonth}
ORDER BY WITHHOLDING_HEAD_ID DESC;
```

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 已存在重新生成【xxx】单据，无需重复生成！ | 重新生成按钮 | 该年月+交易公司下已存在非作废状态的预提单。先作废现有预提单后再重新生成 | 高 | doRegenerate中selectRegenerate查询非空时抛出 |
| 参数错误！ | 执行/作废按钮 | 传入的DTO对象为null。检查前端参数传递 | 高 | operateExpenseWithholding中DTO为null时抛出 |
| 单据编码不能为空 | 执行/作废按钮 | 共享单号(SHARE_NO)为空。检查预提单数据完整性 | 高 | operateExpenseWithholding中shareNo为空时抛出 |
| 年月不能为空 | 执行/作废按钮 | 审批通过时间(checkTimeStr)为空。检查年月数据 | 中 | operateExpenseWithholding中checkTimeStr为null时抛出 |
| 交易公司不能为空 | 执行/作废按钮 | 交易公司编码(TRADING_COMPANY_CODE)为空。检查交易公司数据 | 中 | operateExpenseWithholding中tradingCompanyCode为空时抛出 |
| 未查询到预提单数据，单据号：xxx | 执行/作废按钮 | 按共享单号+isHome查询不到预提单数据。确认数据未被删除 | 高 | doserviceWithHolding中headList为空时抛出 |
| 共享接口返回null,执行共享接口失败！ | 执行/作废按钮 | 共享接口调用失败返回null。检查共享系统连通性 | 高 | arrowFsscSdk.inLimitBudPush返回null时抛出 |
| 共享返回的错误信息 | 执行/作废按钮 | 共享系统处理失败。根据具体错误信息排查共享系统 | 高 | 共享返回processStatus非S时抛出processMsgData |
| 请传入正确的同步时间段 | 定时任务 | startDate/endDate均未传或格式错误。传入正确的yyyy-MM格式时间参数 | 中 | WithholdingInProjectJob中startDate和endDate均空时抛出 |
| 【xxx】该时间格式错误，请输入正确的时间格式：yyyy-MM | 定时任务 | 时间参数格式不符合yyyy-MM。修正时间格式 | 中 | checkDateFormat中SimpleDateFormat.parse失败时抛出 |
| 找不到符合的预提/冲销单号【xxx】的信息 | 共享回调 | 共享回调时按共享单号查询不到预提单。确认数据未被删除 | 高 | FsscApiServiceImpl中WITHHOLDING_PROJECT分支查询为空时抛出 |
| 找不到符合的更新状态码【xxx】 | 共享回调 | 共享回调传入的状态码不在系统定义范围内。检查共享系统状态码配置 | 高 | FsscApiServiceImpl中status解析失败时抛出 |
| 请确认该单据号【xxx】是预提/冲销头单号！ | 共享回调 | 共享回调按单据号查询不到头表记录。确认单据号类型正确 | 高 | FsscApiServiceImpl中headerCode查询为空时抛出 |
| 未知的单据类型 | 共享回调 | 共享回调传入的单据类型不在枚举范围内。检查共享系统单据类型配置 | 高 | FsscApiServiceImpl中billType未匹配枚举时抛出 |

#### 报错1：已存在重新生成【xxx】单据，无需重复生成！
- **触发条件**：点击重新生成按钮时，该年月+交易公司下已存在非作废状态的预提单
- **逻辑分析**：doRegenerate方法中调用selectRegenerate按年月和交易公司编码查询EXPENSE_WITHHOLDING_HEAD，若存在非作废(STATUS<>'VOID')的预提单则抛出阻断性报错。需先作废现有预提单后再重新生成
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.CHECK_TIME_STR,
         ewh.TRADING_COMPANY_CODE, ewh.STATUS, ewh.HZ_APPROVE_STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.CHECK_TIME_STR = :checkTimeStr
    AND ewh.TRADING_COMPANY_CODE = :tradingCompanyCode
    AND ewh.STATUS <> 'VOID'
  -- 查出已存在的非作废预提单
  ```

#### 报错2：参数错误！
- **触发条件**：执行/作废按钮时，传入的DTO对象为null
- **逻辑分析**：operateExpenseWithholding方法中校验DTO非null，若为null则抛出阻断性报错。需检查前端参数传递
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
  -- 校验预提单数据是否存在
  ```

#### 报错3：单据编码不能为空
- **触发条件**：执行/作废按钮时，共享单号(SHARE_NO)为空
- **逻辑分析**：operateExpenseWithholding方法中校验shareNo非空，因共享单号为推送共享系统的关键标识。需检查预提单数据完整性
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO IS NULL OR TRIM(ewh.SHARE_NO) = ''
  -- 查出共享单号为空的预提单
  ```

#### 报错4：年月不能为空
- **触发条件**：执行/作废按钮时，审批通过时间(checkTimeStr)为空
- **逻辑分析**：operateExpenseWithholding方法中校验checkTimeStr非空，因年月用于定位预提数据。需检查年月数据
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.CHECK_TIME_STR, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.CHECK_TIME_STR IS NULL OR TRIM(ewh.CHECK_TIME_STR) = ''
  -- 查出年月为空的预提单
  ```

#### 报错5：交易公司不能为空
- **触发条件**：执行/作废按钮时，交易公司编码(TRADING_COMPANY_CODE)为空
- **逻辑分析**：operateExpenseWithholding方法中校验tradingCompanyCode非空，因交易公司为预提单的关键维度。需检查交易公司数据
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.TRADING_COMPANY_CODE, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.TRADING_COMPANY_CODE IS NULL OR TRIM(ewh.TRADING_COMPANY_CODE) = ''
  -- 查出交易公司为空的预提单
  ```

#### 报错6：未查询到预提单数据，单据号：xxx
- **触发条件**：执行/作废按钮时，按共享单号+isHome查询不到预提单数据
- **逻辑分析**：doserviceWithHolding方法中按SHARE_NO和IS_HOME查询EXPENSE_WITHHOLDING_HEAD，若headList为空则抛出阻断性报错。需确认数据未被删除
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.IS_HOME, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
    AND ewh.IS_HOME = :isHome
  -- 若返回空，说明预提单数据不存在
  ```

#### 报错7：共享接口返回null,执行共享接口失败！
- **触发条件**：执行/作废按钮时，共享接口(arrowFsscSdk.inLimitBudPush)返回null
- **逻辑分析**：调用共享接口推送预提数据，若返回null则抛出阻断性报错。可能原因：共享系统不可用、网络异常。需检查共享系统连通性
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.STATUS, ewh.HZ_APPROVE_STATUS,
         ewh.TOTAL_AMOUNT, ewh.CHECK_TIME_STR
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
  -- 检查预提单数据完整性，作为共享推送参数
  ```

#### 报错8：共享返回的错误信息
- **触发条件**：执行/作废按钮时，共享系统返回processStatus非S(失败)
- **逻辑分析**：共享接口返回processStatus非S时抛出processMsgData错误信息。需根据具体错误信息排查共享系统
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.STATUS, ewh.PROCESS_STATUS, ewh.PROCESS_MSG
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
    AND ewh.PROCESS_STATUS <> 'S'
  -- 查出共享处理失败的预提单及错误信息
  ```

#### 报错9：请传入正确的同步时间段
- **触发条件**：定时任务执行时，startDate/endDate均未传或格式错误
- **逻辑分析**：WithholdingInProjectJob中校验startDate和endDate不同时为空，若均为空则抛出中等优先级报错。需传入正确的yyyy-MM格式时间参数
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 视图数据量
  FROM EXPENSE_WITHHOLDING_VIEW ewv
  WHERE ewv.CHECK_TIME BETWEEN :startDate AND :endDate
  -- 校验时间范围内是否有视图数据
  ```

#### 报错10：【xxx】该时间格式错误，请输入正确的时间格式：yyyy-MM
- **触发条件**：定时任务执行时，时间参数格式不符合yyyy-MM
- **逻辑分析**：checkDateFormat方法中用SimpleDateFormat("yyyy-MM").parse()解析时间参数，若解析失败则抛出中等优先级报错。需修正时间格式
- **排查SQL**：
  ```sql
  SELECT ewv.CHECK_TIME, ewv.TRADING_COMPANY_CODE, COUNT(*) AS 数据量
  FROM EXPENSE_WITHHOLDING_VIEW ewv
  WHERE ewv.CHECK_TIME = :checkTime
  GROUP BY ewv.CHECK_TIME, ewv.TRADING_COMPANY_CODE
  -- 校验时间格式对应的视图数据
  ```

#### 报错11：找不到符合的预提/冲销单号【xxx】的信息
- **触发条件**：共享回调时，按共享单号查询不到预提单
- **逻辑分析**：FsscApiServiceImpl中WITHHOLDING_PROJECT分支按共享单号查询EXPENSE_WITHHOLDING_HEAD，若为空则抛出阻断性报错。需确认数据未被删除
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.STATUS, ewh.VALID
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
  -- 若返回空，说明共享单号对应的预提单不存在
  ```

#### 报错12：找不到符合的更新状态码【xxx】
- **触发条件**：共享系统回调更新预提单状态时，传入的状态码不在系统定义范围内
- **逻辑分析**：FsscApiServiceImpl中按共享回调传入的status匹配系统状态枚举，若未匹配到则抛出阻断性报错。需检查共享系统状态码配置与系统枚举一致
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.STATUS, ewh.PROCESS_STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
  -- 检查预提单当前状态与共享回调状态码
  ```

#### 报错13：请确认该单据号【xxx】是预提/冲销头单号！
- **触发条件**：共享系统回调时，按单据号查询不到预提/冲销头表记录
- **逻辑分析**：FsscApiServiceImpl中按headerCode查询头表，若为空则抛出阻断性报错。需确认单据号类型正确且数据未被删除
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.WITHHOLDING_NO, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :headerCode
     OR ewh.WITHHOLDING_NO = :headerCode
  -- 校验单据号是否为预提/冲销头单号
  ```

#### 报错14：未知的单据类型
- **触发条件**：共享系统回调时，传入的单据类型不在系统枚举(SharedBillTypeEnum)范围内
- **逻辑分析**：FsscApiServiceImpl中按billType匹配WITHHOLDING_IN/WITHHOLDING_OUT/WITHHOLDING_PROJECT等枚举，若未匹配则抛出阻断性报错。需检查共享系统单据类型配置
- **排查SQL**：
  ```sql
  SELECT ewh.WITHHOLDING_HEAD_ID, ewh.SHARE_NO, ewh.IS_HOME, ewh.STATUS
  FROM EXPENSE_WITHHOLDING_HEAD ewh
  WHERE ewh.SHARE_NO = :shareNo
  -- 检查预提单类型与共享单据类型枚举
  ```

### 常见问题

**Q1: 服务费预提按什么周期生成？**
A: 按年度和月份生成。定时任务WithholdingInProjectJob支持两种模式：指定单月(syncFlag=1,仅传startDate)和指定时间段(syncFlag=2,传startDate+endDate)。系统从expense_withholding_view视图按交易公司+法人客户+年月+成本中心+费用科目维度汇总出库签收服务费自动生成。

**Q2: 预提数据推送至哪里？**
A: 推送至FSSC共享中心，单据类型为GCYT（工程服务费预提），共享单据类型枚举SharedBillTypeEnum.WITHHOLDING_PROJECT。推送数据包含表头(总金额、申请人、审批日期取年月最后一天)和明细行(每条预提记录的金额、成本中心、费用科目、法人客户、借贷类型DR)。

**Q3: 如何防止重复生成预提？**
A: 同一交易公司+法人+年月+成本中心维度下，若已存在非作废状态的预提单则跳过不生成；若存在保存状态的预提单则更新金额不重复建单。重新生成时会校验该年月+交易公司下是否存在非作废单，存在则报错。

**Q4: 定时任务执行后未生成预提数据**
- **原因**：expense_withholding_view视图中无对应年月的出库签收数据，或DIVISION_BASE_SET中无对应事业部配置
- **处理**：
  1. 检查视图数据：`SELECT * FROM expense_withholding_view WHERE check_time = '{年月}'`
  2. 检查事业部配置：`SELECT * FROM division_base_set WHERE organization_id IN (...)`

**Q5: 执行推送共享后状态未更新**
- **原因**：共享接口返回成功但updateBillStatus执行异常，或共享单号对应多条记录
- **处理**：`SELECT * FROM EXPENSE_WITHHOLDING_HEAD WHERE SHARE_NO = '{共享单号}'`检查记录状态

**Q6: 工程和家装预提单有什么区别？**
A: 编码规则不同。工程(isHome=1)使用AE.SHARE_NO/AE.WITHHOLDING_NO，家装(isHome=2)使用AE.SHARE_NO_HOME/AE.WITHHOLDING_NO_HOME。推送共享时共享单号变量为交易公司编码，预提单号变量为事业部编码。

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-10-29 | - | hfy | 初始版本，工程服务费预提功能开发 |
| 2026-07-31 | - | - | 初始生成知识库文档 |
| 2026-08-30 | - | CodeArts | 按skill规范重写：移除定义章节、界面模块表格改为6列、数据库表改为5列、报错表改为5列、新增上游依赖/下游影响/选择弹窗/导入/提交校验章节、补充工程家装双编码规则 |
