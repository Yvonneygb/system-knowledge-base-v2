# 门店管理-装修报表-门店装修申请与进度更新报表

## 业务流程

### 业务流程图

```text
选择查询条件      查询报表数据        展示结果          导出
(年度/经销商/  →   调用报表查询  →    分页展示装修     →   导出Excel
 门店/状态)        接口              申请与进度
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 门店装修申请 | 数据依赖 | 提供装修申请单、审批状态、进度信息 | 装修申请已存在 |
| 完工验收 | 数据依赖 | 提供6验收进度数据 | 验收单已存在 |

### 下游影响

- 影响1：无
  - 报表仅查询展示，不产生数据变更

---

## 重点逻辑

### 重点逻辑1：装修申请进度汇总 {数据汇总}

- **业务意义**：展示门店装修申请的全流程进度

- **具体逻辑描述**

  - 第1点：汇总装修申请从申请到完工验收的全流程状态

  - 第2点：包含申请金额、批准金额、验收金额、报销金额等

---

## 详细逻辑

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

### 界面模块1：报表查询页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 年度 | - | 下拉选择框 | 查询年度 | 常显 | 默认当前年度 |
| 经销商名称 | - | 文本框 | 查询经销商 | 常显 | 可输入筛选 |
| 门店名称 | - | 文本框 | 查询门店 | 常显 | 可输入筛选 |
| 申请状态 | - | 下拉选择框 | 查询申请状态 | 常显 | - |

### 界面模块2：报表结果列表

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 申请单号 | - | 文本框 | 装修申请单号 | 常显 | 查询结果 |
| 经销商名称 | - | 文本框 | 经销商名称 | 常显 | 查询结果 |
| 门店名称 | - | 文本框 | 门店名称 | 常显 | 查询结果 |
| 申请金额 | - | 文本框 | 申请金额 | 常显 | 查询结果 |
| 批准金额 | - | 文本框 | 批准金额 | 常显 | 查询结果 |
| 验收金额 | - | 文本框 | 验收金额 | 常显 | 查询结果 |
| 报销金额 | - | 文本框 | 报销金额 | 常显 | 查询结果 |
| 申请状态 | - | 文本框 | 申请状态 | 常显 | 查询结果 |
| 进度状态 | - | 文本框 | 进度状态 | 常显 | 查询结果 |

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 查询报表数据 | 查询页 | 常显 | 调用search接口 |
| 导出 | 导出Excel | 查询页 | 常显 | 调用导出接口 |

#### 按钮1：查询（查询页）

- **触发条件**：常显
- **执行逻辑**：按查询条件调用报表查询接口，分页返回
- **接口调用**：POST /v1/{organizationId}/terminalReport/mkt-terminal-apply-list/search
- **排查SQL**：

```sql
SELECT * FROM fin_fee_apply_header WHERE apply_type = 1 ORDER BY create_time DESC;
```

#### 按钮2：导出（查询页）

- **触发条件**：常显
- **执行逻辑**：按当前查询条件导出Excel
8- **接口调用**：GET /v1/{organizationId}/terminalReport/mkt-terminal-export
- **排查SQL**：无

---

## 数据库表详解

### 相关表：FIN_FEE_APPLY_HEADER（市场费用申请单-查询来源）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| FEE_APPLY_NO | VARCHAR2 | 申请单号 | 申请单号 | 查询来源 |
| CUST_NAME | VARCHAR2 | 经销商名称 | 经销商名称 | 查询来源 |
| TERMINAL_NAME | VARCHAR2 | 门店名称 | 门店名称 | 查询来源 |
| TOTAL_APPLY_AMT_BX | DECIMAL | 申请金额 | 申请金额 | 查询来源 |
| TOTAL_ALLOW_AMT_BX | DECIMAL | 批准金额 | 批准金额 | 查询来源 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 年度不能为空 | 查询 | 未选择查询年度，默认当前年度失败 | 阻断性报错 | [查看](#报错1年度不能为空) |
| 经销商名称不能为空 | 查询 | 经销商名称查询条件传参为空 | 提示性报错 | [查看](#报错2经销商名称不能为空) |
| 门店名称不能为空 | 查询 | 门店名称查询条件传参为空 | 提示性报错 | [查看](#报错3门店名称不能为空) |
| 报表查询失败：申请单状态异常 | 查询 | 装修申请单apply_type非1，数据来源错误 | 阻断性报错 | [查看](#报错4报表查询失败申请单状态异常) |
| 导出失败：数据量超过限制 | 导出 | 导出数据量超过系统上限 | 阻断性报错 | [查看](#报错5导出失败数据量超过限制) |
| 网络请求失败/接口调用异常 | 查询/导出 | 后端接口调用失败，检查网络连接或后端服务状态 | 阻断性报错 | [查看](#报错6网络请求失败接口调用异常) |
| 权限不足/未登录 | 页面加载/查询 | 当前用户无组织级权限或登录态失效，重新登录或联系管理员分配权限 | 阻断性报错 | [查看](#报错7权限不足未登录) |
| 导出失败：网络异常 | 导出 | 导出接口调用过程中网络中断或后端响应超时，重试导出或缩小查询范围 | 阻断性报错 | [查看](#报错8导出失败网络异常) |

#### 报错1：年度不能为空
- **触发条件**：点击"查询"按钮，调用search接口时，传入的cyear（年度）参数为null或空字符串
- **逻辑分析**：报表查询依赖年度定位FIN_FEE_APPLY_HEADER记录。若前端未选择年度、年度下拉框默认值未正确赋值（如系统时间获取异常），或传参丢失，后端校验cyear为空即抛异常，无法执行查询。装修申请单按年度归档，缺失年度将导致全表扫描或空结果。
- **排查SQL**：
  ```sql
  SELECT fee_apply_id        AS 申请单ID,
         fee_apply_no        AS 申请单号,
         cyear               AS 预算年度,
         apply_type          AS 申请类型,
         create_time         AS 创建时间
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1
  AND    cyear IS NULL
  ORDER  BY create_time DESC;
  ```

#### 报错2：经销商名称不能为空
- **触发条件**：点击"查询"按钮，传入的custName（经销商名称）参数为null或空字符串
- **逻辑分析**：报表查询按经销商名称模糊匹配FIN_FEE_APPLY_HEADER.CUST_NAME。若前端未输入经销商名称且未设置默认值，或传参丢失，后端校验为空即抛异常。该条件用于缩小查询范围，缺失会导致全量数据返回，影响性能。
- **排查SQL**：
  ```sql
  SELECT fee_apply_id        AS 申请单ID,
         fee_apply_no        AS 申请单号,
         cust_name           AS 经销商名称,
         terminal_name       AS 门店名称,
         cyear               AS 预算年度
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1
  AND    (cust_name IS NULL OR cust_name = '')
  ORDER  BY create_time DESC;
  ```

#### 报错3：门店名称不能为空
- **触发条件**：点击"查询"按钮，传入的terminalName（门店名称）参数为null或空字符串
- **逻辑分析**：报表查询按门店名称模糊匹配FIN_FEE_APPLY_HEADER.TERMINAL_NAME。若前端未输入门店名称且未设置默认值，或传参丢失，后端校验为空即抛异常。该条件用于定位具体门店的装修进度，缺失会导致该经销商下所有门店数据返回。
- **排查SQL**：
  ```sql
  SELECT fee_apply_id        AS 申请单ID,
         fee_apply_no        AS 申请单号,
         cust_name           AS 经销商名称,
         terminal_name       AS 门店名称,
         cyear               AS 预算年度
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1
  AND    (terminal_name IS NULL OR terminal_name = '')
  ORDER  BY create_time DESC;
  ```

#### 报错4：报表查询失败：申请单状态异常
- **触发条件**：点击"查询"按钮，查询FIN_FEE_APPLY_HEADER时apply_type不等于1（装修申请类型）
- **逻辑分析**：本报表仅展示装修申请（apply_type=1）的进度数据。若数据来源错误（如apply_type被错误更新为其他值）、或历史数据迁移时apply_type未正确赋值，查询结果会缺失或包含非装修类申请单，导致报表数据异常。
- **排查SQL**：
  ```sql
  SELECT fee_apply_id        AS 申请单ID,
         fee_apply_no        AS 申请单号,
         apply_type          AS 申请类型,
         cust_name           AS 经销商名称,
         terminal_name       AS 门店名称,
         create_time         AS 创建时间
  FROM   fin_fee_apply_header
  WHERE  apply_type <> 1
  AND    fee_apply_no LIKE 'ZXSQ%'
  ORDER  BY create_time DESC;
  ```

#### 报错5：导出失败：数据量超过限制
- **触发条件**：点击"导出"按钮，导出Excel时查询结果数据量超过系统配置的上限（如10万条）
- **逻辑分析**：导出接口会先执行查询统计结果数量，若超过系统配置的导出上限（防止OOM和性能问题），抛出异常阻止导出。常见根因：查询条件过宽（如未限制年度、经销商）、或历史数据堆积过多。需缩小查询条件或分批导出。
- **排查SQL**：
  ```sql
  SELECT cyear               AS 预算年度,
         cust_name           AS 经销商名称,
         COUNT(*)            AS 申请单数量
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1
  GROUP  BY cyear, cust_name
  HAVING COUNT(*) > 10000
  ORDER  BY 申请单数量 DESC;
  ```

#### 报错6：网络请求失败/接口调用异常
- **触发条件**：点击"查询"或"导出"按钮，调用POST /v1/{organizationId}/terminalReport/mkt-terminal-apply-list/search或GET /mkt-terminal-export接口时，前端未收到响应或收到非2xx状态码
- **逻辑分析**：本页面为hlod低代码报表页面，查询依赖后端TerminalReportController.mktTerminalApplyListSearch接口分页查询FIN_FEE_APPLY_HEADER（apply_type=1）。若后端ae-report服务未启动、Oracle数据库连接异常、SQL执行超时（如全表扫描无索引）、网络中断、或网关转发失败，均会导致接口调用异常。导出接口因需全量查询数据更易超时。需检查后端服务健康状态、数据库连接、网络连通性。
- **排查SQL**：
  ```sql
  SELECT COUNT(*)            AS 装修申请单总数,
         MIN(create_time)    AS 最早创建时间,
         MAX(create_time)    AS 最晚创建时间
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1;
  ```

#### 报错7：权限不足/未登录
- **触发条件**：页面加载或点击"查询"/"导出"按钮时，接口返回401未授权或403禁止访问，或前端路由守卫拦截
- **逻辑分析**：本报表接口声明@Permission(level = ResourceLevel.ORGANIZATION)，要求用户具备组织级权限。若用户未登录（token过期/丢失）、或当前角色未分配该报表菜单权限、或organizationId路径参数与用户所属组织不匹配，均会触发权限校验失败。hlod低代码页面通过路由配置和接口权限双重校验，任一环节失败均阻断访问。需重新登录或联系管理员分配报表查看权限。
- **排查SQL**：
  ```sql
  SELECT '权限校验为应用层逻辑，无对应数据表' AS 提示
  FROM   dual;
  ```

#### 报错8：导出失败：网络异常
- **触发条件**：点击"导出"按钮，调用GET /v1/{organizationId}/terminalReport/mkt-terminal-export接口过程中，网络中断、后端响应超时或Excel文件流传输中断
- **逻辑分析**：导出接口通过@ExcelExport(MktTerminalApplyListExportVO.class)注解实现Excel导出，后端先全量查询FIN_FEE_APPLY_HEADER（apply_type=1）再生成Excel文件流返回。若查询数据量较大导致响应超时、或生成Excel过程中内存溢出、或网络不稳定导致文件流中断、或浏览器下载被拦截，均会触发导出失败。与"数据量超过限制"不同，本报错侧重网络/超时层面。需重试导出或缩小查询条件减少数据量。
- **排查SQL**：
  ```sql
  SELECT cyear               AS 预算年度,
         COUNT(*)            AS 申请单数量
  FROM   fin_fee_apply_header
  WHERE  apply_type = 1
  GROUP  BY cyear
  ORDER  BY 申请单数量 DESC;
  ```

### 常见问题

- 问题1：报表数据为空
  - 原因：无符合条件的装修申请数据
  - 解决思路：放宽查询条件

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | - | 按skill规范重写知识库文档 |
| 2025-12-10 | - | HZERO | 初始创建报表功能 |
