# 财务管理-对账单-待兑现折扣折让对账单

## 业务流程

### 业务流程图

```text
待兑现折扣折让对账单页面(arrow-crm标准React页面)
        │
        ├── 查询对账单列表 ──→ GET /lHCASHFINBillHead/list（按事业部/经销商/年月查询）
        │
        ├── 查看对账单明细 ──→ GET /lHCASHFINBillHead/{headerId}/detail
        │
        ├── 查询行明细 ──→ GET /LnkCashpoolsBillLine/queryCashPoolsBillLine
        │
        ├── 法人确认对账单 ──→ POST /lHCASHFINBillHead/confirmBill
        │
        └── 导出 ──→ POST /lHCASHFINBillHead/export（头导出）/ /LnkCashpoolsBillLine/export（行导出）
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 折扣折让政策 | 数据依赖 | 对账单数据来源于折扣折让政策 | 折扣政策已审批通过 |
| 经销商主数据 | 数据依赖 | 按经销商维度展示对账单 | 经销商已创建 |
| CRM基础数据 | 数据依赖 | 对账单关联CRM基础数据 | CRM数据已同步 |
| 资金池 | 数据依赖 | 对账单关联资金池余额信息 | 资金池已建立 |

### 下游影响

- 影响1：折扣折让兑现
  - 待兑现对账单为后续兑现流程提供数据依据，兑现动作由兑现流程完成

---

## 重点逻辑

### 重点逻辑1：多维度对账查询 {查询检索}

- **业务意义**：按事业部、经销商、年月等多维度查询待兑现折扣折让对账单，掌握未兑现金额

- **具体逻辑描述**

  - 第1点：支持按事业部、交易公司、法人、年月、经销商等多维度查询

  - 第2点：查询结果展示待兑现的折扣折让金额、政策信息、经销商信息

  - 第3点：列表支持分页查询和排序

### 重点逻辑2：对账单明细查看 {明细展示}

- **业务意义**：查看单条对账单的详细行明细，支撑对账核对

- **具体逻辑描述**

  - 第1点：从列表页点击查看进入详情页，展示对账单头信息和行明细

  - 第2点：行明细通过queryCashPoolsBillLine接口查询，展示资金池对账明细行数据

  - 第3点：详情页数据为只读，不可编辑

### 重点逻辑3：法人确认对账单 {确认操作}

- **业务意义**：法人确认对账单后，对账数据进入正式对账流程

- **具体逻辑描述**

  - 第1点：通过confirmBill接口对对账单进行法人确认

  - 第2点：确认后更新对账单状态

---

## 详细逻辑

### 界面模块1：待兑现折扣折让对账单列表页（arrow-crm标准React页面）

> 前端源码位于 `arrow-crm/src/pages/financialManagement/cashFinBill`，后端基于crm-business服务。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 对账单ID | LNK_CASH_BILL_HEAD_NEW.HEADER_ID | 文本框 | 对账单头ID | 常显 | 系统生成 |
| 期间 | LNK_CASH_BILL_HEAD_NEW.BILLMONTH | 文本框 | 对账期间 | 常显 | 手动选择或系统带出 |
| 经销商编码 | LNK_CASH_BILL_HEAD_NEW.SERVERNO | 文本框 | 经销商编码 | 常显 | 选择经销商带出 |
| 经销商名称 | LNK_CASH_BILL_HEAD_NEW.SERVERNAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 事业部 | LNK_CASH_BILL_HEAD_NEW.DEPT_NAME | 文本框 | 事业部名称 | 常显 | 选择事业部带出 |
| 资金池类型 | LNK_CASH_BILL_HEAD_NEW.FUND_TYPE | 下拉选择框 | 资金池类型 | 常显 | 手动选择 |
| 期初资金池 | LNK_CASH_BILL_HEAD_NEW.BEGINCASHPOOL | 数字显示框 | 期初资金池余额 | 常显 | 系统计算 |
| 广告费 | LNK_CASH_BILL_HEAD_NEW.ADVERTISING | 数字显示框 | 广告费金额 | 常显 | 系统计算 |
| 折扣折让 | LNK_CASH_BILL_HEAD_NEW.REBATE | 数字显示框 | 折扣折让金额 | 常显 | 系统计算 |
| 期末资金池 | LNK_CASH_BILL_HEAD_NEW.ENDCASHPOOL | 数字显示框 | 期末资金池余额 | 常显 | 系统计算 |
| 币种 | LNK_CASH_BILL_HEAD_NEW.CURRENCY | 文本框 | 币种 | 常显 | 系统带出 |
| 状态 | LNK_CASH_BILL_HEAD_NEW.STATUS | 文本框 | 对账单状态 | 常显 | 系统更新 |

### 界面模块2：对账单明细行（arrow-crm标准React页面）

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 行ID | LNK_CASHPOOLS_BILL_LINE.ROW_ID | 文本框 | 明细行ID | 常显 | 系统生成 |
| 关联头ID | LNK_CASHPOOLS_BILL_LINE.HEADER_ID | 文本框 | 关联对账单头 | 常显 | 关联头表 |
| 资金池ID | LNK_CASHPOOLS_BILL_LINE.CASHPOOL_ID | 文本框 | 资金池ID | 常显 | 系统带出 |
| 开始日期 | LNK_CASHPOOLS_BILL_LINE.BEGINDATE | 日期显示框 | 明细开始日期 | 常显 | 系统带出 |
| 结束日期 | LNK_CASHPOOLS_BILL_LINE.ENDDATE | 日期显示框 | 明细结束日期 | 常显 | 系统带出 |

### 选择弹窗

#### 弹窗1：经销商选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| serverno | 经销商编码 | 经销商编码 | "C001" | 当前用户有权限的经销商 |

> 查询SQL（经销商主数据）：

```sql
SELECT CUSTOMER_CODE AS SERVERNO, CUSTOMER_NAME AS SERVERNAME 
FROM CUSTOMER_ORG WHERE ENABLED = 1
```

### 导入

> 本页面为查询页面，无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询对账单列表 | 列表页 | 始终可用 | 调用list接口 |
| 查看明细 | 查看对账单明细 | 列表页 | 选中一条记录 | 调用detail接口弹出明细页 |
| 法人确认 | 法人确认对账单 | 列表页 | 选中未确认的记录 | 调用confirmBill接口更新状态 |
| 导出 | 导出对账单 | 列表页 | 有查询结果时 | 调用export接口导出Excel |

#### 按钮1：查询（列表页）

- **触发条件**：始终可用
- **执行逻辑**：
  - 第1点：按事业部、经销商、年月等条件查询待兑现折扣折让对账单
  - 第2点：支持分页查询和排序
- **接口调用**：GET `/v1/{organizationId}/lHCASHFINBillHead/list`
- **排查SQL**：

```sql
SELECT * FROM LNK_CASH_BILL_HEAD_NEW 
WHERE (DEPT_ID = #{deptId} OR #{deptId} IS NULL)
  AND (SERVERNO = #{serverno} OR #{serverno} IS NULL)
  AND (BILLMONTH = #{billmonth} OR #{billmonth} IS NULL)
ORDER BY BILLMONTH DESC, SERVERNO
```

#### 按钮2：查看明细（列表页）

- **触发条件**：选中一条记录
- **执行逻辑**：
  - 第1点：查询选中对账单的头信息和行明细
  - 第2点：行明细通过queryCashPoolsBillLine接口查询
- **接口调用**：GET `/v1/{organizationId}/lHCASHFINBillHead/{headerId}/detail`
- **排查SQL**：

```sql
SELECT * FROM LNK_CASHPOOLS_BILL_LINE WHERE HEADER_ID = #{headerId};
```

#### 按钮3：法人确认（列表页）

- **触发条件**：选中未确认的对账单
- **执行逻辑**：
  - 第1点：对选中的对账单进行法人确认
  - 第2点：更新对账单状态为已确认
- **接口调用**：POST `/v1/{organizationId}/lHCASHFINBillHead/confirmBill`
- **排查SQL**：

```sql
SELECT * FROM LNK_CASH_BILL_HEAD_NEW WHERE HEADER_ID = #{headerId} AND STATUS != 'CONFIRMED';
```

#### 按钮4：导出（列表页）

- **触发条件**：有查询结果时可用
- **执行逻辑**：
  - 第1点：将当前查询结果导出为Excel文件
  - 第2点：支持头导出和行导出
- **接口调用**：POST `/v1/{organizationId}/lHCASHFINBillHead/export`
- **排查SQL**：同查询SQL

### 保存校验

> 本页面为查询页面，无保存操作。

### 提交校验

> 本页面为查询页面，无提交操作。

### 状态机

#### 状态机流转图

```text
新建 ──系统生成──→ 待确认 ──法人确认──→ 已确认
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| 待确认 | 对账单已生成待法人确认 | 查看、法人确认、导出 |
| 已确认 | 法人已确认对账单 | 查看、导出 |

---

## 数据库表详解

### 表1：LNK_CASH_BILL_HEAD_NEW（待兑现折扣折让对账单头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| HEADER_ID | VARCHAR | 对账单头ID | 对账单ID | 系统生成 |
| BILLMONTH | VARCHAR | 对账期间 | 期间 | 手动选择或系统带出 |
| SERVERNO | VARCHAR | 经销商编码 | 经销商编码 | 选择经销商带出 |
| SERVERNAME | VARCHAR | 经销商名称 | 经销商名称 | 选择经销商带出 |
| DEPT_ID | VARCHAR | 事业部ID | - | 选择事业部带出 |
| DEPT_NAME | VARCHAR | 事业部名称 | 事业部 | 选择事业部带出 |
| FUND_TYPE | VARCHAR | 资金池类型 | 资金池类型 | 手动选择 |
| BEGINCASHPOOL | DECIMAL | 期初资金池余额 | 期初资金池 | 系统计算 |
| ADVERTISING | DECIMAL | 广告费金额 | 广告费 | 系统计算 |
| REBATE | DECIMAL | 折扣折让金额 | 折扣折让 | 系统计算 |
| ENDCASHPOOL | DECIMAL | 期末资金池余额 | 期末资金池 | 系统计算 |
| CURRENCY | VARCHAR | 币种 | 币种 | 系统带出 |
| STATUS | VARCHAR | 对账单状态 | 状态 | 系统更新 |
| CASHPOOL_ID | VARCHAR | 资金池ID | - | 关联资金池 |

### 表2：LNK_CASHPOOLS_BILL_LINE（资金池对账明细行表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ROW_ID | VARCHAR | 明细行ID | 行ID | 系统生成 |
| HEADER_ID | VARCHAR | 关联对账单头ID | 关联头ID | FK → LNK_CASH_BILL_HEAD_NEW |
| CASHPOOL_ID | VARCHAR | 资金池ID | 资金池ID | 系统带出 |
| BEGINDATE | DATE | 明细开始日期 | 开始日期 | 系统带出 |
| ENDDATE | DATE | 明细结束日期 | 结束日期 | 系统带出 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 查询条件不匹配或对账单未生成，放宽条件或确认数据已同步 | toast提醒 | [查看] |
| 请输入id | 法人确认时 | 未传入对账单ID，选中一条记录后重试 | toast提醒 | [查看] |
| 确认对账单失败，请稍后重试 | 法人确认时 | 对账单状态非待确认或并发更新影响行数为0，刷新列表确认状态后重试 | 阻断性报错 | [查看] |
| 对账单明细不存在 | 查看明细时 | 对账单头表或行表数据缺失，刷新列表后重试 | toast提醒 | [查看] |
| 网络请求失败 | 调用接口时 | 后端服务不可用或网络中断，稍后重试或联系运维 | toast提醒 | [查看] |
| 权限不足 | 查询/确认/导出时 | 当前用户无该组织或菜单访问权限，联系管理员分配权限 | toast提醒 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按事业部/经销商/年月/资金池类型组合条件查询待兑现折扣折让对账单，返回结果集为空
- **逻辑分析**：查询接口lHCASHFINBillHead/list基于LNK_CASH_BILL_HEAD_NEW表过滤，该表数据来源于折扣折让政策审批通过后由系统汇总生成，并关联资金池（LNK_CASHPOOLS_BILL_LINE）。无数据根因有三类：(1)查询条件（BILLMONTH+SERVERNO+DEPT_ID）组合过窄；(2)折扣折让政策未审批通过或资金池数据未同步，对账单尚未生成；(3)FUND_TYPE资金池类型筛选与对账单实际类型不匹配。需先确认表中有数据，再核对上游政策审批状态
- **排查SQL**：
  ```sql
  SELECT HEADER_ID, BILLMONTH, SERVERNO, SERVERNAME, DEPT_NAME, FUND_TYPE,
         BEGINCASHPOOL, ADVERTISING, REBATE, ENDCASHPOOL, STATUS
  FROM LNK_CASH_BILL_HEAD_NEW
  WHERE (DEPT_ID = #{deptId} OR #{deptId} IS NULL)
    AND (SERVERNO = #{serverno} OR #{serverno} IS NULL)
    AND (BILLMONTH = #{billmonth} OR #{billmonth} IS NULL)
    AND (FUND_TYPE = #{fundType} OR #{fundType} IS NULL)
  ORDER BY BILLMONTH DESC, SERVERNO;
  ```

#### 报错2：请输入id
- **触发条件**：用户在列表页未选中记录或选中后ID未传入，直接触发法人确认操作
- **逻辑分析**：法人确认接口confirmBill在Controller层（LnkCashBillHeadNewController.java:92）校验入参id非空，若StringUtils.isBlank(id)则抛出CommonException("请输入id")。根因有二：(1)前端未选中行就调用确认接口，id参数为null或空字符串；(2)前端选中行但headerId字段未正确传递到请求参数。校验在Controller层前置拦截，toast提示后阻断确认操作。需选中一条待确认的对账单后再点击法人确认
- **排查SQL**：
  ```sql
  SELECT HEADER_ID, BILLMONTH, SERVERNO, SERVERNAME, STATUS
  FROM LNK_CASH_BILL_HEAD_NEW
  WHERE STATUS != 'Confirmed'
  ORDER BY BILLMONTH DESC;
  ```

#### 报错3：确认对账单失败，请稍后重试
- **触发条件**：用户点击法人确认，updateBillStatus方法返回的影响行数updateCount为0
- **逻辑分析**：法人确认调用LnkCashBillHeadNewServiceImpl.updateBillStatus（LnkCashBillHeadNewServiceImpl.java:115），执行UPDATE LNK_CASH_BILL_HEAD_NEW SET STATUS='Confirmed' WHERE HEADER_ID=#{id}后判断updateCount==0则抛出CommonException("确认对账单失败，请稍后重试")。根因有三：(1)对账单已被他人确认（并发场景），STATUS已为'Confirmed'，UPDATE的WHERE条件未匹配；(2)对账单HEADER_ID不存在或已被删除；(3)对账单状态非待确认（如已关闭、已作废），不允许确认。需刷新列表确认对账单当前状态后重试
- **排查SQL**：
  ```sql
  SELECT HEADER_ID, BILLMONTH, SERVERNO, SERVERNAME, STATUS, 
         (CASE WHEN STATUS = 'Confirmed' THEN '已确认-不可重复确认' 
               WHEN STATUS IS NULL THEN '状态异常'
               ELSE '待确认' END) AS 状态判断
  FROM LNK_CASH_BILL_HEAD_NEW
  WHERE HEADER_ID = #{headerId};
  ```

#### 报错4：对账单明细不存在
- **触发条件**：用户在列表页选中对账单点击"查看明细"，detail接口或queryCashPoolsBillLine接口返回空
- **逻辑分析**：查看明细调用/v1/{organizationId}/lHCASHFINBillHead/{headerId}/detail接口，按HEADER_ID查询LNK_CASH_BILL_HEAD_NEW头表和LNK_CASHPOOLS_BILL_LINE行表。根因有三：(1)并发场景下他人已删除该对账单，头表记录不存在；(2)对账单头表存在但行表LNK_CASHPOOLS_BILL_LINE无关联明细（HEADER_ID外键失效或行数据未生成）；(3)传入的headerId参数格式错误或为null。需核对头行表数据一致性
- **排查SQL**：
  ```sql
  SELECT H.HEADER_ID, H.BILLMONTH, H.SERVERNO, H.STATUS,
         (SELECT COUNT(1) FROM LNK_CASHPOOLS_BILL_LINE L WHERE L.HEADER_ID = H.HEADER_ID) AS 行明细数
  FROM LNK_CASH_BILL_HEAD_NEW H
  WHERE H.HEADER_ID = #{headerId};
  ```

#### 报错5：网络请求失败
- **触发条件**：用户点击查询、查看明细、法人确认或导出，前端axios请求抛出网络异常或超时
- **逻辑分析**：前端调用/v1/{organizationId}/lHCASHFINBillHead/list、detail、confirmBill、export等接口时，因后端crm-business服务不可用、网关路由异常、网络中断或请求超时导致连接失败。根因有四：(1)crm-business微服务未注册到Nacos或已宕机；(2)网关路由配置错误找不到服务；(3)网络中断或防火墙拦截；(4)导出数据量超过maxDataCount=50000限制或SQL执行超时。需联系运维确认服务状态
- **排查SQL**：
  ```sql
  -- 核查对账单数据量是否异常增长导致查询/导出超时
  SELECT COUNT(1) AS 对账单总数, MAX(BILLMONTH) AS 最新期间
  FROM LNK_CASH_BILL_HEAD_NEW;
  ```

#### 报错6：权限不足
- **触发条件**：用户访问待兑现折扣折让对账单页面或调用接口时，返回403或"无权限访问"提示
- **逻辑分析**：后端Controller使用@Permission(level = ResourceLevel.ORGANIZATION)控制访问权限，要求用户拥有当前组织（organizationId）的访问权限。根因有三：(1)用户未分配该菜单（arrow-crm/src/pages/financialManagement/cashFinBill）的访问角色；(2)用户当前切换的组织不在其授权组织范围内；(3)用户数据权限未覆盖查询的经销商（CUSTOMER_ORG）或事业部。需联系管理员分配菜单角色和数据权限
- **排查SQL**：
  ```sql
  -- 核查用户在当前组织下的角色分配（表名以HZERO IAM实际表为准）
  SELECT USER_ID, ROLE_ID, ORGANIZATION_ID
  FROM IAM_USER_ROLE
  WHERE USER_ID = #{userId} AND ORGANIZATION_ID = #{organizationId};
  ```

### 常见问题

- 问题1：对账单数据不正确
  - 原因：折扣折让政策或资金池数据未及时同步
  - 解决思路：检查SQL `SELECT * FROM LNK_CASH_BILL_HEAD_NEW WHERE BILLMONTH = #{billmonth}`

- 问题2：法人确认失败
  - 原因：对账单状态不是"待确认"，已被确认或数据异常
  - 解决思路：检查对账单状态 `SELECT STATUS FROM LNK_CASH_BILL_HEAD_NEW WHERE HEADER_ID = #{headerId}`

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充界面模块、选择弹窗、数据库表详解 |
| 2025-11-10 | - | - | 初始创建LnkCashBillHeadNew实体 |
