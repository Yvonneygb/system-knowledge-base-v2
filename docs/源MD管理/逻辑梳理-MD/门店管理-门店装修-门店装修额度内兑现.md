# 门店管理-门店装修-门店装修额度内兑现

## 业务流程

```
门店装修额度内兑现列表页(hlod低代码页面)
        │
        ├── 新增 ──→ 选择已审批的验收报销单(LOV) ──→ 联动带出门店/经销商/报销金额
        │              ──→ 填写兑现信息(兑现金额/发票信息) ──→ 保存
        │                                    │
        │                                    ▼
        │                              生成兑现单号 ──→ HZ_APPROVE_STATUS=NEW
        │                                    │
        │                                    ▼
        │                              通过菜单88(额度内兑现批量复核)提交审批
        │                                    │
        │                                    ▼
        │                              启动OA工作流(STORE_FIN_FEE_IN_CASH_HEAD)
        │                                    │
        │                         ├──审批通过──→ HZ_APPROVE_STATUS=APPROVED
        │                         │              ├─ 回写验收报销单兑现信息
        │                         │              ├─ 同步资金池(EBS)
        │                         │              └─ 推送MBO系统
        │                         │
        │                         └──审批驳回──→ HZ_APPROVE_STATUS=REJECTED
        │
        └── 删除 ──→ 仅NEW状态可删除
```

## 重点逻辑

### 重点逻辑1：额度内兑现金额计算

- **业务意义**：将验收报销单中额度内可报销金额实际支取出来
- **具体逻辑描述**：
  - 本次兑现金额 ≤ 剩余未兑现总额(inThisSurCashoutAmt)
  - 兑现金额 = 报销标准内可报销金额 - 已兑现金额
  - 不超可兑：本次兑现不得超过剩余可兑额度

### 重点逻辑2：批量复核提交

- **业务意义**：额度内兑现通过菜单88批量复核统一提交审批
- **具体逻辑描述**：
  - 多张兑现单通过batchCashId关联到FIN_FEE_IN_CASH_HEAD
  - 批量提交审批，审批通过后统一处理

### 重点逻辑3：审批通过同步外部系统

- **业务意义**：审批通过后同步资金池和MBO系统
- **具体逻辑描述**：
  - 同步资金池调整接口(EBS)
  - 推送MBO系统数据
  - 回写验收报销单的交易公司信息

## 详细逻辑

### 界面模块

本页面为hlod低代码页面，前端尚无独立React组件。后端Controller(FinFeeTerminalCashoutController)为空Controller，CRUD逻辑由菜单87(门店验收与报销单)的FinFeeCheckBxHeaderServiceImpl统一承载。

#### 头部信息区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 兑现单号 | TERMINAL_CASHOUT_CODE | TextField | 兑现单号 | 始终 | 编码规则生成 |
| 验收报销单号 | CHECK_BX_CODE | Lov | 选择验收报销单 | 始终 | 选择已审批的验收报销单 |
| 门店编码 | TERMINAL_CODE | TextField | 门店编码 | 始终 | 从验收报销单带入 |
| 门店名称 | TERMINAL_NAME | TextField | 门店名称 | 始终 | 从验收报销单带入 |
| 经销商编码 | CUST_CODE | TextField | 经销商编码 | 始终 | 从验收报销单带入 |
| 经销商名称 | CUST_NAME | TextField | 经销商名称 | 始终 | 从验收报销单带入 |
| 交易公司 | TRADING_COMPANY_NAME | TextField | 交易公司 | 始终 | 从验收报销单带入 |
| 法人客户 | BILLING_UNIT_NAME | TextField | 法人客户 | 始终 | 从验收报销单带入 |
| 额度内本次兑现金额 | IN_THIS_CASHOUT_AMT | NumberField | 额度内本次兑现金额 | 始终 | 用户输入，≤剩余可兑额度 |
| 额度内剩余可兑金额 | IN_THIS_SUR_CASHOUT_AMT | NumberField | 额度内剩余可兑金额 | 始终 | 系统计算 |
| 上次累计兑现金额 | LAST_SUM_CASHOUT_AMT | NumberField | 上次累计兑现金额 | 始终 | 系统计算 |
| 本次月兑现金额 | THIS_MONTH_CASHOUT_AMT | NumberField | 本次月兑现金额 | 始终 | 系统计算 |
| 兑现比例 | CASHOUT_RATE | NumberField | 兑现比例 | 始终 | 系统计算 |
| 开票日期 | INVOICE_PAID_DATE | DatePicker | 开票日期 | 始终 | 用户输入 |
| 开票金额 | INVOICE_PAID_AMOUNT | NumberField | 开票金额 | 始终 | 用户输入 |
| 收款状态 | RECEIPT_STATUS | Select | 收款状态 | 始终 | 系统维护 |
| 入账状态 | PROCESS_STATUS | Select | 入账状态 | 始终 | 系统维护 |
| 备注 | NOTE | TextField | 备注 | 始终 | 用户输入 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 入账 | POST | `/v1/{organizationId}/fin-fee-terminal-cashouts/update-recorded` | 入账操作(updateRecorded) |

注：本菜单CRUD逻辑由菜单87的FinFeeCheckBxHeaderServiceImpl统一承载，无独立Controller API。

### 选择弹窗

| 弹窗名称 | LOV编码 | 参数 | 说明 |
|----------|---------|------|------|
| 验收报销单号 | - | - | 选择已审批(APPROVED)的门店验收与报销单，联动带出门店/经销商/报销金额等 |

### 导入

本页面无导入功能。

### 其他按钮

| 按钮名称 | 触发条件 | 执行逻辑 | 接口调用 |
|----------|---------|---------|---------|
| 删除 | HZ_APPROVE_STATUS为NEW | 删除兑现单 | DELETE |
| 入账 | HZ_APPROVE_STATUS为APPROVED | 标记入账状态 | POST /update-recorded |

### 保存校验

- 校验1：验收报销单必填 —— 确保关联有效的验收报销单
  - **详细逻辑**：前端必填校验
  - **系统体现**：C7N内置校验
  - **排查SQL**：`SELECT TERMINAL_CASHOUT_ID FROM FIN_FEE_TERMINAL_CASHOUT WHERE CHECK_BX_ID IS NULL`

- 校验2：兑现金额≤剩余可兑额度 —— 防止超额兑现
  - **详细逻辑**：inThisCashoutAmt ≤ inThisSurCashoutAmt
  - **系统体现**：前端/后端校验
  - **排查SQL**：`SELECT TERMINAL_CASHOUT_ID, IN_THIS_CASHOUT_AMT, IN_THIS_SUR_CASHOUT_AMT FROM FIN_FEE_TERMINAL_CASHOUT WHERE IN_THIS_CASHOUT_AMT > IN_THIS_SUR_CASHOUT_AMT`

### 提交校验

- 校验1：通过菜单88批量复核提交 —— 额度内兑现无独立提交入口
  - **详细逻辑**：多张兑现单通过batchCashId关联到FIN_FEE_IN_CASH_HEAD，批量提交
  - **系统体现**：菜单88批量复核工作流STORE_FIN_FEE_IN_CASH_HEAD
  - **排查SQL**：`SELECT * FROM FIN_FEE_TERMINAL_CASHOUT WHERE BATCH_CASH_ID={cashId}`

### 状态机

```text
NEW(新建) ──批量提交──→ RUN(审批中) ──┬──审批通过──→ APPROVED(已审批)
                                       │              ├─ 回写验收报销单
                                       │              ├─ 同步资金池(EBS)
                                       │              └─ 推送MBO
                                       │
                                       └──审批驳回──→ REJECTED(已驳回)

NEW ──删除──→ (删除)
APPROVED ──入账──→ (已入账)
```

### 工作流

- **工作流编码**：无独立工作流，通过菜单88(STORE_FIN_FEE_IN_CASH_HEAD)批量复核提交
- **编码规则**：兑现单号由系统生成

## 数据库表详解

### FIN_FEE_TERMINAL_CASHOUT（额度内门店装修兑现表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| TERMINAL_CASHOUT_ID | Long | 兑现ID | - | 自增 |
| TERMINAL_CASHOUT_CODE | String | 兑现单号 | 兑现单号 | 编码规则生成 |
| CHECK_BX_ID | Long | 验收报销单ID | - | 用户选择(LOV) |
| CHECK_BX_CODE | String | 验收报销单号 | 验收报销单号 | 选择时带入 |
| TERMINAL_ID | Long | 门店ID | - | 从验收报销单带入 |
| TERMINAL_CODE | String | 门店编码 | 门店编码 | 从验收报销单带入 |
| TERMINAL_NAME | String | 门店名称 | 门店名称 | 从验收报销单带入 |
| CUST_ID | Long | 经销商ID | - | 从验收报销单带入 |
| CUST_CODE | String | 经销商编码 | 经销商编码 | 从验收报销单带入 |
| CUST_NAME | String | 经销商名称 | 经销商名称 | 从验收报销单带入 |
| TRADING_COMPANY_ID | Long | 交易公司ID | - | 从验收报销单带入 |
| TRADING_COMPANY_CODE | String | 交易公司编码 | - | 从验收报销单带入 |
| TRADING_COMPANY_NAME | String | 交易公司名称 | 交易公司 | 从验收报销单带入 |
| BILLING_UNIT_ID | Long | 法人客户ID | - | 从验收报销单带入 |
| BILLING_UNIT_CODE | String | 法人客户编码 | - | 从验收报销单带入 |
| BILLING_UNIT_NAME | String | 法人客户名称 | 法人客户 | 从验收报销单带入 |
| COST_CENTER_CODE | String | 成本中心编码 | - | 从验收报销单带入 |
| COST_CENTER_NAME | String | 成本中心名称 | - | 从验收报销单带入 |
| IN_THIS_CASHOUT_AMT | BigDecimal | 额度内本次兑现金额 | 额度内本次兑现金额 | 用户输入 |
| IN_THIS_SUR_CASHOUT_AMT | BigDecimal | 额度内剩余可兑金额 | 额度内剩余可兑金额 | 系统计算 |
| IN_CAN_NOT_TAX_BX_AMT | BigDecimal | 额度内不可税报销金额 | - | 系统计算 |
| LAST_SUM_CASHOUT_AMT | BigDecimal | 上次累计兑现金额 | 上次累计兑现金额 | 系统计算 |
| THIS_MONTH_CASHOUT_AMT | BigDecimal | 本次月兑现金额 | 本次月兑现金额 | 系统计算 |
| MONTH_CASHOUT_AMT | BigDecimal | 月兑现金额 | - | 系统计算 |
| MONTH_REDUCE_AMT | BigDecimal | 月扣减金额 | - | 系统计算 |
| IN_REDUCE_AMT | BigDecimal | 额度内扣减金额 | - | 系统计算 |
| CASHOUT_RATE | BigDecimal | 兑现比例 | 兑现比例 | 系统计算 |
| START_CASHOUT_TIME | LocalDate | 兑现开始时间 | - | 从验收报销单带入 |
| END_CASHOUT_TIME | LocalDate | 兑现结束时间 | - | 从验收报销单带入 |
| INVOICE_PAID_DATE | LocalDate | 开票日期 | 开票日期 | 用户输入 |
| INVOICE_PAID_AMOUNT | BigDecimal | 开票金额 | 开票金额 | 用户输入 |
| RECEIPT_STATUS | String | 收款状态 | 收款状态 | 系统维护 |
| PROCESS_STATUS | String | 入账状态 | 入账状态 | 系统维护 |
| CASH_ID | Long | 批量复核ID | - | 关联FIN_FEE_IN_CASH_HEAD |
| IS_SHARE | Long | 是否分摊 | - | 系统维护 |
| NOTE | String | 备注 | 备注 | 用户输入 |
| CREATOR | String | 创建人 | - | 系统赋值 |
| CREATE_TIME | Date | 创建时间 | - | 系统赋值 |
| CHECKER | String | 审核人 | - | 审批通过时赋值 |
| CHECK_TIME | LocalDateTime | 审核时间 | - | 审批通过时赋值 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |
| HZ_INSTANCE_ID | String | 流程实例ID | - | 工作流启动后赋值 |
| HZ_APPROVE_STATUS | String | 流程审批状态 | - | NEW/RUN/APPROVED/REJECTED |

## 常见问题FAQ

### Q1：兑现金额超过剩余可兑额度

**根因**：inThisCashoutAmt > inThisSurCashoutAmt
**解决方案**：调整兑现金额不超过剩余可兑额度

### Q2：无法提交审批

**根因**：额度内兑现无独立提交入口，需通过菜单88批量复核提交
**解决方案**：在菜单88中创建批量复核单，关联多张兑现单后统一提交

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 门店验收与报销单(FIN_FEE_CHECK_BX_HEADER) | 数据依赖 | 选择已审批的验收报销单，带入报销金额等 | 存在APPROVED状态的验收报销单 |
| 额度内兑现批量复核(FIN_FEE_IN_CASH_HEAD) | 流程依赖 | 通过batchCashId关联，批量提交审批 | 菜单88已创建批量复核单 |

## 下游影响

- **门店验收与报销单(FIN_FEE_CHECK_BX_HEADER)** — 审批通过后回写兑现信息
- **资金池(EBS)** — 审批通过后同步资金池调整
- **MBO系统** — 审批通过后推送MBO数据
- **额度内预算** — 更新额度预算已使用金额

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 兑现金额超过剩余可兑额度 | 保存时 | inThisCashoutAmt>inThisSurCashoutAmt | 中 | [查看](#报错1兑现金额超过剩余可兑额度) |
| 验收报销单不存在 | 保存时 | checkBxId对应的验收报销单不存在 | 高 | [查看](#报错2验收报销单不存在) |
| 单据信息不存在 | 批量复核查询/删除/审批回调 | 批量复核单cashId对应FIN_FEE_IN_CASH_HEAD记录不存在 | 高 | [查看](#报错3单据信息不存在) |
| 验收报销单号和门头兑现单号为空 | 推送共享 | 兑现单关联的checkBxCode和dhCashoutNo均为空 | 高 | [查看](#报错4验收报销单号和门头兑现单号为空) |
| 验收单号X，总账日期不能为空，请检查 | 批量复核提交 | 关联的兑现单ledgerDate为空 | 高 | [查看](#报错5验收单号x总账日期不能为空请检查) |

#### 报错1：兑现金额超过剩余可兑额度
- **触发条件**：点击"保存"按钮，前端/后端校验inThisCashoutAmt>inThisSurCashoutAmt（本次兑现金额>剩余可兑额度）
- **逻辑分析**：额度内兑现金额计算规则：本次兑现金额≤剩余未兑现总额(inThisSurCashoutAmt)，剩余可兑额度=报销标准内可报销金额-已兑现金额。若用户填写的本次兑现金额超过剩余可兑额度（如已部分兑现后超额兑现、或剩余可兑额度被其他兑现单占用未同步更新），校验不通过抛异常。需修改兑现金额至≤剩余可兑额度。
- **排查SQL**：
  ```sql
  SELECT c.terminal_cashout_id     AS 兑现单ID,
         c.terminal_cashout_code   AS 兑现单号,
         c.check_bx_id             AS 验收报销ID,
         c.check_bx_code           AS 验收报销单号,
         c.in_this_cashout_amt     AS 本次兑现金额,
         c.in_this_sur_cashout_amt AS 剩余可兑金额,
         c.hz_approve_status       AS 审批状态
  FROM   fin_fee_terminal_cashout c
  WHERE  c.hz_approve_status = 'NEW'
  AND    c.in_this_cashout_amt > c.in_this_sur_cashout_amt
  ORDER  BY c.creation_date DESC;
  ```

#### 报错2：验收报销单不存在
- **触发条件**：点击"保存"按钮，按checkBxId关联查询FIN_FEE_CHECK_BX_HEADER返回null
- **逻辑分析**：额度内兑现需基于已审批的验收报销单发起，兑现单通过checkBxId关联报销单。若验收报销单在兑现前被删除、checkBxId传值错误（如LOV缓存失效ID）、或验收报销单未审批通过即被选入兑现，查询返回空抛异常。需核查验收报销单是否存在且已APPROVED。
- **排查SQL**：
  ```sql
  SELECT c.terminal_cashout_id   AS 兑现单ID,
         c.terminal_cashout_code AS 兑现单号,
         c.check_bx_id           AS 验收报销ID,
         c.check_bx_code         AS 验收报销单号,
         ck.hz_approve_status    AS 报销单审批状态
  FROM   fin_fee_terminal_cashout c
  LEFT   JOIN fin_fee_check_bx_header ck ON ck.check_bx_id = c.check_bx_id
  WHERE  c.hz_approve_status = 'NEW'
  AND    ck.check_bx_id IS NULL
  ORDER  BY c.creation_date DESC;
  ```

#### 报错3：单据信息不存在
- **触发条件**：菜单88批量复核执行doSelect查询详情/doDelete删除/onWfComplete审批通过回调/onUserSubmit提交/onWfBreak审批驳回回调时，selectByPrimaryKey按cashId查询FIN_FEE_IN_CASH_HEAD返回null
- **逻辑分析**：额度内兑现通过菜单88批量复核统一提交审批，批量复核单(FIN_FEE_IN_CASH_HEAD)通过cashId关联多张兑现单。若批量复核单在操作期间被其他用户删除、cashId传值错误（如前端缓存失效ID）、或并发场景下被清理，查询返回空抛异常。需刷新列表页重新获取有效数据。
- **排查SQL**：
  ```sql
  SELECT h.cash_id         AS 批量复核ID,
         h.cash_code       AS 批量复核单号,
         h.hz_approve_status AS 审批状态,
         h.creation_date   AS 创建时间
  FROM   fin_fee_in_cash_head h
  WHERE  h.cash_id = #{传入的cashId};
  ```

#### 报错4：验收报销单号和门头兑现单号为空
- **触发条件**：批量复核审批通过后doSendShare推送共享时，遍历关联的兑现单校验checkBxCode和dhCashoutNo均为空字符串
- **逻辑分析**：额度内兑现单来源有两种：基于验收报销单发起(checkBxCode非空)和基于门头展板兑现发起(dhCashoutNo非空)。推送共享服务时需按来源单号定位数据，若兑现单两个来源单号均为空（数据迁移遗漏、手工修改数据库清空单号、或新建兑现单未正确关联来源），无法确定推送来源即抛异常。需核查兑现单来源单号完整性。
- **排查SQL**：
  ```sql
  SELECT c.terminal_cashout_id     AS 兑现单ID,
         c.terminal_cashout_code   AS 兑现单号,
         c.check_bx_code           AS 验收报销单号,
         c.dh_cashout_no           AS 门头兑现单号,
         c.cash_id                 AS 批量复核ID,
         c.hz_approve_status       AS 审批状态
  FROM   fin_fee_terminal_cashout c
  WHERE  c.cash_id IS NOT NULL
  AND    NVL(c.check_bx_code, '') = ''
  AND    NVL(c.dh_cashout_no, '') = ''
  ORDER  BY c.creation_date DESC;
  ```

#### 报错5：验收单号X，总账日期不能为空，请检查
- **触发条件**：菜单88批量复核提交审批onUserSubmit时，遍历关联兑现单筛选ledgerDate为空的记录，收集其checkBxCode拼接成bxCodes
- **逻辑分析**：批量复核提交审批需推送总账，要求每张兑现单已维护总账日期(ledgerDate)。若验收报销单未回写总账日期（总账同步失败、EBS接口异常）、或兑现单新建后未触发总账日期回写，ledgerDate为空抛异常。需核查对应验收报销单的总账日期是否已回写。
- **排查SQL**：
  ```sql
  SELECT c.terminal_cashout_id   AS 兑现单ID,
         c.terminal_cashout_code AS 兑现单号,
         c.check_bx_id           AS 验收报销ID,
         c.check_bx_code         AS 验收报销单号,
         c.ledger_date           AS 总账日期,
         c.hz_approve_status     AS 审批状态
  FROM   fin_fee_terminal_cashout c
  WHERE  c.cash_id IS NOT NULL
  AND    c.ledger_date IS NULL
  AND    c.hz_approve_status = 'NEW'
  ORDER  BY c.creation_date DESC;
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-10-23 | - | - | 初始创建FinFeeTerminalCashout实体 |
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
