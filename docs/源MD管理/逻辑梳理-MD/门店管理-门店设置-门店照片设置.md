# 门店管理-门店设置-门店照片设置

## 业务流程

```
门店照片设置列表页(hlod低代码页面)
        │
        ├── 新增 ──→ 填写照片设置(照片项目/拍摄要求/是否必填) ──→ 保存
        │
        ├── 编辑 ──→ 修改照片设置
        │
        └── 删除 ──→ 删除照片设置
```

## 重点逻辑

### 重点逻辑1：照片项目配置

- **业务意义**：配置门店装修申请时需要上传的装修前后照片项目
- **具体逻辑描述**：
  - 定义照片项目名称和说明
  - 定义拍摄角度及要求
  - 设置是否验收提供/装修提供及数量要求

## 详细逻辑

### 界面模块

本页面为hlod低代码页面，后端无独立Controller/Entity，查询接口嵌入在FinFeeApplyFinishedHeaderController的queryMktStorephotoSetLine方法中，通过FinFeeApplyFinishedHeaderMapper的mktStorePhotoSetHead/mktStorePhotoSetLine查询。

#### 照片设置区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 照片项目 | PHOTO_ITEM | TextField | 照片项目名称 | 始终 | 用户输入 |
| 照片说明 | PHOTO_ITEM_NOTE | TextField | 照片项目说明 | 始终 | 用户输入 |
| 拍摄要求 | SHOOT_REQUIRE | TextField | 拍摄角度及要求 | 始终 | 用户输入 |
| 是否验收提供 | IS_YS_PROVIDE | Select | 是否验收提供 | 始终 | 用户选择 |
| 验收提供数量 | YS_PROVIDE_COUNT | NumberField | 验收提供数量 | 始终 | 用户输入 |
| 是否装修提供 | IS_ZX_PROVIDE | Select | 是否装修提供 | 始终 | 用户选择 |
| 装修提供数量 | ZX_PROVIDE_COUNT | NumberField | 装修提供数量 | 始终 | 用户输入 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询照片设置 | GET | `/v1/{organizationId}/fin-fee-apply-finished-headers/storephoto-set-line` | 查询门店照片设置(嵌入在FinFeeApplyFinishedHeaderController中) |

### 选择弹窗

本页面无选择弹窗。

### 导入

本页面无导入功能。

### 其他按钮

本页面无其他按钮。

### 保存校验

- 校验1：照片项目必填 —— 确保照片项目名称完整
  - **详细逻辑**：前端必填校验
  - **系统体现**：C7N内置校验
  - **排查SQL**：`SELECT * FROM MKT_STOREPHOTO_SET_LINE WHERE PHOTO_ITEM IS NULL`

### 提交校验

本页面为设置页面，无提交校验。

### 状态机

本页面为设置页面，无状态机。

### 工作流

本页面无工作流。

## 数据库表详解

### MKT_STOREPHOTO_SET_HEAD（门店照片设置头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| STOREPHOTO_SET_ID | Long | 照片设置头ID | - | 自增 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |

### MKT_STOREPHOTO_SET_LINE（门店照片设置明细表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| STOREPHOTO_SET_ID | Long | 照片设置头ID | - | 关联头表 |
| PHOTO_ITEM | String | 照片项目名称 | 照片项目 | 用户输入 |
| PHOTO_ITEM_NOTE | String | 照片项目说明 | 照片说明 | 用户输入 |
| SHOOT_REQUIRE | String | 拍摄角度及要求 | 拍摄要求 | 用户输入 |
| IS_YS_PROVIDE | Long | 是否验收提供 | 是否验收提供 | 用户选择 |
| YS_PROVIDE_COUNT | Long | 验收提供数量 | 验收提供数量 | 用户输入 |
| IS_ZX_PROVIDE | Long | 是否装修提供 | 是否装修提供 | 用户选择 |
| ZX_PROVIDE_COUNT | Long | 装修提供数量 | 装修提供数量 | 用户输入 |

## 常见问题FAQ

### Q1：装修申请时无照片项目

**根因**：未配置门店照片设置
**解决方案**：在本页面配置照片项目和拍摄要求

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 无 | - | 独立配置数据 | - |

## 下游影响

- **门店装修申请与进度更新** — 装修申请时获取照片设置，控制装修前后照片上传
- **门店验收与报销单** — 验收时获取照片设置，控制验收照片上传

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 照片设置不存在 | queryMktStorephotoSetLine | 未配置照片设置 | 中 | [查看](#报错1照片设置不存在) |

#### 报错1：照片设置不存在
- **触发条件**：门店装修申请或门店验收报销单加载照片Tab时调用queryMktStorephotoSetLine接口，查询MKT_STOREPHOTO_SET_LINE返回空列表
- **逻辑分析**：门店装修申请需上传装修前后照片，门店验收报销单需上传验收照片，照片项目和拍摄要求由本设置页面配置。若管理员未配置任何照片设置行（MKT_STOREPHOTO_SET_LINE无记录），或按ORGANIZATION_ID关联MKT_STOREPHOTO_SET_HEAD过滤后无匹配明细，查询返回空，装修申请/验收报销单的照片上传区域无项目可展示，用户无法对照拍摄要求上传照片，影响装修进度和验收完整性。该异常为非阻断性提示，需管理员补全照片设置配置。
- **排查SQL**：
  ```sql
  SELECT l.storephoto_set_id    AS 照片设置头ID,
         l.photo_item           AS 照片项目,
         l.photo_item_note      AS 照片说明,
         l.shoot_require        AS 拍摄要求,
         l.is_ys_provide        AS 是否验收提供,
         l.ys_provide_count     AS 验收提供数量,
         l.is_zx_provide        AS 是否装修提供,
         l.zx_provide_count     AS 装修提供数量,
         h.organization_id      AS 组织ID
  FROM   mkt_storephoto_set_line l
  INNER JOIN mkt_storephoto_set_head h
  ON     l.storephoto_set_id = h.storephoto_set_id
  WHERE  h.organization_id = #{当前组织ID};
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
