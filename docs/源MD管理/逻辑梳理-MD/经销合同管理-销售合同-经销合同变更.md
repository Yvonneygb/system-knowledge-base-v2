# 经销合同管理-销售合同-经销合同变更

## 业务流程

### 业务流程图

```text
经销合同变更页面
        │
        ├── 查询变更列表 ──→ GET /sale-contract-add-heads/search
        │
        ├── 查询原合同 ──→ GET /sale-contract-add-heads/oldSaSale
        │
        ├── 新建/编辑 ──→ POST /sale-contract-add-heads/save
        │
        ├── 保存并提交 ──→ POST /sale-contract-add-heads/saveAndSubmit
        │              └── 发起OA审批流程(CONTRACT_ADD_EVENT/CONTRACT_JXHTBG_AW_XS)
        │
        ├── 删除 ──→ POST /sale-contract-add-heads/remove
        │
        └── 导出 ──→ GET /sale-contract-add-heads/selectExportList
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 年度经销合同 | 数据依赖 | 变更基于已审批的年度经销合同 | 合同已审批通过 |
| OA审批系统 | 服务依赖 | 变更需经OA审批 | OA系统可用 |

### 下游影响

- 影响1：年度经销合同
  - 变更审批通过后回写原合同信息

- 影响2：合同任务
  - 变更可能影响合同任务拆分

---

## 重点逻辑

### 重点逻辑1：变更回写原合同 {变更回写}

- **业务意义**：变更审批通过后将变更内容回写到原年度经销合同

- **具体逻辑描述**

  - 第1点：通过oldSaSale接口查询原销售合同信息

  - 第2点：变更审批通过后回写原合同的区域、任务拆分等信息

  - 第3点：支持变更区域(SaleContractAddArea)和变更任务拆分(SaleContractAddTaskSplit)

### 重点逻辑2：OA审批流程 {审批流转}

- **业务意义**：合同变更需经OA审批，确保变更合规

- **具体逻辑描述**

  - 第1点：保存并提交时发起OA审批(CONTRACT_ADD_EVENT或CONTRACT_JXHTBG_AW_XS)

  - 第2点：validBeforeSubmit方法进行提交前校验

---

## 详细逻辑

### 界面模块1：经销合同变更列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 变更单号 | SALE_CONTRACT_ADD_HEAD.ADD_HEAD_NO | 文本框 | 变更申请单号 | 常显 | 自动生成 |
| 原合同编号 | SALE_CONTRACT_ADD_HEAD.ORIGINAL_CONTRACT_NO | 文本框 | 原合同编号 | 常显 | 选择原合同带出 |
| 经销商 | SALE_CONTRACT_ADD_HEAD.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 系统带出 |
| 变更类型 | SALE_CONTRACT_ADD_HEAD.CHANGE_TYPE | 下拉选择框 | 变更类型 | 常显 | 手动选择 |
| H0流程审批状态 | SALE_CONTRACT_ADD_HEAD.HZ_APPROVE_STATUS | 文本框 | 流程审批状态 | 常显 | 流程回调更新 |

### 选择弹窗

#### 弹窗1：原合同选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| contractNo | 合同编号 | 原合同编号 | "HT001" | 已审批通过的年度经销合同 |

> 查询SQL（后端接口oldSaSale）：

```sql
SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, CUSTOMER_NAME, CONTRACT_YEAR
FROM SA_SALE_CONTRACT_HEAD 
WHERE HZ_APPROVE_STATUS = 'APPROVED' AND STATE_PIGEONHOLE IS NOT NULL
```

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新增 | 新建变更申请 | 列表页 | 始终可用 | 打开新建页面 |
| 保存 | 保存变更信息 | 编辑页 | 编辑状态 | 调用save接口 |
| 保存并提交 | 发起OA审批 | 编辑页 | 保存后 | 发起CONTRACT_ADD_EVENT流程 |
| 删除 | 删除变更申请 | 列表页 | 选中未提交记录 | 调用remove接口 |
| 导出 | 导出变更列表 | 列表页 | 有数据时 | 调用selectExportList接口 |

### 保存校验

- 校验1：原合同不能为空 —— 确保变更关联明确合同

  - 详细逻辑

    - 第1点：保存时校验原合同ID不为空

  - 系统体现：toast提醒

  - 排查SQL：

    ```sql
    SELECT * FROM SALE_CONTRACT_ADD_HEAD WHERE ORIGINAL_CONTRACT_ID IS NULL;
    ```

### 提交校验

- 校验1：提交前校验 —— 确保变更数据完整

  - 详细逻辑

    - 第1点：通过validBeforeSubmit方法进行提交前校验

    - 第2点：校验变更区域合法性(checkContract)

  - 系统体现：阻断性报错

  - 排查SQL：

    ```sql
    SELECT * FROM SALE_CONTRACT_ADD_HEAD WHERE ADD_HEAD_ID = #{id} AND ORIGINAL_CONTRACT_ID IS NULL;
    ```

### 状态机

#### 状态机流转图

```text
新建 ──保存──→ 已保存 ──提交──→ 审批中 ──OA审批通过──→ 已审核(回写原合同)
                                │
                                └──OA审批拒绝──→ 已拒绝
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| NEW | 已保存未提交 | 编辑、保存、提交、删除 |
| RUN | OA审批中 | 无（等待审批结果） |
| APPROVED | OA审批通过(已回写) | 查看 |
| REJECTED | OA审批拒绝 | 修改、重新提交 |

---

## 数据库表详解

### 表1：SALE_CONTRACT_ADD_HEAD（经销合同变更头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ADD_HEAD_ID | BIGINT | 主键ID | - | 自增主键 |
| ADD_HEAD_NO | VARCHAR | 变更单号 | 变更单号 | 自动生成 |
| ORIGINAL_CONTRACT_ID | BIGINT | 原合同ID | - | 选择原合同带出 |
| ORIGINAL_CONTRACT_NO | VARCHAR | 原合同编号 | 原合同编号 | 选择原合同带出 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商 | 系统带出 |
| CHANGE_TYPE | VARCHAR | 变更类型 | 变更类型 | 手动选择 |
| HZ_APPROVE_STATUS | VARCHAR | H0流程审批状态 | H0流程审批状态 | 流程回调更新 |

### 表2：SALE_CONTRACT_ADD_AREA（变更区域表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ADD_AREA_ID | BIGINT | 主键ID | - | 自增主键 |
| ADD_HEAD_ID | BIGINT | 关联变更头ID | - | FK → SALE_CONTRACT_ADD_HEAD |

### 表3：SALE_CONTRACT_ADD_TASK_SPLIT（变更任务拆分表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| ADD_TASK_SPLIT_ID | BIGINT | 主键ID | - | 自增主键 |
| ADD_HEAD_ID | BIGINT | 关联变更头ID | - | FK → SALE_CONTRACT_ADD_HEAD |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 原合同不能为空 | 保存时 | 未选择原合同 | toast提醒 | [查看] |
| 原合同未归档 | 选择原合同时 | 原合同未审批通过或未归档 | toast提醒 | [查看] |
| 未找到该记录 | 查询/编辑时 | 变更单据不存在或已被删除 | toast提醒 | [查看] |
| 合同信息不匹配 | 编辑时 | 变更单与原合同关联异常 | toast提醒 | [查看] |
| 不能删除非制单状态的单据 | 删除时 | 仅未提交的单据可删除 | toast提醒 | [查看] |
| 流程编码缺失，请选择流程 | 提交时 | OA流程编码未配置 | 阻断性报错 | [查看] |
| 电子合同签署中，不允许变更 | 提交时 | 原合同电子签章流程未完成 | 阻断性报错 | [查看] |
| 销售合同编码不能为空 | 校验时 | 原合同编码参数缺失 | toast提醒 | [查看] |
| 该合同已有单据在走合同归档，请先归档完再变更 | 提交时 | 原合同存在未完成归档单据 | 阻断性报错 | [查看] |
| 已经存在未审核完的合同变更单 | 提交时 | 同一原合同已有未审核变更单 | 阻断性报错 | [查看] |
| 经销商或者法人不存在 | 校验时 | 经销商或法人主数据缺失 | toast提醒 | [查看] |
| 合同不存在 | 校验时 | 原合同或新合同不存在 | toast提醒 | [查看] |
| 单据信息不合法 | 提交时 | 变更单据状态或数据不合法 | 阻断性报错 | [查看] |
| MBO作废原合同失败 | 审批回调时 | MBO侧作废原合同异常 | toast提醒 | [查看] |
| 网络请求失败 | 全局 | 后端服务不可达或超时 | toast提醒 | [查看] |
| 权限不足，无法操作 | 全局 | 当前用户无对应操作权限 | toast提醒 | [查看] |

#### 报错1：原合同不能为空
- **触发条件**：用户在新建变更申请页未选择原合同直接点击保存
- **逻辑分析**：保存接口sale-contract-add-heads/save在写入SALE_CONTRACT_ADD_HEAD前校验ORIGINAL_CONTRACT_ID非空。原合同是变更的对象，未选择原合同将导致变更内容无回写目标，后续变更区域（SaleContractAddArea）和变更任务拆分（SaleContractAddTaskSplit）也无从关联。校验在Controller层前置拦截，toast提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, ORIGINAL_CONTRACT_ID, ORIGINAL_CONTRACT_NO,
         CUSTOMER_NAME, CHANGE_TYPE, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ORIGINAL_CONTRACT_ID IS NULL OR ORIGINAL_CONTRACT_NO IS NULL;
  ```

#### 报错2：原合同未归档
- **触发条件**：用户在原合同选择弹窗中选择合同，oldSaSale接口校验发现合同未审批通过或未归档
- **逻辑分析**：原合同选择弹窗调用oldSaSale接口查询SA_SALE_CONTRACT_HEAD表，要求HZ_APPROVE_STATUS='APPROVED'（审批通过）且STATE_PIGEONHOLE IS NOT NULL（已归档）。未归档根因有二：(1)合同未审批通过（HZ_APPROVE_STATUS为NEW/RUN/REJECTED），变更需基于已审批合同；(2)合同已审批但未归档（STATE_PIGEONHOLE为空），变更需基于已归档合同。需先完成原合同的审批和归档再发起变更
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, CUSTOMER_NAME, CONTRACT_YEAR,
         HZ_APPROVE_STATUS, STATE_PIGEONHOLE, ACTUAL_PIGEONHOLE_DATE
  FROM SA_SALE_CONTRACT_HEAD
  WHERE CONTRACT_NO = #{contractNo}
    AND (HZ_APPROVE_STATUS != 'APPROVED' OR STATE_PIGEONHOLE IS NULL);
  ```

#### 报错3：未找到该记录
- **触发条件**：用户编辑或查看变更申请时，根据ADD_HEAD_ID查询SALE_CONTRACT_ADD_HEAD返回空
- **逻辑分析**：SaleContractAddHeadServiceImpl在doSelect、doUpdate等操作前根据主键查询变更单，若记录不存在或已被删除将抛出此异常。根因有三类：(1)变更单已被其他用户删除；(2)ADD_HEAD_ID传入错误；(3)数据权限隔离导致当前用户不可见。需确认变更单存在且当前用户有权限
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, ORIGINAL_CONTRACT_NO, CUSTOMER_NAME,
         CHANGE_TYPE, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ADD_HEAD_ID = #{addHeadId};
  ```

#### 报错4：合同信息不匹配
- **触发条件**：用户编辑变更申请时，变更单关联的原合同信息与实际原合同不一致
- **逻辑分析**：SaleContractAddHeadServiceImpl校验变更单（SALE_CONTRACT_ADD_HEAD）关联的原合同（ORIGINAL_CONTRACT_ID）与SA_SALE_CONTRACT_HEAD中的实际记录是否一致。不一致根因有二：(1)原合同已被变更或删除；(2)变更单ORIGINAL_CONTRACT_ID指向错误。需重新选择原合同
- **排查SQL**：
  ```sql
  SELECT A.ADD_HEAD_ID, A.ADD_HEAD_NO, A.ORIGINAL_CONTRACT_ID, A.ORIGINAL_CONTRACT_NO,
         S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD A
  LEFT JOIN SA_SALE_CONTRACT_HEAD S ON A.ORIGINAL_CONTRACT_ID = S.SALE_CONTRACT_HEAD_ID
  WHERE A.ORIGINAL_CONTRACT_ID IS NOT NULL
    AND (S.SALE_CONTRACT_HEAD_ID IS NULL OR A.ORIGINAL_CONTRACT_NO != S.CONTRACT_NO);
  ```

#### 报错5：不能删除非制单状态的单据
- **触发条件**：用户选中已提交或已审批的变更单点击"删除"按钮
- **逻辑分析**：SaleContractAddHeadServiceImpl在remove方法中校验变更单状态，仅HZ_APPROVE_STATUS='NEW'（制单状态）的单据可删除。已提交（RUN）或已审批（APPROVED）的单据已进入OA流程或已回写原合同，删除将导致流程数据不一致。需先撤回OA流程再删除
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, ORIGINAL_CONTRACT_NO, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ADD_HEAD_ID = #{addHeadId}
    AND HZ_APPROVE_STATUS != 'NEW';
  ```

#### 报错6：流程编码缺失，请选择流程
- **触发条件**：用户点击"保存并提交"，校验OA流程编码（CONTRACT_ADD_EVENT或CONTRACT_JXHTBG_AW_XS）为空
- **逻辑分析**：SaleContractAddHeadServiceImpl在saveAndSubmit中校验流程编码非空。流程编码缺失将导致OA流程无法启动。根因有二：(1)系统未配置CONTRACT_ADD_EVENT或CONTRACT_JXHTBG_AW_XS流程编码；(2)变更类型未关联对应流程编码。需在流程配置中维护对应关系
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, CHANGE_TYPE, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ADD_HEAD_ID = #{addHeadId}
    AND HZ_APPROVE_STATUS = 'NEW';
  ```

#### 报错7：电子合同签署中，不允许变更
- **触发条件**：用户对原合同发起变更申请时，原合同正处于电子签章流程中
- **逻辑分析**：SaleContractAddHeadServiceImpl校验原合同的电子签章状态，若原合同正在电子签章流程中（签署中状态），不允许发起变更。变更将导致原合同内容变化，电子签章数据失效。需先完成或中止原合同电子签章流程再发起变更
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.HZ_APPROVE_STATUS,
         S.ELECTRONIC_SIGN_STATUS, S.STATE_PIGEONHOLE
  FROM SA_SALE_CONTRACT_HEAD S
  WHERE S.SALE_CONTRACT_HEAD_ID = #{originalContractId}
    AND S.ELECTRONIC_SIGN_STATUS = 'SIGNING';
  ```

#### 报错8：销售合同编码不能为空
- **触发条件**：校验变更申请时，原合同编码（ORIGINAL_CONTRACT_NO或SA_CONTR_HEAD_CODE）参数为空
- **逻辑分析**：SaleContractAddHeadServiceImpl在多处校验销售合同编码非空。合同编码是关联原合同的关键字段，为空将导致原合同信息无法带出，变更回写无目标。需前端正确传入原合同编码
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, ORIGINAL_CONTRACT_ID, ORIGINAL_CONTRACT_NO,
         CHANGE_TYPE, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ORIGINAL_CONTRACT_NO IS NULL OR ORIGINAL_CONTRACT_NO = '';
  ```

#### 报错9：该合同已有单据在走合同归档，请先归档完再变更
- **触发条件**：用户对原合同发起变更申请时，原合同存在未完成的归档单据
- **逻辑分析**：SaleContractAddHeadServiceImpl校验原合同是否存在归档流程中的单据。若原合同有单据正在归档流程中，发起变更将导致归档单据与变更后合同数据不一致。需先完成原合同相关单据的归档流程再发起变更
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.HZ_APPROVE_STATUS,
         S.STATE_PIGEONHOLE, S.IS_PIGEONHOLE
  FROM SA_SALE_CONTRACT_HEAD S
  WHERE S.SALE_CONTRACT_HEAD_ID = #{originalContractId}
    AND S.IS_PIGEONHOLE = 1
    AND S.STATE_PIGEONHOLE IS NULL;
  ```

#### 报错10：已经存在未审核完的合同变更单
- **触发条件**：用户对原合同发起变更申请时，同一原合同已存在未审核完成的变更单
- **逻辑分析**：SaleContractAddHeadServiceImpl校验同一原合同（ORIGINAL_CONTRACT_ID）不允许存在多个未审核完成的变更单（HZ_APPROVE_STATUS为NEW或RUN）。重复发起变更将导致变更内容冲突，回写原合同时数据不一致。需先完成或中止已有变更单再发起新变更
- **排查SQL**：
  ```sql
  SELECT ORIGINAL_CONTRACT_ID, ORIGINAL_CONTRACT_NO,
         ADD_HEAD_ID, ADD_HEAD_NO, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ORIGINAL_CONTRACT_ID = #{originalContractId}
    AND HZ_APPROVE_STATUS IN ('NEW', 'RUN');
  ```

#### 报错11：经销商或者法人不存在
- **触发条件**：变更申请校验时，经销商或法人主数据查询返回空
- **逻辑分析**：SaleContractAddHeadServiceImpl根据经销商ID查询经销商和法人主数据，若经销商已被删除或法人关联未配置将抛出此异常。经销商和法人是变更回写和CRM推送的关键主体。需确认经销商存在且已配置法人关联
- **排查SQL**：
  ```sql
  SELECT A.ADD_HEAD_ID, A.CUSTOMER_NAME, C.CUSTOMER_ID, C.CORPORATE_CODE,
         C.CORPORATE_NAME
  FROM SALE_CONTRACT_ADD_HEAD A
  LEFT JOIN CUSTOMER C ON A.CUSTOMER_ID = C.CUSTOMER_ID
  WHERE C.CUSTOMER_ID IS NULL OR C.CORPORATE_CODE IS NULL;
  ```

#### 报错12：合同不存在
- **触发条件**：变更申请校验或回写时，原合同或新合同在SA_SALE_CONTRACT_HEAD中查询不到
- **逻辑分析**：SaleContractAddHeadServiceImpl在多处校验合同存在性。合同不存在根因有三类：(1)合同已被删除；(2)合同ID传入错误；(3)数据同步延迟。需确认合同存在且已同步
- **排查SQL**：
  ```sql
  SELECT A.ADD_HEAD_ID, A.ORIGINAL_CONTRACT_ID, A.ORIGINAL_CONTRACT_NO,
         S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO
  FROM SALE_CONTRACT_ADD_HEAD A
  LEFT JOIN SA_SALE_CONTRACT_HEAD S ON A.ORIGINAL_CONTRACT_ID = S.SALE_CONTRACT_HEAD_ID
  WHERE A.ORIGINAL_CONTRACT_ID IS NOT NULL AND S.SALE_CONTRACT_HEAD_ID IS NULL;
  ```

#### 报错13：单据信息不合法
- **触发条件**：变更申请提交或审批回调时，变更单据状态或数据不满足操作前提
- **逻辑分析**：SaleContractAddHeadServiceImpl在多处校验单据合法性。不合法根因有多类：(1)单据状态非预期（如已审批单据再次提交）；(2)必填字段缺失；(3)关联数据异常。需核查变更单状态和完整数据
- **排查SQL**：
  ```sql
  SELECT ADD_HEAD_ID, ADD_HEAD_NO, ORIGINAL_CONTRACT_ID, ORIGINAL_CONTRACT_NO,
         CUSTOMER_NAME, CHANGE_TYPE, HZ_APPROVE_STATUS
  FROM SALE_CONTRACT_ADD_HEAD
  WHERE ADD_HEAD_ID = #{addHeadId};
  ```

#### 报错14：MBO作废原合同失败
- **触发条件**：变更审批通过后回写原合同，调用MBO接口作废原合同返回失败
- **逻辑分析**：SaleContractAddHeadServiceImpl在变更审批通过后调用MBO接口作废原合同（变更回写场景）。MBO作废失败根因有三类：(1)MBO系统不可用；(2)原合同在MBO侧状态不允许作废；(3)MBO接口认证失败。需联系MBO管理员核查
- **排查SQL**：
  ```sql
  SELECT A.ADD_HEAD_ID, A.ADD_HEAD_NO, A.HZ_APPROVE_STATUS,
         S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.HZ_APPROVE_STATUS AS 原合同状态
  FROM SALE_CONTRACT_ADD_HEAD A
  JOIN SA_SALE_CONTRACT_HEAD S ON A.ORIGINAL_CONTRACT_ID = S.SALE_CONTRACT_HEAD_ID
  WHERE A.HZ_APPROVE_STATUS = 'APPROVED';
  ```

#### 报错15：网络请求失败
- **触发条件**：前端调用sale-contract-add-heads相关接口时，后端服务不可达或请求超时
- **逻辑分析**：前端通过axios调用AE_BUSINESS服务，网络异常、服务宕机、网关超时均会触发。前端拦截器统一捕获并toast提示。需检查AE_BUSINESS服务状态、网络连通性、网关配置
- **排查SQL**：
  ```sql
  SELECT '网络层异常，无SQL排查' AS 提示 FROM DUAL;
  ```

#### 报错16：权限不足，无法操作
- **触发条件**：当前用户对变更保存、提交、删除等操作无对应功能权限或数据权限
- **逻辑分析**：后端通过权限注解校验用户角色，前端通过菜单和按钮权限控制显隐。用户无权限时后端返回403，前端拦截器toast提示。需在权限管理中为用户分配对应角色
- **排查SQL**：
  ```sql
  SELECT '权限层异常，请核查用户角色配置' AS 提示 FROM DUAL;
  ```

### 常见问题

- 问题1：变更未回写原合同
  - 原因：变更审批未通过或回写逻辑异常
  - 解决思路：检查SQL `SELECT HZ_APPROVE_STATUS FROM SALE_CONTRACT_ADD_HEAD WHERE ADD_HEAD_ID = #{id}`，确认状态为APPROVED

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
