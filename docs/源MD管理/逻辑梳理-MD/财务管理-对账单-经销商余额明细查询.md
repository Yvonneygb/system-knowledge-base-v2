# 财务管理-对账单-经销商余额明细查询

## 业务流程

### 业务流程图

```text
经销商余额明细查询页面(hlod低代码页面)
        │
        ├── 查询余额明细 ──→ GET /inlimit-balance-header/query（按经销商/事业部/年月查询）
        │                      └── 数据来源：MKT_INLIMIT_BALANCE_DETAILS表
        │                          （由定时任务AdvertDetailJob定期生成）
        │
        └── 导出 ──→ GET /inlimit-balance-header/exportBalance
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 定时任务AdvertDetailJob | 数据依赖 | 明细数据由定时任务定期生成 | 定时任务已配置并运行 |
| 广告费余额查询 | 数据依赖 | 余额头表数据来源 | 余额数据已生成 |
| 经销商主数据 | 数据依赖 | 按经销商维度查询 | 经销商已创建 |

### 下游影响

- 影响1：财务对账
  - 经销商余额明细为财务对账提供明细级别核对依据

---

## 重点逻辑

### 重点逻辑1：多来源明细汇总 {数据汇总}

- **业务意义**：经销商余额明细由多个来源汇总计算，全面反映余额变动

- **具体逻辑描述**

  - 第1点：定时任务AdvertDetailJob定期调用executeDetailLine方法生成明细数据

  - 第2点：明细数据汇总多个来源：扣减金额、额度内兑现、装修申请兑现、到期调整、其他调整、出库占用、门头额度内兑现

  - 第3点：每个来源独立记录，便于追溯余额变动原因

### 重点逻辑2：定时任务生成 {数据生成}

- **业务意义**：明细数据通过定时任务定期生成，确保数据及时更新

- **具体逻辑描述**

  - 第1点：AdvertDetailJob定时任务按配置的时间间隔执行

  - 第2点：调用getDetailDate获取指定时间范围内的明细数据

  - 第3点：调用executeDetailLine执行明细行生成，汇总各来源金额

---

## 详细逻辑

### 界面模块1：经销商余额明细查询列表页（hlod低代码页面）

> 本页面为hlod低代码页面，无独立前端源码，基于后端Entity和API梳理。明细数据由定时任务AdvertDetailJob定期生成。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 经销商编码 | MKT_INLIMIT_BALANCE_DETAILS.CUSTOMER_CODE | 文本框 | 经销商编码 | 常显 | 选择经销商带出 |
| 经销商名称 | MKT_INLIMIT_BALANCE_DETAILS.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 事业部 | MKT_INLIMIT_BALANCE_DETAILS.ENTNAME | 文本框 | 事业部名称 | 常显 | 选择事业部带出 |
| 订单类型 | MKT_INLIMIT_BALANCE_DETAILS.ORDER_TYPE | 下拉选择框 | 余额变动类型 | 常显 | 系统带出 |
| 来源系统 | MKT_INLIMIT_BALANCE_DETAILS.SOURCE_SYSTEM | 文本框 | 数据来源系统 | 常显 | 系统带出 |
| 订单编号 | MKT_INLIMIT_BALANCE_DETAILS.ORDER_NUMBER | 文本框 | 关联订单编号 | 常显 | 系统带出 |
| 状态 | MKT_INLIMIT_BALANCE_DETAILS.STATUS | 文本框 | 明细状态 | 常显 | 系统带出 |
| 金额 | MKT_INLIMIT_BALANCE_DETAILS.AMOUNT | 数字显示框 | 变动金额 | 常显 | 系统计算 |
| 发生时间 | MKT_INLIMIT_BALANCE_DETAILS.CREATE_TIME | 日期显示框 | 变动发生时间 | 常显 | 系统带出 |

### 选择弹窗

#### 弹窗1：经销商选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| customerCode | 经销商编码 | 经销商编码 | "C001" | 当前用户有权限的经销商 |

> 查询SQL（经销商主数据）：

```sql
SELECT CUSTOMER_CODE, CUSTOMER_NAME FROM CUSTOMER_ORG WHERE ENABLED = 1
```

### 导入

> 本页面为查询页面，无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询余额明细 | 列表页 | 始终可用 | 调用query接口 |
| 导出 | 导出查询结果 | 列表页 | 有查询结果时 | 导出Excel |

#### 按钮1：查询（列表页）

- **触发条件**：始终可用
- **执行逻辑**：
  - 第1点：按经销商、事业部、年月等条件查询余额明细
  - 第2点：返回各来源的余额变动明细记录
- **接口调用**：GET `/v1/{organizationId}/inlimit-balance-header/query`
- **排查SQL**：

```sql
SELECT * FROM MKT_INLIMIT_BALANCE_DETAILS 
WHERE (CUSTOMER_CODE = #{customerCode} OR #{customerCode} IS NULL)
  AND (ENTID = #{entid} OR #{entid} IS NULL)
  AND CREATE_TIME BETWEEN #{startTime} AND #{endTime}
ORDER BY CREATE_TIME DESC
```

#### 按钮2：导出（列表页）

- **触发条件**：有查询结果时可用
- **执行逻辑**：
  - 第1点：将当前查询结果导出为Excel文件
- **接口调用**：GET `/v1/{organizationId}/inlimit-balance-header/exportBalance`
- **排查SQL**：同查询SQL

### 保存校验

> 本页面为纯查询页面，无保存操作。

### 提交校验

> 本页面为纯查询页面，无提交操作。

### 状态机

> 本页面为纯查询页面，无状态流转。

---

## 数据库表详解

### 表1：MKT_INLIMIT_BALANCE_DETAILS（广告费余额明细表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| INLIMIT_BALANCE_DETAILS_ID | BIGINT | 主键ID | - | 自增主键 |
| CUSTOMER_CODE | VARCHAR | 经销商编码 | 经销商编码 | 关联经销商 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商名称 | 关联经销商 |
| ENTID | BIGINT | 事业部ID | - | 关联事业部 |
| ENTNAME | VARCHAR | 事业部名称 | 事业部 | 关联事业部 |
| ORDER_TYPE | VARCHAR | 订单类型 | 订单类型 | 余额变动类型（扣减/兑现/调整/占用等） |
| SOURCE_SYSTEM | VARCHAR | 来源系统 | 来源系统 | 数据来源系统标识 |
| ORDER_NUMBER | VARCHAR | 订单编号 | 订单编号 | 关联订单编号 |
| STATUS | VARCHAR | 状态 | 状态 | 明细记录状态 |
| AMOUNT | DECIMAL | 金额 | 金额 | 变动金额，正数增加/负数减少 |
| CREATE_TIME | DATETIME | 创建时间 | 发生时间 | 定时任务生成时间 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 定时任务未执行或明细数据未生成，确认AdvertDetailJob已运行 | toast提醒 | [查看] |
| 网络请求失败 | 查询/导出时 | 后端服务不可用或接口超时，确认ae-business服务正常 | toast提醒 | [查看] |
| 权限不足 | 进入页面时 | 当前用户无该经销商/事业部数据权限，联系管理员分配权限 | 阻断性报错 | [查看] |
| 年月查询条件为空 | 查询时 | 未选择年月区间，选择后查询 | toast提醒 | [查看] |
| 导出失败 | 导出时 | 查询结果集过大或服务异常，缩小查询范围后重试 | toast提醒 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按经销商/事业部/年月查询余额明细，MKT_INLIMIT_BALANCE_DETAILS表返回空结果集
- **逻辑分析**：明细数据由定时任务AdvertDetailJob定期调用executeDetailLine方法生成，汇总扣减金额、额度内兑现、装修申请兑现、到期调整、其他调整、出库占用、门头额度内兑现7个来源。无数据根因有三类：(1)定时任务AdvertDetailJob未配置或未启动，MKT_INLIMIT_BALANCE_DETAILS表为空；(2)查询的年月区间（CREATE_TIME BETWEEN startTime AND endTime）内无明细记录；(3)指定经销商/事业部在区间内无余额变动。需先确认定时任务执行日志，再核查表中是否有该经销商任何数据
- **排查SQL**：
  ```sql
  SELECT INLIMIT_BALANCE_DETAILS_ID, CUSTOMER_CODE, CUSTOMER_NAME, ENTNAME,
         ORDER_TYPE, SOURCE_SYSTEM, ORDER_NUMBER, AMOUNT, CREATE_TIME
  FROM MKT_INLIMIT_BALANCE_DETAILS
  WHERE (CUSTOMER_CODE = #{customerCode} OR #{customerCode} IS NULL)
    AND (ENTID = #{entid} OR #{entid} IS NULL)
    AND CREATE_TIME BETWEEN #{startTime} AND #{endTime}
  ORDER BY CREATE_TIME DESC;
  ```

#### 报错2：网络请求失败
- **触发条件**：用户点击查询或导出按钮，前端调用GET /v1/{organizationId}/inlimit-balance-header/query或/exportBalance接口返回非2xx状态码或超时
- **逻辑分析**：本页面为hlod低代码页面，数据通过后端MktInlimitBalanceHeaderService提供。网络请求失败根因有四类：(1)ae-business服务未启动或宕机，接口无法访问；(2)数据库连接异常，MKT_INLIMIT_BALANCE_DETAILS表查询超时；(3)查询条件导致SQL执行计划退化（如未带索引的全表扫描）；(4)网关或网络层故障。需先确认ae-business服务健康状态，再核查接口响应时间
- **排查SQL**：
  ```sql
  -- 核查表数据量是否异常增长导致查询超时
  SELECT COUNT(*) AS 总记录数, MIN(CREATE_TIME) AS 最早时间, MAX(CREATE_TIME) AS 最晚时间
  FROM MKT_INLIMIT_BALANCE_DETAILS;
  ```

#### 报错3：权限不足
- **触发条件**：用户登录后进入经销商余额明细查询页面，当前用户无该经销商或事业部的数据权限
- **逻辑分析**：本页面按经销商和事业部维度查询，数据权限通过用户上下文CustomUserDetails的additionInfo控制可见经销商范围。权限不足根因有二：(1)用户未分配对应经销商的数据权限，查询时自动过滤导致空结果；(2)用户未分配菜单访问权限，页面入口不可见。需联系管理员在权限系统中分配对应经销商/事业部数据权限
- **排查SQL**：
  ```sql
  -- 核查用户是否有该经销商的数据权限（具体权限表视系统配置而定）
  SELECT USER_ID, USER_NAME, CUSTOMER_CODE, ENABLED
  FROM USER_CUSTOMER_AUTH
  WHERE USER_ID = #{userId} AND CUSTOMER_CODE = #{customerCode};
  ```

#### 报错4：年月查询条件为空
- **触发条件**：用户未选择年月区间直接点击查询
- **逻辑分析**：年月区间是查询余额明细的关键条件，CREATE_TIME BETWEEN startTime AND endTime用于限定明细记录的时间范围。未选择年月将导致查询全表数据，可能因数据量过大引起超时或返回无关数据。低代码页面查询栏配置年月为建议必填条件，未填写时toast提示后阻断查询
- **排查SQL**：
  ```sql
  -- 核查未带时间条件的全表数据量
  SELECT COUNT(*) AS 全表记录数
  FROM MKT_INLIMIT_BALANCE_DETAILS
  WHERE CUSTOMER_CODE = #{customerCode};
  ```

#### 报错5：导出失败
- **触发条件**：用户点击"导出"按钮，GET /v1/{organizationId}/inlimit-balance-header/exportBalance接口执行失败
- **逻辑分析**：导出接口基于当前查询条件导出MKT_INLIMIT_BALANCE_DETAILS数据为Excel。导出失败根因有三类：(1)查询结果集过大，超过导出限制（如单次导出超过10万行）；(2)服务内存不足，Excel生成时OOM；(3)查询条件未先执行，导出空结果。需缩小查询范围（限定经销商+年月）后重试
- **排查SQL**：
  ```sql
  -- 核查导出数据量是否超限
  SELECT COUNT(*) AS 待导出行数
  FROM MKT_INLIMIT_BALANCE_DETAILS
  WHERE (CUSTOMER_CODE = #{customerCode} OR #{customerCode} IS NULL)
    AND (ENTID = #{entid} OR #{entid} IS NULL)
    AND CREATE_TIME BETWEEN #{startTime} AND #{endTime};
  ```

### 常见问题

- 问题1：明细数据未更新
  - 原因：定时任务AdvertDetailJob未执行或执行失败
  - 解决思路：检查定时任务配置和执行日志，确认executeDetailLine方法是否正常执行

- 问题2：明细金额与余额不一致
  - 原因：定时任务执行时部分来源数据未同步
  - 解决思路：检查SQL `SELECT ORDER_TYPE, SUM(AMOUNT) FROM MKT_INLIMIT_BALANCE_DETAILS WHERE CUSTOMER_CODE = #{customerCode} GROUP BY ORDER_TYPE`，核对各来源汇总

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充界面模块、选择弹窗、数据库表详解 |
