# 门店管理-装修报表-兑现汇总

## 业务流程

### 业务流程图

```text
选择查询条件      查询报表数据        展示汇总结果
(年度/经销商/  →   调用报表查询  →    分页展示兑现汇总
 门店/时间范围)    接口
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 发票兑现数据 | 数据依赖 | 提供兑现记录、兑现金额、兑现类型 | 兑现单已存在 |
| 门店档案 | 数据依赖 | 提供G门店信息 | 门店已建档 |

### 下游影响

- 影响1：无
  - 报表仅查询展示，不产生数据变更

---

## 重点逻辑

### 重点逻辑1：兑现数据汇总统计 {数据汇总}

- **业务意义**：按多维度汇总兑现数据，支持管理层决策

- **具体逻辑描述**

  - 第1点：按年度、经销商、门店、兑现类型等维度汇总

  - 第2点：统计可兑现总额、已兑现金额、剩余可兑现金额

---

## 详细逻辑

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

### 界面模块1：兑现汇总报表查询页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 年度 | - | 下拉选择框 | 查询年度 | 常显 | 默认当前年度 |
| 经销商名称 | - | 文本框 | 查询经销商 | 常显 | 可输入筛选 |
| 门店名称 | - | 文本框 | 查询门店 | 常显 | 可输入筛选 |
| 兑现类型 | - | 下拉选择框 | 额度内/额度外 | 常显 | 1=额度内，2=额度外 |
| 时间范围 | - | 日期范围选择框 | 查询时间范围 | 常显 | 可选择起止日期 |

### 界面模块2：兑现汇总报表结果列表

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 经销商名称 | - | 文本框 | 经销商名称 | 常显 | 查询结果 |
| 门店名称 | - | 文本框 | 门店名称 | 常显 | 查询结果 |
| 兑现单号 | - | 文本框 | 兑现单号 | 常显 | 查询结果 |
| 报销单号 | - | 文本框 | 关联报销单号 | 常显 | 查询结果 |
| 兑现类型 | - | 文本框 | 额度内/额度外 | 常显 | 查询结果 |
| 可兑现总额 | - | 文本框 | 可兑现总额 | 常显 | 查询结果 |
| 已兑现金额 | - | 文本框 | 已兑现金额 | 常显 | 查询结果 |
| 本次兑现金额 | - | 文本框 | 本次兑现金额 | 常显 | 查询结果 |
| 审批状态 | - | 文本框 | 审批状态 | 常显 | 查询结果 |

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 查询报表数据 | 查询页 | 常显 | 调用search接口 |
| 重置 | 重置查询条件 | 查询页 | 常显 | 清空查询条件 |

#### 按钮1：查询（查询页）

- **触发条件**：常显
- **执行逻辑**：
  - 第1点：按查询条件调用报表查询接口
  - 第2点：分页返回兑现汇总数据
- **接口调用**：POST /v1/{organizationId}/terminalReport/fin-fee-cashout-summary/search
- **排查SQL**：

```sql
SELECT * FROM fin_fee_cashout_header WHERE 1=1
  AND (#{budYear} IS NULL OR bud_year = #{budYear})
  AND (#{custName} IS NULL OR cust_name LIKE '%'||#{custName}||'%')
  AND (#{terminalName} IS NULL OR terminal_name LIKE '%'||#{terminalName}||'%')
  AND (#{cashoutType} IS NULL OR cashout_type = #{cashoutType})
  AND (#{startDate} IS NULL OR create_time >= #{startDate})
  AND (#{endDate} IS NULL OR create_time <= #{endDate})
ORDER BY create_time DESC;
```

---

## 数据库表详解

### 相关表：FIN_FEE_CASHOUT_HEADER（报销发票兑现主表-查询来源）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| FEE_CASHOUT_NO | VARCHAR2 | 兑现单号 | 兑现单号 | 查询来源 |
| BX_NO | VARCHAR2 | 报销单号 | 报销单号 | 查询来源 |
| CASHOUT_TYPE | NUMBER | 兑现类型 | 兑现类型 | 1=额度内，2=额度外 |
| TOTAL_CAN_CASHOUT_AMT | DECIMAL | 可兑现总额 | 可兑现总额 | 查询来源 |
| THIS_CASHOUT_AMT | DECIMAL | 本次兑现金额 | 本次兑现金额 | 查询来源 |
| CUST_NAME | VARCHAR2 | 经销商名称 | 经销商名称 | 查询来源 |
| TERMINAL_NAME | VARCHAR2 | 门店名称 | 门店名称 | 查询来源 |
| HZ_APPROVE_STATUS | VARCHAR2 | 审批状态 | 审批状态 | 查询来源 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询年度不能为空 | 查询 | 未选择查询年度，选择年度后查询 | 阻断性报错 | [查看](#报错1查询年度不能为空) |
| 查询时间范围异常 | 查询 | 起始日期大于结束日期，调整时间范围 | 阻断性报错 | [查看](#报错2查询时间范围异常) |
| 兑现类型异常 | 查询 | 兑现类型非1或2，重新选择兑现类型 | 阻断性报错 | [查看](#报错3兑现类型异常) |
| 网络请求失败/接口调用异常 | 查询 | 后端接口调用失败，检查网络连接或后端服务状态 | 阻断性报错 | [查看](#报错4网络请求失败接口调用异常) |
| 权限不足/未登录 | 页面加载/查询 | 当前用户无组织级权限或登录态失效，重新登录或联系管理员分配权限 | 阻断性报错 | [查看](#报错5权限不足未登录) |

#### 报错1：查询年度不能为空
- **触发条件**：点击"查询"按钮，调用search接口时，传入的查询年度budYear参数为null或空字符串
- **逻辑分析**：兑现汇总报表按年度维度统计数据，年度是核心查询条件。校验逻辑检查入参budYear，为空则抛异常阻止查询。常见根因：用户未选择年度、年度下拉框未默认赋值当前年度、或前端传参丢失。
- **排查SQL**：
  ```sql
  SELECT COUNT(*)            AS 兑现单数量
  FROM   fin_fee_cashout_header
  WHERE  bud_year IS NULL
  AND    hz_approve_status = 'APPROVED';
  ```

#### 报错2：查询时间范围异常
- **触发条件**：点击"查询"按钮，调用search接口时，传入的起始日期startDate>结束日期endDate
- **逻辑分析**：时间范围查询要求起始日期不晚于结束日期。校验逻辑检查入参startDate与endDate，当startDate>endDate则抛异常阻止查询。常见根因：用户手动选择日期范围错误、日期选择组件未做范围限制、或前端传参顺序颠倒。
- **排查SQL**：
  ```sql
  SELECT fee_cashout_id      AS 兑现单ID,
         fee_cashout_no      AS 兑现单号,
         create_time         AS 创建时间,
         hz_approve_status   AS 审批状态
  FROM   fin_fee_cashout_header
  WHERE  hz_approve_status = 'APPROVED'
  AND    create_time BETWEEN #{startDate} AND #{endDate}
  AND    #{startDate} > #{endDate}
  ORDER  BY create_time DESC;
  ```

#### 报错3：兑现类型异常
- **触发条件**：点击"查询"按钮，调用search接口时，传入的兑现类型cashoutType既非1（额度内）也非2（额度外）且非空
- **逻辑分析**：兑现类型仅支持额度内(1)和额度外(2)两种，传入其他值会导致查询条件异常。校验逻辑检查入参cashoutType，非空且不在(1,2)范围内则抛异常。常见根因：前端下拉框值集配置错误、传参被篡改、或数据迁移导致脏数据。
- **排查SQL**：
  ```sql
  SELECT fee_cashout_id      AS 兑现单ID,
         fee_cashout_no      AS 兑现单号,
         cashout_type        AS 兑现类型,
         hz_approve_status   AS 审批状态
  FROM   fin_fee_cashout_header
  WHERE  cashout_type IS NOT NULL
  AND    cashout_type NOT IN (1, 2)
  ORDER  BY create_time DESC;
  ```

#### 报错4：网络请求失败/接口调用异常
- **触发条件**：点击"查询"按钮，调用POST /v1/{organizationId}/terminalReport/fin-fee-cashout-summary/search接口时，前端未收到响应或收到非2xx状态码（如500、502、504）
- **逻辑分析**：本页面为hlod低代码报表页面，查询依赖后端TerminalReportController.finFeeCashoutSummarySearch接口分页查询FIN_FEE_CASHOUT_HEADER。若后端服务未启动、数据库连接异常、SQL执行超时（如全表扫描无索引）、网络中断、或反向代理（网关）转发失败，均会导致接口调用异常。常见根因：后端ae-report服务宕机、Oracle数据库连接池耗尽、查询条件过宽导致慢SQL、或网络抖动。需检查后端服务健康状态、数据库连接、网络连通性。
- **排查SQL**：
  ```sql
  SELECT COUNT(*)            AS 兑现单总数,
         MIN(create_time)    AS 最早创建时间,
         MAX(create_time)    AS 最晚创建时间
  FROM   fin_fee_cashout_header
  WHERE  hz_approve_status = 'APPROVED';
  ```

#### 报错5：权限不足/未登录
- **触发条件**：页面加载或点击"查询"按钮时，接口返回401未授权或403禁止访问，或前端路由守卫拦截
- **逻辑分析**：本报表接口声明@Permission(level = ResourceLevel.ORGANIZATION)，要求用户具备组织级权限。若用户未登录（token过期/丢失）、或当前角色未分配该报表菜单权限、或organizationId路径参数与用户所属组织不匹配，均会触发权限校验失败。hlod低代码页面通过路由配置和接口权限双重校验，任一环节失败均阻断访问。需重新登录或联系管理员分配报表查看权限。
- **排查SQL**：
  ```sql
  SELECT '权限校验为应用层逻辑，无对应数据表' AS 提示
  FROM   dual;
  ```

### 常见问题

- 问题1：报表数据为空
  - 原因：查询条件过窄或无符合条件的兑现数据
  - 解决思路：放宽查询条件，检查是否有已审批通过的兑现单

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | - | 按skill规范重写知识库文档 |
| 2025-12-10 | - | HZERO | 初始创建报表功能 |
