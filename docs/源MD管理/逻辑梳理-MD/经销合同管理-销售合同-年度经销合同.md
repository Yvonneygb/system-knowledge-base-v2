# 经销合同管理-销售合同-年度经销合同

## 业务流程

### 业务流程图

```text
年度经销合同页面
        │
        ├── 查询列表 ──→ GET /sa-sale-contract-heads/search
        │
        ├── 新建/编辑 ──→ POST /sa-sale-contract-heads/save
        │
        ├── 保存并提交 ──→ POST /sa-sale-contract-heads/saveAndSubmit
        │              └── 发起OA审批流程(DISTRIBUTION_CONTRACT_DKHB)
        │
        ├── 删除 ──→ POST /sa-sale-contract-heads/remove
        │
        ├── 上合同完成情况 ──→ POST /sa-sale-contract-heads/deliver
        │
        └── 导出 ──→ GET /sa-sale-contract-heads/selectExportList
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 经销商主数据 | 数据依赖 | 合同关联经销商 | 经销商已创建 |
| 年度营销政策 | 数据依赖 | 合同关联营销政策 | 政策已生效 |
| OA审批系统 | 服务依赖 | 合同需经OA审批 | OA系统可用 |

### 下游影响

- 影响1：合同归档
  - 合同审批通过后可进行归档操作(菜单111)

- 影响2：经销合同变更
  - 已审批合同可通过变更流程修改(菜单116)

- 影响3：保证金管理
  - 合同关联保证金缴纳和认缴

---

## 重点逻辑

### 重点逻辑1：OA审批流程 {审批流转}

- **业务意义**：年度经销合同需经OA审批，确保合同合规

- **具体逻辑描述**

  - 第1点：保存并提交时通过workFlowStart发起OA审批(DISTRIBUTION_CONTRACT_DKHB)

  - 第2点：workFlowStartValid方法进行流程启动前校验

  - 第3点：审批通过后合同生效，可进行归档和变更

### 重点逻辑2：合同区域校验 {区域校验}

- **业务意义**：合同需校验销售区域合法性

- **具体逻辑描述**

  - 第1点：通过doCheckAreaNew/doCheckArea方法校验区域

  - 第2点：校验经销商在选定区域内的合法性

---

## 详细逻辑

### 界面模块1：年度经销合同列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 合同编号 | SA_SALE_CONTRACT_HEAD.CONTRACT_NO | 文本框 | 合同编号 | 常显 | 自动生成 |
| 经销商 | SA_SALE_CONTRACT_HEAD.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 合同年度 | SA_SALE_CONTRACT_HEAD.CONTRACT_YEAR | 文本框 | 合同年度 | 常显 | 手动选择 |
| 事业部 | SA_SALE_CONTRACT_HEAD.ENTNAME | 文本框 | 事业部名称 | 常显 | 选择事业部带出 |
| 交易公司 | SA_SALE_CONTRACT_HEAD.TRADING_COMPANY_NAME | 文本框 | 交易公司名称 | 常显 | 选择交易公司带出 |
| H0流程审批状态 | SA_SALE_CONTRACT_HEAD.HZ_APPROVE_STATUS | 文本框 | 流程审批状态 | 常显 | 流程回调更新 |
| 归档状态 | SA_SALE_CONTRACT_HEAD.STATE_PIGEONHOLE | 文本框 | 合同归档状态 | 常显 | 归档后更新 |

### 选择弹窗

> 本页面查询条件使用文本输入和下拉选择，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新增 | 新建年度经销合同 | 列表页 | 始终可用 | 打开新建页面 |
| 保存 | 保存合同信息 | 编辑页 | 编辑状态 | 调用save接口 |
| 保存并提交 | 发起OA审批 | 编辑页 | 保存后 | 发起DISTRIBUTION_CONTRACT_DKHB流程 |
| 删除 | 删除合同 | 列表页 | 选中未提交记录 | 调用remove接口 |
| 导出 | 导出合同列表 | 列表页 | 有数据时 | 调用selectExportList接口 |

### 保存校验

- 校验1：经销商不能为空 —— 确保合同关联明确经销商

  - 详细逻辑

    - 第1点：保存时校验经销商ID不为空

  - 系统体现：toast提醒

  - 排查SQL：

    ```sql
    SELECT * FROM SA_SALE_CONTRACT_HEAD WHERE CUSTOMER_ID IS NULL;
    ```

### 提交校验

- 校验1：流程启动校验 —— 确保合同数据完整

  - 详细逻辑

    - 第1点：通过workFlowStartValid方法进行流程启动前校验

    - 第2点：校验区域合法性(doCheckArea)

  - 系统体现：阻断性报错

  - 排查SQL：

    ```sql
    SELECT * FROM SA_SALE_CONTRACT_HEAD WHERE SALE_CONTRACT_HEAD_ID = #{id} AND (CUSTOMER_ID IS NULL OR CONTRACT_YEAR IS NULL);
    ```

### 状态机

#### 状态机流转图

```text
新建 ──保存──→ 已保存 ──提交──→ 审批中 ──OA审批通过──→ 已审批 ──归档──→ 已归档
                                │
                                └──OA审批拒绝──→ 已拒绝
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| NEW | 已保存未提交 | 编辑、保存、提交、删除 |
| RUN | OA审批中 | 无（等待审批结果） |
| APPROVED | OA审批通过 | 查看、归档、变更 |
| REJECTED | OA审批拒绝 | 修改、重新提交 |
| ARCHIVED | 已归档 | 查看 |

---

## 数据库表详解

### 表1：SA_SALE_CONTRACT_HEAD（年度经销合同头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| SALE_CONTRACT_HEAD_ID | BIGINT | 主键ID | 合同ID | 自增主键 |
| CONTRACT_NO | VARCHAR | 合同编号 | 合同编号 | 自动生成 |
| CUSTOMER_ID | BIGINT | 经销商ID | - | 选择经销商带出 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商 | 选择经销商带出 |
| CONTRACT_YEAR | VARCHAR | 合同年度 | 合同年度 | 手动选择 |
| ENTID | BIGINT | 事业部ID | - | 选择事业部带出 |
| ENTNAME | VARCHAR | 事业部名称 | 事业部 | 选择事业部带出 |
| TRADING_COMPANY_NAME | VARCHAR | 交易公司名称 | 交易公司 | 选择交易公司带出 |
| HZ_APPROVE_STATUS | VARCHAR | H0流程审批状态 | H0流程审批状态 | 流程回调更新 |
| STATE_PIGEONHOLE | VARCHAR | 合同归档状态 | 归档状态 | 归档后更新 |
| PIGEONHOLE_DATE | DATE | 应归档时间 | - | 系统带出 |
| ACTUAL_PIGEONHOLE_DATE | DATE | 实际归档时间 | - | 归档后更新 |
| PIGEONHOLE_BY | VARCHAR | 归档人 | - | 归档后更新 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 经销商不能为空 | 保存时 | 未选择经销商 | toast提醒 | [查看] |
| 区域校验失败 | 提交时 | 经销商在选定区域不合法，重新选择区域 | 阻断性报错 | [查看] |
| 流程编码缺失，请选择流程 | 提交时 | OA流程编码未配置 | 阻断性报错 | [查看] |
| 合同ID为空，请检查 | 保存/提交时 | 合同ID参数丢失 | toast提醒 | [查看] |
| 当前合同有效结束时间小于当前时间，不允许新建 | 保存时 | 合同结束日期已过期 | toast提醒 | [查看] |
| 合同有效开始时间必须小于等于结束时间 | 保存时 | 开始日期大于结束日期 | toast提醒 | [查看] |
| 合同开始日期须为月度第一天 | 保存时 | 开始日期非月初 | toast提醒 | [查看] |
| 法人编码不存在 | 保存时 | 经销商法人编码主数据缺失 | toast提醒 | [查看] |
| 关联合同不存在，请稍后再试 | 保存时 | 关联的母合同不存在 | toast提醒 | [查看] |
| 提前结束合同，必须为合同结束日期前至少一整月 | 保存时 | 提前结束日期不满足整月要求 | toast提醒 | [查看] |
| 当前已存在同类型经销合同，无法再签订 | 保存时 | 经销期限内已存在同类型合同 | 阻断性报错 | [查看] |
| 未能获取合同保证金标准，不能创建合同 | 保存时 | 保证金标准未配置 | 阻断性报错 | [查看] |
| 未配置经销合同延期归档天数，请联系管理员 | 归档时 | 系统参数未配置 | toast提醒 | [查看] |
| 该合同应归档日期为空，请联系管理员 | 归档时 | 合同应归档日期缺失 | toast提醒 | [查看] |
| 经销商编码不能为空 | 校验时 | 经销商编码参数缺失 | toast提醒 | [查看] |
| 网络请求失败 | 全局 | 后端服务不可达或超时 | toast提醒 | [查看] |
| 权限不足，无法操作 | 全局 | 当前用户无对应操作权限 | toast提醒 | [查看] |

#### 报错1：经销商不能为空
- **触发条件**：用户在新建/编辑页未选择经销商直接点击保存
- **逻辑分析**：保存接口sa-sale-contract-heads/save在写入SA_SALE_CONTRACT_HEAD前校验CUSTOMER_ID非空。经销商是年度经销合同的核心主体，未选择经销商将导致后续交易公司、事业部、销售区域无法带出，合同任务拆分和保证金关联也无从执行。校验在Controller层前置拦截，toast提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, CUSTOMER_ID, CUSTOMER_NAME,
         CONTRACT_YEAR, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE CUSTOMER_ID IS NULL OR CUSTOMER_NAME IS NULL;
  ```

#### 报错2：区域校验失败
- **触发条件**：用户点击"保存并提交"，workFlowStartValid方法调用doCheckAreaNew/doCheckArea校验区域不通过
- **逻辑分析**：提交前通过doCheckAreaNew/doCheckArea方法校验经销商在选定销售区域（SALE_AREA）内的合法性，确保合同授权区域与经销商实际授权区域一致。校验失败根因有三类：(1)经销商在选定区域无销售授权（经销商主数据中未配置该区域）；(2)合同销售区域与经销商授权区域不匹配；(3)区域数据未维护或已失效。此为阻断性报错，阻止OA流程（DISTRIBUTION_CONTRACT_DKHB）发起，需重新选择区域或联系主数据维护经销商授权区域
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.CUSTOMER_NAME, S.SALE_AREA, S.HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD S
  WHERE S.SALE_CONTRACT_HEAD_ID = #{id}
    AND S.HZ_APPROVE_STATUS IN ('NEW', 'RUN');
  -- 核查经销商授权区域（经销商主数据，具体表名以主数据为准）
  SELECT CUSTOMER_CODE, CUSTOMER_NAME, SALE_AREA, ENABLED
  FROM CUSTOMER_SALE_AREA
  WHERE CUSTOMER_CODE = #{customerCode};
  ```

#### 报错3：流程编码缺失，请选择流程
- **触发条件**：用户点击"保存并提交"，workFlowStartValid方法校验OA流程编码为空
- **逻辑分析**：年度经销合同提交需发起OA审批流程（DISTRIBUTION_CONTRACT_DKHB）。SaSaleContractHeadServiceImpl在saveAndSubmit中校验流程编码非空，流程编码缺失将导致OA流程无法启动。根因有二：(1)系统未配置DISTRIBUTION_CONTRACT_DKHB流程编码；(2)合同类型未关联对应流程编码。需在流程配置中维护对应关系
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, CONTRACT_TYPE, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE SALE_CONTRACT_HEAD_ID = #{id}
    AND HZ_APPROVE_STATUS = 'NEW';
  ```

#### 报错4：合同ID为空，请检查
- **触发条件**：用户编辑或提交合同时，SALE_CONTRACT_HEAD_ID参数为空
- **逻辑分析**：SaSaleContractHeadServiceImpl在更新、提交、归档等操作前校验合同ID非空。合同ID是主键，缺失将导致无法定位合同记录，后续所有操作均无法执行。通常由前端未正确传入选中记录ID导致
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE SALE_CONTRACT_HEAD_ID IS NULL OR SALE_CONTRACT_HEAD_ID = 0;
  ```

#### 报错5：当前合同有效结束时间小于当前时间，不允许新建
- **触发条件**：用户新建合同时，填写的合同结束日期小于当前系统日期
- **逻辑分析**：SaSaleContractHeadServiceImpl校验合同结束日期（END_DATE）必须大于等于当前日期。结束日期已过期意味着合同生效即失效，无业务意义。校验在保存前拦截，toast提示后阻断保存。需修改合同结束日期为未来日期
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, START_DATE, END_DATE,
         HZ_APPROVE_STATUS, SYSDATE AS 当前日期
  FROM SA_SALE_CONTRACT_HEAD
  WHERE END_DATE < SYSDATE
    AND HZ_APPROVE_STATUS = 'NEW';
  ```

#### 报错6：合同有效开始时间必须小于等于结束时间
- **触发条件**：用户填写的合同开始日期大于结束日期
- **逻辑分析**：SaSaleContractHeadServiceImpl校验START_DATE <= END_DATE。开始日期大于结束日期违反时间逻辑，合同有效期区间为空。校验在保存前拦截，需重新确认合同起止日期
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, START_DATE, END_DATE, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE START_DATE > END_DATE;
  ```

#### 报错7：合同开始日期须为月度第一天
- **触发条件**：用户填写的合同开始日期非月初（如2026-08-15）
- **逻辑分析**：SaSaleContractHeadServiceImpl校验合同开始日期必须为月度第一天（如yyyy-MM-01）。经销合同按月度维度拆分任务和结算，非月初将导致月度任务拆分异常。校验在保存前拦截，需将开始日期调整为月初
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, START_DATE,
         EXTRACT(DAY FROM START_DATE) AS 开始日
  FROM SA_SALE_CONTRACT_HEAD
  WHERE EXTRACT(DAY FROM START_DATE) != 1;
  ```

#### 报错8：法人编码不存在
- **触发条件**：用户选择经销商后保存合同，根据经销商查询法人编码返回空
- **逻辑分析**：SaSaleContractHeadServiceImpl根据经销商CUSTOMER_ID查询法人主数据，若经销商未配置法人关联或法人编码（CORPORATE_CODE）在主数据中不存在，将抛出此异常。法人编码是合同归档、CRM推送的关键标识。需在经销商主数据中维护法人关联
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.CUSTOMER_ID, S.CUSTOMER_NAME,
         C.CORPORATE_CODE, C.CORPORATE_NAME
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN CUSTOMER C ON S.CUSTOMER_ID = C.CUSTOMER_ID
  WHERE C.CORPORATE_CODE IS NULL;
  ```

#### 报错9：关联合同不存在，请稍后再试
- **触发条件**：用户保存合同时，关联的母合同或关联合同在SA_SALE_CONTRACT_HEAD中查询不到
- **逻辑分析**：SaSaleContractHeadServiceImpl校验关联合同存在性。关联合同不存在根因有三类：(1)母合同已被删除；(2)母合同ID传入错误；(3)数据同步延迟（母合同在CRM侧创建但未同步到AE）。需确认母合同存在且已同步
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.PARENT_CONTRACT_ID,
         P.CONTRACT_NO AS 母合同编号, P.HZ_APPROVE_STATUS AS 母合同状态
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN SA_SALE_CONTRACT_HEAD P ON S.PARENT_CONTRACT_ID = P.SALE_CONTRACT_HEAD_ID
  WHERE S.PARENT_CONTRACT_ID IS NOT NULL AND P.SALE_CONTRACT_HEAD_ID IS NULL;
  ```

#### 报错10：提前结束合同，必须为合同结束日期前至少一整月
- **触发条件**：用户对已生效合同执行提前结束操作，提前结束日期不满足整月要求
- **逻辑分析**：SaSaleContractHeadServiceImpl校验提前结束合同的操作，要求提前结束日期必须在原合同结束日期前至少一整月。此校验确保月度任务拆分和结算完整。需调整提前结束日期至整月前
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, START_DATE, END_DATE,
         ACTUAL_END_DATE, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE ACTUAL_END_DATE IS NOT NULL
    AND ADD_MONTHS(ACTUAL_END_DATE, 1) > END_DATE;
  ```

#### 报错11：当前已存在同类型经销合同，无法再签订
- **触发条件**：用户新建合同时，校验发现当前经销商在经销期限内已存在同合同类型的有效合同
- **逻辑分析**：SaSaleContractHeadServiceImpl校验同一经销商在同一经销期限内不允许签订同类型的多个经销合同。重复签订将导致保证金重复计算、任务重复拆分。需调整合同期限或变更合同类型，或先终止已有合同
- **排查SQL**：
  ```sql
  SELECT CUSTOMER_ID, CUSTOMER_NAME, CONTRACT_TYPE, CONTRACT_NO,
         START_DATE, END_DATE, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE CUSTOMER_ID = #{customerId}
    AND CONTRACT_TYPE = #{contractType}
    AND HZ_APPROVE_STATUS = 'APPROVED'
    AND START_DATE <= #{newEndDate}
    AND END_DATE >= #{newStartDate};
  ```

#### 报错12：未能获取合同保证金标准，不能创建合同
- **触发条件**：用户新建合同时，根据事业部、合同类型查询保证金标准（CM_DEPOSITS_PAY_STANDARD）返回空
- **逻辑分析**：SaSaleContractHeadServiceImpl在创建合同前查询保证金标准，保证金标准是计算合同应缴保证金的依据。标准未配置将导致合同保证金金额无法计算。需先在保证金标准配置中维护对应事业部和合同类型的标准
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.ENTID, S.CONTRACT_TYPE,
         P.PAY_STANDARD_ID, P.STANDARD_AMT
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN CM_DEPOSITS_PAY_STANDARD P ON S.ENTID = P.ENTID AND S.CONTRACT_TYPE = P.CONTRACT_TYPE
  WHERE P.PAY_STANDARD_ID IS NULL
    AND S.HZ_APPROVE_STATUS = 'NEW';
  ```

#### 报错13：未配置经销合同延期归档天数，请联系管理员
- **触发条件**：合同归档时，系统参数中未配置延期归档天数
- **逻辑分析**：SaSaleContractHeadServiceImpl在归档时读取系统参数（经销合同延期归档天数），参数缺失将导致无法计算应归档日期。需联系管理员在系统参数中配置
- **排查SQL**：
  ```sql
  SELECT PARAM_CODE, PARAM_NAME, PARAM_VALUE
  FROM SYS_PARAM
  WHERE PARAM_CODE = 'SALE_CONTRACT_DELAY_PIGEONHOLE_DAYS';
  ```

#### 报错14：该合同应归档日期为空，请联系管理员
- **触发条件**：合同归档时，PIGEONHOLE_DATE（应归档时间）字段为空
- **逻辑分析**：SaSaleContractHeadServiceImpl校验合同应归档日期非空。应归档日期由合同审批通过时间加延期归档天数计算得出，为空将导致归档流程无法判断归档时机。需联系管理员补充应归档日期
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, HZ_APPROVE_STATUS,
         PIGEONHOLE_DATE, ACTUAL_PIGEONHOLE_DATE, PIGEONHOLE_BY
  FROM SA_SALE_CONTRACT_HEAD
  WHERE HZ_APPROVE_STATUS = 'APPROVED'
    AND PIGEONHOLE_DATE IS NULL;
  ```

#### 报错15：经销商编码不能为空
- **触发条件**：校验合同或出库单时，经销商编码（CUSTOMER_CODE）参数为空
- **逻辑分析**：SaSaleContractHeadServiceImpl在多处校验经销商编码非空。经销商编码是关联经销商主数据的关键字段，为空将导致经销商信息无法带出，合同与出库单关联无法建立。需前端正确传入经销商编码
- **排查SQL**：
  ```sql
  SELECT SALE_CONTRACT_HEAD_ID, CONTRACT_NO, CUSTOMER_ID, CUSTOMER_NAME, CUST_CODE
  FROM SA_SALE_CONTRACT_HEAD
  WHERE CUST_CODE IS NULL OR CUST_CODE = '';
  ```

#### 报错16：网络请求失败
- **触发条件**：前端调用sa-sale-contract-heads相关接口时，后端服务不可达或请求超时
- **逻辑分析**：前端通过axios调用AE_BUSINESS服务，网络异常、服务宕机、网关超时均会触发。前端拦截器统一捕获并toast提示。需检查AE_BUSINESS服务状态、网络连通性、网关配置
- **排查SQL**：
  ```sql
  SELECT '网络层异常，无SQL排查' AS 提示 FROM DUAL;
  ```

#### 报错17：权限不足，无法操作
- **触发条件**：当前用户对合同保存、提交、归档等操作无对应功能权限或数据权限
- **逻辑分析**：后端通过权限注解校验用户角色，前端通过菜单和按钮权限控制显隐。用户无权限时后端返回403，前端拦截器toast提示。需在权限管理中为用户分配对应角色
- **排查SQL**：
  ```sql
  SELECT '权限层异常，请核查用户角色配置' AS 提示 FROM DUAL;
  ```

### 常见问题

- 问题1：合同无法归档
  - 原因：合同未审批通过或不满足归档条件
  - 解决思路：检查SQL `SELECT HZ_APPROVE_STATUS, STATE_PIGEONHOLE FROM SA_SALE_CONTRACT_HEAD WHERE SALE_CONTRACT_HEAD_ID = #{id}`

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
