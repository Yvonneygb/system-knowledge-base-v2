# 工程管理-项目往来-工程真实性核销

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

```text
用户进入工程真实性核销页面
  → 选择项目/合同/折扣政策
  → 查询出库单行(searchOutBill) → 选择需核销的出库行
  → 查询发票信息(invoiceInfo) → 选择已生效的核销发票上传单
  → 填写核销行信息(本次核销数量、核销截止日)
  → 数量转化(convertNum) → 计算可核销数量、剩余可核销数量
  → 保存(POST /save或/update) → 生成核销单号(AE.INVOICE_AUTHENTICITY_VERIFI)
  → 提交审批 → 触发工作流(PROJECT_XMZSXHX_AW/PROJECT_JZXMZSXHX_AW)
    → 审批通过 → 更新出库行已核销数量+发票明细有效状态valid → 触发返利计算
    → 审批驳回 → 流程中断
  → 可终止(POST /terminate)/撤回终止(POST /undo-terminate)
  → 可取消核销(POST /invoice-truth/cancel) → 5种取消类型
```

### 全链路流程图

```text
开始 → 选择项目/合同 → 选择出库单行 → 选择发票上传单 → ★保存真实性核销单★
  → 提交审批(工作流) → ⚖审批通过？
    ├── 是 → 更新出库行核销数量+发票明细valid+触发返利 → 结束
    └── 否 → 驳回中断 → 结束
  → 取消核销(5种类型) → 恢复出库行可核销数量 → 结束
  → 终止/撤回终止 → 结束
```

### 关键规则

- **核销行+发票明细**：每行核销需明确出库行与对应发票明细，保证账实相符
- **核销数量不超额**：本次核销数量受出库行可核销余额约束，超额将被拦截，小数位≤3
- **审批后联动**：审批通过更新出库行与发票状态，并触发供应商返利计算
- **三性校验**：发票购买方与工程采购单位一致性(UNIT_NAME_IS_AGREEMENT)、销售方与经销商法人一致性(BILLING_NAME_IS_AGREEMENT)、发票日期与发货日期一致性(DATE_IS_AGREEMENT)
- **取消核销5种类型**：invoice(按发票)/invoiceDetail(按发票明细)/invLine(按出库单行)/veriferDetail(按核销明细)/obsInvoice(作废发票)
- **工程/家装区分**：isHome=1工程使用PROJECT_XMZSXHX_AW工作流+AE.INVOICE_AUTHENTICITY_VERIFI编码，isHome=2家装使用PROJECT_JZXMZSXHX_AW工作流+AE.JZ_INVOICE_AUTHENTICITY_VER编码

---

## 重点逻辑

### 重点逻辑1：真实性核销保存逻辑

- **业务意义**：保存真实性核销单，包含出库单行和发票核销明细，确认业务真实发生
- **具体逻辑描述**：
  - 新增时自动生成核销单号(编码规则AE.INVOICE_AUTHENTICITY_VERIFI/AE.JZ_INVOICE_AUTHENTICITY_VER)，初始状态为新建
  - 保存数据包含两部分：核销行(EPM_INVOICE_TRUTH_LINE)和核销发票明细(EPM_VERIFER_INVOICE_DETAILS)
  - 每行核销数据包含：出库单号、发货日期、产品编码、可核销数量、剩余可核销数量、本次核销数量
  - 本次核销数量的小数位不可超过3位
  - 修改时采用先删后插策略处理子表数据

### 重点逻辑2：核销数量校验

- **业务意义**：确保本次核销数量不超过剩余可核销数量，防止超额核销
- **具体逻辑描述**：
  - 保存时校验每行的本次核销数量是否大于剩余可核销数量
  - 剩余可核销数量来源于出库单行的可核销数量(CAN_VERIFY_NUM)
  - 校验发货日期和产品编码是否匹配
  - 单位转换通过UOM_RATE处理，转化后剩余可核销数量=剩余可核销数量×UOM_RATE

### 重点逻辑3：审批通过后处理

- **业务意义**：审批通过后更新出库单行核销数量，触发返利计算
- **具体逻辑描述**：
  - 更新出库单行的已核销数量增加本次核销数量，可核销数量减少本次核销数量
  - 更新核销发票明细有效状态为valid
  - 若出库单行渠道上调价>0，触发返利计算(operationRebateBiz)

### 重点逻辑4：取消核销/作废发票

- **业务意义**：支持多种维度取消核销或作废发票，灵活处理核销异常
- **具体逻辑描述**：
  - 支持5种操作类型：invoice(按发票)/invoiceDetail(按发票明细)/invLine(按出库单行)/veriferDetail(按核销明细)/obsInvoice(作废发票)
  - 取消核销时，查询受影响的核销明细，更新有效状态为canceled，记录取消操作人和时间
  - 若受影响的核销单处于审批中(stat=3)，则中断审批，将核销单状态重置为新建
  - 更新出库单行：可核销数量回加取消数量，已核销数量减少取消数量
  - 作废发票时，更新发票主要信息有效状态为obsolete，同时更新关联核销明细状态
  - 取消操作使用分布式锁控制并发，避免同时取消导致数据不一致

### 重点逻辑5：终止与撤回终止

- **业务意义**：对真实性核销单进行终止操作，支持撤回终止恢复
- **具体逻辑描述**：
  - 终止(terminate)：标记核销单为终止状态，记录终止历史
  - 撤回终止(undoTerminate)：恢复为正常状态
  - bizType区分：isHome=2为211115(家装)，否则210916(工程)

### 重点逻辑6：数量转化

- **业务意义**：处理发票明细单位与核销单位不一致时的数量转换
- **具体逻辑描述**：
  - convertNum接口处理单位转换
  - 通过UOM_RATE计算转化后剩余可核销数量(CONVERT_SUR_VERIFER_NUMBER)
  - 保证核销数量在不同计量单位下准确

---

## 详细逻辑

### 界面模块1：查询区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 核销单号 | EPM_INVOICE_TRUTH_HEADER.INVOICE_TRUTH_NO | 文本输入框 | 模糊查询核销单号 | 常显 | 手动输入 |
| 项目编码 | EPM_INVOICE_TRUTH_HEADER.PROJECT_CODE | 文本输入框 | 模糊查询项目 | 常显 | 手动输入 |
| 合同编码 | EPM_INVOICE_TRUTH_HEADER.CONTRACT_CODE | 文本输入框 | 模糊查询合同 | 常显 | 手动输入 |
| 经销商名称 | EPM_INVOICE_TRUTH_HEADER.CUSTOMER_NAME | 文本输入框 | 模糊查询经销商 | 常显 | 手动输入 |
| 审批状态 | EPM_INVOICE_TRUTH_HEADER.HZ_APPROVE_STATUS | 下拉选择框 | 筛选审批状态 | 常显 | 值集HWKF.APPROVE_STATUS |
| 生效状态 | EPM_VERIFER_INVOICE_DETAILS.EFFECT_STATUS | 下拉选择框 | 筛选生效状态 | 常显 | 值集VERIFER_EFFECT_STATUS |

### 界面模块2：列表表格

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 核销单号 | EPM_INVOICE_TRUTH_HEADER.INVOICE_TRUTH_NO | 文本框 | 真实性核销单号 | 常显 | 新增时按编码规则自动生成，不可编辑 |
| 项目编码 | EPM_INVOICE_TRUTH_HEADER.PROJECT_CODE | 文本框 | 关联项目编码 | 常显 | 来源于项目选择，不可编辑 |
| 项目名称 | EPM_INVOICE_TRUTH_HEADER.PROJECT_NAME | 文本框 | 关联项目名称 | 常显 | 来源于项目选择，不可编辑 |
| 合同编码 | EPM_INVOICE_TRUTH_HEADER.CONTRACT_CODE | 文本框 | 关联合同编码 | 常显 | 来源于合同选择，不可编辑 |
| 经销商名称 | EPM_INVOICE_TRUTH_HEADER.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 来源于项目/合同，不可编辑 |
| 核销发票上传单号 | EPM_INVOICE_TRUTH_HEADER.INVOICE_VERIFER_NO | 文本框 | 关联的核销发票上传单号 | 常显 | 选择核销发票上传单后带入 |
| 核销类型 | EPM_INVOICE_TRUTH_HEADER.VERIFER_TYPE | 文本框 | 核销类型 | 常显 | 来源于核销发票上传单 |
| 审批状态 | EPM_INVOICE_TRUTH_HEADER.HZ_APPROVE_STATUS | 标签 | H0流程审批状态 | 常显 | 值集HWKF.APPROVE_STATUS翻译 |
| 是否家装 | EPM_INVOICE_TRUTH_HEADER.IS_HOME | 文本框 | 是否家装 | 常显 | 1-工程/2-家装 |
| 折扣政策 | EPM_INVOICE_TRUTH_HEADER.DISCOUNT_POLICY_NAME | 文本框 | 关联折扣政策名称 | 常显 | 选择折扣政策后带入 |
| 创建人 | EPM_INVOICE_TRUTH_HEADER.CREATOR_NAME | 文本框 | 创建人 | 常显 | 系统自动记录 |
| 创建时间 | EPM_INVOICE_TRUTH_HEADER.CREATE_TIME | 日期 | 创建时间 | 常显 | 系统自动记录 |

### 界面模块3：详情页-核销行（明细字段列表）

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 出库单号 | EPM_INVOICE_TRUTH_LINE.INVBILLNO | 文本框 | ERP出库单号 | 常显 | 来源于出库单行，不可编辑 |
| 发货日期 | EPM_INVOICE_TRUTH_LINE.DATE_INVBILL | 日期选择框 | 发货日期 | 常显 | 来源于出库单行，不可编辑 |
| 产品编码 | EPM_INVOICE_TRUTH_LINE.ITEM_CODE | 文本框 | 产品编码 | 常显 | 来源于出库单行，不可编辑 |
| 产品名称 | EPM_INVOICE_TRUTH_LINE.ITEM_NAME | 文本框 | 产品名称 | 常显 | 来源于出库单行，不可编辑 |
| 实发数量 | EPM_INVOICE_TRUTH_LINE.QTY_BILL | 数值框 | 实发数量 | 常显 | 来源于出库单行，不可编辑 |
| 可核销数量 | EPM_INVOICE_TRUTH_LINE.CAN_VERIFER_NUMBER | 数值框 | 出库单行可核销数量 | 常显 | 来源于出库单行CAN_VERIFY_NUM，不可编辑 |
| 剩余可核销数量 | EPM_INVOICE_TRUTH_LINE.SURPLUS_CAN_VERIFER_NUMBER | 数值框 | 剩余可核销数量 | 常显 | 可核销数量-已核销数量，不可编辑 |
| 本次核销数量 | EPM_INVOICE_TRUTH_LINE.THIS_VERIFER_NUMBER | 数值框 | 本次核销数量 | 常显 | 手工录入，可编辑，小数位≤3，>0且≤剩余可核销数量 |
| 核销截止日 | EPM_INVOICE_TRUTH_LINE.VERIFER_END_DATE | 日期选择框 | 核销截止日期 | 常显 | 手工录入，可编辑 |
| 补核销截止日 | EPM_INVOICE_TRUTH_LINE.REPAIR_VERIFER_END_DATE | 日期选择框 | 补核销截止日期 | 常显 | 手工录入，可编辑 |
| 发票号码 | EPM_INVOICE_TRUTH_LINE.INVOICE_NUMBER | 文本框 | 关联发票号码 | 常显 | 来源于发票信息，不可编辑 |
| 发票代码 | EPM_INVOICE_TRUTH_LINE.INVOICE_CODE | 文本框 | 关联发票代码 | 常显 | 来源于发票信息，不可编辑 |

### 界面模块4：详情页-核销发票明细

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 发票号码 | EPM_VERIFER_INVOICE_DETAILS.INVOICE_NUMBER | 文本框 | 发票号码 | 常显 | 来源于核销发票上传，不可编辑 |
| 发票代码 | EPM_VERIFER_INVOICE_DETAILS.INVOICE_CODE | 文本框 | 发票代码 | 常显 | 来源于核销发票上传，不可编辑 |
| 货物或服务名称 | EPM_VERIFER_INVOICE_DETAILS.SERVICES_NAME | 文本框 | 货物或服务名称 | 常显 | 来源于核销发票上传，不可编辑 |
| 产品编码 | EPM_VERIFER_INVOICE_DETAILS.SERVICES_CODE | 文本框 | 产品编码 | 常显 | 来源于核销发票上传，不可编辑 |
| 可核销数量 | EPM_VERIFER_INVOICE_DETAILS.CAN_VERIFER_NUMBER | 数值框 | 可核销数量 | 常显 | 来源于发票明细，不可编辑 |
| 剩余可核销数量 | EPM_VERIFER_INVOICE_DETAILS.SURPLUS_CAN_VERIFER_NUMBER | 数值框 | 剩余可核销数量 | 常显 | 来源于发票明细，不可编辑 |
| 本次核销数量 | EPM_VERIFER_INVOICE_DETAILS.THIS_VERIFER_NUMBER | 数值框 | 本次核销数量 | 常显 | 来源于核销行，不可编辑 |
| 单位转换率 | EPM_VERIFER_INVOICE_DETAILS.UOM_RATE | 数值框 | 单位转换率 | 常显 | 计量单位转换 |
| 转化后剩余可核销数量 | EPM_VERIFER_INVOICE_DETAILS.CONVERT_SUR_VERIFER_NUMBER | 数值框 | 转化后剩余可核销数量 | 常显 | 剩余可核销数量×UOM_RATE |
| 购买方一致 | EPM_VERIFER_INVOICE_DETAILS.UNIT_NAME_IS_AGREEMENT | 标签 | 发票购买方与工程采购单位一致 | 常显 | 1=一致 |
| 销售方一致 | EPM_VERIFER_INVOICE_DETAILS.BILLING_NAME_IS_AGREEMENT | 标签 | 发票销售方与经销商法人一致 | 常显 | 1=一致 |
| 日期一致 | EPM_VERIFER_INVOICE_DETAILS.DATE_IS_AGREEMENT | 标签 | 发票日期在发货日期之前 | 常显 | 1=一致 |
| 有效状态 | EPM_VERIFER_INVOICE_DETAILS.EFFECT_STATUS | 标签 | 有效状态 | 常显 | invalid/valid/canceled |
| 取消类型 | EPM_VERIFER_INVOICE_DETAILS.CANCEL_TYPE | 文本框 | 取消核销类型 | 已取消时显示 | invoice/invoiceDetail/invLine/veriferDetail/obsInvoice |
| 取消操作人 | EPM_VERIFER_INVOICE_DETAILS.CANCEL_OPERATOR | 文本框 | 核销取消操作人 | 已取消时显示 | 系统自动记录 |
| 取消时间 | EPM_VERIFER_INVOICE_DETAILS.CANCEL_TIME | 日期 | 核销取消操作时间 | 已取消时显示 | 系统自动记录 |

### 后端接口

| 接口名称 | Controller | Service | 路径 | 说明 |
|---------|-----------|---------|------|------|
| 查询项目 | EpmInvoiceTruthHeaderController | queryProjectList | GET /search-project | 查询可核销项目 |
| 保存 | EpmInvoiceTruthHeaderController | save | POST /save | 保存真实性核销单 |
| 修改 | EpmInvoiceTruthHeaderController | update | POST /update | 修改真实性核销单 |
| 列表查询 | EpmInvoiceTruthHeaderController | searchList | GET /invoice-truth/list | 分页查询核销列表 |
| 明细查询 | EpmInvoiceTruthHeaderController | searchDetail | GET /invoice-truth/detail/{id} | 查询核销明细 |
| 删除 | EpmInvoiceTruthHeaderController | invoiceTruthDelete | POST /invoice-truth/delete/{id} | 删除核销单 |
| 查询交易公司 | EpmInvoiceTruthHeaderController | doSearchTradingCompany | GET /do-search-trading-company | 查询交易公司 |
| 数量转化 | EpmInvoiceTruthHeaderController | convertNum | POST /convert-num | 单位转换数量计算 |
| 终止 | EpmInvoiceTruthHeaderController | terminate | POST /terminate | 终止核销单 |
| 撤回终止 | EpmInvoiceTruthHeaderController | undoTerminate | POST /undo-terminate | 撤回终止 |
| 查询 | EpmInvoiceTruthHeaderController | doSelect | POST /select | 查询核销详情 |
| 查询出库单报表 | EpmInvoiceTruthHeaderController | doSearchOutbillReport | POST /search-outbill-report | 查询工程出库单 |
| 查询发票信息 | EpmInvoiceTruthHeaderController | doGetInvoiceInfo | POST /search-invoice-info | 查询核销发票信息 |
| 明细 | EpmInvoiceTruthHeaderController | selectDetailByInvoiceTruthId | GET /truth-detail | 查询核销明细 |
| 发票明细 | EpmInvoiceTruthHeaderController | queryInvoiceDetailListByTruthId | GET /invoice-detail-list | 查询发票明细列表 |
| 项目查询 | EpmInvoiceTruthHeaderController | searchProject | GET /invoice-truth/search-project | 真实性核销项目查询 |
| 出库单列表 | EpmInvoiceTruthHeaderController | searchOutBill | GET /invoice-truth/search-out-bill | 出库单列表查询 |
| 发票列表 | EpmInvoiceTruthHeaderController | invoiceInfo | GET /invoice-truth/invoice-info | 发票列表查询 |
| 核对 | EpmInvoiceTruthHeaderController | queryInvoiceDetail | GET /invoice-truth/query-invoice-detail/{id} | 核销核对查询 |
| 取消/作废 | EpmInvoiceTruthHeaderController | cancel | POST /invoice-truth/cancel | 取消核销或作废发票 |
| 获取出库单 | EpmInvoiceTruthHeaderController | queryInvOutBillHeadList | POST /invoutlist | 获取出库单列表 |
| 获取发票信息 | EpmInvoiceTruthHeaderController | queryInvoiceInfoList | POST /invoice-info | 获取发票信息列表 |

### 选择弹窗

| 弹窗名称 | 触发位置 | 作用 | 入参 | 出参 |
|---------|---------|------|------|------|
| 项目选择弹窗 | 详情页选择项目 | 选择可核销项目 | 组织ID | projectId/projectCode/projectName |
| 合同选择弹窗 | 详情页选择合同 | 选择关联合同 | projectId | contractId/contractCode/contractName |
| 出库单选择弹窗 | 详情页选择出库单行 | 选择需核销的出库单行 | projectId/contractId | invbillno/dateInvbill/itemCode/qtyBill/canVerifyNum |
| 发票上传单选择弹窗 | 详情页选择发票 | 选择已生效的核销发票上传单 | projectId/contractId | invoiceVeriferId/invoiceVeriferNo |
| 折扣政策选择弹窗 | 详情页选择折扣政策 | 选择关联折扣政策 | projectId/contractId | discountPolicyId/discountPolicyCode/discountPolicyName |
| 交易公司选择弹窗 | 详情页选择交易公司 | 选择交易公司 | 组织ID | tradingCompanyId/tradingCompanyName |

### 导入

本页面无导入功能。核销数据通过选择出库单行和发票上传单自动带入，手工填写本次核销数量。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 保存 | 保存真实性核销单 | 详情页 | 新建/编辑状态 | 调用save/update接口，保存核销行和核销明细 |
| 提交 | 提交审批 | 详情页 | 核销单已保存 | 启动工作流PROJECT_XMZSXHX_AW/PROJECT_JZXMZSXHX_AW |
| 删除 | 删除核销单 | 详情页 | 新建状态 | 调用invoiceTruthDelete接口 |
| 终止 | 终止核销单 | 详情页 | 非终止状态 | 调用terminate接口 |
| 撤回终止 | 撤回终止 | 详情页 | 已终止状态 | 调用undoTerminate接口 |
| 取消核销 | 取消已核销的明细 | 详情页 | 存在有效核销明细 | 调用cancel接口，支持5种操作类型 |
| 作废发票 | 作废指定发票 | 详情页 | 存在有效发票 | 调用cancel接口(actionType=obsInvoice) |
| 数量转化 | 单位转换数量计算 | 详情页 | 需单位转换时 | 调用convertNum接口 |
| 查询出库单 | 查询可核销出库单 | 详情页 | 已选项目 | 调用searchOutBill接口 |
| 查询发票信息 | 查询可核销发票 | 详情页 | 已选项目 | 调用invoiceInfo接口 |

#### 按钮1：保存（详情页）

- **触发条件**：新建或编辑状态
- **处理逻辑**：
  1. 校验本次核销数量>0且≤剩余可核销数量，小数位≤3
  2. 校验发货日期和产品编码匹配
  3. 新增时生成核销单号(编码规则AE.INVOICE_AUTHENTICITY_VERIFI/AE.JZ_INVOICE_AUTHENTICITY_VER)
  4. 保存核销行(EPM_INVOICE_TRUTH_LINE)和核销发票明细(EPM_VERIFER_INVOICE_DETAILS)

#### 按钮2：提交审批（详情页）

- **触发条件**：核销单已保存且存在本次核销数量>0的行
- **处理逻辑**：
  1. 校验核销单下存在核销行数据且存在本次核销数量>0的记录
  2. 按isHome选择工作流编码(工程PROJECT_XMZSXHX_AW/家装PROJECT_JZXMZSXHX_AW)
  3. 启动工作流实例，更新hzInstanceId和hzApproveStatus=RUN

#### 按钮3：取消核销（详情页）

- **触发条件**：存在有效核销明细
- **处理逻辑**：
  1. 校验actionType非空且在5种合法值范围内(invoice/invoiceDetail/invLine/veriferDetail/obsInvoice)
  2. 校验idList非空
  3. 查询受影响的核销明细，更新有效状态为canceled，记录取消操作人和时间
  4. 若受影响核销单处于审批中，中断审批重置为新建
  5. 更新出库单行：可核销数量回加，已核销数量减少
  6. 作废发票时更新发票主要信息状态为obsolete

### 保存校验

#### 校验1：本次核销数量不可超过剩余可核销数量 —— 防止超额核销

- **详细逻辑**：每行的本次核销数量(THIS_VERIFER_NUMBER)不可大于剩余可核销数量(SURPLUS_CAN_VERIFER_NUMBER)，小数位≤3
- **系统体现**：保存时逐行校验
- **排查SQL**：
```sql
SELECT INVBILLNO, ITEM_CODE, SURPLUS_CAN_VERIFER_NUMBER, THIS_VERIFER_NUMBER
FROM EPM_INVOICE_TRUTH_LINE
WHERE INVOICE_TRUTH_ID = #{invoiceTruthId}
  AND THIS_VERIFER_NUMBER > SURPLUS_CAN_VERIFER_NUMBER;
```

#### 校验2：发货日期和产品编码校验 —— 确保核销行与出库单行匹配

- **详细逻辑**：校验核销行的发货日期和产品编码与出库单行一致
- **系统体现**：checkDateAndItemCode方法校验
- **排查SQL**：
```sql
SELECT l.INVBILLNO, l.DATE_INVBILL, l.ITEM_CODE, iobl.INV_OUT_BILL_LINE_ID
FROM EPM_INVOICE_TRUTH_LINE l
JOIN INV_OUT_BILL_LINE iobl ON l.INV_OUT_BILL_LINE_ID = iobl.INV_OUT_BILL_LINE_ID
WHERE l.INVOICE_TRUTH_ID = #{invoiceTruthId};
```

### 提交校验

#### 校验1：核销单必须存在有效明细行 —— 确保有核销数据可提交

- **详细逻辑**：检查核销单下是否存在核销行数据且存在本次核销数量>0的记录
- **系统体现**：提交前查询EPM_INVOICE_TRUTH_LINE，无数据或无本次核销数量>0的行则报错
- **排查SQL**：
```sql
SELECT COUNT(*) FROM EPM_INVOICE_TRUTH_LINE
WHERE INVOICE_TRUTH_ID = #{invoiceTruthId}
  AND THIS_VERIFER_NUMBER > 0;
```

### 状态机

```text
[新建stat=1] ──提交──> [审批中stat=3] ──审批通过──> [已审批stat=5]
                         │
                         └──审批驳回──> [已中断]
[任意状态] ──终止──> [已终止]
[已终止] ──撤回终止──> [原状态]
[审批中] ──取消核销──> [新建stat=1] (中断审批)
```

| 状态值 | 状态名称 | 可执行操作 |
|--------|---------|-----------|
| 1 | 新建 | 保存、提交、删除、终止、取消核销 |
| 3 | 审批中 | 终止、取消核销(中断审批) |
| 5 | 审批通过 | 终止、取消核销 |
| INTERRUPT | 审批驳回 | 终止、取消核销 |
| TERMINATED | 已终止 | 撤回终止 |

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| EPM_UPLOAD_INVOICE_VERIFER(核销发票上传) | 数据关联 | 读取已上传的核销发票信息，关联发票号码、代码、金额 | 发票上传单已审批通过(effectStatus=valid) |
| EPM_INVOICE_TRUTH_LINE(真实性核销行) | 数据关联 | 读取核销明细行，关联产品和服务 | 核销行已保存 |
| INV_OUT_BILL_LINE(出库单行) | 数据关联 | 读取出库单行可核销数量、发货日期、产品信息 | 出库单行存在且有可核销数量 |
| EPM_DISCOUNT_POLICY(折扣政策) | 数据关联 | 读取折扣政策信息 | 折扣政策已存在 |
| HPFM_CODE_RULE(编码规则) | 编码规则 | 生成核销单号AE.INVOICE_AUTHENTICITY_VERIFI/AE.JZ_INVOICE_AUTHENTICITY_VER | 编码规则已配置 |
| H0 Workflow(工作流) | 工作流 | 触发工作流PROJECT_XMZSXHX_AW/PROJECT_JZXMZSXHX_AW | 工作流已部署 |

### 下游影响

- **EPM_VERIFER_INVOICE_DETAILS(核销发票明细)**：新增核销记录，更新生效状态、剩余可核销数量
- **EPM_INVOICE_TRUTH_LINE(真实性核销行)**：更新核销行状态和已核销数量
- **INV_OUT_BILL_LINE(出库单行)**：核销生效后更新已核销数量和可核销数量
- **INVOICE_TRUTH_CANCEL_RECORD(核销终止记录)**：取消核销时记录终止历史
- **返利计算**：审批通过后若出库单行渠道上调价>0，触发供应商返利计算
- **发票管理**：作废发票时更新发票主要信息有效状态为obsolete

---

## 数据库表详解

### EPM_INVOICE_TRUTH_HEADER（真实性核销头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| INVOICE_TRUTH_ID | Long | 真实性核销ID | - | 自增主键，@Id @GeneratedValue |
| INVOICE_TRUTH_NO | String | 真实性核销单号 | 核销单号 | 按编码规则AE.INVOICE_AUTHENTICITY_VERIFI(工程)/AE.JZ_INVOICE_AUTHENTICITY_VER(家装)生成 |
| PROJECT_ID | BigDecimal | 项目ID | - | 关联EPM_PROJECT |
| PROJECT_CODE | String | 项目编码 | 项目编码 | 冗余自EPM_PROJECT |
| PROJECT_NAME | String | 项目名称 | 项目名称 | 冗余自EPM_PROJECT |
| CONTRACT_ID | BigDecimal | 合同ID | - | 关联EPM_PROJECT_CONTRACT |
| CONTRACT_CODE | String | 合同编码 | 合同编码 | 冗余自EPM_PROJECT_CONTRACT |
| CONTRACT_NAME | String | 合同名称 | 合同名称 | 冗余自EPM_PROJECT_CONTRACT |
| CUSTOMER_ID | BigDecimal | 经销商ID | - | 关联CUSTOMER |
| CUSTOMER_CODE | String | 经销商编码 | 经销商编码 | 冗余自CUSTOMER |
| CUSTOMER_NAME | String | 经销商名称 | 经销商名称 | 冗余自CUSTOMER |
| TRADING_COMPANY_ID | BigDecimal | 交易公司ID | - | 关联交易公司 |
| TRADING_COMPANY_CODE | String | 交易公司编码 | - | 冗余自交易公司 |
| TRADING_COMPANY_NAME | String | 交易公司名称 | 交易公司 | 冗余自交易公司 |
| BILLING_UNIT_ID | BigDecimal | 开票单位ID | - | 关联CUSTOMER |
| BILLING_UNIT_CODE | String | 开票单位编码 | - | 冗余自CUSTOMER |
| BILLING_UNIT_NAME | String | 开票单位名称 | - | 冗余自CUSTOMER |
| CONTRACT_UNIT | String | 工程签约单位 | - | - |
| IS_HOME | Integer | 是否家装 | 是否家装 | 1-工程/2-家装，NotNull |
| VERIFER_TYPE | BigDecimal | 核销类型 | 核销类型 | 来源于核销发票上传单 |
| INVOICE_VERIFER_ID | BigDecimal | 核销发票上传ID | - | 关联EPM_UPLOAD_INVOICE_VERIFER |
| INVOICE_VERIFER_NO | String | 核销发票上传单号 | 核销发票上传单号 | 来源于核销发票上传单 |
| DISCOUNT_POLICY_ID | BigDecimal | 折扣政策ID | - | 关联EPM_DISCOUNT_POLICY |
| DISCOUNT_POLICY_CODE | String | 折扣政策编码 | - | 冗余自折扣政策 |
| DISCOUNT_POLICY_NAME | String | 折扣政策名称 | 折扣政策 | 冗余自折扣政策 |
| STAT | Long | 单据状态(已弃用) | - | 使用HZ_APPROVE_STATUS替代，NotNull |
| WFID | Long | 流程ID | - | 旧字段，NotNull |
| WFFLAG | Long | 流程标识 | - | 旧字段，NotNull |
| HZ_APPROVE_STATUS | String | H0流程审批状态 | 审批状态 | NEW/RUN/APPROVED/INTERRUPT，NotBlank |
| HZ_INSTANCE_ID | Long | H0流程实例ID | - | 工作流返回 |
| AUDIT_STAT | String | 审核状态 | - | 审核状态 |
| VERIFY_STAT | String | 核销类型 | - | 对应字典truth_header.verify_stat |
| NOTE | String | 备注 | - | 用户输入 |
| ORGANIZATION_ID | BigDecimal | 组织ID | - | 当前用户事业部 |
| CREATE_TIME | Date | 核销发起日期 | 创建时间 | 系统自动记录 |
| CREATOR | String | 创建人 | - | 系统自动记录 |
| CREATOR_NAME | String | 创建人姓名 | 创建人 | 系统自动记录 |

**查询SQL**：
```sql
SELECT * FROM EPM_INVOICE_TRUTH_HEADER
WHERE ORGANIZATION_ID = #{organizationId}
  AND NVL(INVOICE_TRUTH_NO, '') LIKE '%' || #{invoiceTruthNo} || '%'
  AND NVL(PROJECT_CODE, '') LIKE '%' || #{projectCode} || '%'
ORDER BY LAST_UPDATE_DATE DESC;
```

### EPM_INVOICE_TRUTH_LINE（真实性核销行表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| INVOICE_TRUTH_LINE_ID | Long | 核销行ID | - | 自增主键，@Id @GeneratedValue |
| INVOICE_TRUTH_ID | Long | 核销单ID | - | 关联EPM_INVOICE_TRUTH_HEADER |
| INVBILLNO | String | ERP出库单号 | 出库单号 | 来源于出库单行 |
| DATE_INVBILL | Date | 发货日期 | 发货日期 | 来源于出库单行 |
| VERIFER_END_DATE | Date | 核销截止日 | 核销截止日 | 手工录入 |
| REPAIR_VERIFER_END_DATE | Date | 补核销截止日 | 补核销截止日 | 手工录入 |
| ITEM_CODE | String | 产品编码 | 产品编码 | 来源于出库单行 |
| ITEM_NAME | String | 产品名称 | 产品名称 | 来源于出库单行 |
| ITEM_ID | Long | 产品ID | - | 来源于出库单行 |
| QTY_BILL | Long | 实发数量 | 实发数量 | 来源于出库单行 |
| INVOICE_NUMBER | String | 发票号码 | 发票号码 | 来源于发票信息 |
| INVOICE_CODE | String | 发票代码 | 发票代码 | 来源于发票信息 |
| SERVICES_NAME | String | 货物或服务名称 | - | 来源于发票明细 |
| CAN_VERIFER_NUMBER | Long | 可核销数量 | 可核销数量 | 来源于出库单行CAN_VERIFY_NUM |
| SURPLUS_CAN_VERIFER_NUMBER | Long | 剩余可核销数量 | 剩余可核销数量 | 可核销数量-已核销数量 |
| THIS_VERIFER_NUMBER | BigDecimal | 本次核销数量 | 本次核销数量 | 手工录入，小数位≤3 |
| SEQ | Long | 行ID | - | 行序号 |
| INVOICE_DETAILS_ID | Long | 发票详细信息ID | - | 关联EPM_UPLOAD_INVOICE_DETAILS |
| INV_OUT_BILL_LINE_ID | Long | 出库单行ID | - | 关联INV_OUT_BILL_LINE |
| SUR_VERIFY_NUM | Long | 出库单可核销数量 | - | 来源于出库单行 |
| CAN_VERIFY_NUM | Long | 出库单可核销数量 | - | 来源于出库单行 |
| UNIT_NAME_IS_AGREEMENT | Long | 购买方与采购单位一致 | - | 1=一致 |
| BILLING_NAME_IS_AGREEMENT | Long | 销售方与经销商法人一致 | - | 1=一致 |
| DATE_IS_AGREEMENT | Long | 发票日期在发货日期前 | - | 1=一致 |

### EPM_VERIFER_INVOICE_DETAILS（核销发票明细表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PK_ID | Long | 主键ID | - | 自增主键，@Id @GeneratedValue |
| INVOICE_NUMBER | String | 发票号码 | 发票号码 | 来源于核销发票上传 |
| INVOICE_CODE | String | 发票代码 | 发票代码 | 来源于核销发票上传 |
| SERVICES_NAME | String | 货物或服务名称 | 货物或服务名称 | 来源于核销发票上传 |
| SERVICES_CODE | String | 产品编码 | 产品编码 | 来源于核销发票上传 |
| INVOICE_DETAILS_ID | Long | 发票详细信息ID | - | 关联EPM_UPLOAD_INVOICE_DETAILS |
| VERIFER_INVOICE_DETAILS_ID | Long | 核销发票信息ID | - | 关联核销发票 |
| INVOICE_TRUTH_LINE_ID | Long | 真实性核销行ID | - | 关联EPM_INVOICE_TRUTH_LINE |
| CAN_VERIFER_NUMBER | String | 可核销数量 | 可核销数量 | 来源于发票明细 |
| SURPLUS_CAN_VERIFER_NUMBER | String | 剩余可核销数量 | 剩余可核销数量 | 可核销数量-已核销数量 |
| THIS_VERIFER_NUMBER | Long | 本次核销数量 | 本次核销数量 | 来源于核销行 |
| UOM_RATE | String | 单位转换率 | 单位转换率 | 计量单位转换 |
| CONVERT_SUR_VERIFER_NUMBER | String | 转化后剩余可核销数量 | 转化后剩余可核销数量 | 剩余可核销数量×UOM_RATE |
| SEQ | Long | 行ID | - | 行序号 |
| EFFECT_STATUS | String | 有效状态 | 有效状态 | invalid/valid/canceled，字典VERIFER_EFFECT_STATUS |
| CANCEL_OPERATOR | String | 核销取消操作人 | 取消操作人 | 取消时记录当前用户 |
| CANCEL_TIME | Date | 核销取消操作时间 | 取消时间 | 取消时记录系统时间 |
| CANCEL_TYPE | String | 取消核销类型 | 取消类型 | invoice/invoiceDetail/invLine/veriferDetail/obsInvoice，字典CANCEL_TYPE |
| UNIT_NAME_IS_AGREEMENT | Long | 购买方与采购单位一致 | 购买方一致 | 1=一致 |
| BILLING_NAME_IS_AGREEMENT | Long | 销售方与经销商法人一致 | 销售方一致 | 1=一致 |
| DATE_IS_AGREEMENT | Long | 发票日期在发货日期前 | 日期一致 | 1=一致 |

**取消核销更新SQL**：
```sql
UPDATE EPM_VERIFER_INVOICE_DETAILS
SET EFFECT_STATUS = 'canceled',
    CANCEL_OPERATOR = #{cancelOperator},
    CANCEL_TIME = SYSDATE,
    CANCEL_TYPE = #{cancelType}
WHERE VERIFER_INVOICE_DETAILS_ID IN (#{detailIdList});
```

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 本次核销数量的小数位不能超过3位 | 保存 | 核销数量精度超限。调整小数位至≤3位 | 高 | 保存时校验THIS_VERIFER_NUMBER小数位 |
| 参数不能为空 | 保存 | 核销参数未填写。补全核销参数后保存 | 高 | 保存时参数非空校验 |
| 核销数量必须大于0 | 保存 | 核销数量≤0。调整核销数量大于0 | 高 | 保存时校验THIS_VERIFER_NUMBER>0 |
| 核销数量超过剩余可核销数量 | 保存 | 本次核销数量超过剩余可核销数量。调整至剩余可核销范围内 | 高 | 保存时校验THIS_VERIFER_NUMBER≤SURPLUS_CAN_VERIFER_NUMBER |
| 发票必须上传 | 保存 | 未上传核销发票。先上传核销发票再保存 | 高 | 保存时校验发票关联 |
| 核销行不存在 | 保存 | INVOICE_TRUTH_LINE_ID对应核销行不存在。确认核销行有效性 | 高 | 保存时校验核销行存在 |
| 操作类型不能为空 | 取消核销 | actionType参数为空。传入合法actionType | 高 | cancel方法中actionType非空校验 |
| 对应列表id数组不能为空 | 取消核销 | idList参数为空。传入需取消的ID列表 | 高 | cancel方法中idList非空校验 |
| 不支持xxx操作 | 取消核销 | actionType不在5种合法值范围内。传入合法actionType | 高 | cancel方法中actionType合法性校验 |
| 核销取消数据为空 | 取消核销 | 按条件查询不到可取消的核销明细。确认数据状态 | 高 | 查询受影响核销明细为空时抛出 |
| 核销明细状态异常,请刷新数据后重试 | 取消核销 | 更新canceled状态时影响行数与预期不一致。刷新数据后重试 | 高 | 更新影响行数校验 |
| 更新失败,取消后出库单行已核销数量小于0 | 取消核销 | 取消数量大于出库单行已核销数量。检查数据一致性 | 高 | 出库单行已核销数量校验 |
| 请选择核销类型 | 查询出库单/发票 | 未选择核销类型(veriferType)。先选择核销类型再查询 | 高 | doSearchOutbillReport/doGetInvoiceInfo中veriferType非空校验 |
| 核销出库单明细不可为空 | 提交审批 | 核销单下无核销行数据。先添加核销行再提交 | 高 | checkWorkFlowData中核销行列表为空校验 |
| 本次核销数量不能大于可核销数量 | 保存 | 核销行本次核销数量合计超过出库单行可核销数量。调整核销数量 | 高 | checkVeriferInvoice中sumThisVeriferNumber>canVerifyNum校验 |
| 对应的出库单明细不存在 | 提交审批 | 核销行关联的出库单行(INV_OUT_BILL_LINE_ID)不存在。确认出库单行有效性 | 高 | checkWorkFlowData中invOutBillLineId为空校验 |
| 对应的出库单明细可核销数量不足 | 提交审批 | 出库单行可核销数量<0。检查出库单行可核销数量 | 高 | checkWorkFlowData中selectCanVerifyNumOfLine<0校验 |
| 发票核销数量超过剩余可核销数量 | 提交审批 | 发票明细本次核销数量超过剩余可核销数量。调整核销数量 | 高 | checkWorkFlowData中surplusCanVeriferNumber<0校验 |

#### 报错1：本次核销数量的小数位不能超过3位
- **触发条件**：保存核销时，本次核销数量(THIS_VERIFER_NUMBER)小数位超过3位
- **逻辑分析**：保存校验中检查THIS_VERIFER_NUMBER的小数位数，超过3位则抛出阻断性报错。需调整小数位至≤3位
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.THIS_VERIFER_NUMBER, vid.INVOICE_TRUTH_LINE_ID
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.SA_OUT_BILL_HEAD_ID = :saOutBillHeadId
    AND LENGTH(TO_CHAR(vid.THIS_VERIFER_NUMBER - TRUNC(vid.THIS_VERIFER_NUMBER))) - 1 > 3
  -- 查出小数位超过3位的核销明细
  ```

#### 报错2：参数不能为空
- **触发条件**：保存核销时，核销参数(如核销单ID、明细ID等)未填写
- **逻辑分析**：保存方法中校验关键参数非空，缺失则抛出阻断性报错。需补全核销参数后保存
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.VERIFER_INVOICE_ID, vid.INVOICE_TRUTH_LINE_ID,
         vid.THIS_VERIFER_NUMBER, vid.SA_OUT_BILL_HEAD_ID
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.VERIFER_INVOICE_ID IS NULL
     OR vid.INVOICE_TRUTH_LINE_ID IS NULL
     OR vid.THIS_VERIFER_NUMBER IS NULL
  -- 查出关键参数为空的核销明细
  ```

#### 报错3：核销数量必须大于0
- **触发条件**：保存核销时，本次核销数量(THIS_VERIFER_NUMBER)≤0
- **逻辑分析**：保存校验中检查THIS_VERIFER_NUMBER>0，因核销数量必须为正数。需调整核销数量大于0
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.THIS_VERIFER_NUMBER, vid.INVOICE_TRUTH_LINE_ID
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.THIS_VERIFER_NUMBER <= 0
  -- 查出核销数量≤0的异常数据
  ```

#### 报错4：核销数量超过剩余可核销数量
- **触发条件**：保存核销时，本次核销数量(THIS_VERIFER_NUMBER)超过剩余可核销数量(SURPLUS_CAN_VERIFER_NUMBER)
- **逻辑分析**：保存校验中按INVOICE_TRUTH_LINE_ID查询剩余可核销数量，若THIS_VERIFER_NUMBER>SURPLUS_CAN_VERIFER_NUMBER则抛出阻断性报错。需调整至剩余可核销范围内
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.THIS_VERIFER_NUMBER,
         itl.SURPLUS_CAN_VERIFER_NUMBER, itl.CAN_VERIFER_NUMBER, itl.VERIFERED_NUMBER
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  JOIN EPM_INVOICE_TRUTH_LINE itl ON vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID
  WHERE vid.THIS_VERIFER_NUMBER > itl.SURPLUS_CAN_VERIFER_NUMBER
  -- 查出核销数量超限的明细
  ```

#### 报错5：发票必须上传
- **触发条件**：保存核销时，未上传核销发票(发票关联为空)
- **逻辑分析**：保存校验中检查发票关联非空，因核销必须关联已上传的发票。需先上传核销发票再保存
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.VERIFER_INVOICE_ID, vid.INVOICE_TRUTH_LINE_ID,
         ith.INVOICE_TRUTH_ID, ith.INVOICE_NO
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  LEFT JOIN EPM_INVOICE_TRUTH_HEADER ith ON vid.INVOICE_TRUTH_ID = ith.INVOICE_TRUTH_ID
  WHERE vid.VERIFER_INVOICE_ID IS NULL OR ith.INVOICE_TRUTH_ID IS NULL
  -- 查出未关联发票的核销明细
  ```

#### 报错6：核销行不存在
- **触发条件**：保存核销时，INVOICE_TRUTH_LINE_ID对应的核销行不存在
- **逻辑分析**：保存校验中按INVOICE_TRUTH_LINE_ID查询EPM_INVOICE_TRUTH_LINE，若返回null则抛出阻断性报错。需确认核销行有效性
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.INVOICE_TRUTH_LINE_ID,
         itl.INVOICE_TRUTH_LINE_ID AS 核销行存在性, itl.CAN_VERIFER_NUMBER
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  LEFT JOIN EPM_INVOICE_TRUTH_LINE itl ON vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID
  WHERE itl.INVOICE_TRUTH_LINE_ID IS NULL
  -- 查出核销行不存在的明细
  ```

#### 报错7：操作类型不能为空
- **触发条件**：取消核销时，actionType参数为空
- **逻辑分析**：cancel方法中校验actionType非空，因需根据操作类型确定取消范围。需传入合法actionType(invoice/invoiceDetail/invLine/veriferDetail/obsInvoice)
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.EFFECT_STATUS, vid.ACTION_TYPE
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.ACTION_TYPE IS NULL
  -- 查出操作类型为空的核销明细
  ```

#### 报错8：对应列表id数组不能为空
- **触发条件**：取消核销时，idList参数为空
- **逻辑分析**：cancel方法中校验idList非空，因需指定要取消的核销明细ID列表。需传入需取消的ID列表
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.EFFECT_STATUS, vid.VERIFER_INVOICE_ID
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.VERIFER_INVOICE_DETAILS_ID IN (:idList)
  -- 校验传入的ID列表对应数据是否存在
  ```

#### 报错9：不支持xxx操作
- **触发条件**：取消核销时，actionType不在5种合法值(invoice/invoiceDetail/invLine/veriferDetail/obsInvoice)范围内
- **逻辑分析**：cancel方法中校验actionType合法性，不在5种合法值内则抛出阻断性报错。需传入合法actionType
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.ACTION_TYPE, vid.EFFECT_STATUS
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.ACTION_TYPE NOT IN ('invoice', 'invoiceDetail', 'invLine', 'veriferDetail', 'obsInvoice')
  -- 查出操作类型不合法的核销明细
  ```

#### 报错10：核销取消数据为空
- **触发条件**：取消核销时，按条件查询不到可取消的核销明细
- **逻辑分析**：cancel方法中按actionType和idList查询受影响的核销明细，若为空则抛出阻断性报错。需确认数据状态
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.EFFECT_STATUS, vid.VERIFER_INVOICE_ID,
         vid.INVOICE_TRUTH_LINE_ID, vid.SA_OUT_BILL_HEAD_ID
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.VERIFER_INVOICE_DETAILS_ID IN (:idList)
    AND vid.EFFECT_STATUS IN ('valid', 'invalid')
  -- 若返回空，说明无可取消的核销明细
  ```

#### 报错11：核销明细状态异常,请刷新数据后重试
- **触发条件**：取消核销时，更新canceled状态时影响行数与预期不一致
- **逻辑分析**：cancel方法中更新EFFECT_STATUS为canceled，若更新影响行数与预期不一致(并发修改或数据已被其他操作变更)则抛出阻断性报错。需刷新数据后重试
- **排查SQL**：
  ```sql
  SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.EFFECT_STATUS, vid.LAST_UPDATE_DATE, vid.OBJECT_VERSION_NUMBER
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  WHERE vid.VERIFER_INVOICE_DETAILS_ID IN (:idList)
  -- 检查核销明细当前状态和版本号，判断是否被并发修改
  ```

#### 报错12：更新失败,取消后出库单行已核销数量小于0
- **触发条件**：取消核销时，取消数量大于出库单行已核销数量
- **逻辑分析**：cancel方法中更新INV_OUT_BILL_LINE的已核销数量(原已核销数量-取消数量)，若结果<0则抛出阻断性报错。需检查数据一致性
- **排查SQL**：
  ```sql
  SELECT iobl.INV_OUT_BILL_LINE_ID, iobl.VERIFERED_NUMBER, iobl.CAN_VERIFER_NUMBER,
         vid.THIS_VERIFER_NUMBER, vid.VERIFER_INVOICE_DETAILS_ID,
         iobl.VERIFERED_NUMBER - vid.THIS_VERIFER_NUMBER AS 取消后已核销数量
  FROM EPM_VERIFER_INVOICE_DETAILS vid
  JOIN INV_OUT_BILL_LINE iobl ON vid.INV_OUT_BILL_LINE_ID = iobl.INV_OUT_BILL_LINE_ID
  WHERE vid.VERIFER_INVOICE_DETAILS_ID IN (:idList)
    AND iobl.VERIFERED_NUMBER - vid.THIS_VERIFER_NUMBER < 0
  -- 查出取消后已核销数量<0的异常数据
  ```

#### 报错13：请选择核销类型
- **触发条件**：查询出库单(doSearchOutbillReport)或查询发票信息(doGetInvoiceInfo)时，未选择核销类型(VERIFER_TYPE为空)
- **逻辑分析**：doSearchOutbillReport和doGetInvoiceInfo方法中校验veriferType非空，因核销类型决定查询出库单的SQL逻辑(工程方queryProjectOutbillReportForInvoiceTruth/经销商方queryDealerOutbillReportForInvoiceTruth)。需先选择核销类型再查询
- **排查SQL**：
  ```sql
  SELECT ith.INVOICE_TRUTH_ID, ith.INVOICE_TRUTH_NO, ith.VERIFER_TYPE
  FROM EPM_INVOICE_TRUTH_HEADER ith
  WHERE ith.VERIFER_TYPE IS NULL
  -- 查出核销类型为空的核销单
  ```

#### 报错14：核销出库单明细不可为空
- **触发条件**：提交审批时，核销单下无核销行数据(EPM_INVOICE_TRUTH_LINE为空)
- **逻辑分析**：checkWorkFlowData方法中按INVOICE_TRUTH_ID查询核销行列表，若为空则抛出阻断性报错。需先添加核销行再提交
- **排查SQL**：
  ```sql
  SELECT ith.INVOICE_TRUTH_ID, ith.INVOICE_TRUTH_NO, ith.HZ_APPROVE_STATUS,
         (SELECT COUNT(*) FROM EPM_INVOICE_TRUTH_LINE itl
          WHERE itl.INVOICE_TRUTH_ID = ith.INVOICE_TRUTH_ID) AS 核销行数
  FROM EPM_INVOICE_TRUTH_HEADER ith
  WHERE ith.INVOICE_TRUTH_ID = :invoiceTruthId
  -- 若核销行数为0，则触发该报错
  ```

#### 报错15：本次核销数量不能大于可核销数量
- **触发条件**：保存核销时，核销行下所有发票明细的本次核销数量合计超过出库单行可核销数量(CAN_VERIFY_NUM)
- **逻辑分析**：checkVeriferInvoice方法中累计核销行下所有发票明细的本次核销数量(sumThisVeriferNumber)，若超过出库单行可核销数量(canVerifyNum)则抛出阻断性报错。与"核销数量超过剩余可核销数量"不同，该校验针对单行发票明细，本校验针对核销行合计
- **排查SQL**：
  ```sql
  SELECT itl.INVOICE_TRUTH_LINE_ID, itl.INVBILLNO, itl.ITEM_CODE,
         itl.CAN_VERIFER_NUMBER, itl.SUR_VERIFY_NUM,
         (SELECT SUM(vid.THIS_VERIFER_NUMBER) FROM EPM_VERIFER_INVOICE_DETAILS vid
          WHERE vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID) AS 合计核销数量
  FROM EPM_INVOICE_TRUTH_LINE itl
  WHERE (SELECT SUM(vid.THIS_VERIFER_NUMBER) FROM EPM_VERIFER_INVOICE_DETAILS vid
         WHERE vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID) > itl.SUR_VERIFY_NUM
  -- 查出合计核销数量超过可核销数量的核销行
  ```

#### 报错16：对应的出库单明细不存在
- **触发条件**：提交审批时，核销行关联的出库单行(INV_OUT_BILL_LINE_ID)不存在或为空
- **逻辑分析**：checkWorkFlowData方法中遍历核销行，若invOutBillLineId为空则收集错误信息并抛出阻断性报错。需确认出库单行有效性
- **排查SQL**：
  ```sql
  SELECT itl.INVOICE_TRUTH_LINE_ID, itl.INVBILLNO, itl.ITEM_CODE, itl.INV_OUT_BILL_LINE_ID
  FROM EPM_INVOICE_TRUTH_LINE itl
  WHERE itl.INVOICE_TRUTH_ID = :invoiceTruthId
    AND (itl.INV_OUT_BILL_LINE_ID IS NULL
         OR NOT EXISTS (SELECT 1 FROM INV_OUT_BILL_LINE iobl
                        WHERE iobl.INV_OUT_BILL_LINE_ID = itl.INV_OUT_BILL_LINE_ID))
  -- 查出出库单行不存在的核销行
  ```

#### 报错17：对应的出库单明细可核销数量不足
- **触发条件**：提交审批时，出库单行可核销数量(selectCanVerifyNumOfLine)<0
- **逻辑分析**：checkWorkFlowData方法中按invOutBillLineId查询出库单行可核销数量，若<0则收集错误信息并抛出阻断性报错。可能原因：并发核销导致可核销数量被占用。需检查出库单行可核销数量
- **排查SQL**：
  ```sql
  SELECT iobl.INV_OUT_BILL_LINE_ID, iobl.INV_BILL_NO, iobl.ITEM_CODE,
         iobl.CAN_VERIFY_NUM, iobl.USED_VERIFY_NUM,
         iobl.CAN_VERIFY_NUM - NVL(iobl.USED_VERIFY_NUM, 0) AS 剩余可核销数量
  FROM INV_OUT_BILL_LINE iobl
  WHERE iobl.INV_OUT_BILL_LINE_ID IN (
    SELECT itl.INV_OUT_BILL_LINE_ID FROM EPM_INVOICE_TRUTH_LINE itl
    WHERE itl.INVOICE_TRUTH_ID = :invoiceTruthId
  )
    AND iobl.CAN_VERIFY_NUM - NVL(iobl.USED_VERIFY_NUM, 0) < 0
  -- 查出可核销数量不足的出库单行
  ```

#### 报错18：发票核销数量超过剩余可核销数量
- **触发条件**：提交审批时，发票明细的本次核销数量合计超过发票明细剩余可核销数量(SURPLUS_CAN_VERIFER_NUMBER<0)
- **逻辑分析**：checkWorkFlowData方法中按发票明细ID汇总本次核销数量，查询发票明细剩余可核销数量，若剩余可核销数量<0则收集错误信息并抛出阻断性报错。需调整核销数量
- **排查SQL**：
  ```sql
  SELECT uid.INVOICE_DETAILS_ID, uid.INVOICE_NUMBER, uid.SERVICES_NAME,
         uid.SURPLUS_CAN_VERIFER_NUMBER,
         (SELECT SUM(vid.THIS_VERIFER_NUMBER) FROM EPM_VERIFER_INVOICE_DETAILS vid
          WHERE vid.INVOICE_DETAILS_ID = uid.INVOICE_DETAILS_ID) AS 本次核销合计
  FROM EPM_UPLOAD_INVOICE_DETAILS uid
  WHERE uid.INVOICE_DETAILS_ID IN (
    SELECT vid.INVOICE_DETAILS_ID FROM EPM_VERIFER_INVOICE_DETAILS vid
    JOIN EPM_INVOICE_TRUTH_LINE itl ON vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID
    WHERE itl.INVOICE_TRUTH_ID = :invoiceTruthId
  )
    AND uid.SURPLUS_CAN_VERIFER_NUMBER < 0
  -- 查出剩余可核销数量<0的发票明细
  ```

### 常见问题

**Q1: 真实性核销凭证号如何生成？**
A: 通过编码规则AE.INVOICE_CANCELLATION_UPLOAD自动生成(RuleCodeEnum.INVOICE_CANCELLATION_UPLOAD_NO)。核销单号通过AE.INVOICE_AUTHENTICITY_VERIFI(工程)/AE.JZ_INVOICE_AUTHENTICITY_VER(家装)生成。

**Q2: 取消核销的五种类型区别？**
A: invoice=取消整张发票，invoiceDetail=取消单条发票明细，invLine=取消出库单行核销，veriferDetail=取消核销明细行，obsInvoice=作废发票。不同类型影响的数据范围不同，取消时均更新EFFECT_STATUS为canceled并记录取消信息。

**Q3: 三性校验是什么？**
A: 发票购买方与工程采购单位名称一致性(UNIT_NAME_IS_AGREEMENT)、发票销售方与经销商法人名称一致性(BILLING_NAME_IS_AGREEMENT)、发票日期是否在发货日期之前(DATE_IS_AGREEMENT)，用于验证发票真实性。

**Q4: 取消核销后出库单行可核销数量未回加**
- **原因**：updateClData中invLine为空(无审批通过的核销明细)，或分布式锁获取失败
- **处理**：
```sql
SELECT vid.VERIFER_INVOICE_DETAILS_ID, vid.EFFECT_STATUS, ith.HZ_APPROVE_STATUS
FROM EPM_VERIFER_INVOICE_DETAILS vid
JOIN EPM_INVOICE_TRUTH_LINE itl ON vid.INVOICE_TRUTH_LINE_ID = itl.INVOICE_TRUTH_LINE_ID
JOIN EPM_INVOICE_TRUTH_HEADER ith ON ith.INVOICE_TRUTH_ID = itl.INVOICE_TRUTH_ID
WHERE vid.EFFECT_STATUS IN ('invalid','valid')
  AND ith.HZ_APPROVE_STATUS IN ('NEW','RUN','APPROVED');
```

**Q5: 审批通过后出库单行核销数量未更新**
- **原因**：wfComplete回调未正确执行，或核销明细有效状态更新失败
- **处理**：检查EPM_VERIFER_INVOICE_DETAILS的EFFECT_STATUS是否为valid，检查INV_OUT_BILL_LINE的已核销数量/可核销数量

**Q6: 单位转换率的作用？**
A: UOM_RATE用于计量单位转换，当发票明细单位与核销单位不一致时，通过转换率计算转化后剩余可核销数量(CONVERT_SUR_VERIFER_NUMBER=剩余可核销数量×UOM_RATE)，保证核销数量准确。

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-09-23 | - | - | 初始版本，工程真实性核销功能 |
| 2026-07-31 | - | - | 初始生成知识库文档 |
| 2026-08-30 | - | CodeArts | 按skill规范重写：移除网站内容补充/业务是什么/权限控制/历史排查记录章节、章节去编号、界面模块表格改为6列、上游依赖改为4列、下游影响改为bullet points、数据库表改为5列、报错表改为5列、新增明细字段列表/选择弹窗/导入/提交校验章节、合并6个重点逻辑和22个后端接口 |
