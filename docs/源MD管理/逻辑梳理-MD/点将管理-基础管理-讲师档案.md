# 点将管理-基础管理-讲师档案

> 后端代码位于 mbo-business 微服务（源码未包含在当前源码目录下），前端 API 路径前缀为 `mlt/maLecturerArchive/*`。本文档基于前端代码与接口契约梳理，后端实现以微服务实际代码为准。

## 业务流程

### 业务流程图

```
讲师档案管理(本页面)
        |
        ├──→ 新建 ──→ 跳转详情页(/detail/null/add)
        ├──→ 编辑 ──→ 跳转详情页(/detail/{id}/edit)
        ├──→ 删除 ──→ 确认弹窗 ──→ 调用 batchDelete 接口
        ├──→ 提交 ──→ 调用 submitData 接口 ──→ 进入审批流程
        ├──→ 牌价变更申请 ──→ 校验状态 ──→ 跳转详情页(/detail/{id}/priceChange)
        ├──→ 查看审批 ──→ 弹窗展示审批历史列表
        ├──→ 生效/失效 ──→ 校验审批通过 ──→ 调用 updateArchivesStatus 接口
        ├──→ 查看日程 ──→ 弹窗展示 LecturerCalendar 日历
        └──→ 查看饱和度 ──→ 弹窗展示 SaturationForm 饱和度表单
```

### 全链路流程图

```
开始 → ★讲师档案创建★ → 提交审批 → ⚖审批通过？ → 档案生效 → 结束（拒绝退回修改重提）
```

**维护原则**：档案在审批中期间不可编辑，只有审批通过、状态生效的讲师才允许被排期与点将选用。

### 双 Tab 页签架构

| Tab 页签 | pageType | 说明 | 可见性 |
|---------|---------|------|--------|
| 个人档案（personal） | archives | 讲师个人基本信息（姓名、手机号、邮箱、所属组织、身份证号等） | 始终可见 |
| 讲师档案（other） | lecturerType（讲师类型值） | 讲师资质与业务信息（讲师类型、等级、价格等） | 权限 `list_other` 控制 |

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| HZERO 权限服务 | 服务依赖 | 提供按钮权限校验、讲师档案 Tab 可见性 | 用户已登录并分配权限编码 |
| HZERO 值集服务 | 服务依赖 | 提供讲师类型、档案状态、审批状态、讲师等级等值集 | 值集编码已配置（MBO.LECTURER_TYPE、MBO.ARCHIVES_STATUS、MBO.APPROVAL_STATUS、MBO.TRAIN/ACTIVITY/DESIGN_LECTURER_LEVEL） |
| 事业部数据 | 数据依赖 | 提供负责事业部下拉选项与树形结构 | 事业部主档已维护 |
| 组织主档 | 数据依赖 | 提供讲师所属组织信息 | 组织主档已维护 |

### 下游影响

- **讲师档案详情页**：新建/编辑/牌价变更跳转，openTab 新标签页打开
- **档案审批**：提交后进入审批流程，状态流转
- **讲师排期**：查看日程展示排期日历，LecturerCalendar 组件
- **讲师饱和度**：查看饱和度展示饱和度信息，SaturationForm 组件
- **点将申请**：讲师档案作为点将申请的被点将人来源，数据库关联

## 重点逻辑

### 重点逻辑1：Tab 页签切换

- **业务意义**：分离个人基本信息与讲师业务信息，按权限控制可见性
- **具体逻辑描述**：
  - **个人档案（personal）**：始终可见，查询 `pageType=archives`
  - **讲师档案（other）**：根据权限 `hzero.general_manage.base_manage.lecturer_profile.ps.list_other` 控制可见性，查询时 `pageType=lecturerType`（讲师类型值）
  - 切换 Tab 时重新创建 DataSet 并查询
  - 初始化时调用 `getLecturerTypeList` 接口获取讲师类型列表，过滤值集 `MBO.LECTURER_TYPE`，自动设置默认讲师类型

### 重点逻辑2：讲师级别动态值集

- **业务意义**：不同业务方向的讲师等级独立管控，避免等级混淆
- **具体逻辑描述**：
  - 讲师级别（lecturerLevel）根据讲师类型（lecturerType）动态切换值集
  - `train`（培训）→ `MBO.TRAIN_LECTURER_LEVEL`
  - `activity`（活动）→ `MBO.ACTIVITY_LECTURER_LEVEL`
  - `design`（设计）→ `MBO.DESIGN_LECTURER_LEVEL`
  - 其他 → `MBO.TRAIN_LECTURER_LEVEL`（默认）

### 重点逻辑3：牌价变更申请校验

- **业务意义**：仅对已生效且非特训营的档案允许发起牌价变更，保证变更对象合法
- **具体逻辑描述**：
  1. 档案状态必须为 `valid`（生效）
  2. 培训类型不能为 `camp`（特训营）
  3. 校验不通过提示"只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！"
  4. 校验通过跳转 `/detail/{id}/priceChange`

### 重点逻辑4：生效/失效操作校验

- **业务意义**：仅对审批通过的档案允许状态切换，保证状态变更合规
- **具体逻辑描述**：
  1. 审批状态必须为 `approved`（审批通过）
  2. 校验不通过提示"只能对【审批通过】的档案进行操作！"
  3. 校验通过调用 `POST mlt/maLecturerArchive/updateArchivesStatus` 接口，参数 `ArchivesStatus=valid/invalid`

### 重点逻辑5：查看审批历史

- **业务意义**：提供审批过程可追溯性，便于查询档案审批轨迹
- **具体逻辑描述**：
  - 点击"查看审批"按钮，弹窗展示审批历史列表
  - 弹窗宽度 1000px，仅关闭按钮
  - 审批列表通过 `lecturerApprovalApi.list` 接口查询，参数 `lecturerArchivesCode`
  - 展示字段：序号、时间、申请类型、审批人、审批结果、审批意见

### 重点逻辑6：内置审批机制

- **业务意义**：档案与价格上线前统一把关，保证数据质量与成本可控
- **具体逻辑描述**：
  - 提交审批调用内置审批接口 `mlt/maLecturerApproval/*`
  - 审批状态通过值集 `MBO.APPROVAL_STATUS` 控制
  - 提交后档案状态变更为"审批中"，不可编辑
  - 审批结果返回后自动更新档案状态

## 详细逻辑

### 列表页

- **路由**：`/general/base/lecturerProfile/list`
- **分页 API**：`POST mlt/maLecturerArchive/page`
- **查询条件**：讲师姓名、讲师类型（activeTab=other 时显示）、负责事业部、档案状态
- **列表字段**：讲师档案编码、讲师姓名、讲师类型、审批状态、档案状态、级别、报价、所属部门、负责事业部、负责区域、标签、档案更新时间、最后修改人
- **操作按钮**：新建、编辑、删除、提交、牌价变更申请、查看审批、生效、失效、查看日程、查看饱和度

### 详情页

- **路由**：`/general/base/lecturerProfile/detail/:id/:type`
  - `:id` — 计师档案ID（lecturerArchivesId）
  - `:type` — 操作类型（new/edit/view）
- **API**：
  - 查询详情：`GET mlt/maLecturerArchive/detail`
  - 编辑保存：`POST mlt/maLecturerArchive/edit`
  - 保存：`POST mlt/maLecturerArchive/saveData`
  - 提交：`POST mlt/maLecturerArchive/submitData`

### 个人档案 Tab

- 维护讲师个人基本信息
- 字段：讲师姓名、性别、手机号、邮箱、身份证号、所属组织、入职日期等
- 保存时校验必填字段

### 讲师档案 Tab

- 维护讲师资质与业务信息
- 字段：讲师类型（MBO.LECTURER_TYPE）、培训讲师等级、活动讲师等级、设计讲师等级、讲师价格、资质证书等
- 档案状态（MBO.ARCHIVES_STATUS）：草稿/审批中/生效/失效
- 保存时校验讲师类型和等级的合法性

### 审批提交逻辑

1. 校验档案信息完整性（必填字段、等级信息）
2. 生成审批单，关联 `lecturerArchivesCode`
3. 调用 `POST mlt/maLecturerArchive/submitData` 提交
4. 更新档案状态为"审批中"
5. 审批结果回调后更新档案状态：
   - 通过 → 状态变为"生效"
   - 拒绝 → 状态变为"审批拒绝"，允许修改重新提交

### 界面模块

#### 查询栏字段

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 讲师姓名 | LECTURER_NAME | TextField | 讲师姓名模糊查询 | 始终显示 | 手动输入 |
| 讲师类型 | LECTURER_TYPE | Select | 讲师类型筛选 | activeTab=other | 动态加载 getLecturerTypeList 过滤值集 MBO.LECTURER_TYPE |
| 负责事业部 | RESPONSIBLE_DEPARTMENT_CODE | Select | 事业部筛选 | 始终显示 | divisionDS 数据源（getDivisionList/getDivisionTree） |
| 档案状态 | ARCHIVES_STATUS | Select | 档案状态筛选 | 始终显示 | 值集 MBO.ARCHIVES_STATUS |

#### 列表展示字段

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 讲师档案编码 | LECTURER_ARCHIVES_CODE | TextField | 档案唯一编码 | 始终显示 | 左锁定列 |
| 讲师姓名 | LECTURER_NAME | TextField | 讲师姓名 | 始终显示 | 左锁定列 |
| 讲师类型 | LECTURER_TYPE | Select | 讲师类型 | 始终显示 | 值集 MBO.LECTURER_TYPE |
| 审批状态 | APPROVAL_STATUS | Select | 档案审批状态 | 始终显示 | 值集 MBO.APPROVAL_STATUS |
| 档案状态 | ARCHIVES_STATUS | Select | 档案生效状态 | 始终显示 | 值集 MBO.ARCHIVES_STATUS |
| 级别 | LECTURER_LEVEL | Select | 讲师级别 | 始终显示 | 动态值集，根据讲师类型切换 |
| 报价 | PRICE | NumberField | 讲师点将牌价 | 始终显示 | 后端返回 |
| 所属部门 | ORG_NAME | TextField | 讲师所属部门 | 始终显示 | 后端返回 |
| 负责事业部 | RESPONSIBLE_DEPARTMENT_NAME | TextField | 负责事业部名称 | 始终显示 | 后端返回 |
| 负责区域 | COVER_AREA | TextField | 讲师负责区域 | 始终显示 | 后端返回 |
| 标签 | LECTURER_LABEL | TextField | 讲师标签 | 始终显示 | 后端返回 |
| 档案更新时间 | UPDATE_DATE | TextField | 最后更新时间 | 始终显示 | 后端返回 |
| 最后修改人 | UPDATE_NAME | TextField | 最后修改人姓名 | 始终显示 | 后端返回 |
| 操作 | - | Button(link) | 查看详情按钮 | 始终显示 | 右锁定列，跳转详情页 |

### 选择弹窗

#### 弹窗1：查看审批历史（单选）

- **触发**：点击"查看审批"按钮，选择一条数据
- **入参**：

| 参数名 | 来源 | 说明 |
|--------|------|------|
| lecturerArchivesCode | 选中行 | 讲师档案编码，用于查询审批历史 |

- **查询SQL**：
  ```sql
  SELECT
    lap.APPROVAL_TYPE        AS 申请类型,
    TO_CHAR(lap.SUBMIT_DATE, 'YYYY-MM-DD HH24:MI:SS') AS 时间,
    lap.APPROVER             AS 审批人,
    lap.APPROVAL_RESULT      AS 审批结果,
    lap.APPROVAL_REMARK      AS 审批意见
  FROM MA_LECTURER_APPROVAL lap
  WHERE lap.LECTURER_ARCHIVES_CODE = :lecturerArchivesCode
  ORDER BY lap.SUBMIT_DATE DESC;
  ```
- **展示字段**：序号、时间、申请类型、审批人、审批结果、审批意见
- **弹窗宽度**：1000px，仅关闭按钮

#### 弹窗2：查看日程（单选）

- **触发**：点击"查看日程"按钮，选择一条数据
- **入参**：lecturerArchivesCode（讲师档案编码）
- **展示**：LecturerCalendar 日历组件，展示该讲师排期

#### 弹窗3：查看饱和度（单选）

- **触发**：点击"查看饱和度"按钮，选择一条数据
- **入参**：lecturerArchivesCode（讲师档案编码）
- **展示**：SaturationForm 饱和度表单组件

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新建 | 跳转新建详情页 | 列表表头 | 始终显示 | 跳转 `/detail/null/add` |
| 编辑 | 跳转编辑详情页 | 列表表头 | 已选择一条数据 | 跳转 `/detail/{id}/edit` |
| 删除 | 批量删除档案 | 列表表头 | 已选择至少一条数据 | 调用 batchDelete 接口 |
| 提交 | 提交档案审批 | 列表表头 | 已选择一条数据 | 调用 submitData 接口，进入审批流程 |
| 牌价变更申请 | 跳转牌价变更详情页 | 列表表头 | 已选择一条数据且状态生效且非特训营 | 跳转 `/detail/{id}/priceChange` |
| 查看审批 | 弹窗展示审批历史 | 列表表头 | 已选择一条数据 | 弹窗展示审批列表 |
| 生效 | 设置档案生效 | 列表表头 | 已选择一条数据且审批通过 | 调用 updateArchivesStatus 设置 valid |
| 失效 | 设置档案失效 | 列表表头 | 已选择一条数据且审批通过 | 调用 updateArchivesStatus 设置 invalid |
| 查看日程 | 弹窗展示排期日历 | 列表表头 | 已选择一条数据 | 弹窗 LecturerCalendar |
| 查看饱和度 | 弹窗展示饱和度 | 列表表头 | 已选择一条数据 | 弹窗 SaturationForm |

#### 按钮1：新建（列表表头）

- **可点击条件**：始终可点击
- **业务逻辑**：跳转新建详情页 `/detail/null/add`
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.save`

#### 按钮2：编辑（列表表头）

- **可点击条件**：已选择一条数据；未选择或多选时提示"请选择一条数据"
- **业务逻辑**：跳转编辑详情页 `/detail/{id}/edit`，仅草稿/审批拒绝状态可编辑
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.edit`

#### 按钮3：删除（列表表头）

- **可点击条件**：已选择至少一条数据；未选择时提示"请选择至少一条数据"
- **业务逻辑**：弹出确认弹窗 → 确认后调用 `POST mlt/maLecturerArchive/batchDelete`，仅草稿状态可删除
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.delete`

#### 按钮4：提交（列表表头）

- **可点击条件**：已选择一条数据
- **业务逻辑**：调用 `POST mlt/maLecturerArchive/submitData` 提交审批，档案状态变更为"审批中"
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.submit`

#### 按钮5：牌价变更申请（列表表头）

- **可点击条件**：已选择一条数据；校验档案状态=valid 且培训类型≠camp，否则提示"只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！"
- **业务逻辑**：跳转牌价变更详情页 `/detail/{id}/priceChange`
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.price_change`

#### 按钮6：查看审批（列表表头）

- **可点击条件**：已选择一条数据
- **业务逻辑**：弹窗展示审批历史列表，通过 lecturerApprovalApi.list 查询
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.show_approval`

#### 按钮7：生效（列表表头）

- **可点击条件**：已选择一条数据；校验审批状态=approved，否则提示"只能对【审批通过】的档案进行操作！"
- **业务逻辑**：调用 `POST mlt/maLecturerArchive/updateArchivesStatus`，参数 ArchivesStatus=valid
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.validate`

#### 按钮8：失效（列表表头）

- **可点击条件**：已选择一条数据；校验审批状态=approved，否则提示"只能对【审批通过】的档案进行操作！"
- **业务逻辑**：调用 `POST mlt/maLecturerArchive/updateArchivesStatus`，参数 ArchivesStatus=invalid
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.invalid`

#### 按钮9：查看日程（列表表头）

- **可点击条件**：已选择一条数据
- **业务逻辑**：弹窗展示 LecturerCalendar 日历组件，传入 lecturerArchivesCode
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.show_schedule`

#### 按钮10：查看饱和度（列表表头）

- **可点击条件**：已选择一条数据
- **业务逻辑**：弹窗展示 SaturationForm 饱和度表单组件，传入 lecturerArchivesCode
- **权限编码**：`hzero.general_manage.base_manage.lecturer_profile.ps.show_saturation`

### 保存/提交校验

- 校验1：baseFormDS.validate() 必填项校验 —— 保证档案基本信息完整
  - **详细逻辑**：必填项包括讲师名称、讲师类型、所属部门、负责事业部、负责区域、讲师标签；培训类型在讲师类型为 train 时必填
  - **系统体现**：前端表单校验，未通过阻止保存
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME, LECTURER_TYPE, ORG_NAME,
           RESPONSIBLE_DEPARTMENT_NAME, COVER_AREA, LECTURER_LABEL
    FROM MA_LECTURER_ARCHIVE
    WHERE LECTURER_NAME IS NULL OR LECTURER_TYPE IS NULL OR ORG_NAME IS NULL
       OR RESPONSIBLE_DEPARTMENT_NAME IS NULL OR COVER_AREA IS NULL
       OR LECTURER_LABEL IS NULL;
    ```
- 校验2：讲师简历非空 —— 保证讲师资质信息完整
  - **详细逻辑**：保存时校验讲师简历字段非空
  - **系统体现**：前端校验，提示补充简历
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME
    FROM MA_LECTURER_ARCHIVE
    WHERE RESUME IS NULL OR RESUME = '';
    ```
- 校验3：讲师案例资料非空 —— 保证讲师案例信息完整
  - **详细逻辑**：保存时校验讲师案例资料字段非空
  - **系统体现**：前端校验，提示补充案例资料
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME
    FROM MA_LECTURER_ARCHIVE
    WHERE CASE_INFO IS NULL OR CASE_INFO = '';
    ```
- 校验4：讲师照片非空 —— 保证讲师身份可视化
  - **详细逻辑**：保存时校验讲师照片字段非空
  - **系统体现**：前端校验，提示上传照片
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME
    FROM MA_LECTURER_ARCHIVE
    WHERE PHOTO IS NULL OR PHOTO = '';
    ```
- 校验5：牌价变更申请时档案状态必须为 valid —— 仅生效档案可发起变更
  - **详细逻辑**：点击牌价变更申请按钮时校验 ARCHIVES_STATUS=valid
  - **系统体现**：前端校验，提示"只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！"
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME, ARCHIVES_STATUS, TRAIN_TYPE
    FROM MA_LECTURER_ARCHIVE
    WHERE ARCHIVES_STATUS <> 'valid' OR TRAIN_TYPE = 'camp';
    ```
- 校验6：牌价变更申请时培训类型不能为 camp —— 特训营讲师不适用牌价变更
  - **详细逻辑**：点击牌价变更申请按钮时校验 TRAIN_TYPE≠camp
  - **系统体现**：前端校验，提示信息同上
  - **排查SQL**：同校验5
- 校验7：生效/失效时审批状态必须为 approved —— 仅审批通过档案可切换状态
  - **详细逻辑**：点击生效/失效按钮时校验 APPROVAL_STATUS=approved
  - **系统体现**：前端校验，提示"只能对【审批通过】的档案进行操作！"
  - **排查SQL**：
    ```sql
    SELECT LECTURER_ARCHIVES_CODE, LECTURER_NAME, APPROVAL_STATUS, ARCHIVES_STATUS
    FROM MA_LECTURER_ARCHIVE
    WHERE APPROVAL_STATUS <> 'approved' AND ARCHIVES_STATUS IN ('valid', 'invalid');
    ```

### 状态机

```
草稿(draft) ──提交──→ 审批中(approving) ──审批通过──→ 审批通过(approved)
                                          ──审批拒绝──→ 审批拒绝(reject) ──重新提交──→ 审批中(approving)
审批通过(approved) ──生效──→ 生效(valid) ──失效──→ 失效(invalid) ──生效──→ 生效(valid)
```

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|-------------|
| 草稿（draft） | 档案新建未提交 | 编辑、删除、提交 |
| 审批中（approving） | 已提交待审批 | 无（锁定不可编辑） |
| 审批通过（approved） | 审批通过待生效 | 生效、失效 |
| 审批拒绝（reject） | 审批拒绝 | 编辑、提交（重新提交） |
| 生效（valid） | 档案生效，可被点将选用 | 失效、牌价变更申请 |
| 失效（invalid） | 档案失效，不可被点将选用 | 生效 |

### 值集依赖

| 值集编码 | 用途 | 使用位置 |
|---------|------|---------|
| MBO.LECTURER_TYPE | 讲师类型 | 查询栏讲师类型、列表讲师类型列、详情页讲师类型字段 |
| MBO.ARCHIVES_STATUS | 档案状态 | 查询栏档案状态、列表档案状态列、详情页状态显示 |
| MBO.APPROVAL_STATUS | 审批状态 | 列表审批状态列 |
| MBO.TRAIN_LECTURER_LEVEL | 培训讲师等级 | 详情页培训讲师等级字段 |
| MBO.ACTIVITY_LECTURER_LEVEL | 活动讲师等级 | 详情页活动讲师等级字段 |
| MBO.DESIGN_LECTURER_LEVEL | 设计讲师等级 | 详情页设计讲师等级字段 |

### API 清单

| API 路径 | 方法 | 说明 |
|---------|------|------|
| mlt/maLecturerArchive/page | POST | 分页查询讲师档案列表 |
| mlt/maLecturerArchive/batchDelete | POST | 批量删除讲师档案 |
| mlt/maLecturerArchive/detail | GET | 查询讲师档案详情 |
| mlt/maLecturerArchive/edit | POST | 编辑保存讲师档案 |
| mlt/maLecturerArchive/submitData | POST | 提交讲师档案审批 |
| mlt/maLecturerArchive/saveData | POST | 保存讲师档案 |
| mlt/maLecturerArchive/updateArchivesStatus | POST | 更新档案状态（生效/失效） |
| mlt/maLecturerArchive/getLecturerTypeList | GET | 获取讲师类型列表 |
| mlt/trainCamp/getDivisionList | GET | 获取事业部列表 |
| mlt/trainCamp/getDivisionTree | GET | 获取事业部树 |

### 导入

不支持导入功能。

## 数据库表详解

### MA_LECTURER_ARCHIVE（讲师档案主表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| LECTURER_ARCHIVES_ID | VARCHAR2 | 主键ID | - | 主键，序列自增 |
| LECTURER_ARCHIVES_CODE | VARCHAR2(64) | 档案编码 | 讲师档案编码 | 系统生成，唯一 |
| LECTURER_NAME | VARCHAR2(255) | 讲师姓名 | 讲师姓名 | 录入时填写 |
| LECTURER_TYPE | VARCHAR2(32) | 讲师类型 | 讲师类型 | 值集 MBO.LECTURER_TYPE |
| ARCHIVES_STATUS | VARCHAR2(32) | 档案状态 | 档案状态 | 值集 MBO.ARCHIVES_STATUS |
| APPROVAL_STATUS | VARCHAR2(32) | 审批状态 | 审批状态 | 值集 MBO.APPROVAL_STATUS |
| TRAIN_LECTURER_LEVEL | VARCHAR2(32) | 培训讲师等级 | - | 值集 MBO.TRAIN_LECTURER_LEVEL |
| ACTIVITY_LECTURER_LEVEL | VARCHAR2(32) | 活动讲师等级 | - | 值集 MBO.ACTIVITY_LECTURER_LEVEL |
| DESIGN_LECTURER_LEVEL | VARCHAR2(32) | 设计讲师等级 | - | 值集 MBO.DESIGN_LECTURER_LEVEL |
| LECTURER_LEVEL | VARCHAR2(32) | 讲师级别 | 级别 | 动态值集，根据讲师类型切换 |
| LECTURER_PRICE | NUMBER | 讲师价格 | - | 录入时填写 |
| PRICE | NUMBER(18,2) | 报价/牌价 | 报价 | 录入时填写 |
| PHONE | VARCHAR2 | 手机号 | - | 录入时填写 |
| EMAIL | VARCHAR2 | 邮箱 | - | 录入时填写 |
| ID_CARD | VARCHAR2 | 身份证号 | - | 录入时填写 |
| ORGANIZATION_ID | VARCHAR2 | 所属组织ID | - | 录入时选择 |
| ORGANIZATION_NAME | VARCHAR2 | 所属组织名称 | - | 录入时选择 |
| ORG_NAME | VARCHAR2(255) | 所属部门 | 所属部门 | 录入时选择 |
| RESPONSIBLE_DEPARTMENT_CODE | VARCHAR2(64) | 负责事业部编码 | 负责事业部 | 录入时选择 |
| RESPONSIBLE_DEPARTMENT_NAME | VARCHAR2(255) | 负责事业部名称 | 负责事业部 | 关联事业部 |
| COVER_AREA | VARCHAR2(2000) | 负责区域 | 负责区域 | 录入时填写 |
| LECTURER_LABEL | VARCHAR2(2000) | 标签 | 标签 | 录入时填写 |
| TRAIN_TYPE | VARCHAR2(32) | 培训类型 | - | 区分特训营（camp）等 |
| RESUME | VARCHAR2 | 讲师简历 | - | 录入时填写，保存时校验非空 |
| CASE_INFO | VARCHAR2 | 案例资料 | - | 录入时填写，保存时校验非空 |
| PHOTO | VARCHAR2 | 讲师照片 | - | 录入时上传，保存时校验非空 |
| UPDATE_DATE | TIMESTAMP | 档案更新时间 | 档案更新时间 | 系统自动 |
| UPDATE_NAME | VARCHAR2(255) | 最后修改人 | 最后修改人 | 系统自动 |
| CREATED_BY | VARCHAR2 | 创建人 | - | 系统自动 |
| CREATION_DATE | DATE | 创建时间 | - | 系统自动 |
| LAST_UPDATED_BY | VARCHAR2 | 最后更新人 | - | 系统自动 |
| LAST_UPDATE_DATE | DATE | 最后更新时间 | - | 系统自动 |
| OBJECT_VERSION_NUMBER | NUMBER | 乐观锁版本号 | - | 框架自动维护 |

### MA_LECTURER_APPROVAL（讲师审批历史表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| LECTURER_APPROVAL_ID | VARCHAR2 | 主键ID | - | 主键，序列自增 |
| LECTURER_ARCHIVES_CODE | VARCHAR2(64) | 关联档案编码 | - | 外键关联 MA_LECTURER_ARCHIVE |
| APPROVAL_TYPE | VARCHAR2(32) | 审批类型/申请类型 | 申请类型 | 值集 MBO.APPROVAL_TYPE |
| APPROVAL_STATUS | VARCHAR2(32) | 审批状态 | - | 值集 MBO.APPROVAL_STATUS |
| SUBMIT_DATE | DATE | 提交时间 | 时间 | 系统自动 |
| APPROVER | VARCHAR2(255) | 审批人 | 审批人 | 系统自动 |
| APPROVAL_DATE | DATE | 审批时间 | - | 系统自动 |
| APPROVAL_RESULT | VARCHAR2(32) | 审批结果 | 审批结果 | 值集 MBO.APPROVAL_RESULT |
| APPROVAL_REMARK | VARCHAR2(2000) | 审批备注/审批意见 | 审批意见 | 审批时填写 |
| CREATED_BY | VARCHAR2 | 创建人 | - | 系统自动 |
| CREATION_DATE | DATE | 创建时间 | - | 系统自动 |

### 排查SQL

```sql
-- 查询讲师档案列表
SELECT
  la.LECTURER_ARCHIVES_CODE AS 讲师档案编码,
  la.LECTURER_NAME     AS 讲师姓名,
  la.LECTURER_TYPE     AS 讲师类型,
  la.APPROVAL_STATUS   AS 审批状态,
  la.ARCHIVES_STATUS   AS 档案状态,
  la.LECTURER_LEVEL    AS 级别,
  la.PRICE             AS 报价,
  la.ORG_NAME          AS 所属部门,
  la.RESPONSIBLE_DEPARTMENT_NAME AS 负责事业部,
  la.COVER_AREA        AS 负责区域
FROM MA_LECTURER_ARCHIVE la
ORDER BY la.UPDATE_DATE DESC;

-- 查询可发起牌价变更的讲师档案
SELECT
  la.LECTURER_ARCHIVES_CODE AS 讲师档案编码,
  la.LECTURER_NAME     AS 讲师姓名,
  la.PRICE             AS 当前报价
FROM MA_LECTURER_ARCHIVE la
WHERE la.ARCHIVES_STATUS = 'valid'
  AND la.TRAIN_TYPE <> 'camp'
  AND la.APPROVAL_STATUS = 'approved';

-- 查询审批未通过的档案
SELECT
  la.LECTURER_ARCHIVES_CODE AS 讲师档案编码,
  la.LECTURER_NAME     AS 讲师姓名,
  la.APPROVAL_STATUS   AS 审批状态
FROM MA_LECTURER_ARCHIVE la
WHERE la.APPROVAL_STATUS IN ('to_be_submit', 'approving', 'reject');
```

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|---------------|------|---------|
| 请选择一条数据 | 单选操作按钮 | 未选择或选择多行；选择一条数据后重试 | 提示 | 前端校验选中行数量=1 |
| 请选择至少一条数据 | 删除按钮 | 未选择数据；选择至少一条数据后重试 | 提示 | 前端校验选中行数量>=1 |
| 请选择讲师类型 | 查询时 | 讲师档案 Tab 未选择类型；选择讲师类型后查询 | 提示 | pageType 取自讲师类型，未选择阻止查询 |
| 只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！ | 牌价变更 | 档案非生效或为特训营；确认档案状态=valid 且 TRAIN_TYPE≠camp | 提示 | 前端双重校验 |
| 只能对【审批通过】的档案进行操作！ | 生效/失效 | 档案未审批通过；确认 APPROVAL_STATUS=approved | 提示 | 前端校验审批状态 |
| 请求失败 | 接口调用 | 后端服务异常；检查 mbo-business 微服务状态 | 错误 | HTTP 状态码非 2xx |
| 网络异常/接口超时 | 任意接口调用 | 网络中断或接口响应超时，检查网络及后端超时配置 | error | axios catch 或 timeout |
| 权限不足 | 点击操作按钮 | 当前用户无对应按钮权限码，联系管理员授权 | error | permissionList 校验未通过 |
| 数据不存在 | 编辑/生效/失效 | 档案编码不存在或已删除，检查 LECTURER_ARCHIVES_CODE 有效性 | error | 接口返回数据为空 |
| 状态不允许操作 | 提交/编辑 | 档案状态不允许该操作，如审批中不可编辑，检查 ARCHIVES_STATUS | error | 后端校验状态机失败 |
| 值集数据不显示 | 下拉选项 | 值集 MBO.TRAIN_LECTURER_LEVEL 等未配置，检查值集配置 | warning | lookupCode 查询返回空 |
| 讲师姓名不能为空 | 保存提交 | 未填写讲师姓名，填写后保存 | error | 前端校验 LECTURER_NAME 非空 |
| 档案编码已存在 | 新建保存 | 档案编码重复，更换编码后保存 | error | 后端校验 LECTURER_ARCHIVES_CODE 唯一性 |

#### 报错1：请选择一条数据
- **触发条件**：点击编辑、提交、牌价变更申请、查看审批、生效、失效、查看日程、查看饱和度等单选操作按钮时，未选择数据或选择了多行
- **逻辑分析**：前端在执行单选操作前校验选中行数量，若 selectedRows.length ≠ 1 则阻止操作并提示"请选择一条数据"。单选操作需要明确的目标档案，未选择时无法确定操作对象，多选时操作对象不唯一
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码,
         LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态,
         APPROVAL_STATUS AS 审批状态
  FROM MA_LECTURER_ARCHIVE
  WHERE ARCHIVES_STATUS IN ('draft', 'reject', 'valid', 'invalid', 'approved')
  ORDER BY UPDATE_DATE DESC;
  ```

#### 报错2：请选择至少一条数据
- **触发条件**：点击删除按钮时，未选择任何数据
- **逻辑分析**：前端在执行删除操作前校验选中行数量，若 selectedRows.length < 1 则阻止操作并提示"请选择至少一条数据"。删除支持批量操作，但至少需要一条目标数据
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 计师档案编码,
         LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态
  FROM MA_LECTURER_ARCHIVE
  WHERE ARCHIVES_STATUS = 'draft';
  ```

#### 报错3：请选择讲师类型
- **触发条件**：在讲师档案 Tab（activeTab=other）下查询时，未选择讲师类型
- **逻辑分析**：讲师档案 Tab 的查询依赖讲师类型（lecturerType）作为 pageType 参数，未选择讲师类型时 pageType 为空，无法确定查询的讲师业务方向，前端校验阻止查询并提示"请选择讲师类型"。讲师类型决定讲师级别值集（TRAIN/ACTIVITY/DESIGN_LECTURER_LEVEL）的切换
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码,
         LECTURER_NAME AS 讲师姓名,
         LECTURER_TYPE AS 讲师类型
  FROM MA_LECTURER_ARCHIVE
  WHERE LECTURER_TYPE IS NULL OR LECTURER_TYPE = '';
  ```

#### 报错4：只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！
- **触发条件**：点击牌价变更申请按钮时，档案状态不为 valid 或培训类型为 camp
- **逻辑分析**：前端执行双重校验：①校验 ARCHIVES_STATUS = 'valid'（仅生效档案可发起牌价变更，草稿/审批中/失效档案不允许）；②校验 TRAIN_TYPE ≠ 'camp'（特训营讲师不适用牌价变更流程）。任一校验不通过则提示"只有生效状态的档案且培训类型不是特训营的可以发起牌价变更申请！"。牌价变更涉及讲师报价调整，需在档案生效后进行，特训营讲师价格由特训营统一管理
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 计师档案编码,
         LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态,
         TRAIN_TYPE AS 培训类型,
         PRICE AS 当前报价
  FROM MA_LECTURER_ARCHIVE
  WHERE ARCHIVES_STATUS <> 'valid' OR TRAIN_TYPE = 'camp';
  ```

#### 报错5：只能对【审批通过】的档案进行操作！
- **触发条件**：点击生效或失效按钮时，档案审批状态不为 approved
- **逻辑分析**：前端校验 APPROVAL_STATUS = 'approved'，仅审批通过的档案才允许执行生效/失效状态切换。审批未通过（草稿/审批中/审批拒绝）的档案不具备业务合法性，不应进入生效/失效流转。校验不通过提示"只能对【审批通过】的档案进行操作！"
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 计师档案编码,
         LECTURER_NAME AS 讲师姓名,
         APPROVAL_STATUS AS 审批状态,
         ARCHIVES_STATUS AS 档案状态
  FROM MA_LECTURER_ARCHIVE
  WHERE APPROVAL_STATUS <> 'approved'
    AND ARCHIVES_STATUS IN ('valid', 'invalid');
  ```

#### 报错6：请求失败
- **触发条件**：调用 mlt/maLecturerArchive/* 系列接口时，后端返回 HTTP 状态码非 2xx
- **逻辑分析**：前端通过 axios 调用后端接口，若响应状态码非 2xx 或网络异常则触发错误回调，统一提示"请求失败"。常见根因包括：mbo-business 微服务未启动或异常、数据库连接失败、SQL 执行超时、后端业务异常未捕获、网络中断等。需检查 mbo-business 微服务运行状态、数据库连接池、后端日志定位具体异常堆栈
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码,
         LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态,
         APPROVAL_STATUS AS 审批状态,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间,
         LAST_UPDATED_BY AS 最后更新人
  FROM MA_LECTURER_ARCHIVE
  WHERE LAST_UPDATE_DATE >= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
  ```

#### 报错7：网络异常/接口超时
- **触发条件**：任意接口调用时，网络中断或接口响应超过 axios timeout 配置
- **逻辑分析**：前端 axios 请求未收到响应或响应超时，触发 catch 回调统一提示"请求失败"。常见根因：网络中断、mbo-business 服务假死、数据库慢查询等。需检查网络连通性、后端服务负载、数据库性能
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码, LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间
  FROM MA_LECTURER_ARCHIVE
  WHERE LAST_UPDATE_DATE >= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
  ```

#### 报错8：权限不足
- **触发条件**：点击新建、编辑、删除、提交、生效/失效、牌价变更等按钮时，当前用户无对应 permissionList 权限码
- **逻辑分析**：前端 Button 组件通过 permissionList 配置权限码，HZERO 框架校验当前用户角色是否包含该权限码，未包含则按钮不可见或禁用。若强制调用接口，后端也会校验权限返回403。需联系管理员配置对应角色权限
- **排查SQL**：
  ```sql
  SELECT U.USER_NAME AS 用户名, R.ROLE_NAME AS 角色名, P.PERMISSION_CODE AS 权限码
  FROM SYS_USER U
  LEFT JOIN SYS_USER_ROLE UR ON U.USER_ID = UR.USER_ID
  LEFT JOIN SYS_ROLE R ON UR.ROLE_ID = R.ROLE_ID
  LEFT JOIN SYS_ROLE_PERMISSION RP ON R.ROLE_ID = RP.ROLE_ID
  LEFT JOIN SYS_PERMISSION P ON RP.PERMISSION_ID = P.PERMISSION_ID
  WHERE P.PERMISSION_CODE LIKE '%lecturer_archive%' ORDER BY U.USER_NAME;
  ```

#### 报错9：数据不存在
- **触发条件**：编辑、生效/失效等操作时，接口返回数据为空或档案编码不存在
- **逻辑分析**：前端通过 lecturerArchivesCode 调用详情接口，后端查询 MA_LECTURER_ARCHIVE 表无对应记录或记录已逻辑删除，返回空数据。常见根因：档案编码错误、档案已被删除、跨租户查询、数据权限隔离等。需检查 LECTURER_ARCHIVES_CODE 有效性及数据权限
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码, LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态, DELETE_FLAG AS 删除标记
  FROM MA_LECTURER_ARCHIVE
  WHERE DELETE_FLAG = 'Y' OR LECTURER_ARCHIVES_CODE IS NULL;
  ```

#### 报错10：状态不允许操作
- **触发条件**：点击提交、编辑等按钮时，档案状态不允许该操作
- **逻辑分析**：后端校验状态机，如审批中（approving）不可编辑、已生效不可重复生效等。状态不匹配时后端返回业务异常。需检查档案当前状态及操作流程
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 讲师档案编码, LECTURER_NAME AS 讲师姓名,
         ARCHIVES_STATUS AS 档案状态, APPROVAL_STATUS AS 审批状态,
         ERROR_INFO AS 异常问题
  FROM MA_LECTURER_ARCHIVE
  WHERE ARCHIVES_STATUS NOT IN ('draft','valid','invalid','approved','reject')
  ORDER BY CREATE_DATE DESC;
  ```

#### 报错11：值集数据不显示
- **触发条件**：查询条件或列表中讲师类型、讲师级别等下拉选项为空
- **逻辑分析**：前端通过 lookupCode 查询值集 MBO.TRAIN_LECTURER_LEVEL、MBO.ACTIVITY_LECTURER_LEVEL、MBO.DESIGN_LECTURER_LEVEL 等，值集未配置或未启用则下拉选项为空。需在值集管理页面配置对应值集
- **排查SQL**：
  ```sql
  SELECT LOOKUP_CODE AS 值集编码, LOOKUP_VALUE_CODE AS 值编码,
         LOOKUP_VALUE_NAME AS 值名称, ENABLE_FLAG AS 启用标记
  FROM SYS_LOOKUP_VALUE
  WHERE LOOKUP_CODE IN ('MBO.TRAIN_LECTURER_LEVEL','MBO.ACTIVITY_LECTURER_LEVEL','MBO.DESIGN_LECTURER_LEVEL','MBO.LECTURER_TYPE')
    AND ENABLE_FLAG = 'N' ORDER BY LOOKUP_CODE;
  ```

#### 报错12：讲师姓名不能为空
- **触发条件**：保存或提交时，LECTURER_NAME 字段为空
- **逻辑分析**：前端表单对 lecturerName 字段配置 required 校验，提交前校验讲师姓名是否填写，为空则阻止提交并提示"讲师姓名不能为空"。讲师姓名是档案核心信息，必须明确
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 计师档案编码, LECTURER_NAME AS 讲师姓名,
         LECTURER_TYPE AS 讲师类型, ARCHIVES_STATUS AS 档案状态
  FROM MA_LECTURER_ARCHIVE
  WHERE LECTURER_NAME IS NULL OR LECTURER_NAME = '';
  ```

#### 报错13：档案编码已存在
- **触发条件**：新建讲师档案保存时，LECTURER_ARCHIVES_CODE 已存在于 MA_LECTURER_ARCHIVE 表
- **逻辑分析**：后端校验 LECTURER_ARCHIVES_CODE 唯一性，若已存在则返回业务异常。需更换档案编码后保存
- **排查SQL**：
  ```sql
  SELECT LECTURER_ARCHIVES_CODE AS 计师档案编码, COUNT(*) AS 重复数
  FROM MA_LECTURER_ARCHIVE
  WHERE DELETE_FLAG = 'N'
  GROUP BY LECTURER_ARCHIVES_CODE
  HAVING COUNT(*) > 1;
  ```

### 常见问题

- **Q1：讲师档案提交审批后能否修改？**
  A：审批中状态不可修改，需等审批完成。审批拒绝后可修改重新提交。

- **Q2：一个讲师可以同时拥有多个等级吗？**
  A：可以。培训讲师等级、活动讲师等级、设计讲师等级分别独立设置，互不影响。

- **Q3：讲师档案编码如何生成？**
  A：通过 `lecturerArchivesCode` 按编码规则自动生成，确保唯一性。

- **Q4：删除讲师档案有什么限制？**
  A：仅草稿状态的档案可删除，审批中/生效/失效状态均不可删除。

- **Q5：讲师类型有哪些？**
  A：由值集 MBO.LECTURER_TYPE 定义，常见如：内部讲师、外部讲师等。

- **Q6：讲师档案 Tab 不显示？**
  A：检查权限 `list_other` 是否分配。

- **Q7：讲师类型下拉为空？**
  A：检查 getLecturerTypeList 接口返回，值集 MBO.LECTURER_TYPE 是否配置。

- **Q8：讲师级别值集不正确？**
  A：检查讲师类型对应值集（TRAIN/ACTIVITY/DESIGN_LECTURER_LEVEL）是否配置。

- **Q9：查看日程日历无数据？**
  A：检查讲师排期数据是否存在，LecturerCalendar 组件参数。

- **Q10：查看饱和度无数据？**
  A：检查 SaturationForm 组件 lecturerArchivesCode 参数，饱和度数据是否计算。

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-11-12 | - | hfy | 初始创建 |
| 2026-07-31 | - | AI | 对比网站补充详细逻辑、数据库表字段等 |
| 2026-08-03 | - | AI | 初始创建 |
| 2026-08-28 | - | AI | 完整重写，补充业务流程、界面模块、数据库表、FAQ等 |
| 2026-08-30 | - | AI | 按 skill 规范格式重写，删除定义章节，调整章节顺序与表格列数 |
