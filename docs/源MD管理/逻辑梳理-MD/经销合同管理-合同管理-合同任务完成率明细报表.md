# 经销合同管理-合同管理-合同任务完成率明细报表

## 业务流程

### 业务流程图

```text
合同任务完成率明细报表页面(ae-report报表)
        │
        ├── 查询合同明细 ──→ POST /contractReport/epm-project-contract/searchContractInvRate
        │                      └── 按合同维度展示出货完成率明细
        │
        └── 导出 ──→ GET /contractReport/exportContractInvRate
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 合同任务完成率 | 数据依赖 | 明细报表为完成率报表的明细下钻 | 合同已签订 |
| 出库单 | 数据依赖 | 明细展示出货完成情况 | 出库单已生成 |

### 下游影响

- 影响1：合同执行分析
  - 明细报表为合同执行分析提供明细级别数据支撑

---

## 重点逻辑

### 重点逻辑1：合同出货完成率明细 {明细展示}

- **业务意义**：按合同维度展示出货完成率明细，精确掌握每个合同的执行情况

- **具体逻辑描述**

  - 第1点：通过searchContractDetailWithSubtotal接口查询合同明细

  - 第2点：展示每个合同的出货明细，含小计行

  - 第3点：完成率=已出货数量/合同任务数量×100%

---

## 详细逻辑

### 界面模块1：合同任务完成率明细报表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 合同编码 | EPM_PROJECT_CONTRACT.CONTRACT_CODE | 文本框 | 合同编码 | 常显 | 系统带出 |
| 合同名称 | EPM_PROJECT_CONTRACT.CONTRACT_NAME | 文本框 | 合同名称 | 常显 | 系统带出 |
| 产品编码 | - | 文本框 | 产品编码 | 常显 | 系统带出 |
| 产品名称 | - | 文本框 | 产品名称 | 常显 | 系统带出 |
| 任务数量 | - | 数字显示框 | 合同任务数量 | 常显 | 系统带出 |
| 已出货数量 | - | 数字显示框 | 已出货数量 | 常显 | 系统计算 |
| 完成率 | - | 数字显示框 | 出货完成率 | 常显 | 自动计算=已出货/任务×100% |

### 选择弹窗

> 本页面为报表查询页面，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询合同明细 | 列表页 | 始终可用 | 调用searchContractDetailWithSubtotal接口 |
| 导出 | 导出明细报表 | 列表页 | 有数据时 | 调用exportContractInvRate接口 |

### 保存校验

> 本页面为报表页面，无保存操作。

### 提交校验

> 本页面为报表页面，无提交操作。

### 状态机

> 本页面为报表页面，无状态流转。

---

## 数据库表详解

### 表1：EPM_PROJECT_CONTRACT（项目合同表）

> 与菜单108共用同一张表，字段详见"经销合同管理-合同管理-合同任务完成率"。

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 合同无出货数据或条件不匹配，放宽条件 | toast提醒 | [查看] |
| 导出失败 | 导出时 | 导出数据量为空或导出接口异常，先查询再导出 | toast提醒 | [查看] |
| 网络请求失败 | 查询/导出时 | 后端服务不可用或网络中断，重试或联系IT | toast提醒 | [查看] |
| 权限不足 | 进入页面时 | 当前用户无报表查询权限，联系管理员授权 | 阻断性报错 | [查看] |
| 必填查询条件为空 | 查询时 | 未填写年度/合同编码等必填条件，补全后查询 | toast提醒 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按合同/产品查询合同出货完成率明细，searchContractInvRate接口返回空结果集
- **逻辑分析**：明细报表为完成率报表（菜单108）的下钻视图，通过searchContractDetailWithSubtotal接口按合同维度展示出货完成率明细，完成率=已出货数量/合同任务数量×100%，含小计行。无数据根因有三类：(1)选中的合同无出货数据（DELIVERY_AMT为0或出库单未生成）；(2)合同任务数量为0（合同未配置任务明细行）；(3)产品/合同筛选条件与记录不匹配。需先确认合同是否有出库单及任务明细
- **排查SQL**：
  ```sql
  SELECT CONTRACT_CODE, CONTRACT_NAME, CUSTOMER_NAME,
         TASK_AMT, DELIVERY_AMT,
         CASE WHEN TASK_AMT > 0 THEN ROUND(DELIVERY_AMT / TASK_AMT * 100, 2) ELSE 0 END AS 完成率
  FROM EPM_PROJECT_CONTRACT
  WHERE (CONTRACT_CODE = #{contractCode} OR #{contractCode} IS NULL)
  ORDER BY CONTRACT_CODE;
  ```

#### 报错2：导出失败
- **触发条件**：用户点击"导出"按钮，exportContractInvRate接口返回空数据集或抛出异常
- **逻辑分析**：导出接口exportContractInvRate基于searchContractDetailWithSubtotal同一查询逻辑（ContractInvRateConvert.INSTANCE.toExportVO），若查询结果集为空则导出文件无内容；若导出过程中后端服务异常（如内存溢出、LOV翻译失败@ProcessLovValue）也会失败。前端通过ExcelExportCom组件触发导出，需先执行查询确认有数据再导出
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 可导出记录数
  FROM EPM_PROJECT_CONTRACT
  WHERE (CONTRACT_CODE = #{contractCode} OR #{contractCode} IS NULL)
    AND TASK_AMT IS NOT NULL;
  ```

#### 报错3：网络请求失败
- **触发条件**：前端调用contractReport/epm-project-contract/searchContractInvRate或exportContractInvRate接口时，axios请求超时或返回非2xx状态码
- **逻辑分析**：报表服务ae-report与前端跨服务调用，网络中断、服务未启动、网关路由异常均会导致请求失败。前端index.tsx通过DataSet.query()发起请求，失败时控制台输出"获取图表数据失败"日志。需确认ae-report服务状态及网关配置
- **排查SQL**：
  ```sql
  -- 确认报表服务数据源连通性（间接验证服务可用性）
  SELECT COUNT(*) AS 合同总记录数 FROM EPM_PROJECT_CONTRACT;
  ```

#### 报错4：权限不足
- **触发条件**：当前用户角色未配置报表查询权限（PERMISSION_PREFIX.arrow-ae:contractInvRate），访问页面时被拦截
- **逻辑分析**：报表页面通过permissionList控制访问权限，用户无对应权限码时无法进入页面或调用接口。需在角色管理中为用户分配"合同任务完成率明细报表"查询权限
- **排查SQL**：
  ```sql
  -- 查询用户角色权限（示意，实际表名依权限框架）
  SELECT ROLE_CODE, PERMISSION_CODE FROM USER_ROLE_PERMISSION
  WHERE USER_ID = #{userId} AND PERMISSION_CODE LIKE '%contractInvRate%';
  ```

#### 报错5：必填查询条件为空
- **触发条件**：用户未填写年度（POLICY_YEAR）或合同编码（CONTRACT_CODE）等必填查询条件直接点击查询
- **逻辑分析**：YearMonthContractRateDTO中年度是报表维度核心字段，未填写年度将导致查询无明确时间范围，返回全量或空数据。前端QueryDS中年度字段配置required校验，未填写时DataSet.query()前置校验拦截并提示
- **排查SQL**：
  ```sql
  SELECT DISTINCT POLICY_YEAR FROM EPM_PROJECT_CONTRACT
  WHERE POLICY_YEAR IS NOT NULL ORDER BY POLICY_YEAR DESC;
  ```

### 常见问题

- 问题1：与菜单108的关系
  - 原因：菜单108为年月汇总视图，菜单109为合同明细下钻视图
  - 解决思路：两个菜单共用ContractReportController，分别调用不同API方法

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
