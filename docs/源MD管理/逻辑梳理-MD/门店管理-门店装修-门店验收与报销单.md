# 门店管理-门店装修-门店验收与报销单

## 业务流程

```
门店验收与报销单列表页
        │
        ├── 新增 ──→ 选择已审批的装修申请单(LOV) ──→ 联动带出门店/经销商/装修信息
        │              ──→ 查询装修标准(doSearchDecorate) ──→ 填写验收报销信息
        │              ──→ 绑定CRM软装灯具订单(confirm-crm-soft-order) ──→ 保存
        │                                    │
        │                                    ▼
        │                              生成验收报销单号 ──→ HZ_APPROVE_STATUS=NEW
        │                                    │
        │                                    ▼
        │                              提交审批 ──→ 启动OA工作流(SUB_STORE_MDYSYBXD)
        │                                    │
        │                                    ▼
        │                              OA审批流程中(多节点)
        │                                    │
        │                         ├──审批通过──→ HZ_APPROVE_STATUS=APPROVED
        │                         │              ├─ 回写装修申请单验收信息
        │                         │              ├─ 生成额度内/外兑现记录
        │                         │              └─ 同步资金池
        │                         │
        │                         ├──审批驳回──→ HZ_APPROVE_STATUS=REJECTED
        │                         │
        │                         └──退回──→ HZ_APPROVE_STATUS=REBUT
        │
        ├── 作废 ──→ 仅NEW/REJECTED状态可作废 ──→ HZ_APPROVE_STATUS=INTERRUPT
        │
        └── 删除 ──→ 仅NEW状态可删除
```

## 重点逻辑

### 重点逻辑1：基于已审批装修申请

- **业务意义**：验收报销必须关联已审批通过的装修申请单
- **具体逻辑描述**：
  - 选择APPROVED状态的FinFeeApplyFinishedHeader
  - 联动带出门店/经销商/装修面积/装修标准等全部信息
  - 查询装修标准(doSearchDecorate)获取政策补贴标准

### 重点逻辑2：CRM软装灯具订单绑定

- **业务意义**：验收时绑定CRM系统的软装灯具采购订单
- **具体逻辑描述**：
  - select-crm-soft-order查询可绑定的CRM软装灯具订单
  - confirm-crm-soft-order确认绑定
  - query-bind-crm-soft-order查询已绑定订单
  - unbind-soft-order取消绑定

### 重点逻辑3：审批通过生成兑现记录

- **业务意义**：验收报销审批通过后自动生成额度内/外兑现记录
- **具体逻辑描述**：
  - 生成FIN_FEE_TERMINAL_CASHOUT(额度内兑现)记录
  - 生成FIN_FEE_TERMINAL_RE_CASHOUT(额度外兑现)记录
  - 回写装修申请单的验收信息
  - 同步资金池

## 详细逻辑

### 界面模块

本页面为前端React页面，位于`arrow-ae/src/pages/storeManage/storeAcceptanceReimbursementInfo/`，含列表页和详情页。

#### 头部信息区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 验收报销单号 | CHECK_BX_CODE | TextField | 验收报销单号 | 始终 | 编码规则生成 |
| 装修申请单号 | TERMINAL_APPLY_NO | Lov | 选择装修申请单 | hzApproveStatus=NEW时可编辑 | 选择已审批的装修申请单，必填 |
| 门店编码 | TERMINAL_CODE | TextField | 门店编码 | 始终 | 从申请单带入，必填 |
| 门店名称 | TERMINAL_NAME | TextField | 门店名称 | 始终 | 从申请单带入，必填 |
| 经销商名称 | CUST_NAME | TextField | 经销商名称 | 始终 | 从申请单带入，必填 |
| 经销商简称 | SHORT_NAME | TextField | 经销商简称 | 始终 | 从申请单带入 |
| 交易公司 | TRADING_COMPANY_NAME | TextField | 交易公司 | 始终 | 从申请单带入 |
| 法人客户 | BILLING_UNIT_NAME | TextField | 法人客户 | 始终 | 从申请单带入 |
| 装修等级 | FIXUP_GRADE | Select(AE.FIXUP_GRADE) | 装修等级 | 始终 | 从申请单带入 |
| 门店面积 | TERMINAL_AREA | NumberField | 门店面积 | 始终 | 从申请单带入 |
| 实际装修面积 | THIS_TERMINAL_AREA | NumberField | 实际装修面积 | 始终 | 从申请单带入 |
| 额度内报销金额 | IN_BX_AMT | NumberField | 额度内报销金额 | 始终 | 系统计算 |
| 额度外报销金额 | OUT_BX_AMT | NumberField | 额度外报销金额 | 始终 | 系统计算 |
| 验收日期 | CHECK_DATE | DatePicker | 验收日期 | 始终 | 用户输入 |
| 备注 | NOTE | TextField | 备注 | 始终 | 用户输入 |
| 申请日期 | CREATION_DATE | TextField | 创建时间 | 始终 | 默认当前时间，只读 |
| 创建人 | CREATED_BY_NAME | TextField | 创建人姓名 | 始终 | 默认当前用户，只读 |
| 审核状态 | HZ_APPROVE_STATUS | Select(HWKF.APPROVE_STATUS) | 工作流审批状态 | 始终 | 默认NEW，只读 |
| 审核状态 | AUDIT_STAT | TextField | 审核状态 | 始终 | 系统维护 |
| 经营属性 | CUSTOMER_CLASS | Select(AE.CUSTOMER_CLASS) | 经营属性 | 始终 | 从申请单带入 |
| 法人客户编码 | BILLING_UNIT_CODE | TextField | 法人客户编码 | 始终 | 从申请单带入 |
| 销售区域 | SALEZONE_ORG_NAME | TextField | 销售区域名称 | 始终 | 从申请单带入 |
| 运营中心 | OPERAT_CENTER_ORG_NAME | TextField | 运营中心名称 | 始终 | 从申请单带入 |
| 成本中心编码 | COST_CENTER_CODE | TextField | 成本中心编码 | 始终 | 从申请单带入 |
| 成本中心名称 | COST_CENTER_NAME | TextField | 成本中心名称 | 始终 | 从申请单带入 |
| 项目编码 | OBJECT_CODE | TextField | 项目编码 | 始终 | 从申请单带入 |
| 项目名称 | OBJECT_NAME | TextField | 项目名称 | 始终 | 从申请单带入 |
| 预算年度 | BUD_YEAR | TextField | 预算年度 | 始终 | 从申请单带入 |
| 展示设计师 | DESIGNER | TextField | 展示设计师 | 始终 | 从申请单带入 |
| 设计师用户名 | USERNAME | TextField | 设计师用户名 | 始终 | 从申请单带入 |
| 设计师手机号 | MOBIL | TextField | 设计师手机号 | 始终 | 从申请单带入 |
| 软装设计师 | SOFT_DESIGNER | TextField | 软装设计师 | 始终 | 从申请单带入 |
| 位置类型 | STORE_LOCATION_TYPE | Select(AE.STORE_LOCATION_TYPE) | 位置类型 | 始终 | 从申请单带入 |
| 装修周期 | DECORATION_DAYS | NumberField | 装修天数 | 始终 | 从申请单带入 |
| 门店类型 | TERMINAL_TYPE | Select(AE.TERMINAL_TYPE) | 门店类型 | 始终 | 从申请单带入 |
| 门店详细地址 | ADDR | TextField | 门店详细地址 | 始终 | 从申请单带入 |
| 装修性质 | DECORATION_TYPE | Select(AE.DECORATION_TYPE) | 装修性质 | 始终 | 从申请单带入 |
| 旧店上次装修时间 | LAST_DECORATION_DATE | DatePicker | 旧店上次装修时间 | 始终 | 从申请单带入 |
| 超期天数 | OVER_DATE | NumberField | 装修超期天数 | 始终 | 系统计算 |
| 线下验收日期 | OFFLINE_CHECK_DATE | DatePicker | 线下验收日期 | 始终 | 用户输入 |
| 装修风格 | DECORATION_STYLE | Select(AE.DECORATION_STYLE) | 装修风格 | 始终 | 从申请单带入 |
| 本次装修风格 | THIS_DECORATION_STYLE | Select(AE.DECORATION_STYLE) | 本次装修风格 | 始终 | 从申请单带入 |
| 额度内有效期 | IN_VALIDITY_DATE | DatePicker | 额度内有效期 | 始终 | 从申请单带入 |
| 额度外兑现结束时间 | OUT_CASHOUT_END_TIME | DatePicker | 额度外兑现结束时间 | 始终 | 从申请单带入 |
| 门头装修等级 | FRONTDOOR_FIXUP_GRADE | TextField | 门头装修等级 | 始终 | 从申请单带入 |
| 验收超期天数 | CHECK_OVER_DAY | NumberField | 验收超期天数 | 始终 | 系统计算 |
| 是否超标准 | IS_OVER_STANDARD | NumberField | 是否超标准 | 始终 | 系统计算 |
| 装修超期免责份数 | DECORATION_OVERTIME_FREE_COPY | NumberField | 装修超期免责份数 | 始终 | 系统参数 |
| 验收超期免责份数 | CHECK_OVERTIME_FREE_COPY | NumberField | 验收超期免责份数 | 始终 | 系统参数 |
| 审核备注 | REVIEW_NOTE | TextField | 审核备注 | 始终 | 审批节点可编辑 |
| 发票税率 | INVOICE_TAX_RATE | Select | 发票税率 | 始终 | 用户选择 |
| 发票类型 | INVOICE_TYPE | Select | 发票类型 | 始终 | 用户选择 |
| 开票办公室编码 | ISSUING_OFFICE_CODE | TextField | 开票办公室编码 | 始终 | 从LOV带入 |
| 开票办公室 | ISSUING_OFFICE | TextField | 开票办公室 | 始终 | 从LOV带入 |
| 付款方式 | PAY_TYPE | Select(AE.PAY_TYPE) | 付款方式 | 始终 | 用户选择 |
| 供应商编码 | VENDOR_CODE | TextField | 供应商编码 | 始终 | 从LOV带入 |
| 供应商名称 | VENDOR_NAME | TextField | 供应商名称 | 始终 | 从LOV带入 |
| 联系人 | CONTACT | TextField | 联系人 | 始终 | 从LOV带入 |
| 联系电话 | TELE | TextField | 联系电话 | 始终 | 从LOV带入 |
| 额度外验收金额 | OUT_CHECK_AMT | NumberField | 额度外验收金额 | 始终 | 系统计算 |
| 我司开票 | OUR_INVOICING | Select | 我司开票 | 始终 | 用户选择 |
| 三方协议 | TRIPAR_AGREE | Select | 三方协议 | 始终 | 用户选择 |
| 是否重签 | IS_RESIGN | Select | 是否重签 | 始终 | 系统维护 |
| 任务名称 | TASK_NAME | TextField | 当前审批任务名称 | 始终 | 工作流带入 |
| 提前兑现标识 | EARLY_ENCASHMENT_FLAG | TextField | 提前兑现标识 | 始终 | 系统维护 |
| 软装补贴金额 | SOFT_PURCHASE_AMT | NumberField | 软装补贴金额 | 始终 | 系统计算 |
| 软装广告费采购金额 | SOFT_ADV_PURCHASE_AMT | NumberField | 软装广告费采购金额 | 始终 | 系统计算 |
| 软装物品采购金额 | SOFT_GOODS_PURCHASE_AMT | NumberField | 软装物品采购金额 | 始终 | 系统计算 |
| 软装采购标准 | SOFT_PURCHASE_STANDARD | TextField | 软装采购标准 | 始终 | 从装修标准带入 |
| 软装采购标准金额 | SOFT_PURCHASE_STANDARD_AMT | NumberField | 软装采购标准金额 | 始终 | 系统计算 |
| 软装达成采购金额 | SOFT_REACH_PURCHASE_AMT | NumberField | 软装达成采购金额 | 始终 | 系统计算 |
| 软装审批标准临时 | SOFT_APPROVAL_STANDARD_TMP | NumberField | 软装审批标准临时 | 始终 | 审批节点可编辑 |
| 软装采购百分比 | SOFT_PURCHASE_PERCENT | NumberField | 软装采购百分比 | 始终 | 系统计算 |
| 软装扣减金额 | SOFT_DEDUCT_AMT | NumberField | 软装扣减金额 | 始终 | 系统计算 |
| 灯具补贴金额 | LANTERN_AMT | NumberField | 灯具补贴金额 | 始终 | 系统计算 |
| 灯具广告费金额 | LANTERN_ADV_AMT | NumberField | 灯具广告费金额 | 始终 | 系统计算 |
| 灯具物品金额 | LANTERN_GOODS_AMT | NumberField | 灯具物品金额 | 始终 | 系统计算 |
| 灯具标准 | LANTERN_STANDARD | TextField | 灯具标准 | 始终 | 从装修标准带入 |
| 灯具标准金额 | LANTERN_STANDARD_AMT | NumberField | 灯具标准金额 | 始终 | 系统计算 |
| 灯具达成金额 | LANTERN_REACH_AMT | NumberField | 灯具达成金额 | 始终 | 系统计算 |
| 灯具审批标准临时 | LANTERN_APPROVAL_STANDARD_TMP | NumberField | 灯具审批标准临时 | 始终 | 审批节点可编辑 |
| 灯具百分比 | LANTERN_PERCENT | NumberField | 灯具百分比 | 始终 | 系统计算 |
| 灯具扣减金额 | LANTERN_DEDUCT_AMT | NumberField | 灯具扣减金额 | 始终 | 系统计算 |
| 软装灯具扣罚金额 | ALL_DEDUCT_AMT | NumberField | 软装灯具扣罚金额(元) | 始终 | 系统计算 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 列表查询 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/search` | 查询验收报销列表 |
| 保存 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/save` | 新增验收报销 |
| 更新 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/update` | 更新验收报销 |
| 详情查询 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/detail/{checkBxId}` | 查询验收报销详情 |
| 打印 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/detail/print` | 获取打印数据 |
| 额度内兑现详情 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/do-select` | 额度内兑现新增-详情 |
| 作废 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/cancel` | 作废验收报销 |
| 删除 | DELETE | `/v1/{organizationId}/fin-fee-check-bx-headers/do-delete` | 删除验收报销 |
| 查询CRM软装灯具 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/select-crm-soft-order` | 查询可绑定的CRM软装灯具订单 |
| 绑定软装订单 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/confirm-crm-soft-order` | 绑定CRM软装灯具订单 |
| 查询已绑定订单 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/query-bind-crm-soft-order` | 查询已绑定的软装订单 |
| 取消绑定 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/unbind-soft-order` | 取消绑定软装订单 |
| 查询装修标准 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/doSearchDecorate` | 查询装修标准 |
| 查询装修标准(新) | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/do-search-decorate` | 查询装修标准 |
| 查询采购清单金额 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/get-inv-order-amt` | 查询采购清单及金额 |
| 获取质量信息 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/get-acceptance-info` | 获取质量信息设置 |
| 政策性补贴查询 | GET | `/v1/{organizationId}/fin-fee-check-bx-headers/get-lov-search` | 政策性补贴search |

### 选择弹窗

| 弹窗名称 | LOV编码 | 参数 | 说明 |
|----------|---------|------|------|
| 装修申请单号 | `AE.STORE_FINFEEAPPLYCLOSE_DATA_CHANGE_VIEW` | organizationId | 选择已审批(APPROVED)的装修申请单，联动带出门店/经销商/装修信息 |
| CRM软装灯具订单 | - | terminalApplyNo | 查询可绑定的CRM软装灯具采购订单 |

### 导入

本页面无导入功能。

### 其他按钮

| 按钮名称 | 触发条件 | 执行逻辑 | 接口调用 |
|----------|---------|---------|---------|
| 作废 | HZ_APPROVE_STATUS为NEW或REJECTED | 弹窗确认→标记INTERRUPT | POST /cancel |
| 删除 | HZ_APPROVE_STATUS为NEW | 弹窗确认→删除 | DELETE /do-delete |
| 打印 | 任意状态 | 获取打印数据→打印 | GET /detail/print |
| 绑定软装订单 | 编辑模式 | 查询可绑定订单→选择→确认绑定 | GET /select-crm-soft-order → POST /confirm-crm-soft-order |
| 取消绑定软装订单 | 已绑定订单 | 选择已绑定订单→取消绑定 | POST /unbind-soft-order |

### 保存校验

- 校验1：装修申请单必填 —— 确保关联有效的装修申请
  - **详细逻辑**：前端必填校验
  - **系统体现**：C7N内置校验
  - **排查SQL**：`SELECT CHECK_BX_ID FROM FIN_FEE_CHECK_BX_HEADER WHERE TERMINAL_APPLY_ID IS NULL`

- 校验2：验收数据完整性 —— 确保验收面积/金额等关键字段完整
  - **详细逻辑**：后端校验关键字段非空
  - **系统体现**：后端抛异常
  - **排查SQL**：`SELECT CHECK_BX_ID FROM FIN_FEE_CHECK_BX_HEADER WHERE THIS_TERMINAL_AREA IS NULL`

### 提交校验

- 校验1：装修申请单已审批 —— 确保关联的装修申请单状态为APPROVED
  - **详细逻辑**：提交时校验原申请单hzApproveStatus=APPROVED
  - **系统体现**：后端校验
  - **排查SQL**：`SELECT * FROM FIN_FEE_APPLY_FINISHED_HEADER WHERE TERMINAL_APPLY_ID={id} AND HZ_APPROVE_STATUS!='APPROVED'`

### 状态机

```text
NEW(新建) ──提交审批──→ RUN(审批中) ──┬──审批通过──→ APPROVED(已审批)
                                       │              ├─ 生成额度内/外兑现记录
                                       │              ├─ 回写装修申请单
                                       │              └─ 同步资金池
                                       │
                                       ├──审批驳回──→ REJECTED(已驳回)
                                       │
                                       └──退回──→ REBUT(已退回)

NEW/REJECTED ──作废──→ INTERRUPT(已作废)
NEW ──删除──→ (删除)
```

### 工作流

- **工作流编码**：`SUB_STORE_MDYSYBXD`（门店验收与报销单）
- **编码规则**：验收报销单号由系统生成

## 数据库表详解

### FIN_FEE_CHECK_BX_HEADER（门店验收与报销单表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| CHECK_BX_ID | Long | 主键ID | - | 自增 |
| CHECK_BX_CODE | String | 验收报销单号 | 验收报销单号 | 编码规则生成 |
| TERMINAL_APPLY_ID | Long | 装修申请ID | - | 用户选择(LOV) |
| TERMINAL_APPLY_NO | String | 装修申请单号 | 装修申请单号 | 选择时带入 |
| TERMINAL_ID | Long | 门店ID | - | 从申请单带入 |
| TERMINAL_CODE | String | 门店编码 | 门店编码 | 从申请单带入 |
| TERMINAL_NAME | String | 门店名称 | 门店名称 | 从申请单带入 |
| CUST_ID | Long | 经销商ID | - | 从申请单带入 |
| CUST_CODE | String | 经销商编码 | - | 从申请单带入 |
| CUST_NAME | String | 经销商名称 | 经销商名称 | 从申请单带入 |
| SHORT_NAME | String | 经销商简称 | 经销商简称 | 从申请单带入 |
| TRADING_COMPANY_ID | Long | 交易公司ID | - | 从申请单带入 |
| TRADING_COMPANY_CODE | String | 交易公司编码 | - | 从申请单带入 |
| TRADING_COMPANY_NAME | String | 交易公司名称 | 交易公司 | 从申请单带入 |
| BILLING_UNIT_ID | Long | 法人客户ID | - | 从申请单带入 |
| BILLING_UNIT_CODE | String | 法人客户编码 | - | 从申请单带入 |
| BILLING_UNIT_NAME | String | 法人客户名称 | 法人客户 | 从申请单带入 |
| FIXUP_GRADE | Long | 装修等级 | 装修等级 | 从申请单带入 |
| TERMINAL_AREA | BigDecimal | 门店面积 | 门店面积 | 从申请单带入 |
| THIS_TERMINAL_AREA | BigDecimal | 实际装修面积 | 实际装修面积 | 从申请单带入 |
| IN_BX_AMT | BigDecimal | 额度内报销金额 | 额度内报销金额 | 系统计算 |
| OUT_BX_AMT | BigDecimal | 额度外报销金额 | 额度外报销金额 | 系统计算 |
| CHECK_DATE | LocalDate | 验收日期 | 验收日期 | 用户输入 |
| NOTE | String | 备注 | 备注 | 用户输入 |
| CREATOR | String | 创建人 | - | 系统赋值 |
| CREATE_TIME | Date | 创建时间 | - | 系统赋值 |
| CHECKER | String | 审核人 | - | 审批通过时赋值 |
| CHECK_TIME | LocalDateTime | 审核时间 | - | 审批通过时赋值 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |
| HZ_INSTANCE_ID | String | 流程实例ID | - | 工作流启动后赋值 |
| HZ_APPROVE_STATUS | String | 流程审批状态 | - | NEW/RUN/APPROVED/REJECTED/REBUT/INTERRUPT |

注：Entity共836行，含100+字段，上表列出核心字段。完整字段包括各类报销金额/税额/发票信息/供应商信息/成本中心/销售区域/运营中心/设计师/软装采购/灯具采购等。

## 常见问题FAQ

### Q1：无法选择装修申请单

**根因**：没有APPROVED状态的装修申请单
**解决方案**：确认FIN_FEE_APPLY_FINISHED_HEADER中存在APPROVED状态的记录

### Q2：作废时报状态不允许

**根因**：单据状态非NEW或REJECTED
**解决方案**：仅NEW和REJECTED状态可作废

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 门店装修申请(FIN_FEE_APPLY_FINISHED_HEADER) | 数据依赖 | 选择已审批的装修申请单，带入全部装修信息 | 存在APPROVED状态的装修申请单 |
| 装修标准(TERMINAL_DECORATE_STANDARD) | 数据依赖 | 查询装修标准获取政策补贴标准 | 装修标准政策在有效期内 |
| CRM软装灯具订单 | 数据依赖 | 绑定CRM系统的软装灯具采购订单 | CRM中存在可绑定的订单 |

## 下游影响

- **门店装修额度内兑现(FIN_FEE_TERMINAL_CASHOUT)** — 审批通过后生成额度内兑现记录
- **门店装修额度外兑现(FIN_FEE_TERMINAL_RE_CASHOUT)** — 审批通过后生成额度外兑现记录
- **门店装修申请单(FIN_FEE_APPLY_FINISHED_HEADER)** — 审批通过后回写验收信息
- **资金池(EBS)** — 审批通过后同步资金池
- **CRM软装订单** — 绑定/取消绑定软装灯具采购订单

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 验收报销单不存在 | doSelect/doDelete/cancel | checkBxId对应记录不存在 | 高 | [查看](#报错1验收报销单不存在) |
| 装修申请单不存在 | save/submit | terminalApplyId对应的装修申请单不存在 | 高 | [查看](#报错2装修申请单不存在) |
| 不能删除非制单状态的单据 | doDelete | hzApproveStatus≠NEW | 中 | [查看](#报错3不能删除非制单状态的单据) |
| 装修申请单未审批 | submit | 原申请单hzApproveStatus≠APPROVED | 中 | [查看](#报错4装修申请单未审批) |
| 单据id信息不存在 | doSelect | checkBxId为null或<=0 | 高 | [查看](#报错5单据id信息不存在) |
| 报销单【{checkBxCode}】的额度内有效期未找到 | onWfComplete | 验收报销单关联的额度内有效期IN_VALIDITY_DATE为空 | 高 | [查看](#报错6报销单checkbxcode的额度内有效期未找到) |
| 未找到对应的事业部基础设置，请联系相关业务员处理 | save | 事业部基础设置DIVISION_BASE_SET未配置 | 高 | [查看](#报错7未找到对应的事业部基础设置请联系相关业务员处理) |
| 作废失败，门店验收与报销单查询为空 | cancel | 按checkBxId查询FIN_FEE_CHECK_BX_HEADER返回null | 高 | [查看](#报错8作废失败门店验收与报销单查询为空) |
| 支付方式为折扣折让时，才允许提前兑现 | save/submit | payType≠3但earlyEncashmentFlag=提前兑现 | 中 | [查看](#报错9支付方式为折扣折让时才允许提前兑现) |
| 单据信息不合法 | submit | 单据关键字段缺失或格式4错误 | 中 | [查看](#报错10单据信息不合法) |
| 事业部信息不合法 | submit | 事业部ID/编码为空或不匹配 | 中 | [查看](#报错11事业部信息不合法) |
| 门店装修申请与进度更新单据预算年度大于当前年度，无法发起验收 | submit | budYear>当前年份 | 中 | [查看](#报错12门店装修申请与进度更新单据预算年度大于当前年度无法发起验收) |
| 当前单据事业部异常，请联系相关人员处理 | submit | 单据事业部与当前用户事业部不一致 | 高 | [查看](#报错13当前单据事业部异常请联系相关人员处理) |
| {budYear}的预算剩余可用额度不足 | submit | viewOverBudgetAmt返回null或<0 | 高 | [查看](#报错14budyear的预算剩余可用额度不足) |
| 以下所选设计师与保证书不一致：{errorList} | submit | 设计师与保证书设计师不匹配 | 中 | [查看](#报错15以下所选设计师与保证书不一致errorlist) |
| 扣除比例不可小于0 | 保存 | 扣除比例字段<0 | 中 | [查看](#报错16扣除比例不可小于0) |
| 扣除比例不可大于验收比例 | 保存 | 扣除比例>验收比例 | 中 | [查看](#报错17扣除比例不可大于验收比例) |
| 发票税点必须大于0小于等于13 | 保存 | invoiceTaxRate<=0或>13 | 中 | [查看](#报错18发票税点必须大于0小于等于13) |
| 请在附件上传OA签呈减免申请附件 | submit | OA签呈减免申请附件未上传 | 中 | [查看](#报错19请在附件上传oa签呈减免申请附件) |
| 财务复核结果:"补贴标准"大于0,"可报销金额-含税(元)"小于等于0，不能提交 | submit | 补贴标准>0且可报销金额含税<=0 | 中 | [查看](#报错20财务复核结果补贴标准大于0可报销金额-含税元小于等于0不能提交) |
| 请核算验收面积 | submit | 验收面积未核算 | 中 | [查看](#报错21请核算验收面积) |
| 事业部名称获取失败，请检查 | 保存 | 事业部名称查询失败 | 中 | [查看](#报错22事业部名称获取失败请检查) |
| 本次兑现比例不能小于0%或者大于100% | 保存 | 兑现比例<0或>100% | 中 | [查看](#报错23本次兑现比例不能小于0或者大于100) |
| 验收面积不可大于申请面积 | 保存 | 验收面积>申请面积 | 中 | [查看](#报错24验收面积不可大于申请面积) |
| 验收标准不能大于申请标准 | 保存 | 验收标准>申请标准 | 中 | [查看](#报错25验收标准不能大于申请标准) |
| 复核标准不能大于验收标准 | 保存 | 复核标准>验收标准 | 中 | [查看](#报错26复核标准不能大于验收标准) |
| 复核面积不能大于验收面积 | 保存 | 复核面积>验收面积 | 中 | [查看](#报错27复核面积不能大于验收面积) |
| 特殊扣点不能小于0大于100% | 保存 | 特殊扣点<0或>100% | 中 | [查看](#报错28特殊扣点不能小于0大于100) |
| 请先填写装修金额信息栏中的发票税点 | 保存 | 发票信息未填写发票税点 | 中 | [查看](#报错29请先填写装修金额信息栏中的发票税点) |

#### 报错1：验收报销单不存在
- **触发条件**：执行doSelect(额度内兑现新增-详情)、doDelete(删除)、cancel(作废)接口时，按checkBxId调用selectByPrimaryKey查询FIN_FEE_CHECK_BX_HEADER返回null
- **逻辑分析**：详情查询、删除、作废均需先按主键CHECK_BX_ID定位验收报销单。若单据在操作前被其他用户物理删除，或前端传入的checkBxId为错误值/过期值（如列表缓存后他人已删除），selectByPrimaryKey返回null，后端抛出"验收报销单不存在"阻断性异常，后续读取字段（如TERMINAL_APPLY_ID、HZ_APPROVE_STATUS）均无法进行。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态,
         h.create_time        AS 创建时间
  FROM   fin_fee_check_bx_header h
  WHERE  h.check_bx_id = #{传入的checkBxId};
  ```

#### 报错2：装修申请单不存在
- **触发条件**：执行save(保存)/submit(提交)接口时，按验收报销单的TERMINAL_APPLY_ID关联查询FIN_FEE_APPLY_FINISHED_HEADER返回null
- **逻辑分析**：验收报销单必须关联已审批的装修申请单，保存和提交时需回查装修申请单以带入门店/经销商/装修面积/装修标准等信息并校验其状态。若装修申请单在验收报销单保存前被删除，或TERMINAL_APPLY_ID未正确赋值（如LOV选择失败但前端仍提交），关联查询返回null，无法组装保存/提交所需数据，抛出阻断性异常。
- **排查SQL**：
  ```sql
  SELECT c.check_bx_id        AS 验收报销单ID,
         c.check_bx_code      AS 验收报销单号,
         c.terminal_apply_id  AS 装修申请单ID,
         c.terminal_apply_no  AS 装修申请单号,
         f.hz_approve_status  AS 申请单审批状态
  FROM   fin_fee_check_bx_header c
  LEFT JOIN fin_fee_apply_finished_header f
  ON     c.terminal_apply_id = f.fee_apply_id
  WHERE  c.terminal_apply_id IS NOT NULL
  AND    f.fee_apply_id IS NULL
  ORDER  BY c.create_time DESC;
  ```

#### 报错3：不能删除非制单状态的单据
- **触发条件**：执行doDelete(删除)接口时，校验当前单据HZ_APPROVE_STATUS不等于WorkflowInstanceStatus.NEW.getCode()（即非"新建"状态）
- **逻辑分析**：删除操作仅允许在NEW(新建)状态执行，避免删除已进入审批流程或已审批生效的单据造成数据不一致。后端通过WorkflowInstanceStatus.NEW.getCode().equals(hzApproveStatus)校验，若单据已提交审批(RUN)、已审批(APPROVED)、已驳回(REJECTED)、已退回(REBUT)或已作废(INTERRUPT)，校验不通过，抛出"不能删除非制单状态的单据"阻断性异常，删除操作被拒绝。
- **排查SQL**：
  ```sql
  SELECT check_bx_id        AS 验收报销单ID,
         check_bx_code      AS 验收报销单号,
         hz_approve_status  AS 审批状态,
         create_time        AS 创建时间
  FROM   fin_fee_check_bx_header
  WHERE  hz_approve_status <> 'NEW'
  ORDER  BY create_time DESC;
  ```

#### 报错4：装修申请单未审批
- **触发条件**：执行submit(提交)接口时，按TERMINAL_APPLY_ID查询FIN_FEE_APPLY_FINISHED_HEADER的HZ_APPROVE_STATUS不等于APPROVED
- **逻辑分析**：验收报销单提交审批前，需确保关联的装修申请单已审批通过，因为验收报销依赖装修申请单审批通过后确定的装修面积、装修标准、政策补贴等结果。若装修申请单仍处于NEW/RUN/REJECTED/REBUT等状态（如装修申请单被驳回后未重新审批，用户即尝试提交验收报销），后端校验不通过，抛出"装修申请单未审批"阻断性异常，阻止验收报销单提交审批。
- **排查SQL**：
  ```sql
  SELECT c.check_bx_id        AS 验收报销单ID,
         c.check_bx_code      AS 验收报销单号,
         c.terminal_apply_id  AS 装修申请单ID,
         c.terminal_apply_no  AS 装修申请单号,
         f.hz_approve_status  AS 申请单审批状态
  FROM   fin_fee_check_bx_header c
  INNER JOIN fin_fee_apply_finished_header f
  ON     c.terminal_apply_id = f.fee_apply_id
  WHERE  c.hz_approve_status = 'NEW'
  AND    f.hz_approve_status <> 'APPROVED'
  ORDER  BY c.create_time DESC;
  ```

#### 报错5：单据id信息不存在
- **触发条件**：执行doSelect(额度内兑现新增-详情)接口时，传入的checkBxId为null或<=0
- **逻辑分析**：详情查询需按主键CHECK_BX_ID定位验收报销单。若前端传入的checkBxId为null（如未选择记录即查询）、为0或负数（如ID未正确赋值），后端校验不通过抛异常。需核查前端传入的checkBxId是否有效。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.check_bx_id = #{传入的checkBxId};
  ```

#### 报错6：报销单【{checkBxCode}】的额度内有效期未找到
- **触发条件**：onWfComplete审批通过回调时，验收报销单关联的装修申请单IN_VALIDITY_DATE(额度内有效期)为空
- **逻辑分析**：审批通过后需生成额度内兑现记录，兑现记录需关联额度内有效期。若装修申请单未维护IN_VALIDITY_DATE（如装修标准未配置有效期、或有效期字段未回写），无法生成兑现记录，抛出阻断性异常。需在装修申请单或装修标准中维护额度内有效期。
- **排查SQL**：
  ```sql
  SELECT c.check_bx_code      AS 验收报销单号,
         c.terminal_apply_id  AS 装修申请ID,
         f.in_validity_date   AS 额度内有效期,
         c.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header c
  INNER JOIN fin_fee_apply_finished_header f
  ON     c.terminal_apply_id = f.terminal_apply_id
  WHERE  c.hz_approve_status = 'RUN'
  AND    f.in_validity_date IS NULL
  ORDER  BY c.create_time DESC;
  ```

#### 报错7：未找到对应的事业部基础设置，请联系相关业务员处理
- **触发条件**：save保存操作时，按当前用户查询事业部基础设置DIVISION_BASE_SET返回null
- **逻辑分析**：验收报销单需关联事业部用于生成单号、组织ID等。若当前用户未配置事业部基础设置、事业部被删除、或用户与事业部关联关系缺失，查询返回null抛异常。需联系业务员配置事业部基础设置。
- **排查SQL**：
  ```sql
  SELECT d.division_id      AS 事业部ID,
         d.division_code    AS 事业部编码,
         d.division_name    AS 事业部名称,
         d.organization_id  AS 组织ID
  FROM   division_base_set d
  WHERE  d.division_id = #{当前用户事业部ID};
  ```

#### 报错8：作废失败，门店验收与报销单查询为空
- **触发条件**：cancel作废操作时，按checkBxId查询FIN_FEE_CHECK_BX_HEADER返回null
- **逻辑分析**：作废需先校验验收报销单存在。若单据在作废期间被其他用户删除、checkBxId传值错误、或并发场景下被清理，查询返回空抛异常。需刷新列表页重新获取有效数据。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态,
         h.create_time        AS 创建时间
  FROM   fin_fee_check_bx_header h
  WHERE  h.check_bx_id = #{传入的checkBxId};
  ```

#### 报错9：支付方式为折扣折让时，才允许提前兑现
- **触发条件**：save保存或submit提交时，payType(支付方式)≠3(折扣折让)但earlyEncashmentFlag(提前兑现标识)为提前兑现
- **逻辑分析**：提前兑现仅限支付方式为折扣折让(payType=3)的单据。若非折扣折让支付方式设置了提前兑现标识（如用户误选提前兑现、或数据迁移异常），校验不通过抛异常。需修改支付方式为折扣折让或取消提前兑现标识。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id            AS 验收报销单ID,
         h.check_bx_code          AS 验收报销单号,
         h.pay_type               AS 支付方式,
         h.early_encashment_flag  AS 提前兑现标识
  FROM   fin_fee_check_bx_header h
  WHERE  h.pay_type <> 3
  AND    h.early_encashment_flag IS NOT NULL
  AND    h.early_encashment_flag <> '0';
  ```

#### 报错10：单据信息不合法
- **触发条件**：submit提交操作时，校验单据关键字段（如terminalApplyId/tradingCompanyId/billingUnitId等）缺失或格式错误
- **逻辑分析**：提交审批需确保单据关键字段完整合法。若必填字段为null（如未选择装修申请单、未维护交易公司）、字段格式错误（如ID为0或负数）、或关联关系不完整，校验不通过抛异常。需检查单据填写完整性。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id           AS 验收报销单ID,
         h.check_bx_code         AS 验收报销单号,
         h.terminal_apply_id     AS 装修申请ID,
         h.trading_company_id    AS 交易公司ID,
         h.billing_unit_id       AS 开票单位ID,
         h.hz_approve_status     AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    (h.terminal_apply_id IS NULL
       OR h.trading_company_id IS NULL
       OR h.billing_unit_id IS NULL);
  ```

#### 报错11：事业部信息不合法
- **触发条件**：submit提交操作时，事业部ID/编码为空或与当前用户事业部不匹配
- **逻辑分析**：提交需确保事业部信息合法。若单据未关联事业部、事业部被删除、或当前用户事业部与单据事业部不一致（跨事业部操作），校验不通过抛异常。需核查事业部关联一致性。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.organization_id    AS 组织ID,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    h.organization_id IS NULL;
  ```

#### 报错12：门店装修申请与进度更新单据预算年度大于当前年度，无法发起验收
- **触发条件**：submit提交操作时，budYear-LocalDate.now().getYear()>0（预算年度大于当前年份）
- **逻辑分析**：验收报销要求预算年度≤当前年份（预算需在已生效年度内）。若关联装修申请单的budYear大于当前年份（如装修申请单预算年度2027但当前2026年），无法发起验收，校验不通过抛异常。需核查装修申请单预算年度配置。
- **排查SQL**：
  ```sql
  SELECT c.check_bx_id        AS 验收报销单ID,
         c.check_bx_code      AS 验收报销单号,
         c.bud_year           AS 预算年度,
         TO_NUMBER(TO_CHAR(SYSDATE,'YYYY')) AS 当前年份
  FROM   fin_fee_check_bx_header c
  WHERE  c.hz_approve_status = 'NEW'
  AND    c.bud_year > TO_NUMBER(TO_CHAR(SYSDATE,'YYYY'))
  ORDER  BY c.create_time DESC;
  ```

#### 报错13：当前单据事业部异常，请联系相关人员处理
- **触发条件**：submit提交操作时，单据事业部与当前用户事业部不一致或单据事业部数据异常
- **逻辑分析**：提交需确保单据事业部与操作用户事业部一致，防止跨事业部操作。若单据事业部被变更、用户切换事业部后操作旧单据、或事业部数据不一致，校验不通过抛异常。需联系相关人员核查事业部配置。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.organization_id    AS 单据组织ID,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    h.organization_id <> #{当前用户组织ID};
  ```

#### 报错14：{budYear}的预算剩余可用额度不足
- **触发条件**：submit提交操作时，viewOverBudgetAmt查询预算剩余可用额度返回null或<0
- **逻辑分析**：验收报销占用年度预算总额，需确保预算充足。校验逻辑查询budYear年份同事业部的剩余可用预算(viewOverBudgetAmt)，若返回null（预算未配置）或<0（已超额）即抛异常。需联系财务确认预算或调整报销金额。
- **排查SQL**：
  ```sql
  SELECT b.bud_year              AS 预算年度,
         b.division_id           AS 事业部ID,
         b.total_amt             AS 年度总预算,
         NVL((SELECT SUM(h.in_bx_amt + h.out_bx_amt)
              FROM   fin_fee_check_bx_header h
              WHERE  h.bud_year = b.bud_year
              AND    h.hz_approve_status IN ('RUN','APPROVED')), 0) AS 已占用金额,
         b.total_amt - NVL((SELECT SUM(h.in_bx_amt + h.out_bx_amt)
              FROM   fin_fee_check_bx_header h
              WHERE  h.bud_year = b.bud_year
              AND    h.hz_approve_status IN ('RUN','APPROVED')), 0) AS 剩余可用额度
  FROM   fin_fee_check_bx_header b
  ORDER  BY b.bud_year DESC;
  ```

#### 报错15：以下所选设计师与保证书不一致：{errorList}
- **触发条件**：submit提交操作时，校验所选设计师与保证书(GuaranteeBook)中设计师不一致，收集不一致设计师拼接为errorList
- **逻辑分析**：验收报销需确保所选设计师与保证书设计师一致，保证责任对应。若用户选择了保证书未涉及的设计师、保证书设计师变更后未同步、或数据不一致，校验不通过抛异常。需核查设计师与保证书关联一致性。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id     AS 验收报销单ID,
         h.check_bx_code   AS 验收报销单号,
         h.designer         AS 展示设计师,
         h.soft_designer    AS 软装设计师,
         h.hz_approve_status AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  ORDER  BY h.create_time DESC;
  ```

#### 报错16：扣除比例不可小于0
- **触发条件**：详情页保存时，前端校验扣除比例字段<0
- **逻辑分析**：扣除比例表示扣减百分比，不可为负数。若用户输入负值或计算异常导致扣除比例为负，前端notification.error提示并阻断保存。需修改扣除比例至≥0。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.deduct_proportion  AS 扣除比例
  FROM   fin_fee_check_bx_header h
  WHERE  h.deduct_proportion < 0;
  ```

#### 报错17：扣除比例不可大于验收比例
- **触发条件**：详情页保存时，前端校验扣除比例>验收比例
- **逻辑分析**：扣除比例应≤验收比例，防止扣减金额超过验收金额。若用户输入的扣除比例超过验收比例，前端notification.error提示并阻断保存。需修改扣除比例至≤验收比例。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.deduct_proportion  AS 扣除比例
  FROM   fin_fee_check_bx_header h
  WHERE  h.deduct_proportion > h.check_over_day;
  ```

#### 报错18：发票税点必须大于0小于等于13
- **触发条件**：详情页保存时，前端校验invoiceTaxRate(发票税点)<=0或>13
- **逻辑分析**：发票税点合法范围为(0, 13]，对应常见税率1%/3%/6%/9%/13%等。若用户输入0、负数或>13的非法值，前端notification.error提示并阻断保存。需修改发票税点至合法范围。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.invoice_tax_rate   AS 发票税点
  FROM   fin_fee_check_bx_header h
  WHERE  h.invoice_tax_rate <= 0
  OR     h.invoice_tax_rate > 13;
  ```

#### 报错19：请在附件上传OA签呈减免申请附件
- **触发条件**：submit提交操作时，前端校验OA签呈减免申请附件未上传
- **逻辑分析**：涉及减免的验收报销需上传OA签呈减免申请附件作为凭证。若用户未上传附件即提交、或附件上传失败，前端notification.error提示并阻断提交。需先上传OA签呈减免申请附件再提交。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    NOT EXISTS (SELECT 1 FROM obj_attach_rel a
                     WHERE a.obj_id = h.check_bx_id
                     AND a.attach_conf_id = #{OA签呈减免附件配置ID});
  ```

#### 报错20：财务复核结果:"补贴标准"大于0,"可报销金额-含税(元)"小于等于0，不能提交
- **触发条件**：submit提交操作时，前端校验财务复核结果中补贴标准>0且可报销金额含税<=0
- **逻辑分析**：补贴标准>0时应有可报销金额，若可报销金额含税<=0（如扣减后金额为0或负数），业务上无报销意义，校验不通过抛异常。需核查财务复核结果和扣减计算。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.soft_purchase_standard AS 补贴标准,
         h.in_bx_amt         AS 额度内报销金额,
         h.out_bx_amt        AS 额度外报销金额,
         h.hz_approve_status AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    h.soft_purchase_standard > 0
  AND    NVL(h.in_bx_amt, 0) + NVL(h.out_bx_amt, 0) <= 0;
  ```

#### 报错21：请核算验收面积
- **触发条件**：submit提交操作时，前端校验验收面积未核算（如验收面积为空或为0）
- **逻辑分析**：提交需确保验收面积已核算，验收面积是计算报销金额的基础。若用户未点击核算按钮、核算失败、或验收面积为空/0，前端notification.error提示并阻断提交。需先核算验收面积再提交。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.this_terminal_area AS 实际装修面积,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW'
  AND    (h.this_terminal_area IS NULL OR h.this_terminal_area = 0);
  ```

#### 报错22：事业部名称获取失败，请检查
- **触发条件**：详情页加载或保存时，前端查询事业部名称失败
- **逻辑分析**：页面需显示事业部名称用于业务展示。若事业部基础设置查询接口异常、事业部被删除、或网络问题导致查询失败，前端notification.error提示。需检查事业部基础设置配置和网络连接。
- **排查SQL**：
  ```sql
  SELECT d.division_id      AS 事业部ID,
         d.division_code    AS 事业部编码,
         d.division_name    AS 事业部名称
  FROM   division_base_set d
  WHERE  d.division_id = #{当前事业部ID};
  ```

#### 报错23：本次兑现比例不能小于0%或者大于100%
- **触发条件**：详情页保存时，前端校验本次兑现比例<0或>100%
- **逻辑分析**：兑现比例合法范围为[0%, 100%]，表示兑现进度。若用户输入负值或>100%的非法值，前端notification.error提示并阻断保存。需修改兑现比例至合法范围。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'NEW';
  ```

#### 报错24：验收面积不可大于申请面积
- **触发条件**：详情页保存时，前端校验验收面积>申请面积
- **逻辑分析**：验收面积应≤申请面积，防止验收超过申请范围。若用户输入的验收面积超过装修申请单的申请面积，前端notification.error提示并阻断保存。需修改验收面积至≤申请面积。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id          AS 验收报销单ID,
         h.check_bx_code        AS 验收报销单号,
         h.this_terminal_area   AS 验收面积,
         f.this_terminal_area   AS 申请面积
  FROM   fin_fee_check_bx_header h
  INNER JOIN fin_fee_apply_finished_header f
  ON     h.terminal_apply_id = f.terminal_apply_id
  WHERE  h.this_terminal_area > f.this_terminal_area;
  ```

#### 报错25：验收标准不能大于申请标准
- **触发条件**：详情页保存时，前端校验验收标准>申请标准
- **逻辑分析**：验收标准应≤申请标准，防止验收标准超过申请范围。若用户输入的验收标准超过装修申请单的申请标准，前端notification.error提示并阻断保存。需修改验收标准至≤申请标准。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id          AS 验收报销单ID,
         h.check_bx_code        AS 验收报销单号,
         f.in_apply_standard    AS 申请标准,
         h.hz_approve_status    AS 审批状态
  FROM   fin_fee_check_bx_header h
  INNER JOIN fin_fee_apply_finished_header f
  ON     h.terminal_apply_id = f.terminal_apply_id
  WHERE  h.hz_approve_status = 'NEW';
  ```

#### 报错26：复核标准不能大于验收标准
- **触发条件**：详情页保存时，前端校验复核标准>验收标准
- **逻辑分析**：财务复核标准应≤验收标准，防止复核超过验收范围。若财务复核节点输入的复核标准超过验收标准，前端notification.error提示并阻断保存。需修改复核标准至≤验收标准。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'RUN';
  ```

#### 报错27：复核面积不能大于验收面积
- **触发条件**：详情页保存时，前端校验复核面积>验收面积
- **逻辑分析**：财务复核面积应≤验收面积，防止复核超过验收范围。若财务复核节点输入的复核面积超过验收面积，前端notification.error提示并阻断保存。需修改复核面积至≤验收面积。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.this_terminal_area AS 验收面积,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status = 'RUN';
  ```

#### 报错28：特殊扣点不能小于0大于100%
- **触发条件**：详情页保存时，前端校验特殊扣点<0或>100%
- **逻辑分析**：特殊扣点合法范围为[0%, 100%]，表示特殊扣减比例。若用户输入负值或>100%的非法值，前端notification.error提示并阻断保存。需修改特殊扣点至合法范围。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.hz_approve_status IN ('NEW','RUN');
  ```

#### 报错29：请先填写装修金额信息栏中的发票税点
- **触发条件**：详情页保存发票信息时，前端校验装修金额信息栏中的发票税点未填写
- **逻辑分析**：发票信息需关联装修金额信息栏的发票税点。若用户在发票信息栏操作但装修金额信息栏未填写发票税点，前端notification.error提示并阻断操作。需先在装修金额信息栏填写发票税点。
- **排查SQL**：
  ```sql
  SELECT h.check_bx_id        AS 验收报销单ID,
         h.check_bx_code      AS 验收报销单号,
         h.invoice_tax_rate   AS 发票税点,
         h.hz_approve_status  AS 审批状态
  FROM   fin_fee_check_bx_header h
  WHERE  h.invoice_tax_rate IS NULL
  AND    h.hz_approve_status IN ('NEW','RUN');
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
