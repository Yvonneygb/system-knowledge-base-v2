# 经销合同管理-合同管理-年度营销政策

## 业务流程

### 业务流程图

```text
年度营销政策页面
        │
        ├── 查询政策列表 ──→ GET /annual-dealer-policy-heads/detail
        │
        ├── 保存/修改 ──→ POST /annual-dealer-policy-heads/insert 或 /update
        │
        ├── 生效 ──→ POST /annual-dealer-policy-heads/makeEffective
        │
        ├── 失效 ──→ POST /annual-dealer-policy-heads/failure
        │
        ├── 推送ERP ──→ POST /sa-policy-year-headers/pushErpAndCrm
        │
        └── 查询年度合同执行明细 ──→ GET /sa-policy-year-lines/select
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 年度经销合同 | 数据依赖 | 营销政策关联年度经销合同 | 合同已签订 |
| 经销商主数据 | 数据依赖 | 政策按经销商配置 | 经销商已创建 |
| ERP系统 | 服务依赖 | 政策推送至ERP系统 | ERP系统可用 |

### 下游影响

- 影响1：合同执行
  - 营销政策影响合同任务拆分和执行

- 影响2：ERP/CRM系统
  - 政策推送至ERP和CRM系统，影响下游业务

---

## 重点逻辑

### 重点逻辑1：政策生效与失效 {状态管理}

- **业务意义**：营销政策有有效期控制，生效后才能被引用，失效后不再适用

- **具体逻辑描述**

  - 第1点：通过makeEffective接口使政策生效

  - 第2点：通过failure接口使政策失效

  - 第3点：仅生效状态的政策可被合同引用

### 重点逻辑2：ERP/CRM推送 {数据推送}

- **业务意义**：营销政策需推送至ERP和CRM系统，确保下游系统数据同步

- **具体逻辑描述**

  - 第1点：通过pushErpAndCrm接口推送政策至ERP和CRM

  - 第2点：支持更新ERP总账日期(modifyLegerDate)

---

## 详细逻辑

### 界面模块1：年度营销政策列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 政策头ID | ANNUAL_DEALER_POLICY_HEAD.POLICY_HEAD_ID | 文本框 | 政策头ID | 常显 | 系统生成 |
| 年度 | ANNUAL_DEALER_POLICY_HEAD.POLICY_YEAR | 文本框 | 政策年度 | 常显 | 手动选择 |
| 经销商 | ANNUAL_DEALER_POLICY_HEAD.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 事业部 | ANNUAL_DEALER_POLICY_HEAD.ENTNAME | 文本框 | 事业部名称 | 常显 | 选择事业部带出 |
| 政策状态 | ANNUAL_DEALER_POLICY_HEAD.STATUS | 文本框 | 政策状态 | 常显 | 系统更新 |

### 选择弹窗

> 本页面查询条件使用文本输入和下拉选择，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新增 | 新建营销政策 | 列表页 | 始终可用 | 打开新建页面 |
| 保存 | 保存政策信息 | 编辑页 | 编辑状态 | 调用insert或update接口 |
| 生效 | 使政策生效 | 编辑页 | 已保存状态 | 调用makeEffective接口 |
| 失效 | 使政策失效 | 编辑页 | 已生效状态 | 调用failure接口 |
| 推送ERP | 推送至ERP/CRM | 列表页 | 已生效状态 | 调用pushErpAndCrm接口 |

### 保存校验

- 校验1：年度不能为空 —— 确保政策归属明确年度

  - 详细逻辑

    - 第1点：保存时校验年度不为空

  - 系统体现：toast提醒

  - 排查SQL：

    ```sql
    SELECT * FROM ANNUAL_DEALER_POLICY_HEAD WHERE POLICY_YEAR IS NULL;
    ```

### 提交校验

> 本页面无提交审批流程，通过生效操作控制政策可用性。

### 状态机

#### 状态机流转图

```text
新建 ──保存──→ 已保存 ──生效──→ 已生效 ──失效──→ 已失效
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| 已保存 | 已保存未生效 | 编辑、保存、生效 |
| 已生效 | 政策已生效 | 失效、推送ERP |
| 已失效 | 政策已失效 | 查看 |

---

## 数据库表详解

### 表1：ANNUAL_DEALER_POLICY_HEAD（年度经销商政策头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| POLICY_HEAD_ID | BIGINT | 主键ID | 政策头ID | 自增主键 |
| POLICY_YEAR | VARCHAR | 政策年度 | 年度 | 手动选择 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商 | 选择经销商带出 |
| ENTNAME | VARCHAR | 事业部名称 | 事业部 | 选择事业部带出 |
| STATUS | VARCHAR | 政策状态 | 政策状态 | 系统更新 |

### 表2：SA_POLICY_YEAR_HEADER（年度政策头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| SA_POLICY_YEAR_HEADER_ID | BIGINT | 主键ID | - | 自增主键 |

### 表3：SA_POLICY_YEAR_LINE（年度政策行表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| SA_POLICY_YEAR_LINE_ID | BIGINT | 主键ID | - | 自增主键 |
| SA_POLICY_YEAR_HEADER_ID | BIGINT | 关联头表ID | - | FK → SA_POLICY_YEAR_HEADER |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 年度不能为空 | 保存时 | 未选择年度，选择后保存 | toast提醒 | [查看] |
| ERP推送失败 | 推送ERP时 | ERP系统不可用或数据异常 | toast提醒 | [查看] |
| 参数id不能为空 | 查询详情时 | 未传入政策头ID，重新进入页面 | toast提醒 | [查看] |
| 配置类型行不能为空 | 保存时 | 未添加政策配置行，添加行后保存 | toast提醒 | [查看] |
| 数据的审核状态异常 | 修改时 | 政策状态非"未生效"，需先失效再修改 | 阻断性报错 | [查看] |
| 数据的状态异常 | 生效时 | 政策状态非"未生效"，无法生效 | 阻断性报错 | [查看] |
| 执行值类型不能为空 | 保存时 | 违约金行未配置执行值类型，补全后保存 | toast提醒 | [查看] |
| 经销商信息异常 | 推送ERP时 | 经销商编码在系统中无对应等级，核对经销商主数据 | 阻断性报错 | [查看] |
| 对应的出库总额未找到 | 推送ERP时 | 合同无出库总额记录，先确认出库数据 | 阻断性报错 | [查看] |
| 次年折扣分段区间未找到对应数值 | 推送ERP时 | 完成率未落入任何折扣区间，补全区间配置 | 阻断性报错 | [查看] |
| 返点政策行配置为空 | 推送ERP时 | 合同关联返点行已删除，重新关联合同 | 阻断性报错 | [查看] |
| 违约金政策行配置为空 | 推送ERP时 | 合同关联违约金行已删除，重新关联合同 | 阻断性报错 | [查看] |
| 未找到对应的B/B1类系统参数值 | 推送ERP时 | 系统参数未配置B/B1类折扣比例，联系管理员 | 阻断性报错 | [查看] |

#### 报错1：年度不能为空
- **触发条件**：用户在新建/编辑页未选择政策年度（POLICY_YEAR）直接点击保存
- **逻辑分析**：年度营销政策按年度（POLICY_YEAR）归属，年度是政策生效期和合同引用的前置维度。未选择年度将导致政策无法被年度经销合同按年度引用，也无法推送ERP（ERP按年度建账）。校验POLICY_YEAR非空，toast提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT POLICY_HEAD_ID, POLICY_YEAR, CUSTOMER_NAME, ENTNAME, STATUS
  FROM ANNUAL_DEALER_POLICY_HEAD
  WHERE POLICY_YEAR IS NULL;
  ```

#### 报错2：ERP推送失败
- **触发条件**：用户对已生效政策点击"推送ERP"，pushErpAndCrm接口推送至ERP/CRM时返回失败
- **逻辑分析**：推送接口将营销政策推送至ERP和CRM系统，支持更新ERP总账日期（modifyLegerDate）。失败根因有三类：(1)ERP/CRM系统不可用或网络中断；(2)推送数据异常，如政策年度在ERP中不存在、经销商编码在ERP中不匹配、政策行明细数据缺失；(3)ERP侧重复推送或政策状态非"已生效"（仅已生效政策可推送）。需确认政策STATUS为已生效及ERP/CRM连接状态
- **排查SQL**：
  ```sql
  SELECT POLICY_HEAD_ID, POLICY_YEAR, CUSTOMER_NAME, ENTNAME, STATUS
  FROM ANNUAL_DEALER_POLICY_HEAD
  WHERE STATUS = '已生效';
  -- 核查年度政策头表推送相关字段
  SELECT SA_POLICY_YEAR_HEADER_ID, PUSH_STATUS, PUSH_TIME, ERROR_MSG
  FROM SA_POLICY_YEAR_HEADER
  WHERE PUSH_STATUS != 'SUCCESS' OR PUSH_STATUS IS NULL;
  ```

#### 报错3：参数id不能为空
- **触发条件**：用户进入年度营销政策详情页，detail接口未传入政策头ID（id为null）
- **逻辑分析**：AnnualDealerPolicyHeadServiceImpl.detail方法首行校验id非空，id为null时抛CommonException。根因是前端跳转详情页时未携带POLICY_HEAD_ID参数，或URL参数丢失。需重新从列表页点击"查看"进入详情页
- **排查SQL**：
  ```sql
  SELECT POLICY_HEAD_ID, POLICY_YEAR, CUSTOMER_NAME FROM ANNUAL_DEALER_POLICY_HEAD
  WHERE POLICY_HEAD_ID = #{id};
  ```

#### 报错4：配置类型行不能为空
- **触发条件**：用户新建营销政策未添加任何配置类型行（lines为空集合）直接点击保存
- **逻辑分析**：AnnualDealerPolicyHeadServiceImpl.insert方法校验dto.getLines()非空，政策头必须关联至少一行配置（折扣/返点/违约金）。无配置行的政策无业务意义，无法被合同引用计算折扣返点。需在编辑页点击"增加行"添加配置类型行后保存
- **排查SQL**：
  ```sql
  SELECT H.POLICY_HEAD_ID, H.POLICY_YEAR, COUNT(L.LINE_ID) AS 配置行数
  FROM ANNUAL_DEALER_POLICY_HEAD H
  LEFT JOIN ANNUAL_DEALER_POLICY_LINE L ON H.POLICY_HEAD_ID = L.HEAD_ID
  GROUP BY H.POLICY_HEAD_ID, H.POLICY_YEAR
  HAVING COUNT(L.LINE_ID) = 0;
  ```

#### 报错5：数据的审核状态异常
- **触发条件**：用户对状态非"未生效"（POLICY_STATUS_DIS_NOT_ENABLE）的政策点击修改并提交update接口
- **逻辑分析**：AnnualDealerPolicyHeadServiceImpl.update方法校验dto.getPolicyStatus()必须为POLICY_STATUS_DIS_NOT_ENABLE（未生效），其他状态（已生效、已失效）不允许修改。已生效政策被合同引用，修改会影响已结算数据；已失效政策为历史归档。需先将政策失效再修改
- **排查SQL**：
  ```sql
  SELECT POLICY_HEAD_ID, POLICY_YEAR, POLICY_STATUS FROM ANNUAL_DEALER_POLICY_HEAD
  WHERE POLICY_HEAD_ID = #{id} AND POLICY_STATUS != 'DIS_NOT_ENABLE';
  ```

#### 报错6：数据的状态异常
- **触发条件**：用户对状态非"未生效"的政策点击"生效"按钮，makeEffective接口校验不通过
- **逻辑分析**：AnnualDealerPolicyHeadServiceImpl.makeEffective方法校验dto非空且POLICY_STATUS为POLICY_STATUS_DIS_NOT_ENABLE（未生效），其他状态无法生效。生效逻辑会先将同事业部+年度已生效政策置为失效，再将当前政策置为已生效。需确认政策状态为未生效再生效
- **排查SQL**：
  ```sql
  SELECT POLICY_HEAD_ID, POLICY_YEAR, ENTID, POLICY_STATUS FROM ANNUAL_DEALER_POLICY_HEAD
  WHERE POLICY_HEAD_ID = #{id} AND POLICY_STATUS != 'DIS_NOT_ENABLE';
  ```

#### 报错7：执行值类型不能为空
- **触发条件**：用户添加违约金类型（EXECUTE_TYPE_BREACH）配置行，未填写执行值类型（EXECUTE_VAL_TYPE）或执行值（EXECUTE_VAL）直接保存
- **逻辑分析**：AnnualDealerPolicyHeadServiceImpl.validateLine方法对违约金行校验executeValType和executeVal非空。违约金计算需依据执行值类型（按金额或按比例）和执行值，缺失将导致违约金无法计算。需补全执行值类型和执行值后保存
- **排查SQL**：
  ```sql
  SELECT LINE_ID, HEAD_ID, EXECUTE_TYPE, EXECUTE_VAL_TYPE, EXECUTE_VAL
  FROM ANNUAL_DEALER_POLICY_LINE
  WHERE EXECUTE_TYPE = 'BREACH'
    AND (EXECUTE_VAL_TYPE IS NULL OR EXECUTE_VAL IS NULL);
  ```

#### 报错8：经销商信息异常
- **触发条件**：推送ERP/CRM时，saPolicyYearHeaderMapper.getCustomerOrgRankByCode返回空，经销商在系统中无对应等级
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.getSuccessfulRecords方法通过getCustomerOrgRankByCode查询经销商等级（rank），rank为空时抛CommonException。等级用于决定折扣修正值计算方式（1=直营，2=B类，3=B1类）。需核对经销商主数据是否配置等级
- **排查SQL**：
  ```sql
  SELECT C.CUSTOMER_ID, C.CUSTOMER_CODE, C.CUSTOMER_NAME, C.RANK
  FROM CUSTOMER C
  WHERE C.CUSTOMER_CODE = #{custCode} AND (C.RANK IS NULL OR C.RANK = '');
  ```

#### 报错9：对应的出库总额未找到
- **触发条件**：推送ERP/CRM计算完成率或返点时，ANNUAL_OUTBOUND_AMOUNT表无该合同的出库总额记录
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.getCompletionRate方法查询ANNUAL_OUTBOUND_AMOUNT表，若结果集为空抛CommonException。出库总额是完成率计算的分母来源，缺失意味着合同从未执行出库总额计算任务。需先执行出库总额计算任务（MktSaPolicyYearIntfJob）
- **排查SQL**：
  ```sql
  SELECT S.SA_CONTR_HEAD_ID, S.CONTRACT_NO, A.SALE_AMOUNT AS 出库总额
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN ANNUAL_OUTBOUND_AMOUNT A ON S.SALE_CONTRACT_HEAD_ID = A.SA_CONTR_HEAD_ID
  WHERE S.SALE_CONTRACT_HEAD_ID = #{saContrHeadId} AND A.SALE_AMOUNT IS NULL;
  ```

#### 报错10：次年折扣分段区间未找到对应数值
- **触发条件**：推送ERP/CRM计算次年折扣时，合同完成率未落入任何折扣分段区间（START_POINT, END_POINT）
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.calculateDiscount方法通过matchingInterval匹配完成率所在区间，未匹配时tierVal为0，抛CommonException。根因是政策行未配置覆盖该完成率的区间（如完成率105%但区间仅配置到100%）。需在政策编辑页补全区间配置
- **排查SQL**：
  ```sql
  SELECT T.LINE_ID, T.START_POINT, T.END_POINT, T.TIER_VAL
  FROM ANNUAL_DEALER_POLICY_TIERS T
  WHERE T.LINE_ID = #{discountId}
    AND #{completionRate} BETWEEN T.START_POINT AND T.END_POINT;
  ```

#### 报错11：返点政策行配置为空
- **触发条件**：推送ERP/CRM计算返点时，annualDealerPolicyLineRepository.selectByPrimaryKey(rebateId)返回null
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.calculateRatesAndDiscounts方法在完成率≥100%时查询返点政策行，policyLine为null时抛CommonException。根因是合同关联的返点行（REBATE_ID）已被删除或不存在。需重新为合同关联有效的返点政策行
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.REBATE_ID, L.LINE_ID
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN ANNUAL_DEALER_POLICY_LINE L ON S.REBATE_ID = L.LINE_ID
  WHERE S.REBATE_ID IS NOT NULL AND L.LINE_ID IS NULL;
  ```

#### 报错12：违约金政策行配置为空
- **触发条件**：推送ERP/CRM计算违约金时，annualDealerPolicyLineRepository.selectByPrimaryKey(breachId)返回null
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.calculateBreach方法查询违约金政策行，policyLine为null时抛CommonException。根因是合同关联的违约金行（BREACH_ID）已被删除或不存在。需重新为合同关联有效的违约金政策行
- **排查SQL**：
  ```sql
  SELECT S.SALE_CONTRACT_HEAD_ID, S.CONTRACT_NO, S.BREACH_ID, L.LINE_ID
  FROM SA_SALE_CONTRACT_HEAD S
  LEFT JOIN ANNUAL_DEALER_POLICY_LINE L ON S.BREACH_ID = L.LINE_ID
  WHERE S.BREACH_ID IS NOT NULL AND L.LINE_ID IS NULL;
  ```

#### 报错13：未找到对应的B/B1类系统参数值
- **触发条件**：推送ERP/CRM计算修正值时，经销商等级为B类(2)或B1类(3)，但系统参数未配置Rank_B_Discount或Rank_B1_Discount
- **逻辑分析**：SaPolicyYearHeaderServiceImpl.getCorrectionValue方法根据经销商等级查询系统参数（SYS_PARAM），B类查询Rank_B_Discount，B1类查询Rank_B1_Discount，未找到时抛CommonException。修正值用于调整非直营经销商的出库总额计算。需联系管理员在系统参数配置中补全B/B1类折扣比例
- **排查SQL**：
  ```sql
  SELECT PARAM_CODE, PARAM_VALUE FROM SYS_PARAM
  WHERE ENTID = #{entId}
    AND PARAM_CODE IN ('Rank_B_Discount', 'Rank_B1_Discount');
  ```

### 常见问题

- 问题1：政策无法被合同引用
  - 原因：政策状态不是"已生效"
  - 解决思路：检查SQL `SELECT STATUS FROM ANNUAL_DEALER_POLICY_HEAD WHERE POLICY_HEAD_ID = #{id}`，确认状态为已生效

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
