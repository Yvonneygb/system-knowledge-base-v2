# 门店管理-门店设置-门头验收信息设置

## 业务流程

```
门头验收信息设置列表页(hlod低代码页面)
        │
        ├── 新增 ──→ 填写验收信息设置(验收项目/拍摄要求/是否必填) ──→ 保存
        │
        ├── 编辑 ──→ 修改验收信息设置
        │
        └── 删除 ──→ 删除验收信息设置
```

## 重点逻辑

### 重点逻辑1：验收信息配置

- **业务意义**：配置门店验收时需要检查的验收项目和拍摄要求
- **具体逻辑描述**：
  - 定义验收项目名称和说明
  - 定义拍摄角度及要求
  - 设置是否验收提供/装修提供及数量要求

## 详细逻辑

### 界面模块

本页面为hlod低代码页面，前端嵌入在门店验收与报销单详情页(storeAcceptanceReimbursementInfo)的Tab页签中。后端无独立Controller，查询接口嵌入在FinFeeCheckBxHeaderController的getAcceptanceInfo方法中。

#### 验收信息设置区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|--------------|
| 验收项目 | ACCEPTANCE_ITEM | TextField | 验收项目名称 | 始终 | 用户输入 |
| 验收说明 | ACCEPTANCE_NOTE | TextField | 验收项目说明 | 始终 | 用户输入 |
| 拍摄要求 | SHOOT_REQUIRE | TextField | 拍摄角度及要求 | 始终 | 用户输入 |
| 是否验收提供 | IS_YS_PROVIDE | Select | 是否验收提供 | 始终 | 用户选择 |
| 验收提供数量 | YS_PROVIDE_COUNT | NumberField | 验收提供数量 | 始终 | 用户输入 |
| 是否装修提供 | IS_ZX_PROVIDE | Select | 是否装修提供 | 始终 | 用户选择 |
| 装修提供数量 | ZX_PROVIDE_COUNT | NumberField | 装修提供数量 | 始终 | 用户输入 |

### 后端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取验收信息 | POST | `/v1/{organizationId}/fin-fee-check-bx-headers/get-acceptance-info` | 获取质量信息设置(嵌入在FinFeeCheckBxHeaderController中) |

### 选择弹窗

本页面无选择弹窗。

### 导入

本页面无导入功能。

### 其他按钮

本页面无其他按钮。

### 保存校验

- 校验1：验收项目必填 —— 确保验收项目名称完整
  - **详细逻辑**：前端必填校验
  - **系统体现**：C7N内置校验
  - **排查SQL**：`SELECT * FROM ACCEPTANCE_INFO_SET_LINE WHERE ACCEPTANCE_ITEM IS NULL`

### 提交校验

本页面为设置页面，无提交校验。

### 状态机

本页面为设置页面，无状态机。

### 工作流

本页面无工作流。

## 数据库表详解

### ACCEPTANCE_INFO_SET_LINE（门店验收信息设置明细表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| ACCEPTANCE_ITEM | String | 验收项目名称 | 验收项目 | 用户输入 |
| ACCEPTANCE_NOTE | String | 验收项目说明 | 验收说明 | 用户输入 |
| SHOOT_REQUIRE | String | 拍摄角度及要求 | 拍摄要求 | 用户输入 |
| IS_YS_PROVIDE | Long | 是否验收提供 | 是否验收提供 | 用户选择 |
| YS_PROVIDE_COUNT | Long | 验收提供数量 | 验收提供数量 | 用户输入 |
| IS_ZX_PROVIDE | Long | 是否装修提供 | 是否装修提供 | 用户选择 |
| ZX_PROVIDE_COUNT | Long | 装修提供数量 | 装修提供数量 | 用户输入 |
| ORGANIZATION_ID | Long | 组织ID | - | 系统赋值 |

## 常见问题FAQ

### Q1：验收时无验收信息

**根因**：未配置验收信息设置
**解决方案**：在本页面配置验收项目和拍摄要求

## 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| 无 | - | 独立配置数据 | - |

## 下游影响

- **门店验收与报销单** — 验收时获取验收信息设置，控制验收项目必填

## 报错一览

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 验收信息设置不存在 | getAcceptanceInfo | 未配置验收信息 | 中 | [查看](#报错1验收信息设置不存在) |

#### 报错1：验收信息设置不存在
- **触发条件**：门店验收与报销单详情页加载时调用getAcceptanceInfo接口，查询ACCEPTANCE_INFO_SET_LINE返回空列表
- **逻辑分析**：门店验收报销单详情页Tab页签需展示验收信息设置项，用于控制验收时需检查的验收项目、拍摄要求及是否必填。若管理员未在本设置页面配置任何验收信息行（ACCEPTANCE_INFO_SET_LINE无记录），或按ORGANIZATION_ID过滤后无匹配记录，查询返回空，验收报销单详情页的验收信息Tab无项目可展示，验收人员无法对照拍摄要求和验收项目执行验收，影响验收完整性。该异常为非阻断性提示，需管理员补全验收信息配置。
- **排查SQL**：
  ```sql
  SELECT acceptance_item      AS 验收项目,
         acceptance_note      AS 验收说明,
         shoot_require        AS 拍摄要求,
         is_ys_provide        AS 是否验收提供,
         ys_provide_count     AS 验收提供数量,
         is_zx_provide        AS 是否装修提供,
         zx_provide_count     AS 装修提供数量,
         organization_id      AS 组织ID
  FROM   acceptance_info_set_line
  WHERE  organization_id = #{当前组织ID};
  ```

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2026-08-30 | - | - | 按skill规范重写业务逻辑梳理MD文件 |
