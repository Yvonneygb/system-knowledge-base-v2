# 门店管理-门店报表-门店区域分布

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

门店区域分布报表用于查询门店在各区域的分布情况，按省、市、区县三级展示每个区域的门店数量。数据来源于区域表（SCPAREA）和门店表（MKT_TERMINAL），通过区域表的areatype=4(省)/5(市)/6(区)三级关联，统计每个区县的门店数量。本页面为纯查询报表，仅支持查看和导出，不支持新增、修改、删除等写操作。

**核心流程：**
1. 用户进入门店区域分布报表页面
2. 填写查询条件（组织ID、省份、城市、区县、门店数量下限等）
3. 点击查询，调用后端接口分页返回门店区域分布数据
4. 用户查看报表数据，可导出Excel归档

```
开始 → 设定查询条件 → 点击查询 → 后端查询SCPAREA(省/市/区三级关联)+MKT_TERMINAL(统计门店数) → 返回区域分布数据 → 查看报表/导出Excel → 结束
```

- 上游支撑：区域表（SCPAREA）、门店表（MKT_TERMINAL）、hlod报表引擎
- 下游影响：门店区域分布分析、门店数量统计、Excel导出归档、内部管理决策参考

## 重点逻辑

### 重点逻辑1：纯报表查询页面【只读查询】

- **业务意义**：供内部人员查询门店在各区域的分布情况，了解门店的区域覆盖密度
- **具体逻辑描述**：
  - 本页面为hlod低代码报表页面，无独立前端源码
  - 仅提供查询和导出功能，不支持新增、修改、删除操作
  - 数据来源于区域表和门店表的关联查询

### 重点逻辑2：省市区三级关联

- **业务意义**：通过区域表的areatype和superid实现省→市→区三级关联
- **具体逻辑描述**：
  - 省级（sa1）：`SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 4`
  - 市级（sa2）：`SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 5`，关联条件 `sa1.areaid = sa2.superid`
  - 区县级（sa3）：`SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 6`，关联条件 `sa2.areaid = sa3.superid`

### 重点逻辑3：门店数量统计

- **业务意义**：统计每个区县的门店数量
- **具体逻辑描述**：
  - 子查询mt：`SELECT county_areaid, COUNT(county_areaid) AS terminal_num FROM epms.mkt_terminal WHERE entid = #{orgId} GROUP BY county_areaid`
  - 通过 `sa3.areaid = mt.county_areaid` 关联到区县级
  - 无门店的区域显示0：`NVL(mt.terminal_num, 0)`

### 重点逻辑4：门店数量下限筛选

- **业务意义**：支持按门店数量下限筛选，查找门店数量不足的区域
- **具体逻辑描述**：
  - 当terminalNumLow不为空且>=0时，筛选 `terminal_num < #{terminalNumLow}`
  - 用于查找门店数量低于下限的区域，辅助门店拓展决策

## 详细逻辑

### 界面模块

#### 查询条件区域

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 组织ID | ORG_ID | 隐藏 | 当前用户组织ID | 常显 | 自动获取，必填，用于筛选门店entid |
| 省份ID | PROVINCE_ID | 下拉选择框 | 按省份筛选 | 常显 | 用户选择，精确匹配 |
| 城市ID | CITY_ID | 下拉选择框 | 按城市筛选 | 常显 | 用户选择，精确匹配 |
| 区县ID | COUNTY_ID | 下拉选择框 | 按区县筛选 | 常显 | 用户选择，精确匹配 |
| 门店数量下限 | TERMINAL_NUM_LOW | 数值输入框 | 筛选门店数量低于此值的区域 | 常显 | 用户输入，筛选terminal_num < 此值 |

#### 报表数据区域

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 省份区域名称 | PROVINCE_AREANAME | 文本 | 省份名称 | 常显 | 来自scparea(areatype=4) |
| 省份ID | PROVINCE_ID | 隐藏 | 省份ID | 常显 | 来自scparea(areatype=4) |
| 城市区域名称 | CITY_AREANAME | 文本 | 城市名称 | 常显 | 来自scparea(areatype=5) |
| 城市ID | CITY_ID | 隐藏 | 城市ID | 常显 | 来自scparea(areatype=5) |
| 区县区域名称 | COUNTY_AREANAME | 文本 | 区县名称 | 常显 | 来自scparea(areatype=6) |
| 区县ID | COUNTY_ID | 隐藏 | 区县ID | 常显 | 来自scparea(areatype=6) |
| 门店数量 | TERMINAL_NUM | 数值 | 该区县的门店数量 | 常显 | COUNT(mkt_terminal WHERE county_areaid=区县ID AND entid=orgId)，无门店显示0 |

#### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 查询门店区域分布数据 | 列表页查询区域 | 常显 | 调用POST /mkt-store-areal-distribution/search接口，分页返回区域分布数据 |
| 导出 | 导出报表数据为Excel | 列表页查询区域 | 常显 | 导出当前查询结果为Excel文件 |

#### 按钮1：查询（列表页查询区域）

- **业务意义**：根据查询条件搜索门店区域分布数据
- **具体逻辑描述**：
  - 点击查询按钮，触发POST `/v1/{organizationId}/terminalReport/mkt-store-areal-distribution/search` 接口
  - 请求参数为MktTerminalArealDistributionSearchDTO，包含orgId、provinceId、cityId、countyId、terminalNumLow
  - 后端通过PageHelper.doPageAndSort实现分页查询
  - 查询scparea表省市区三级关联，统计mkt_terminal门店数量
  - 返回MktTerminalArealDistributionSearchVO分页结果

#### 按钮2：导出（列表页查询区域）

- **业务意义**：将查询结果导出为Excel文件
- **具体逻辑描述**：
  - 点击导出按钮，将当前查询条件下的报表数据导出为Excel

### 后端接口

| 接口名称 | 请求方式 | 接口路径 | 权限 | 说明 |
|----------|----------|----------|------|------|
| 门店区域分布报表查询 | POST | `/v1/{organizationId}/terminalReport/mkt-store-areal-distribution/search` | 组织级权限 | 分页查询门店区域分布数据 |

**接口入参（MktTerminalArealDistributionSearchDTO）：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orgId | Long | 是 | 组织ID，用于筛选门店entid |
| provinceId | Long | 否 | 省份ID（精确匹配） |
| cityId | Long | 否 | 城市ID（精确匹配） |
| countyId | Long | 否 | 区县ID（精确匹配） |
| searchFlag | Integer | 否 | 搜索标志 |
| terminalNumLow | Integer | 否 | 门店数量下限，筛选terminal_num < 此值 |

### 后端接口Mapper SQL

```sql
-- 门店区域分布报表查询
SELECT * FROM (
    SELECT
        sa1.areaname AS province_areaname,
        sa1.areaid AS province_id,
        sa2.areaname AS city_areaname,
        sa2.areaid AS city_id,
        sa3.areaname AS county_areaname,
        sa3.areaid AS county_id,
        NVL(mt.terminal_num, 0) AS terminal_num
    FROM (
        SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 4 ORDER BY areaid ASC
    ) sa1
    LEFT JOIN (
        SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 5 ORDER BY areaid ASC
    ) sa2 ON sa1.areaid = sa2.superid
    LEFT JOIN (
        SELECT areaname, areaid, superid FROM epms.scparea WHERE areatype = 6 ORDER BY areaid ASC
    ) sa3 ON sa2.areaid = sa3.superid
    LEFT JOIN (
        SELECT county_areaid, COUNT(county_areaid) AS terminal_num
        FROM epms.mkt_terminal
        WHERE entid = #{orgId}
        GROUP BY county_areaid
    ) mt ON sa3.areaid = mt.county_areaid
)
WHERE 1 = 1
    AND province_id = #{provinceId}          -- 省份ID（精确）
    AND city_id = #{cityId}                  -- 城市ID（精确）
    AND county_id = #{countyId}              -- 区县ID（精确）
    AND terminal_num < #{terminalNumLow}     -- 门店数量下限筛选
```

### 状态机

> 本页面为纯查询报表页面，无状态流转。

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 区域表 | 数据来源 | 提供省市区三级区域数据（areatype=4/5/6） | 区域数据已维护 |
| 门店表 | 数据来源 | 统计每个区县的门店数量 | 门店档案已创建 |

### 下游影响

- 门店区域分布分析：管理层通过报表了解门店在各省市区县的分布情况
- 门店数量统计：统计每个区域的门店数量，辅助门店拓展决策
- Excel导出归档：导出报表数据供内部管理决策与归档使用

## 数据库表详解

### SCPAREA（区域表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|------------|------|
| AREAID | NUMBER | 区域ID(主键) | 省份ID/城市ID/区县ID | 主键 |
| AREANAME | VARCHAR2 | 区域名称 | 省份名称/城市名称/区县名称 | 区域名称 |
| AREATYPE | NUMBER | 区域类型 | - | 4=省, 5=市, 6=区县 |
| SUPERID | NUMBER | 上级区域ID | - | 关联上级区域，省superid为空 |

### MKT_TERMINAL（门店表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|------------|------|
| COUNTY_AREAID | NUMBER | 区县区域ID | - | 关联SCPAREA(AREATYPE=6) |
| ENTID | NUMBER | 企业ID | - | 用于按组织ID筛选门店 |
| TERMINAL_STAT | NUMBER | 门店状态 | - | 1=运营中, 2=撤店 |

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|---------------|------|---------|
| 查询结果为空 | 查询按钮点击时 | 当前查询条件下无门店区域分布数据，请调整查询条件后重试 | 低 | [查看](#报错1查询结果为空) |
| 网络请求失败/接口调用异常 | 查询/导出 | 后端接口调用失败，检查网络连接或后端服务状态 | 阻断性报错 | [查看](#报错2网络请求失败接口调用异常) |
| 权限不足/未登录 | 页面加载/查询 | 当前用户无组织级权限或登录态失效，重新登录或联系管理员分配权限 | 阻断性报错 | [查看](#报错3权限不足未登录) |
| 导出失败：网络异常 | 导出 | 导出接口调用过程中网络中断或后端响应超时，重试导出或缩小查询范围 | 阻断性报错 | [查看](#报错4导出失败网络异常) |

#### 报错1：查询结果为空
- **触发条件**：点击"查询"按钮，按当前查询条件（组织ID、省份、城市、区县、门店数量下限等）查询SCPAREA关联MKT_TERMINAL返回空结果集
- **逻辑分析**：报表通过SCPAREA表的areatype=4(省)/5(市)/6(区县)三级关联展示区域，通过子查询 `SELECT county_areaid, COUNT(county_areaid) AS terminal_num FROM epms.mkt_terminal WHERE entid = #{orgId} GROUP BY county_areaid` 统计每个区县的门店数量，无门店的区域显示0。若查询条件过严（如选择的省份/城市/区县下无SCPAREA记录）、或组织ID（entid）下无任何门店、或门店数量下限设置过高筛掉所有区域，均会返回空结果。注意：无门店的区域会显示0但仍返回行，只有当SCPAREA本身无匹配区域时才完全为空。
- **排查SQL**：
  ```sql
  SELECT sa3.areaid          AS 区县ID,
         sa3.areaname        AS 区县名称,
         NVL(mt.terminal_num, 0) AS 门店数量
  FROM   scparea sa3
  LEFT   JOIN (
           SELECT county_areaid, COUNT(county_areaid) AS terminal_num
           FROM   mkt_terminal
           WHERE  entid = #{当前用户组织ID}
           GROUP  BY county_areaid
         ) mt ON sa3.areaid = mt.county_areaid
  WHERE  sa3.areatype = 6
  ORDER  BY sa3.areaid;
  ```

#### 报错2：网络请求失败/接口调用异常
- **触发条件**：点击"查询"或"导出"按钮，调用POST /v1/{organizationId}/terminalReport/mkt-store-areal-distribution/search接口时，前端未收到响应或收到非2xx状态码（如500、502、504）
- **逻辑分析**：本页面为hlod低代码报表页面，查询依赖后端TerminalReportController.mktTerminalArealDistributionSearch接口分页查询SCPAREA省市区三级关联并统计MKT_TERMINAL门店数量。若后端ae-report服务未启动、Oracle数据库连接异常、SCPAREA三级关联LEFT JOIN导致慢SQL、网络中断、或网关转发失败，均会导致接口调用异常。需检查后端服务健康状态、数据库连接、网络连通性。
- **排查SQL**：
  ```sql
  SELECT COUNT(*)            AS 区县总数
  FROM   scparea
  WHERE  areatype = 6;
  ```

#### 报错3：权限不足/未登录
- **触发条件**：页面加载或点击"查询"/"导出"按钮时，接口返回401未授权或403禁止访问，或前端路由守卫拦截
- **逻辑分析**：本报表接口声明@Permission(level = ResourceLevel.ORGANIZATION)，要求用户具备组织级权限。若用户未登录（token过期/丢失）、或当前角色未分配该报表菜单权限、或organizationId路径参数与用户所属组织不匹配，均会触发权限校验失败。hlod低代码页面通过路由配置和接口权限双重校验，任一环节失败均阻断访问。需重新登录或联系管理员分配报表查看权限。
- **排查SQL**：
  ```sql
  SELECT '权限校验为应用层逻辑，无对应数据表' AS 提示
  FROM   dual;
  ```

#### 报错4：导出失败：网络异常
- **触发条件**：点击"导出"按钮，导出Excel过程中网络中断、后端响应超时或Excel文件流传输中断
- **逻辑分析**：导出接口将当前查询条件下的门店区域分布数据全量查询后生成Excel文件流返回。若查询数据量较大（如未限定省市区导致全国区县数据）导致响应超时、或生成Excel过程中内存溢出、或网络不稳定导致文件流中断、或浏览器下载被拦截，均会触发导出失败。需重试导出或缩小查询条件（如限定省份、城市）减少数据量。
- **排查SQL**：
  ```sql
  SELECT sa1.areaname        AS 省份名称,
         COUNT(sa3.areaid)   AS 区县数量
  FROM   scparea sa1
  LEFT   JOIN scparea sa2 ON sa1.areaid = sa2.superid AND sa2.areatype = 5
  LEFT   JOIN scparea sa3 ON sa2.areaid = sa3.superid AND sa3.areatype = 6
  WHERE  sa1.areatype = 4
  GROUP  BY sa1.areaname
  ORDER  BY 区县数量 DESC;
  ```

## 常见问题FAQ

**Q1：门店数量如何统计？**
A：通过子查询 `SELECT county_areaid, COUNT(county_areaid) AS terminal_num FROM epms.mkt_terminal WHERE entid = #{orgId} GROUP BY county_areaid` 统计每个区县的门店数量，无门店的区域显示0。

**Q2：省市区三级如何关联？**
A：通过SCPAREA表的areatype和superid关联：省(areatype=4) → 市(areatype=5, superid=省areaid) → 区县(areatype=6, superid=市areaid)。

**Q3：门店数量下限筛选的作用？**
A：筛选门店数量低于指定值的区域，用于查找门店覆盖不足的区域，辅助门店拓展决策。

**Q4：报表是否支持导出？**
A：是，支持导出Excel。

**Q5：报表是否支持新增/修改/删除？**
A：不支持，本页面为纯查询报表，仅支持查看和导出。

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-12-15 | - | HZERO | 初始创建门店区域分布报表查询接口 |
| 2025-12-10 | - | HZERO | 初始创建TerminalReportController |
