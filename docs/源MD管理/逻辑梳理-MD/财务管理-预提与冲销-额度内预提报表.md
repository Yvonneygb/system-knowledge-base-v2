# 财务管理-预提与冲销-额度内预提报表

## 业务流程

### 业务流程图

```text
额度内预提报表页面(arrow-ae标准React页面)
        │
        ├── 查询预提数据 ──→ 查询FIN_FEE_WITHHOLDING_IN_QUOTA表
        │                      └── Repository层: queryNewWriteoffInQuotaView
        │
        └── 更新预提数据 ──→ POST /withholding-in-quotas/updateAccrualData
                              └── 推送共享财务系统(额度内预提)
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 出库单 | 数据依赖 | 预提数据来源于出库单预提 | 出库单已生成 |
| 额度内兑现 | 数据依赖 | 预提与兑现关联计算 | 兑现数据已生成 |
| 共享财务系统 | 服务依赖 | 预提结果推送至共享财务系统 | 共享财务系统可用 |

### 下游影响

- 影响1：共享财务系统
  - 预提数据推送至共享财务系统，影响财务核算

- 影响2：额度内冲销
  - 预提数据作为冲销计算的基础

---

## 重点逻辑

### 重点逻辑1：预提数据生成与更新 {数据维护}

- **业务意义**：预提数据需要定期更新，确保报表数据准确

- **具体逻辑描述**

  - 第1点：通过updateAccrualData接口更新预提数据

  - 第2点：Repository层通过queryNewWriteoffInQuotaView方法查询预提视图数据

  - 第3点：汇总出库预提、兑现等数据生成报表

### 重点逻辑2：推送共享财务系统 {数据推送}

- **业务意义**：预提数据推送至共享财务系统，确保财务核算同步

- **具体逻辑描述**

  - 第1点：单据类型fin_fee_withholding_in_quota（额度内预提）

  - 第2点：通过FsscApiController推送至共享财务系统

  - 第3点：推送含预提含税金额、不含税金额、总额等关键信息

---

## 详细逻辑

### 界面模块1：额度内预提报表列表页（arrow-ae标准React页面）

> 前端源码位于 `arrow-ae/src/pages/storeManage/finFeeWithholdingInQuota`，后端基于ae-business服务。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 预提单号 | FIN_FEE_WITHHOLDING_IN_QUOTA.WITHHOLDING_NO | 文本框 | 预提单号 | 常显 | 系统生成 |
| 预提头单号 | FIN_FEE_WITHHOLDING_IN_QUOTA.WITHHOLDING_HEADER_NO | 文本框 | 预提头单据编码 | 常显 | 系统带出 |
| 共享单号 | FIN_FEE_WITHHOLDING_IN_QUOTA.SHARE_NO | 文本框 | 共享财务系统单号 | 常显 | 推送后带出 |
| 年月 | FIN_FEE_WITHHOLDING_IN_QUOTA.YEARMONTH | 文本框 | 预提年月 | 常显 | 系统带出 |
| 法人编码 | FIN_FEE_WITHHOLDING_IN_QUOTA.CORPORATE_CODE | 文本框 | 法人编码 | 常显 | 系统带出 |
| 法人名称 | FIN_FEE_WITHHOLDING_IN_QUOTA.CORPORATE_NAME | 文本框 | 法人客户名称 | 常显 | 系统带出 |
| 事业部 | FIN_FEE_WITHHOLDING_IN_QUOTA.DIVISION_NAME | 文本框 | 事业部名称 | 常显 | 系统带出 |
| 成本中心 | FIN_FEE_WITHHOLDING_IN_QUOTA.COST_CENTER_NAME | 文本框 | 成本中心名称 | 常显 | 系统带出 |
| 交易公司 | FIN_FEE_WITHHOLDING_IN_QUOTA.TRADING_COMPANY_NAME | 文本框 | 交易公司名称 | 常显 | 系统带出 |
| 预提含税金额 | FIN_FEE_WITHHOLDING_IN_QUOTA.WITHHOLDING_TAX_AMT | 数字显示框 | 预提含税金额 | 常显 | 系统计算 |
| 预提不含税金额 | FIN_FEE_WITHHOLDING_IN_QUOTA.WITHHOLDING_ETAX_AMT | 数字显示框 | 预提不含税金额 | 常显 | 系统计算 |
| 总金额 | FIN_FEE_WITHHOLDING_IN_QUOTA.SUMAMT | 数字显示框 | 预提总金额 | 常显 | 系统计算 |
| 总支付金额 | FIN_FEE_WITHHOLDING_IN_QUOTA.SUMPAYAMT | 数字显示框 | 总支付金额 | 常显 | 系统计算 |
| 同步时间 | FIN_FEE_WITHHOLDING_IN_QUOTA.SYNC_ITEM | 日期显示框 | 同步时间 | 常显 | 推送后更新 |
| 单据状态 | FIN_FEE_WITHHOLDING_IN_QUOTA.BILL_STATUS | 文本框 | 单据状态 | 常显 | 系统更新 |
| 生成次数 | FIN_FEE_WITHHOLDING_IN_QUOTA.GENERATED_TIMES | 文本框 | 生成次数 | 常显 | 系统更新 |
| 科目名称 | FIN_FEE_WITHHOLDING_IN_QUOTA.SUBJECT_NAME | 文本框 | 会计科目 | 常显 | 系统带出 |
| 兑现方式 | FIN_FEE_WITHHOLDING_IN_QUOTA.CASH_OUT_MODE | 文本框 | 兑现方式 | 常显 | 系统带出 |

### 选择弹窗

> 本页面为报表查询页面，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询预提数据 | 列表页 | 始终可用 | 查询FIN_FEE_WITHHOLDING_IN_QUOTA表 |
| 更新预提数据 | 更新预提数据并推送 | 列表页 | 始终可用 | 调用updateAccrualData接口 |

#### 按钮1：查询（列表页）

- **触发条件**：始终可用
- **执行逻辑**：按年月、事业部、交易公司、法人等条件查询预提数据
- **接口调用**：查询FIN_FEE_WITHHOLDING_IN_QUOTA表
- **排查SQL**：

```sql
SELECT * FROM FIN_FEE_WITHHOLDING_IN_QUOTA 
WHERE (YEARMONTH = #{yearmonth} OR #{yearmonth} IS NULL)
  AND (DIVISION_ID = #{divisionId} OR #{divisionId} IS NULL)
ORDER BY YEARMONTH DESC, WITHHOLDING_NO
```

#### 按钮2：更新预提数据（列表页）

- **触发条件**：始终可用
- **执行逻辑**：
  - 第1点：通过Repository层queryNewWriteoffInQuotaView方法查询预提视图数据
  - 第2点：更新FIN_FEE_WITHHOLDING_IN_QUOTA表数据
  - 第3点：推送至共享财务系统（单据类型fin_fee_withholding_in_quota）
  - 第4点：更新同步时间、单据状态和共享单号
- **接口调用**：POST `/v1/{organizationId}/withholding-in-quotas/updateAccrualData`
- **排查SQL**：

```sql
SELECT * FROM FIN_FEE_WITHHOLDING_IN_QUOTA WHERE YEARMONTH = #{yearmonth} AND SYNC_ITEM IS NULL;
```

### 保存校验

> 本页面为报表页面，无保存操作。

### 提交校验

> 本页面无提交操作。

### 状态机

#### 状态机流转图

```text
数据生成 ──更新预提数据──→ 已更新(已推送)
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| 0 | 待更新 | 查询、更新预提数据 |
| 1 | 已更新/已推送 | 查询 |

---

## 数据库表详解

### 表1：FIN_FEE_WITHHOLDING_IN_QUOTA（费用额度内预提表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| WITHHOLDING_ID | BIGINT | 预提主键 | - | 自增主键 |
| WITHHOLDING_NO | VARCHAR | 预提单号 | 预提单号 | 系统生成 |
| WITHHOLDING_HEADER_NO | VARCHAR | 预提头单据编码 | 预提头单号 | 系统带出 |
| SHARE_NO | VARCHAR | 共享单号 | 共享单号 | 推送后带出 |
| YEARMONTH | VARCHAR | 年月 | 年月 | 系统带出 |
| ORGANIZATION_ID | BIGINT | 组织ID | - | 系统带出 |
| CORPORATE_CODE | VARCHAR | 法人编码 | 法人编码 | 系统带出 |
| CORPORATE_NAME | VARCHAR | 法人客户名称 | 法人名称 | 系统带出 |
| DIVISION_ID | BIGINT | 事业部ID | - | 系统带出 |
| DIVISION_NAME | VARCHAR | 事业部名称 | 事业部 | 系统带出 |
| COST_CENTER_CODE | VARCHAR | 成本中心编码 | - | 系统带出 |
| COST_CENTER_NAME | VARCHAR | 成本中心名称 | 成本中心 | 系统带出 |
| TRADING_COMPANY_NAME | VARCHAR | 交易公司 | 交易公司 | 系统带出 |
| TRADING_COMPANY_CODE | VARCHAR | 交易公司编码 | - | 系统带出 |
| WITHHOLDING_TAX_AMT | DECIMAL | 预提含税金额 | 预提含税金额 | 系统计算 |
| WITHHOLDING_ETAX_AMT | DECIMAL | 预提不含税金额 | 预提不含税金额 | 系统计算 |
| SUMAMT | DECIMAL | 总金额 | 总金额 | 系统计算 |
| SUMPAYAMT | DECIMAL | 总支付金额 | 总支付金额 | 系统计算 |
| SYNC_ITEM | DATE | 同步时间 | 同步时间 | 推送后更新 |
| BILL_STATUS | INTEGER | 单据状态 | 单据状态 | 系统更新 |
| GENERATED_TIMES | INTEGER | 生成次数 | 生成次数 | 系统更新 |
| SUBJECT_NAME | VARCHAR | 科目名称 | 科目名称 | 系统带出 |
| CASH_OUT_MODE | VARCHAR | 兑现方式 | 兑现方式 | 系统带出 |
| DATA_LOG | VARCHAR | 数据日志 | - | 系统记录 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 预提数据为空 | 查询时 | 预提数据未生成，先执行更新预提数据 | toast提醒 | [查看] |
| 推送共享财务失败 | 更新预提数据时 | 共享财务系统不可用，检查系统状态 | toast提醒 | [查看] |
| 预提视图数据为空 | 更新预提数据时 | 上游出库单预提数据未生成，预提视图无数据 | toast提醒 | [查看] |
| 找不到符合的预提/冲销单号信息 | 共享回调时 | 共享财务系统回调时预提单号不存在，检查单号有效性 | toast提醒 | [查看] |
| 请确认该单据号是预提/冲销头单号 | 共享回调时 | 共享回调传入的单号类型不匹配，确认单号类型 | toast提醒 | [查看] |
| 未知的单据类型 | 共享回调时 | 共享回调传入的单据类型未配置，检查单据类型配置 | toast提醒 | [查看] |
| 网络请求失败 | 更新预提数据时 | 网络中断或服务不可用，检查网络和服务状态 | toast提醒 | [查看] |
| 权限不足 | 操作时 | 当前用户无操作权限，联系管理员授权 | toast提醒 | [查看] |

#### 报错1：预提数据为空
- **触发条件**：用户按年月/事业部/交易公司查询预提数据，FIN_FEE_WITHHOLDING_IN_QUOTA表返回空结果集
- **逻辑分析**：预提数据需通过"更新预提数据"按钮（updateAccrualData接口）主动生成，Repository层通过queryNewWriteoffInQuotaView方法查询预提视图数据并写入FIN_FEE_WITHHOLDING_IN_QUOTA表。无数据根因有三类：(1)从未执行过更新预提数据操作，表为空；(2)上游出库单预提数据未生成或额度内兑现数据未同步，预提视图无数据；(3)查询的年月/事业部区间内无预提记录。需先执行更新预提数据，再查询确认
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, YEARMONTH, DIVISION_NAME, TRADING_COMPANY_NAME,
         CORPORATE_NAME, WITHHOLDING_TAX_AMT, WITHHOLDING_ETAX_AMT, SUMAMT,
         SYNC_ITEM, BILL_STATUS, GENERATED_TIMES
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE (YEARMONTH = #{yearmonth} OR #{yearmonth} IS NULL)
    AND (DIVISION_ID = #{divisionId} OR #{divisionId} IS NULL)
  ORDER BY YEARMONTH DESC, WITHHOLDING_NO;
  ```

#### 报错2：推送共享财务失败
- **触发条件**：用户点击"更新预提数据"，updateAccrualData接口推送至共享财务系统时返回失败
- **逻辑分析**：更新预提数据接口在更新FIN_FEE_WITHHOLDING_IN_QUOTA表后，通过FsscApiController推送至共享财务系统，单据类型fin_fee_withholding_in_quota（额度内预提）。失败根因有三类：(1)共享财务系统不可用或网络中断；(2)推送数据异常，如预提含税/不含税金额为0、法人编码（CORPORATE_CODE）在共享系统中不存在、成本中心编码（COST_CENTER_CODE）不匹配、科目名称（SUBJECT_NAME）未配置；(3)共享侧重复推送校验。推送失败需检查SHARE_NO和SYNC_ITEM字段，重新执行更新预提数据
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, SHARE_NO, CORPORATE_CODE, COST_CENTER_CODE,
         SUBJECT_NAME, WITHHOLDING_TAX_AMT, WITHHOLDING_ETAX_AMT, SYNC_ITEM, BILL_STATUS
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE YEARMONTH = #{yearmonth} AND (SHARE_NO IS NULL OR SYNC_ITEM IS NULL OR BILL_STATUS != 1);
  ```

#### 报错3：预提视图数据为空
- **触发条件**：用户点击"更新预提数据"，updateAccrualData接口查询queryWithholdingInQuotaNewView返回空集合
- **逻辑分析**：updateAccrualData方法在FinFeeWithholdingInQuotaServiceImpl.java:58处校验withholdingInQuotaNewViewList为空时直接return 0，不抛异常但前端无数据更新。queryWithholdingInQuotaNewView从预提视图查询数据，视图数据来源于出库单预提和额度内兑现。无数据根因有三类：(1)上游出库单预提数据未生成；(2)额度内兑现数据未同步；(3)查询的起始时间（startDate，默认上个月）内无预提记录。需核查上游出库单预提和兑现数据
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, YEARMONTH, DIVISION_NAME, TRADING_COMPANY_NAME,
         CORPORATE_NAME, WITHHOLDING_TAX_AMT, BILL_STATUS, GENERATED_TIMES
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE YEARMONTH = #{yearmonth};
  ```

#### 报错4：找不到符合的预提/冲销单号信息
- **触发条件**：共享财务系统回调更新预提/冲销单据状态时，传入的预提/冲销单号在FIN_FEE_WITHHOLDING_IN_QUOTA表中不存在
- **逻辑分析**：FsscApiServiceImpl.java:324/339/354/369/384/399处校验finFeeWithholdingInQuotaRepository.selectByCondition查询结果为空时抛出CommonException("找不到符合的预提/冲销单号【%s】的信息")。该异常在共享财务系统回调时触发，表示共享侧传入的actualHeaderCode（预提头单号）在本地表中不存在。根因有三类：(1)预提单号已被删除；(2)共享侧传入单号错误；(3)数据不一致。需核查预提头单号是否存在
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, WITHHOLDING_HEADER_NO, SHARE_NO,
         YEARMONTH, BILL_STATUS, SYNC_ITEM
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE WITHHOLDING_HEADER_NO = #{actualHeaderCode};
  ```

#### 报错5：请确认该单据号是预提/冲销头单号
- **触发条件**：共享财务系统回调时，传入的headerCode无法匹配到预提头单号或冲销头单号
- **逻辑分析**：FsscApiServiceImpl.java:311处校验headerCode匹配失败时抛出CommonException("请确认该单据号【%s】是预提/冲销头单号！")。该异常表示共享侧回调传入的单号既不是预提头单号也不是冲销头单号，单据类型不匹配。需确认共享侧传入的单号类型和单号有效性
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, WITHHOLDING_HEADER_NO, BILL_STATUS
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE WITHHOLDING_HEADER_NO = #{headerCode}
     OR WITHHOLDING_NO = #{headerCode};
  ```

#### 报错6：未知的单据类型
- **触发条件**：共享财务系统回调时，传入的单据类型不在系统支持的类型范围内
- **逻辑分析**：FsscApiServiceImpl.java:408处校验单据类型未匹配到任何已知类型时抛出CommonException("未知的单据类型")。该异常表示共享侧回调传入的单据类型未在系统中配置，系统无法识别该单据类型对应的处理逻辑。需检查共享侧单据类型配置和本地单据类型枚举
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, WITHHOLDING_HEADER_NO, BILL_STATUS
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE WITHHOLDING_HEADER_NO = #{headerCode};
  ```

#### 报错7：网络请求失败
- **触发条件**：前端调用updateAccrualData接口或共享财务系统回调时网络中断
- **逻辑分析**：前端handleExecute方法在ListPage/index.tsx:28处通过axios调用execute接口，catch块中注释"error handled by interceptor"由全局拦截器处理。网络请求失败根因有三类：(1)AE业务服务不可用；(2)网络连接中断；(3)请求超时。需检查网络连通性和AE业务服务状态
- **排查SQL**：
  ```sql
  SELECT WITHHOLDING_ID, WITHHOLDING_NO, YEARMONTH, BILL_STATUS, SYNC_ITEM
  FROM FIN_FEE_WITHHOLDING_IN_QUOTA
  WHERE YEARMONTH = #{yearmonth};
  ```

#### 报错8：权限不足
- **触发条件**：当前用户无"更新预提数据"或"执行"或"作废"按钮的操作权限
- **逻辑分析**：前端按钮通过PermissionButton组件配置permissionList进行权限校验，无权限时按钮不显示或点击报错。需联系管理员为当前用户分配相应权限编码（PERMISSION_PREFIX.button.execute等）
- **排查SQL**：
  ```sql
  -- 权限校验（伪SQL，具体表名依权限框架而定）
  SELECT ROLE_CODE, PERMISSION_CODE
  FROM USER_ROLE_PERMISSION
  WHERE USER_ID = #{userId} AND PERMISSION_CODE LIKE '%withholding-in-quotas%';
  ```

### 常见问题

- 问题1：预提数据不正确
  - 原因：出库单预提数据未及时同步
  - 解决思路：执行更新预提数据操作，检查SQL `SELECT * FROM FIN_FEE_WITHHOLDING_IN_QUOTA WHERE YEARMONTH = #{yearmonth}`

- 问题2：共享单号为空
  - 原因：推送共享财务系统失败
  - 解决思路：检查SHARE_NO字段是否为空，重新执行更新预提数据

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充界面模块、数据库表详解 |
| 2025-09-03 | - | jiaqiang.fu01 | 初始创建FinFeeWithholdingInQuota实体 |
