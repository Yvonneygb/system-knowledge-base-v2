# 门店管理-门店报表-户外广告分布

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

户外广告分布报表用于查询已审批通过的户外广告投放申请的分布情况，包括费用申请单号、经销商信息、广告媒介类型、广告尺寸、发布日期、申请金额等。数据来源于广告费申请头表（FIN_FEE_APPLY_HEADER），筛选条件为Apply_Type=2（广告投放）且HZ_APPROVE_STATUS='APPROVED'（已审批通过）。本页面为纯查询报表，仅支持查看和导出，不支持新增、修改、删除等写操作。

**核心流程：**
1. 用户进入户外广告分布报表页面
2. 填写查询条件（事业部、销售区域、运营中心、经销商、省份、费用申请单号、合同有效期等）
3. 点击查询，调用后端接口分页返回户外广告分布数据
4. 用户查看报表数据，可导出Excel归档

```
开始 → 设定查询条件 → 点击查询 → 后端查询FIN_FEE_APPLY_HEADER(Apply_Type=2且APPROVED) → 关联DIVISION_BASE_SET/CUSTOMER → 返回分布数据 → 查看报表/导出Excel → 结束
```

- 上游支撑：广告费申请头表（FIN_FEE_APPLY_HEADER）、事业部基础设置（DIVISION_BASE_SET）、客户组织（CUSTOMER_ORG）、值集（HPFM_LOV_VALUE）、hlod报表引擎
- 下游影响：户外广告投放分布分析、广告费申请统计、Excel导出归档、内部管理决策参考

## 重点逻辑

### 重点逻辑1：纯报表查询页面【只读查询】

- **业务意义**：供内部人员查询户外广告投放的分布情况，了解广告投放的整体分布
- **具体逻辑描述**：
  - 本页面为hlod低代码报表页面，无独立前端源码
  - 仅提供查询和导出功能，不支持新增、修改、删除操作
  - 数据来源于广告费申请头表，筛选Apply_Type=2且HZ_APPROVE_STATUS='APPROVED'

### 重点逻辑2：数据筛选条件

- **业务意义**：只展示已审批通过的户外广告投放申请
- **具体逻辑描述**：
  - Apply_Type = 2：广告投放申请类型
  - HZ_APPROVE_STATUS = 'APPROVED'：只展示已审批通过的单据
  - 排除未审批、审批中、已驳回的单据

### 重点逻辑3：广告媒介项目LOV翻译

- **业务意义**：将广告媒介项目编码翻译为可读含义
- **具体逻辑描述**：
  - 通过子查询 `HZERO.HPFM_LOV_VALUE` 翻译，LOV_CODE = 'AE.ASVERT_MEDIUM_ITEM'
  - SQL：`(SELECT meaning FROM HZERO.HPFM_LOV_VALUE WHERE lov_code = 'AE.ASVERT_MEDIUM_ITEM' and value = f.advert_medium_item)`
  - VO中使用@LovValue注解：`@LovValue(value = "AE.ASVERT_MEDIUM_ITEM", delimiter = ",")`

### 重点逻辑4：详细地址拼接

- **业务意义**：将省、市、区、详细地址拼接为完整地址
- **具体逻辑描述**：
  - SQL中使用concat嵌套拼接：`concat(concat(concat(province_areaname, city_areaname), county_areaname), addr)`
  - 结果为"省名市名区名详细地址"的完整地址字符串

### 重点逻辑5：查询条件匹配规则

- **业务意义**：支持多维度灵活筛选户外广告分布数据
- **具体逻辑描述**：
  - 模糊查询（LIKE '%xxx%'）：经销商名称
  - 精确查询：组织ID、事业部ID、经销商编码、省份名称、费用申请单号
  - 子查询匹配：销售区域（通过scporg表orgid查orgname）、运营中心（同上）
  - 日期精确匹配：合同有效期起/止使用to_date精确比较

## 详细逻辑

### 界面模块

#### 查询条件区域

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 组织ID | ORGANIZATION_ID | 隐藏 | 当前用户组织ID | 常显 | 自动获取，精确匹配 |
| 事业部ID | DIVISION_ID | 下拉选择框 | 按事业部筛选 | 常显 | 用户选择，精确匹配 |
| 销售区域ID | SALEZONE_ORG_ID | 下拉选择框 | 按销售区域筛选 | 常显 | 用户选择，子查询scporg表获取orgname匹配 |
| 运营中心ID | OPERAT_CENTER_ORG_ID | 下拉选择框 | 按运营中心筛选 | 常显 | 用户选择，子查询scporg表获取orgname匹配 |
| 经销商编码 | CUST_CODE | 文本框 | 按经销商编码筛选 | 常显 | 用户输入，精确匹配 |
| 经销商名称 | CUST_NAME | 文本框 | 按经销商名称筛选 | 常显 | 用户输入，模糊匹配（LIKE '%xxx%'） |
| 省份名称 | PROVINCE_AREANAME | 下拉选择框 | 按省份筛选 | 常显 | 用户选择，精确匹配 |
| 费用申请单号 | FEE_APPLY_NO | 文本框 | 按费用申请单号筛选 | 常显 | 用户输入，精确匹配 |
| 合同有效期起 | PUBLISH_FROM_DATE | 日期选择器 | 发布开始日期 | 常显 | 用户选择，精确匹配 |
| 合同有效期止 | PUBLISH_TO_DATE | 日期选择器 | 发布结束日期 | 常显 | 用户选择，精确匹配 |

#### 报表数据区域

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 费用申请单号 | FEE_APPLY_NO | 文本 | 费用申请单号 | 常显 | 表字段直接输出 |
| 事业部ID | DIVISION_ID | 隐藏 | 事业部ID | 常显 | 关联DIVISION_BASE_SET获取 |
| 销售区域名称 | SALEZONE_ORG_NAME | 文本 | 销售区域组织名称 | 常显 | 表字段直接输出 |
| 运营中心名称 | OPERAT_CENTER_ORG_NAME | 文本 | 运营中心组织名称 | 常显 | 表字段直接输出 |
| 经销商ID | CUST_ID | 隐藏 | 经销商ID | 常显 | 表字段直接输出 |
| 经销商编码 | CUST_CODE | 文本 | 经销商编码 | 常显 | 表字段直接输出 |
| 经销商名称 | CUST_NAME | 文本 | 经销商名称 | 常显 | 表字段直接输出 |
| 省份区域名称 | PROVINCE_AREANAME | 文本 | 省份名称 | 常显 | 表字段直接输出 |
| 城市区域名称 | CITY_AREANAME | 文本 | 城市名称 | 常显 | 表字段直接输出 |
| 县区区域名称 | COUNTY_AREANAME | 文本 | 区县名称 | 常显 | 表字段直接输出 |
| 详细地址 | ADDR | 文本 | 完整地址 | 常显 | concat(省+市+区+详细地址)拼接 |
| 广告媒介类型 | ADVERT_MEDIUM_TYPE | 文本 | 广告媒介类型 | 常显 | 表字段直接输出 |
| 广告尺寸 | ADVERT_SIZE | 文本 | 广告尺寸 | 常显 | 表字段直接输出 |
| 广告媒介项目 | ADVERT_MEDIUM_ITEM | 文本 | 广告媒介项目 | 常显 | 子查询HPFM_LOV_VALUE翻译(AE.ASVERT_MEDIUM_ITEM) |
| 广告公司 | ADVERT_COMPANY | 文本 | 广告公司 | 常显 | 表字段直接输出 |
| 创建人 | CREATOR | 文本 | 创建人 | 常显 | 子查询customer表获取customer_code |
| 发布开始日期 | PUBLISH_FROM_DATE | 日期 | 发布开始日期 | 常显 | TO_CHAR格式化YYYY-MM-DD |
| 发布结束日期 | PUBLISH_TO_DATE | 日期 | 发布结束日期 | 常显 | TO_CHAR格式化YYYY-MM-DD |
| 申请金额 | TOTAL_APPLY_AMT_BX | 数值 | 申请金额 | 常显 | 表字段直接输出 |
| 组织ID | ORGANIZATION_ID | 隐藏 | 组织ID | 常显 | 关联DIVISION_BASE_SET获取 |

#### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 查询户外广告分布数据 | 列表页查询区域 | 常显 | 调用POST /outdoor-advertising-distribution/search接口，分页返回分布数据 |
| 导出 | 导出报表数据为Excel | 列表页查询区域 | 常显 | 导出当前查询结果为Excel文件 |

#### 按钮1：查询（列表页查询区域）

- **业务意义**：根据查询条件搜索户外广告分布数据
- **具体逻辑描述**：
  - 点击查询按钮，触发POST `/v1/{organizationId}/terminalReport/outdoor-advertising-distribution/search` 接口
  - 请求参数为OutdoorAdvertisingDistributionSearchDTO
  - 后端通过PageHelper.doPageAndSort实现分页查询
  - 查询FIN_FEE_APPLY_HEADER表，筛选Apply_Type=2且HZ_APPROVE_STATUS='APPROVED'
  - 返回OutdoorAdvertisingDistributionSearchVO分页结果

#### 按钮2：导出（列表页查询区域）

- **业务意义**：将查询结果导出为Excel文件
- **具体逻辑描述**：
  - 点击导出按钮，将当前查询条件下的报表数据导出为Excel

### 后端接口

| 接口名称 | 请求方式 | 接口路径 | 权限 | 说明 |
|----------|----------|----------|------|------|
| 户外广告投放分布报表查询 | POST | `/v1/{organizationId}/terminalReport/outdoor-advertising-distribution/search` | 组织级权限 | 分页查询户外广告分布数据 |

**接口入参（OutdoorAdvertisingDistributionSearchDTO）：**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| organizationId | Long | 组织ID（精确匹配） |
| divisionId | Long | 事业部ID（精确匹配） |
| salezoneOrgId | Long | 销售区域组织ID（子查询匹配） |
| operatCenterOrgId | Long | 运营中心组织ID（子查询匹配） |
| custCode | String | 经销商编码（精确匹配） |
| custName | String | 经销商名称（模糊匹配） |
| provinceName | String | 省份名称（精确匹配） |
| feeApplyNo | String | 费用申请单号（精确匹配） |
| startDate | String | 合同有效期起（精确匹配） |
| endDate | String | 合同有效期止（精确匹配） |

### 后端接口Mapper SQL

```sql
-- 户外广告投放分布报表查询
SELECT * FROM (
    SELECT
        f.fee_apply_no AS fee_Apply_No,
        del.organization_id AS organization_Id,
        f.salezone_org_name AS salezone_Org_Name,
        f.operat_center_org_name AS operat_Center_Org_Name,
        f.province_areaname AS province_Area_name,
        f.city_areaname AS city_Area_name,
        f.county_areaname AS county_Area_name,
        f.division_id AS division_Id,
        f.cust_code AS cust_Code,
        f.cust_id AS cust_Id,
        f.cust_name AS cust_Name,
        f.total_apply_amt_bx AS total_apply_amt_bx,
        f.advert_medium_type AS advert_Medium_Type,
        f.advert_size AS advert_Size,
        (SELECT meaning FROM HZERO.HPFM_LOV_VALUE WHERE lov_code = 'AE.ASVERT_MEDIUM_ITEM' AND value = f.advert_medium_item) AS advert_Medium_Item,
        TO_CHAR(f.publish_from_date, 'YYYY-MM-DD') AS publish_From_Date,
        TO_CHAR(f.publish_to_date, 'YYYY-MM-DD') AS publish_To_Date,
        f.advert_company AS advert_Company,
        (SELECT customer_code FROM epms.customer c WHERE c.customer_id = f.cust_id) AS creator,
        concat(concat(concat(f.province_areaname, f.city_areaname), f.county_areaname), f.addr) AS addr
    FROM epms.fin_fee_apply_header f
    LEFT JOIN epms.DIVISION_BASE_SET del ON del.organization_id = f.entid
    LEFT JOIN epms.customer_org cuo ON f.cust_id = cuo.customer_id
    WHERE f.Apply_Type = 2
      AND f.HZ_APPROVE_STATUS = 'APPROVED'
) t
WHERE 1 = 1
    AND t.organization_Id = #{organizationId}                                        -- 组织ID（精确）
    AND t.division_Id = #{divisionId}                                                -- 事业部ID（精确）
    AND t.sale_zone_Org_Name = (SELECT s.orgname FROM epms.scporg s WHERE s.orgid = #{salezoneOrgId})  -- 销售区域（子查询）
    AND t.operat_Center_Org_Name = (SELECT orgname FROM epms.scporg s WHERE s.orgid = #{operatCenterOrgId})  -- 运营中心（子查询）
    AND t.cust_Code = #{custCode}                                                    -- 经销商编码（精确）
    AND t.cust_Name LIKE '%' || #{custName} || '%'                                   -- 经销商名称（模糊）
    AND t.province_Area_name = #{provinceName}                                       -- 省份名称（精确）
    AND t.fee_Apply_No = #{feeApplyNo}                                               -- 费用申请单号（精确）
    AND to_date(t.publish_from_date, 'YYYY-MM-DD') = to_date(#{startDate}, 'YYYY-MM-DD')  -- 合同有效期起（精确）
    AND to_date(t.publish_To_Date, 'YYYY-MM-DD') = to_date(#{endDate}, 'YYYY-MM-DD')      -- 合同有效期止（精确）
```

### 状态机

> 本页面为纯查询报表页面，无状态流转。

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 广告费申请头表 | 数据来源 | 提供广告投放申请数据（Apply_Type=2且APPROVED） | 广告投放申请已审批通过 |
| 事业部基础设置 | 数据关联 | 关联DIVISION_BASE_SET获取组织ID | 事业部已配置 |
| 客户组织 | 数据关联 | 关联CUSTOMER_ORG | 客户主数据已维护 |
| 值集（HPFM_LOV_VALUE） | 数据翻译 | 翻译广告媒介项目编码为含义（LOV_CODE='AE.ASVERT_MEDIUM_ITEM'） | 值集已维护 |

### 下游影响

- 户外广告投放分布分析：管理层通过报表了解户外广告投放的区域和经销商分布
- 广告费申请统计：统计各维度的广告费申请金额
- Excel导出归档：导出报表数据供内部管理决策与归档使用

## 数据库表详解

### FIN_FEE_APPLY_HEADER（广告费申请头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|------------|------|
| FEE_APPLY_NO | VARCHAR2 | 费用申请单号 | 费用申请单号 | 编码规则生成 |
| APPLY_TYPE | NUMBER | 申请类型 | - | 2=广告投放 |
| HZ_APPROVE_STATUS | VARCHAR2 | 审批状态 | - | APPROVED=已审批通过 |
| ENTID | NUMBER | 企业ID | - | 关联DIVISION_BASE_SET |
| SALEZONE_ORG_NAME | VARCHAR2 | 销售区域名称 | 销售区域名称 | 保存时带入 |
| OPERAT_CENTER_ORG_NAME | VARCHAR2 | 运营中心名称 | 运营中心名称 | 保存时带入 |
| PROVINCE_AREANAME | VARCHAR2 | 省份名称 | 省份区域名称 | 保存时带入 |
| CITY_AREANAME | VARCHAR2 | 城市名称 | 城市区域名称 | 保存时带入 |
| COUNTY_AREANAME | VARCHAR2 | 区县名称 | 县区区域名称 | 保存时带入 |
| ADDR | VARCHAR2 | 详细地址 | - | 保存时带入 |
| DIVISION_ID | NUMBER | 事业部ID | 事业部ID | 保存时带入 |
| CUST_ID | NUMBER | 经销商ID | 经销商ID | 关联客户主数据 |
| CUST_CODE | VARCHAR2 | 经销商编码 | 经销商编码 | 保存时带入 |
| CUST_NAME | VARCHAR2 | 经销商名称 | 经销商名称 | 保存时带入 |
| TOTAL_APPLY_AMT_BX | DECIMAL | 申请金额 | 申请金额 | 用户输入 |
| ADVERT_MEDIUM_TYPE | VARCHAR2 | 广告媒介类型 | 广告媒介类型 | 用户输入 |
| ADVERT_SIZE | VARCHAR2 | 广告尺寸 | 广告尺寸 | 用户输入 |
| ADVERT_MEDIUM_ITEM | VARCHAR2 | 广告媒介项目 | 广告媒介项目 | 用户选择，LOV翻译 |
| ADVERT_COMPANY | VARCHAR2 | 广告公司 | 广告公司 | 用户输入 |
| PUBLISH_FROM_DATE | DATE | 发布开始日期 | 发布开始日期 | 用户选择 |
| PUBLISH_TO_DATE | DATE | 发布结束日期 | 发布结束日期 | 用户选择 |

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|---------------|------|---------|
| 查询结果为空 | 查询按钮点击时 | 当前查询条件下无户外广告分布数据，请调整查询条件后重试 | 低 | [查看](#报错1查询结果为空) |
| 网络请求失败/接口调用异常 | 查询/导出 | 后端接口调用失败，检查网络连接或后端服务状态 | 阻断性报错 | [查看](#报错2网络请求失败接口调用异常) |
| 权限不足/未登录 | 页面加载/查询 | 当前用户无组织级权限或登录态失效，重新登录或联系管理员分配权限 | 阻断性报错 | [查看](#报错3权限不足未登录) |
| 导出失败：网络异常 | 导出 | 导出接口调用过程中网络中断或后端响应超时，重试导出或缩小查询范围 | 阻断性报错 | [查看](#报错4导出失败网络异常) |

#### 报错1：查询结果为空
- **触发条件**：点击"查询"按钮，按当前查询条件（事业部、销售区域、运营中心、经销商、省份、费用申请单号、合同有效期等）查询FIN_FEE_APPLY_HEADER返回空结果集
- **逻辑分析**：报表查询FIN_FEE_APPLY_HEADER，筛选Apply_Type=2（广告投放申请）且HZ_APPROVE_STATUS='APPROVED'（已审批通过），关联DIVISION_BASE_SET、CUSTOMER_ORG获取补充信息。若查询条件过严（如经销商编码拼写错误、事业部ID不匹配）、或户外广告投放申请未审批通过（HZ_APPROVE_STATUS非APPROVED）、或申请类型非广告投放（Apply_Type非2）、或用户组织ID与数据不匹配，均会返回空结果。该报错为提示性，不影响系统。
- **排查SQL**：
  ```sql
  SELECT fee_apply_id        AS 申请单ID,
         fee_apply_no        AS 申请单号,
         cust_name           AS 经销商名称,
         total_apply_amt_bx  AS 申请金额,
         apply_type          AS 申请类型,
         hz_approve_status   AS 审批状态,
         organization_id     AS 组织ID
  FROM   fin_fee_apply_header
  WHERE  apply_type = 2
  AND    hz_approve_status = 'APPROVED'
  AND    organization_id = #{当前用户组织ID}
  ORDER  BY create_time DESC;
  ```

#### 报错2：网络请求失败/接口调用异常
- **触发条件**：点击"查询"或"导出"按钮，调用POST /v1/{organizationId}/terminalReport/outdoor-advertising-distribution/search接口时，前端未收到响应或收到非2xx状态码（如500、502、504）
- **逻辑分析**：本页面为hlod低代码报表页面，查询依赖后端TerminalReportController.outdoorAdvertisingDistributionSearch接口分页查询FIN_FEE_APPLY_HEADER（Apply_Type=2且HZ_APPROVE_STATUS='APPROVED'），关联DIVISION_BASE_SET、CUSTOMER_ORG，子查询HPFM_LOV_VALUE翻译广告媒介项目。若后端ae-report服务未启动、Oracle数据库连接异常、子查询scporg返回多行触发ORA-01427、to_date转换失败、网络中断、或网关转发失败，均会导致接口调用异常。需检查后端服务健康状态、数据库连接、网络连通性。
- **排查SQL**：
  ```sql
  SELECT COUNT(*)            AS 广告投放申请总数,
         MIN(create_time)    AS 最早创建时间,
         MAX(create_time)    AS 最晚创建时间
  FROM   fin_fee_apply_header
  WHERE  apply_type = 2
  AND    hz_approve_status = 'APPROVED';
  ```

#### 报错3：权限不足/未登录
- **触发条件**：页面加载或点击"查询"/"导出"按钮时，接口返回401未授权或403禁止访问，或前端路由守卫拦截
- **逻辑分析**：本报表接口声明@Permission(level = ResourceLevel.ORGANIZATION)，要求用户具备组织级权限。若用户未登录（token过期/丢失）、或当前角色未分配该报表菜单权限、或organizationId路径参数与用户所属组织不匹配，均会触发权限校验失败。hlod低代码页面通过路由配置和接口权限双重校验，任一环节失败均阻断访问。需重新登录或联系管理员分配报表查看权限。
- **排查SQL**：
  ```sql
  SELECT '权限校验为应用层逻辑，无对应数据表' AS 提示
  FROM   dual;
  ```

#### 报错4：导出失败：网络异常
- **触发条件**：点击"导出"按钮，导出Excel过程中网络中断、后端响应超时或Excel文件流传输中断
- **逻辑分析**：导出接口将当前查询条件下的户外广告分布数据全量查询后生成Excel文件流返回。若查询数据量较大导致响应超时、或生成Excel过程中内存溢出、或网络不稳定导致文件流中断、或浏览器下载被拦截，均会触发导出失败。需重试导出或缩小查询条件（如限定事业部、经销商）减少数据量。
- **排查SQL**：
  ```sql
  SELECT TO_CHAR(create_time, 'YYYY') AS 年度,
         COUNT(*)                     AS 广告申请数量
  FROM   fin_fee_apply_header
  WHERE  apply_type = 2
  AND    hz_approve_status = 'APPROVED'
  GROUP  BY TO_CHAR(create_time, 'YYYY')
  ORDER  BY 年度 DESC;
  ```

## 常见问题FAQ

**Q1：报表只展示哪些数据？**
A：只展示Apply_Type=2（广告投放申请）且HZ_APPROVE_STATUS='APPROVED'（已审批通过）的单据。

**Q2：广告媒介项目如何展示？**
A：通过子查询HPFM_LOV_VALUE（LOV_CODE='AE.ASVERT_MEDIUM_ITEM'）将编码翻译为可读含义。

**Q3：详细地址如何拼接？**
A：通过concat嵌套拼接：concat(concat(concat(省名, 市名), 区名), 详细地址)。

**Q4：报表是否支持导出？**
A：是，支持导出Excel。

**Q5：报表是否支持新增/修改/删除？**
A：不支持，本页面为纯查询报表，仅支持查看和导出。

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-12-16 | - | HZERO | 初始创建户外广告投放分布报表查询接口 |
| 2025-12-10 | - | HZERO | 初始创建TerminalReportController |
