# 工程管理-项目往来-项目到款引入

> 本页面为低代码页面(hlod)，无独立前端源码，基于后端API梳理。

## 业务流程

```text
ERP系统到款单 → 调度任务(syncPaymentDataFromErpJob)/手动引入(getErpPaymentData)
  → 调用ERP接口获取到款数据 → 判断是否已存在(按EXT_PAYMENT_IMPORT_ID)
    → 已存在则更新金额/状态 → 不存在则校验客户/交易公司/事业部并新增
  → 商票类型(商业承兑/银行承兑)默认未兑付(isCashout=N)，其他默认已兑付(isCashout=Y)
  → 写入EPM_PAYMENT_IMPORT表 → 列表查询
  → 商票兑付(draftPayment) → 更新为已兑付 → 插入兑付记录
  → 认领时实时获取ERP最新剩余可认款金额(getPaymentImportCanAllotAmt)
```

### 全链路流程图

```text
开始 → ERP到款数据 → ★写入EPM_PAYMENT_IMPORT★ → 列表查询
  → 商票类型？ → 是 → 兑付(draftPayment) → 更新已兑付 → 结束
              → 否 → 直接可用 → 结束
  → 认领时 → 实时查ERP剩余可认款金额 → 结束
```

### 关键规则

- **ERP同步**：到款数据来源于ERP系统，通过调度任务自动同步或手动触发引入，编号(paymentImportCode)来自ERP
- **存在性判断**：按EXT_PAYMENT_IMPORT_ID判断是否已存在，已存在更新金额/状态，不存在新增
- **状态校验**：信用卡拖欠、暂停付款、冲销付款、资金不足状态的到款单不允许认领
- **商票兑付**：商业承兑/银行承兑类型默认未兑付(isCashout=N)，需手动兑付后才可认领；其他类型默认已兑付
- **虚拟单/实际单**：billType=RECEIPT为实际到款单，billType=VIRTUAL_RECEIPT为虚拟到款单，可认款金额查询方式不同
- **剩余可认款金额**：虚拟单查DB，实际单实时查ERP获取最新金额

---

## 重点逻辑

### 重点逻辑1：ERP到款数据同步

- **业务意义**：将ERP回传的到款数据同步引入系统，作为后续认领、兑付的业务源头
- **具体逻辑描述**：
  - 调度任务syncPaymentDataFromErpJob支持指定编码或时间区间同步
  - 批量处理ERP返回数据(handleErpReceipt/handleErpReceiptBiz)
  - 核心流程paymentImportErpProcess：已存在则更新，不存在则校验客户/交易公司/事业部并新增
  - 商票类型默认isCashout=N(未兑付)，其他类型默认isCashout=Y(已兑付)

### 重点逻辑2：到款单状态校验

- **业务意义**：确保只有有效状态的到款单才能被认领
- **具体逻辑描述**：
  - verifyImportStat方法校验到款单状态
  - 信用卡拖欠、暂停付款、冲销付款、资金不足状态不允许认领

### 重点逻辑3：剩余可认款金额获取

- **业务意义**：认领时获取到款单最新可认款金额，防止超额认领
- **具体逻辑描述**：
  - 虚拟单(VIRTUAL_RECEIPT)查DB获取剩余可认款金额
  - 实际单(RECEIPT)实时查ERP获取最新剩余可认款金额
  - getPaymentImportCanAllotAmt接口提供认领时实时查询

### 重点逻辑4：商票兑付

- **业务意义**：商业承兑/银行承兑类型到款单需手动兑付后才可认领
- **具体逻辑描述**：
  - draftPayment方法处理兑付：校验类型(必须为商票)和状态(必须未兑付)
  - 更新isCashout=Y(已兑付)
  - 插入兑付记录(EPM_PAYMENT_IMPORT_RCD)

---

## 详细逻辑

### 界面模块1：查询区

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 到款记录编号 | EPM_PAYMENT_IMPORT.PAYMENT_IMPORT_CODE | 文本输入框 | 模糊查询到款编号 | 常显 | 手动输入 |
| 银行流水号 | EPM_PAYMENT_IMPORT.SERIAL_NUMBER | 文本输入框 | 模糊查询银行流水号 | 常显 | 手动输入 |
| 客户编码 | EPM_PAYMENT_IMPORT.CUSTOMER_CODE | 文本输入框 | 模糊查询客户 | 常显 | 手动输入 |
| 客户名称 | EPM_PAYMENT_IMPORT.CUSTOMER_NAME | 文本输入框 | 模糊查询客户名称 | 常显 | 手动输入 |
| 收款日期起 | EPM_PAYMENT_IMPORT.RECEIVE_DATE | 日期选择框 | 收款日期范围查询起 | 常显 | 手动选择 |
| 收款日期止 | EPM_PAYMENT_IMPORT.RECEIVE_DATE | 日期选择框 | 收款日期范围查询止 | 常显 | 手动选择 |
| 到款引入状态 | EPM_PAYMENT_IMPORT.IMPORT_STAT | 下拉选择框 | 筛选引入状态 | 常显 | 已生效/已失效 |
| 兑付状态 | EPM_PAYMENT_IMPORT.PAYMENT_STATUS | 下拉选择框 | 筛选兑付状态 | 常显 | 值集payment_status |
| 是否家装 | EPM_PAYMENT_IMPORT.IS_HOME | 下拉选择框 | 筛选家装/工程 | 常显 | 1-工程/2-家装 |

### 界面模块2：列表表格

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|--------|-----------|------|---------|---------|-------------|
| 到款记录编号 | EPM_PAYMENT_IMPORT.PAYMENT_IMPORT_CODE | 文本框 | 到款记录编号 | 常显 | 来源于ERP，不可编辑 |
| 银行流水号 | EPM_PAYMENT_IMPORT.SERIAL_NUMBER | 文本框 | 银行流水号 | 常显 | 来源于ERP，不可编辑 |
| 收款日期 | EPM_PAYMENT_IMPORT.RECEIVE_DATE | 日期 | 收款日期 | 常显 | 来源于ERP，不可编辑 |
| 收款金额 | EPM_PAYMENT_IMPORT.RECEIVE_AMT | 数值框 | 收款金额 | 常显 | 来源于ERP，不可编辑 |
| 剩余可认款金额 | EPM_PAYMENT_IMPORT.UNALLOT_AMT | 数值框 | 剩余可认款金额 | 常显 | 实际单实时查ERP，虚拟单查DB |
| 已认款金额 | EPM_PAYMENT_IMPORT.CONFIRMED_AMT | 数值框 | 已认款金额 | 常显 | 系统自动维护 |
| 客户编码 | EPM_PAYMENT_IMPORT.CUSTOMER_CODE | 文本框 | 客户编码 | 常显 | 来源于ERP，不可编辑 |
| 客户名称 | EPM_PAYMENT_IMPORT.CUSTOMER_NAME | 文本框 | 客户名称 | 常显 | 来源于ERP，不可编辑 |
| 收款公司 | EPM_PAYMENT_IMPORT.RECEIVE_UNIT_NAME | 文本框 | 收款公司 | 常显 | 来源于ERP，不可编辑 |
| 汇款单位 | EPM_PAYMENT_IMPORT.REMIT_UNIT_NAME | 文本框 | 汇款单位 | 常显 | 来源于ERP，不可编辑 |
| 到款单据类型 | EPM_PAYMENT_IMPORT.BILL_TYPE | 文本框 | 到款单据类型 | 常显 | RECEIPT实际到款单/VIRTUAL_RECEIPT虚拟到款单 |
| 票据类型 | EPM_PAYMENT_IMPORT.ACCEPTANCE_TYPE | 文本框 | 票据类型 | 常显 | 值集payment_acceptance_type |
| 兑付状态 | EPM_PAYMENT_IMPORT.PAYMENT_STATUS | 文本框 | �@兑付状态 | 常显 | 值集payment_status |
| 是否已兑现 | EPM_PAYMENT_IMPORT.IS_CASHOUT | 标签 | 是否已兑现 | 常显 | Y-已兑现/N-未兑现 |
| 到款引入状态 | EPM_PAYMENT_IMPORT.IMPORT_STAT | 标签 | 到款引入状态 | 常显 | 已生效/已失效 |
| 认领状态 | EPM_PAYMENT_IMPORT.ALLOT_STATUS | 标签 | 认领状态 | 常显 | WAITING待认领/CLEAR认领完成 |
| 是否家装 | EPM_PAYMENT_IMPORT.IS_HOME | 文本框 | 是否家装 | 常显 | 1-工程/2-家装 |

### 后端接口

| 接口名称 | Controller | Service | 路径 | 说明 |
|---------|-----------|---------|------|------|
| 获取ERP到款单 | EpmPaymentImportController | getOnePaymentImportFromErp | POST /v1/{orgId}/epmPaymentImport/getErpPaymentData | 从ERP获取单条到款单并处理 |
| 获取剩余可认款金额 | EpmPaymentImport&ImportController | getPaymentImportCanAllotAmt | POST /v1/{orgId}/epmPaymentImport/getPaymentImportCanAllotAmt | 认领时实时获取到款单剩余认款金额 |
| 列表查询 | EpmPaymentImportController | listSearch | POST /v1/{orgId}/epmPaymentImport/list/search | 项目到款引入列表查询 |
| 获取剩余金额 | EpmPaymentImportController | getSurAmtByImport | POST /v1/{orgId}/epmPaymentImport/sur-amt-by-import | 调用到款引入接口判断剩余可认款金额 |
| 兑现 | EpmPaymentImportController | draftPayment | POST /v1/{orgId}/epmPaymentImport/draftPayment | 商票兑付按钮 |
| 定时同步 | PaymentImportServiceImpl | syncPaymentDataFromErpJob | Job | 调度任务同步ERP到款数据 |

### 选择弹窗

本页面无选择弹窗。到款数据由ERP同步引入，非手工选择。

### 导入

本页面无手工导入功能。到款数据由ERP系统通过调度任务自动同步引入。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显&显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 兑现 | 商票兑付 | 列表页 | 票据类型为商票且未兑付 | 调用draftPayment接口，更新isCashout=Y，插入兑付记录 |
| 获取ERP到款 | 手动触发从ERP获取到款单 | 列表页 | 常显 | 调用getErpPaymentData接口，从ERP获取并处理到款单 |

#### 按钮1：兑现（列表页）

- **触发条件**：票据类型为商业承兑/银行承兑且兑付状态为未兑付(isCashout=N)
- **处理逻辑**：
  1. 校验票据类型必须为商票类型
  2. 校验兑付状态必须为未兑付
  3. 更新isCashout=Y(已兑付)
  4. 插入兑付记录到EPM_PAYMENT_IMPORT_RCD表

### 保存校验

本页面无手工保存操作。到款数据由ERP同步引入，引入时自动校验客户/交易公司/事业部有效性。

### 提交校验

本页面无提交审批操作。到款引入模块为数据引入模块，不涉及工作流审批。

### 状态机

```text
[9: ERP到款 → 引入 → [已生效] → 认领 → [认领完成(CLEAR)]
                         → 商票未兑付 → 兑付 → [已兑现] → 认领
                         → 异常 → [已失效]
```

| 状态值 | 状态名称 | 说明 |
|--------|---------|------|
| 已生效 | IMPORT_STAT=已生效 | 可认领 |
| 已失效 | IMPORT_STAT=已失效 | 不可认领 |
| WAITING | ALLOT_STATUS=待认领 | 待认领 |
| CLEAR | ALLOT_STATUS=认领完成 | 已认领完成 |

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|-------------|
| ERP系统 | 外部接口 | 到款数据来源，通过ERP接口获取到款单 | ERP系统连通且返回有效数据 |
| CUSTOMER(客户档案) | 数据校验 | 校验到款单对应的客户是否存在 | 客户已存在 |
| 交易公司 | 数据校验 | 校验交易公司是否存在 | 交易公司已存在 |
| DIVISION_BASE_SET(事业部配置) | 数据校验 | 校验事业部是否存在 | 事业部已配置 |

### 下游影响

- **项目到款认领(EPM_PAYMENT_ALLOT)**：到款引入数据作为认领的业务源头，认领时选取未认领的到款记录
- **商票兑付记录(EPM_PAYMENT_IMPORT_RCD)**：兑付操作插入兑付记录
- **剩余可认款金额**：认领时实时查询到款单剩余可认款金额，影响认领额度

---

## 数据库表详解

### EPM_PAYMENT_IMPORT（项目到款引入表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|--------|------|------|-------------|------|
| PAYMENT_IMPORT_ID | Long | 到款记录单ID | - | 自增主键 |
| PAYMENT_IMPORT_CODE | String | 到款记录编号 | 到款记录编号 | 来源于ERP(receiptNumber) |
| ORGANIZATION_ID | Long | 组织ID | - | 当前用户事业部 |
| SERIAL_NUMBER | String | 银行流水号 | 银行流水号 | 来源于ERP |
| RECEIVE_DATE | LocalDateTime | 收款日期 | 收款日期 | 来源于ERP |
| RECEIVE_AMT | BigDecimal | 收款金额 | 收款金额 | 来源于ERP |
| BASE_CURRENCY_ID | Long | 币别 | - | 来源于ERP |
| EXCHANGE_RATE | BigDecimal | 汇率 | - | 来源于ERP |
| DIVISION_ID | Long | 事业部ID | - | 来源于ERP |
| DIVISION_NAME | String | 事业部名称 | - | 来源于ERP |
| RECEIVE_UNIT_ID | Long | 收款公司ID | - | 来源于ERP |
| RECEIVE_UNIT_NAME | String | 收款公司名称 | 收款公司 | 来源于ERP |
| RECEIVE_BANK | String | 收款银行 | - | 来源于ERP |
| RECEIVE_ACCOUNT | String | 收款账号 | - | 来源于ERP |
| REMIT_UNIT_ID | Long | 汇款单位ID | - | 来源于ERP |
| REMIT_UNIT_CODE | String | 汇款单位编码 | - | 来源于ERP |
| REMIT_UNIT_NAME | String | 汇款单位名称 | 汇款单位 | 来源于ERP |
| REMIT_ACCOUNT | String | 汇款账号 | - | 来源于ERP |
| PAYMENT_REMARK | String | 打款说明 | - | 来源于ERP |
| STAT | Integer | 状态 | - | 来源于ERP |
| CONFIRMED_AMT | BigDecimal | 已认款金额 | 已认款金额 | 系统自动维护 |
| UNALLOT_AMT | BigDecimal | 剩余可认款金额 | 剩余可认款金额 | 实际单实时查ERP，虚拟单查DB |
| CUSTOMER_ID | Long | 客户ID | - | 来源于ERP，NotNull |
| CUSTOMER_CODE | String | 客户编码 | 客户编码 | 来源于ERP |
| CUSTOMER_NAME | String | 客户名称 | 客户名称 | 来源于ERP |
| ORIGINAL_AMT | BigDecimal | 到款单实际收款总金额 | - | 来源于ERP |
| BILL_TYPE | String | 到款单据类型 | 到款单据类型 | RECEIPT实际到款单/VIRTUAL_RECEIPT虚拟到款单 |
| IS_CASHOUT | String | 是否已兑现 | 是否已兑现 | Y-已兑现/N-未兑现，商票默认N |
| ACCEPTANCE_TYPE | String | 票据类型 | 票据类型 | 值集payment_acceptance_type |
| PAYMENT_STATUS | String | 兑付状态 | 兑付状态 | 值集payment_status |
| ALLOT_STATUS | String | 认领状态 | 认领状态 | WAITING待认领/CLEAR认领完成 |
| IMPORT_STAT | String | 到款引入状态 | 到款引入状态 | 已生效/已失效 |
| IMPORT_DATE | LocalDateTime | 到款引入时间 | - | 系统自动记录 |
| IS_HOME | Long | 是否家装 | 是否家装 | 1-工程/2-家装，NotNull |
| EXT_PAYMENT_IMPORT_ID | String | 外部系统对应的到款单ID | - | 用于判断是否已存在 |
| SOURCE_SYSTEM | String | 来源系统 | - | ERP |
| CUSTOMER_TRX_ID | Long | 应收事务处理ID | - | 来源于ERP |
| AUTO_ALLOT_FLAG | String | 自动认领标识 | - | Y/N |
| VIRTUAL_CLAIM_STAT | String | 虚拟单认领状态 | - | BEGINNING/FINISH |
| CREATOR_NAME | String | 创建人名称 | - | 系统自动记录 |

**查询SQL**：
```sql
SELECT * FROM EPM_PAYMENT_IMPORT
WHERE ORGANIZATION_ID = #{organizationId}
  AND NVL(PAYMENT_IMPORT_CODE, '') LIKE '%' || #{paymentImportCode} || '%'
  AND NVL(SERIAL_NUMBER, '') LIKE '%' || #{serialNumber} || '%'
  AND NVL(CUSTOMER_CODE, '') LIKE '%' || #{customerCode} || '%'
  AND RECEIVE_DATE BETWEEN #{startDate} AND #{endDate}
ORDER BY RECEIVE_DATE DESC;
```

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|--------------|------|---------|
| 客户不存在 | 引入时 | ERP到款单对应的客户在系统中不存在。先同步客户数据 | 高 | [查看] |
| 交易公司不存在 | 引入时 | 交易公司在系统中不存在。先同步交易公司数据 | 高 | [查看] |
| 事业部不存在 | 引入时 | 事业部在系统中未配置。先配置事业部 | 高 | [查看] |
| 到款单状态不允许认领 | 认领时 | 信用卡拖欠/暂停付款/冲销付款/资金不足状态。确认到款单状态 | 高 | [查看] |
| 票据类型不支持兑付 | 兑现时 | 非商票类型不支持兑付。确认票据类型 | 高 | [查看] |
| 已兑付不可重复兑付 | 兑现时 | 到款单已兑付。无需重复操作 | 高 | [查看] |
| 对账单对应经销商信息查询异常 | 引入时 | ERP到款单客户编码在系统客户档案中不存在。先同步客户数据 | 高 | [查看] |
| 对账单对应法人信息查询异常 | 引入时 | ERP到款单汇款法人编码在系统客户档案中不存在。先同步法人数据 | 高 | [查看] |
| 未找到该erp的到款单 | 获取ERP到款 | ERP接口未返回到款数据。确认ERP到款单存在 | 高 | [查看] |
| erp的到款单业务处理异常 | 获取ERP到款 | ERP到款单处理返回null。检查ERP数据完整性 | 高 | [查看] |
| 请传入到款单id | 认领校验 | 到款单ID为空。传入有效到款单ID | 高 | [查看] |
| 该到款单已撤销，不允许认领 | 认领校验 | 到款单已撤销。检查到款单状态 | 高 | [查看] |
| 未指定到款单id | 获取剩余金额 | 到款单ID未指定。传入有效到款单ID | 高 | [查看] |
| 已核销、未核销的到款单才能发起兑付 | 兑现时 | 到款单状态非已核销/未核销。确认到款单状态 | 高 | [查看] |
| 编码最大支持100个 | 定时同步 | 批量同步编码超过100个限制。减少批量编码数量 | 高 | [查看] |
| 开始时间-结束时间必须同时有值 | 定时同步 | 时间区间同步时起止时间未同时传入。补全时间区间 | 高 | [查看] |

#### 报错1：客户不存在
- **触发条件**：引入ERP到款数据时，到款单对应的CUSTOMER_ID/CUSTOMER_CODE在系统客户档案中不存在
- **逻辑分析**：在paymentImportErpProcess方法中校验，从ERP获取到款数据后，按CUSTOMER_CODE查询系统客户档案(CUSTOMER表)，若不存在则报错。需先通过客户同步接口将ERP客户数据同步到系统
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.CUSTOMER_CODE, epi.CUSTOMER_NAME,
         epi.RECEIVE_AMT, epi.RECEIVE_DATE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE NOT EXISTS (SELECT 1 FROM CUSTOMER c WHERE c.CUSTOMER_CODE = epi.CUSTOMER_CODE)
  ```

#### 报错2：交易公司不存在
- **触发条件**：引入ERP到款数据时，到款单对应的交易公司(TRADING_COMPANY)在系统中不存在
- **逻辑分析**：在paymentImportErpProcess方法中校验，从ERP获取到款数据后，按交易公司编码查询系统交易公司主数据，若不存在则报错。需先同步交易公司数据
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.RECEIVE_UNIT_NAME, epi.REMIT_UNIT_NAME,
         epi.RECEIVE_AMT, epi.RECEIVE_DATE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE NOT EXISTS (SELECT 1 FROM TRADING_COMPANY tc WHERE tc.COMPANY_NAME = epi.RECEIVE_UNIT_NAME)
  ```

#### 报错3：事业部不存在
- **触发条件**：引入ERP到款数据时，到款单对应的事业部(DEPT_CODE)在系统中未配置
- **逻辑分析**：在paymentImportErpProcess方法中校验，从ERP获取到款数据后，按事业部编码查询系统事业部配置，若不存在则报错。需先在系统中配置对应事业部
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.DEPT_CODE, epi.RECEIVE_AMT
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.DEPT_CODE IS NOT NULL
    AND NOT EXISTS (SELECT 1 FROM DEPT d WHERE d.DEPT_CODE = epi.DEPT_CODE)
  ```

#### 报错4：到款单状态不允许认领
- **触发条件**：认领到款单时，到款单状态(PAYMENT_STATUS)为信用卡拖欠/暂停付款/冲销付款/资金不足等异常状态
- **逻辑分析**：在verifyImportStat方法中校验，认领前检查到款单状态，仅正常状态(如已生效且未兑付异常)的到款单允许认领。状态为信用卡拖欠/暂停付款/冲销付款/资金不足等异常状态时阻断认领
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.PAYMENT_STATUS,
         epi.IS_CASHOUT, epi.UNALLOT_AMT, epi.ALLOT_STATUS
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.PAYMENT_STATUS IN ('OVERDUE', 'SUSPENDED', 'WRITE_OFF', 'INSUFFICIENT_FUNDS')
  ```

#### 报错5：票据类型不支持兑付
- **触发条件**：兑付到款单时，票据类型(ACCEPTANCE_TYPE)非商票类型(非商业承兑/银行承兑)
- **逻辑分析**：在draftPayment方法中校验，仅商业承兑汇票/银行承兑汇票类型(ACCEPTANCE_TYPE)的到款单支持兑付操作。其他票据类型(如电汇、现金)不支持兑付，默认已兑付(IS_CASHOUT=Y)
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.ACCEPTANCE_TYPE,
         epi.IS_CASHOUT, epi.PAYMENT_STATUS, epi.BILL_TYPE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.ACCEPTANCE_TYPE NOT IN ('COMMERCIAL_ACCEPTANCE', 'BANK_ACCEPTANCE')
    AND epi.IS_CASHOUT = 'N'
  ```

#### 报错6：已兑付不可重复兑付
- **触发条件**：兑付到款单时，到款单已兑付(IS_CASHOUT=Y)
- **逻辑分析**：在draftPayment方法中校验，兑付前检查到款单的IS_CASHOUT字段，若已为Y(已兑付)则阻断重复兑付操作。无需重复操作
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.IS_CASHOUT,
         epi.PAYMENT_STATUS, epi.ACCEPTANCE_TYPE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.IS_CASHOUT = 'Y'
  ```

#### 报错7：对账单对应经销商信息查询异常
- **触发条件**：引入ERP到款数据时，到款单对应的客户编码(CRM_NUM)在系统客户档案(CUSTOMER)中不存在
- **逻辑分析**：在paymentImportErpProcess方法中，按vo.getCrmNum()查询CUSTOMER表，若返回null则抛出阻断性报错，提示到款单号+客户编码。需先通过客户同步接口将ERP客户数据同步到系统
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.CUSTOMER_CODE, epi.CUSTOMER_NAME,
         epi.RECEIVE_AMT, epi.RECEIVE_DATE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE NOT EXISTS (SELECT 1 FROM CUSTOMER c WHERE c.CUSTOMER_CODE = epi.CUSTOMER_CODE)
  ```

#### 报错8：对账单对应法人信息查询异常
- **触发条件**：引入ERP到款数据时，到款单对应的汇款法人编码(PAY_FROM_CUST)在系统客户档案(CUSTOMER)中不存在
- **逻辑分析**：在paymentImportErpProcess方法中，按vo.getPayFromCust()查询CUSTOMER表作为汇款单位，若返回null则抛出阻断性报错，提示到款单号+法人编码。需先同步法人数据到系统客户档案
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.REMIT_UNIT_CODE, epi.REMIT_UNIT_NAME,
         epi.RECEIVE_AMT, epi.RECEIVE_DATE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE NOT EXISTS (SELECT 1 FROM CUSTOMER c WHERE c.CUSTOMER_CODE = epi.REMIT_UNIT_CODE)
  ```

#### 报错9：未找到该erp的到款单
- **触发条件**：调用getOnePaymentImportFromErp接口从ERP获取到款单时，ERP接口返回空列表
- **逻辑分析**：getOnePaymentImportFromErp方法中调用erpSdk.getErpReceipt获取ERP到款数据，若返回null或空列表则抛出阻断性报错。可能原因：ERP到款单不存在、ERP接口异常。需确认ERP到款单存在
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.EXT_PAYMENT_IMPORT_ID,
         epi.SOURCE_SYSTEM, epi.IMPORT_STAT
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.EXT_PAYMENT_IMPORT_ID = :extPaymentImportId
  -- 若返回空，说明ERP到款单未同步到系统
  ```

#### 报错10：erp的到款单业务处理异常
- **触发条件**：调用getOnePaymentImportFromErp接口处理ERP到款单时，handleErpReceiptBiz返回null
- **逻辑分析**：getOnePaymentImportFromErp方法中调用handleErpReceiptBiz处理ERP到款数据，若返回ID为null则抛出阻断性报错。可能原因：ERP数据字段缺失、客户/交易公司/事业部校验失败。需检查ERP数据完整性
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.IMPORT_STAT, epi.VALID
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.EXT_PAYMENT_IMPORT_ID = :extPaymentImportId
    AND epi.IMPORT_STAT IS NULL
  -- 查出业务处理异常的到款单
  ```

#### 报错11：请传入到款单id
- **触发条件**：调用verifyImportStat校验到款单状态时，到款单ID(paymentImportId)为空或<=0
- **逻辑分析**：verifyImportStat方法中校验paymentImportId非空且>0，因需按ID查询到款单状态。需传入有效到款单ID
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.PAYMENT_STATUS, epi.IMPORT_STAT
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.PAYMENT_IMPORT_ID = :paymentImportId
  -- 校验到款单ID是否有效
  ```

#### 报错12：该到款单已撤销，不允许认领
- **触发条件**：校验到款单状态时，到款单已撤销(IMPORT_STAT=已失效或ALLOT_STATUS=CANCEL)
- **逻辑分析**：verifyImportStat方法中查询到款单状态，若到款单已撤销则抛出阻断性报错。需检查到款单状态，确认到款单未撤销
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.IMPORT_STAT, epi.ALLOT_STATUS,
         epi.PAYMENT_STATUS, epi.IS_CASHOUT
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.IMPORT_STAT = '已失效'
     OR epi.ALLOT_STATUS = 'CANCEL'
  -- 查出已撤销的到款单
  ```

#### 报错13：未指定到款单id
- **触发条件**：调用getSurAmtByImport获取剩余可认款金额时，到款单ID未指定
- **逻辑分析**：getSurAmtByImport方法中校验到款单ID非空，因需按ID查询到款单剩余可认领金额。需传入有效到款单ID
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.UNALLOT_AMT, epi.BILL_TYPE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.PAYMENT_IMPORT_ID = :paymentImportId
  -- 校验到款单ID是否有效
  ```

#### 报错14：已核销、未核销的到款单才能发起兑付
- **触发条件**：兑付到款单时，到款单状态(PAYMENT_STATUS)非已核销(SUCCESS)或未核销(PENDING)
- **逻辑分析**：draftPayment方法中校验到款单状态，仅PAYMENT_STATUS为SUCCESS(已核销)或PENDING(未核销)的到款单允许发起兑付。其他状态(如处理中、失败)不允许兑付。需确认到款单状态
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.PAYMENT_STATUS,
         epi.IS_CASHOUT, epi.ACCEPTANCE_TYPE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.ACCEPTANCE_TYPE IN ('COMMERCIAL_ACCEPTANCE', 'BANK_ACCEPTANCE')
    AND epi.PAYMENT_STATUS NOT IN ('SUCCESS', 'PENDING')
  -- 查出状态不允许兑付的商票到款单
  ```

#### 报错15：编码最大支持100个
- **触发条件**：定时同步任务syncPaymentDataFromErpJob按指定编码同步时，传入的编码列表超过100个
- **逻辑分析**：syncPaymentDataFromErpJob方法中校验编码列表大小，超过100个则抛出阻断性报错。因ERP接口对批量查询有数量限制。需减少批量编码数量，分批同步
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 编码数量
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.PAYMENT_IMPORT_CODE IN (:codeList)
  -- 校验传入的编码数量是否超过100
  ```

#### 报错16：开始时间-结束时间必须同时有值
- **触发条件**：定时同步任务syncPaymentDataFromErpJob按时间区间同步时，开始时间或结束时间只传了一个
- **逻辑分析**：syncPaymentDataFromErpJob方法中校验时间区间，开始时间和结束时间必须同时有值或同时为空。需补全时间区间
- **排查SQL**：
  ```sql
  SELECT epi.PAYMENT_IMPORT_ID, epi.PAYMENT_IMPORT_CODE, epi.RECEIVE_DATE, epi.IMPORT_DATE
  FROM EPM_PAYMENT_IMPORT epi
  WHERE epi.RECEIVE_DATE BETWEEN :startDate AND :endDate
  -- 校验时间区间是否完整
  ```

### 常见问题

**Q1: 到款数据从哪里来？**
A: 来源于ERP系统，通过调度任务syncPaymentDataFromErpJob自动同步，或通过getErpPaymentData接口手动触发引入。编号(paymentImportCode)来自ERP的receiptNumber。

**Q2: 商票兑付的作用？**
A: 商业承兑/银行承兑类型到款单默认未兑付(isCashout=N)，需手动点击兑付按钮更新为已兑付(isCashout=Y)后才可认领。其他类型默认已兑付。

**Q3: 剩余可认款金额如何获取？**
A: 虚拟单(VIRTUAL_RECEIPT)查DB获取，实际单(RECEIPT)!实时查ERP获取最新金额，确保认领时不#时不超额。

**Q4: 定时同步任务支持哪些模式？**
A: syncPaymentDataFromErpJob支持指定编码或时间区间同步，批量处理ERP返回数据。

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|--------|--------|---------|
| 2025-12-06 | - | tzx | 初始版本，项目到款引入功能 |
| 2026-07-31 | - | - | 初始生成知识库文档 |
| 2026-08-30 | - | CodeArts | 按skill规范重写：移除定义章节、章节去编号、界面模块表格改为6列、数据库表改为5列、报错表改为5列、新增上游依赖/下游影响/选择弹窗/导入/保存校验/提交校验/状态机章节、补充4个重点逻辑和6个后端接口 |
