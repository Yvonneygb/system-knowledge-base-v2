# 经销合同管理-报表中心-经销合同销售区域报表

## 业务流程

### 业务流程图

```text
经销合同销售区域报表页面(ae-report报表)
        │
        ├── 查询报表 ──→ POST /contractReport/sa-sale-contract-head/search
        │                  （按年度/经销商/事业部/销售区域查询）
        │
        └── 导出 ──→ GET /contractReport/sa-sale-contract-head/export
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 年度经销合同 | 数据依赖 | 报表数据来源于经销合同 | 合同已签订 |

### 下游影响

- 影响1：销售区域分析
  - 报表为销售区域分析提供合同维度数据支撑

---

## 重点逻辑

### 重点逻辑1：销售区域维度统计 {区域统计}

- **业务意义**：按销售区域维度统计经销合同数据，掌握各区域合同分布

- **具体逻辑描述**

  - 第1点：通过saSaleContractHeadSearch接口按销售区域查询合同数据

  - 第2点：支持按年度、经销商、事业部、销售区域等条件筛选

---

## 详细逻辑

### 界面模块1：经销合同销售区域报表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 合同编号 | SA_SALE_CONTRACT_HEAD.CONTRACT_NO | 文本框 | 合同编号 | 常显 | 系统带出 |
| 经销商 | SA_SALE_CONTRACT_HEAD.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 系统带出 |
| 合同年度 | SA_SALE_CONTRACT_HEAD.CONTRACT_YEAR | 文本框 | 合同年度 | 常显 | 系统带出 |
| 销售区域 | SA_SALE_CONTRACT_HEAD.SALE_AREA | 文本框 | �=售区域 | 常显 | 系统带出 |
| 事业部 | SA_SALE_CONTRACT_HEAD.ENTNAME | 文本框 | 事业部名称 | 常显 | 系统带出 |

### 选择弹窗

> 本页面为报表查询页面，无独立弹窗。

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询报表 | 列表页 | 始终可用 | 调用saSaleContractHeadSearch接口 |
| 导出 | 导出报表 | 列表页 | 有数据时 | 调用saSaleContractHeadExport接口 |

### 保存校验

> 本页面为报表页面，无保存操作。

### 提交校验

> 本页面为报表页面，无提交操作。

### 状态机

> 本页面为报表页面，无状态流转。

---

## 数据库表详解

### 表1：SA_SALE_CONTRACT_HEAD（年度经销合同头表）

> 与菜单115共用同一张表。报表仅查询展示，字段详见"经销合同管理-销售合同-年度经销合同"。

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | �4报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 条件不匹配或无合同数据，放宽条件 | toast提醒 | [查看] |
| 导出失败 | 导出时 | 导出数据量为空或导出接口异常，先查询再导出 | toast提醒 | [查看] |
| 网络请求失败 | 查询/导出时 | 后端报表服务不可用或网络中断，重试或联系IT | toast提醒 | [查看] |
| 权限不足 | 进入页面时 | 当前用户无报表查询权限，联系管理员授权 | 阻断性报错 | [查看] |
| 必填查询条件为空 | 查询时 | 未填写年度/经销商等必填条件，补全后查询 | toast提醒 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按年度/经销商/事业部/销售区域查询经销合同销售区域报表，saSaleContractHeadSearch接口返回空结果集
- **逻辑分析**：报表基于SA_SALE_CONTRACT_HEAD表按销售区域（SALE_AREA）维度统计经销合同数据。无数据根因有三类：(1)查询条件（CONTRACT_YEAR+CUSTOMER_NAME+ENTNAME+SALE_AREA）组合过窄，无匹配合同；(2)合同未审批通过（HZ_APPROVE_STATUS != 'APPROVED'），报表仅展示已审批合同；(3)合同销售区域（SALE_AREA）未配置，字段为空导致按区域筛选时被过滤。需先放宽条件（去掉销售区域）确认是否有合同数据，再核查SALE_AREA配置
- **排查SQL**：
  ```sql
  SELECT CONTRACT_NO, CUSTOMER_NAME, CONTRACT_YEAR, SALE_AREA, ENTNAME, HZ_APPROVE_STATUS
  FROM SA_SALE_CONTRACT_HEAD
  WHERE HZ_APPROVE_STATUS = 'APPROVED'
    AND (CONTRACT_YEAR = #{contractYear} OR #{contractYear} IS NULL)
    AND (CUSTOMER_NAME = #{customerName} OR #{customerName} IS NULL)
    AND (SALE_AREA = #{saleArea} OR #{saleArea} IS NULL)
  ORDER BY CONTRACT_YEAR DESC, CONTRACT_NO;
  ```

#### 报错2：导出失败
- **触发条件**：用户点击"导出"按钮，saSaleContractHeadExport接口返回空数据集或抛出异常
- **逻辑分析**：导出接口saSaleContractHeadExport基于saSaleContractHeadSearch同一查询逻辑，通过SaSaleContractHeadConvert.INSTANCE.convert转换导出VO。若查询结果集为空则导出文件无内容；若导出过程中LOV翻译失败（@ProcessLovValue注解处理）或内存溢出也会失败。需先执行查询确认有数据再导出
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 可导出记录数
  FROM SA_SALE_CONTRACT_HEAD
  WHERE HZ_APPROVE_STATUS = 'APPROVED'
    AND (CONTRACT_YEAR = #{contractYear} OR #{contractYear} IS NULL);
  ```

#### 报错3：网络请求失败
- **触发条件**：前端调用contractReport/sa-sale-contract-head/search或export接口时，axios请求超时或返回非2xx状态码
- **逻辑分析**：报表服务ae-report与前端跨服务调用，网络中断、服务未启动、网关路由异常均会导致请求失败。SaSaleContractHeadServiceImpl.saSaleContractHeadSearch通过PageHelper.doPageAndSort分页查询，服务异常时无法返回数据。需确认ae-report服务状态及网关配置
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 合同总记录数 FROM SA_SALE_CONTRACT_HEAD
  WHERE HZ_APPROVE_STATUS = 'APPROVED';
  ```

#### 报错4：权限不足
- **触发条件**：当前用户角色未配置报表查询权限，访问页面时被拦截
- **逻辑分析**：报表页面通过permissionList控制访问权限，用户无对应权限码时无法进入页面或调用接口。需在角色管理中为用户分配"经销合同销售区域报表"查询权限
- **排查SQL**：
  ```sql
  SELECT ROLE_CODE, PERMISSION_CODE FROM USER_ROLE_PERMISSION
  WHERE USER_ID = #{userId} AND PERMISSION_CODE LIKE '%saSaleContractHead%';
  ```

#### 报错5：必填查询条件为空
- **触发条件**：用户未填写年度（CONTRACT_YEAR）等必填查询条件直接点击查询
- **逻辑分析**：SaSaleContractHeadSearchDTO中年度是报表维度核心字段，未填写年度将导致查询无明确时间范围，返回全量或空数据。前端查询DS中年度字段配置required校验，未填写时DataSet.query()前置校验拦截并提示
- **排查SQL**：
  ```sql
  SELECT DISTINCT CONTRACT_YEAR FROM SA_SALE_CONTRACT_HEAD
  WHERE CONTRACT_YEAR IS NOT NULL ORDER BY CONTRACT_YEAR DESC;
  ```

### 常见问题

- 问题1：报表数据不完整
  - 原因：部分合同未审批通过或销售区域未配置
  - 解决思路：检查SQL `SELECT * FROM SA_SALE_CONTRACT_HEAD WHERE HZ_APPROVE_STATUS = 'APPROVED' AND SALE_AREA IS NOT NULL`

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写 |
