# 家装管理-项目往来-家装真实性核销

---

## 业务流程

### 业务流程图

```text
开始 → ★新建真实性核销★ → 选择项目/合同/经销商 → 查询出库单 → 选择发票信息 → 核对发票与出库明细 → 保存 → 提交审批 → ⚖审批通过？
  ├─（通过）核销完成 → 结束
  ├─（拒绝）可修改后重新提交 → 结束
  ├─（终止）终止流程 → 可撤回终止 → 结束
  └─（取消/作废）作废核销单 → 结束
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 项目主档 | 数据依赖 | 选择家装项目（isHome=2, contractType=2） | 项目已创建 |
| 合同主档 | 数据依赖 | 关联家装合同 | 合同已签订 |
| 经销商主档 | 数据依赖 | 选择经销商 | 经销商已维护 |
| 出库单 | 数据依赖 | 查询工程出库单报表 | 出库单已生成 |
| 发票信息 | 数据依赖 | 查询关联发票 | 发票已开具 |
| 交易公司主档 | 数据依赖 | 选择交易公司（isHome=2） | 交易公司已维护 |
| OA工作流引擎 | 服务依赖 | 提交审批触发工作流PROJECT_JZXMZSXHX_AW | 工作流已配置 |
| 值集HWKF.APPROVE_STATUS | 配置依赖 | 单据状态显示 | 值集已维护 |
| 值集AE.VERIFER_TYPE | 配置依赖 | 核销类型选择 | 值集已维护 |

### 下游影响

- 影响1：财务核销
  - 审批通过后真实性核销数据进入财务核销流程

- 影响2：家装核销发票上传
  - 真实性核销关联核销发票上传记录

---

## 重点逻辑

### 重点逻辑1：出库单与发票核对 `核对校验`

- **业务意义**：核销时需核对出库明细与发票明细是否一致

- **具体逻辑描述**

  - 第1点：查询工程出库单报表获取出库明细

  - 第2点：查询发票信息获取发票明细

  - 第3点：在核对页面比对出库数量与发票数量

### 重点逻辑2：自助查找出库单和发票 `灵活查询`

- **业务意义**：支持自助查找关联的出库单和发票信息

- **具体逻辑描述**

  - 第1点：self-search-out-bill接口支持自助查找出库单

  - 第2点：self-search-invoice接口支持自助查找发票

### 重点逻辑3：关联核销明细 `关联查询`

- **业务意义**：查看当前核销单关联的其他核销明细

- **具体逻辑描述**

  - 第1点：related-verify-details接口查询关联核销明细

  - 第2点：在弹窗中展示关联明细信息

### 重点逻辑4：终止与取消 `流程控制`

- **业务意义**：支持审批流程的终止、撤回和取消/作废

- **具体逻辑描述**

  - 第1点：终止操作将流程标记为终止状态，可撤回

  - 第2点：取消/作废操作将核销单标记为作废，不可恢复

---

## 详细逻辑

### 界面模块1：家装真实性核销列表页

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 真实性核销单号 | EPM_INVOICE_TRUTH_HEADERS.INVOICE_TRUTH_NO | 文本框 | 核销单唯一标识 | 常显 | 1. 查询支持模糊匹配 |
| 审核状态 | EPM_INVOICE_TRUTH_HEADERS.HZ_APPROVE_STATUS | 下拉选择框 | 审批状态 | 常显 | 1. 来源：值集HWKF.APPROVE_STATUS |
| 核销发起日期 | EPM_INVOICE_TRUTH_HEADERS.CREATION_DATE | 日期选择框 | 核销发起日期 | 常显 | 1. 查询为范围查询 |
| 项目编码 | EPM_INVOICE_TRUTH_HEADERS.PROJECT_CODE | 文本框 | 项目编码 | 常显 | 1. 查询支持模糊匹配 |
| 项目名称 | EPM_INVOICE_TRUTH_HEADERS.PROJECT_NAME | 文本框 | 项目名称 | 常显 | 1. 查询支持模糊匹配 |
| 合同编码 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_CODE | 文本框 | 合同编码 | 常显 | 1. 查询支持模糊匹配 |
| 合同名称 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_NAME | 文本框 | 合同名称 | 常显 | 1. 查询支持模糊匹配 |
| 经销商编码 | EPM_INVOICE_TRUTH_HEADERS.CUSTOMER_CODE | 文本框 | 经销商编码 | 常显 | 1. 查询支持模糊匹配 |
| 经销商名称 | EPM_INVOICE_TRUTH_HEADERS.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 1. 查询支持模糊匹配 |
| 交易公司 | EPM_INVOICE_TRUTH_HEADERS.TRADING_COMPANY_NAME | 文本框 | 交易公司 | 常显 | 1. 查询支持模糊匹配 |
| 开票单位 | EPM_INVOICE_TRUTH_HEADERS.BILLING_UNIT_NAME | 文本框 | 开票单位 | 常显 | 1. 查询支持模糊匹配 |
| 工程签约单位 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_UNIT | 文本框 | 工程签约单位 | 常显 | 1. 查询支持模糊匹配 |
| 申请人 | EPM_INVOICE_TRUTH_HEADERS.CREATED_BY_NAME | 文本框 | 申请人 | 常显 | 1. 查询支持模糊匹配 |
| 修改人 | EPM_INVOICE_TRUTH_HEADERS.LAST_UPDATED_BY_NAME | 文本框 | 修改人 | 常显 | 1. 查询支持模糊匹配 |
| 备注 | EPM_INVOICE_TRUTH_HEADERS.NOTE | 文本框 | 备注 | 常显 | 1. 查询支持模糊匹配 |
| 申请时间 | EPM_INVOICE_TRUTH_HEADERS.CREATION_DATE | 日期选择框 | 申请时间 | 常显 | 1. 查询为范围查询 |
| 修改时间 | EPM_INVOICE_TRUTH_HEADERS.LAST_UPDATE_DATE | 日期选择框 | 修改时间 | 常显 | 1. 查询为范围查询 |

### 界面模块2：家装真实性核销详情页-头信息

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 真实性核销单号 | EPM_INVOICE_TRUTH_HEADERS.INVOICE_TRUTH_NO | 文本框 | 核销单唯一标识 | 常显 | 1. 保存时自动生成 2. 可编辑：否 |
| 核销发起日期 | EPM_INVOICE_TRUTH_HEADERS.CREATION_DATE | 日期选择框 | 核销发起日期 | 常显 | 1. 默认值：当前日期 2. 可编辑：否 |
| 申请人 | EPM_INVOICE_TRUTH_HEADERS.CREATOR_NAME | 文本框 | 申请人 | 常显 | 1. 默认值：当前用户 2. 可编辑：否 |
| 单据状态 | EPM_INVOICE_TRUTH_HEADERS.HZ_APPROVE_STATUS | 下拉选择框 | 审批状态 | 常显 | 1. 默认值：NEW 2. 来源：值集HWKF.APPROVE_STATUS 3. 可编辑：否 |
| 项目编码 | EPM_INVOICE_TRUTH_HEADERS.PROJECT_CODE | LOV选择 | 项目编码 | 常显 | 1. 来源：LOV AE.UPLOAD_WRITE_OFF_INVOICE_PROJECT_VIEW（isHome=2, contractType=2） 2. 必填 |
| 项目名称 | EPM_INVOICE_TRUTH_HEADERS.PROJECT_NAME | 文本框 | 项目名称 | 常显 | 1. 来源：选择项目后自动带出 2. 可编辑：否 |
| 合同编码 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_CODE | LOV选择 | 合同编码 | 常显 | 1. 来源：LOV AE.QUERY_CONTRACT_VIEW（isHome=2, contractType=2） 2. 必填 |
| 合同名称 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_NAME | 文本框 | 合同名称 | 常显 | 1. 来源：选择合同后自动带出 2. 可编辑：否 |
| 经销商编码 | EPM_INVOICE_TRUTH_HEADERS.CUSTOMER_CODE | LOV选择 | 经销商编码 | 常显 | 1. 来源：LOV BASIC_CUSTOM_ORG_LOV_2（searchFlag=142） 2. 必填 |
| 经销商名称 | EPM_INVOICE_TRUTH_HEADERS.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 1. 来源：选择经销商后自动带出 2. 可编辑：否 |
| 核销类型 | EPM_INVOICE_TRUTH_HEADERS.VERIFER_TYPE | 下拉选择框 | 核销类型 | 常显 | 1. 默认值：1 2. 来源：值集AE.VERIFER_TYPE 3. 必填 |
| 交易公司 | EPM_INVOICE_TRUTH_HEADERS.TRADING_COMPANY_NAME | LOV选择 | 交易公司 | 常显 | 1. 来源：LOV TRADING_LEGAL_SQL_V（isHome=2） |
| 开票单位 | EPM_INVOICE_TRUTH_HEADERS.BILLING_UNIT_NAME | 文本框 | 开票单位 | 常显 | 1. 可编辑：是 |
| 签约单位 | EPM_INVOICE_TRUTH_HEADERS.CONTRACT_UNIT | 文本框 | 签约单位 | 常显 | 1. 可编辑：是 |
| 备注 | EPM_INVOICE_TRUTH_HEADERS.NOTE | 文本框 | 备注 | 常显 | 1. 可编辑：是 |

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新建 | 新建真实性核销 | 列表页 | 常显 | 跳转详情页新建态 |
| 保存 | 保存核销单 | 详情页 | 编辑态显示 | 保存核销信息 |
| 提交 | 提交审批 | 详情页 | 状态为NEW或REJECTED时显示 | 触发工作流PROJECT_JZXMZSXHX_AW |
| 编辑 | 进入编辑态 | 详情页 | 非编辑态且状态允许时显示 | 切换为编辑态 |
| 删除 | 删除核销单 | 详情页 | 新建状态显示 | 删除当前记录 |
| 终止 | 终止流程 | 详情页 | 审批中状态显示 | 终止审批流程 |
| 撤回终止 | 撤回终止 | 详情页 | 已终止状态显示 | 恢复流程到终止前状态 |
| 取消/作废 | 作废核销单 | 详情页 | 非新建状态显示 | 作废核销单，不可恢复 |
| 自助查找出库单 | 查找出库单 | 详情页 | 编辑态显示 | 弹出出库单查询弹窗 |
| 自助查找发票 | 查找发票 | 详情页 | 编辑态显示 | 弹出发票查询弹窗 |
| 关联核销明细 | 查看关联明细 | 详情页 | 常显 | 弹出关联核销明细弹窗 |

### 状态机

#### 状态机流转图

```text
NEW（新建） ──提交──→ RUN（审批中） ──审批通过──→ APPROVED（已审核）
                         │                    │
                         ├──审核拒绝──→ REJECTED ──提交──→ RUN
                         ├──终止──→ TERMINATED ──撤回终止──→ RUN
                         └──撤回──→ NEW

任意状态 ──取消/作废──→ CANCELLED（已作废）
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| NEW | 新建 | 编辑、保存、提交、删除、作废 |
| RUN | 审批中 | 查看、终止、作废 |
| APPROVED | 已审核 | 查看、作废 |
| REJECTED | 审核拒绝 | 编辑、保存、提交、作废 |
| TERMINATED | 已终止 | 撤回终止、作废 |
| CANCELLED | 已作废 | 查看 |

---

## 数据库表详解

### 表1：EPM_INVOICE_TRUTH_HEADERS（真实性核销头表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| INVOICE_TRUTH_ID | NUMBER | 主键 | 无（隐藏） | 自增生成 |
| INVOICE_TRUTH_NO | VARCHAR | 真实性核销单号 | 真实性核销单号 | 保存时自动生成 |
| HZ_APPROVE_STATUS | VARCHAR | 审批状态 | 单据状态 | 默认NEW，值集HWKF.APPROVE_STATUS |
| PROJECT_CODE | VARCHAR | 项目编码 | 项目编码 | 关联项目主档 |
| PROJECT_NAME | VARCHAR | 项目名称 | 项目名称 | 冗余字段 |
| CONTRACT_CODE | VARCHAR | 合同编码 | 合同编码 | 关联合同主档 |
| CONTRACT_NAME | VARCHAR | 合同名称 | 合同名称 | 冗余字段 |
| CUSTOMER_CODE | VARCHAR | 经销商编码 | 经销商编码 | 关联经销商主档 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商名称 | 冗余字段 |
| VERIFER_TYPE | VARCHAR | 核销类型 | 核销类型 | 默认1，值集AE.VERIFER_TYPE |
| TRADING_COMPANY_NAME | VARCHAR | 交易公司 | 交易公司 | - |
| BILLING_UNIT_NAME | VARCHAR | 开票单位 | 开票单位 | - |
| CONTRACT_UNIT | VARCHAR | 签约单位 | 签约单位 | - |
| NOTE | VARCHAR | 备注 | 备注 | 选填 |
| CREATION_DATE | TIMESTAMP | 创建时间 | 核销发起日期 | 框架自动填充 |
| CREATED_BY | NUMBER | 创建人 | 无 | 框架自动填充 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 无出库单数据 | 查询出库单 | 项目/合同下无出库单。确认出库单已生成 | toast提醒 | [查看] |
| 无发票数据 | 查询发票 | 未找到关联发票。确认发票已开具 | toast提醒 | [查看] |
| 请选择项目 | 查询出库单/发票 | 核销类型为项目核销时未选择项目。先选择项目 | toast提醒 | [查看] |
| 请选择经销商 | 查询出库单/发票 | 未选择经销商。先选择经销商 | toast提醒 | [查看] |
| 请选择交易公司 | 查询出库单/发票 | 未选择交易公司。先选择交易公司 | toast提醒 | [查看] |
| 交易公司的法人为空 | 查询出库单/发票 | 交易公司未配置法人实体。重新选择交易公司 | toast提醒 | [查看] |
| 请先选择发票明细 | 查询出库单 | 发票优先模式下未选择发票行。先选择发票明细 | toast提醒 | [查看] |
| 请先选择出库单明细 | 查询发票 | 出库单优先模式下未选择出库单行。先选择出库单明细 | toast提醒 | [查看] |
| 表头必填字段未填写 | 自助查找 | 经销商/核销类型/交易公司/开票单位未填。先完善表头 | toast提醒 | [查看] |
| 本次核销数量必须大于0 | 保存 | 核销明细行本次核销数量为空或小于0。填写正数 | 阻断性报错 | [查看] |
| 请选择出库明细 | 确定核对 | 未选择出库单行。先选择出库明细行 | toast提醒 | [查看] |
| 请选择发票信息 | 确定核对 | 未选择发票行。先选择发票信息行 | toast提醒 | [查看] |
| 本次核销数量不能大于剩余可核销数量 | 保存/核销 | 本次核销数量超过剩余可核销数量。调整核销数量 | 阻断性报错 | [查看] |
| 核销出库单明细不可为空 | 提交 | 未维护核销出库单明细。先添加明细再提交 | 阻断性报错 | [查看] |
| 核销明细状态异常 | 取消核销 | 核销明细状态不一致。刷新数据后重试 | 阻断性报错 | [查看] |
| 不支持当前操作 | 终止/取消 | 单据状态不支持当前操作。确认单据状态 | 阻断性报错 | [查看] |
| 暂无数据 | 列表查询 | 查询条件无匹配数据。调整查询条件 | toast提醒 | [查看] |
| 会话过期 | 任意操作 | 登录态失效。重新登录 | 阻断性报错 | [查看] |

#### 报错1：无出库单数据
- **触发条件**：详情页点击"自助查找出库单"或自动查询出库单时，所选项目/合同下无出库单记录
- **逻辑分析**：后端self-search-out-bill接口根据项目编码（PROJECT_CODE）和合同编码（CONTRACT_CODE）查询工程出库单报表。若项目下无出库单、出库单未生成或出库单状态不符合查询条件，则返回空列表，前端提示"无出库单数据"。
- **排查SQL**：
  ```sql
  SELECT H.PROJECT_CODE AS 项目编码, H.PROJECT_NAME AS 项目名称,
         H.CONTRACT_CODE AS 合同编码, H.CONTRACT_NAME AS 合同名称
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 关联查询出库单（具体表名依据工程出库单报表表）
  -- SELECT * FROM 工程出库单表 WHERE PROJECT_CODE = :projectCode AND CONTRACT_CODE = :contractCode;
  ```

#### 报错2：无发票数据
- **触发条件**：详情页点击"自助查找发票"或自动查询发票时，未找到关联发票记录
- **逻辑分析**：后端self-search-invoice接口根据项目、合同、经销商等条件查询关联发票。若发票未开具、发票已作废或发票关联关系未建立，则返回空列表，前端提示"无发票数据"。
- **排查SQL**：
  ```sql
  SELECT H.PROJECT_CODE AS 项目编码, H.CUSTOMER_CODE AS 经销商编码,
         H.CONTRACT_CODE AS 合同编码, H.VERIFER_TYPE AS 核销类型
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 关联查询发票（具体表名依据发票主表）
  -- SELECT * FROM 发票主表 WHERE PROJECT_CODE = :projectCode AND CUSTOMER_CODE = :customerCode;
  ```

#### 报错3：请选择项目
- **触发条件**：核销类型为项目核销（veriferType=1）时，查询出库单或发票但未选择项目（projectId为空）
- **逻辑分析**：前端outStockListLoad和invoiceListLoad校验veriferType==1时projectId非空。若为空则提示"请选择项目"并阻断查询。项目是项目核销类型的必要条件。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.VERIFER_TYPE AS 核销类型,
         H.PROJECT_CODE AS 项目编码, H.PROJECT_ID AS 项目ID
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId
    AND H.VERIFER_TYPE = 1
    AND H.PROJECT_ID IS NULL;
  ```

#### 报错4：请选择经销商
- **触发条件**：核销类型非项目核销（veriferType≠1）时，查询出库单或发票但未选择经销商（customerId为空或小于0）
- **逻辑分析**：前端outStockListLoad和invoiceListLoad校验veriferType≠1时customerId非空且大于0。若为空则提示"请选择经销商"并阻断查询。经销商是非项目核销类型的必要条件。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.VERIFER_TYPE AS 核销类型,
         H.CUSTOMER_CODE AS 经销商编码, H.CUSTOMER_ID AS 经销商ID
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId
    AND H.VERIFER_TYPE <> 1
    AND (H.CUSTOMER_ID IS NULL OR H.CUSTOMER_ID < 0);
  ```

#### 报错5：请选择交易公司
- **触发条件**：核销类型非项目核销（veriferType≠1）时，查询出库单或发票但未选择交易公司（tradingCompanyId为空或小于0）
- **逻辑分析**：前端outStockListLoad和invoiceListLoad校验veriferType≠1时tradingCompanyId非空且大于0。若为空则提示"请选择交易公司"并阻断查询。交易公司是非项目核销类型的必要条件。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.VERIFER_TYPE AS 核销类型,
         H.TRADING_COMPANY_NAME AS 交易公司, H.TRADING_COMPANY_ID AS 交易公司ID
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId
    AND H.VERIFER_TYPE <> 1
    AND (H.TRADING_COMPANY_ID IS NULL OR H.TRADING_COMPANY_ID < 0);
  ```

#### 报错6：交易公司的法人为空
- **触发条件**：核销类型非项目核销（veriferType≠1）时，已选择交易公司但开票单位（billingUnitId）为空或小于0
- **逻辑分析**：前端outStockListLoad和invoiceListLoad校验billingUnitId非空且大于0。若为空则提示"交易公司的法人为空，重新选择交易公司"。交易公司未配置法人实体时开票单位无法带出。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号,
         H.TRADING_COMPANY_NAME AS 交易公司, H.BILLING_UNIT_NAME AS 开票单位
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId
    AND H.VERIFER_TYPE <> 1
    AND (H.BILLING_UNIT_ID IS NULL OR H.BILLING_UNIT_ID < 0);
  ```

#### 报错7：请先选择发票明细
- **触发条件**：发票优先模式（actionType为invoiceToOrder）下查询出库单时，未选择发票列表行（invoiceListTableDs.selected为空）
- **逻辑分析**：前端outStockListLoad和handleOpenSelfSearchOutBill在发票优先模式下校验invoiceListTableDs.selected.length > 0。若为空则提示"请先选择发票明细"。发票优先模式需先选定发票再查关联出库单。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.VERIFER_TYPE AS 核销类型
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 检查发票明细是否已选择
  -- SELECT * FROM EPM_INVOICE_TRUTH_LINE WHERE INVOICE_TRUTH_ID = :truthId AND INVOICE_DETAILS_ID IS NOT NULL;
  ```

#### 报错8：请先选择出库单明细
- **触发条件**：出库单优先模式（actionType为orderToInvoice）下查询发票时，未选择出库单列表行（outStockListTableDs.selected为空）
- **逻辑分析**：前端invoiceListLoad和handleOpenSelfSearchInvoice在出库单优先模式下校验outStockListTableDs.selected或outStockDetailTableDs.selected非空。若为空则提示"请先在核销出库单明细中选择一行出库单"。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.VERIFER_TYPE AS 核销类型
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 检查出库单明细是否已选择
  -- SELECT * FROM EPM_INVOICE_TRUTH_LINE WHERE INVOICE_TRUTH_ID = :truthId AND INV_OUT_BILL_LINE_ID IS NOT NULL;
  ```

#### 报错9：表头必填字段未填写
- **触发条件**：点击"自助查找出库单"或"自助查找发票"时，表头经销商编码、经销商名称、核销类型、交易公司、开票单位任一为空
- **逻辑分析**：前端validateHeadFields校验customerCode、customerName、veriferType、tradingCompanyName、billingUnitName非空。若任一为空则提示"请先填写以下表头字段：X、Y"。这些字段是查询出库单和发票的前置条件。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号,
         H.CUSTOMER_CODE AS 经销商编码, H.CUSTOMER_NAME AS 经销商名称,
         H.VERIFER_TYPE AS 核销类型, H.TRADING_COMPANY_NAME AS 交易公司,
         H.BILLING_UNIT_NAME AS 开票单位
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId
    AND (H.CUSTOMER_CODE IS NULL OR H.CUSTOMER_NAME IS NULL
         OR H.VERIFER_TYPE IS NULL OR H.TRADING_COMPANY_NAME IS NULL
         OR H.BILLING_UNIT_NAME IS NULL);
  ```

#### 报错10：本次核销数量必须大于0
- **触发条件**：详情页保存时，核销明细行（outStockDetailTableDs）的本次核销数量（thisVeriferNumber）为空或小于0
- **逻辑分析**：前端validCheck遍历outStockDetailTableDs.toData()，若任一行thisVeriferNumber为空或小于0则提示"第X行，本次核销数量必须大于0！"并阻断保存。
- **排查SQL**：
  ```sql
  SELECT L.INVOICE_TRUTH_ID AS 核销ID, L.INV_OUT_BILL_LINE_ID AS 出库单行ID,
         L.ITEM_CODE AS 产品编码, L.THIS_VERIFER_NUMBER AS 本次核销数量
  FROM EPM_INVOICE_TRUTH_LINE L
  WHERE L.INVOICE_TRUTH_ID = :truthId
    AND (L.THIS_VERIFER_NUMBER IS NULL OR L.THIS_VERIFER_NUMBER < 0);
  ```

#### 报错11：请选择出库明细
- **触发条件**：详情页核对出库单与发票时，未选择出库单列表行（outStockListTableDs.selected为空）
- **逻辑分析**：前端getInvoiceList校验outStockListTableDs.selected.length > 0。若为空则提示"请选择出库明细!"。核对前必须先选择出库单行。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 检查出库单列表是否已选择
  -- SELECT COUNT(*) FROM EPM_INVOICE_TRUTH_LINE WHERE INVOICE_TRUTH_ID = :truthId;
  ```

#### 报错12：请选择发票信息
- **触发条件**：详情页核对出库单与发票时，未选择发票列表行（invoiceListTableDs.selected为空）
- **逻辑分析**：前端getInvoiceList校验invoiceListTableDs.selected.length > 0。若为空则提示"请选择发票信息!"。核对前必须先选择发票行。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  -- 检查发票列表是否已选择
  -- SELECT COUNT(*) FROM EPM_VERIFER_INVOICE_DETAILS WHERE INVOICE_TRUTH_ID = :truthId;
  ```

#### 报错13：本次核销数量不能大于剩余可核销数量
- **触发条件**：保存或提交时，核销明细行的本次核销数量（thisVeriferNumber）超过剩余可核销数量（surplusCanVeriferNumber或convertSurVeriferNumber）
- **逻辑分析**：前端handleVeriferChange校验value > convertSurVeriferNumber时提示"本次核销数量超过了转化后剩余可核销数量"。后端EpmInvoiceTruthHeaderServiceImpl也校验"本次核销数量不能大于剩余可核销数量"和"本次核销数量不能大于可核销数量"。
- **排查SQL**：
  ```sql
  SELECT L.INVOICE_TRUTH_ID AS 核销ID, L.ITEM_CODE AS 产品编码,
         L.THIS_VERIFER_NUMBER AS 本次核销数量,
         L.SURPLUS_CAN_VERIFER_NUMBER AS 剩余可核销数量,
         L.CONVERT_SUR_VERIFER_NUMBER AS 转化后剩余可核销数量
  FROM EPM_INVOICE_TRUTH_LINE L
  WHERE L.INVOICE_TRUTH_ID = :truthId
    AND L.THIS_VERIFER_NUMBER > NVL(L.CONVERT_SUR_VERIFER_NUMBER, L.SURPLUS_CAN_VERIFER_NUMBER);
  ```

#### 报错14：核销出库单明细不可为空
- **触发条件**：详情页提交时，核销出库单明细（epmInvoiceTruthLines）为空
- **逻辑分析**：后端EpmInvoiceTruthHeaderServiceImpl校验核销出库单明细非空，若为空则抛出"核销出库单明细不可为空"。提交前必须至少维护一条核销明细。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.HZ_APPROVE_STATUS AS 审核状态,
         (SELECT COUNT(*) FROM EPM_INVOICE_TRUTH_LINE L
          WHERE L.INVOICE_TRUTH_ID = H.INVOICE_TRUTH_ID) AS 核销明细数量
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  ```

#### 报错15：核销明细状态异常
- **触发条件**：取消核销操作时，核销明细状态不一致或已变更
- **逻辑分析**：后端EpmInvoiceTruthHeaderServiceImpl在取消核销时校验明细状态，若状态异常则抛出"核销明细状态异常,请刷新数据后重试"。通常由并发操作或页面缓存导致状态不同步。
- **排查SQL**：
  ```sql
  SELECT L.INVOICE_TRUTH_ID AS 核销ID, L.INV_OUT_BILL_LINE_ID AS 出库单行ID,
         L.ITEM_CODE AS 产品编码, L.EFFECT_STATUS AS 生效状态,
         L.CANCEL_TYPE AS 取消类型, L.CANCEL_OPERATOR AS 取消人
  FROM EPM_INVOICE_TRUTH_LINE L
  WHERE L.INVOICE_TRUTH_ID = :truthId
  ORDER BY L.INV_OUT_BILL_LINE_ID;
  ```

#### 报错16：不支持当前操作
- **触发条件**：对单据执行终止、取消或作废操作时，单据当前状态不支持该操作
- **逻辑分析**：后端EpmInvoiceTruthHeaderServiceImpl和InvoiceCancelServiceImpl校验单据状态与操作类型匹配。若不匹配则抛出"不支持X操作"或"当前单据不支持当前操作"。如已审核单据不允许终止。
- **排查SQL**：
  ```sql
  SELECT H.INVOICE_TRUTH_NO AS 核销单号, H.HZ_APPROVE_STATUS AS 审核状态
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.INVOICE_TRUTH_ID = :truthId;
  ```

#### 报错17：暂无数据
- **触发条件**：列表页查询时，查询条件无匹配的真实性核销记录
- **逻辑分析**：前端列表DataSet查询返回空集合时框架提示"暂无数据"。通常由查询条件过严、数据未生成或权限不足导致。
- **排查SQL**：
  ```sql
  SELECT COUNT(*) AS 记录数
  FROM EPM_INVOICE_TRUTH_HEADERS H
  WHERE H.HZ_APPROVE_STATUS = NVL(:status, H.HZ_APPROVE_STATUS)
    AND H.PROJECT_CODE LIKE NVL(:projectCode, '%')
    AND H.CREATION_DATE BETWEEN NVL(:dateFrom, H.CREATION_DATE) AND NVL(:dateTo, H.CREATION_DATE);
  ```

#### 报错18：会话过期
- **触发条件**：任意操作时，用户登录态失效（token过期或被踢出）
- **逻辑分析**：前端请求拦截器检测到响应401状态码时提示"会话过期"并跳转登录页。通常由长时间未操作、多端登录或服务端session过期导致。
- **排查SQL**：
  ```sql
  -- 会话过期为前端拦截，无对应数据查询
  -- 可检查用户最近登录时间
  SELECT U.USER_ID AS 用户ID, U.LOGIN_NAME AS 登录名,
         U.LAST_LOGIN_DATE AS 最近登录时间
  FROM HPFM_USER U
  WHERE U.USER_ID = :userId;
  ```

### 常见问题

- 问题1：核对页面出库数量与发票数量不一致
  - 原因：出库单和发票的明细不匹配
  - 解决思路：检查出库单和发票明细，确认数据正确性

- 问题2：取消/作废后能否恢复
  - 原因：取消/作废操作不可逆
  - 解决思路：作废后需重新新建核销单

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-29 | - | - | 按skill规范完整重写，基于前后端代码梳理 |
