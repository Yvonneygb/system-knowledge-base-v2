# 财务管理-对账单-额度外预算总额

## 业务流程

### 业务流程图

```text
额度外预算总额页面(hlod低代码页面)
        │
        ├── 查询预算总额 ──→ 查询MKT_OUTLIMIT_BUD_HEADER表
        │                      （按年度/经销商/事业部/交易公司查询）
        │
        └── 查询额度外可用余额 ──→ POST /fin-fee-apply-headers/query-amt
                                    └── 汇总额度外费用申请单已审批未冲销金额
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 预算导入 | 数据依赖 | 额度外预算由预算导入形成 | 预算已导入 |
| 费用兑现 | 数据依赖 | 费用兑现驱动已使用金额变化 | 兑现单已审批 |
| 费用申请 | 数据依赖 | 可用余额汇总额度外费用申请单 | 申请单已审批 |

### 下游影响

- 影响1：额度外兑现
  - 预算总额控制额度外兑现的上限，兑现时校验预算剩余

- 影响2：预算管控
  - 预算总额与已使用情况支撑预算管控决策

---

## 重点逻辑

### 重点逻辑1：预算总额与使用明细 {预算管理}

- **业务意义**：按年度/经销商展示额度外预算总额及各月使用明细，支撑预算管控

- **具体逻辑描述**

  - 第1点：展示上年结转预算总额、已用、剩余

  - 第2点：展示本年预算总额（含税/不含税）、预算调整

  - 第3点：按月展示1-12月已使用金额（thisOutlimitBudUsed1~12）

  - 第4点：展示累计已用金额和预算剩余

### 重点逻辑2：可用余额实时计算 {余额计算}

- **业务意义**：实时汇总额度外可用余额，支撑兑现决策

- **具体逻辑描述**

  - 第1点：通过query-amt接口汇总额度外费用申请单已审批未冲销金额

  - 第2点：可用余额=预算总额-累计已用金额

### 重点逻辑3：预算同步 {数据同步}

- **业务意义**：额度外兑现时自动同步使用金额到预算表

- **具体逻辑描述**

  - 第1点：FinFeeCashoutHeaderServiceImpl在兑现审批通过时调用MktOutlimitBudHeaderRepository.selectByBudYear查询预算

  - 第2点：同步额度外的数据到额度外预算模块中，更新对应月份的已使用金额

---

## 详细逻辑

### 界面模块1：额度外预算总额查询列表页（hlod低代码页面）

> 本页面为hlod低代码页面，无独立前端源码，基于后端Entity和API梳理。无独立Controller，数据通过FinFeeApplyHeaderController的query-amt接口查询。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 预算单号 | MKT_OUTLIMIT_BUD_HEADER.OUTLIMIT_BUD_ID_NO | 文本框 | 预算单号 | 常显 | 系统生成 |
| 预算年度 | MKT_OUTLIMIT_BUD_HEADER.BUD_YEAR | 文本框 | 预算年度 | 常显 | 手动选择 |
| 事业部 | MKT_OUTLIMIT_BUD_HEADER.ENTNAME | 文本框 | 事业部名称 | 常显 | 选择事业部带出 |
| 经销商编码 | MKT_OUTLIMIT_BUD_HEADER.CUSTOMER_CODE | 文本框 | 经销商编码 | 常显 | 选择经销商带出 |
| 经销商名称 | MKT_OUTLIMIT_BUD_HEADER.CUSTOMER_NAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 交易公司 | MKT_OUTLIMIT_BUD_HEADER.TRADING_COMPANY_NAME | 文本框 | 交易公司名称 | 常显 | 选择交易公司带出 |
| 开票单位 | MKT_OUTLIMIT_BUD_HEADER.BILLING_UNIT_NAME | 文本框 | 开票单位名称 | 常显 | 选择开票单位带出 |
| 门店编码 | MKT_OUTLIMIT_BUD_HEADER.TERMINAL_CODE | 文本框 | 门店编码 | 常显 | 选择门店带出 |
| 门店名称 | MKT_OUTLIMIT_BUD_HEADER.TERMINAL_NAME | 文本框 | 门店名称 | 常显 | 选择门店带出 |
| 上年结转预算总额 | MKT_OUTLIMIT_BUD_HEADER.LAST_OUTLIMIT_BUD_TOTAL | 数字显示框 | 上年结转预算 | 常显 | 系统带出 |
| 上年结转已用 | MKT_OUTLIMIT_BUD_HEADER.LAST_OUTLIMIT_BUD_USED | 数字显示框 | 上年结转已使用 | 常显 | 系统计算 |
| 上年结转剩余 | MKT_OUTLIMIT_BUD_HEADER.LAST_OUTLIMIT_BUD_SUR | 数字显示框 | 上年结转剩余 | 常显 | 系统计算=总额-已用 |
| 本年预算总额 | MKT_OUTLIMIT_BUD_HEADER.OUTLIMIT_BUD_TOTAL | 数字显示框 | 本年预算总额(含税) | 常显 | 预算导入 |
| 本年预算(不含税) | MKT_OUTLIMIT_BUD_HEADER.NOTAX_OUTLIMIT_BUD_TOTAL | 数字显示框 | 本年预算总额(不含税) | 常显 | 系统计算=含税总额/(1+税率) |
| 预算调整 | MKT_OUTLIMIT_BUD_HEADER.OUTLIMIT_BUD_ADJ | 数字显示框 | 预算调整金额 | 常显 | 调整单审批后更新 |
| 1月已用 | MKT_OUTLIMIT_BUD_HEADER.THIS_OUTLIMIT_BUD_USED_1 | 数字显示框 | 1月已使用金额 | 常显 | 兑现同步更新 |
| 2月已用 | MKT_OUTLIMIT_BUD_HEADER.THIS_OUTLIMIT_BUD_USED_2 | 数字显示框 | 2月已使用金额 | 常显 | 兑现同步更新 |
| ... | ... | ... | 3-11月已使用 | 常显 | 兑现同步更新 |
| 12月已用 | MKT_OUTLIMIT_BUD_HEADER.THIS_OUTLIMIT_BUD_USED_12 | 数字显示框 | 12月已使用金额 | 常显 | 兑现同步更新 |
| 累计已用 | MKT_OUTLIMIT_BUD_HEADER.TOTAL_OUTLIMIT_BUD_USED | 数字显示框 | 累计已使用金额 | 常显 | 系统计算=1~12月之和 |
| 预算剩余 | MKT_OUTLIMIT_BUD_HEADER.OUTLIMIT_BUD_SUR | 数字显示框 | 预算剩余 | 常显 | 系统计算=总额+调整-累计已用 |
| 税率 | MKT_OUTLIMIT_BUD_HEADER.TAX_RATE | 数字显示框 | 税率 | 常显 | 系统带出 |

### 选择弹窗

#### 弹窗1：经销商选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| customerCode | 经销商编码 | 经销商编码 | "C001" | 当前用户有权限的经销商 |

> 查询SQL（经销商主数据）：

```sql
SELECT CUSTOMER_CODE, CUSTOMER_NAME FROM CUSTOMER_ORG WHERE ENABLED = 1
```

### 导入

> 本页面为查询页面，无导入功能。预算数据通过预算导入功能维护。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 查询 | 按条件查询预算总额 | 列表页 | 始终可用 | 查询MKT_OUTLIMIT_BUD_HEADER表 |
| 查询可用余额 | 查询额度外可用余额 | 列表页 | 选中记录 | 调用query-amt接口汇总 |

#### 按钮1：查询（列表页）

- **触发条件**：始终可用
- **执行逻辑**：
  - 第1点：按年度、经销商、事业部、交易公司等条件查询额度外预算总额
  - 第2点：返回预算总额、各月使用明细、累计已用、预算剩余
- **接口调用**：查询MKT_OUTLIMIT_BUD_HEADER表（通过FinFeeApplyHeaderController间接查询）
- **排查SQL**：

```sql
SELECT * FROM MKT_OUTLIMIT_BUD_HEADER 
WHERE BUD_YEAR = #{budYear}
  AND (CUSTOMER_CODE = #{customerCode} OR #{customerCode} IS NULL)
  AND (ENTID = #{entid} OR #{entid} IS NULL)
ORDER BY CUSTOMER_CODE, BUD_YEAR
```

#### 按钮2：查询可用余额（列表页）

- **触发条件**：选中记录
- **执行逻辑**：
  - 第1点：汇总额度外费用申请单已审批未冲销金额
  - 第2点：返回可用余额
- **接口调用**：POST `/v1/{organizationId}/fin-fee-apply-headers/query-amt`
- **排查SQL**：

```sql
-- 汇总额度外费用申请单已审批未冲销金额
SELECT SUM(APPLY_AMT - CASHOUT_AMT) AS AVAILABLE_AMT
FROM FIN_FEE_APPLY_HEADER 
WHERE CUSTOMER_CODE = #{customerCode}
  AND BUD_YEAR = #{budYear}
  AND HZ_APPROVE_STATUS = 'APPROVED'
  AND CASHOUT_AMT < APPLY_AMT;
```

### 保存校验

> 本页面为纯查询页面，无保存操作。

### 提交校验

> 本页面为纯查询页面，无提交操作。

### 状态机

> 本页面为纯查询页面，无状态流转。预算数据由预算导入和兑现同步维护。

---

## 数据库表详解

### 表1：MKT_OUTLIMIT_BUD_HEADER（额度外预算总额表）

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| OUTLIMIT_BUD_ID | BIGINT | 主键ID | - | 自增主键 |
| OUTLIMIT_BUD_ID_NO | VARCHAR | 预算单号 | 预算单号 | 系统生成 |
| BUD_YEAR | VARCHAR | 预算年度 | 预算年度 | 手动选择 |
| DIVISION_ID | BIGINT | 事业部词汇值 | - | 事业部词汇值 |
| DIVISION_NAME | VARCHAR | 事业部名称 | - | 事业部名称 |
| ENTID | BIGINT | 事业部ID | - | 选择事业部带出 |
| ENTNAME | VARCHAR | 事业部名称 | 事业部 | 选择事业部带出 |
| CUSTOMER_ID | BIGINT | 经销商ID | - | 选择经销商带出 |
| CUSTOMER_CODE | VARCHAR | 经销商编码 | 经销商编码 | 选择经销商带出 |
| CUSTOMER_NAME | VARCHAR | 经销商名称 | 经销商名称 | 选择经销商带出 |
| SHORT_NAME | VARCHAR | 经销商简称 | - | 选择经销商带出 |
| TRADING_COMPANY_ID | BIGINT | 交易公司ID | - | 选择交易公司带出 |
| TRADING_COMPANY_NAME | VARCHAR | 交易公司名称 | 交易公司 | 选择交易公司带出 |
| TRADING_COMPANY_CODE | VARCHAR | 交易公司编码 | - | 选择交易公司带出 |
| BILLING_UNIT_ID | BIGINT | 开票单位ID | - | 选择开票单位带出 |
| BILLING_UNIT_CODE | VARCHAR | 开票单位编码 | - | 选择开票单位带出 |
| BILLING_UNIT_NAME | VARCHAR | 开票单位名称 | 开票单位 | 选择开票单位带出 |
| TERMINAL_ID | BIGINT | 门店ID | - | 选择门店带出 |
| TERMINAL_CODE | VARCHAR | 门店编码 | 门店编码 | 选择门店带出 |
| TERMINAL_NAME | VARCHAR | 门店名称 | 门店名称 | 选择门店带出 |
| CITY_AREAID | BIGINT | 城市区域ID | - | 系统带出 |
| CITY_AREANAME | VARCHAR | 城市区域名称 | - | 系统带出 |
| ADDR | VARCHAR | 地址 | - | 系统带出 |
| TERMINAL_AREA | DECIMAL | 门店面积 | - | 系统带出 |
| LAST_OUTLIMIT_BUD_TOTAL | DECIMAL | 上年结转预算总额 | 上年结转预算总额 | 系统带出 |
| LAST_OUTLIMIT_BUD_USED | DECIMAL | 上年结转已使用 | 上年结转已用 | 系统计算 |
| LAST_OUTLIMIT_BUD_SUR | DECIMAL | 上年结转剩余 | 上年结转剩余 | 系统计算=总额-已用 |
| OUTLIMIT_BUD_TOTAL | DECIMAL | 本年预算总额(含税) | 本年预算总额 | 预算导入 |
| NOTAX_OUTLIMIT_BUD_TOTAL | DECIMAL | 本年预算总额(不含税) | 本年预算(不含税) | 系统计算=含税/(1+税率) |
| OUTLIMIT_BUD_ADJ | DECIMAL | 预算调整金额 | 预算调整 | 调整单审批后更新 |
| OUTLIMIT_BUD_ADJ_NO | VARCHAR | 预算调整单号 | - | 关联调整单 |
| THIS_OUTLIMIT_BUD_USED_1 | DECIMAL | 1月已使用金额 | 1月已用 | 兑现同步更新 |
| THIS_OUTLIMIT_BUD_USED_2 | DECIMAL | 2月已使用金额 | 2月已用 | 兑现同步更新 |
| THIS_OUTLIMIT_BUD_USED_3~11 | DECIMAL | 3-11月已使用金额 | ... | 兑现同步更新 |
| THIS_OUTLIMIT_BUD_USED_12 | DECIMAL | 12月已使用金额 | 12月已用 | 兑现同步更新 |
| TOTAL_OUTLIMIT_BUD_USED | DECIMAL | 累计已使用金额 | 累计已用 | 系统计算=1~12月之和 |
| OUTLIMIT_BUD_SUR | DECIMAL | 预算剩余 | 预算剩余 | 系统计算=总额+调整-累计已用 |
| TAX_RATE | DECIMAL | 税率 | 税率 | 系统带出 |
| IMPORT_FLAG | VARCHAR | 导入标识 | - | 预算导入时设置 |
| NEXT_OUTLIMIT_BUD_USED_1~12 | DECIMAL | 下年1-12月已使用 | - | 兑现同步更新 |
| NOW_TOTAL_OUTLIMIT_BUD_USED | DECIMAL | 当前累计已使用 | - | 系统计算 |
| SUR_WRITEOFF_AMT | DECIMAL | 剩余可核销金额 | - | 系统计算 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 查询无数据 | 查询时 | 预算未导入或年度选择错误，确认预算已导入 | toast提醒 | [查看] |
| 可用余额查询失败 | 查询可用余额时 | 费用申请单数据异常或接口超时，确认fin-fee-apply-headers服务正常 | toast提醒 | [查看] |
| 预算剩余为负数 | 查询时 | 累计已用超过预算总额+调整，存在超支，核查兑现同步逻辑 | toast提醒 | [查看] |
| 年度查询条件为空 | 查询时 | 未选择预算年度，选择后查询 | toast提醒 | [查看] |
| 网络请求失败 | 查询/查询可用余额时 | 后端服务不可用，确认ae-business服务正常 | toast提醒 | [查看] |
| 权限不足 | 进入页面时 | 当前用户无经销商/事业部数据权限，联系管理员分配权限 | 阻断性报错 | [查看] |

#### 报错1：查询无数据
- **触发条件**：用户按年度/经销商/事业部/交易公司查询额度外预算总额，MKT_OUTLIMIT_BUD_HEADER表返回空结果集
- **逻辑分析**：预算总额数据由"预算导入"功能写入MKT_OUTLIMIT_BUD_HEADER表，IMPORT_FLAG标记导入来源。无数据根因有三类：(1)该年度（BUD_YEAR）预算从未导入，头表无对应记录；(2)用户选择的年度与预算实际导入年度不符（如选2025但只导入了2024）；(3)经销商/事业部/交易公司组合条件与预算记录不匹配。需先确认预算是否已导入，再核对年度和经销商维度
- **排查SQL**：
  ```sql
  SELECT OUTLIMIT_BUD_ID_NO, BUD_YEAR, ENTNAME, CUSTOMER_CODE, CUSTOMER_NAME,
         TRADING_COMPANY_NAME, OUTLIMIT_BUD_TOTAL, OUTLIMIT_BUD_ADJ, TOTAL_OUTLIMIT_BUD_USED,
         OUTLIMIT_BUD_SUR, IMPORT_FLAG
  FROM MKT_OUTLIMIT_BUD_HEADER
  WHERE BUD_YEAR = #{budYear}
    AND (CUSTOMER_CODE = #{customerCode} OR #{customerCode} IS NULL)
    AND (ENTID = #{entid} OR #{entid} IS NULL)
  ORDER BY CUSTOMER_CODE, BUD_YEAR;
  ```

#### 报错2：可用余额查询失败
- **触发条件**：用户选中预算记录后点击"查询可用余额"按钮，POST /v1/{organizationId}/fin-fee-apply-headers/query-amt接口执行失败
- **逻辑分析**：可用余额通过汇总额度外费用申请单（FIN_FEE_APPLY_HEADER）已审批未冲销金额计算，公式为SUM(APPLY_AMT - CASHOUT_AMT)，条件为HZ_APPROVE_STATUS='APPROVED'且CASHOUT_AMT < APPLY_AMT。失败根因有三类：(1)FIN_FEE_APPLY_HEADER表数据量过大，汇总超时；(2)费用申请单状态异常（HZ_APPROVE_STATUS字段值不规范）；(3)FinFeeApplyHeaderController服务异常。需确认费用申请单数据正常后重试
- **排查SQL**：
  ```sql
  -- 核查费用申请单汇总数据
  SELECT COUNT(*) AS 申请单数, SUM(APPLY_AMT - CASHOUT_AMT) AS 可用余额合计
  FROM FIN_FEE_APPLY_HEADER
  WHERE CUSTOMER_CODE = #{customerCode}
    AND BUD_YEAR = #{budYear}
    AND HZ_APPROVE_STATUS = 'APPROVED'
    AND CASHOUT_AMT < APPLY_AMT;
  ```

#### 报错3：预算剩余为负数
- **触发条件**：用户查询预算总额，返回的OUTLIMIT_BUD_SUR字段值小于0
- **逻辑分析**：预算剩余计算公式为OUTLIMIT_BUD_SUR = OUTLIMIT_BUD_TOTAL + OUTLIMIT_BUD_ADJ - TOTAL_OUTLIMIT_BUD_USED，其中TOTAL_OUTLIMIT_BUD_USED为1~12月已使用金额之和（THIS_OUTLIMIT_BUD_USED_1~12）。负数意味着累计已用金额超过预算总额+调整，存在超支。根因有二：(1)额度外兑现审批通过后同步更新预算表时未校验剩余预算，导致超额兑现；(2)预算调整（OUTLIMIT_BUD_ADJ）未及时同步。需核查兑现同步逻辑及调整单审批回写
- **排查SQL**：
  ```sql
  SELECT OUTLIMIT_BUD_ID_NO, BUD_YEAR, CUSTOMER_CODE,
         OUTLIMIT_BUD_TOTAL AS 预算总额, OUTLIMIT_BUD_ADJ AS 预算调整,
         TOTAL_OUTLIMIT_BUD_USED AS 累计已用, OUTLIMIT_BUD_SUR AS 预算剩余,
         THIS_OUTLIMIT_BUD_USED_1 AS 一月, THIS_OUTLIMIT_BUD_USED_2 AS 二月,
         THIS_OUTLIMIT_BUD_USED_3 AS 三月, THIS_OUTLIMIT_BUD_USED_4 AS 四月,
         THIS_OUTLIMIT_BUD_USED_5 AS 五月, THIS_OUTLIMIT_BUD_USED_6 AS 六月,
         THIS_OUTLIMIT_BUD_USED_7 AS 七月, THIS_OUTLIMIT_BUD_USED_8 AS 八月,
         THIS_OUTLIMIT_BUD_USED_9 AS 九月, THIS_OUTLIMIT_BUD_USED_10 AS 十月,
         THIS_OUTLIMIT_BUD_USED_11 AS 十一月, THIS_OUTLIMIT_BUD_USED_12 AS 十二月
  FROM MKT_OUTLIMIT_BUD_HEADER
  WHERE OUTLIMIT_BUD_SUR < 0
    AND BUD_YEAR = #{budYear};
  ```

#### 报错4：年度查询条件为空
- **触发条件**：用户未选择预算年度直接点击查询
- **逻辑分析**：预算年度（BUD_YEAR）是查询额度外预算总额的关键条件，预算数据按年度划分。未选择年度将导致跨年度全表查询，可能因数据量过大引起超时或返回无关数据。低代码页面查询栏配置年度为建议必填条件，未填写时toast提示后阻断查询
- **排查SQL**：
  ```sql
  -- 核查各年度数据分布
  SELECT BUD_YEAR, COUNT(*) AS 记录数
  FROM MKT_OUTLIMIT_BUD_HEADER
  GROUP BY BUD_YEAR
  ORDER BY BUD_YEAR;
  ```

#### 报错5：网络请求失败
- **触发条件**：用户点击查询或查询可用余额按钮，前端调用对应接口返回非2xx状态码或超时
- **逻辑分析**：本页面为hlod低代码页面，数据通过后端FinFeeApplyHeaderController的query-amt接口及MKT_OUTLIMIT_BUD_HEADER表查询提供。网络请求失败根因有四类：(1)ae-business服务未启动或宕机；(2)数据库连接异常；(3)query-amt接口汇总FIN_FEE_APPLY_HEADER表超时；(4)网关或网络层故障。需先确认ae-business服务健康状态
- **排查SQL**：
  ```sql
  -- 核查预算表数据量
  SELECT BUD_YEAR, COUNT(*) AS 记录数
  FROM MKT_OUTLIMIT_BUD_HEADER
  GROUP BY BUD_YEAR;
  ```

#### 报错6：权限不足
- **触发条件**：用户登录后进入额度外预算总额页面，当前用户无对应经销商或事业部的数据权限
- **逻辑分析**：本页面按经销商和事业部维度查询，数据权限通过用户上下文CustomUserDetails的additionInfo控制可见经销商范围。权限不足根因有二：(1)用户未分配对应经销商的数据权限，查询时自动过滤导致空结果；(2)用户未分配菜单访问权限，页面入口不可见。需联系管理员在权限系统中分配对应经销商/事业部数据权限
- **排查SQL**：
  ```sql
  -- 核查用户是否有该经销商的数据权限
  SELECT USER_ID, USER_NAME, CUSTOMER_CODE, ENABLED
  FROM USER_CUSTOMER_AUTH
  WHERE USER_ID = #{userId} AND CUSTOMER_CODE = #{customerCode};
  ```

### 常见问题

- 问题1：预算剩余为负数
  - 原因：累计已用金额超过预算总额+调整，存在超支
  - 解决思路：检查SQL `SELECT OUTLIMIT_BUD_TOTAL, OUTLIMIT_BUD_ADJ, TOTAL_OUTLIMIT_BUD_USED, OUTLIMIT_BUD_SUR FROM MKT_OUTLIMIT_BUD_HEADER WHERE CUSTOMER_CODE = #{customerCode} AND BUD_YEAR = #{budYear}`

- 问题2：各月已用金额未更新
  - 原因：额度外兑现审批通过后未同步到预算表
  - 解决思路：检查兑现审批回调逻辑，确认FinFeeCashoutHeaderServiceImpl是否正确调用selectByBudYear

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，补充界面模块、选择弹窗、数据库表详解 |
| 2026-03-03 | - | - | 初始创建MktOutlimitBudHeader实体 |
