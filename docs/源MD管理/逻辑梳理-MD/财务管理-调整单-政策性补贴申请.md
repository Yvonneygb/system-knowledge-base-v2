# 财务管理-调整单-政策性补贴申请

> **注意**：本菜单与"财务管理-对账单-政策性补贴申请"共用同一套后端代码（SaPolicySpecialHeaderController/SaPolicySpecialHeader），是同一功能的不同菜单入口。以下内容与对账单入口一致。

## 业务流程

### 业务流程图

```text
政策性补贴申请页面(arrow-ae标准React页面)
        │
        ├── 查询列表 ──→ GET /sa-policy-special-headers/search
        │
        ├── 新建/编辑 ──→ POST /sa-policy-special-headers/save 或 /save-policy-special
        │
        ├── 保存并提交 ──→ POST /sa-policy-special-headers/save-and-submit
        │              └── 发起OA审批流程(SA_POLICY_SPECIAL_MCS_AW)
        │                    ├── 审批通过 ──→ 更新审核状态为APPROVED
        │                    └── 审批拒绝 ──→ 更新审核状态为REJECTED
        │
        ├── 删除 ──→ POST /sa-policy-special-headers/remove
        ├── 查询账户 ──→ GET /sa-policy-special-headers/select-account
        ├── 查询币种 ──→ GET /sa-policy-special-headers/get-currency
        └── 查询资金池 ──→ GET /sa-policy-special-headers/select-capital
```

### 上游依赖

| 上游模块 | 依赖类型 | 依赖说明 | 依赖成立条件 |
|---------|---------|---------|------------|
| 经销商主数据 | 数据依赖 | 申请关联经销商信息 | 经销商已创建 |
| 交易公司配置 | 配置依赖 | 申请关联交易公司 | 交易公司已配置 |
| 资金池 | 数据依赖 | 申请关联资金池余额 | 资金池已建立 |
| OA审批系统 | 服务依赖 | 提交后发起OA审批流程 | OA系统可用 |

### 下游影响

- 影响1：资金池余额
  - 审批通过后扣减资金池余额

- 影响2：财务对账
  - 政策性补贴申请审批通过后影响财务对账数据

---

## 重点逻辑

### 重点逻辑1：OA审批流程 {审批流转}

- **业务意义**：政策性补贴申请需经OA审批，确保补贴发放合规

- **具体逻辑描述**

  - 第1点：保存并提交时通过workFlowStart发起OA审批流程(SA_POLICY_SPECIAL_MCS_AW)

  - 第2点：OA审批通过后通过wfComplete回调更新审核状态为APPROVED

  - 第3点：OA审批拒绝后更新审核状态为REJECTED

  - 第4点：审批状态流转：NEW→RUN→APPROVED/REJECTED

### 重点逻辑2：资金池关联 {余额校验}

- **业务意义**：申请金额需关联资金池余额，确保补贴有充足资金来源

- **具体逻辑描述**

  - 第1点：通过select-capital接口查询资金池余额

  - 第2点：通过select-account接口查询余额账户信息

  - 第3点：申请金额不能超过资金池可用余额

---

## 详细逻辑

### 界面模块1：政策性补贴申请列表页（arrow-ae标准React页面）

> 前端源码位于 `packages/arrow-ae/src/pages/contractPolicy/saPolicySpecial/`，路由：`/sa-policy-special/list`。与菜单97共用同一套代码。

| 字段名 | 数据库列名 | 组件 | 业务释义 | 显隐条件 | 取值/赋值逻辑 |
|-------|-----------|------|---------|---------|-------------|
| 补贴单号 | SA_POLICY_SPECIAL_HEADER.POLICY_SPECIAL_NO | 文本框 | 政策性补贴申请单号 | 常显 | 自动生成 |
| 申请人 | SA_POLICY_SPECIAL_HEADER.CREATOR | 文本框 | 申请人 | 常显 | 自动带出当前用户 |
| 申请时间 | SA_POLICY_SPECIAL_HEADER.CREATE_TIME | 日期选择框 | 申请时间 | 常显 | 自动带出当前时间 |
| 经销商 | SA_POLICY_SPECIAL_HEADER.CUST_NAME | 文本框 | 经销商名称 | 常显 | 选择经销商带出 |
| 交易公司 | SA_POLICY_SPECIAL_HEADER.TRADING_COMPANY_NAME | 文本框 | 交易公司名称 | 常显 | 选择交易公司带出 |
| 法人 | SA_POLICY_SPECIAL_HEADER.LEGAL_ENTITY_NAME | 文本框 | 法人名称 | 常显 | 根据交易公司带出 |
| 申请金额 | SA_POLICY_SPECIAL_HEADER.APPLY_AMT | 数字输入框 | 申请补贴金额 | 常显 | 手动输入 |
| 审核金额 | SA_POLICY_SPECIAL_HEADER.AMOUNT | 数字显示框 | 审核通过金额 | 审核后显示 | 审批通过后带出 |
| OA审核状态 | SA_POLICY_SPECIAL_HEADER.AUDIT_STAT | 文本框 | OA审核状态 | 常显 | 流程回调更新 |
| 备注 | SA_POLICY_SPECIAL_HEADER.NOTE | 文本框 | 备注 | 常显 | 手动输入 |

### 选择弹窗

#### 弹窗1：经销商选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| custCode | 经销商编码 | 经销商编码 | "C001" | 当前用户有权限的经销商 |

> 查询SQL（经销商主数据）：

```sql
SELECT CUST_ID, CUST_CODE, CUST_NAME, SHORT_NAME FROM CUSTOMER_ORG WHERE ENABLED = 1
```

#### 弹窗2：余额账户选择（单选）

| 入参 | | | | 数据范围 |
|------|------|------|------|---------|
| 字段名 | 中文名 | 释义 | 示例 | |
| tradingCompanyId | 交易公司ID | 交易公司ID | 2001 | 该交易公司下的余额账户 |

> 查询SQL（后端接口select-account）：

```sql
SELECT ACCOUNT_ID, ACCOUNT_NAME, CAPITAL_POOL FROM CAPITAL_ACCOUNT 
WHERE TRADING_COMPANY_ID = #{tradingCompanyId} AND ENABLED = 1
```

### 导入

> 本页面无导入功能。

### 其他按钮

| 按钮名称 | 按钮作用 | 所在位置 | 显隐条件/可点击条件 | 影响 |
|---------|---------|---------|-------------------|------|
| 新增 | 新建政策性补贴申请 | 列表页 | 始终可用 | 打开新建页面 |
| 保存 | 保存申请信息 | 编辑页 | 编辑状态 | 调用save接口 |
| 保存并提交 | 保存并发起OA审批 | 编辑页 | 编辑状态 | 调用save-and-submit接口 |
| 删除 | 删除申请单 | 列表页 | 选中未提交记录 | 调用remove接口 |

#### 按钮1：保存并提交（编辑页）

- **触发条件**：编辑状态
- **执行逻辑**：
  - 第1点：先执行保存校验
  - 第2点：保存数据到SA_POLICY_SPECIAL_HEADER
  - 第3点：通过workFlowStart发起OA审批流程(SA_POLICY_SPECIAL_MCS_AW)
  - 第4点：更新审核状态为RUN
- **接口调用**：POST `/v1/{organizationId}/sa-policy-special-headers/save-and-submit`
- **排查SQL**：

```sql
SELECT * FROM SA_POLICY_SPECIAL_HEADER WHERE POLICY_SPECIAL_ID = #{id} AND AUDIT_STAT = 'RUN';
```

### 保存校验

- 校验1：经销商不能为空 —— 确保申请关联明确经销商

  - 详细逻辑

    - 第1点：保存时校验经销商ID不为空

  - 系统体现：toast提醒

  - 排查SQL：

    ```sql
    SELECT * FROM SA_POLICY_SPECIAL_HEADER WHERE CUST_ID IS NULL;
    ```

- 校验2：申请金额必须大于0 —— 确保申请金额有效

  - 详细逻辑

    - 第1点：保存时校验申请金额大于0

  - 系统体现：toast提醒

  - 排查SQL：

    ```sql
    SELECT * FROM SA_POLICY_SPECIAL_HEADER WHERE APPLY_AMT <= 0;
    ```

### 提交校验

- 校验1：申请金额不超过资金池可用余额 —— 确保有充足资金

  - 详细逻辑

    - 第1点：提交时校验申请金额不超过资金池可用余额

  - 系统体现：阻断性报错

  - 排查SQL：

    ```sql
    SELECT H.POLICY_SPECIAL_ID, H.APPLY_AMT, C.CAPITAL_POOL 
    FROM SA_POLICY_SPECIAL_HEADER H, CAPITAL_ACCOUNT C
    WHERE H.EXT_ACCOUNT_ID = C.ACCOUNT_ID AND H.APPLY_AMT > C.CAPITAL_POOL;
    ```

### 状态机

#### 状态机流转图

```text
新建 ──保存──→ 已保存(NEW) ──保存并提交──→ 审批中(RUN) ──OA审批通过──→ 已审核(APPROVED)
                                              │
                                              └──OA审批拒绝──→ 已拒绝(REJECTED)
```

#### 状态机列表

| 状态机名称 | 状态释义 | 可执行的操作 |
|-----------|---------|------------|
| NEW | 新建/已保存未提交 | 编辑、保存、保存并提交、删除 |
| RUN | 已提交OA审批中 | 无（等待审批结果） |
| APPROVED | OA审批通过 | 查看 |
| REJECTED | OA审批拒绝 | 修改、重新提交 |

---

## 数据库表详解

### 表1：SA_POLICY_SPECIAL_HEADER（政策性补贴申请头表）

> 与菜单97共用同一张表，字段详见"财务管理-对账单-政策性补贴申请"。

| 字段名 | 类型 | 释义 | 对应界面字段 | 逻辑 |
|-------|------|------|------------|------|
| POLICY_SPECIAL_ID | BIGINT | 主键ID | - | 自增主键 |
| POLICY_SPECIAL_NO | VARCHAR | 补贴单号 | 补贴单号 | 自动生成 |
| CREATOR | VARCHAR | 申请人 | 申请人 | 自动带出当前用户 |
| CREATE_TIME | DATETIME | 申请时间 | 申请时间 | 自动带出当前时间 |
| CUST_ID | BIGINT | 经销商ID | - | 选择经销商带出 |
| CUST_CODE | VARCHAR | 经销商编码 | - | 选择经销商带出 |
| CUST_NAME | VARCHAR | 经销商名称 | 经销商 | 选择经销商带出 |
| APPLY_AMT | BIGINT | 申请金额 | 申请金额 | 手动输入 |
| AMOUNT | BIGINT | 审核金额 | 审核金额 | 审批通过后带出 |
| TRADING_COMPANY_ID | BIGINT | 交易公司ID | 交易公司 | 选择交易公司带出 |
| TRADING_COMPANY_NAME | VARCHAR | 交易公司名称 | - | 选择交易公司带出 |
| ACCOUNT_NAME | VARCHAR | 余额账户 | 余额账户 | 选择余额账户带出 |
| CAPITAL_POOL | BIGINT | 资金池余额 | 资金池余额 | 通过select-capital接口查询 |
| LEGAL_ENTITY_NAME | VARCHAR | 法人名称 | 法人 | 根据交易公司带出 |
| AUDIT_STAT | VARCHAR | OA审核状态 | OA审核状态 | 流程回调更新 |
| NOTE | VARCHAR | 备注 | 备注 | 手动输入 |

---

## 常见问题FAQ

### 报错一览表

| 报错信息 | 提示节点 | 根因与解决方案 | 等级 | 详细逻辑 |
|---------|---------|-------------|------|---------|
| 经销商不能为空 | 保存时 | 未选择经销商，选择后保存 | toast提醒 | [查看] |
| 申请金额必须大于0 | 保存时 | 申请金额填写了0或负数 | toast提醒 | [查看] |
| 申请金额超过资金池余额 | 提交时 | 申请金额大于资金池可用余额 | 阻断性报错 | [查看] |
| 资金池来源类型为空 | 导入明细前 | 未选择资金池来源类型，选择后导入 | toast提醒 | [查看] |
| 申请原因为空 | 保存时 | 未填写申请原因，填写后保存 | toast提醒 | [查看] |
| 交易公司不能为空 | 保存时 | 未选择交易公司，选择后保存 | toast提醒 | [查看] |
| 法人不能为空 | 保存时 | 未选择法人，选择后保存 | toast提醒 | [查看] |
| 流程编码缺失，请选择流程！ | 保存并提交时 | 未选择审批流程，选择流程后提交 | 阻断性报错 | [查看] |
| 请先选择经销商或交易公司 | 查询资金池时 | 经销商或交易公司未选择，先选择后再查询资金池 | toast提醒 | [查看] |
| 行数据重复 | 保存时 | 同一经销商+交易公司+法人组合重复，去重后保存 | toast提醒 | [查看] |
| 请选择需要删除的数据！ | 删除时 | 未选中任何记录，选中后删除 | toast提醒 | [查看] |
| 单据未在OA审核节点 | OA审批回调时 | 单据审核状态非"已送签OA"，检查OA回调时序 | 阻断性报错 | [查看] |
| 未查询到业务单据 | OA审批回调时 | OA回传ID在系统中找不到，检查OA单据映射 | 阻断性报错 | [查看] |
| 网络请求失败 | 保存/提交/删除时 | 后端服务不可用或OA系统不可达，确认服务正常 | toast提醒 | [查看] |
| 权限不足 | 进入页面时 | 当前用户无经销商数据权限，联系管理员分配权限 | 阻断性报错 | [查看] |

#### 报错1：经销商不能为空
- **触发条件**：用户在新建/编辑页未选择经销商直接点击保存
- **逻辑分析**：本菜单与菜单97共用SA_POLICY_SPECIAL_HEADER表及SaPolicySpecialHeaderController。保存校验CUST_ID非空，经销商是关联交易公司、法人、余额账户的前置条件。未选择经销商将导致资金池余额校验无执行对象。校验在Controller层前置拦截，toast提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CUST_ID, CUST_NAME, APPLY_AMT, AUDIT_STAT
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE CUST_ID IS NULL OR CUST_NAME IS NULL;
  ```

#### 报错2：申请金额必须大于0
- **触发条件**：用户在申请金额输入框填写0、负数或留空后点击保存
- **逻辑分析**：F：补贴申请金额APPLY_AMT代表实际发放金额，必须为正数。0或负数无业务意义，且审批通过后扣减资金池将出现异常（扣减0或反向增加余额）。校验APPLY_AMT > 0，toast提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CUST_NAME, APPLY_AMT, AUDIT_STAT
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE APPLY_AMT IS NULL OR APPLY_AMT <= 0;
  ```

#### 报错3：申请金额超过资金池余额
- **触发条件**：用户点击"保存并提交"，提交校验发现APPLY_AMT > 资金池可用余额（CAPITAL_POOL）
- **逻辑分析**：提交时通过select-capital接口查询关联余额账户（CAPITAL_ACCOUNT）的CAPITAL_POOL，校验APPLY_AMT <= CAPITAL_POOL。超出余额意味着补贴无充足资金来源，审批通过后扣减资金池将出现负数。此为阻断性报错，阻止OA流程（SA_POLICY_SPECIAL_MCS_AW）发起，需调减申请金额或先补充资金池
- **排查SQL**：
  ```sql
  SELECT H.POLICY_SPECIAL_ID, H.POLICY_SPECIAL_NO, H.CUST_NAME, H.APPLY_AMT,
         C.ACCOUNT_NAME, C.CAPITAL_POOL, (H.APPLY_AMT - C.CAPITAL_POOL) AS 超额金额
  FROM SA_POLICY_SPECIAL_HEADER H
  JOIN CAPITAL_ACCOUNT C ON H.EXT_ACCOUNT_ID = C.ACCOUNT_ID
  WHERE H.APPLY_AMT > C.CAPITAL_POOL
    AND H.AUDIT_STAT IN ('NEW', 'RUN');
  ```

#### 报错4：资金池来源类型为空
- **触发条件**：用户在详情页未选择"资金池来源类型"（capitalType）直接点击导入明细
- **逻辑分析**：前端DetailPage/index.tsx的beforeImportUpload回调中校验headDs.current?.get('capitalType')，若为空则notification.error提示"资金池来源类型为空，请检查！"并返回false阻断导入。资金池来源类型（lookupCode: AE.CAPITAL_TYPE）决定资金池查询的業務类型，HeadDS中配置为required: true。需先选择资金池来源类型再导入明细
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CAPITAL_TYPE, AUDIT_STAT
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE CAPITAL_TYPE IS NULL
    AND AUDIT_STAT IN ('NEW', 'REJECTED');
  ```

#### 报错5：申请原因为空
- **触发条件**：用户未填写"申请原因"（note）直接点击保存
- **逻辑分析**：前端HeadDS.ts中note字段配置为required: true，DataSet提交时自动校验必填。申请原因记录补贴申请的业务背景，是OA审批的重要参考信息。未填写时DataSet校验失败，前端表单标红提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CUST_NAME, NOTE, AUDIT_STAT
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE NOTE IS NULL
    AND AUDIT_STAT IN ('NEW', 'REJECTED');
  ```

#### 报错6：交易公司不能为空
- **触发条件**：用户在明细行未选择交易公司直接点击保存
- **逻辑分析**：前端LineDS.ts中tradingCompanyObj字段配置为required: true，DataSet提交时自动校验必填。交易公司是关联法人、余额账户的前置条件，未选择交易公司将导致资金池查询无执行对象（handleSelectCapital中校验tradingCompanyCode）。校验失败时前端表单标红提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT H.POLICY_SPECIAL_ID, H.POLICY_SPECIAL_NO, L.POLICY_SPECIAL_LINE_ID,
         L.CUST_NAME, L.TRADING_COMPANY_ID, L.TRADING_COMPANY_NAME
  FROM SA_POLICY_SPECIAL_HEADER H
  JOIN SA_POLICY_SPECIAL_LINE L ON H.POLICY_SPECIAL_ID = L.POLICY_SPECIAL_ID
  WHERE L.TRADING_COMPANY_ID IS NULL
    AND H.AUDIT_STAT IN ('NEW', 'REJECTED');
  ```

#### 报错7：法人不能为空
- **触发条件**：用户在明细行未选择法人直接点击保存
- **逻辑分析**：前端LineDS.ts中legalEntityObj字段配置为required: true，DataSet提交时自动校验必填。法人是关联余额账户（CAPITAL_ACCOUNT）和资金池查询的关键字段，select-capital接口入参legalEntityCode即来自法人选择。未选择法人将导致资金池余额无法查询，提交校验无法执行。校验失败时前端表单标红提示后阻断保存
- **排查SQL**：
  ```sql
  SELECT H.POLICY_SPECIAL_ID, H.POLICY_SPECIAL_NO, L.POLICY_SPECIAL_LINE_ID,
         L.CUST_NAME, L.LEGAL_ENTITY_ID, L.LEGAL_ENTITY_CODE
  FROM SA_POLICY_SPECIAL_HEADER H
  JOIN SA_POLICY_SPECIAL_LINE L ON H.POLICY_SPECIAL_ID = L.POLICY_SPECIAL_ID
  WHERE L.LEGAL_ENTITY_ID IS NULL
    AND H.AUDIT_STAT IN ('NEW', 'REJECTED');
  ```

#### 报错8：流程编码缺失，请选择流程！
- **触发条件**：用户点击"保存并提交"按钮，dto.getFlowCode()为空
- **逻辑分析**：saveAndSubmit方法中校验StringUtils.isEmpty(dto.getFlowCode())，若为空则抛出CommonException("流程编码缺失，请选择流程！")。流程编码（flowCode）是发起OA审批流程（SA_POLICY_SPECIAL_MCS_AW）的必要参数，缺失将导致workFlowStart无法启动流程。需在详情页选择审批流程后再次提交
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, HZ_APPROVE_STATUS, HZ_INSTANCE_ID, AUDIT_STAT
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE HZ_APPROVE_STATUS = 'NEW'
    AND HZ_INSTANCE_ID IS NULL;
  ```

#### 报错9：请先选择经销商或交易公司
- **触发条件**：用户在明细行点击"查询资金池"按钮，但custCode或tradingCompanyCode为空
- **逻辑分析**：前端handleSelectCapital回调中校验record?.get('custCode')和record?.get('tradingCompanyCode')，若任一为空则notification.error提示"请先选择经销商 或 交易公司"并return。资金池查询（select-capital接口）需要legalEntityCode和tradingCompanyCode作为入参，二者分别来自经销商和交易公司选择。需先选择经销商和交易公司再查询资金池
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_LINE_ID, CUST_CODE, CUST_NAME, TRADING_COMPANY_CODE, TRADING_COMPANY_NAME
  FROM SA_POLICY_SPECIAL_LINE
  WHERE CUST_CODE IS NULL OR TRADING_COMPANY_CODE IS NULL;
  ```

#### 报错10：行数据重复
- **触发条件**：用户保存时，明细行中存在相同的"经销商编码+交易公司名称+法人编码"组合
- **逻辑分析**：前端DetailPage/index.tsx的校验逻辑中，按custCode + '-' + tradingCompanyName + '-' + legalEntityCode生成唯一键，若重复则notification.error提示"第X行数据重复"并返回false阻断保存。同一申请单中不允许相同经销商+交易公司+法人组合重复，避免资金池重复扣减。需去重后保存
- **排查SQL**：
  ```sql
  SELECT CUST_CODE, TRADING_COMPANY_NAME, LEGAL_ENTITY_CODE, COUNT(*) AS 重复行数
  FROM SA_POLICY_SPECIAL_LINE
  WHERE POLICY_SPECIAL_ID = #{policySpecialId}
  GROUP BY CUST_CODE, TRADING_COMPANY_NAME, LEGAL_ENTITY_CODE
  HAVING COUNT(*) > 1;
  ```

#### 报错11：请选择需要删除的数据！
- **触发条件**：用户未选中任何申请单直接点击"删除"按钮
- **逻辑分析**：remove方法接收cmContractPaymentApplyList列表，若CollectionUtils.isEmpty为true则抛出CommonException("请选择需要删除的数据！")。前端列表页需选中至少一条记录才可触发删除操作。需在列表中勾选目标记录后重试
- **排查SQL**：
  ```sql
  -- 核查可删除的申请单（未提交或已拒绝状态）
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CREATOR, AUDIT_STAT, HZ_APPROVE_STATUS
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE HZ_APPROVE_STATUS IN ('NEW', 'REJECTED')
  ORDER BY CREATE_TIME DESC;
  ```

#### 报错12：单据未在OA审核节点
- **触发条件**：OA审批回调doProcessOA方法时，单据的AUDIT_STAT不等于"已送签OA"
- **逻辑分析**：doProcessOA方法中按policySpecialId查询单据，若saPolicySpecialHeaderVO.getAuditStat()不等于"已送签OA"则抛出CommonException("单据【{}】未在oa审核节点", policySpecialId)。根因有三类：(1)OA回调时序异常，单据尚未推送OA即收到回调；(2)单据已被其他回调处理更新状态；(3)人工修改了AUDIT_STAT字段。需核查OA回调时序及单据状态流转
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, AUDIT_STAT, HZ_APPROVE_STATUS,
         HZ_INSTANCE_ID, CALLBACK_SOURCE
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE POLICY_SPECIAL_ID = #{policySpecialId};
  ```

#### 报错13：未查询到业务单据
- **触发条件**：OA审批回调doProcessOA方法时，按policySpecialId查询SA_POLICY_SPECIAL_HEADER返回空集合
- **逻辑分析**：doProcessOA方法中校验policySpecialId非空且大于0，然后通过saPolicySpecialHeaderRepository.search查询。若返回空集合则抛出CommonException("未查询到业务单据，请检查")。根因有三类：(1)OA回传的ID在系统中不存在（如测试环境OA回调到生产）；(2)申请单已被物理删除；(3)数据权限过滤导致查询不到。需核对OA回传ID与系统POLICY_SPECIAL_ID字段
- **排查SQL**：
  ```sql
  SELECT POLICY_SPECIAL_ID, POLICY_SPECIAL_NO, CUST_NAME, APPLY_AMT,
         AUDIT_STAT, HZ_APPROVE_STATUS
  FROM SA_POLICY_SPECIAL_HEADER
  WHERE POLICY_SPECIAL_ID = #{policySpecialId};
  ```

#### 报错14：网络请求失败
- **触发条件**：用户点击保存/保存并提交/删除按钮，前端调用对应接口返回非2xx状态码或超时
- **逻辑分析**：本页面通过SaPolicySpecialHeaderController提供save/save-and-submit/remove等接口，提交时通过workFlowStart发起OA流程，doOaAudit中通过oaService.toDataOA推送OA系统。网络请求失败根因有四类：(1)ae-business服务未启动或宕机；(2)OA系统不可达，toDataOA调用超时或失败；(3)数据库连接异常；(4)网关或网络层故障。需先确认ae-business服务和OA系统连通性
- **排查SQL**：
  ```sql
  -- 核查申请单状态分布
  SELECT HZ_APPROVE_STATUS, COUNT(*) AS 记录数
  FROM SA_POLICY_SPECIAL_HEADER
  GROUP BY HZ_APPROVE_STATUS;
  ```

#### 报错15：权限不足
- **触发条件**：用户登录后进入政策性补贴申请页面，当前用户无对应经销商的数据权限
- **逻辑分析**：本页面按经销商维度查询，数据权限通过用户上下文CustomUserDetails的additionInfo控制可见经销商范围。前端LineDS.ts中customerObj的lovCode为BASIC_CUSTOM_ORG_LOV_2，通过lovPara的searchFlag: 146过滤当前用户有权限的经销商。权限不足根因有二：(1)用户未分配对应经销商的数据权限，LOV查询返回空；(2)用户未分配菜单访问权限，页面入口不可见。需联系管理员在权限系统中分配对应经销商数据权限
- **排查SQL**：
  ```sql
  -- 核查用户是否有该经销商的数据权限
  SELECT USER_ID, USER_NAME, CUSTOMER_CODE, ENABLED
  FROM USER_CUSTOMER_AUTH
  WHERE USER_ID = #{userId} AND CUSTOMER_CODE = #{customerCode};
  ```

### 常见问题

- 问题1：与菜单97的关系
  - 原因：菜单97"财务管理-对账单-政策性补贴申请"和菜单103"财务管理-调整单-政策性补贴申请"共用同一套代码
  - 解决思路：两个菜单入口指向同一个功能，数据完全共享

---

## 更新记录

| 日期 | 提交ID | 提交人 | 提交内容 |
|------|-------|-------|---------|
| 2026-08-30 | - | AI | 按skill规范重写，标注与菜单97共用代码 |
| 2025-09-17 | - | jiaqiang.fu01 | 初始创建SaPolicySpecialHeader实体 |
