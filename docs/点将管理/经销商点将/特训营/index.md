<BreadcrumbTabs />

<div id="biz-intro" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<!-- 一、业务是什么 -->
<div class="kl-card">
  <div class="biz-kl-hdr">
    <span class="biz-tag" style="background:rgba(124,58,237,0.08);color:#7C3AED;border-color:rgba(124,58,237,0.18);"> 定义</span>
    <h2>经销商侧的特训营是什么</h2>
    <p>经销商查看被点将参加的集中式特训营安排并确认参训</p>
  </div>
  <div class="biz-2col-inner">
    <div class="kl-col-box">
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;">
        <div style="width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#7c3aed,#6d28d9);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="2" y="3" width="5" height="5" rx="1" stroke="white" stroke-width="1.5"/><path d="M3 6.5L4.5 8L7 5" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M9 5H14" stroke="white" stroke-width="1.5" stroke-linecap="round"/><rect x="2" y="10" width="5" height="3" rx="1" stroke="white" stroke-width="1.5"/><path d="M9 10H14" stroke="white" stroke-width="1.5" stroke-linecap="round"/><path d="M9 13H13" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
        <h4 style="font-size:1rem;font-weight:800;color:#1F2937;margin:0;">解决什么问题</h4>
      </div>
      <p style="font-size:0.78rem;font-weight:600;color:#6B7280;margin:0 0 10px;">让经销商及时掌握集中培训的参训要求</p>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F5F3FF;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>特训营是集中培训</strong> — 与到店培训不同，特训营在统一地点集中开办，由总部或大区点派参训对象。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F5F3FF;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>参训需异地安排</strong> — 涉及外出学习，经销商要提前排班、报名人员与行程，信息必须提前可见。</div>
        </div>
      </div>
    </div>
    <div class="kl-col-box alt">
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;">
        <div style="width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#10b981,#059669);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="2" width="10" height="12" rx="1.5" stroke="white" stroke-width="1.5"/><path d="M5 6H11M5 9H9" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
        <h4 style="font-size:1rem;font-weight:800;color:#1F2937;margin:0;">能看到什么</h4>
      </div>
      <p style="font-size:0.78rem;font-weight:600;color:#6B7280;margin:0 0 10px;">一期特训营的关键信息</p>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F0FDF4;border-radius:8px;border:1px solid #D1FAE5;">
          <div style="font-size:.75rem;"><strong>培训主题与时间</strong> — 本期特训营讲什么、什么时候开班结班。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F0FDF4;border-radius:8px;border:1px solid #D1FAE5;">
          <div style="font-size:.75rem;"><strong>培训地点与讲师</strong> — 在哪里集中开办、由哪位讲师授课。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#EDE9FE;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>参训门店与状态</strong> — 本商户哪些门店被点将，以及点将当前状态。</div>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- 二、流程 -->
<div class="kl-card">
  <div class="biz-kl-hdr">
    <span class="biz-tag" style="background:rgba(124,58,237,0.08);color:#7C3AED;border-color:rgba(124,58,237,0.18);"> 流程</span>
    <h2>经销商侧的处理链路</h2>
    <p>接到通知 → 查看开班安排 → 确认参训 → 培训执行留痕</p>
  </div>
  <div class="biz-steps">
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#7C3AED,#6D28D9);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 2C5.5 2 4 3.8 4 6V9L2.5 11.5H13.5L12 9V6C12 3.8 10.5 2 8 2Z" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.5 11.5C6.5 12.6 7.2 13.5 8 13.5C8.8 13.5 9.5 12.6 9.5 11.5" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
      <h5>接收点将通知</h5>
      <small>上游开班点将后<br>经销商侧可见</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#F59E0B,#FBBF24);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="2" width="10" height="12" rx="1.5" stroke="white" stroke-width="1.5"/><path d="M5 6H11M5 9H11" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
      <h5>查看开班详情</h5>
      <small>核对主题·时间<br>地点与讲师</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#10B981,#34D399);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="8" cy="8" r="6" stroke="white" stroke-width="1.5"/><path d="M5 8L7 10L11 6" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h5>确认参训状态</h5>
      <small>反馈参训人员<br>与行程安排</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#3B82F6,#60A5FA);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M2 12L6 8L10 10L14 4" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h5>培训执行留痕</h5>
      <small>形成培训执行记录<br>与点将状态更新</small>
    </div>
  </div>
  <div class="kl-tip" style="margin-top:14px;">
    <strong>数据边界：</strong>本页为经销商视角的只读查询，只展示当前登录经销商自身被点将的特训营记录，不能新建或修改；开班与点将发起在总部侧完成。
  </div>
</div>
</div>
</div>
</div>

<div id="biz-flow" style="display:none;">
<div class="tab-pad">
<div class="bf-truth-flow">
  <h4 class="bf-main-title">特训营 — 全链路流程图</h4>
  <p class="bf-main-sub">开始 → 接收被点将通知 → ★查看并确认特训营点将★ → 确认/反馈参训状态 → 结束</p>
  <div class="bf-fc-svg-wrap">
    <svg class="bf-fc-svg" style="max-height:none;" viewBox="0 0 1200 580" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <marker id="arr-green" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#16A34A"/></marker>
        <marker id="arr-gray" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#9CA3AF"/></marker>
        <marker id="arr-blue" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#3B82F6"/></marker>
        <marker id="arr-red" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#EF4444"/></marker>
        <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%"><feDropShadow dx="0" dy="2" stdDeviation="3" flood-color="#000" flood-opacity="0.15"/></filter>
      </defs>
      <rect x="50" y="20" width="1100" height="95" rx="8" fill="#EFF6FF" stroke="#3B82F6" stroke-width="1.5" stroke-dasharray="6,4"/>
      <text x="600" y="42" text-anchor="middle" fill="#1D4ED8" font-size="13" font-weight="600">上游支撑</text>
      <rect x="270" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="345" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">总部/大区发起</text>
      <rect x="440" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="515" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">经销商主档</text>
      <rect x="610" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="685" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">讲师主档</text>
      <rect x="780" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="855" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">特训营主数据</text>
      <line x1="235" y1="115" x2="235" y2="150" stroke="#3B82F6" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr-blue)"/>
      <rect x="195" y="150" width="80" height="44" rx="6" fill="#FAF5FF" stroke="#9333EA" stroke-width="1.5" stroke-dasharray="5,3"/>
      <text x="235" y="177" text-anchor="middle" fill="#7C3AED" font-size="13" font-weight="600">开始</text>
      <line x1="235" y1="194" x2="235" y2="230" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="155" y="230" width="160" height="54" rx="6" fill="#16A34A" stroke="#15803D" stroke-width="2" filter="url(#shadow)"/>
      <text x="235" y="254" text-anchor="middle" fill="#FFFFFF" font-size="13" font-weight="700">★查看并确认特训营点将★</text>
      <text x="235" y="272" text-anchor="middle" fill="#DCFCE7" font-size="10">查看主题/时间/地点/讲师</text>
      <line x1="235" y1="284" x2="235" y2="310" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="150" y="310" width="170" height="40" rx="6" fill="#F0FDF4" stroke="#16A34A" stroke-width="2"/>
      <text x="235" y="335" text-anchor="middle" fill="#166534" font-size="13" font-weight="600">确认/反馈参训状态</text>
      <line x1="235" y1="350" x2="235" y2="370" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="180" y="370" width="110" height="40" rx="6" fill="#FAF5FF" stroke="#9333EA" stroke-width="1.5" stroke-dasharray="5,3"/>
      <text x="235" y="395" text-anchor="middle" fill="#7C3AED" font-size="13" font-weight="600">结束</text>
      <line x1="235" y1="410" x2="235" y2="460" stroke="#16A34A" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr-green)"/>
      <rect x="50" y="460" width="1100" height="95" rx="8" fill="#F0FDF4" stroke="#16A34A" stroke-width="1.5" stroke-dasharray="6,4"/>
      <text x="600" y="482" text-anchor="middle" fill="#166534" font-size="13" font-weight="600">下游影响</text>
      <rect x="355" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="430" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">参训确认回执</text>
      <rect x="525" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="600" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">培训执行记录</text>
      <rect x="695" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="770" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">点将状态更新</text>
    </svg>
  </div>
  <div class="bf-fc-legend">
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-green"></span> 主流程步骤</span>
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-purple"></span> 开始/结束</span>
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-blue"></span> 上游支撑</span>
    <span class="bf-fc-legend-item"><span style="display:inline-block;width:22px;height:2px;background:#EF4444;"></span> 审批拒绝/驳回</span>
  </div>
</div>
</div>
</div>

<div id="key-logic" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard num="1" title="重点逻辑1：经销商发起视角">
<ul><li><strong>业务意义</strong>：本菜单由经销商主动发起特训营点将申请，经销商自主选择特训营、法人主体与讲师。</li><li><strong>具体逻辑描述</strong>：通过<code>pageForDealer</code>接口实现经销商发起，后端自动按当前登录经销商过滤数据并写入DEALER_CODE、DEALER_NAME。</li></ul>
</KbCard>

<KbCard num="2" title="重点逻辑2：数据范围隔离">
<ul><li><strong>业务意义</strong>：经销商仅能查看和操作自己发起的特训营点将记录，避免跨经销商数据泄露。</li><li><strong>具体逻辑描述</strong>：通过<code>pageForDealer</code>接口自动按当前登录经销商过滤，前端无需传经销商参数。</li></ul>
</KbCard>

<KbCard num="3" title="重点逻辑3：applyCode自动生成">
<ul><li><strong>业务意义</strong>：保证特训营点将申请编码全局唯一、可追溯，作为主键贯穿全链路。</li><li><strong>具体逻辑描述</strong>：提交申请时系统自动生成唯一编码applyCode，写入APPLY_CODE字段。</li></ul>
</KbCard>

<KbCard num="4" title="重点逻辑4：特训营选择">
<ul><li><strong>业务意义</strong>：从已发布的特训营中选择参训营次，确保参训信息来源唯一且有效。</li><li><strong>具体逻辑描述</strong>：点击"申请点将"按钮，弹窗展示特训营列表（<code>campApplyApi.campSelectPage</code>）。行操作：查看介绍（CampDetail弹窗）、报名（ApplyInfo弹窗）。报名弹窗填写：法人主体、交易公司、参训人员明细（入职日期、省市区、门店等）。</li></ul>
</KbCard>

<KbCard num="5" title="重点逻辑5：报名数据转换">
<ul><li><strong>业务意义</strong>：将前端报名人员明细数据转换为后端可存储格式，保证数据完整性。</li><li><strong>具体逻辑描述</strong>：提交时将报名人员明细数据转换：entryDate格式化为YYYY-MM-DD；province数组拆分为provinceName/provinceCode、cityName/cityCode、areaName/areaCode；store对象拆分为store（terminalName）和storeCode（terminalCode）。</li></ul>
</KbCard>

<KbCard num="6" title="重点逻辑6：取消申请校验">
<ul><li><strong>业务意义</strong>：约束取消申请的时间窗口与状态，避免临近培训取消影响讲师排期与参训安排。</li><li><strong>具体逻辑描述</strong>：时间校验 timeDiff &gt;= 7（培训开始前7天），基于planStartTime，否则提示"只有培训时间开始前7天的流程可以发起点将取消申请！"；状态校验 approvalState === 'fdd_sign'（已法大大签约）；取消状态校验 !cancelApprovalState || cancelApprovalState === 'reject' || cancelApprovalState === 'oa_reject'。校验通过弹窗填写取消原因，调用<code>POST /mlt/trainCampApply/cancelApply</code>接口。</li></ul>
</KbCard>

<KbCard num="7" title="重点逻辑7：结算前确认">
<ul><li><strong>业务意义</strong>：在结算前由经销商确认结算明细，保障结算数据准确。</li><li><strong>具体逻辑描述</strong>：行操作"结算前确认"，显示条件 beforeSettlementConfirmState === 'to_be_confirm'。弹窗展示CampSettleDetail结算明细。确认：beforeSettlementConfirmState=confirm；驳回：beforeSettlementConfirmState=reject。调用<code>POST /mlt/trainCampApply/settleBeforeConfirm</code>接口。</li></ul>
</KbCard>

<KbCard num="8" title="重点逻辑8：行操作显隐">
<ul><li><strong>业务意义</strong>：根据申请状态差异化展示行操作，避免误操作。</li><li><strong>具体逻辑描述</strong>：编辑再发起：applyState === 'draft' 或 approvalState 为 reject/oa_reject；结算前确认：beforeSettlementConfirmState === 'to_be_confirm'；删除：applyState === 'draft' 或 approvalState 为 reject/fdd_reject/oa_reject。</li></ul>
</KbCard>

<KbCard num="9" title="重点逻辑9：讲师排期冲突校验">
<ul><li><strong>业务意义</strong>：避免同一讲师在同一时间段被重复点将，保障讲师档期合理性。</li><li><strong>具体逻辑描述</strong>：保存时校验所选讲师在拟定开始时间至拟定结束时间区间内不存在已生效点将记录，存在冲突则提示并阻止保存。</li></ul>
</KbCard>

</div>
</div>
</div>

<div id="detail-logic" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="界面模块">
<h4>前端路由</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>路由</th><th>用途</th></tr>
</thead>
<tbody>
<tr><td>`/general/distributorGeneral/distributorGeneralCamp/list`</td><td>特训营点将列表页</td></tr>
</tbody>
</table>
<h4>API接口</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>接口路径</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>`mlt/trainCampApply/page`</td><td>经销商分页查询（pageForDealer）</td></tr>
<tr><td>`mlt/trainCampApply/save`</td><td>保存</td></tr>
<tr><td>`mlt/trainCampApply/saveAndSubmit`</td><td>保存并提交</td></tr>
<tr><td>`mlt/trainCampApply/cancel`</td><td>取消</td></tr>
<tr><td>`mlt/trainCampApply/detail`</td><td>查询申请详情</td></tr>
<tr><td>`mlt/trainCamp/dealer/page`</td><td>特训营选择分页（campSelectPage）</td></tr>
<tr><td>`mlt/trainCampApply/companyList`</td><td>法人主体列表（getDistributorLegalList）</td></tr>
<tr><td>`mlt/trainCampApply/delete`</td><td>删除申请</td></tr>
<tr><td>`mlt/trainCampApply/approvalPage`</td><td>审批分页查询</td></tr>
<tr><td>`mlt/trainCampApply/pushCrm`</td><td>同步CRM</td></tr>
<tr><td>`mlt/trainCampApply/pushOa`</td><td>同步OA</td></tr>
<tr><td>`mlt/trainCampApply/pushFdd`</td><td>同步FDD</td></tr>
<tr><td>`mlt/trainCampApply/cancelApply`</td><td>发起取消申请</td></tr>
<tr><td>`mlt/trainCampApply/cancelApplyApproval`</td><td>取消审批</td></tr>
<tr><td>`mlt/trainCampApply/campApplyApproval`</td><td>特训营点将审批（applyApproval）</td></tr>
<tr><td>`mlt/trainCampApply/getSealAuthorizers`</td><td>获取用印审批人</td></tr>
<tr><td>`mlt/trainCampApply/settleBeforeConfirm`</td><td>结算前确认</td></tr>
</tbody>
</table>
<h4>内置审批</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>审批流编码</th><th>说明</th><th>触发时机</th></tr>
</thead>
<tbody>
<tr><td>campApplyApproval</td><td>特训营点将申请审批</td><td>保存并提交时触发</td></tr>
</tbody>
</table>
<h4>查询栏字段</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>申请编码</td><td>APPLY_CODE</td><td>TextField</td><td>申请编码模糊查询</td><td>始终显示</td><td>手动输入</td></tr>
<tr><td>状态</td><td>APPLY_STATE</td><td>Select</td><td>申请状态筛选</td><td>始终显示</td><td>值集MBO.CAMP_APPLY_STATE</td></tr>
<tr><td>申请审批状态</td><td>APPROVAL_STATE</td><td>Select</td><td>审批状态筛选</td><td>始终显示</td><td>值集MBO.APPLY_APPROVAL_STATE</td></tr>
</tbody>
</table>
<h4>列表展示字段</h4>
<blockquote>数据来源：<code>stores/listConfig.tsx</code> 的 <code>listDScolumns</code>，列表 DataSet 使用 <code>dataKey=data.content</code>、<code>totalKey=data.totalElements</code>，多选 <code>DataSetSelection.multiple</code>，接口 <code>campApplyApi.page</code>。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>申请编码</td><td>APPLY_CODE</td><td>TextField</td><td>申请唯一编码</td><td>始终显示</td><td>左锁定列，width=180</td></tr>
<tr><td>类型</td><td>CAMP_TYPE</td><td>Select</td><td>特训营类型</td><td>始终显示</td><td>值集MBO.CAMP_TYPE，width=130</td></tr>
<tr><td>特训营名称</td><td>CAMP_NAME</td><td>Button(link)</td><td>特训营名称，可点击查看详情</td><td>始终显示</td><td>左锁定列，width=160，点击触发handleShowDetail(type='apply')</td></tr>
<tr><td>状态</td><td>APPLY_STATE</td><td>Select</td><td>申请状态</td><td>始终显示</td><td>值集MBO.CAMP_APPLY_STATE，width=130</td></tr>
<tr><td>申请审核状态</td><td>APPROVAL_STATE</td><td>Select</td><td>审批状态</td><td>始终显示</td><td>值集MBO.APPLY_APPROVAL_STATE，width=130</td></tr>
<tr><td>培训时间</td><td>PLAN_START_TIME/PLAN_END_TIME</td><td>TextField(组合)</td><td>培训时间范围</td><td>始终显示</td><td>width=180，渲染为"planStartTime 至 planEndTime"</td></tr>
<tr><td>培训地点</td><td>ADDRESS</td><td>TextField</td><td>培训地点</td><td>始终显示</td><td>后端返回，width=130</td></tr>
<tr><td>讲师</td><td>LECTURER</td><td>TextField</td><td>讲师姓名</td><td>始终显示</td><td>后端返回，width=130</td></tr>
<tr><td>取消申请审核状态</td><td>CANCEL_APPROVAL_STATE</td><td>Select</td><td>取消审批状态</td><td>始终显示</td><td>值集MBO.CANCEL_APPROVAL_STATE，width=130</td></tr>
<tr><td>结算前确认状态</td><td>BEFORE_SETTLEMENT_CONFIRM_STATE</td><td>Select</td><td>结算前确认状态</td><td>始终显示</td><td>值集MBO.BEFORE_SETTLEMENT_CONFIRM_STATE，width=130</td></tr>
<tr><td>结算确认状态</td><td>SETTLEMENT_CONFIRM_STATE</td><td>Select</td><td>结算确认状态</td><td>始终显示</td><td>值集MBO.SETTLEMENT_COMFIRM_STATE，width=130</td></tr>
<tr><td>CRM单号</td><td>CRM_ORDER_CODE</td><td>TextField</td><td>CRM订单编号</td><td>始终显示</td><td>推送CRM后返回，width=130</td></tr>
<tr><td>申请时间</td><td>CREATE_DATE</td><td>TextField</td><td>申请创建时间</td><td>始终显示</td><td>后端返回，width=150</td></tr>
<tr><td>操作</td><td>-</td><td>Button(link)</td><td>编辑再发起/结算前确认/查看流程/删除</td><td>始终显示</td><td>右锁定列，width=240，按状态条件显隐</td></tr>
</tbody>
</table>
<h4>详情页字段</h4>
<blockquote>特训营无独立详情页（无 detail.tsx），报名/编辑通过弹窗内嵌 <code>components/ApplyInfo/index.tsx</code> 实现。ApplyInfo 包含两个部分：报名表单（applyFormDS）+ 参训人员列表（userListDS）。</blockquote>
<h5>报名表单（applyFormDS）</h5>
<blockquote>数据来源：<code>components/ApplyInfo/config.tsx</code> 的 <code>applyFormDSProps</code>。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>交易公司</td><td>TRADING_COMPANY_NAME</td><td>TextField+Icon(search)</td><td>交易公司</td><td>始终显示</td><td>必填，readOnly+搜索图标，点击弹出交易公司选择弹窗</td></tr>
<tr><td>开票账户</td><td>LEGAL_ENTITY_NAME</td><td>TextField</td><td>开票账户（法人单位名称）</td><td>始终显示</td><td>必填，disabled，placeholder="选择交易公司后自动带出"，选交易公司后自动写入legalEntityName/legalEntityCode/tradingCompanyName/tradingCompanyId</td></tr>
</tbody>
</table>
<h5>参训人员列表（userListDS）</h5>
<blockquote>数据来源：<code>components/ApplyInfo/config.tsx</code> 的 <code>userListDSProps</code>/<code>userListDSColumns</code>。通过"添加"按钮新增行，支持多行参训人员。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>序号</td><td>-</td><td>TextField</td><td>行序号</td><td>始终显示</td><td>自动计算dataSet.indexOf(record)+1，左锁定列，width=60</td></tr>
<tr><td>姓名</td><td>NAME</td><td>TextField</td><td>参训人员姓名</td><td>始终显示</td><td>必填，editor模式</td></tr>
<tr><td>手机号</td><td>PHONE</td><td>TextField</td><td>参训人员手机号</td><td>始终显示</td><td>必填，editor模式，width=120，校验正则/^1[3456789]\d&#123;9&#125;$/</td></tr>
<tr><td>性别</td><td>GENDER</td><td>Select</td><td>参训人员性别</td><td>始终显示</td><td>必填，值集MBO.CAMP_USER_SEX，width=80</td></tr>
<tr><td>学历</td><td>EDUCATION</td><td>TextField</td><td>参训人员学历</td><td>始终显示</td><td>必填，editor模式</td></tr>
<tr><td>所属门店</td><td>STORE</td><td>TextField+Icon(search)</td><td>参训人员所属门店</td><td>始终显示</td><td>必填，readOnly+搜索图标，点击弹出门店选择弹窗，width=250</td></tr>
<tr><td>省市区</td><td>PROVINCE</td><td>Cascader</td><td>参训人员所在省市区</td><td>始终显示</td><td>必填，级联选择器，loadAddressData动态加载，width=180</td></tr>
<tr><td>详细地址</td><td>ADDRESS</td><td>TextField</td><td>参训人员详细地址</td><td>始终显示</td><td>editor模式，width=160</td></tr>
<tr><td>岗位</td><td>POST</td><td>TextField</td><td>参训人员岗位</td><td>始终显示</td><td>必填，editor模式</td></tr>
<tr><td>入职日期</td><td>ENTRY_DATE</td><td>DatePicker</td><td>参训人员入职日期</td><td>始终显示</td><td>必填，width=120</td></tr>
<tr><td>备注</td><td>REMARK</td><td>TextField</td><td>备注</td><td>始终显示</td><td>editor模式，width=140</td></tr>
<tr><td>操作</td><td>-</td><td>Button(link)</td><td>删除</td><td>始终显示</td><td>右锁定列，width=80，删除当前行</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="选择弹窗">
<h4>弹窗1：申请点将弹窗（特训营列表选择，列表页头部"申请点将"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleApply</code> + <code>stores/listConfig.tsx</code> 的 <code>applyListDSProps</code>/<code>applyListDScolumns</code>。接口 <code>campApplyApi.campSelectPage</code>。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>campName</td><td>查询栏输入</td><td>特训营名称模糊查询</td></tr>
</tbody>
</table>
<p>列表列：序号、类型（值集MBO.CAMP_TYPE）、特训营名称、培训时间（planStartTime至planEndTime组合）、培训地点、讲师、操作（查看介绍/报名）。</p>
<p>行操作：</p>
<ul><li>查看介绍：弹出特训营详情弹窗（CampDetail组件，type='camp'）</li><li>报名：调用 <code>handleSendApply</code>，弹出报名信息弹窗</li></ul>
<p>查询SQL：</p>

```sql
SELECT
  tc.CAMP_CODE    AS 特训营编码,
  tc.CAMP_NAME    AS 特训营名称,
  tc.CAMP_TYPE    AS 特训营类型,
  TO_CHAR(tc.PLAN_START_TIME, 'YYYY-MM-DD') AS 培训开始时间,
  TO_CHAR(tc.PLAN_END_TIME, 'YYYY-MM-DD') AS 培训结束时间,
  tc.LECTURER     AS 讲师,
  tc.ADDRESS      AS 培训地点
FROM TRAIN_CAMP tc
WHERE tc.CAMP_STATUS = 'valid'
  AND (:campName IS NULL OR tc.CAMP_NAME LIKE '%' || :campName || '%')
ORDER BY tc.CREATE_DATE DESC;
```
<h4>弹窗2：特训营详情弹窗（申请点将弹窗行操作"查看介绍"或列表"特训营名称"点击触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleShowDetail</code>，组件 <code>components/CampDetail</code>，入参 rowData + type（'camp'查看介绍/'apply'申请详情/'cancel'取消详情）。无确定按钮，cancelText='关闭'。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>rowData</td><td>列表/弹窗选中行</td><td>特训营或申请数据</td></tr>
<tr><td>type</td><td>固定值</td><td>'camp'=查看介绍，'apply'=申请详情，'cancel'=取消详情</td></tr>
</tbody>
</table>
<h4>弹窗3：报名信息弹窗（申请点将弹窗行操作"报名"或列表行"编辑再发起"触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleSendApply</code>/<code>handleUpdate</code>，组件 <code>components/ApplyInfo</code>。弹窗含自定义footer：保存（onOk）、保存并提交（自定义按钮）。编辑模式传入applyCode加载已有数据。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>campCode</td><td>特训营选择</td><td>特训营编码</td></tr>
<tr><td>specialTrainCampApplyId</td><td>特训营选择</td><td>特训营申请ID</td></tr>
<tr><td>applyCode</td><td>编辑模式</td><td>申请编码（编辑时加载已有数据）</td></tr>
<tr><td>tradingCompanyName</td><td>交易公司选择</td><td>交易公司名称</td></tr>
<tr><td>tradingCompanyId</td><td>交易公司选择</td><td>交易公司ID</td></tr>
<tr><td>legalEntityName</td><td>交易公司选择</td><td>法人单位名称</td></tr>
<tr><td>legalEntityCode</td><td>交易公司选择</td><td>法人单位编码</td></tr>
<tr><td>tradingCompanyDetailTDTOList</td><td>参训人员列表</td><td>参训人员明细</td></tr>
</tbody>
</table>
<p>提交逻辑：</p>
<ul><li>保存：调用 <code>campApplyApi.save</code></li><li>保存并提交：调用 <code>campApplyApi.saveAndSubmit</code>，触发campApplyApproval审批流</li><li>参训人员数据转换：entryDate格式化为YYYY-MM-DD，store拆为store/storeCode，province级联拆为provinceName/provinceCode/cityName/cityCode/areaName/areaCode</li></ul>
<h4>弹窗4：交易公司选择弹窗（报名信息弹窗"交易公司"字段搜索图标触发）</h4>
<blockquote>数据来源：<code>components/ApplyInfo/index.tsx</code> 的 <code>handleShowCompanyList</code> + <code>components/ApplyInfo/config.tsx</code> 的 <code>companyListDSProps</code>/<code>companyListDSColumns</code>。接口 <code>campApplyApi.companyList</code>，单选，入参 distributorCode（取自当前用户 additionInfo.DC）。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>distributorCode</td><td>当前用户 additionInfo.DC</td><td>经销商编码</td></tr>
</tbody>
</table>
<p>列表列：开票单位编码(legalEntityCode)、开票单位名称(legalEntityName)、交易公司ID(tradingCompanyId)、交易公司名称(tradingCompanyName)。</p>
<p>选择后回写：tradingCompanyName、tradingCompanyId、legalEntityName、legalEntityCode。</p>
<p>查询SQL：</p>

```sql
SELECT
  tc.LEGAL_ENTITY_CODE    AS 开票单位编码,
  tc.LEGAL_ENTITY_NAME    AS 开票单位名称,
  tc.TRADING_COMPANY_ID   AS 交易公司ID,
  tc.TRADING_COMPANY_NAME AS 交易公司名称
FROM MBO_TRADING_COMPANY tc
WHERE tc.DISTRIBUTOR_CODE = :distributorCode;
```
<h4>弹窗5：门店选择弹窗（参训人员列表"所属门店"字段搜索图标触发）</h4>
<blockquote>数据来源：<code>components/ApplyInfo/config.tsx</code> 的 <code>handleShowStoreList</code> + <code>storeDS</code>/<code>storeListDSColumns</code>。接口 <code>campApi.storePage</code>，单选。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>terminalCode</td><td>查询栏输入</td><td>门店编码</td></tr>
<tr><td>terminalName</td><td>查询栏输入</td><td>门店名称</td></tr>
</tbody>
</table>
<p>列表列：门店编码、门店名称、门店地址、负责人、联系电话。</p>
<p>选择后回写：store（完整对象）写入userListDS当前行。</p>
<h4>弹窗6：取消申请弹窗（列表页"取消点将"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleCancel</code>，DataSet 为 <code>cancelApplyFormDSProps</code>。弹窗内嵌取消原因表单 + CampDetail（type='cancel'）。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
<tr><td>cancelReason</td><td>手动输入</td><td>取消原因（必填，TextArea）</td></tr>
</tbody>
</table>
<p>查询SQL：</p>

```sql
SELECT
  tca.APPLY_CODE    AS 申请编码,
  TO_CHAR(tca.PLAN_START_TIME, 'YYYY-MM-DD') AS 培训开始时间,
  tca.APPROVAL_STATE AS 审核状态,
  ROUND((tca.PLAN_START_TIME - SYSDATE)) AS 距开始天数
FROM TRAIN_CAMP_APPLY tca
WHERE tca.APPLY_CODE = :applyCode;
```
<h4>弹窗7：结算前确认弹窗（列表行"结算前确认"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleSettlementConfirm</code>，组件 <code>components/CampSettleDetail</code>，入参 rowData。弹窗含自定义footer：确认（onOk）、驳回（自定义按钮）。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
<tr><td>beforeSettlementConfirmState</td><td>操作类型</td><td>confirm=确认，reject=驳回</td></tr>
</tbody>
</table>
<p>提交接口：<code>campApplyApi.settleBeforeConfirm</code>，data=&#123; applyCode, beforeSettlementConfirmState &#125;。</p>
<p>查询SQL：</p>

```sql
SELECT
  tca.APPLY_CODE    AS 申请编码,
  tca.CAMP_NAME     AS 特训营名称,
  tca.BEFORE_SETTLEMENT_CONFIRM_STATE AS 结算前确认状态
FROM TRAIN_CAMP_APPLY tca
WHERE tca.APPLY_CODE = :applyCode
  AND tca.BEFORE_SETTLEMENT_CONFIRM_STATE = 'to_be_confirm';
```
<h4>弹窗8：流程摘要弹窗（列表行"查看流程"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleShowProcess</code>，组件 <code>components/ProcessDetail</code>，入参 applyCode。无确定按钮，cancelText='关闭'。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="其他按钮">
<blockquote>数据来源：<code>list.tsx</code>。权限前缀 <code>hzero.general_manage.distributor.distributor_general_camp.ps.</code>。特训营无独立详情页，保存/保存并提交在报名信息弹窗footer中。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>序号</th><th>按钮名称</th><th>所在位置</th><th>显隐条件</th><th>业务逻辑</th></tr>
</thead>
<tbody>
<tr><td>1</td><td>申请点将</td><td>列表页头部</td><td>始终显示</td><td>弹出特训营列表，选择后报名</td></tr>
<tr><td>2</td><td>申请确认</td><td>列表页头部</td><td>始终显示</td><td>校验FDD签署中状态，调用FDD签合同</td></tr>
<tr><td>3</td><td>取消点将</td><td>列表页头部</td><td>始终显示</td><td>校验提前7天且已签约，弹窗填写取消原因</td></tr>
<tr><td>4</td><td>编辑再发起</td><td>列表行</td><td>applyState='draft'或approvalState为reject/oa_reject</td><td>弹出报名信息弹窗编辑</td></tr>
<tr><td>5</td><td>结算前确认</td><td>列表行</td><td>beforeSettlementConfirmState='to_be_confirm'</td><td>弹窗展示结算明细，确认/驳回</td></tr>
<tr><td>6</td><td>查看流程</td><td>列表行</td><td>始终显示</td><td>弹出流程摘要组件</td></tr>
<tr><td>7</td><td>删除</td><td>列表行</td><td>applyState='draft'或approvalState为reject/fdd_reject/oa_reject</td><td>确认后调用delete接口</td></tr>
<tr><td>8</td><td>保存</td><td>报名信息弹窗footer</td><td>报名/编辑模式</td><td>校验后调用save接口</td></tr>
<tr><td>9</td><td>保存并提交</td><td>报名信息弹窗footer</td><td>报名/编辑模式</td><td>校验后调用saveAndSubmit接口</td></tr>
</tbody>
</table>
<h4>按钮1：申请点将（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;apply</code></li><li><strong>业务逻辑</strong>：弹出"申请点将"弹窗，展示特训营列表（applyTableDS），支持按特训营名称查询。行操作"报名"后弹出报名信息弹窗。</li></ul>
<h4>按钮2：申请确认（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;confirm_apply</code></li><li><strong>业务逻辑</strong>：校验选中1行且 approvalState='fdd_in_approval'，调用 <code>useFDD</code> 钩子的 <code>handleOpenFDD</code>。否则提示"只有FDD签署中的申请单才可以签合同"。</li></ul>
<h4>按钮3：取消点将（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;cancel_apply</code></li><li><strong>业务逻辑</strong>：校验选中1行，时间校验 timeDiff=(planStartTime-nowTime)/(24*60*60*1000) &gt;= 7，状态校验 approvalState='fdd_sign' 且 (!cancelApprovalState || cancelApprovalState='reject'/'oa_reject')。通过后弹出取消申请弹窗（含CampDetail type='cancel'），调用 <code>campApplyApi.cancelApply</code>。</li><li><strong>校验</strong>：不满足提前7天提示"只有培训时间开始前7天的流程可以发起点将取消申请！"；状态不符提示"该状态单据无法发起取消申请！"。</li></ul>
<h4>按钮4：编辑再发起（列表行）</h4>
<ul><li><strong>显隐条件</strong>：applyState='draft' 或 approvalState 为 reject/oa_reject</li><li><strong>业务逻辑</strong>：校验通过后弹出报名信息弹窗（ApplyInfo），传入applyCode加载已有数据。否则提示"当前状态数据无法编辑！"。</li></ul>
<h4>按钮5：结算前确认（列表行）</h4>
<ul><li><strong>显隐条件</strong>：beforeSettlementConfirmState='to_be_confirm'</li><li><strong>业务逻辑</strong>：弹出结算前确认弹窗（CampSettleDetail），onOk调用 <code>campApplyApi.settleBeforeConfirm</code>（beforeSettlementConfirmState='confirm'），自定义"驳回"按钮调用 <code>settleBeforeConfirm</code>（beforeSettlementConfirmState='reject'）。</li></ul>
<h4>按钮6：查看流程（列表行）</h4>
<ul><li><strong>显隐条件</strong>：始终显示</li><li><strong>业务逻辑</strong>：弹出流程摘要弹窗，渲染 <code>ProcessDetail</code> 组件，入参 applyCode。</li></ul>
<h4>按钮7：删除（列表行）</h4>
<ul><li><strong>显隐条件</strong>：applyState='draft' 或 approvalState 为 reject/fdd_reject/oa_reject</li><li><strong>业务逻辑</strong>：确认弹窗"确定要删除当前数据吗？"，确认后调用 <code>campApplyApi.delete</code>（URL追加applyCode），成功后刷新列表。</li></ul>
<h4>按钮8：保存（报名信息弹窗footer）</h4>
<ul><li><strong>显隐条件</strong>：报名/编辑模式（弹窗onOk）</li><li><strong>业务逻辑</strong>：校验 applyFormDS（交易公司必填）+ userListDS（参训人员每行必填、手机号正则校验）。调用 <code>campApplyApi.save</code>，data含campCode、specialTrainCampApplyId、交易公司信息、tradingCompanyDetailTDTOList（参训人员明细）。</li></ul>
<h4>按钮9：保存并提交（报名信息弹窗footer）</h4>
<ul><li><strong>显隐条件</strong>：报名/编辑模式（自定义按钮）</li><li><strong>业务逻辑</strong>：校验同保存，调用 <code>campApplyApi.saveAndSubmit</code>，触发campApplyApproval审批流，成功后关闭弹窗。</li></ul>
</KbCard>

<KbCard title="保存/提交校验">
<blockquote>数据来源：<code>list.tsx</code> 的 <code>submitApply</code> + <code>components/ApplyInfo/index.tsx</code> 的 <code>getApplyFormData</code>/<code>getUserList</code>。保存与保存并提交共用同一校验逻辑。</blockquote>
<ul><li>校验1：交易公司必填 —— applyFormDS.tradingCompanyName，明确交易主体</li><li>校验2：开票账户必填 —— applyFormDS.legalEntityName（选交易公司自动带出）</li><li>校验3：参训人员列表不能为空 —— userListDS至少1行</li><li>校验4：参训人员姓名必填 —— 每行name不能为空</li><li>校验5：参训人员手机号必填 —— 每行phone不能为空，且校验正则/^1[3456789]\d&#123;9&#125;$/</li><li>校验6：参训人员性别必填 —— 每行gender不能为空，值集MBO.CAMP_USER_SEX</li><li>校验7：参训人员学历必填 —— 每行education不能为空</li><li>校验8：参训人员所属门店必填 —— 每行store不能为空</li><li>校验9：参训人员省市区必填 —— 每行province不能为空</li><li>校验10：参训人员岗位必填 —— 每行post不能为空</li><li>校验11：参训人员入职日期必填 —— 每行entryDate不能为空</li><li>校验12：取消申请时取消原因必填 —— cancelFormDS.cancelReason</li><li>校验13：讲师排期冲突校验（后端） —— 避免同一讲师同一时间段被重复点将</li></ul>
</KbCard>

<KbCard title="状态机">
<p>流转图：</p>

```sql
draft(新建) → submitted(已提交) → approved(审批通过) → valid(生效)
   → executing(执行中) → end(已完成)
rejected(审批驳回) → 可修改重提
valid → cancel_applied(取消申请中) → cancel_approved(已取消)
valid → to_be_confirm(待结算前确认) → confirm(已确认) → 结算
```
<p>状态列表：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>当前状态</th><th>触发动作</th><th>目标状态</th></tr>
</thead>
<tbody>
<tr><td>draft</td><td>保存并提交</td><td>submitted</td></tr>
<tr><td>submitted</td><td>审批通过</td><td>approved</td></tr>
<tr><td>submitted</td><td>审批驳回</td><td>rejected</td></tr>
<tr><td>rejected</td><td>修改后重新提交</td><td>submitted</td></tr>
<tr><td>approved</td><td>生效</td><td>valid</td></tr>
<tr><td>valid</td><td>开始执行</td><td>executing</td></tr>
<tr><td>executing</td><td>结束执行</td><td>end</td></tr>
<tr><td>valid</td><td>发起取消申请</td><td>cancel_applied</td></tr>
<tr><td>cancel_applied</td><td>取消审批通过</td><td>cancel_approved</td></tr>
<tr><td>valid</td><td>结算前确认</td><td>to_be_confirm</td></tr>
<tr><td>to_be_confirm</td><td>确认</td><td>confirm</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="导入">
<p>不支持导入功能。</p>
</KbCard>

<KbCard title="特训营点将申请表（TRAIN_CAMP_APPLY）">
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>类型</th><th>释义</th><th>对应界面字段</th><th>逻辑</th></tr>
</thead>
<tbody>
<tr><td>ID</td><td>NUMBER</td><td>主键ID</td><td>-</td><td>序列自增</td></tr>
<tr><td>APPLY_CODE</td><td>VARCHAR2(64)</td><td>申请编码</td><td>申请编码</td><td>系统生成，PK</td></tr>
<tr><td>SPECIAL_TRAIN_CAMP_APPLY_ID</td><td>NUMBER</td><td>申请ID</td><td>-</td><td>序列自增</td></tr>
<tr><td>CAMP_CODE</td><td>VARCHAR2(64)</td><td>特训营编码</td><td>-</td><td>关联特训营</td></tr>
<tr><td>CAMP_NAME</td><td>VARCHAR2(255)</td><td>特训营名称</td><td>特训营选择</td><td>关联特训营</td></tr>
<tr><td>CAMP_TYPE</td><td>VARCHAR2(32)</td><td>特训营类型</td><td>-</td><td>值集MBO.CAMP_TYPE</td></tr>
<tr><td>APPLY_STATE</td><td>VARCHAR2(32)</td><td>申请状态</td><td>点将状态</td><td>值集MBO.CAMP_APPLY_STATE</td></tr>
<tr><td>APPROVAL_STATE</td><td>VARCHAR2(32)</td><td>审核状态</td><td>审核状态</td><td>值集MBO.APPLY_APPROVAL_STATE</td></tr>
<tr><td>CANCEL_APPROVAL_STATE</td><td>VARCHAR2(32)</td><td>取消审核状态</td><td>-</td><td>值集MBO.CANCEL_APPROVAL_STATE</td></tr>
<tr><td>BEFORE_SETTLEMENT_CONFIRM_STATE</td><td>VARCHAR2(32)</td><td>结算前确认状态</td><td>-</td><td>值集MBO.BEFORE_SETTLEMENT_CONFIRM_STATE</td></tr>
<tr><td>SETTLEMENT_CONFIRM_STATE</td><td>VARCHAR2(32)</td><td>结算确认状态</td><td>-</td><td>值集MBO.SETTLEMENT_COMFIRM_STATE</td></tr>
<tr><td>END_CONFIRM_STATE</td><td>VARCHAR2(32)</td><td>结束确认状态</td><td>结束确认状态</td><td>值集MBO.END_COMFIRM_STATE</td></tr>
<tr><td>PLAN_START_TIME</td><td>DATE</td><td>培训开始时间</td><td>开始时间</td><td>关联特训营</td></tr>
<tr><td>PLAN_END_TIME</td><td>DATE</td><td>培训结束时间</td><td>结束时间</td><td>关联特训营</td></tr>
<tr><td>PRE_ORD_LECTURER_DAYS</td><td>NUMBER</td><td>拟点将天数</td><td>拟点将天数</td><td>申请时填写</td></tr>
<tr><td>REAL_ORD_LECTURER_DAYS</td><td>NUMBER</td><td>实际点将天数</td><td>实际点将天数</td><td>执行环节填写</td></tr>
<tr><td>ADDRESS</td><td>VARCHAR2(500)</td><td>培训地点</td><td>-</td><td>关联特训营</td></tr>
<tr><td>LECTURER</td><td>VARCHAR2(255)</td><td>讲师</td><td>培训师</td><td>关联特训营</td></tr>
<tr><td>LECTURER_CODE</td><td>VARCHAR2(64)</td><td>讲师编码</td><td>-</td><td>关联特训营</td></tr>
<tr><td>DEALER_CODE</td><td>VARCHAR2(64)</td><td>经销商编码</td><td>-</td><td>当前登录经销商</td></tr>
<tr><td>DEALER_NAME</td><td>VARCHAR2(255)</td><td>经销商名称</td><td>经销商</td><td>当前登录经销商</td></tr>
<tr><td>LEGAL_ENTITY_CODE</td><td>VARCHAR2(64)</td><td>法人主体编码</td><td>-</td><td>报名时选择</td></tr>
<tr><td>LEGAL_ENTITY_NAME</td><td>VARCHAR2(255)</td><td>法人主体名称</td><td>法人主体</td><td>报名时选择</td></tr>
<tr><td>TRADING_COMPANY_ID</td><td>NUMBER</td><td>交易公司ID</td><td>-</td><td>报名时选择</td></tr>
<tr><td>TRADING_COMPANY_NAME</td><td>VARCHAR2(255)</td><td>交易公司名称</td><td>-</td><td>报名时选择</td></tr>
<tr><td>CANCEL_REASON</td><td>VARCHAR2(2000)</td><td>取消原因</td><td>-</td><td>取消点将时填写</td></tr>
<tr><td>CRM_ORDER_CODE</td><td>VARCHAR2(64)</td><td>CRM单号</td><td>CRM单号</td><td>推送CRM后回写</td></tr>
<tr><td>REMARK</td><td>VARCHAR2(2000)</td><td>备注</td><td>备注</td><td>手动输入</td></tr>
<tr><td>CREATE_DATE</td><td>TIMESTAMP</td><td>申请时间</td><td>申请时间</td><td>系统自动</td></tr>
<tr><td>CREATED_BY</td><td>VARCHAR2(64)</td><td>创建人</td><td>-</td><td>系统自动</td></tr>
<tr><td>LAST_UPDATED_BY</td><td>VARCHAR2(64)</td><td>最后更新人</td><td>-</td><td>系统自动</td></tr>
<tr><td>LAST_UPDATE_DATE</td><td>TIMESTAMP</td><td>最后更新时间</td><td>-</td><td>系统自动</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="排查SQL">

```sql
-- 查询经销商特训营点将申请
SELECT
  tca.APPLY_CODE    AS 申请编码,
  tca.CAMP_NAME     AS 特训营名称,
  tca.LECTURER      AS 培训师,
  tca.APPLY_STATE   AS 点将状态,
  tca.APPROVAL_STATE AS 审核状态,
  TO_CHAR(tca.PLAN_START_TIME, 'YYYY-MM-DD') AS 开始时间,
  TO_CHAR(tca.PLAN_END_TIME, 'YYYY-MM-DD') AS 结束时间,
  tca.PRE_ORD_LECTURER_DAYS AS 拟点将天数,
  tca.REAL_ORD_LECTURER_DAYS AS 实际点将天数
FROM TRAIN_CAMP_APPLY tca
ORDER BY tca.CREATE_DATE DESC;

-- 查询可取消的特训营申请（提前7天且已签约）
SELECT
  tca.APPLY_CODE    AS 申请编码,
  TO_CHAR(tca.PLAN_START_TIME, 'YYYY-MM-DD') AS 培训开始时间,
  ROUND((tca.PLAN_START_TIME - SYSDATE)) AS 距开始天数
FROM TRAIN_CAMP_APPLY tca
WHERE tca.APPROVAL_STATE = 'fdd_sign'
  AND tca.PLAN_START_TIME > SYSDATE + 7
  AND (tca.CANCEL_APPROVAL_STATE IS NULL
       OR tca.CANCEL_APPROVAL_STATE IN ('reject', 'oa_reject'));

-- 查询待结算前确认的申请
SELECT
  tca.APPLY_CODE    AS 申请编码,
  tca.CAMP_NAME     AS 特训营名称,
  tca.BEFORE_SETTLEMENT_CONFIRM_STATE AS 结算前确认状态
FROM TRAIN_CAMP_APPLY tca
WHERE tca.BEFORE_SETTLEMENT_CONFIRM_STATE = 'to_be_confirm';

-- 查询讲师排期冲突
SELECT
  tca.APPLY_CODE    AS 申请编码,
  tca.LECTURER      AS 培训师,
  TO_CHAR(tca.PLAN_START_TIME, 'YYYY-MM-DD') AS 开始时间,
  TO_CHAR(tca.PLAN_END_TIME, 'YYYY-MM-DD') AS 结束时间
FROM TRAIN_CAMP_APPLY tca
WHERE tca.LECTURER_CODE = :lecturerCode
  AND tca.APPLY_STATE IN ('valid', 'executing')
  AND tca.PLAN_START_TIME <= :planEndTime
  AND tca.PLAN_END_TIME >= :planStartTime;
```
</KbCard>

</div>
</div>
</div>

<div id="faq" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="报错一览表">
<table class="kb-field-tbl">
<thead>
<tr><th>报错信息</th><th>提示节点</th><th>根因与解决方案</th><th>等级</th><th>详细逻辑</th></tr>
</thead>
<tbody>
<tr><td>请选择一条数据</td><td>操作按钮前置校验</td><td>未选择或选择多行；选择一条数据后重试</td><td>提示</td><td>通用单选校验</td></tr>
<tr><td>只有培训时间开始前7天的流程可以发起点将取消申请！</td><td>取消申请按钮</td><td>不满足提前7天；确认planStartTime与当前时间差值&gt;=7</td><td>提示</td><td>timeDiff&gt;=7</td></tr>
<tr><td>该状态单据无法发起取消申请！</td><td>取消申请按钮</td><td>状态校验不通过；确认审批状态和取消状态</td><td>提示</td><td>approvalState非fdd_sign或cancelApprovalState非空且非驳回</td></tr>
<tr><td>当前状态数据无法编辑！</td><td>编辑按钮</td><td>状态非草稿且非驳回；确认申请状态</td><td>提示</td><td>applyState非draft且approvalState非驳回</td></tr>
<tr><td>经销商不能为空</td><td>保存提交</td><td>未选择经销商；选择经销商后保存</td><td>提示</td><td>dealerName必填</td></tr>
<tr><td>特训营不能为空</td><td>保存提交</td><td>未选择特训营；选择特训营后保存</td><td>提示</td><td>campName必填</td></tr>
<tr><td>法人主体不能为空</td><td>保存提交</td><td>未选择法人主体；选择法人主体后保存</td><td>提示</td><td>legalEntityName必填</td></tr>
<tr><td>讲师不能为空</td><td>保存提交</td><td>未选择讲师；选择讲师后保存</td><td>提示</td><td>lecturer必填</td></tr>
<tr><td>拟点将天数不能为空</td><td>保存提交</td><td>未填写拟点将天数；填写后保存</td><td>提示</td><td>preOrdLecturerDays必填</td></tr>
<tr><td>讲师排期冲突</td><td>保存提交</td><td>所选讲师在时间段内已有生效点将；更换讲师或调整时间</td><td>提示</td><td>排期冲突校验</td></tr>
<tr><td>手机号格式不正确</td><td>报名人员明细</td><td>手机号不符合正则；填写正确手机号</td><td>提示</td><td>/^1[3456789]\d&#123;9&#125;$/</td></tr>
<tr><td>请求失败</td><td>接口调用</td><td>后端服务异常；检查后端服务状态</td><td>错误</td><td>接口返回非成功状态</td></tr>
<tr><td>网络异常/接口超时</td><td>任意接口调用</td><td>网络中断或接口响应超时，检查网络及后端超时配置</td><td>error</td><td>axios catch 或 timeout</td></tr>
<tr><td>权限不足</td><td>点击操作按钮</td><td>当前用户无对应按钮权限码，联系管理员授权</td><td>error</td><td>permissionList 校验未通过</td></tr>
<tr><td>数据不存在</td><td>查看/编辑/删除</td><td>申请编码不存在或已删除，检查 APPLY_CODE 有效性</td><td>error</td><td>接口返回数据为空</td></tr>
<tr><td>状态不允许操作</td><td>取消申请/结算确认</td><td>申请状态不在允许操作的状态范围内，检查 APPLY_STATE</td><td>error</td><td>后端校验状态机失败</td></tr>
<tr><td>值集数据不显示</td><td>下拉选项</td><td>值集 MBO.ORDER_LECTURE_STATE 等未配置，检查值集配置</td><td>warning</td><td>lookupCode 查询返回空</td></tr>
<tr><td>报名人员明细不能为空</td><td>保存提交</td><td>未添加报名人员明细，添加后保存</td><td>error</td><td>后端校验明细行非空</td></tr>
<tr><td>拟点将天数必须大于0</td><td>保存提交</td><td>拟点将天数为0或负数，检查 PRE_ORD_LECTURER_DAYS</td><td>error</td><td>后端校验 PRE_ORD_LECTURER_DAYS &gt; 0</td></tr>
</tbody>
</table>
<h4>报错1：请选择一条数据</h4>
<ul><li><strong>触发条件</strong>：点击编辑、删除、取消申请、结算前确认、查看流程等行操作按钮时，未选择数据或选择了多行</li><li><strong>逻辑分析</strong>：前端在执行单选操作前校验选中行数量，若 selectedRows.length ≠ 1 则阻止操作并提示"请选择一条数据"。单选操作需要明确的目标申请，未选择时无法确定操作对象，多选时操作对象不唯一</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         LECTURER AS 培训师,
         APPLY_STATE AS 申请状态,
         APPROVAL_STATE AS 审核状态
  FROM TRAIN_CAMP_APPLY
  ORDER BY CREATE_DATE DESC;
```
<h4>报错2：只有培训时间开始前7天的流程可以发起点将取消申请！</h4>
<ul><li><strong>触发条件</strong>：点击取消申请按钮时，不满足"培训开始前7天"条件</li><li><strong>逻辑分析</strong>：前端计算时间差值 timeDiff = (PLAN_START_TIME - nowTime) / (24*60*60*1000)，校验 timeDiff &gt;= 7（培训开始前至少7天）。校验不通过则提示"只有培训时间开始前7天的流程可以发起点将取消申请！"。此校验确保有充足时间通知讲师和参训人员调整安排，避免临时取消造成资源浪费</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         TO_CHAR(PLAN_START_TIME,'YYYY-MM-DD') AS 培训开始时间,
         APPROVAL_STATE AS 审核状态,
         ROUND(PLAN_START_TIME - SYSDATE) AS 距开始天数
  FROM TRAIN_CAMP_APPLY
  WHERE PLAN_START_TIME < SYSDATE + 7;
```
<h4>报错3：该状态单据无法发起取消申请！</h4>
<ul><li><strong>触发条件</strong>：点击取消申请按钮时，审批状态不为 fdd_sign 或已有进行中的取消申请</li><li><strong>逻辑分析</strong>：前端执行多重校验：①校验 APPROVAL_STATE = 'fdd_sign'（已法大大签约）；②校验 CANCEL_APPROVAL_STATE 为空或为 reject/oa_reject（未取消或已驳回）。任一校验不通过则提示"该状态单据无法发起取消申请！"。确保仅已签约生效且未重复取消的单据可发起取消</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         APPROVAL_STATE AS 审核状态,
         CANCEL_APPROVAL_STATE AS 取消审核状态
  FROM TRAIN_CAMP_APPLY
  WHERE APPROVAL_STATE <> 'fdd_sign'
    OR (CANCEL_APPROVAL_STATE IS NOT NULL
        AND CANCEL_APPROVAL_STATE NOT IN ('reject', 'oa_reject'));
```
<h4>报错4：当前状态数据无法编辑！</h4>
<ul><li><strong>触发条件</strong>：点击编辑按钮时，状态非草稿且非各类驳回状态</li><li><strong>逻辑分析</strong>：前端校验 APPLY_STATE = 'draft'（草稿）或 APPROVAL_STATE 为 reject/oa_reject（各种驳回），任一条件满足才允许编辑。已生效（valid）、审批中（approving）等状态的申请已被下游引用，编辑可能影响数据一致性，故限制编辑。校验不通过提示"当前状态数据无法编辑！"</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         APPLY_STATE AS 申请状态,
         APPROVAL_STATE AS 审核状态
  FROM TRAIN_CAMP_APPLY
  WHERE APPLY_STATE <> 'draft'
    AND APPROVAL_STATE NOT IN ('reject', 'oa_reject');
```
<h4>报错5：经销商不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，dealerName 字段为空</li><li><strong>逻辑分析</strong>：前端表单对 dealerName 字段配置 required 校验，提交前校验经销商是否选择，为空则阻止提交并提示"经销商不能为空"。经销商是点将申请的发起主体，必须明确</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         DEALER_CODE AS 经销商编码,
         DEALER_NAME AS 经销商名称
  FROM TRAIN_CAMP_APPLY
  WHERE DEALER_NAME IS NULL OR DEALER_CODE IS NULL;
```
<h4>报错6：特训营不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，campName 字段为空</li><li><strong>逻辑分析</strong>：前端表单对 campName 字段配置 required 校验，提交前校验特训营是否选择，为空则阻止提交并提示"特训营不能为空"。特训营是点将的参训营次来源，必须明确</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_CODE AS 特训营编码,
         CAMP_NAME AS 特训营名称
  FROM TRAIN_CAMP_APPLY
  WHERE CAMP_NAME IS NULL OR CAMP_CODE IS NULL;
```
<h4>报错7：法人主体不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，legalEntityName 字段为空</li><li><strong>逻辑分析</strong>：前端表单对 legalEntityName 字段配置 required 校验，提交前校验法人主体是否选择，为空则阻止提交并提示"法人主体不能为空"。法人主体用于后续合同签订与结算，必须明确</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         LEGAL_ENTITY_CODE AS 法人主体编码,
         LEGAL_ENTITY_NAME AS 法人主体名称
  FROM TRAIN_CAMP_APPLY
  WHERE LEGAL_ENTITY_NAME IS NULL OR LEGAL_ENTITY_CODE IS NULL;
```
<h4>报错8：讲师不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，lecturer 字段为空</li><li><strong>逻辑分析</strong>：前端表单对 lecturer 字段配置 required 校验，提交前校验讲师是否选择，为空则阻止提交并提示"讲师不能为空"。讲师是点将的核心对象，必须明确被点将人</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         LECTURER_CODE AS 讲师编码,
         LECTURER AS 讲师姓名
  FROM TRAIN_CAMP_APPLY
  WHERE LECTURER IS NULL OR LECTURER_CODE IS NULL;
```
<h4>报错9：拟点将天数不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，preOrdLecturerDays 字段为空</li><li><strong>逻辑分析</strong>：前端表单对 preOrdLecturerDays 字段配置 required 校验，提交前校验拟点将天数是否填写，为空则阻止提交并提示"拟点将天数不能为空"。拟点将天数用于费用计算与讲师排期，必须明确</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         PRE_ORD_LECTURER_DAYS AS 拟点将天数
  FROM TRAIN_CAMP_APPLY
  WHERE PRE_ORD_LECTURER_DAYS IS NULL;
```
<h4>报错10：讲师排期冲突</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，所选讲师在拟定时间段内已有生效点将记录</li><li><strong>逻辑分析</strong>：后端保存前校验讲师排期冲突，查询 TRAIN_CAMP_APPLY 表中同一讲师（LECTURER_CODE 相同）且状态为 valid 或 executing 的点将记录，判断新申请的 [PLAN_START_TIME, PLAN_END_TIME] 与已有记录的时间段是否存在交集。若存在交集则提示"讲师排期冲突"并阻止保存。此校验避免同一讲师在同一时间段被重复点将，保障讲师档期合理性</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT a.APPLY_CODE AS 申请1编码,
         b.APPLY_CODE AS 申请2编码,
         a.LECTURER AS 培训师,
         TO_CHAR(a.PLAN_START_TIME,'YYYY-MM-DD') AS 申请1开始,
         TO_CHAR(a.PLAN_END_TIME,'YYYY-MM-DD') AS 申请1结束,
         TO_CHAR(b.PLAN_START_TIME,'YYYY-MM-DD') AS 申请2开始,
         TO_CHAR(b.PLAN_END_TIME,'YYYY-MM-DD') AS 申请2结束
  FROM TRAIN_CAMP_APPLY a
  JOIN TRAIN_CAMP_APPLY b ON a.APPLY_CODE < b.APPLY_CODE
    AND a.LECTURER_CODE = b.LECTURER_CODE
    AND a.APPLY_STATE IN ('valid', 'executing')
    AND b.APPLY_STATE IN ('valid', 'executing')
    AND a.PLAN_START_TIME <= b.PLAN_END_TIME
    AND a.PLAN_END_TIME >= b.PLAN_START_TIME;
```
<h4>报错11：手机号格式不正确</h4>
<ul><li><strong>触发条件</strong>：报名人员明细中手机号字段不符合正则 /^1[3456789]\d&#123;9&#125;$/</li><li><strong>逻辑分析</strong>：前端对报名人员明细中手机号字段配置正则校验，提交前校验手机号格式是否匹配 /^1[3456789]\d&#123;9&#125;$/（1开头，第二位3-9，共11位数字）。校验不通过则提示"手机号格式不正确"。确保参训人员联系方式有效，便于后续通知</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称
  FROM TRAIN_CAMP_APPLY tca
  WHERE EXISTS (
    SELECT 1 FROM TRAIN_CAMP_APPLY_USER u
    WHERE u.APPLY_CODE = tca.APPLY_CODE
      AND (u.PHONE IS NULL
           OR u.PHONE NOT LIKE '1[3456789]________'
           OR LENGTH(u.PHONE) <> 11
           OR REGEXP_LIKE(u.PHONE, '[^0-9]'))
  );
```
<h4>报错12：请求失败</h4>
<ul><li><strong>触发条件</strong>：调用 mlt/trainCampApply/* 系列接口时，后端返回 HTTP 状态码非 2xx</li><li><strong>逻辑分析</strong>：前端通过 axios 调用后端接口，若响应状态码非 2xx 或网络异常则触发错误回调，统一提示"请求失败"。常见根因包括：mbo-business 微服务未启动或异常、数据库连接失败、SQL 执行超时、后端业务异常未捕获、外部系统（OA/FDD/CRM）调用失败、工作流引擎异常、网络中断等。需检查 mbo-business 微服务运行状态、外部系统连通性、工作流配置、后端日志定位具体异常堆栈</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码,
         CAMP_NAME AS 特训营名称,
         APPLY_STATE AS 申请状态,
         APPROVAL_STATE AS 审核状态,
         CRM_ORDER_CODE AS CRM单号,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间
  FROM TRAIN_CAMP_APPLY
  WHERE LAST_UPDATE_DATE >= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
```
<h4>报错13：网络异常/接口超时</h4>
<ul><li><strong>触发条件</strong>：任意接口调用时，网络中断或接口响应超过 axios timeout 配置</li><li><strong>逻辑分析</strong>：前端 axios 请求未收到响应或响应超时，触发 catch 回调统一提示"请求失败"。常见根因：网络中断、mbo-business 服务假死、数据库慢查询、工作流引擎响应慢等。需检查网络连通性、后端服务负载、数据库性能</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码, CAMP_NAME AS 特训营名称,
         APPLY_STATE AS 申请状态,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间
  FROM TRAIN_CAMP_APPLY
  WHERE LAST_UPDATE_DATE >= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
```
<h4>报错14：权限不足</h4>
<ul><li><strong>触发条件</strong>：点击编辑、删除、取消申请、结算前确认等按钮时，当前用户无对应 permissionList 权限码</li><li><strong>逻辑分析</strong>：前端 Button 组件通过 permissionList 配置权限码，HZERO 框架校验当前用户角色是否包含该权限码，未包含则按钮不可见或禁用。若强制调用接口，后端也会校验权限返回403。需联系管理员配置对应角色权限</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT U.USER_NAME AS 用户名, R.ROLE_NAME AS 角色名, P.PERMISSION_CODE AS 权限码
  FROM SYS_USER U
  LEFT JOIN SYS_USER_ROLE UR ON U.USER_ID = UR.USER_ID
  LEFT JOIN SYS_ROLE R ON UR.ROLE_ID = R.ROLE_ID
  LEFT JOIN SYS_ROLE_PERMISSION RP ON R.ROLE_ID = RP.ROLE_ID
  LEFT JOIN SYS_PERMISSION P ON RP.PERMISSION_ID = P.PERMISSION_ID
  WHERE P.PERMISSION_CODE LIKE '%camp_general%' ORDER BY U.USER_NAME;
```
<h4>报错15：数据不存在</h4>
<ul><li><strong>触发条件</strong>：查看、编辑、删除等操作时，接口返回数据为空或申请编码不存在</li><li><strong>逻辑分析</strong>：前端通过 applyCode 调用详情接口，后端查询 TRAIN_CAMP_APPLY 表无对应记录或记录已逻辑删除，返回空数据。常见根因：申请编码错误、申请已被删除、跨租户查询、数据权限隔离等。需检查 APPLY_CODE 有效性及数据权限</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码, CAMP_NAME AS 特训营名称,
         APPLY_STATE AS 申请状态, DELETE_FLAG AS 删除标记
  FROM TRAIN_CAMP_APPLY
  WHERE DELETE_FLAG = 'Y' OR APPLY_CODE IS NULL;
```
<h4>报错16：状态不允许操作</h4>
<ul><li><strong>触发条件</strong>：点击取消申请、结算前确认等按钮时，申请状态不在允许操作的状态范围内</li><li><strong>逻辑分析</strong>：后端校验申请状态机，如取消申请要求 approvalState 为 fdd_sign 且提前7天、结算确认要求状态为执行完成等。状态不匹配时后端返回业务异常，前端提示后端返回的 message。需检查申请当前状态及操作流程</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码, CAMP_NAME AS 特训营名称,
         APPLY_STATE AS 申请状态, APPROVAL_STATE AS 审核状态,
         CANCEL_APPROVAL_STATE AS 取消审核状态, ERROR_INFO AS 异常问题
  FROM TRAIN_CAMP_APPLY
  WHERE APPLY_STATE NOT IN ('draft','valid','executing','finished')
  ORDER BY CREATE_DATE DESC;
```
<h4>报错17：值集数据不显示</h4>
<ul><li><strong>触发条件</strong>：查询条件或列表中申请状态、审核状态等下拉选项为空</li><li><strong>逻辑分析</strong>：前端通过 lookupCode 查询值集 MBO.ORDER_LECTURE_STATE、MBO.APPLY_APPROVAL_STATE 等，值集未配置或未启用则下拉选项为空。需在值集管理页面配置对应值集</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT LOOKUP_CODE AS 值集编码, LOOKUP_VALUE_CODE AS 值编码,
         LOOKUP_VALUE_NAME AS 值名称, ENABLE_FLAG AS 启用标记
  FROM SYS_LOOKUP_VALUE
  WHERE LOOKUP_CODE IN ('MBO.ORDER_LECTURE_STATE','MBO.APPLY_APPROVAL_STATE','MBO.CANCEL_APPROVAL_STATE')
    AND ENABLE_FLAG = 'N' ORDER BY LOOKUP_CODE;
```
<h4>报错18：报名人员明细不能为空</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，报名人员明细行数据为空</li><li><strong>逻辑分析</strong>：后端校验报名人员明细行非空，特训营点将需明确报名人员列表。若明细行为空则返回业务异常。需添加报名人员明细后保存</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT A.APPLY_CODE AS 申请编码, A.CAMP_NAME AS 特训营名称,
         COUNT(B.LINE_ID) AS 报名人员数
  FROM TRAIN_CAMP_APPLY A
  LEFT JOIN TRAIN_CAMP_APPLY_LINE B ON A.APPLY_CODE = B.APPLY_CODE
  WHERE A.APPLY_STATE IN ('draft','valid')
  GROUP BY A.APPLY_CODE, A.CAMP_NAME
  HAVING COUNT(B.LINE_ID) = 0;
```
<h4>报错19：拟点将天数必须大于0</h4>
<ul><li><strong>触发条件</strong>：保存或保存并提交时，PRE_ORD_LECTURER_DAYS 字段非正数</li><li><strong>逻辑分析</strong>：后端校验 PRE_ORD_LECTURER_DAYS &gt; 0，拟点将天数用于费用计算与讲师排期，必须为正数。若为0或负数则返回业务异常。需检查拟点将天数输入是否正确</li><li><strong>排查SQL</strong>：</li></ul>

```sql
SELECT APPLY_CODE AS 申请编码, CAMP_NAME AS 特训营名称,
         PRE_ORD_LECTURER_DAYS AS 拟点将天数
  FROM TRAIN_CAMP_APPLY
  WHERE PRE_ORD_LECTURER_DAYS IS NOT NULL AND PRE_ORD_LECTURER_DAYS <= 0;
```
</KbCard>

<KbCard title="常见问题">
<p><strong>Q1：经销商看不到自己发起的特训营记录？</strong></p>
<p>A：检查该经销商是否正确关联当前登录用户，确认DEALER_CODE与当前登录用户关联的经销商一致。</p>
<p><strong>Q2：特训营状态未更新？</strong></p>
<p>A：状态由审批流程驱动，需联系审批人确认审批进度。</p>
<p><strong>Q3：pageForDealer接口返回数据为空？</strong></p>
<p>A：确认当前登录用户已关联经销商角色，且该经销商存在发起记录。</p>
<p><strong>Q4：申请点将弹窗无特训营数据？</strong></p>
<p>A：检查campApplyApi.campSelectPage接口，确认有生效特训营。</p>
<p><strong>Q5：报名保存失败？</strong></p>
<p>A：检查ApplyInfo组件数据，法人主体和交易公司是否选择。</p>
<p><strong>Q6：结算前确认按钮不显示？</strong></p>
<p>A：检查beforeSettlementConfirmState是否为to_be_confirm。</p>
</KbCard>

</div>
</div>
</div>

<div id="changelog" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="更新记录">
<table class="kb-field-tbl">
<thead>
<tr><th>日期</th><th>提交ID</th><th>提交人</th><th>提交内容</th></tr>
</thead>
<tbody>
<tr><td>2025-11-12</td><td>-</td><td>hfy</td><td>初始创建</td></tr>
<tr><td>2026-08-03</td><td>-</td><td>AI</td><td>初始创建</td></tr>
<tr><td>2026-08-28</td><td>-</td><td>AI</td><td>完整重写，补充业务流程、界面模块、数据库表、FAQ等</td></tr>
<tr><td>2026-07-31</td><td>-</td><td>AI</td><td>对比网站补充定义、详细逻辑、数据库表字段等</td></tr>
<tr><td>2026-08-30</td><td>-</td><td>AI</td><td>按skill规范格式重写，调整为经销商发起视角，规范上下游、重点逻辑、弹窗、按钮、校验、状态机、更新记录等格式</td></tr>
</tbody>
</table>
</KbCard>
</div>
</div>
</div>

<div id="history" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="历史排查记录">

<!-- 空白:待补充 -->

</KbCard>
</div>
</div>
</div>