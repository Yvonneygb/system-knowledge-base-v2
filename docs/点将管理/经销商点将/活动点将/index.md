<BreadcrumbTabs />

<div id="biz-intro" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<!-- 一、业务是什么 -->
<div class="kl-card">
  <div class="biz-kl-hdr">
    <span class="biz-tag" style="background:rgba(124,58,237,0.08);color:#7C3AED;border-color:rgba(124,58,237,0.18);"> 定义</span>
    <h2>经销商侧的活动点将是什么</h2>
    <p>经销商查看策划师支持本商户营销活动的点将安排并确认参与</p>
  </div>
  <div class="biz-2col-inner">
    <div class="kl-col-box">
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;">
        <div style="width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#7c3aed,#6d28d9);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="2" y="3" width="5" height="5" rx="1" stroke="white" stroke-width="1.5"/><path d="M3 6.5L4.5 8L7 5" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M9 5H14" stroke="white" stroke-width="1.5" stroke-linecap="round"/><rect x="2" y="10" width="5" height="3" rx="1" stroke="white" stroke-width="1.5"/><path d="M9 10H14" stroke="white" stroke-width="1.5" stroke-linecap="round"/><path d="M9 13H13" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
        <h4 style="font-size:1rem;font-weight:800;color:#1F2937;margin:0;">解决什么问题</h4>
      </div>
      <p style="font-size:0.78rem;font-weight:600;color:#6B7280;margin:0 0 10px;">让经销商掌握活动支持资源的到位情况</p>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F5F3FF;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>活动靠专业策划</strong> — 促销、样板房、爆破等活动需要策划师到场支持，资源由总部或大区统一点派。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F5F3FF;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>活动筹备要对齐</strong> — 经销商需按策划师到场时间安排场地、物料与人员，避免活动当天资源脱节。</div>
        </div>
      </div>
    </div>
    <div class="kl-col-box alt">
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;">
        <div style="width:40px;height:40px;border-radius:10px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#10b981,#059669);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="2" width="10" height="12" rx="1.5" stroke="white" stroke-width="1.5"/><path d="M5 6H11M5 9H9" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
        <h4 style="font-size:1rem;font-weight:800;color:#1F2937;margin:0;">能看到什么</h4>
      </div>
      <p style="font-size:0.78rem;font-weight:600;color:#6B7280;margin:0 0 10px;">一条活动点将的关键信息</p>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F0FDF4;border-radius:8px;border:1px solid #D1FAE5;">
          <div style="font-size:.75rem;"><strong>活动主题与时间</strong> — 做什么活动、活动起止时间。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#F0FDF4;border-radius:8px;border:1px solid #D1FAE5;">
          <div style="font-size:.75rem;"><strong>活动地点与策划师</strong> — 活动在哪办、由哪位策划师支持。</div>
        </div>
        <div style="display:flex;align-items:flex-start;gap:8px;padding:8px 10px;background:#EDE9FE;border-radius:8px;">
          <div style="font-size:.75rem;"><strong>当前状态</strong> — 该点将处在申请、审批还是已生效待执行阶段。</div>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- 二、流程 -->
<div class="kl-card">
  <div class="biz-kl-hdr">
    <span class="biz-tag" style="background:rgba(124,58,237,0.08);color:#7C3AED;border-color:rgba(124,58,237,0.18);"> 流程</span>
    <h2>经销商侧的处理链路</h2>
    <p>接到通知 → 查看活动安排 → 确认参与 → 活动执行留痕</p>
  </div>
  <div class="biz-steps">
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#7C3AED,#6D28D9);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 2C5.5 2 4 3.8 4 6V9L2.5 11.5H13.5L12 9V6C12 3.8 10.5 2 8 2Z" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M6.5 11.5C6.5 12.6 7.2 13.5 8 13.5C8.8 13.5 9.5 12.6 9.5 11.5" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
      <h5>接收点将通知</h5>
      <small>上游发起活动点将<br>后经销商侧可见</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#F59E0B,#FBBF24);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><rect x="3" y="2" width="10" height="12" rx="1.5" stroke="white" stroke-width="1.5"/><path d="M5 6H11M5 9H11" stroke="white" stroke-width="1.5" stroke-linecap="round"/></svg></div>
      <h5>查看活动详情</h5>
      <small>核对主题·时间<br>地点与策划师</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#10B981,#34D399);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><circle cx="8" cy="8" r="6" stroke="white" stroke-width="1.5"/><path d="M5 8L7 10L11 6" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h5>确认参与状态</h5>
      <small>反馈参与门店<br>与筹备安排</small>
    </div>
    <div class="biz-step-arrow">&rarr;</div>
    <div class="biz-step-item">
      <div class="biz-step-circle" style="background:linear-gradient(135deg,#3B82F6,#60A5FA);"><svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M2 12L6 8L10 10L14 4" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg></div>
      <h5>活动执行留痕</h5>
      <small>形成活动执行记录<br>与点将状态更新</small>
    </div>
  </div>
  <div class="kl-tip" style="margin-top:14px;">
    <strong>数据边界：</strong>本页为经销商视角的只读查询，仅展示当前登录经销商自身的活动点将，不能新建或修改；活动点将的发起与审批在总部侧完成。
  </div>
</div>
</div>
</div>
</div>

<div id="biz-flow" style="display:none;">
<div class="tab-pad">
<div class="bf-truth-flow">
  <h4 class="bf-main-title">活动点将 — 全链路流程图</h4>
  <p class="bf-main-sub">开始 → 接收被点将通知 → ★查看并确认活动点将★ → 确认/反馈参与状态 → 结束</p>
  <div class="bf-fc-svg-wrap">
    <svg class="bf-fc-svg" style="max-height:none;" viewBox="0 0 1200 580" xmlns="http://www.w3.org/2000/svg">
      <defs>
        <marker id="arr-green" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#16A34A"/></marker>
        <marker id="arr-gray" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#9CA3AF"/></marker>
        <marker id="arr-blue" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#3B82F6"/></marker>
        <marker id="arr-red" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><polygon points="0,0 10,5 0,10" fill="#EF4444"/></marker>
        <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%"><feDropShadow dx="0" dy="2" stdDeviation="3" flood-color="#000" flood-opacity="0.15"/></filter>
      </defs>
      <rect x="50" y="20" width="1100" height="95" rx="8" fill="#EFF6FF" stroke="#3B82F6" stroke-width="1.5" stroke-dasharray="6,4"/>
      <text x="600" y="42" text-anchor="middle" fill="#1D4ED8" font-size="13" font-weight="600">上游支撑</text>
      <rect x="270" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="345" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">总部/大区/策划师</text>
      <rect x="440" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="515" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">经销商主档</text>
      <rect x="610" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="685" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">策划师主档</text>
      <rect x="780" y="56" width="150" height="34" rx="5" fill="#FFFFFF" stroke="#3B82F6" stroke-width="1.2"/>
      <text x="855" y="78" text-anchor="middle" fill="#1D4ED8" font-size="11" font-weight="600">活动主数据</text>
      <line x1="235" y1="115" x2="235" y2="150" stroke="#3B82F6" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr-blue)"/>
      <rect x="195" y="150" width="80" height="44" rx="6" fill="#FAF5FF" stroke="#9333EA" stroke-width="1.5" stroke-dasharray="5,3"/>
      <text x="235" y="177" text-anchor="middle" fill="#7C3AED" font-size="13" font-weight="600">开始</text>
      <line x1="235" y1="194" x2="235" y2="230" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="155" y="230" width="160" height="54" rx="6" fill="#16A34A" stroke="#15803D" stroke-width="2" filter="url(#shadow)"/>
      <text x="235" y="254" text-anchor="middle" fill="#FFFFFF" font-size="13" font-weight="700">★查看并确认活动点将★</text>
      <text x="235" y="272" text-anchor="middle" fill="#DCFCE7" font-size="10">查看主题/时间/地点/策划师</text>
      <line x1="235" y1="284" x2="235" y2="310" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="150" y="310" width="170" height="40" rx="6" fill="#F0FDF4" stroke="#16A34A" stroke-width="2"/>
      <text x="235" y="335" text-anchor="middle" fill="#166534" font-size="13" font-weight="600">确认/反馈参与状态</text>
      <line x1="235" y1="350" x2="235" y2="370" stroke="#16A34A" stroke-width="2" marker-end="url(#arr-green)"/>
      <rect x="180" y="370" width="110" height="40" rx="6" fill="#FAF5FF" stroke="#9333EA" stroke-width="1.5" stroke-dasharray="5,3"/>
      <text x="235" y="395" text-anchor="middle" fill="#7C3AED" font-size="13" font-weight="600">结束</text>
      <line x1="235" y1="410" x2="235" y2="460" stroke="#16A34A" stroke-width="1.5" stroke-dasharray="4,3" marker-end="url(#arr-green)"/>
      <rect x="50" y="460" width="1100" height="95" rx="8" fill="#F0FDF4" stroke="#16A34A" stroke-width="1.5" stroke-dasharray="6,4"/>
      <text x="600" y="482" text-anchor="middle" fill="#166534" font-size="13" font-weight="600">下游影响</text>
      <rect x="355" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="430" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">参与确认回执</text>
      <rect x="525" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="600" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">活动执行记录</text>
      <rect x="695" y="500" width="150" height="36" rx="5" fill="#FFFFFF" stroke="#16A34A" stroke-width="1.2"/>
      <text x="770" y="523" text-anchor="middle" fill="#166534" font-size="11" font-weight="600">点将状态更新</text>
    </svg>
  </div>
  <div class="bf-fc-legend">
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-green"></span> 主流程步骤</span>
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-purple"></span> 开始/结束</span>
    <span class="bf-fc-legend-item"><span class="bf-fc-dot bf-fc-dot-blue"></span> 上游支撑</span>
    <span class="bf-fc-legend-item"><span style="display:inline-block;width:22px;height:2px;background:#EF4444;"></span> 审批拒绝/驳回</span>
  </div>
</div>
</div>
</div>

<div id="key-logic" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard num="1" title="重点逻辑1：经销商发起视角">
<ul><li><strong>业务意义</strong>：本菜单由经销商主动发起活动点将申请，经销商自主选择门店、讲师与活动信息。</li><li><strong>具体逻辑描述</strong>：通过<code>dealer</code>路径接口（dealerPage、dealerSave、dealerSaveAndSubmit）实现经销商发起，后端自动按当前登录经销商过滤数据并写入DEALER_CODE、DEALER_NAME，APPLY_TYPE_ONE='activity'，APPLY_TYPE_TWO='apply'。</li></ul>
</KbCard>

<KbCard num="2" title="重点逻辑2：数据范围隔离">
<ul><li><strong>业务意义</strong>：经销商仅能查看和操作自己发起的活动点将记录，避免跨经销商数据泄露。</li><li><strong>具体逻辑描述</strong>：通过<code>dealer</code>路径接口自动按当前登录经销商过滤，前端无需传经销商参数。</li></ul>
</KbCard>

<KbCard num="3" title="重点逻辑3：applyCode自动生成">
<ul><li><strong>业务意义</strong>：保证活动点将申请编码全局唯一、可追溯，作为主键贯穿全链路。</li><li><strong>具体逻辑描述</strong>：提交申请时系统自动生成唯一编码applyCode，写入APPLY_CODE字段。</li></ul>
</KbCard>

<KbCard num="4" title="重点逻辑4：取消申请校验">
<ul><li><strong>业务意义</strong>：约束取消申请的时间窗口与状态，避免临近活动开始取消影响讲师排期与活动筹备。</li><li><strong>具体逻辑描述</strong>：时间校验 timeDiff &gt;= 7（提前7天），基于activityStartDate；状态校验 approvalState === 'fdd_sign'（已法大大签约）；取消状态校验 cancelApprovalState 为空或 reject/oa_reject；点将状态校验 orderLectureState !== 'end'（未结束）。校验通过弹窗填写取消原因，调用<code>POST cancelApply</code>接口。</li></ul>
</KbCard>

<KbCard num="5" title="重点逻辑5：编辑再发起校验">
<ul><li><strong>业务意义</strong>：仅草稿或各类驳回状态可编辑，避免已生效或审批中单据被误改。</li><li><strong>具体逻辑描述</strong>：行操作"编辑再发起"校验状态：orderLectureState === 'draft'（草稿）或 approvalState 为 reject/oa_reject/fdd_reject（各种驳回）。校验通过跳转编辑详情页，否则提示"当前状态数据无法编辑！"。</li></ul>
</KbCard>

<KbCard num="6" title="重点逻辑6：讲师排期冲突校验">
<ul><li><strong>业务意义</strong>：避免同一讲师在同一时间段被重复点将，保障讲师档期合理性。</li><li><strong>具体逻辑描述</strong>：保存时校验所选讲师在拟定开始时间至拟定结束时间区间内不存在已生效点将记录，存在冲突则提示并阻止保存。</li></ul>
</KbCard>

<KbCard num="7" title="重点逻辑7：点将费计算">
<ul><li><strong>业务意义</strong>：明确活动点将费用计算方式，为结算提供依据。</li><li><strong>具体逻辑描述</strong>：点将费用 = 被点将人点将牌价 × 具体折扣。最终点将价格由系统生成结算确认书由经销商确认。</li></ul>
</KbCard>

</div>
</div>
</div>

<div id="detail-logic" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="界面模块">
<h4>前端路由</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>路由</th><th>用途</th></tr>
</thead>
<tbody>
<tr><td>`/general/distributorGeneral/distributorGeneralActivity/list`</td><td>活动点将列表页</td></tr>
<tr><td>`/general/distributorGeneral/distributorGeneralActivity/detail/:applyCode/:type`</td><td>活动点将详情页</td></tr>
</tbody>
</table>
<h4>API接口</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>接口路径</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>`mlt/activityApply/dealerPage`</td><td>经销商分页查询（pageForDealer）</td></tr>
<tr><td>`mlt/activityApply/dealerSave`</td><td>经销商保存</td></tr>
<tr><td>`mlt/activityApply/dealerSaveAndSubmit`</td><td>经销商保存并提交</td></tr>
<tr><td>`mlt/activityApply/delete`</td><td>删除申请</td></tr>
<tr><td>`mlt/activityApply/detail`</td><td>查询申请详情</td></tr>
<tr><td>`mlt/activityApply/page`</td><td>分页查询</td></tr>
<tr><td>`mlt/activityApply/activityApplyApproval`</td><td>活动点将审批</td></tr>
<tr><td>`mlt/activityApply/cancelApplyApproval`</td><td>取消审批</td></tr>
<tr><td>`mlt/activityApply/pushOa`</td><td>同步OA</td></tr>
<tr><td>`mlt/activityApply/pushFdd`</td><td>同步FDD</td></tr>
<tr><td>`mlt/activityApply/pushCrm`</td><td>同步CRM</td></tr>
<tr><td>`mlt/activityApply/specialCancel`</td><td>特殊取消</td></tr>
<tr><td>`mlt/activityApply/cancelApply`</td><td>发起取消申请</td></tr>
<tr><td>`mlt/activityApply/settleForPlan`</td><td>按计划结算</td></tr>
<tr><td>`mlt/activityApply/startSettle`</td><td>发起结算</td></tr>
<tr><td>`mlt/activityApply/startExecute`</td><td>开始执行</td></tr>
<tr><td>`mlt/activityApply/endExecute`</td><td>结束执行</td></tr>
<tr><td>`mlt/activityApply/confirmEndExecute`</td><td>确认结束执行</td></tr>
</tbody>
</table>
<h4>内置审批</h4>
<table class="kb-field-tbl">
<thead>
<tr><th>审批流编码</th><th>说明</th><th>触发时机</th></tr>
</thead>
<tbody>
<tr><td>activityApplyApproval</td><td>活动点将申请审批</td><td>保存并提交时触发</td></tr>
</tbody>
</table>
<h4>查询栏字段</h4>
<blockquote>数据来源：<code>stores/listConfig.tsx</code> 的 <code>listDSProps.queryFields</code>，列表页使用 <code>TableQueryBarType.professionalBar</code> 专业查询栏。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>申请编码</td><td>APPLY_CODE</td><td>TextField</td><td>申请编码模糊查询</td><td>始终显示</td><td>手动输入，labelWidth=100</td></tr>
<tr><td>申请审批状态</td><td>APPROVAL_STATE</td><td>Select</td><td>审批状态筛选</td><td>始终显示</td><td>值集MBO.APPLY_APPROVAL_STATE，labelWidth=100</td></tr>
</tbody>
</table>
<h4>列表展示字段</h4>
<blockquote>数据来源：<code>stores/listConfig.tsx</code> 的 <code>listDScolumns</code>，列表 DataSet 使用 <code>dataKey=data.content</code>、<code>totalKey=data.totalElements</code>，多选 <code>DataSetSelection.multiple</code>，接口 <code>activityApplyApi.dealerPage</code>。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>申请编码</td><td>APPLY_CODE</td><td>TextField</td><td>申请唯一编码</td><td>始终显示</td><td>左锁定列，width=160</td></tr>
<tr><td>点将类型</td><td>ORDER_LECTURE_TYPE</td><td>Select</td><td>点将类型</td><td>始终显示</td><td>值集MBO.LECTURER_TYPE，width=130</td></tr>
<tr><td>活动名称</td><td>ACTIVITY_NAME</td><td>Button(link)</td><td>活动名称，可点击跳转详情</td><td>始终显示</td><td>左锁定列，width=160，点击触发handleShowDetail</td></tr>
<tr><td>被点将人</td><td>LECTURER_NAME</td><td>TextField</td><td>被点将讲师姓名</td><td>始终显示</td><td>后端返回</td></tr>
<tr><td>点将状态</td><td>ORDER_LECTURE_STATE</td><td>Select</td><td>点将流程状态</td><td>始终显示</td><td>值集MBO.ORDER_LECTURE_STATE，width=130</td></tr>
<tr><td>申请审核状态</td><td>APPROVAL_STATE</td><td>Select</td><td>审批状态</td><td>始终显示</td><td>值集MBO.APPLY_APPROVAL_STATE，width=130</td></tr>
<tr><td>结束确认状态</td><td>END_CONFIRM_STATE</td><td>Select</td><td>结束确认状态</td><td>始终显示</td><td>值集MBO.END_COMFIRM_STATE，width=130</td></tr>
<tr><td>申请人</td><td>DISTRIBUTOR_NAME</td><td>TextField</td><td>申请人（经销商名称）</td><td>始终显示</td><td>后端返回，width=130</td></tr>
<tr><td>取消申请审核状态</td><td>CANCEL_APPROVAL_STATE</td><td>Select</td><td>取消审批状态</td><td>始终显示</td><td>值集MBO.CANCEL_APPROVAL_STATE，width=130</td></tr>
<tr><td>结算确认状态</td><td>SETTLEMENT_CONFIRM_STATE</td><td>Select</td><td>结算确认状态</td><td>始终显示</td><td>值集MBO.SETTLEMENT_COMFIRM_STATE，width=130</td></tr>
<tr><td>CRM单号</td><td>CRM_ORDER_CODE</td><td>TextField</td><td>CRM订单编号</td><td>始终显示</td><td>推送CRM后返回，width=130</td></tr>
<tr><td>申请时间</td><td>CREATE_DATE</td><td>TextField</td><td>申请创建时间</td><td>始终显示</td><td>后端返回，width=150</td></tr>
<tr><td>更新时间</td><td>UPDATE_DATE</td><td>TextField</td><td>最后更新时间</td><td>始终显示</td><td>后端返回，width=150</td></tr>
<tr><td>活动开始时间</td><td>ACTIVITY_START_DATE</td><td>TextField</td><td>活动开始时间</td><td>始终显示</td><td>后端返回，width=150</td></tr>
<tr><td>活动结束时间</td><td>ACTIVITY_END_DATE</td><td>TextField</td><td>活动结束时间</td><td>始终显示</td><td>后端返回，width=150</td></tr>
<tr><td>拟点将天数</td><td>PRE_ORD_LECTURER_DAYS</td><td>NumberField</td><td>计划点将天数</td><td>始终显示</td><td>申请时填写，width=130</td></tr>
<tr><td>实际点将天数</td><td>REAL_ORD_LECTURER_DAYS</td><td>NumberField</td><td>实际点将天数</td><td>始终显示</td><td>执行环节填写，width=130</td></tr>
<tr><td>操作</td><td>-</td><td>Button(link)</td><td>编辑再发起/查看流程/删除</td><td>始终显示</td><td>右锁定列，width=180</td></tr>
</tbody>
</table>
<h4>详情页字段</h4>
<blockquote>数据来源：<code>stores/detailConfig.tsx</code> 定义两个 DataSet：<code>generalInfoFormDSProps</code>（点将信息表单）与 <code>activityInfoFormDSProps</code>（活动信息表单）。详情页由 <code>components/ActivityApplyDetail/index.tsx</code> 渲染，分三个 ContentCard：点将信息、活动信息、结算信息（仅查看模式显示）。<code>isDetail = (type === 'view')</code>，编辑模式字段 required=true、disabled=false，查看模式 disabled=true。</blockquote>
<h5>表单1：点将信息（generalInfoFormDS）</h5>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>讲师</td><td>LECTURER_NAME</td><td>TextField</td><td>被点将讲师姓名</td><td>始终显示</td><td>disabled，由申请点将弹窗选择讲师后带入state</td></tr>
<tr><td>部门</td><td>ORG_NAME</td><td>TextField</td><td>讲师所属部门</td><td>始终显示</td><td>disabled，由讲师信息带入</td></tr>
<tr><td>讲师级别</td><td>LECTURER_LEVEL</td><td>Select</td><td>讲师级别</td><td>始终显示</td><td>disabled，值集MBO.ACTIVITY_LECTURER_LEVEL，由讲师信息带入</td></tr>
<tr><td>点将费</td><td>PRICE</td><td>TextField</td><td>讲师报价（点将费单价）</td><td>始终显示</td><td>disabled，由讲师信息带入（lecturerPrice字段）</td></tr>
<tr><td>拟点将时间</td><td>PRE_START_DATE/PRE_END_DATE</td><td>DatePicker(range)</td><td>拟定点将时间范围</td><td>始终显示</td><td>range=true，新增/编辑必填，查看disabled。label随模式变化：查看"拟点将时间"，新增/编辑"点将时间"。onChange触发点将费小计计算</td></tr>
<tr><td>实际点将时间</td><td>REAL_START_DATE/REAL_END_DATE</td><td>DatePicker(range)</td><td>实际点将时间范围</td><td>仅查看模式显示</td><td>range=true，disabled，详情加载时由realStartDate/realEndDate回填</td></tr>
<tr><td>点将费小计</td><td>SETTLEMENT_AMOUNT</td><td>TextField</td><td>点将费小计金额</td><td>始终显示</td><td>disabled，自动计算=(拟点将天数)*点将费，拟点将天数=preDate[1]-preDate[0]+1</td></tr>
<tr><td>签订人</td><td>SIGNER_ID/SIGNER_NAME</td><td>Select(object)</td><td>签订人</td><td>haveSigner=true时显示</td><td>options=signerDS，textField=name，valueField=accountId，必填。根据legalEntityName查询签订人列表</td></tr>
</tbody>
</table>
<h5>表单2：活动信息（activityInfoFormDS）</h5>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>经销商编码</td><td>DISTRIBUTOR_CODE</td><td>TextField</td><td>经销商编码</td><td>始终显示</td><td>disabled，placeholder="提交/保存后自动生成"</td></tr>
<tr><td>经销商名称</td><td>DISTRIBUTOR_NAME</td><td>TextField</td><td>经销商名称</td><td>始终显示</td><td>disabled，placeholder="提交/保存后自动生成"</td></tr>
<tr><td>所属运营中心</td><td>OP_CENTER_CODE/OP_CENTER_NAME</td><td>Select(object)</td><td>所属运营中心</td><td>始终显示</td><td>值集MBO.OP_CENTER，新增/编辑必填，提交时拆为opCenterCode(value)+opCenterName(meaning)</td></tr>
<tr><td>交易公司</td><td>TRADING_COMPANY_NAME</td><td>TextField+Icon(search)</td><td>交易公司</td><td>始终显示</td><td>新增/编辑必填，readOnly+搜索图标，点击弹出交易公司选择弹窗。查看模式纯TextField</td></tr>
<tr><td>开票账户</td><td>LEGAL_ENTITY_NAME</td><td>TextField</td><td>开票账户（法人单位名称）</td><td>始终显示</td><td>disabled，placeholder="选择交易公司后自动带出"，选交易公司后自动写入legalEntityName/legalEntityCode</td></tr>
<tr><td>活动类型</td><td>ACTIVITY_TYPE</td><td>Select</td><td>活动类型</td><td>始终显示</td><td>值集MBO.ORD_ACTIVITY_TYPE，新增/编辑必填</td></tr>
<tr><td>活动名称</td><td>ACTIVITY_NAME</td><td>TextField</td><td>活动名称</td><td>始终显示</td><td>新增/编辑必填，手动输入</td></tr>
<tr><td>门店名称</td><td>STORE_NAME</td><td>TextField+Icon(search)</td><td>门店名称</td><td>始终显示</td><td>新增/编辑必填，readOnly+搜索图标，点击弹出门店选择弹窗。查看模式纯TextField</td></tr>
<tr><td>活动区域</td><td>ACTIVITY_AREA</td><td>TextField</td><td>活动区域</td><td>始终显示</td><td>新增/编辑必填，手动输入</td></tr>
<tr><td>门店地址</td><td>STORE_AREA</td><td>TextField</td><td>门店地址（省/市/区）</td><td>始终显示</td><td>disabled，placeholder="请选择门店后自动带出"，选门店后自动拼接为"省/市/区"。下方跟随显示storeAddress（门店详细地址，disabled）</td></tr>
<tr><td>活动时间</td><td>ACTIVITY_START_DATE/ACTIVITY_END_DATE</td><td>DatePicker(range)</td><td>活动时间范围</td><td>始终显示</td><td>range=true，新增/编辑必填，提交时拆为activityStartDate/activityEndDate(YYYY-MM-DD)</td></tr>
<tr><td>参与门店数</td><td>APPLY_STORE_NUMBER</td><td>NumberField</td><td>参与门店数</td><td>始终显示</td><td>新增/编辑必填，手动输入</td></tr>
<tr><td>投资预算金额</td><td>BUDGET_AMOUNT</td><td>NumberField</td><td>投资预算金额</td><td>始终显示</td><td>新增/编辑必填，手动输入</td></tr>
<tr><td>活动简介</td><td>ACTIVITY_INTRODUCTION</td><td>TextArea</td><td>活动简介</td><td>始终显示</td><td>新增/编辑必填，多行文本</td></tr>
</tbody>
</table>
<h5>表单3：结算信息（settlementFormDS，仅查看模式显示）</h5>
<blockquote>数据来源：<code>specialTrainingCamp/campGeneral/components/CampApplyDetail/config.tsx</code> 的 <code>settlementFormDSProps</code>。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>数据库列名</th><th>组件</th><th>业务释义</th><th>显隐条件</th><th>取值/赋值逻辑</th></tr>
</thead>
<tbody>
<tr><td>CRM单号</td><td>CRM_ORDER_CODE</td><td>TextField</td><td>CRM订单编号</td><td>仅查看模式显示</td><td>disabled，后端返回</td></tr>
<tr><td>点将费小计</td><td>SETTLEMENT_AMOUNT</td><td>TextField</td><td>点将费小计金额</td><td>仅查看模式显示</td><td>disabled，后端返回</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="选择弹窗">
<h4>弹窗1：申请点将弹窗（讲师选择，列表页头部"申请点将"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleApply</code> + <code>stores/listConfig.tsx</code> 的 <code>applyListDSProps</code>/<code>applyListDScolumns</code>。接口 <code>campApi.lecturerList</code>，固定传参 <code>pageType='activity'</code>。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>lecturerName</td><td>查询栏输入</td><td>讲师姓名模糊查询</td></tr>
<tr><td>lecturerLevel</td><td>查询栏选择</td><td>讲师级别，值集MBO.ACTIVITY_LECTURER_LEVEL</td></tr>
<tr><td>pageType</td><td>固定值</td><td>固定'activity'，标识活动点将</td></tr>
</tbody>
</table>
<p>列表列：序号、讲师姓名（点击跳转讲师档案详情）、讲师级别、报价、讲师类型（值集MBO.LECTURER_TYPE）、负责区域、标签、操作（查看日程/选择）。</p>
<p>行操作：</p>
<ul><li>查看日程：弹出讲师日历组件 <code>LecturerCalendar</code>，传入rowData和haveLecturerCode=true</li><li>选择：调用 <code>handleChoose</code>，<code>openTab</code> 跳转详情页 <code>/detail/null/add</code>，state携带讲师信息</li></ul>
<p>查询SQL：</p>

```sql
SELECT
  la.ID              AS 讲师档案ID,
  la.LECTURER_NAME   AS 讲师姓名,
  la.LECTURER_LEVEL  AS 讲师级别,
  la.PRICE           AS 报价,
  la.LECTURER_TYPE   AS 讲师类型,
  la.COVER_AREA      AS 负责区域,
  la.LECTURER_LABEL  AS 标签
FROM MBO_TRAIN_LECTURER_ARCHIVE la
WHERE la.PAGE_TYPE = 'activity'
  AND (:lecturerName IS NULL OR la.LECTURER_NAME LIKE '%' || :lecturerName || '%')
  AND (:lecturerLevel IS NULL OR la.LECTURER_LEVEL = :lecturerLevel)
ORDER BY la.CREATE_DATE DESC;
```
<h4>弹窗2：交易公司选择弹窗（详情页"交易公司"字段搜索图标触发）</h4>
<blockquote>数据来源：<code>components/ActivityApplyDetail/index.tsx</code> 的 <code>handleShowCompanyList</code> + <code>distributorGeneralCamp/components/ApplyInfo/config.tsx</code> 的 <code>companyListDSProps</code>/<code>companyListDSColumns</code>。接口 <code>campApplyApi.companyList</code>，单选，入参 distributorCode（取自当前用户 additionInfo.DC）。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>distributorCode</td><td>当前用户 additionInfo.DC</td><td>经销商编码</td></tr>
</tbody>
</table>
<p>列表列：开票单位编码(legalEntityCode)、开票单位名称(legalEntityName)、交易公司ID(tradingCompanyId)、交易公司名称(tradingCompanyName)。</p>
<p>选择后回写：tradingCompanyName、tradingCompanyId、legalEntityName、legalEntityCode。</p>
<p>查询SQL：</p>

```sql
SELECT
  tc.LEGAL_ENTITY_CODE    AS 开票单位编码,
  tc.LEGAL_ENTITY_NAME    AS 开票单位名称,
  tc.TRADING_COMPANY_ID   AS 交易公司ID,
  tc.TRADING_COMPANY_NAME AS 交易公司名称
FROM MBO_TRADING_COMPANY tc
WHERE tc.DISTRIBUTOR_CODE = :distributorCode;
```
<h4>弹窗3：门店选择弹窗（详情页"门店名称"字段搜索图标触发）</h4>
<blockquote>数据来源：<code>components/ActivityApplyDetail/index.tsx</code> 的 <code>handleShowStoreList</code> + <code>distributorGeneralCamp/components/ApplyInfo/config.tsx</code> 的 <code>storeDS</code>/<code>storeListDSColumns</code>。接口 <code>campApi.storePage</code>，单选，autoQuery=true。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>terminalCode</td><td>查询栏输入</td><td>门店编码</td></tr>
<tr><td>terminalName</td><td>查询栏输入</td><td>门店名称</td></tr>
</tbody>
</table>
<p>列表列：门店编码、门店名称、门店地址、负责人、联系电话。</p>
<p>选择后回写：storeInfo（完整对象）、storeName=terminalName、storeArea=省/市/区、storeAddress=addr。</p>
<p>查询SQL：</p>

```sql
SELECT
  t.TERMINAL_CODE    AS 门店编码,
  t.TERMINAL_NAME    AS 门店名称,
  t.ADDR             AS 门店地址,
  t.SHOPMANAGER_NAME AS 负责人,
  t.SHOPMANAGER_MOB  AS 联系电话,
  t.PROVINCE_NAME, t.PROVINCE_CODE, t.CITY_NAME, t.CITY_CODE, t.AREA_NAME, t.AREACODE
FROM MBO_TERMINAL t
WHERE (:terminalCode IS NULL OR t.TERMINAL_CODE LIKE '%' || :terminalCode || '%')
  AND (:terminalName IS NULL OR t.TERMINAL_NAME LIKE '%' || :terminalName || '%');
```
<h4>弹窗4：取消申请弹窗（列表页"取消点将"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleCancel</code>，DataSet 为 <code>cancelApplyFormDSProps</code>。弹窗内嵌取消原因表单 + ActivityApplyDetail（只读）。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
<tr><td>cancelReason</td><td>手动输入</td><td>取消原因（必填，TextArea）</td></tr>
</tbody>
</table>
<p>查询SQL：</p>

```sql
SELECT
  aa.APPLY_CODE    AS 申请编码,
  TO_CHAR(aa.ACTIVITY_START_DATE, 'YYYY-MM-DD') AS 活动开始时间,
  aa.APPROVAL_STATE AS 审核状态,
  aa.ORDER_LECTURE_STATE AS 点将状态,
  ROUND((aa.ACTIVITY_START_DATE - SYSDATE)) AS 距开始天数
FROM TRAIN_APPLY aa
WHERE aa.APPLY_CODE = :applyCode
  AND aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply';
```
<h4>弹窗5：开始执行弹窗（列表页"开始执行"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleStartExecute</code> + <code>stores/listConfig.tsx</code> 的 <code>startExecuteFormDSProps</code>。校验 orderLectureState='valid'。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
<tr><td>realStartDate</td><td>手动选择</td><td>实际开始时间（必填，DatePicker）</td></tr>
<tr><td>confirmPerson</td><td>系统生成</td><td>确认人（disabled，placeholder="提交后生成"）</td></tr>
<tr><td>confirmDate</td><td>系统生成</td><td>确认时间（disabled，placeholder="提交后生成"）</td></tr>
<tr><td>startRemark</td><td>手动输入</td><td>开始备注（TextArea）</td></tr>
</tbody>
</table>
<p>提交接口：<code>activityApplyApi.startExecute</code>，data=&#123; startRemark, realStartDate(YYYY-MM-DD), applyCode &#125;。</p>
<h4>弹窗6：确认结束弹窗（列表页"确认结束"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleConfirmEnd</code> + <code>stores/listConfig.tsx</code> 的 <code>confirmEndFormDSProps</code>。校验 endConfirmState='to_be_confirm'。beforeOpen 时调用 <code>activityApplyApi.detail</code> 加载详情回填。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
<tr><td>realStartDate</td><td>详情回填</td><td>实际开始时间（TextField，disabled）</td></tr>
<tr><td>realEndDate</td><td>详情回填</td><td>实际结束时间（TextField，disabled）</td></tr>
<tr><td>endExecuteUserName</td><td>详情回填</td><td>发起人（TextField，disabled）</td></tr>
<tr><td>startTime</td><td>默认当前日期</td><td>发起时间（TextField，disabled）</td></tr>
<tr><td>endExecuteRemark</td><td>详情回填</td><td>备注（TextArea，disabled）</td></tr>
</tbody>
</table>
<p>提交接口：<code>activityApplyApi.confirmEndExecute</code>，params=&#123; applyCode, endConfirmState &#125;。</p>
<ul><li>onOk（确认）：endConfirmState='confirm'</li><li>onCancel（驳回）：endConfirmState='reject'</li></ul>
<h4>弹窗7：流程摘要弹窗（列表行"查看流程"按钮触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleShowProcess</code>，组件 <code>components/ProcessDetail</code>，入参 applyCode。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>列表选中行</td><td>申请编码</td></tr>
</tbody>
</table>
<p>查询SQL：</p>

```sql
SELECT *
FROM TRAIN_APPLY aa
WHERE aa.APPLY_CODE = :applyCode
  AND aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply';
```
<h4>弹窗8：查看日程弹窗（申请点将弹窗行操作"查看日程"触发）</h4>
<blockquote>数据来源：<code>list.tsx</code> 的 <code>handleShowSchedule</code>，组件 <code>base/lecturerProfile/components/LecturerCalendar</code>，入参 rowData + haveLecturerCode=true。无确定按钮，cancelText='关闭'。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>rowData</td><td>讲师列表选中行</td><td>讲师信息（含 lecturerCode）</td></tr>
<tr><td>haveLecturerCode</td><td>固定值 true</td><td>标识已有讲师编码</td></tr>
</tbody>
</table>
<h4>弹窗9：特殊取消弹窗（详情页"特殊取消"按钮触发）</h4>
<blockquote>数据来源：<code>detail.tsx</code> 的 <code>handleCancel</code>，DataSet 为 <code>cancelApplyFormDSProps</code>。仅 state.isCrm=true 时显示按钮。弹窗内嵌取消原因表单。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>路由参数</td><td>申请编码</td></tr>
<tr><td>cancelReason</td><td>手动输入</td><td>取消原因（必填，TextArea）</td></tr>
</tbody>
</table>
<p>提交接口：<code>activityApplyApi.specialCancel</code>，data=&#123; cancelReason, applyCode &#125;。</p>
<h4>弹窗10：按计划结算弹窗（详情页"按计划结算"按钮触发）</h4>
<blockquote>数据来源：<code>detail.tsx</code> 的 <code>handleSettlement</code>，组件 <code>activityGeneral/activityGeneralManage/components/PlanSettlementInfo</code> + ActivityApplyDetail（只读）。仅 state.isCrm=true 时显示按钮。</blockquote>
<p>入参：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>参数名</th><th>来源</th><th>说明</th></tr>
</thead>
<tbody>
<tr><td>applyCode</td><td>路由参数</td><td>申请编码</td></tr>
<tr><td>saleTarget</td><td>PlanSettlementInfo</td><td>销售目标</td></tr>
<tr><td>collectionTarget</td><td>PlanSettlementInfo</td><td>回款目标</td></tr>
<tr><td>orderTarget</td><td>PlanSettlementInfo</td><td>订单目标</td></tr>
<tr><td>saleComplete</td><td>PlanSettlementInfo</td><td>销售完成</td></tr>
<tr><td>collectionComplete</td><td>PlanSettlementInfo</td><td>回款完成</td></tr>
<tr><td>orderComplete</td><td>PlanSettlementInfo</td><td>订单完成</td></tr>
<tr><td>completeRate</td><td>PlanSettlementInfo</td><td>完成率</td></tr>
<tr><td>realStartDate</td><td>表单</td><td>实际开始时间</td></tr>
<tr><td>realEndDate</td><td>表单</td><td>实际结束时间</td></tr>
<tr><td>lecturerPrice</td><td>表单</td><td>讲师点将费</td></tr>
<tr><td>settlementAmount</td><td>表单</td><td>点将费小计</td></tr>
<tr><td>signerId/signerName/signerPhone</td><td>签订人</td><td>签订人信息</td></tr>
</tbody>
</table>
<p>onDetail 回调将详情中的目标/完成数据写入 PlanSettlementInfo 组件。onOk 校验签订人，提交接口 <code>activityApplyApi.settleForPlan</code>。</p>
</KbCard>

<KbCard title="其他按钮">
<blockquote>数据来源：<code>list.tsx</code>（列表页头部+行操作）和 <code>detail.tsx</code>（详情页头部）。权限前缀 <code>hzero.general_manage.activity.activity_general_manage.ps.</code>。</blockquote>
<table class="kb-field-tbl">
<thead>
<tr><th>序号</th><th>按钮名称</th><th>所在位置</th><th>显隐条件</th><th>业务逻辑</th></tr>
</thead>
<tbody>
<tr><td>1</td><td>申请点将</td><td>列表页头部</td><td>始终显示</td><td>弹出讲师选择弹窗，选择讲师后跳转详情页新建</td></tr>
<tr><td>2</td><td>申请确认</td><td>列表页头部</td><td>始终显示</td><td>校验FDD签署中状态，调用FDD签合同</td></tr>
<tr><td>3</td><td>结算确认</td><td>列表页头部</td><td>始终显示</td><td>校验待确认结算状态，调用FDD签结算合同</td></tr>
<tr><td>4</td><td>开始执行</td><td>列表页头部</td><td>始终显示</td><td>校验valid状态，弹窗填写开始信息</td></tr>
<tr><td>5</td><td>确认结束</td><td>列表页头部</td><td>始终显示</td><td>校验待确认状态，弹窗确认或驳回</td></tr>
<tr><td>6</td><td>取消点将</td><td>列表页头部</td><td>始终显示</td><td>校验提前7天且已签约且未结束，弹窗填写取消原因</td></tr>
<tr><td>7</td><td>编辑再发起</td><td>列表行</td><td>草稿或驳回状态</td><td>跳转详情页编辑模式</td></tr>
<tr><td>8</td><td>查看流程</td><td>列表行</td><td>始终显示</td><td>弹出流程摘要组件</td></tr>
<tr><td>9</td><td>删除</td><td>列表行</td><td>草稿或驳回状态</td><td>确认后调用delete接口</td></tr>
<tr><td>10</td><td>保存</td><td>详情页</td><td>新增/编辑模式</td><td>校验后调用dealerSave接口</td></tr>
<tr><td>11</td><td>保存并提交</td><td>详情页</td><td>新增/编辑模式</td><td>校验后调用dealerSaveAndSubmit接口</td></tr>
<tr><td>12</td><td>查看结算确认</td><td>详情页</td><td>state.isCrm或isExecute</td><td>查询结算确认PDF并新窗口打开</td></tr>
<tr><td>13</td><td>查看确认书</td><td>详情页</td><td>state.isCrm或isExecute</td><td>查询申请确认书PDF并新窗口打开</td></tr>
<tr><td>14</td><td>特殊取消</td><td>详情页</td><td>state.isCrm</td><td>弹窗填写取消原因，调用specialCancel接口</td></tr>
<tr><td>15</td><td>按计划结算</td><td>详情页</td><td>state.isCrm</td><td>弹窗填写结算信息，调用settleForPlan接口</td></tr>
<tr><td>16</td><td>取消/关闭</td><td>详情页</td><td>始终显示</td><td>关闭当前标签页</td></tr>
</tbody>
</table>
<h4>按钮1：申请点将（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;apply</code></li><li><strong>业务逻辑</strong>：弹出"申请点将"弹窗，展示讲师列表（applyTableDS），支持按讲师姓名、讲师级别查询。选择讲师后 <code>openTab</code> 跳转详情页 <code>/detail/null/add</code>，state携带讲师信息。</li></ul>
<h4>按钮2：申请确认（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;apply_confirm</code></li><li><strong>业务逻辑</strong>：校验选中1行且 approvalState='fdd_in_approval'，调用 <code>useFDD</code> 钩子的 <code>handleOpenFDD</code>。否则提示"只有FDD签署中的申请单才可以签合同"。</li></ul>
<h4>按钮3：结算确认（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;setlement_confirm</code></li><li><strong>业务逻辑</strong>：校验选中1行且 settlementConfirmState='to_be_confirm'，调用 <code>useFDD</code> 钩子的 <code>handleOpenFDD</code>。否则提示"只有待确认的申请单才可以签结算合同"。</li></ul>
<h4>按钮4：开始执行（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;start_execute</code></li><li><strong>业务逻辑</strong>：校验选中1行且 orderLectureState='valid'，弹出开始执行弹窗。确认后调用 <code>activityApplyApi.startExecute</code>，data=&#123; startRemark, realStartDate(YYYY-MM-DD), applyCode &#125;。</li><li><strong>校验</strong>：非valid状态提示"请选择有效状态的点将数据"。</li></ul>
<h4>按钮5：确认结束（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;confirm_end</code></li><li><strong>业务逻辑</strong>：校验选中1行且 endConfirmState='to_be_confirm'，弹出确认结束弹窗。onOk调用 <code>confirmEndExecute</code>（endConfirmState='confirm'），onCancel调用 <code>confirmEndExecute</code>（endConfirmState='reject'）。</li><li><strong>校验</strong>：非待确认状态提示"只有结束确认状态为待确认的申请才能进行此操作"。</li></ul>
<h4>按钮6：取消点将（列表页头部）</h4>
<ul><li><strong>显隐条件</strong>：始终显示，权限码 <code>$&#123;permissionPrefix&#125;cancel_apply</code></li><li><strong>业务逻辑</strong>：校验选中1行，时间校验 timeDiff=(activityStartDate-nowTime)/(24*60*60*1000) &gt;= 7，状态校验 approvalState='fdd_sign' 且 (cancelApprovalState为空/reject/oa_reject) 且 orderLectureState!='end'。通过后弹出取消申请弹窗，调用 <code>activityApplyApi.cancelApply</code>。</li><li><strong>校验</strong>：不满足提前7天提示"需提前7天且已生效的单据才可以发起申请取消！"；状态不符提示"该状态单据无法发起取消申请！"。</li></ul>
<h4>按钮7：编辑再发起（列表行）</h4>
<ul><li><strong>显隐条件</strong>：orderLectureState='draft' 或 approvalState 为 reject/oa_reject（注意：活动点将不含fdd_reject）</li><li><strong>业务逻辑</strong>：跳转详情页 <code>/detail/&#123;applyCode&#125;/edit</code>。</li><li><strong>校验</strong>：状态非草稿且非驳回时提示"当前状态数据无法编辑！"。</li></ul>
<h4>按钮8：查看流程（列表行）</h4>
<ul><li><strong>显隐条件</strong>：始终显示</li><li><strong>业务逻辑</strong>：弹出流程摘要弹窗，渲染 <code>ProcessDetail</code> 组件，入参 applyCode。</li></ul>
<h4>按钮9：删除（列表行）</h4>
<ul><li><strong>显隐条件</strong>：orderLectureState='draft' 或 approvalState 为 reject/oa_reject</li><li><strong>业务逻辑</strong>：确认弹窗"确定要删除当前数据吗？"，确认后调用 <code>activityApplyApi.delete</code>（URL追加ordLectureActivityApplyId），成功后刷新列表。</li></ul>
<h4>按钮10：保存（详情页）</h4>
<ul><li><strong>显隐条件</strong>：新增/编辑模式（!isDetail），权限码 <code>$&#123;permissionPrefix&#125;save</code></li><li><strong>业务逻辑</strong>：依次校验 generalFormDS、activityInfoFormDS。合并数据：preDate拆为preStartDate/preEndDate，activityDate拆为activityStartDate/activityEndDate，opCenter拆为opCenterCode/opCenterName，storeInfo拆为省市区/门店编码名称。调用 <code>activityApplyApi.dealerSave</code>，成功后关闭标签页。</li></ul>
<h4>按钮11：保存并提交（详情页）</h4>
<ul><li><strong>显隐条件</strong>：新增/编辑模式（!isDetail），权限码 <code>$&#123;permissionPrefix&#125;save_submit</code></li><li><strong>业务逻辑</strong>：校验同保存，调用 <code>activityApplyApi.dealerSaveAndSubmit</code>，触发activityApplyApproval审批流，成功后关闭标签页。</li></ul>
<h4>按钮12：查看结算确认（详情页）</h4>
<ul><li><strong>显隐条件</strong>：state.isCrm 或 state.isExecute，权限码 <code>$&#123;permissionPrefix&#125;show_settlement_confirm</code></li><li><strong>业务逻辑</strong>：调用 <code>generalCommonApi.queryPDF</code>，data=&#123; applyCode, applyTypeOne='activity', applyTypeTwo='settle' &#125;。返回URL则 <code>window.open</code>，否则提示"未查询到对应的确认书"。</li></ul>
<h4>按钮13：查看确认书（详情页）</h4>
<ul><li><strong>显隐条件</strong>：state.isCrm 或 state.isExecute，权限码 <code>$&#123;permissionPrefix&#125;show_confirm</code></li><li><strong>业务逻辑</strong>：调用 <code>generalCommonApi.queryPDF</code>，data=&#123; applyCode, applyTypeOne='activity', applyTypeTwo='apply' &#125;。返回URL则 <code>window.open</code>，否则提示"未查询到对应的确认书"。</li></ul>
<h4>按钮14：特殊取消（详情页）</h4>
<ul><li><strong>显隐条件</strong>：state.isCrm，权限码 <code>$&#123;permissionPrefix&#125;cancel</code></li><li><strong>业务逻辑</strong>：弹出特殊取消弹窗，填写cancelReason（必填），调用 <code>activityApplyApi.specialCancel</code>，data=&#123; cancelReason, applyCode &#125;。</li></ul>
<h4>按钮15：按计划结算（详情页）</h4>
<ul><li><strong>显隐条件</strong>：state.isCrm，权限码 <code>$&#123;permissionPrefix&#125;settlement</code></li><li><strong>业务逻辑</strong>：弹出按计划结算弹窗，包含 PlanSettlementInfo 组件（目标/完成数据）+ ActivityApplyDetail（只读）。onOk 校验签订人后，调用 <code>activityApplyApi.settleForPlan</code>，data含目标/完成数据、实际时间、点将费、签订人信息。</li></ul>
<h4>按钮16：取消/关闭（详情页）</h4>
<ul><li><strong>显隐条件</strong>：始终显示</li><li><strong>业务逻辑</strong>：调用 <code>closeTab(tabUrl)</code> 关闭当前标签页。新增/编辑模式显示"取消"，查看模式显示"关闭"。</li></ul>
</KbCard>

<KbCard title="自动计算逻辑">
<table class="kb-field-tbl">
<thead>
<tr><th>计算项</th><th>计算公式</th><th>触发时机</th><th>数据来源</th></tr>
</thead>
<tbody>
<tr><td>点将费小计</td><td>settlementAmount = (preDate[1] - preDate[0] + 1) * lecturerPrice</td><td>拟点将时间 onChange</td><td>`ActivityApplyDetail/index.tsx` 的 `computedSettlementAmount`</td></tr>
<tr><td>门店地址</td><td>storeArea = provinceName + '/' + cityName + '/' + areaName</td><td>选择门店弹窗确认</td><td>`handleShowStoreList` 的 onOk</td></tr>
<tr><td>门店详细地址</td><td>storeAddress = storeInfo.addr</td><td>选择门店弹窗确认</td><td>`handleShowStoreList` 的 onOk</td></tr>
<tr><td>开票账户</td><td>legalEntityName/legalEntityCode</td><td>选择交易公司弹窗确认</td><td>`handleShowCompanyList` 的 onOk</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="保存/提交校验">
<blockquote>数据来源：<code>detail.tsx</code> 的 <code>handleSave</code> + <code>stores/detailConfig.tsx</code> 字段 required 配置。保存与保存并提交共用同一校验逻辑，依次校验 generalFormDS 和 activityInfoFormDS。</blockquote>
<p><strong>校验1：</strong>拟点将时间必填 —— generalFormDS.preDate（range），明确点将时间安排</p>
<p><strong>校验2：</strong>签订人必填 —— generalFormDS.signer（haveSigner=true时），明确合同签订人</p>
<p><strong>校验3：</strong>所属运营中心必填 —— activityInfoFormDS.opCenter</p>
<p><strong>校验4：</strong>交易公司必填 —— activityInfoFormDS.tradingCompanyName</p>
<p><strong>校验5：</strong>开票账户必填 —— activityInfoFormDS.legalEntityName（选交易公司自动带出）</p>
<p><strong>校验6：</strong>活动类型必填 —— activityInfoFormDS.activityType，值集MBO.ORD_ACTIVITY_TYPE</p>
<p><strong>校验7：</strong>活动名称必填 —— activityInfoFormDS.activityName</p>
<p><strong>校验8：</strong>门店名称必填 —— activityInfoFormDS.storeName</p>
<p><strong>校验9：</strong>活动区域必填 —— activityInfoFormDS.activityArea</p>
<p><strong>校验10：</strong>门店地址必填 —— activityInfoFormDS.storeArea（选门店自动带出）</p>
<p><strong>校验11：</strong>活动时间必填 —— activityInfoFormDS.activityDate（range）</p>
<p><strong>校验12：</strong>参与门店数必填 —— activityInfoFormDS.applyStoreNumber</p>
<p><strong>校验13：</strong>投资预算金额必填 —— activityInfoFormDS.budgetAmount</p>
<p><strong>校验14：</strong>活动简介必填 —— activityInfoFormDS.activityIntroduction</p>
<p><strong>校验15：</strong>取消申请时取消原因必填 —— cancelFormDS.cancelReason</p>
<p><strong>校验16：</strong>开始执行时实际开始时间必填 —— startExecuteFormDS.realStartDate</p>
<p><strong>校验17：</strong>按计划结算时签订人必填 —— generalFormDS.signer</p>
<p><strong>校验18：</strong>讲师排期冲突校验（后端） —— 避免同一讲师同一时间段被重复点将</p>
</KbCard>

<KbCard title="状态机">
<p>流转图：</p>

```sql
draft(新建) → submitted(已提交) → approved(审批通过) → valid(生效)
   → executing(执行中) → end(已完成)
rejected(审批驳回) → 可修改重提
valid → cancel_applied(取消申请中) → cancel_approved(已取消)
```
<p>状态列表：</p>
<table class="kb-field-tbl">
<thead>
<tr><th>当前状态</th><th>触发动作</th><th>目标状态</th></tr>
</thead>
<tbody>
<tr><td>draft</td><td>保存并提交</td><td>submitted</td></tr>
<tr><td>submitted</td><td>审批通过</td><td>approved</td></tr>
<tr><td>submitted</td><td>审批驳回</td><td>rejected</td></tr>
<tr><td>rejected</td><td>修改后重新提交</td><td>submitted</td></tr>
<tr><td>approved</td><td>生效</td><td>valid</td></tr>
<tr><td>valid</td><td>开始执行</td><td>executing</td></tr>
<tr><td>executing</td><td>结束执行</td><td>end</td></tr>
<tr><td>valid</td><td>发起取消申请</td><td>cancel_applied</td></tr>
<tr><td>cancel_applied</td><td>取消审批通过</td><td>cancel_approved</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="导入">
<p>不支持导入功能。</p>
</KbCard>

<KbCard title="活动点将申请表（TRAIN_APPLY）">
<table class="kb-field-tbl">
<thead>
<tr><th>字段名</th><th>类型</th><th>释义</th><th>对应界面字段</th><th>逻辑</th></tr>
</thead>
<tbody>
<tr><td>ID</td><td>NUMBER</td><td>主键ID</td><td>-</td><td>序列自增</td></tr>
<tr><td>APPLY_CODE</td><td>VARCHAR2(64)</td><td>申请编码</td><td>申请编码</td><td>PK，系统生成</td></tr>
<tr><td>APPLY_TYPE_ONE</td><td>VARCHAR2(30)</td><td>申请类型一</td><td>-</td><td>固定值'activity'</td></tr>
<tr><td>APPLY_TYPE_TWO</td><td>VARCHAR2(30)</td><td>申请类型二</td><td>-</td><td>固定值'apply'</td></tr>
<tr><td>ACTIVITY_NAME</td><td>VARCHAR2(255)</td><td>活动名称</td><td>活动名称</td><td>申请时填写，NOT NULL</td></tr>
<tr><td>ACTIVITY_THEME</td><td>VARCHAR2(500)</td><td>活动主题</td><td>活动主题</td><td>申请时填写</td></tr>
<tr><td>LECTURER_CODE</td><td>VARCHAR2(64)</td><td>讲师编码</td><td>-</td><td>关联讲师主档</td></tr>
<tr><td>LECTURER_NAME</td><td>VARCHAR2(255)</td><td>被点将人</td><td>培训师</td><td>选择讲师时写入</td></tr>
<tr><td>PLANNER_CODE</td><td>VARCHAR2(64)</td><td>策划师编码</td><td>-</td><td>关联策划师主档</td></tr>
<tr><td>PLANNER_NAME</td><td>VARCHAR2(255)</td><td>策划师姓名</td><td>-</td><td>关联策划师主档</td></tr>
<tr><td>DEALER_CODE</td><td>VARCHAR2(64)</td><td>经销商编码</td><td>-</td><td>当前登录经销商</td></tr>
<tr><td>DEALER_NAME</td><td>VARCHAR2(255)</td><td>经销商名称</td><td>经销商</td><td>当前登录经销商</td></tr>
<tr><td>TERMINAL_CODE</td><td>VARCHAR2(64)</td><td>门店编码</td><td>-</td><td>关联门店主档</td></tr>
<tr><td>TERMINAL_NAME</td><td>VARCHAR2(255)</td><td>门店名称</td><td>门店</td><td>关联门店主档</td></tr>
<tr><td>DISTRIBUTOR_NAME</td><td>VARCHAR2(255)</td><td>申请人</td><td>-</td><td>经销商发起时写入</td></tr>
<tr><td>ORDER_LECTURE_STATE</td><td>VARCHAR2(32)</td><td>点将状态</td><td>点将状态</td><td>值集MBO.ORDER_LECTURE_STATE</td></tr>
<tr><td>APPROVAL_STATE</td><td>VARCHAR2(32)</td><td>审核状态</td><td>审核状态</td><td>值集MBO.APPLY_APPROVAL_STATE</td></tr>
<tr><td>APPLY_STATUS</td><td>VARCHAR2(32)</td><td>申请状态</td><td>-</td><td>申请时设置</td></tr>
<tr><td>CANCEL_APPROVAL_STATE</td><td>VARCHAR2(32)</td><td>取消审核状态</td><td>-</td><td>值集MBO.CANCEL_APPROVAL_STATE</td></tr>
<tr><td>SETTLEMENT_CONFIRM_STATE</td><td>VARCHAR2(32)</td><td>结算确认状态</td><td>-</td><td>值集MBO.SETTLEMENT_COMFIRM_STATE</td></tr>
<tr><td>END_CONFIRM_STATE</td><td>VARCHAR2(32)</td><td>结束确认状态</td><td>结束确认状态</td><td>值集MBO.END_COMFIRM_STATE</td></tr>
<tr><td>ACTIVITY_START_DATE</td><td>DATE</td><td>活动开始时间</td><td>开始时间</td><td>申请时填写</td></tr>
<tr><td>ACTIVITY_END_DATE</td><td>DATE</td><td>活动结束时间</td><td>结束时间</td><td>申请时填写</td></tr>
<tr><td>PRE_ORD_LECTURER_DAYS</td><td>NUMBER</td><td>拟点将天数</td><td>拟点将天数</td><td>申请时填写</td></tr>
<tr><td>REAL_ORD_LECTURER_DAYS</td><td>NUMBER</td><td>实际点将天数</td><td>实际点将天数</td><td>执行环节填写</td></tr>
<tr><td>CANCEL_REASON</td><td>VARCHAR2(2000)</td><td>取消原因</td><td>-</td><td>取消点将时填写</td></tr>
<tr><td>CRM_ORDER_CODE</td><td>VARCHAR2(64)</td><td>CRM单号</td><td>CRM单号</td><td>推送CRM后回写</td></tr>
<tr><td>REMARK</td><td>VARCHAR2(2000)</td><td>备注</td><td>备注</td><td>手动输入</td></tr>
<tr><td>CREATE_DATE</td><td>TIMESTAMP</td><td>申请时间</td><td>申请时间</td><td>系统自动</td></tr>
<tr><td>UPDATE_DATE</td><td>TIMESTAMP</td><td>更新时间</td><td>-</td><td>系统自动</td></tr>
<tr><td>CREATED_BY</td><td>VARCHAR2(64)</td><td>创建人</td><td>-</td><td>系统自动</td></tr>
<tr><td>CREATION_DATE</td><td>DATE</td><td>创建时间</td><td>-</td><td>系统自动</td></tr>
<tr><td>LAST_UPDATED_BY</td><td>VARCHAR2(64)</td><td>最后更新人</td><td>-</td><td>系统自动</td></tr>
<tr><td>LAST_UPDATE_DATE</td><td>DATE</td><td>最后更新时间</td><td>-</td><td>系统自动</td></tr>
</tbody>
</table>
</KbCard>

<KbCard title="排查SQL">

```sql
-- 查询经销商活动点将申请
SELECT
  aa.APPLY_CODE    AS 申请编码,
  aa.ACTIVITY_NAME AS 活动名称,
  aa.LECTURER_NAME AS 培训师,
  aa.ORDER_LECTURE_STATE AS 点将状态,
  aa.APPROVAL_STATE AS 审核状态,
  aa.SETTLEMENT_CONFIRM_STATE AS 结算确认状态,
  aa.PRE_ORD_LECTURER_DAYS AS 拟点将天数,
  aa.REAL_ORD_LECTURER_DAYS AS 实际点将天数
FROM TRAIN_APPLY aa
WHERE aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply'
ORDER BY aa.CREATE_DATE DESC;

-- 查询可取消的活动点将（提前7天且已签约且未结束）
SELECT
  aa.APPLY_CODE    AS 申请编码,
  TO_CHAR(aa.ACTIVITY_START_DATE, 'YYYY-MM-DD') AS 活动开始时间,
  ROUND((aa.ACTIVITY_START_DATE - SYSDATE)) AS 距开始天数
FROM TRAIN_APPLY aa
WHERE aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply'
  AND aa.APPROVAL_STATE = 'fdd_sign'
  AND aa.ORDER_LECTURE_STATE <> 'end'
  AND aa.ACTIVITY_START_DATE > SYSDATE + 7
  AND (aa.CANCEL_APPROVAL_STATE IS NULL
       OR aa.CANCEL_APPROVAL_STATE IN ('reject', 'oa_reject'));

-- 查询讲师排期冲突
SELECT
  aa.APPLY_CODE    AS 申请编码,
  aa.LECTURER_NAME AS 培训师,
  TO_CHAR(aa.ACTIVITY_START_DATE, 'YYYY-MM-DD') AS 开始时间,
  TO_CHAR(aa.ACTIVITY_END_DATE, 'YYYY-MM-DD') AS 结束时间
FROM TRAIN_APPLY aa
WHERE aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply'
  AND aa.LECTURER_CODE = :lecturerCode
  AND aa.ORDER_LECTURE_STATE IN ('valid', 'executing')
  AND aa.ACTIVITY_START_DATE <= :activityEndDate
  AND aa.ACTIVITY_END_DATE >= :activityStartDate;

-- 查询待结算确认的申请
SELECT
  aa.APPLY_CODE    AS 申请编码,
  aa.ACTIVITY_NAME AS 活动名称,
  aa.SETTLEMENT_CONFIRM_STATE AS 结算确认状态
FROM TRAIN_APPLY aa
WHERE aa.APPLY_TYPE_ONE = 'activity'
  AND aa.APPLY_TYPE_TWO = 'apply'
  AND aa.SETTLEMENT_CONFIRM_STATE = 'to_be_confirm';
```
</KbCard>

</div>
</div>
</div>

<div id="faq" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="报错一览表">
<table class="kb-field-tbl">
<thead>
<tr><th>报错信息</th><th>提示节点</th><th>根因与解决方案</th><th>等级</th><th>详细逻辑</th></tr>
</thead>
<tbody>
<tr><td>请选择一条数据</td><td>操作按钮前置校验</td><td>未选择或选择多行；选择一条数据后重试</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-1" class="view-btn">查看</a></td></tr>
<tr><td>需提前7天且已生效的单据才可以发起申请取消！</td><td>取消申请按钮</td><td>不满足提前7天；确认activityStartDate与当前时间差值&gt;=7</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-2" class="view-btn">查看</a></td></tr>
<tr><td>该状态单据无法发起取消申请！</td><td>取消申请按钮</td><td>状态校验不通过；确认审批状态和点将状态</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-3" class="view-btn">查看</a></td></tr>
<tr><td>当前状态数据无法编辑！</td><td>编辑按钮</td><td>状态非草稿且非驳回；确认申请状态</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-4" class="view-btn">查看</a></td></tr>
<tr><td>经销商不能为空</td><td>保存提交</td><td>未选择经销商；选择经销商后保存</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-5" class="view-btn">查看</a></td></tr>
<tr><td>门店不能为空</td><td>保存提交</td><td>未选择门店；选择门店后保存</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-6" class="view-btn">查看</a></td></tr>
<tr><td>讲师不能为空</td><td>保存提交</td><td>未选择讲师；选择讲师后保存</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-7" class="view-btn">查看</a></td></tr>
<tr><td>活动名称不能为空</td><td>保存提交</td><td>未填写活动名称；填写后保存</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-8" class="view-btn">查看</a></td></tr>
<tr><td>拟点将天数不能为空</td><td>保存提交</td><td>未填写拟点将天数；填写后保存</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-9" class="view-btn">查看</a></td></tr>
<tr><td>讲师排期冲突</td><td>保存提交</td><td>所选讲师在时间段内已有生效点将；更换讲师或调整时间</td><td>提示</td><td style="text-align:center;"><a href="#err-detail-10" class="view-btn">查看</a></td></tr>
<tr><td>请求失败</td><td>接口调用</td><td>后端服务异常；检查后端服务状态</td><td>错误</td><td style="text-align:center;"><a href="#err-detail-11" class="view-btn">查看</a></td></tr>
<tr><td>网络异常/接口超时</td><td>任意接口调用</td><td>网络中断或接口响应超时，检查网络及后端超时配置</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-12" class="view-btn">查看</a></td></tr>
<tr><td>权限不足</td><td>点击操作按钮</td><td>当前用户无对应按钮权限码，联系管理员授权</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-13" class="view-btn">查看</a></td></tr>
<tr><td>数据不存在</td><td>查看/编辑/删除</td><td>申请编码不存在或已删除，检查 APPLY_CODE 有效性</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-14" class="view-btn">查看</a></td></tr>
<tr><td>状态不允许操作</td><td>取消申请/结束确认</td><td>申请状态不在允许操作的状态范围内，检查 ORDER_LECTURE_STATE</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-15" class="view-btn">查看</a></td></tr>
<tr><td>值集数据不显示</td><td>下拉选项</td><td>值集 MBO.ORDER_LECTURE_STATE 等未配置，检查值集配置</td><td>toast提醒</td><td style="text-align:center;"><a href="#err-detail-16" class="view-btn">查看</a></td></tr>
<tr><td>活动时间范围校验失败</td><td>保存提交</td><td>开始时间大于结束时间，检查时间范围选择</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-17" class="view-btn">查看</a></td></tr>
<tr><td>拟点将天数必须大于0</td><td>保存提交</td><td>拟点将天数为0或负数，检查 PRE_ORD_LECTURER_DAYS</td><td>阻断性报错</td><td style="text-align:center;"><a href="#err-detail-18" class="view-btn">查看</a></td></tr>
</tbody>
</table>
<div id="err-detail-1" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>请选择一条数据</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击编辑、删除、取消申请、查看流程等行操作按钮时，未选择数据或选择了多行<br><strong>逻辑分析：</strong>前端在执行单选操作前校验选中行数量，若 selectedRows.length ≠ 1 则阻止操作并提示"请选择一条数据"。单选操作需要明确的目标申请，未选择时无法确定操作对象，多选时操作对象不唯一</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         DISTRIBUTOR_NAME AS 申请人,
         ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态,
         APPROVAL_STATE AS 审核状态
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
  ORDER BY CREATE_DATE DESC;
--</code></pre></div>
</div>


<div id="err-detail-2" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>需提前7天且已生效的单据才可以发起申请取消！</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击取消申请按钮时，不满足"提前7天"条件<br><strong>逻辑分析：</strong>前端计算时间差值 timeDiff = (ACTIVITY_START_DATE - nowTime) / (24*60*60*1000)，校验 timeDiff &gt;= 7（活动开始前至少7天）。校验不通过则提示"需提前7天且已生效的单据才可以发起申请取消！"。此校验确保有充足时间通知讲师和门店调整安排，避免临时取消造成资源浪费</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         TO_CHAR(ACTIVITY_START_DATE,'YYYY-MM-DD') AS 活动开始时间,
         APPROVAL_STATE AS 审核状态,
         ROUND(ACTIVITY_START_DATE - SYSDATE) AS 距开始天数
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND ACTIVITY_START_DATE &lt; SYSDATE + 7;
--</code></pre></div>
</div>


<div id="err-detail-3" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>该状态单据无法发起取消申请！</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击取消申请按钮时，审批状态不为 fdd_sign 或点将状态为 end 或已有进行中的取消申请<br><strong>逻辑分析：</strong>前端执行多重校验：①校验 APPROVAL_STATE = 'fdd_sign'（已法大大签约）；②校验 ORDER_LECTURE_STATE ≠ 'end'（未结束）；③校验 CANCEL_APPROVAL_STATE 为空或为 reject/oa_reject（未取消或已驳回）。任一校验不通过则提示"该状态单据无法发起取消申请！"。确保仅已签约生效且未结束、未重复取消的单据可发起取消</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         APPROVAL_STATE AS 审核状态,
         ORDER_LECTURE_STATE AS 点将状态,
         CANCEL_APPROVAL_STATE AS 取消审核状态
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (APPROVAL_STATE &lt;&gt; 'fdd_sign'
         OR ORDER_LECTURE_STATE = 'end'
         OR (CANCEL_APPROVAL_STATE IS NOT NULL
             AND CANCEL_APPROVAL_STATE NOT IN ('reject', 'oa_reject')));
--</code></pre></div>
</div>


<div id="err-detail-4" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>当前状态数据无法编辑！</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击编辑按钮时，状态非草稿且非各类驳回状态<br><strong>逻辑分析：</strong>前端校验 ORDER_LECTURE_STATE = 'draft'（草稿）或 APPROVAL_STATE 为 reject/oa_reject/fdd_reject（各种驳回），任一条件满足才允许编辑。已生效（valid）、审批中（approving）等状态的申请已被下游引用，编辑可能影响数据一致性，故限制编辑。校验不通过提示"当前状态数据无法编辑！"</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态,
         APPROVAL_STATE AS 审核状态
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND ORDER_LECTURE_STATE &lt;&gt; 'draft'
    AND APPROVAL_STATE NOT IN ('reject', 'oa_reject', 'fdd_reject');
--</code></pre></div>
</div>


<div id="err-detail-5" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>经销商不能为空</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，dealerName 字段为空<br><strong>逻辑分析：</strong>前端表单对 dealerName 字段配置 required 校验，提交前校验经销商是否选择，为空则阻止提交并提示"经销商不能为空"。经销商是点将申请的发起主体，必须明确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         DEALER_CODE AS 经销商编码,
         DEALER_NAME AS 经销商名称
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (DEALER_NAME IS NULL OR DEALER_CODE IS NULL);
--</code></pre></div>
</div>


<div id="err-detail-6" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>门店不能为空</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，terminalName 字段为空<br><strong>逻辑分析：</strong>前端表单对 terminalName 字段配置 required 校验，提交前校验门店是否选择，为空则阻止提交并提示"门店不能为空"。门店是活动落地位置，必须明确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         TERMINAL_CODE AS 门店编码,
         TERMINAL_NAME AS 门店名称
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (TERMINAL_NAME IS NULL OR TERMINAL_CODE IS NULL);
--</code></pre></div>
</div>


<div id="err-detail-7" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>讲师不能为空</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，lecturerName 字段为空<br><strong>逻辑分析：</strong>前端表单对 lecturerName 字段配置 required 校验，提交前校验讲师是否选择，为空则阻止提交并提示"讲师不能为空"。讲师是点将的核心对象，必须明确被点将人</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         LECTURER_CODE AS 讲师编码,
         LECTURER_NAME AS 讲师姓名
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (LECTURER_NAME IS NULL OR LECTURER_CODE IS NULL);
--</code></pre></div>
</div>


<div id="err-detail-8" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>活动名称不能为空</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，activityName 字段为空<br><strong>逻辑分析：</strong>前端表单对 activityName 字段配置 required 校验，提交前校验活动名称是否填写，为空则阻止提交并提示"活动名称不能为空"。活动名称用于标识本次活动，必须明确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (ACTIVITY_NAME IS NULL OR ACTIVITY_NAME = '');
--</code></pre></div>
</div>


<div id="err-detail-9" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>拟点将天数不能为空</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，preOrdLecturerDays 字段为空<br><strong>逻辑分析：</strong>前端表单对 preOrdLecturerDays 字段配置 required 校验，提交前校验拟点将天数是否填写，为空则阻止提交并提示"拟点将天数不能为空"。拟点将天数用于费用计算与讲师排期，必须明确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         PRE_ORD_LECTURER_DAYS AS 拟点将天数
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND PRE_ORD_LECTURER_DAYS IS NULL;
--</code></pre></div>
</div>


<div id="err-detail-10" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>讲师排期冲突</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，所选讲师在拟定时间段内已有生效点将记录<br><strong>逻辑分析：</strong>后端保存前校验讲师排期冲突，查询 TRAIN_APPLY 表中同一讲师（LECTURER_CODE 相同）且状态为 valid 或 executing 的点将记录，判断新申请的 [ACTIVITY_START_DATE, ACTIVITY_END_DATE] 与已有记录的时间段是否存在交集。若存在交集则提示"讲师排期冲突"并阻止保存。此校验避免同一讲师在同一时间段被重复点将，保障讲师档期合理性</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT a.APPLY_CODE AS 申请1编码,
         b.APPLY_CODE AS 申请2编码,
         a.LECTURER_NAME AS 培训师,
         TO_CHAR(a.ACTIVITY_START_DATE,'YYYY-MM-DD') AS 申请1开始,
         TO_CHAR(a.ACTIVITY_END_DATE,'YYYY-MM-DD') AS 申请1结束,
         TO_CHAR(b.ACTIVITY_START_DATE,'YYYY-MM-DD') AS 申请2开始,
         TO_CHAR(b.ACTIVITY_END_DATE,'YYYY-MM-DD') AS 申请2结束
  FROM TRAIN_APPLY a
  JOIN TRAIN_APPLY b ON a.APPLY_CODE &lt; b.APPLY_CODE
    AND a.LECTURER_CODE = b.LECTURER_CODE
    AND a.APPLY_TYPE_ONE = 'activity' AND a.APPLY_TYPE_TWO = 'apply'
    AND b.APPLY_TYPE_ONE = 'activity' AND b.APPLY_TYPE_TWO = 'apply'
    AND a.ORDER_LECTURE_STATE IN ('valid', 'executing')
    AND b.ORDER_LECTURE_STATE IN ('valid', 'executing')
    AND a.ACTIVITY_START_DATE &lt;= b.ACTIVITY_END_DATE
    AND a.ACTIVITY_END_DATE &gt;= b.ACTIVITY_START_DATE;
--</code></pre></div>
</div>


<div id="err-detail-11" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>请求失败</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>调用 mlt/activityApply/* 系列接口时，后端返回 HTTP 状态码非 2xx<br><strong>逻辑分析：</strong>前端通过 axios 调用后端接口，若响应状态码非 2xx 或网络异常则触发错误回调，统一提示"请求失败"。常见根因包括：mbo-business 微服务未启动或异常、数据库连接失败、SQL 执行超时、后端业务异常未捕获、外部系统（OA/FDD/CRM）调用失败、工作流引擎异常、网络中断等。需检查 mbo-business 微服务运行状态、外部系统连通性、工作流配置、后端日志定位具体异常堆栈</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态,
         APPROVAL_STATE AS 审核状态,
         CRM_ORDER_CODE AS CRM单号,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND LAST_UPDATE_DATE &gt;= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
--</code></pre></div>
</div>


<div id="err-detail-12" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>网络异常/接口超时</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>任意接口调用时，网络中断或接口响应超过 axios timeout 配置<br><strong>逻辑分析：</strong>前端 axios 请求未收到响应或响应超时，触发 catch 回调统一提示"请求失败"。常见根因：网络中断、mbo-business 服务假死、数据库慢查询、工作流引擎响应慢等。需检查网络连通性、后端服务负载、数据库性能</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码,
         ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态,
         TO_CHAR(LAST_UPDATE_DATE,'YYYY-MM-DD HH24:MI:SS') AS 最后更新时间
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND LAST_UPDATE_DATE &gt;= SYSDATE - 1
  ORDER BY LAST_UPDATE_DATE DESC;
--</code></pre></div>
</div>


<div id="err-detail-13" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>权限不足</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击编辑、删除、取消申请等按钮时，当前用户无对应 permissionList 权限码<br><strong>逻辑分析：</strong>前端 Button 组件通过 permissionList 配置权限码，HZERO 框架校验当前用户角色是否包含该权限码，未包含则按钮不可见或禁用。若强制调用接口，后端也会校验权限返回403。需联系管理员配置对应角色权限</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT U.USER_NAME AS 用户名, R.ROLE_NAME AS 角色名, P.PERMISSION_CODE AS 权限码
  FROM SYS_USER U
  LEFT JOIN SYS_USER_ROLE UR ON U.USER_ID = UR.USER_ID
  LEFT JOIN SYS_ROLE R ON UR.ROLE_ID = R.ROLE_ID
  LEFT JOIN SYS_ROLE_PERMISSION RP ON R.ROLE_ID = RP.ROLE_ID
  LEFT JOIN SYS_PERMISSION P ON RP.PERMISSION_ID = P.PERMISSION_ID
  WHERE P.PERMISSION_CODE LIKE '%activity_general%' ORDER BY U.USER_NAME;
--</code></pre></div>
</div>


<div id="err-detail-14" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>数据不存在</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>查看、编辑、删除等操作时，接口返回数据为空或申请编码不存在<br><strong>逻辑分析：</strong>前端通过 applyCode 调用详情接口，后端查询 TRAIN_APPLY 表无对应记录或记录已逻辑删除，返回空数据。常见根因：申请编码错误、申请已被删除、跨租户查询、数据权限隔离等。需检查 APPLY_CODE 有效性及数据权限</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码, DISTRIBUTOR_NAME AS 申请人,
         ORDER_LECTURE_STATE AS 点将状态, DELETE_FLAG AS 删除标记
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND (DELETE_FLAG = 'Y' OR APPLY_CODE IS NULL);
--</code></pre></div>
</div>


<div id="err-detail-15" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>状态不允许操作</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>点击取消申请、结束确认等按钮时，申请状态不在允许操作的状态范围内<br><strong>逻辑分析：</strong>后端校验申请状态机，如取消申请要求 approvalState 为 fdd_sign 且 orderLectureState 非 end、结束确认要求状态为执行中等。状态不匹配时后端返回业务异常，前端提示后端返回的 message。需检查申请当前状态及操作流程</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码, ACTIVITY_NAME AS 活动名称,
         ORDER_LECTURE_STATE AS 点将状态, APPROVAL_STATE AS 审核状态,
         CANCEL_APPROVAL_STATE AS 取消审核状态, ERROR_INFO AS 异常问题
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND ORDER_LECTURE_STATE NOT IN ('draft','valid','executing','finished')
  ORDER BY CREATE_DATE DESC;
--</code></pre></div>
</div>


<div id="err-detail-16" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>值集数据不显示</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>查询条件或列表中点将状态、审核状态等下拉选项为空<br><strong>逻辑分析：</strong>前端通过 lookupCode 查询值集 MBO.ORDER_LECTURE_STATE、MBO.APPLY_APPROVAL_STATE 等，值集未配置或未启用则下拉选项为空。需在值集管理页面配置对应值集</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT LOOKUP_CODE AS 值集编码, LOOKUP_VALUE_CODE AS 值编码,
         LOOKUP_VALUE_NAME AS 值名称, ENABLE_FLAG AS 启用标记
  FROM SYS_LOOKUP_VALUE
  WHERE LOOKUP_CODE IN ('MBO.ORDER_LECTURE_STATE','MBO.APPLY_APPROVAL_STATE')
    AND ENABLE_FLAG = 'N' ORDER BY LOOKUP_CODE;
--</code></pre></div>
</div>


<div id="err-detail-17" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>活动时间范围校验失败</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，ACTIVITY_START_DATE &gt; ACTIVITY_END_DATE<br><strong>逻辑分析：</strong>前端校验活动时间范围合法性，若开始时间大于结束时间则阻止提交。需检查时间范围选择是否正确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码, ACTIVITY_NAME AS 活动名称,
         TO_CHAR(ACTIVITY_START_DATE,'YYYY-MM-DD') AS 活动开始时间,
         TO_CHAR(ACTIVITY_END_DATE,'YYYY-MM-DD') AS 活动结束时间
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND ACTIVITY_START_DATE &gt; ACTIVITY_END_DATE;
--</code></pre></div>
</div>


<div id="err-detail-18" class="error-detail-overlay">
  <div class="error-detail-box" v-pre>
    <a href="#" class="close-btn">&times;</a>
    <h4><span style="color:#7C3AED;">报错：</span>拟点将天数必须大于0</h4>
    <h5>详细逻辑</h5>
    <div class="detail-text" v-pre><strong>触发条件：</strong>保存或保存并提交时，PRE_ORD_LECTURER_DAYS 字段非正数<br><strong>逻辑分析：</strong>后端校验 PRE_ORD_LECTURER_DAYS &gt; 0，拟点将天数用于费用计算与讲师排期，必须为正数。若为0或负数则返回业务异常。需检查拟点将天数输入是否正确</div>
      <h5>排查SQL</h5>
    <pre class="detail-sql language-sql" v-pre><code>SELECT APPLY_CODE AS 申请编码, ACTIVITY_NAME AS 活动名称,
         PRE_ORD_LECTURER_DAYS AS 拟点将天数
  FROM TRAIN_APPLY
  WHERE APPLY_TYPE_ONE = 'activity' AND APPLY_TYPE_TWO = 'apply'
    AND PRE_ORD_LECTURER_DAYS IS NOT NULL AND PRE_ORD_LECTURER_DAYS &lt;= 0;
--</code></pre></div>
</div>


</KbCard>

<KbCard title="常见问题">
<p><strong>Q1：经销商看不到自己发起的活动点将记录？</strong></p>
<p>A：检查该经销商是否正确关联当前登录用户，确认DEALER_CODE与当前登录用户关联的经销商一致。</p>
<p><strong>Q2：详情页type参数有什么作用？</strong></p>
<p>A：<code>type</code>参数用于区分详情页的展示模式，不同模式可能展示不同的操作按钮和字段。</p>
<p><strong>Q3：活动点将状态未更新？</strong></p>
<p>A：状态由审批流程驱动，需联系审批人确认审批进度。</p>
<p><strong>Q4：申请点将弹窗无讲师数据？</strong></p>
<p>A：检查campApi.lecturerList接口，pageType=activity参数是否正确传递。</p>
<p><strong>Q5：取消点将校验不通过？</strong></p>
<p>A：检查activityStartDate时间差值、approvalState是否为fdd_sign、orderLectureState是否非end。</p>
</KbCard>

</div>
</div>
</div>

<div id="changelog" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="更新记录">
<table class="kb-field-tbl">
<thead>
<tr><th>日期</th><th>提交ID</th><th>提交人</th><th>提交内容</th></tr>
</thead>
<tbody>
<tr><td>2025-11-12</td><td>-</td><td>hfy</td><td>初始创建</td></tr>
<tr><td>2026-08-03</td><td>-</td><td>AI</td><td>初始创建</td></tr>
<tr><td>2026-08-28</td><td>-</td><td>AI</td><td>完整重写，补充业务流程、界面模块、数据库表、FAQ等</td></tr>
<tr><td>2026-07-31</td><td>-</td><td>AI</td><td>对比网站补充定义、详细逻辑、数据库表字段等</td></tr>
<tr><td>2026-08-30</td><td>-</td><td>AI</td><td>按skill规范格式重写，调整为经销商发起视角，规范上下游、重点逻辑、弹窗、按钮、校验、状态机、更新记录等格式</td></tr>
</tbody>
</table>
</KbCard>
</div>
</div>
</div>

<div id="history" style="display:none;">
<div class="tab-pad">
<div class="kl-wrap">
<KbCard title="历史排查记录">

<!-- 空白:待补充 -->

</KbCard>
</div>
</div>
</div>