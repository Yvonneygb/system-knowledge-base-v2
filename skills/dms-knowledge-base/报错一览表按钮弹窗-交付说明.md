# 报错一览表「查看」按钮 + 弹窗改造 · 交付说明

## 一、本次改动(页面)

**目标页**：家装管理 / 家装往来 / 家装核销发票上传 →【常见问题】TAB →「报错一览表」

| 需求 | 实现 | 说明 |
|------|------|------|
| 详细逻辑列「查看」改为按钮 | 每行的「查看」由裸文本 `[查看]` 改为紫色按钮 `<a href="#err-detail-N" class="view-btn">查看</a>` | 复用全局 `.view-btn` 样式(紫色 `#7C3AED`) |
| 点击弹窗展示对应详细逻辑 | 表格下方放置 20 个 `.error-detail-overlay` 弹窗,纯 CSS `:target` 触发(`#err-detail-N` → `display:flex`),`.close-btn`(×)关闭 | 参考【家装真实性核销】,无需 JS |
| 参考家装真实性核销 | 弹窗结构/样式(`.error-detail-overlay`/`.error-detail-box`/`.close-btn`/`.detail-text`/`.detail-tip`)全部复用其全局 CSS | 与参考页一致 |

**额外清理**：移除了原本冗余的「报错详细逻辑」卡片（与一览表的 6 条详细逻辑重复，且与新弹窗机制重复）。

**数据情况**：
- 20 条报错中,**6 条已有详细逻辑**（含编号步骤，其中 4 条带排查 SQL：行 2/7/16/17），已完整转入对应弹窗。
- 其余 **14 条暂无详细逻辑**，弹窗内以「(详细逻辑细则待补充；以下为表格中『根因与解决方案』供参考：)」+ 表格「根因与解决方案」内容兜底，保证每个按钮都可点开、有内容（未编造逻辑）。

**等级徽标**：`toast提醒` 紫色、`阻断性报错` 红色小标签。

## 二、同步更新的规范与 skill

| 文件 | 改动要点 |
|------|----------|
| `UI风格规范.md` | 新增 **§7.7 报错一览表 + 「查看」弹窗(FAQ)**：5 列表结构、`.view-btn` 按钮、`.error-detail-overlay` 纯 CSS `:target` 弹窗结构、等级徽标、未编写详细逻辑的兜底写法 |
| `skills/dms-knowledge-base/SKILL.md` | 新增「报错一览表 + 查看弹窗特殊处理」段（参考家装真实性核销）；§3 增加「报错一览表」为 HTML 化例外二；禁止事项末条补充该例外 |

两份已复制到 `/workspace` 并重新打包进 `dms-kb-bundle.zip`。

## 三、重新拉取工程

- 文件：`/workspace/system-knowledge-base-local.zip`
- 大小：约 232 KB
- 校验 sha256：`aced6f248c2454a43bde895d494e3f5b48605912a634d472d0f1b55bbaf00f1f`

**下载方式**（沿用本地拼接方案，规避代理对 .zip 的拦截）：打开预览站点 → 点右上角「⬇️ 下载工程(zip)」→ 浏览器本地拼回 zip。

> 预览/下载入口（沙箱 8000 被其他进程占用，走 9000）：
> `https://webview.e2b.sh4.sandbox.cloudstudio.club/?x-cs-sandbox-id=5c17a24db3ca43e6ba9ae6cf496fb4f3&x-cs-sandbox-port=9000&restart=true`
> 目标页：`/system-knowledge-base/家装管理/家装往来/家装核销发票上传/` → 【常见问题】TAB

本机运行：`cd system-knowledge-base && npm install && $env:PORT=9000; npm run dev`，访问 `http://127.0.0.1:9000/system-knowledge-base/家装管理/家装往来/家装核销发票上传/`。

## 四、验证记录

- `npm run build`：通过
- 产物：FAQ 区 `view-btn`×20、`error-detail-overlay`×20、无「报错详细逻辑」冗余卡片
- 线上 9000 服务：dl.html 新 sha 匹配、FAQ 按钮/弹窗正常、分片可拉取
