# 家装核销发票上传 · 字段表改造 + UI规范/SKILL 同步

## 一、本次改动(页面)

**目标页**：家装管理 / 家装往来 / 家装核销发票上传 →「详细逻辑」TAB

| 需求 | 实现方式 | 现状 |
|------|----------|------|
| 1. 数据库列名放最后一列 | 6 张字段表列序重排为 `字段名→组件→业务释义→显隐条件→取值/赋值逻辑→合法值→数据库列名` | ✅ |
| 2. 合法值列宽减半 | 列宽由 `<colgroup>` 精确控制：合法值 `21% → 12%`；其余列 `13/9/17/12/21/16%` | ✅ |
| 3. 超宽显示横向滚动条 | 外层 `.kb-field-scroll`(`overflow-x:auto`) + `.kb-field-tbl`(`table-layout:fixed`、`min-width:960px`)；表格超出卡片宽度时在卡片内滚动，不撑破页面 | ✅ |

涉及文件：
- `docs/家装管理/家装往来/家装核销发票上传/index.md`（6 张字段表由 Markdown 管道表转写为 HTML 表）
- `docs/.vitepress/theme/custom.css`（新增 `.kb-page-content .kb-field-scroll` / `.kb-field-tbl` 样式，用 `.kb-page-content` 前缀压过通用表格的 `auto`+`nowrap` 规则）

> 说明：通用表格（如选择弹窗的入参 4 列表）仍保持原样 `auto + nowrap`，本次仅对「详细逻辑」TAB 的 7 列字段清单表做例外处理。

## 二、同步更新的规范与 skill

| 文件 | 改动要点 |
|------|----------|
| `UI风格规范.md` | §7.3 标注适用范围（通用表格）；**新增 §7.6** 字段清单表例外规则（列序固定、列宽、`table-layout:fixed`、单元格可换行、`min-width:960px` 卡片内横向滚动）；§8 第 6 条加例外说明 |
| `skills/dms-knowledge-base/SKILL.md` | §3「表格严格原样」补充字段清单表例外（可转 HTML 表、数据库列名置末、合法值列宽减半、内容逐字保留）；禁止事项末条加例外；description 同步 |

两份文件已复制到 `/workspace/UI风格规范.md` 与 `/workspace/skills/dms-knowledge-base/SKILL.md`，并已重新打包进 `dms-kb-bundle.zip`。

## 三、重新拉取工程

工程源码包已重新生成（含上述全部改动）：

- 文件：`/workspace/system-knowledge-base-local.zip`
- 大小：约 231 KB（已排除 `node_modules` / `docs/.vitepress/dist`）
- 校验 sha256：`0def681a858185f5be648b1219503cf1ba30a296e87657a710ad4131691ac531`

**下载方式**（沙箱 webview 代理禁 `.zip` 直下，沿用本地拼接方案）：

1. 打开预览站点（站点首页已注入右上角「⬇️ 下载工程(zip)」按钮）：
   `https://webview.e2b.sh4.sandbox.cloudstudio.club/?x-cs-sandbox-id=5c17a24db3ca43e6ba9ae6cf496fb4f3&x-cs-sandbox-port=9000&restart=true`
2. 点右上角按钮 → 浏览器本地把 32 个分片拼回 zip 并下载；可比对上面的 sha256 校验完整性。
3. 解压后本机运行：
   ```powershell
   cd system-knowledge-base
   npm install
   $env:PORT=9000; npm run dev
   ```
   访问 `http://127.0.0.1:9000/system-knowledge-base/家装管理/家装往来/家装核销发票上传/`

## 四、验证记录

- `npm run build`：构建通过（vitepress v1.0.0-alpha.28，3.3s）
- 产物校验：目标页含 `kb-field-tbl`×6、`kb-field-scroll`×6，表头含「数据库列名」；CSS 产物含 `.kb-page-content .kb-field-tbl{table-layout:fixed...}` 规则
- 线上 9000 服务验证：下载页、32 个分片、首页下载按钮均正常返回
