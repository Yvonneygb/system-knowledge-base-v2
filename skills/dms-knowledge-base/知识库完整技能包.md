# DMS 知识库 · 完整技能包（Skill Bundle）

> 本文档汇总「系统知识库」项目涉及的全部操作规范与技能，作为知识库的**维护手册**与 **AI 协作标准**。
> 覆盖：技术架构、页面区块结构、业务介绍规范、流程图规范、各区块规范、源MD管理、历史修复规范、操作 SOP、测试与校验。

---

## 一、技术架构总览

### 1.1 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 静态生成 | **VitePress 1.0.0-alpha.28** (SSG) | `npm run build` → `docs/.vitepress/dist` |
| 布局 | 自定义 `KbLayout.vue` | 实际导航由 `primaryNav/secondaryNav/resolvePanel` 渲染（config.ts 的 nav/sidebar 仅作默认主题兜底） |
| 组件系统 | `Kb*` 设计系统 (`theme/ui/index.js`) | `kl-card / kl-tip / kl-table` 等，`kbUiPlugin` 注册 |
| 特殊组件 | `KbSectionTabs` / `ValueSetTabs` / `BreadcrumbTabs` / `ServiceFeeFlow` | 菜单页 Tab 切换与特殊布局 |
| 图标 | Font Awesome 6.4 CDN + 线性 SVG | 动态加载 |
| 代码高亮 | PrismJS (CDN) | SQL 支持 |
| 后端 | `qa-server.js` (Express) | AI 问答 + 源MD上传 |
| 部署 | GitHub Pages + Actions | push master 自动构建发布 |

### 1.2 关键入口文件

| 文件 | 角色 |
|------|------|
| `docs/.vitepress/config.ts` | 站点配置（导航兜底） |
| `docs/.vitepress/theme/index.js` | 组件注册 + 环境变量注入 |
| `docs/.vitepress/theme/KbLayout.vue` | 实际页面布局 |
| `docs/.vitepress/theme/custom.css` | 全部业务介绍/流程图的样式类 |
| `qa-server.js` | 后端服务 |

---

## 二、页面区块结构（核心 Skill）

### 2.1 标准区块顺序（一个菜单页 `index.md`）

```
<BreadcrumbTabs />                          ← 面包屑/页头
<div id="biz-intro" style="display:none;">  ← 业务介绍（人工整理）
<div id="biz-flow" style="display:none;">   ← 业务流程图（人工整理）
<div id="key-logic" style="display:none;">  ← 重点逻辑（源码分析）
<div id="permission" style="display:none;"> ← 权限（源码分析）
<div id="detail-logic" style="display:none;">← 界面逻辑（源码分析）
<div id="faq" style="display:none;">        ← 常见问题（源码分析）
<div id="faq-qa" style="display:none;">     ← FAQ问答对（源码分析）
<div id="changelog" style="display:none;">  ← 更新记录（源码分析）
<div id="history" style="display:none;">    ← 历史（源码分析）
```

> 区块顺序**必须**保持：`biz-intro → biz-flow → key-logic → permission → detail-logic → faq → faq-qa → changelog → history`

### 2.2 区块属性矩阵

| 区块 | id | 归属 | 内容 | 是否可被源MD更新 |
|------|----|------|------|:---:|
| 业务介绍 | `biz-intro` | **人工整理** | 业务是什么/为什么/怎么流转 | ❌ 保留 |
| 业务流程图 | `biz-flow` | **人工整理** | 跨系统业务链路 | ❌ 保留 |
| 重点逻辑 | `key-logic` | 源码分析 | 表单细节/编码规则/接口字段 | ✅ 替换 |
| 权限 | `permission` | 源码分析 | 角色/按钮权限 | ✅ 替换 |
| 界面逻辑 | `detail-logic` | 源码分析 | 界面字段说明 | ✅ 替换 |
| FAQ | `faq` / `faq-qa` | 源码分析 | 常见问题 | ✅ 替换 |
| 更新记录 | `changelog` | 源码分析 | 版本更新 | ✅ 替换 |
| 历史 | `history` | 源码分析 | 历史记录 | ✅ 替换 |

---

## 三、业务介绍（biz-intro）规范

### 3.1 强制容器结构

```
biz-intro → tab-pad → kl-wrap → kl-card × N
```

🔴 禁止：`KbHero` / `biz-hero` 横幅头部、内容散落在 biz-intro 外、用裸 KbCard 平铺代替。

### 3.2 内容卡片（kl-card + biz-kl-hdr）

```html
<div class="kl-card">
  <div class="biz-kl-hdr">
    <span class="biz-tag" style="background:rgba(124,58,237,0.08);color:#7C3AED;border-color:rgba(124,58,237,0.18);">背景与动机</span>
    <h2>为何要做真实性核销</h2>
    <p>防止跨渠道套利，保障集团利润空间与市场稳定</p>
  </div>
  <div class="biz-3col">...</div>
</div>
```

### 3.3 内容布局组件库（biz-intro 内可用的样式类）

| 组件类 | 用途 | 关键结构 |
|--------|------|---------|
| `.biz-3col` | 并列要点（3列） | `grid-template-columns:repeat(3,1fr)` |
| `.biz-2col-inner` | 本质/对比（2列内联） | `1fr 1fr` |
| `.biz-steps` / `.biz-step-item` | 水平步骤流程（全流程/链路） | 44×44圆 + 箭头，3~5步 |
| `.biz-tl` / `.biz-tl-item` | 竖向时间轴（政策演进/历史） | 左侧竖线+圆点 |
| `.biz-roles` / `.biz-role` | 角色卡（谁在做） | `border-left:3px solid` |
| `.biz-formula` | 强调公式条（核心计算/规则） | 深色底 `#1E293B` |
| `.kl-tip` | 提示条（原则/结论/约束） | 业务原则，禁代码细节 |
| `.kl-badge` | 表格列内标签 | `.red/.orange/.green/.yellow` |
| `.kl-table` | 标准/明细表格 | 表头浅紫 `#F5F3FF` |

### 3.4 图标规范

- 16×16 viewBox 线性 SVG，`stroke="white" stroke-width="1.5"`
- 置于渐变圆角方块内；尺寸：要点 36×36、列头 40×40、步骤圆 44×44、事件 28×28

### 3.5 书写风格铁律

1. **先讲业务动机，再讲机制**：为什么存在 → 具体做什么
2. **面向业务人员与实施顾问**，用业务术语
3. **禁止代码级细节**：`isHome=2`、`AE.JZ_INVOICE_VERIFER_NO`、工作流标识、接口/字段/表名、常量一律**不出现**，归入 key-logic
4. 图文结合（图标卡/步骤条/表格/时间轴）
5. 克制篇幅：单功能页卡片 ≤3
6. 数据准确（写具体数值，如 85%、¥10,000、120天）
7. 层次清晰：标题用 `biz-kl-hdr`，正文用 h4/h5

### 3.6 组织建议（按业务域量级）

| 页面类型 | 卡片建议 |
|---------|---------|
| 业务域级大页 | 为何做→做什么→怎么做→谁做→依据→红线→演进（多卡片叙事） |
| 单据类页 | 业务是什么 / 谁在用·流程 / 关键规则（1~3卡片） |
| 查询/报表类 | 查什么 / 怎么用 |
| 配置/基础数据类 | 维护什么 / 影响范围 |

---

## 四、业务流程图（biz-flow）规范

### 4.1 强制外层结构

```
tab-pad → bf-truth-flow → (bf-main-title / bf-main-sub / bf-fc-svg-wrap > bf-fc-svg / bf-fc-legend)
```

🔴 禁止用 `kl-wrap` / 裸 `KbCard` 包 SVG，禁止把 `bf-fc-svg`/`bf-fc-legend` 放到 `bf-fc-svg-wrap` 之外。

### 4.2 SVG 节点组件

| 节点类型 | 样式 |
|---------|------|
| 开始/结束 | 虚线圆角矩形 `stroke-dasharray="5,3"`，紫边 `#9333EA` |
| 普通流程节点 | 实线圆角矩形 |
| 核心高亮节点 | 实心填充圆角矩形（★ 标记） |
| 判断/分支节点 | 菱形（⚖ 标记） |
| 分支标签 | 小实心标签 |
| 分支虚线外框 | 分组容器 |

### 4.3 连接线铁律

- **只允许水平（H）和垂直（V）线段**，禁止斜线
- 判断节点 → 两个并列分支卡片：强制 **T 形垂直分叉线**
- 箭头用 `<defs>` 定义 marker

### 4.4 色彩体系

| 语义 | 颜色 |
|------|------|
| 主流程步骤 | 绿 |
| 开始/结束/判断 | 紫 `#7C3AED` |
| 上游支撑服务 | 蓝 |
| 审批拒绝/驳回 | 红 `#EF4444` |

### 4.5 节点粒度原则（最高优先级铁律）⭐⭐⭐

> **🔴 一个节点 = 一种业务单据 / 一种业务模式 / 一个独立模块，绝不是一个操作步骤。**

**三问判断标准：**
| 问题 | 能/有/需要 → 可作节点 | 不能/没有/不需要 → 必须合并 |
|------|------------------|------------------|
| 能作为一个独立菜单吗？ | 能 | 不能 |
| 有独立的单据号吗？ | 有 | 没有 |
| 上下游系统需要感知吗？ | 需要 | 不需要 |

**错误示例（禁止）：**
```
❌ 新建上传单 → 选择核销类型 → 选择项目 → 上传附件 → OCR → 录入 → 保存 → 提交审批
   ↑ 全是"新建X单"这一张单据内部的表单步骤，必须合并为一个核心节点 ★新建X单★
```

**正确示例：**
```
✅ [开始] → 项目报备 → 项目合同 → 订单 → 出库 → ★核销发票上传★ → 真实性核销 → [结束]
```

**判断菱形**只用于影响后续业务走向的关键决策（审批是否通过、业务模式分支）；表单校验逻辑归入「重点逻辑」。

**绘制前检查清单：**
- [ ] 每个节点对应一种单据/模块？
- [ ] 没有仅描述"怎么操作界面"的节点？
- [ ] 判断菱形代表业务走向决策？
- [ ] 主线节点数 5~9 个？

### 4.6 布局模式

- **上下布局（推荐）**：上游正上方 / 下游正下方
- **对齐三区布局（备选）**：上游/下游左右对称
- **分支并行**：条件分支流程图

---

## 五、其他区块规范（源码分析区块）

### 5.1 key-logic（重点逻辑）
- 承载：表单细节、编码规则、接口/字段/表名、常量、模块标识
- 与业务介绍**不重复**——业务介绍讲"业务是什么"，key-logic 讲"系统怎么实现"

### 5.2 permission（权限）
- 角色、按钮级权限、数据范围

### 5.3 detail-logic（界面逻辑）
- 界面字段说明、控件规则

### 5.4 faq / faq-qa（常见问题）
- 业务/使用常见问题，问答对形式
- **防止泄漏**：FAQ 内容必须包裹在 `faq` 区块内，不能出现在其他 tab 下方

### 5.5 changelog（更新记录）
- 版本更新日志

### 5.6 history（历史）
- 历史记录，含排查记录

---

## 六、源MD管理规范

### 6.1 交互模式（两段式）

| 动作 | 含义 | 连后端 |
|------|------|:---:|
| **上传** | 载入 MD 到浏览器本地（暂存+localStorage 持久化） | ❌ |
| **更新** | 调后端 `POST /api/upload-md` 写回 GitHub | ✅ |

### 6.2 行内管理

```
[菜单行] 📄 菜单名 一级/二级/三级  [上传]                ← 未上传
[菜单行] 📄 菜单名 一级/二级/三级  [📄文件名] [🔄更新] [✕] [更新说明]  ← 已上传
            ↑ 点击文件名 → window.open 新窗口预览 MD 渲染
```

### 6.3 批量上传命名规范
- 文件名必须为 `一级-二级-三级.md`（`-` 分隔）
- 匹配：`split('-')` 3段 → `一级/二级/三级` → 查 Map

### 6.4 localStorage 持久化
- Key: `smm_loaded_files_v1`，存 `{ files: {path:{fileName,content}}, notes }`
- 上传成功 / 输入说明 / 移除 时保存；onMounted 时恢复

### 6.5 区块级合并规则（后端）
```
SOURCE_SECTIONS = ['key-logic','detail-logic','permission','faq','faq-qa','changelog','history']  // 替换
PROTECTED_SECTIONS = ['biz-intro','biz-flow']  // 保留
```
- 从后往前替换源码区块，`biz-flow` 后追加新区块
- `checkDivBalance()` 校验 div 平衡，否则 400

---

## 七、历史修复规范（从 fix_*.md 提炼）

### 7.1 常见问题模式与修复原则

| 历史问题 | 根因 | 修复原则 |
|---------|------|---------|
| 关联依赖 TAB 空白 | 跨 section 的 div 标签错配（mis-nesting） | 严格保持 div 开闭平衡，用 `checkDivBalance` 校验 |
| 其他常见问题出现在每个 TAB 下方 | FAQ 内容泄漏到区块外 | FAQ 必须完整包裹在 `faq` 区块内 |
| 表格结构错误 | table 标签嵌套/缺失 | 表格必须 `kl-table` + 外层圆角容器 |
| 需要插入【更新记录】tab | 缺少 changelog 区块 | 在 faq-qa 后、history 前插入 changelog |
| 删除/调整 tab | 结构调整 | 遵循区块顺序铁律 |

### 7.2 修复记录文档格式

每次修复建立独立 md：`fix_主题_YYYYMMDD_HHMM.md`
```markdown
# 家装真实性核销知识库 - 修复记录
## 日期：YYYY-MM-DD HH:MM - HH:MM
## 目标
## 根因
## 操作
## 校验
```

---

## 八、操作 SOP

### 8.1 人工整理菜单页
1. 按标准区块结构创建 `docs/一级/二级/三级/index.md`
2. 先写 `biz-intro`（业务介绍，见§三）
3. 再写 `biz-flow`（流程图，见§四）
4. 源码分析区块由源MD管理自动更新，人工不写
5. 构建校验（见§九）

### 8.2 用源MD管理更新页面
1. 进入源MD管理页 → 填「🔑 更新密钥」
2. 点菜单行「上传」→ 弹窗选 `一级-二级-三级.md`
3. 点击文件名 → 新窗口预览确认
4. 点「🔄 更新」→ 后端合并 + 写回 GitHub + Actions 自动发布
5. ~1分钟线上更新

### 8.3 批量更新
1. 准备多个 `一级-二级-三级.md`
2. 批量上传区多选 → 逐个/全部「更新」

---

## 九、构建与校验（Skill）

### 9.1 本地开发
```bash
VITE_UPLOAD_API_URL=http://localhost:3456 npm run dev:hmr
```

### 9.2 构建
```bash
npx vitepress build docs
```

### 9.3 硬性校验清单（每个页面）
- [ ] `biz-intro → tab-pad → kl-wrap` 三层嵌套存在，kl-card 全在 kl-wrap 内
- [ ] 所有 `<div>` 开闭平衡（含内联 style 里的 div）
- [ ] SVG 图标 `<svg>` 内部无空行；整块无裸 `<`
- [ ] `biz-tag` 语义色正确（违规类用红）
- [ ] 表格均用 `.kl-table` 且外层包裹圆角容器
- [ ] 不新增未定义的 CSS 类（只用本文档列出的类）
- [ ] 不修改 `biz-flow` / `key-logic` 等其它区块与 frontmatter
- [ ] 区块顺序符合铁律

### 9.4 流程图绘制前校验
- 节点粒度三问
- 连接线只有 H/V
- 判断菱形是业务走向决策
- 主线节点 5~9 个

---

## 十、环境变量

```bash
# 源MD管理（qa-server.js）
KB_REPO=Yvonneygb/system-knowledge-base
KB_BRANCH=master
KB_GITHUB_TOKEN=ghp_xxx          # 有写权限 PAT
KB_UPLOAD_SECRET=your-secret     # 前端上传密钥

# 前端注入（构建时）
VITE_UPLOAD_API_URL=https://your-app.up.railway.app
VITE_QA_API_URL=https://your-app.up.railway.app

# 服务
PORT=3456
NODE_ENV=production
```

---

## 十一、关键源码文件索引

| 文件 | 角色 |
|------|------|
| `docs/.vitepress/theme/KbLayout.vue` | 页面布局 |
| `docs/.vitepress/theme/SourceMdManager.vue` | 源MD管理前端 |
| `docs/.vitepress/theme/index.js` | 组件注册 + 环境注入 |
| `docs/.vitepress/theme/custom.css` | 全部样式类 |
| `docs/.vitepress/theme/ui/index.js` | Kb* 设计系统 |
| `docs/.vitepress/theme/KbSectionTabs.vue` | Tab 切换组件 |
| `qa-server.js` | 后端（AI问答 + 上传） |
| `docs/dms-style.md` | 流程图规范（完整版） |
| `docs/dms-biz-intro-style.md` | 业务介绍规范（完整版） |

---

## 十二、快速速查

| 场景 | 用哪个 Skill | 看哪里 |
|------|-------------|--------|
| 写业务介绍 | 业务介绍规范 | §三 / dms-biz-intro-style.md |
| 画业务流程图 | 流程图规范 | §四 / dms-style.md |
| 判断节点粒度 | 节点粒度三问 | §4.5 |
| 更新页面源码区块 | 源MD管理 | §六 |
| 修 DIV 嵌套问题 | div 平衡校验 | §9.3 |
| 加/删 Tab | 区块顺序铁律 | §2.1 |
| 部署 | 环境变量 | §十 |

---

> **维护约定**：本技能包是知识库的唯一权威规范入口。新增/修改任何区块或交互时，务必同步更新本文档并记录到提交信息。
